# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/install-state.gz

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# planning artifacts (keep in git but exclude from build)
# .planning/

# firecrawl cache
.firecrawl/

# playwright mcp
.playwright-mcp/
*.png

# preserve probe screenshots (provenance for AI tool drift audits)
!.planning/claude-design-probes/**/*.png

# preserve visual baselines (DGN-03 ground truth for AES-04 per-phase pixel-diff)
!.planning/visual-baselines/**/*.png

# lighthouse advisory audit trails (gitignored per plan 35-03)
.planning/phases/35-performance-launch-gate/launch-gate-*.json

# vitest coverage output
/coverage
storybook-static

# Ephemeral Reports & Test State
report*.json
report*.html
test-results/

# Claude Code runtime locks
.claude/scheduled_tasks.lock

# Handoff scratch — per-session cc-* handoff artifacts (keep STATUS.md tracked)
.handoffs/cc-*.md
.handoffs/cc-*.ctx

# Session logs — auto-generated planner run logs
.planning/logs/*.md

