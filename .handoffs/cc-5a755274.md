# Handoff — cc-5a755274 [unset] — 2026-04-19

## Where we stopped
Hero perf refactor complete. 5 commits landed on `main`. Prod server on :3100 stopped. Visuals pixel-compared identical.

## What was done this session
- Measured baseline: prod hero `/` at **10 FPS, 125/126 frames dropped, 49 long tasks / 6062ms in 6s**. LCP ok (198ms) but main thread saturated.
- Isolated hot paths via rAF-caller instrumentation: `PointcloudRing.draw` 95ms/frame (72%), `IrisCloud.draw` 33ms/frame (24%). Together 97% of main-thread budget.
- **A1** `6a1dd1c` — IntersectionObserver pause on both canvases (off-hero 8.7 → 120.8 FPS).
- **A2** `26fcea8` — in-place Uint32 row-sort, no per-run alloc (hero 10 → 14.5 FPS).
- Skipped A3 (Path2D) and A4 (forced-reflow) with rationale — would've been marginal or visually risky.
- **B1** `7558022` — PointcloudRing workerized via OffscreenCanvas + dedicated Web Worker (hero 14.5 → 41.3 FPS).
- **B2** `be564a4` — IrisCloud workerized, plus `willReadFrequently: true` on offscreen contexts (hero 41.3 → **120.1 FPS, 0 dropped**).
- Cross-worker group coherence preserved via `sf-hero-shared-groups` BroadcastChannel; EntrySection broadcasts on every 13s re-roll, both workers subscribe.
- **Docs** `760394d` — HERO-ANIMATION.md inventory updated for the new worker architecture + changelog entry with full before/after numbers.
- New memory: `feedback_autonomous_forward_motion.md` — generalizes the "keep moving on stated defaults" pattern beyond pressure tests.

## Open questions / blockers
- **CLS 0.65** at t≈8s is unchanged by this refactor — pre-existing layout-shift bug, needs separate investigation (likely multilingual ScrambleText width change, but unverified).
- **Bundle 452 kB** vs CLAUDE.md 200 kB target — orthogonal to hero perf, separate work.
- **Dev-only quirk**: React strict-mode double-mounts the canvas; second `transferControlToOffscreen()` throws and canvas stays blank until navigation. Prod unaffected, flagged in commit messages + HERO-ANIMATION.md.
- `.planning/config.json` + two `.planning/logs/*.md` files were dirty at session start (not mine); left untouched.

## Next action
Decide if you want to tackle CLS 0.65 or bundle size next — both are now the top remaining perf items on `/`. Otherwise ship as-is.

## Git state
Branch: main
Status:
 M .planning/config.json
?? .planning/logs/2026-04-20T02-36-33-...md
?? .planning/logs/2026-04-20T03-00-03-...md
Recent commits:
760394d Docs: update hero inventory for worker architecture
be564a4 Feat: workerize IrisCloud — hero now 120 FPS, 0 dropped frames
7558022 Feat: workerize PointcloudRing via OffscreenCanvas (14.5→41.3 FPS)
26fcea8 Fix: in-place Uint32 pixel-sort, no per-run alloc (10→14.5 FPS hero)
6a1dd1c Fix: IntersectionObserver pause on hero canvases (8.7→120.8 FPS off-hero)
