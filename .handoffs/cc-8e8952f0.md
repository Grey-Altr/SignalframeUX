# Handoff — cc-8e8952f0 [design] — 2026-04-19

## Where we stopped
`components/blocks/entry-section.tsx` — hero wired with `IrisCloud` + `PointcloudRing` inside cream `drop-shadow` bloom wrapper. All three files uncommitted.

## What was done this session
- **cdb-v3-dossier branch**: yellow plate tiles (top-right 24px clip, text-left, px-4 py-1), hero font Syne→JetBrains Mono 800 upright one-line with enlarged `//` (letter-spacing -0.2em, fontSize 1.3em). Committed `5dc0ecc`, pushed.
- **Switched to main**; stashed `.handoffs/STATUS.md` + `.planning/config.json` under `stash@{0}` tagged `cdb-v3-dossier: handoff + config wip`.
- **Ported** `components/dossier/pointcloud-ring.tsx` from cdb-v3-dossier. Committed `f4471a9` (local only, not pushed).
- **Mounted ring** in `EntrySection` — behind `<h1>` (z-auto), positioned wrapper (layer 6) fix to paint above ring-vhs stack.
- **Built `IrisCloud`** — radial inward-drift pointcloud, annular disc distribution, edgeFade near pupil, per-particle random phase + speed, pupil at innerRadius=0.06.
- **Visual iteration** — cream `drop-shadow` bloom (3 stacked shadows), `trail` prop via `destination-out` fade, bidirectional horizontal pixel sort (alternating direction per absolute row), thickness decoupling on ring (`thicknessScale = canvasR * 0.42`), borderRadius/borderAlpha props added then disabled.
- **Dead ends reverted**: scoped ring VHS overlay (stacking-context trap), edgeBlur bands (offscreen-compositor explosion), polar radial/tangential sort (visually subtle + user called it "terrible" with chunky particles).
- **Perf finding**: `getImageData` inside `drop-shadow` wrapper + `ctx.filter` nesting = 0.34 FPS catastrophe. Removing `edgeBlur` restored 82 FPS while keeping bloom + sort + trails.

## Open questions / blockers
- Not committed: `entry-section.tsx` + `pointcloud-ring.tsx` modifications + new `iris-cloud.tsx`. Should probably land as one feature commit before next iteration.
- Chrome DevTools MCP disconnected mid-session — no live perf/screenshot verification available from here. User was doing visual verification on their end.
- Perf unverified after bidirectional sort + threshold 8 changes. Last measurement (82 FPS) predates these tweaks.

## Next action
Commit the three files as one feature commit: `Feat: add IrisCloud + pixel-sort bloom treatment to hero` — then user can iterate from a clean rollback point.

## Git state
Branch: main
Status:
 M components/blocks/entry-section.tsx
 M components/dossier/pointcloud-ring.tsx
?? components/dossier/iris-cloud.tsx
?? .planning/logs/
?? .handoffs/cc-*.{ctx,md} (harness state)

Recent commits:
f4471a9 Feat: port PointcloudRing from cdb-v3-dossier
6194732 Feat: add --sfx-ease-linear token; manifest v0.1.2 → v0.1.3
97b3e5c Fix: replace CSS named easings with explicit beziers / tokens (v0.1.2 §C.5)
1b1b5d7 Fix: collapse overshoot + anticipation bezier curves to canonical
a9e42b6 Fix: collapse alternate ease-out bezier variants to canonical

## Final config snapshot
```tsx
<IrisCloud count={4500} outerRadius={0.36} innerRadius={0.06}
  trail={0.04} pixelSort={0.5} sortThreshold={8} />
<PointcloudRing count={2400} radius={0.42}
  trail={0.1} pixelSort={0.33} sortThreshold={20} />
```
Wrapper: `filter: drop-shadow(0 0 4px … / 0.6) drop-shadow(0 0 12px … / 0.4) drop-shadow(0 0 28px … / 0.2)`
