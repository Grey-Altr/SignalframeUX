# Handoff — v1.7 Milestone Ratification

**Created:** 2026-04-25
**Branch:** `chore/v1.7-ratification` (you are here after `/clear`)
**Base:** `main` at `0ff2e38` (Phase 47 complete)

## Mission

Audit Phases 48–56 (the unshipped half of v1.7) against current code on `main`. Two phases (47 and 48) have already turned out to be largely shipped without requirement traceability. The hypothesis: most of v1.7 is in the same state. Produce a single ratification PR that closes whichever requirements are already met and surfaces the genuine remaining gaps.

**Don't re-implement what's shipped. Ratify reality.** Per `feedback_ratify_reality_bias.md` + `project_overdue_lockin_mode.md`.

## Method

For each requirement ID in each phase (48–56):

1. **Grep + file:line citation** to prove (or disprove) the requirement is met by current code
2. If proven: mark RATIFIED with date + file:line in `.planning/REQUIREMENTS.md`
3. If disproven: leave Pending, list as a real gap
4. Update the matrix at `.planning/REQUIREMENTS.md:344+` to match
5. Mark each phase complete in `.planning/ROADMAP.md` IFF all its requirements ratify
6. Fix the stale `44-01-PLAN.md` plan-list refs under Phases 48, 49, 50 (and any others)
7. Update the v1.7 milestone marker in `.planning/ROADMAP.md:12` once all phases close

**The defense against false-positive ratification:** every "RATIFIED" annotation must include a grep + file:line citation. If you can't grep it, you can't ratify it.

## Phases to audit (priority order — start with the cheapest wins)

### Phase 48: Intensity Bridge + Chromatic Setup — ✅ RATIFIED 2026-04-25

All 6/6 requirements ratified across 3 atomic commits:
- `b0d7a51` — Docs(48-01): ratify SIG-01..05 + VRG-01 in REQUIREMENTS
- `99e83d6` — Docs(48-01): fix stale Phase 48 plan reference in ROADMAP
- `40895ad` — Docs(48-01): summary — intensity bridge + Chromatic ratification

VRG-01 gate satisfied: `pnpm build-storybook` runs clean (verified 2026-04-25). Storybook 10.3.5 builds in ~3.3s.

See `.planning/phases/48-intensity-bridge/48-01-SUMMARY.md` for the full verification table.

**Carry-forward intel for Phases 49–55:** `global-effects.tsx:39-57` derives bonus surfaces (halftone, circuit, fx-feedback-decay, fx-displace-gain, fx-bloom-intensity, fx-glitch-rate, fx-particle-opacity) — all tier-scaled via `getQualityTier()`. **These pre-satisfy parts of Phases 51 (halftone), 52 (circuit), 54 (particles), 55 (glitch).** Each future ratification must grep against `global-effects.tsx:39-57` in addition to each phase's own consumer files.

### Phase 49: Grain + Idle Escalation + Visual Baseline — ✅ RATIFIED 2026-04-25

5/5 requirements audited. Verdict split: 1 Ratified / 4 Obsolete (mixed-verdict pattern, like Phase 47). Three atomic commits:
- `efe6aad` — Docs(49-01): ratify GRN-01 + obsolete GRN-02..04 + VRG-02 in REQUIREMENTS
- `c2294c1` — Docs(49-01): fix stale Phase 49 plan reference in ROADMAP
- `a3e1eee` — Docs(49-01): summary — grain ratification + idle escalation closure

**GRN-01 RATIFIED** — `components/layout/global-effects.tsx:36` (same line that ratified SIG-03; baseline = 0.03 at i=0).

**GRN-02 / GRN-03 OBSOLETE** — `useIdleEscalation` hook ships at `hooks/use-idle-escalation.ts:28` (commit `40e2f0d`) but the `IdleOverlay` consumer was deliberately deleted in commit `a260238 fix(cleanup): remove heavy SIGNAL effects from GlobalEffects render for performance gate` to ship PRF-02 Lighthouse Performance compliance. Hook persists as library-only / dead code. Performance launch gate supersedes idle-escalation animation. **This is the canonical "feature lost to launch gate" precedent for the rest of the v1.7 audit.**

**GRN-04 / VRG-02 OBSOLETE** — temporal "before grain changes" gates cannot be satisfied retroactively; forward-looking baseline capture remains available via VRG-01's ratified Chromatic infrastructure.

See `.planning/phases/49-grain-idle-escalation/49-01-SUMMARY.md` for the full verification table + carry-forward intel.

**Carry-forward stale doc-comment residue** — `components/layout/global-effects.tsx:165-186, 201` still contains the `IdleOverlay` JSDoc block that the `a260238` cleanup left stranded. Out of scope for ratification; flag for Phase 50 audit or a future doc-comment cleanup pass.

### Phase 50: VHS Enhancement — ✅ RATIFIED 2026-04-25

6/6 requirements audited. Verdict split: 4 Ratified / 2 Obsolete (mixed-verdict pattern, like Phases 47 + 49). Three atomic commits:
- `5b2e0e5` — Docs(50-01): ratify VHS-01/02/04/05 + obsolete VHS-03/06 in REQUIREMENTS
- `cff3923` — Docs(50-01): fix stale Phase 50 plan reference in ROADMAP
- `b856405` — Docs(50-01): summary — VHS ratification + dropout/coherence-review closure

**VHS-01 / VHS-02 / VHS-04 / VHS-05 RATIFIED** — `vhs-overlay.tsx:153-178` (chromatic-opacity derivation reading SIG-02 derived property) + `globals.css:2195-2210` (red+cyan layers), `globals.css:2213-2225` (sf-vhs-jitter steps(4, end) keyframes), `globals.css:2249-2253` (radial-gradient vignette), `globals.css:2108-2109` + `2125-2126` (paired backdrop-filter / -webkit-backdrop-filter literal values, zero var() refs).

**VHS-03 OBSOLETE** — CSS scaffold at `globals.css:2230-2246` + JSX container at `vhs-overlay.tsx:236` ship, but the runtime activator was tied to the same idle-escalation consumer cut by `a260238` for PRF-02. Same supersedence chain as Phase 49's GRN-02/03. **Second canonical "feature lost to launch gate" precedent.**

**VHS-06 OBSOLETE** — human visual coherence review process gate; same retroactive-temporal-impossibility as GRN-04 / VRG-02.

See `.planning/phases/50-vhs-enhancement/50-01-SUMMARY.md` for the full verification table + carry-forward intel.

**Carry-forward intel for Phase 51 (Halftone Texture):** the next phase's audit should grep for `--sfx-halftone-opacity` (already derived in `global-effects.tsx:39-57` per Phase 48's SIG-02 carry-forward) and search `app/globals.css` for halftone rules. HLF-* requirements may pre-satisfy at the consumer-wiring level via the ratified intensity bridge — same pattern that pre-satisfied VHS-01.

**Stale doc-comment cleanup carry-forward (still deferred):** Phase 49's `IdleOverlay` JSDoc residue at `global-effects.tsx:165-186, 201` was not touched by Phase 50's audit. Carry forward to whichever future phase happens to touch `global-effects.tsx`.

### Phase 51: Halftone Texture — ✅ RATIFIED 2026-04-25

4/4 requirements audited. Verdict split: 3 Ratified / 1 Obsolete (mixed-verdict pattern continues). Three atomic commits:
- `9f36b66` — Docs(51-01): ratify HLF-01/02/03 + obsolete HLF-04 in REQUIREMENTS
- `9fd9f85` — Docs(51-01): fix stale Phase 51 plan reference in ROADMAP
- `a8e5a52` — Docs(51-01): summary — halftone ratification + moiré-review closure

**HLF-01 RATIFIED with technique-divergence annotation** — `app/globals.css:1252-1266` ships `.sf-halftone::before` via `radial-gradient(circle, var(--sfx-halftone-dot) 1px, transparent 1px)` + `opacity: var(--sfx-halftone-opacity, 0)`. Compositing uses `mix-blend-mode: multiply` instead of the spec's `background-blend-mode` + `filter: contrast()`. Both spec'd techniques are redundant on a single 1px-circle dot layer; simpler form skips a GPU filter pass. Outcome (CSS-only halftone dot pattern) met. **First technique-divergence ratification on this branch — establishes the precedent for accepting "outcome met, technique simplified" when shipped reality is functionally equivalent and more performant.**

**HLF-02 RATIFIED** — `components/layout/global-effects.tsx:40-41` derives `--sfx-halftone-opacity` via literal `i < 0.4 ? 0 : (i - 0.4) / 0.6 * 0.15` curve. Exact spec match. **Second SIG-02 carry-forward beneficiary** after VHS-01 — pre-satisfied at the wiring level by Phase 48's ratified intensity bridge.

**HLF-03 RATIFIED** — `.sf-halftone` ambient class consumed only by 4 token-specimen blocks (`spacing-specimen.tsx:18`, `motion-specimen.tsx:49`, `color-specimen.tsx:112`, `type-specimen.tsx:27`). `hero.tsx:21` uses the `--sfx-halftone-dot` token in an inline radial-gradient but does NOT apply the ambient class — clean counter-example demonstrating token/class independence. Zero overlay leakage.

**HLF-04 OBSOLETE** — combined-view human visual review process gate cannot be satisfied retroactively. Same precedent as VHS-06 / GRN-04 / VRG-02.

See `.planning/phases/51-halftone-texture/51-01-SUMMARY.md` for the full verification table + carry-forward intel.

### Phase 52: Circuit Overlay — ✅ RATIFIED 2026-04-25

3/3 requirements audited. Verdict: 3 Ratified / 0 Obsolete — second clean all-ratified phase on this branch after Phase 48 (6/6). Three atomic commits:
- `f289e91` — Docs(52-01): ratify CIR-01/02/03 in REQUIREMENTS
- `16d3759` — Docs(52-01): fix stale Phase 52 plan reference in ROADMAP
- `844e5c3` — Docs(52-01): summary — circuit overlay clean ratification

**CIR-01 RATIFIED** — `app/globals.css:1268-1283` ships `.sf-circuit::after` (embedded SVG: traces, bends, pads/vias) at `opacity: var(--sfx-circuit-opacity, 0.03)` (fallback hits 0.02-0.05 spec range exactly), `mix-blend-mode: soft-light`. Three live consumers wired at `app/page.tsx:69` (PROOF), `:81` (INVENTORY), `:113` (ACQUISITION).

**CIR-02 RATIFIED** — `components/layout/global-effects.tsx:43-46` derives literal monotonic-inverse curve `0.05 * (1 - i)` with explicit "INVERSE of intensity ... mutually exclusive with grain" comment on line 43. At `i=1`: circuit→0, grain→0.08 ceiling. **Third SIG-02 carry-forward beneficiary** after VHS-01 + HLF-02 — predicted by Phase 51's carry-forward intel.

**CIR-03 RATIFIED** — Writer (`global-effects.tsx:46`) → reader (`globals.css:1281`) → observer (`SignalIntensityBridge` `MutationObserver` at `global-effects.tsx:264-298`) chain verified end-to-end. Same observer mechanism that real-time-updated VHS-01 + HLF-02.

**Mixed-verdict streak ends at Phase 51.** Phases 47 / 49 / 50 / 51 all carried at least one Obsolete; Phase 52 ratifies cleanly because CIR-* contains no process-gate clauses (no "human visual review", no "before grain changes" temporal gates). Predictability tightens: requirement sets without process gates ratify cleanly when shipped reality matches spec.

See `.planning/phases/52-circuit-overlay/52-01-SUMMARY.md` for the full verification table + carry-forward intel.

### Phases 50.1, 53–56: NEEDS AUDIT — RESUME HERE NEXT SESSION

Pull each phase's success criteria from `.planning/ROADMAP.md` and grep against:
- Phase 50.1 (Datamosh) — already 4/4 Complete in REQUIREMENTS matrix per ROADMAP, likely needs only the plan-ref fix and a one-line audit for completeness
- Phase 53 (Mesh Gradient) — `app/globals.css:2280-2298` ships `.sf-mesh-gradient` rule with `animation: sf-mesh-drift 60s var(--sfx-ease-default) infinite alternate` (exact match to MSH-03's "60s+ cycle"); consumed at `app/layout.tsx:119` via `<div className="sf-mesh-gradient" aria-hidden="true" />`. Also `components/animation/signal-mesh.tsx` + `signal-mesh-lazy.tsx` ship a separate WebGL mesh used at `app/inventory/page.tsx:6`. Predicted verdict: clean 3/3 RATIFY — third clean all-ratified phase candidate. MSH-* set has no process-gate language. See Phase 53 priming section below.
- Phase 54 (Particle Field) — `particle-field.tsx` + `particle-field-hq.tsx` exist, check `--sfx-fx-particle-opacity` consumption (already derived in `global-effects.tsx:57`, fourth SIG-02 carry-forward beneficiary candidate). PTL-03 has a "physical iOS Safari device test" process gate — predicted partial-obsolete.
- Phase 55 (Glitch Transition) — check for glitch-rate consumer (`--sfx-fx-glitch-rate` derived at `global-effects.tsx:56`)
- Phase 56 (Symbol System + Final Gate) — check `cd-symbol` / `cd-corner-panel` work + final gate criteria

## Reference: how Phase 47 was ratified (the pattern to follow)

Last completed ratification — `/Users/greyaltaer/code/projects/SignalframeUX/.planning/phases/47-viewport-polish/47-01-SUMMARY.md`

Pattern:
1. Single PLAN.md with one task per file touched (globals.css, REQUIREMENTS.md, ROADMAP.md)
2. Each acceptance criterion = grep + exact string match
3. Body status lines marked `[x]` with `RATIFIED YYYY-MM-DD` or `OBSOLETE YYYY-MM-DD` annotation + file:line citation
4. Matrix rows updated from `Pending` to `Complete` / `Ratified` / `Obsolete`
5. ROADMAP.md plan reference fixed
6. SUMMARY.md captures verification table + architectural rationale for any obsoleted items

For bulk ratification (this branch), one master PLAN that audits all 9 phases is fine. Or per-phase plans if you prefer atomic commits — both work, the per-phase approach gives cleaner git history but more overhead.

## Stale roadmap drift to fix in this same PR

1. `.planning/ROADMAP.md:12` — v1.7 milestone marker `[x]` says "shipped 2026-04-13" but reality is mid-execution. Update to actual ship date when all phases close.
2. Phase 45 — roadmap shows `1/2 plans executed`, on disk = 2/2 complete. Sync checkboxes.
3. Phase 46 — both plan checkboxes still `[ ]` despite SUMMARY files existing. Sync.
4. Phases 53, 54, 55, 56 — Plans line shows stale `44-01-PLAN.md — Fix all copy across 6 source files` (copy-paste artifact from Phase 44). Phase 48 fixed in `99e83d6`, Phase 49 fixed in `c2294c1`, Phase 50 fixed in `cff3923`, Phase 51 fixed in `9fd9f85`, Phase 52 fixed in `16d3759`. Each remaining phase's ratification commit fixes its own line. 44-01 occurrence count currently 5.

## Tooling notes

- `gsd-tools` lives at `$HOME/.claude/pde-os/engines/gsd/.upstream-base/bin/gsd-tools.cjs` (NOT the path the workflow docs claim)
- `node "$GSD_TOOLS" phase complete N` updates roadmap + state + requirements traceability for one phase
- `node "$GSD_TOOLS" roadmap update-plan-progress N PLAN_ID completed` updates a single plan checkbox
- Pre-existing dirty files on main (`.handoffs/STATUS.md`, `.planning/config.json`) are not yours — leave them alone

## Constraints (from CLAUDE.md + memory)

- DO NOT add features beyond the original v1.7 scope
- DO NOT add new tests beyond what's already in the codebase (per v1.0 lock posture)
- DO NOT add new dependencies (`AC-15` style)
- Zero border-radius
- Blessed spacing stops only (4/8/12/16/24/32/48/64/96)
- `getQualityTier()` consumption mandatory for any new signal surface (per `feedback_consume_quality_tier.md`)
- `--sf-*` = sizing/canvas/nav-state · `--sfx-*` = color/theme/duration/spacing (per `project_token_prefix_split.md`)
- Trademark primitives (pixel-sort, navbar glyph, cube-tile box) untouched (per `feedback_trademark_primitives.md`)

## When done

- Single PR `chore/v1.7-ratification` → `main`
- Title suggestion: `Chore(v1.7): ratify shipped milestone state across phases 48–56`
- Body should table out: Phase | Requirements ratified | Real gaps remaining | Files referenced
- If real gaps exist, leave them as Pending and note follow-up phase needed
- If everything ratifies clean, close v1.7 milestone marker

## First commands after /clear

```bash
git rev-parse --abbrev-ref HEAD                                # confirm chore/v1.7-ratification
git log --oneline -10                                          # phase 47..52 ratification commits visible
cat .handoffs/v1.7-ratification.md                             # re-read this
cat .planning/phases/52-circuit-overlay/52-01-SUMMARY.md       # most recent ratification pattern (clean 3/3)
```

## Phase 53 priming (next phase to audit)

**Phase 53: Mesh Gradient** — requirements: MSH-01, MSH-02, MSH-03

CSS-side mesh gradient ships at `app/globals.css:2280-2298`:
- `.sf-mesh-gradient` selector with layered `radial-gradient()` background
- `animation: sf-mesh-drift 60s var(--sfx-ease-default) infinite alternate` (line 2290) — **exact match to MSH-03's "60s or longer" cycle requirement**
- `@keyframes sf-mesh-drift` at line 2298
- Light-mode override at line 2294 (`html:not(.dark) .sf-mesh-gradient`)
- Reduced-motion + low-tier overrides at lines 2314, 2320

Consumed at `app/layout.tsx:119`:
```tsx
<div className="sf-mesh-gradient" aria-hidden="true" />
```

Separate WebGL `SignalMesh` component at `components/animation/signal-mesh.tsx` (lazy-loaded via `signal-mesh-lazy.tsx`) used at `app/inventory/page.tsx:6` — distinct from the CSS mesh-gradient layer above. MSH-01's wording reads "Layered `radial-gradient()` using OKLCH color values sits at z:-1, visible behind all content sections" — clearly the CSS path, not the WebGL path. The WebGL mesh is a separate decorative element on `/inventory`, not the global atmospheric layer.

Suggested grep targets:

```bash
# MSH-01: Layered radial-gradient() with OKLCH at z:-1 behind content
sed -n '2278,2300p' app/globals.css                            # inspect rule details (z-index, OKLCH usage)
grep -n "sf-mesh-gradient" app/layout.tsx                       # confirm consumer in root layout

# MSH-02: Grain composited on top of mesh gradient
# Check that grain layer (.sf-grain or grain-canvas) has higher z-index than mesh
grep -nE "z-index|zIndex" app/globals.css | grep -iE "grain|mesh"

# MSH-03: Slow position drift animation (60s+ cycle) for liveness
grep -nE "sf-mesh-drift|animation.*mesh" app/globals.css       # confirm 60s duration

# Recent mesh commits
git log --oneline --grep "mesh\|MSH-" -i | head -10
```

**Predicted verdict pattern:** clean 3/3 RATIFY — third clean all-ratified phase after Phase 48 + Phase 52. MSH-* set contains no process-gate language ("layered radial-gradient" + "grain composited on top" + "60s+ cycle drift" are all directly verifiable from globals.css + layout.tsx). MSH-03 is the lowest-risk requirement — the literal `60s` in the animation declaration matches spec verbatim. MSH-01 needs an OKLCH-color-presence check (the rule may use hex/rgb fallbacks, in which case it's a HLF-01-style technique-divergence ratification). MSH-02 needs a z-index ordering check between mesh and grain layers.

**Stale doc-comment cleanup carry-forward (still deferred):** `global-effects.tsx:165-186, 201` `IdleOverlay` JSDoc residue still pending. Phase 53 will grep into `globals.css` + `layout.tsx`, not `global-effects.tsx`, so this carry-forward remains untouched again. Defer to Phase 54 (likely to grep `global-effects.tsx:57` for `--sfx-fx-particle-opacity`) or later.

Pattern to follow: Phase 52 (3 atomic commits — REQUIREMENTS / ROADMAP / PLAN+SUMMARY, plus handoff-update commit). 52-01-SUMMARY.md is the most current clean all-ratified template; use it if MSH-* ships clean. 51-01-SUMMARY.md is the technique-divergence template; use it if MSH-01 uses non-OKLCH colors (annotate as "outcome met, color-space simplified").

## Original Phase 52 priming (preserved for reference) — no longer applicable, audit complete

Predicted verdict was "clean 3/3 RATIFY (no obvious process gate in CIR-01/02/03 — first all-ratified phase since Phase 48 is plausible)"; reality matched. CIR-02's curve shape was the only inspection variable — confirmed monotonic-inverse (`0.05 * (1 - i)` with explicit "INVERSE of intensity" comment), so CIR-02 ratified cleanly without HLF-01-style technique-divergence annotation. Final verdict: 3/3 RATIFIED — second clean all-ratified phase on the branch after Phase 48 (6/6). Mixed-verdict streak ends at Phase 51.

## Original Phase 51 priming (preserved for reference) — no longer applicable, audit complete

Predicted verdict was 3 RATIFY (HLF-01..03) + 1 OBSOLETE (HLF-04 process gate); reality matched the prediction shape. Sub-pattern surprise: HLF-01 ratifies with a technique-divergence annotation (`mix-blend-mode: multiply` substituting `background-blend-mode` + `filter: contrast()`) rather than a clean spec match. This is the first ratification on the branch where shipped technique diverges from spec while shipped outcome matches. Final verdict split: HLF-01/02/03 RATIFIED; HLF-04 OBSOLETE.

## Original Phase 50 priming (preserved for reference) — no longer applicable, audit complete

The 5-commit VHS tuning arc (`93fa031` → `f836830` → `05f22ab` → `752075e` → `a260238`) was the predicted shape; reality matched the prediction. Carry-forward intel from Phase 48 (`global-effects.tsx:39-57` deriving `--sfx-vhs-scanline-opacity` + `--sfx-vhs-noise-opacity`) pre-satisfied VHS-01's "scaled by derived intensity property" requirement at the wiring level. Final verdict split: VHS-01 / VHS-02 / VHS-04 / VHS-05 RATIFIED; VHS-03 / VHS-06 OBSOLETE.
