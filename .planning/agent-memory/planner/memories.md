# planner Agent Memory

> Loaded at agent spawn. Append-only. Max 50 entries.
> Oldest entries archived automatically.

### 2026-04-05T00:00:00Z | Phase 1 | tags: css-tokens, globals-css, spacing-sweep
Phase 1 is pure CSS/token hardening — no JS logic to test. Split into 3 plans: (1) globals.css token definitions, (2) component file spacing sweep + CVA audit, (3) fallbacks + print. Plans 1+2 run parallel (Wave 1) since they edit different file sections. Plan 3 depends on Plan 1 (needs tokens defined before adding fallbacks). Key insight: gsd-tools validation commands failed — environment may not have them available. Research was thorough and provided exact line numbers for all changes, making plans very specific.

### 2026-04-06T02:45:00Z | Phase 2 | tags: sf-primitives, cva-forwardref, barrel-export, parallel-plans
Phase 2 decomposes into 2 plans, both Wave 1 (fully parallel, no file overlap). Plan 01 covers structural primitives (Container, Section, Stack) — the nesting pair. Plan 02 covers content primitives (Grid, Text) plus SFButton audit. Research confirmed PRM-06 is mostly a verification task — SFButton already wires sf-pressable and uses intent standard. SFText is the most complex primitive (polymorphic as prop) but uses plain Record instead of CVA since variant is required with no default. All primitives are RSC-compatible (no 'use client') with forwardRef for GSAP. gsd-tools still unavailable for validation.

### 2026-04-06T03:30:00Z | Phase 3 | tags: gsap-signal, css-fallback, canvas-cursor, spec-document
Phase 3 has 4 plans in 2 waves. Wave 1 parallelizes CSS+interaction changes (Plan 01: globals.css fallback + hover timing + hard-cut) with the canvas cursor (Plan 02: new component, independent files). Wave 2 depends on Plan 01 for page-animations.tsx changes (Plan 03: ScrambleText ScrollTrigger + stagger batch) and all code plans for the spec document (Plan 04: SIGNAL-SPEC.md + deferred SIG-06/07/08). Key file conflict: page-animations.tsx modified by both Plan 01 (hard-cut) and Plan 03 (scramble+stagger), so Plan 03 depends on Plan 01. Three deferred requirements (SIG-06/07/08) are documented in SIGNAL-SPEC.md rather than implemented, per CONTEXT.md locked decisions.

### 2026-04-06T04:15:00Z | Phase 4 | tags: hero-timing, crafted-states, reduced-motion, component-count
Phase 4 is a polish/integration phase — no new systems, only applying Phase 1-3 outputs. 3 plans in 2 waves. Wave 1 parallelizes hero lock (Plan 01: animation fast-path from 2.3s to 0.4s delay, component count 340->28, hero spacing) with crafted states (Plan 02: error/not-found as FRAME+SIGNAL moments, empty state for ComponentsExplorer). Wave 2 is reduced-motion QA (Plan 03: CSS audit + human-verify checkpoint + SIGNAL-SPEC.md update). Plan 03 depends on both 01 and 02 because it must verify the hero changes AND crafted states in reduced-motion. Key finding: hero-mesh currently renders at opacity-[0.45] via Tailwind — must change to opacity-0 so GSAP controls the fade-in as the first visible motion. Stats-band claims "340" but SF layer has 28 components.

### 2026-04-06T05:30:00Z | Phase 5 | tags: dx-docs, jsdoc-sweep, scaffolding, deferred-specs
Phase 5 is documentation + audit only — no new components or tokens. 2 plans, both Wave 1 (parallel, no file overlap). Plan 01: SCAFFOLDING.md (3 canonical patterns + import boundary), DX-SPEC.md (interface sketches for DX-04/DX-05/STP-01), and STP-02 audit (theme toggle GSAP guard — research predicts no code changes needed, color-cycle-frame.tsx is the only conflict surface). Plan 02: JSDoc sweep on all 28 SF components split into two tasks (A-L and M-Z, 14 files each). Three requirements deferred per user decision: DX-04, DX-05, STP-01 — documented with interface sketches only. gsd-tools init still fails but plans created manually with full frontmatter.

### 2026-04-05T12:00:00Z | Phase 6 | tags: webgl-singleton, threejs, gsap-ticker, infrastructure
Phase 6 is pure WebGL infrastructure — 2 plans in 2 sequential waves. Plan 01 (Wave 1): install Three.js + @next/bundle-analyzer, create color-resolve.ts (OKLCH probe extraction from canvas-cursor.tsx), create signal-canvas-lazy.tsx (SSR-safe wrapper mirroring global-effects-lazy.tsx). Plan 02 (Wave 2): singleton SignalCanvas with globalThis key pattern (from use-scramble-text.ts), scissor/viewport render loop with Y-axis inversion, GSAP ticker as render driver (never setAnimationLoop), reduced-motion guard baked into singleton, useSignalScene hook with IntersectionObserver + disposal, mount in layout.tsx. Key decisions: no transpilePackages (Turbopack issues), no color cache in Phase 6 (color-cycle-frame mutates --color-primary), antialias false for DU/TDR pixel aesthetic. Research was exceptionally thorough with exact code patterns from existing codebase — plans could embed near-complete implementations.

### 2026-04-05T14:00:00Z | Phase 7 | tags: audio-feedback, haptics, idle-overlay, data-cursor
Phase 7 is activation of dormant SIGNAL effects — 2 plans in 2 sequential waves. Plan 01 (Wave 1): new audio-feedback.ts (lazy AudioContext singleton, square wave oscillator, reduced-motion guard) + haptic-feedback.ts (navigator.vibrate feature detect) + InteractionFeedback component in global-effects.tsx using document-level pointerover/pointerdown with target.closest() filtering. Plan 02 (Wave 2): IdleOverlay upgrade from 60s to 8s threshold, OKLCH lightness pulse via GSAP ticker + Math.sin (captures base --color-primary before pulse, restores on snap-back), grain drift overlay via sf-grain-animated class toggle, instant snap-back by temporarily disabling CSS transition. SIG-09 is markup-only — add data-cursor to all homepage data-bg-shift divs + main elements on 4 showcase pages. Key conflict analysis: idle pulse and ColorCycleFrame both mutate --color-primary but cannot run simultaneously because any wheel event fires resetIdle first.

### 2026-04-05T16:00:00Z | Phase 8 | tags: signal-mesh, token-viz, webgl-pipeline, canvas-2d, disposal
Phase 8 decomposes into 2 plans, both Wave 1 (fully parallel, no file overlap). Plan 01 (SCN-01): SignalMesh — first WebGL scene consuming useSignalScene, IcosahedronGeometry detail=2 with ShaderMaterial vertex displacement, EdgesGeometry wireframe overlay, ScrollTrigger onUpdate mutating uDisplacement uniform, GSAP ticker incrementing uTime, ResizeObserver for camera aspect, SVG fallback for no-WebGL, disposal console logging. Also adds optional TTL cache to color-resolve.ts per STATE.md "optimize in Phase 8" flag. Plan 02 (SCN-02): Canvas 2D TokenViz rendering core 5 colors + 9 spacing stops + 9 typography scale entries with MutationObserver live update on /tokens page. Key findings: Lenis already wires ScrollTrigger.update (lenis-provider.tsx:24), so no scrollerProxy needed. Hero section has existing HeroMesh (Canvas 2D dot grid) — SignalMesh layers behind it via absolute positioning in page.tsx wrapper div, not replacing HeroMesh. gsd-tools init still fails but plans created manually with full frontmatter.

### 2026-04-05T18:00:00Z | Phase 9 | tags: glsl-hero, ascii-dither, sf-migration, stagger, signal-motion, signal-overlay
Phase 9 is the final v1.1 phase — 3 plans in 2 waves. Plan 01 (Wave 1): GLSL hero shader with single-pass integrated Bayer 4x4 dither (SCN-03 + SCN-04 combined — research recommended single-pass over two-pass WebGLRenderTarget). Fragment-only shader on PlaneGeometry(2,2) full-screen quad with FBM noise + grid lines + dither. SignalMesh relocated from homepage hero to /components page showcase section. Plan 02 (Wave 1, parallel with 01): SignalMotion (INT-03) scrub-capable ScrollTrigger wrapper distinct from ScrollReveal, + SignalOverlay (INT-04) floating panel with 3 CSS custom property sliders (--signal-intensity, --signal-speed, --signal-accent) toggled by Shift+S. Plan 03 (Wave 2, depends on 01): SF primitive migration (INT-01) across all 5 pages — SFSection replaces raw div/section wrappers. CRITICAL pitfall: data-bg-shift must be passed as spread prop NOT via bgShift boolean (API mismatch). Stagger (INT-02) is CSS-only + data attribute — PageAnimations line 368 already has ScrollTrigger.batch ready. Do NOT add stagger to grids with data-anim="comp-cell" children (double-animation conflict).

### 2026-04-06T14:00:00Z | Phase 10 | tags: css-var-defaults, type-fix, mechanical-fixes, single-wave
Phase 10 is the smallest phase yet — 2 plans, both Wave 1 (fully parallel, no file overlap). Plan 01: globals.css signal defaults + sf-section.tsx bgShift type fix (FND-01 + FND-02). Plan 02: reference page nav clearance + NEXT_CARDS SFSection wrap (INT-01). Research was exceptionally thorough with exact line numbers and code snippets for all 4 files. Zero new dependencies, zero behavioral risk. Plan 02 has only 1 task (both changes are small and in different files but share the same requirement INT-01, so combined). gsd-tools still unavailable — committed manually.

### 2026-04-06T16:00:00Z | Phase 11 | tags: registry-json, shadcn-build, meta-fields, single-plan
Phase 11 is a single plan (2 sequential tasks) — pure data authoring with no behavioral code. Task 1: complete registry.json to 33 items (backfill meta on 27 existing + add 5 layout primitives + sf-theme). Task 2: add registry:build script and run shadcn build to regenerate public/r/. Research provided exact meta classification table for all 33 items and complete sf-theme cssVars JSON, making the plan highly specific. Key risk: shadcn build may wipe base.json (not in registry.json) — plan includes backup/restore step. gsd-tools still unavailable.

### 2026-04-06T18:00:00Z | Phase 12 | tags: webgl-uniforms, mutation-observer, signal-motion, parallel-plans
Phase 12 decomposes into 2 plans, both Wave 1 (fully parallel, no file overlap). Plan 01 (INT-04): module-level signal cache with MutationObserver in glsl-hero.tsx and signal-mesh.tsx — reads --signal-intensity/speed/accent from cache inside GSAP ticker, never getComputedStyle per frame. Research provided complete code patterns including GLSL uniform declarations and ticker modifications. Plan 02 (INT-03): SignalMotion wrapping of 4 homepage sections in app/page.tsx — critical to wrap block components inside SFSection, not the SFSection itself (breaks bg-shift ScrollTrigger). No new dependencies, no test framework needed — all validation is grep-based static analysis plus build check.

### 2026-04-06T20:00:00Z | Phase 13 | tags: config-provider, react-context, factory-pattern, ssr-safe, single-plan
Phase 13 is a single plan with 2 sequential tasks — thin infrastructure, no new dependencies. Task 1: create lib/signalframe-provider.tsx (~80 lines) with createSignalframeUX factory returning { SignalframeProvider, useSignalframe }. Wraps existing toggleTheme from lib/theme.ts and gsap.globalTimeline for motion control. Hole-in-the-donut SSR pattern matching existing LenisProvider convention. Theme reads DOM classList (not localStorage) to match inline blocking script. motion.resume() guarded by prefersReduced (no-op when reduced-motion active). Also exports standalone useSignalframe for direct consumer import. Task 2: mount SignalframeProvider in app/layout.tsx at module scope with static config { defaultTheme: 'dark' }, wrapping {children} inside LenisProvider. layout.tsx remains Server Component (no 'use client'). Research was comprehensive — complete implementation provided in RESEARCH.md, all 5 DX-SPEC.md open questions resolved with codebase evidence. gsd-tools init still unavailable.

### 2026-04-06T22:00:00Z | Phase 14 | tags: session-storage, hydration-safe, use-effect, scroll-restoration, single-plan
Phase 14 is the smallest behavioral phase — single plan, 2 sequential tasks, ~100 lines new code. Task 1: create two hooks — use-session-state.ts (generic SSR-safe sessionStorage hook with SESSION_KEYS constants) and use-scroll-restoration.ts (scroll save/restore via pathname-keyed sessionStorage + rAF). Task 2: integrate into ComponentsExplorer (replace useState with useSessionState for activeFilter + add useScrollRestoration) and TokenTabs (switch SFTabs from uncontrolled defaultValue to controlled value+onValueChange driven by useSessionState). Critical details: GSAP Flip guard at line 385 prevents animation on sessionStorage restore (flipStateRef.current is null at mount). showAll state in TokenTabs intentionally NOT persisted. Scroll restoration uses requestAnimationFrame for Lenis compatibility. app/components/page.tsx is Server Component — scroll hook goes in ComponentsExplorer (client component), not page. No new dependencies, no test framework. gsd-tools still unavailable.

### 2026-04-06T23:00:00Z | Phase 15 | tags: documentation, frontmatter, requirements-checkboxes, scaffolding-api, final-phase
Phase 15 is pure documentation — no code changes. 2 plans, both Wave 1 (fully parallel, no file overlap). Plan 01: add `requirements_completed` field to all 30 SUMMARY.md frontmatters + fix 14 stale checkboxes across v1.0 and v1.1 REQUIREMENTS.md archives (6 in v1.0, 8 in v1.1). Plan 02: append Section 8 (Config Provider API) to SCAFFOLDING.md documenting createSignalframeUX/useSignalframe + verify SFSection JSDoc is already correct + mark DOC-01 complete. Research was thorough — exact stale entries identified with completion attribution, full API contract pre-written. SFSection JSDoc already fixed in Phase 10 (verify-only). This is the final phase of v1.2 Tech Debt Sweep. gsd-tools init still unavailable.

### 2026-04-06T23:30:00Z | Phase 16 | tags: infrastructure, shadcn-install, scaffolding, categories
Phase 16 is pure infrastructure — no new SF components. 2 plans, both Wave 1 (fully parallel, no file overlap). Plan 01: install 7 shadcn bases (accordion, alert-dialog, avatar, navigation-menu, progress, collapsible, toggle-group) + capture performance baseline in BASELINE.md with bundle size and Lighthouse metrics (INFRA-02). Plan 02: create SCAFFOLDING.md from scratch with 9-point wrapper checklist + prop vocabulary table (INFRA-01, INFRA-04) + migrate ComponentsExplorer from 8 filter tags to 6 named categories (INFRA-03). Research was MAXDEPTH — all 29 SF wrappers audited for prop compliance, all 16 explorer entries mapped to new categories, exact rounded-* risks catalogued per future base. SCAFFOLDING.md is confirmed absent from git history — fresh creation, not restore. gsd-tools init still unavailable.

### 2026-04-06T19:00:00Z | Phase 17 | tags: sf-wrappers, barrel-conflict, same-commit-rule, sequential-plans
Phase 17 split into 2 sequential plans (not parallel) because both modify sf/index.ts and registry.json — file ownership conflict prevents parallelism. Plan 01 (Wave 1) takes simpler Pattern A wrappers (SFCollapsible, SFBreadcrumb, SFAvatar, SFAlert) including installation of 2 missing shadcn bases (alert, breadcrumb). Plan 02 (Wave 2) takes design-intensive components (SFAlertDialog with loading state, SFEmptyState with Bayer dither + ScrambleText, SFStatusDot with GSAP pulse) plus all 7 ComponentsExplorer entries. GSAP import path is `@/lib/gsap-core` (not direct `gsap` import). SFBreadcrumb likely Server Component (shadcn Breadcrumb uses semantic HTML, no Radix state). Same-commit rule enforced per task, not per plan.

### 2026-04-06T20:00:00Z | Phase 18 | tags: gsap-animation, sonner-toast, signal-layer, sequential-plans
Phase 18 split into 2 sequential plans because both modify sf/index.ts and registry.json (same barrel/registry conflict as Phase 17). Plan 01 (Wave 1): install Sonner + SFAccordion (GSAP stagger at 50ms per child, reverse on close, reduced-motion guard) + SFProgress (GSAP gsap.to tween on value change, removed transition-all from base indicator, gsap.set for reduced-motion). Plan 02 (Wave 2): SFToast/SFToaster (Sonner unstyled mode + toast.custom with SFToastContent, GSAP slide from x:-40, bottom-left z-100 positioning to avoid SignalOverlay) + 3 ComponentsExplorer entries (ACCORDION, PROGRESS, TOAST under FEEDBACK/SIGNAL) + bundle gate verification (ANALYZE=true pnpm build under 150KB). All three components use Pattern A, 'use client', import gsap from @/lib/gsap-core (not gsap-split). SFProgress rewrites Radix structure directly (not wrapping base) to get ref on indicator. Key pitfalls from research: accordion CSS height animation kept (container) with GSAP stagger only on children; progress must remove transition-all to avoid GSAP conflict; toast uses unstyled:true to avoid !important overrides.

### 2026-04-06T21:00:00Z | Phase 19 | tags: p2-components, navigation-menu, pagination, stepper, toggle-group, sequential-plans
Phase 19 split into 2 sequential plans because Plan 02 (NavigationMenu) is explicitly last per project decision, and both modify sf/index.ts + registry.json. Plan 01 (Wave 1): install shadcn pagination base + SFToggleGroup (Radix wrap, CVA intent replacing base variant, own Context to propagate intent/size, import directly from radix-ui not ui/toggle-group base) + SFPagination (Server Component matching SFBreadcrumb pattern, no ellipsis per locked decision, aria-current on active) + SFStepper (Pattern C, consumes SFProgress as connector between steps, vertical layout, per-step status prop with error state, lucide icons for complete/error). Plan 02 (Wave 2): SFNavigationMenu (most complex — desktop Radix flyout with viewport, mobile SFSheet collapse at <768px via CSS hidden/flex, indicator arrow hidden per DU/TDR, CSS animation not GSAP per research recommendation, rounded-none on 5+ sub-elements, import SFSheet directly not from barrel to avoid circular) + 4 ComponentsExplorer entries (TOGGLE_GRP, PAGINATION, STEPPER, NAV_MENU, indices 023-026) + bundle gate check. Key pitfalls: pagination base not yet installed (unlike nav-menu and toggle-group from Phase 16); SFStepper vertical progress connector may need writing-mode hack or fallback to simple div; ToggleGroup base uses variant Context that must be remapped to intent in SF layer.

### 2026-04-06T22:00:00Z | Phase 20 | tags: p3-lazy, registry-audit, meta-pattern, final-phase, milestone-close
Phase 20 is the final v1.3 phase — 2 plans in 2 sequential waves. Plan 01 (Wave 1): install shadcn calendar + menubar bases, create SFCalendar and SFMenubar as Pattern B (lazy, next/dynamic, ssr:false, SFSkeleton fallback). Neither added to sf/index.ts barrel. Calendar introduces react-day-picker + date-fns (~40-60KB combined — justified as lazy-only). Menubar uses existing Radix, no new deps. Both get registry entries with meta.heavy:true and meta.pattern:"B". Critical pitfall: calendar.tsx generated by shadcn has extensive rounded-* classes on day cells, nav buttons, month grid — must override every one with rounded-none in SF wrapper using classNames prop. Plan 02 (Wave 2): full v1.3 final audit — fix meta.pattern across all 50 registry entries (8 pre-v1.3 entries incorrectly use "B" instead of "A"), run pnpm registry:build to regenerate 10+ missing public/r/ files (Phase 17-18 components never rebuilt), update SCAFFOLDING.md (component count, resolve pitfall #3), add Calendar/Menubar to ComponentsExplorer, run ANALYZE=true pnpm build for bundle gate. Plans are sequential because Plan 02 depends on Plan 01 for the new registry entries.

### 2026-04-06T23:45:00Z | Phase 21 | tags: tech-debt, webgl-lifecycle, lenis-context, parallel-plans
Phase 21 is pure bugfixes — 2 plans, both Wave 1 (fully parallel, no file overlap). Plan 01 (TD-01, TD-02, TD-04): surgical edits to signal-mesh.tsx, glsl-hero.tsx (observer disconnect + isNaN guard), and components-explorer.tsx (TOAST name dedup). Plan 02 (TD-03): larger refactor — add React Context to existing LenisProvider (custom approach, NOT lenis/react ReactLenis, to preserve GSAP ticker wiring) + migrate 5 consumer files from window.scrollTo to lenis.scrollTo with reduced-motion fallback. Key pitfall: page-transition.tsx scrollTo is inside a transitionend DOM handler, requiring useRef to capture Lenis instance. Research was thorough enough that plans embed near-complete implementations. No new dependencies. gsd-tools still unavailable.

### 2026-04-06T23:50:00Z | Phase 22 | tags: css-tokens, theme-migration, documentation, sequential-plans
Phase 22 is the smallest phase yet — 2 plans in 2 sequential waves (both touch globals.css, forcing sequencing). Plan 01 (Wave 1): TK-01 moves --color-success and --color-warning from :root to @theme (functional correctness fix — bg-success utilities currently generate nothing) + TK-04 audit of color-resolve.ts (read-only, no code changes — bridge only resolves --color-primary/foreground/background). Plan 02 (Wave 2): TK-02 elevation absence comment in globals.css + Elevation Policy section in SCAFFOLDING.md + TK-03 Deferred Token Groups section in SCAFFOLDING.md (sidebar/chart). Zero new dependencies, zero behavioral code changes beyond the CSS token move. Research was thorough — exact line numbers, built CSS evidence of missing utilities, complete caller audit. Key pitfall: --sf-yellow and --sf-green are aliases that must NOT move (they use var() inline, not Tailwind utilities).

### 2026-04-06T23:55:00Z | Phase 23 | tags: sf-wrappers, pattern-a-b, barrel-conflict, sequential-plans
Phase 23 split into 2 sequential plans (not parallel) because both modify sf/index.ts, registry.json, and components-explorer.tsx — same barrel/registry file conflict as Phases 17-20. Plan 01 (Wave 1): install 3 shadcn bases (hover-card, input-otp, drawer) + create 3 Pattern A wrappers (SFInputGroup, SFHoverCard, SFInputOTP) with barrel exports and registry entries. Plan 02 (Wave 2): SFDrawer Pattern B (real impl + lazy loader, 8 sub-components) + 3 ComponentsExplorer entries + registry:build + bundle gate. Research was comprehensive with exact code patterns from existing sf-popover.tsx and sf-calendar-lazy.tsx. Key pitfall: InputGroup base has hardcoded rounded-* in CVA that requires explicit rounded-none override at SF layer. DRAWER already exists in ComponentsExplorer at index "012" — only version update needed, not new entry. Four new registry items bring total to 53.

### 2026-04-07T01:00:00Z | Phase 24 | tags: data-authoring, parallel-plans, shiki-rsc, api-docs-volume
Phase 24 is pure data authoring — no UI, no behavioral changes. Split into 2 fully parallel plans (Wave 1, no file overlap). Plan 01: install shiki + server-only, create code-highlight.ts (shiki/core singleton with OKLCH theme, server-only guard) + create component-registry.ts (35 entries mapping grid items to variant props, code snippets, docIds). Plan 02: extend api-docs.ts with ~36 new ComponentDoc entries (22 grid + 14 non-grid) for full ~49+ item coverage. Key decision: docId naming convention agreed upfront (camelCase SF names) to allow parallel authoring. Major risk in Plan 02 is volume — 36 entries is the largest single-file data authoring task in the project. Plan 02 is marked risk="high" and requires reading ~15 SF component source files to extract accurate props. Pattern B components (Calendar, Menubar, Drawer) need direct importPaths, not barrel — a pitfall from research.

### 2026-04-07T02:00:00Z | Phase 25 | tags: ui-assembly, server-client-split, shiki-precompute, sequential-plans
Phase 25 is the milestone's primary feature — 2 sequential plans (Wave 1 then Wave 2). Plan 01 is the heaviest single plan yet: creates ComponentDetail panel (~300 lines) + wires into ComponentsExplorer + adds session state key + z-index CSS rule + converts page.tsx to async for shiki pre-computation. Plan 02 converts homepage ComponentGrid from Server to Client Component for onClick interactivity + bundle gate verification. Key architectural decision: shiki is server-only, so highlighted HTML must be pre-computed in Server Component pages and passed as string props (Record<string, string> map). Plans are sequential because Plan 02 depends on the ComponentDetail component created in Plan 01. Both plans touch only block/page files — no shared barrel/registry conflict, but sequencing is forced by the component dependency. 13 requirements in one phase is the most yet — grouped by concern (core panel vs homepage integration) rather than splitting further, since component-detail.tsx is a single cohesive unit.

### 2026-04-07T03:00:00Z | Phase 27 | tags: gap-closure, id-mapping, surgical-fixes
Phase 27 is a pure gap closure — 1 plan, 2 tasks, Wave 1. All three IBF bugs are mechanical single-file edits. Key discovery during planning: homepage DROPDOWN showcase item has NO COMPONENT_REGISTRY entry — replaced with BADGE (key "008") which has full registry data. The homepageIds array in page.tsx (shiki pre-computation) must be updated atomically with the COMPONENTS array — research correctly flagged this as Pitfall 4. Single plan is justified because all 3 bugs are independent, touch different files, and total ~15 min execution. No new dependencies, no behavioral risk beyond the ID swap.

### 2026-04-07T04:00:00Z | Phase 26 | tags: verification-gate, lighthouse, bundle-analysis, checkpoints
Phase 26 is a pure verification gate — no files modified, only measurement and audit. 2 plans in 2 waves. Plan 01 (Wave 1, autonomous): bundle gate measurement via ANALYZE=true pnpm build + rootMainFiles gzip calculation + Playwright regression suite. Plan 02 (Wave 2, non-autonomous): Vercel deploy (checkpoint:human-action — vercel link is interactive) + Lighthouse audit (checkpoint:human-verify — PageSpeed Insights is a web tool). Key planning decision: Plan 02 depends on Plan 01 because deploying before confirming bundle gate is wasteful. Three checkpoints total in Plan 02 (pre-deploy audit auto task + human deploy + human Lighthouse verify). Research was comprehensive — exact measurement scripts, known risk factors (canvas ARIA, CLS from height animation, console errors), and fix taxonomy all provided. No new dependencies needed.

### 2026-04-08T02:00:00Z | Phase 28 | tags: route-rename, redirects, link-surgery, simple-phase
Phase 28 is pure infrastructure — 2 plans, 2 sequential waves. Plan 01 (Wave 1): add 308 redirects to next.config.ts (6 entries: 3 exact + 3 wildcard) + move 3 App Router directories (components->inventory, tokens->system, start->init) + create Playwright smoke test file. Plan 02 (Wave 2): link surgery across 9 files updating all internal href strings from old to new route names + test URL updates. Key pitfall flagged: `@/components/` import paths vs `/components` route strings must NOT be conflated — naive find-replace will corrupt hundreds of source imports. Registry edge case: lib/component-registry.ts line ~396 has a JSX code sample with hardcoded `/components` href. Discovery level 0. Simplest phase in v1.5. gsd-tools init still unavailable.

### 2026-04-08T03:00:00Z | Phase 29 | tags: infrastructure, scroll-hardening, reduced-motion-gate, parallel-plans
Phase 29 is pure infrastructure — 2 plans, both Wave 1 (fully parallel, no file overlap). Plan 01: 5 micro-edits across existing files (overscroll CSS 1 line, Lenis ignoreMobileResize 1 line, Observer registration 3 lines across 2 files, fonts-ready hook 4 lines) + Playwright smoke tests. Plan 02: new PinnedSection component (follows horizontal-scroll.tsx pattern exactly — gsap.context, reduced-motion early return, invalidateOnRefresh) + token-viz.tsx reduced-motion patch + PF-06 gate audit tests. Research was exceptionally thorough — exact insertion points with line numbers for all 5 edits, complete PinnedSection implementation provided, exhaustive reduced-motion audit of all 26 animation components. MutationObserver disconnect in color-resolve.ts confirmed correctly absent (module-level singleton, not component-scoped). gsd-tools init still unavailable.

### 2026-04-08T05:00:00Z | Phase 31 | tags: scroll, manifesto, content-gated, two-plan, forward-ref, pinned-container
Phase 31 split into 2 plans by content-vs-engineering boundary, NOT by file ownership. Plan 01 (Wave 1) is content authoring only — surfaces the researcher's 8-statement manifesto draft to the user via a checkpoint:decision task, then writes lib/thesis-manifesto.ts after approval, then scaffolds tests/phase-31-thesis.spec.ts with the 6 TH-05 source-grep tests (matching Phase 29 fs.readFileSync pattern since vitest is not installed). Plan 02 (Wave 2, depends_on:["01"]) is engineering — Task 0 upgrades PinnedSection to React.forwardRef using useImperativeHandle to merge internal containerRef with the forwarded ref (semantic-neutral, Phase 32 SIGNAL will benefit), Task 1 builds ThesisSection + ManifestoStatement composing existing primitives with a nested ScrollTrigger using pinnedContainer (NOT extending PinnedSection's API per RESEARCH Option A), Task 2 replaces the Phase 30 stub PinnedSection block in app/page.tsx with <ThesisSection />, Task 3 extends the same Plan-01-created spec file with TH-01..04 + TH-06 browser tests (does not create a new file), Task 4 is the mandatory iOS Safari physical-device checkpoint (D-34 from STATE.md). Key planning decisions: (a) content-as-Wave-0-dep enforces user review at plan boundary not task boundary — engineering literally cannot start until manifesto file is approved; (b) PinnedSection forwardRef chosen over wrapper-div alternative because Phase 32 will need the same pattern; (c) test file owned by Plan 01, extended by Plan 02 — single file, not split; (d) AC count grew to 19 in Plan 02 because each engineering decision (D-13 pin integrity, D-26 no data-anim, D-27-30 reduced motion, D-31 mobile, etc.) deserves its own grep-verifiable AC. AC format used "**AC-N (descriptor):**" with parenthetical for readability — auditor regex must allow the parenthetical. gsd-tools still unavailable.

### 2026-04-09T00:00:00Z | Phase 34 | tags: visual-language, restyle, hud, specimens, wave-0-tests, locked-split
Phase 34 is a "restyle + redeploy" finishing pass with zero new dependencies. Plan structure was LOCKED by the orchestrator prompt into 4 plans in a forced Wave 0 → 1 → 2 sequence: 34-01 Wave 0 scaffolds the Playwright spec in RED, Wave 1 ships the visual language pass (HUD indicator, GhostLabel ×5 deploy, useNavReveal extract, display-type bumps, magenta audit ≤5 per target file, /inventory COMPONENTS→INVENTORY copy fix, VL-04 human verify), then 34-02/03/04 run parallel in Wave 2 consuming the shared hook + HUD outputs. 34-01 has 7 tasks (exceeds 2-3 guideline) but the 6 Wave 1 work items are semantically coupled and the orchestrator locked them into one plan — split attempts would create circular file dependencies (all 6 edit different files, none overlap, so they belong together). Key planning decisions: (a) Wave 0 test scaffolding lives inside 34-01 as task wave="0" — not a separate plan — because it is a dev-time prerequisite tightly coupled to 34-01's implementation, (b) 34-02 ColorSpecimen stays Client (keyboard grid nav load-bearing), other 3 specimens Server, TokenTabs stays as data-layer orchestrator with useSessionState preserved, (c) 34-03 drops STEPS.highlight yellow-band and removes NEXT_CARDS/CHECKLIST/COMMUNITY BAND while preserving STEPS data + CodeBlock helper + COLOR_MAP.kw syntax-highlight (load-bearing magenta), (d) 34-04 removes the 3-panel grid + right preview panel from api-explorer.tsx but preserves API_DOCS (lib/api-docs.ts untouched), DataDrivenDoc render, search/filter input, keyboard navigation — schematic restyle not a rewrite, (e) /inventory h1 string fix piggybacks on 34-01 (sanctioned per CONTEXT.md as string-only, not Phase 33 redo), (f) subpage nav reveal uses the useNavReveal triggerRef===null branch → visible immediately on subpages, avoiding a Nav forwardRef refactor that would complicate all 3 subpage plans. gsd-tools still unavailable — manual frontmatter validation applied. 10/10 requirement IDs covered: VL-01/02/04/05/06 + SP-05 in 34-01, SP-01/02 in 34-02, SP-03 in 34-03, SP-04 in 34-04, SP-05 partial referenced across all 4.

### 2026-04-09T01:00:00Z | Phase 34 | tags: revision, iteration-1, sp-05-correction, hook-redesign, blocker-fix
Phase 34 revision iteration 1/3 — checker flagged 1 blocker + 2 warnings. (1) BLOCKER: 34-04 Task 1 verify <automated> block had two commands concatenated without separator (`...| grep -q "^$"npx tsc --noEmit ...`) AND a logically broken `git diff --name-only | grep -q "^$"` test. Replaced with single well-formed command using `[ -z "$(git diff --name-only lib/api-docs.ts)" ]` for the "no changes" assertion. (2) WARNING: subpage nav reveal contradicted LOCKED CONTEXT.md §VL — Nav reveal pattern. Original 34-01 design had Nav.tsx call `useNavReveal(navRef, entryTriggerRef)` and let subpages fall through `triggerRef===null` for visible-immediately. This is the OPPOSITE of what CONTEXT.md locks: subpages MUST pass header ref so nav appears once header scrolls out of view. Architectural redesign: hook signature changed from `useNavReveal(navRef, triggerRef)` (2-arg) to `useNavReveal(triggerRef)` (single-arg). Hook now flips `document.body.dataset.navVisible` instead of mutating a passed nav element. New `components/layout/nav-reveal-mount.tsx` client island created — takes `targetSelector` prop, resolves DOM element, calls hook. New CSS rule in globals.css: `body[data-nav-visible="true"] .sf-nav-hidden { visibility: visible; opacity: 1; }`. Nav.tsx no longer calls useNavReveal AT ALL — per-page client islands own the call. Each subpage's page header gets `<header data-nav-reveal-trigger>` wrapping the h1 grid, and the page renders `<NavRevealMount targetSelector="[data-nav-reveal-trigger]" />`. trigger===null branch becomes a SAFETY FALLBACK ONLY with dev-mode console.warn, NOT a sanctioned subpage path. Files added to 34-01 files_modified: components/layout/nav-reveal-mount.tsx, app/globals.css. Test sketches in 34-01 Task 0 SP-05 block rewritten: removed "shows nav immediately" assertions for subpages, added scroll-based assertions (`page.evaluate(() => window.scrollTo(0, 600))` then assert `body[data-nav-visible="true"]`), kept reduced-motion test (visible immediately under reduced motion). Same scroll-based test rewrite applied in 34-02, 34-03, 34-04 SP-05 partial blocks. 34-01 Task 4 Parts B (/system) and C (/init) updated to wrap h1 in `<header data-nav-reveal-trigger>` (but the actual NavRevealMount render is owned by 34-02 and 34-03 respectively). 34-01 Task 4 Part D (/reference) creates the new header AND renders NavRevealMount in one shot since 34-04 doesn't touch page.tsx. 34-02 Task 2 Part B fully rewritten from "no code change needed (option b)" to explicit NavRevealMount wiring instructions. 34-03 Task 1 JSX rewrite updated to wrap h1 grid in `<header data-nav-reveal-trigger>` instead of `<div>` and render NavRevealMount as first child of <main>. 34-03 interfaces block §useNavReveal rewritten from "subpage default visible immediately" to the strict header-trigger contract. 34-04 AC-8 + AC-9 + truths + SP-05 test rewritten — 34-04 itself does NOT modify app/reference/page.tsx (boundary preserved); the wiring is owned by 34-01 Task 4 Part D, and 34-04 only verifies it still works. (3) WARNING: 34-01 Task 0 Wave 0 verify only confirmed test registration, not RED state. Replaced with two-command chain: `npx playwright test ... --list | grep -c "Phase 34" && ! npx playwright test ... --reporter=line` — the `!` inverts the spec exit code so verify succeeds only when the spec FAILS (RED confirmed). All structural balance checks still pass: 34-01=7 tasks (6 automated + 1 checkpoint), 34-02=3, 34-03=2, 34-04=2. AC numbering and ac_refs preserved. No tasks added or removed. Surgical revision — no replan from scratch.

### 2026-04-09T14:20:00Z | Phase 35 | tags: launch-gate, pre-brief, brain-wins, 5-fix-cap, vl-05-lock
Phase 35 is v1.5 close — pre-brief at wiki/analyses/v1.5-phase35-brief.md locked every structural decision BEFORE planning, making this a brief-propagation pass not a discovery pass. 5 plans in 4 waves (0/1/2/3): 35-01 Wave 0 brain-wins commit (ROADMAP stale-fix + Vercel CLI upgrade + Gap 2 Day 1 test + 3 CI-gate RED stubs + OPEN-ITEMS.md), 35-02 Wave 1 test suite authoring (5 routes × 3 viewports, locked VIEWPORTS constant, Gap 1 tightening waitUntil:"domcontentloaded"+timeout:500, PF-01 bundle gate with Three.js async assertion, PF-03 LCP with verbatim brief body + D-08 comment, PF-04 CLS parameterized sweep), 35-03 Wave 1 parallel (app/opengraph-image.tsx flex-only fs.readFile no-edge-runtime + twitter-image re-export + layout.tsx metadataBase + scripts/launch-gate.ts 3-run worst-score advisory + lighthouse devDep install), 35-04 Wave 2 (chrome-devtools MCP single-session exploratory pass 90-120 min + 5-fix cap triage + 5 Awwwards screenshots at 1440x900 via CDT-MCP not Playwright + three-act description deck with Liquid Glass named + credits OPEN-1 resolved), 35-05 Wave 3+4 (vercel --prod + LR-03 console sweep on prod URL via CDT-MCP + final launch-gate.ts run + close commit with both audit strings + STATE/ROADMAP flip). Key planning decisions forced by the brief + user correction: (a) NO "reduce/improve/optimize" task phrasing — gate-and-measure only; (b) NO "submit to Awwwards" task — prepare-only, Grey's click is outside phase scope; (c) NO VL-05 modification — entry-section.tsx lines 43-58 boundary lock on every homepage-touching task in every plan, passive regression check only; (d) 1 Wave 2→3 iteration cycle budget + ~10min spot-check re-verification (NOT 2 full cycles); (e) Next 15.5.14 FROZEN — no Next 16 migration work; (f) LR-01 credits + LR-02 metadataBase as checkpoint:decision tasks blocking on Grey, not planner-defaulted; (g) ROADMAP.md:480-481 stale plan list fix landed at PLAN TIME in the brain-wins commit (plan 35-01 Task 1 becomes idempotent verify); (h) PF-04 removed from requirements frontmatter (already [x] from Phase 29) but CI coverage test still authored in 35-02. Requirement distribution across 5 plans: PF-01 (35-01,35-02), PF-02 (35-03,35-05 advisory), PF-03 (35-01,35-02), LR-01 (35-04), LR-02 (35-03), LR-03 (35-05), LR-04 (35-02). Five plans not five agents: brief's "5 parallel Playwright agents" is an EXECUTION concern that execute-phase handles via subagent dispatch inside plan 35-02, NOT a plan boundary. Plans captured DELIVERABLES (test files + OG image + scripts + triage docs + close), agents inside plan 35-02 parallelize the writing. Brain-wins commit pattern: PLAN files + ROADMAP.md fix + tests/phase-35-{init,reference,bundle-gate,lcp-homepage,cls-all-routes}.spec.ts Wave 0 seeds all land in one atomic commit per brief §Compile-Back Contract. 13-15 AC-N entries per plan (35-02 has 13, 35-04 has 12, 35-05 has 12) — high AC count justified by brief's extensive lock table. 33/33 quality gate checks passed. gsd-tools still unavailable — manual Python validation applied.

### 2026-04-10T21:00:00Z | Phase 36 | tags: housekeeping, lighthouse-loop, eslint-flat-config, carry-overs
Phase 36 is a clean housekeeping phase — 2 parallel plans (Wave 1). Plan 01 combines Lighthouse diagnostic loop (iterative deploy+audit), test path fixes, TS strict fixes, REQUIREMENTS.md v1.6 append, and ROADMAP verification. Plan 02 is isolated ESLint flat config creation + zero-violation cleanup. No file overlap between plans. Research was thorough with exact FlatCompat pattern confirmed against installed packages — eslint-config-next 15.5.14 ships legacy format requiring FlatCompat adapter. Key sizing insight: Lighthouse fix loop is variable-scope (unknown audit failures until first run) so it gets its own task with no cycle cap.

### 2026-04-10T23:50:00Z | Phase 38 | tags: vitest, axe-core, husky, test-infrastructure
Phase 38 is pure additive test infrastructure — no production code changes except a11y fixes. Split into 3 plans: (1) Vitest install + config + unit tests targeting pure lib/ modules, (2) axe-core Playwright a11y + reduced-motion specs, (3) husky + lint-staged pre-commit hooks. Plan 01 installs ALL deps in one pass so Plans 02+03 don't duplicate installs — forced Wave 2 dependency. The checkpoint in Plan 03 is essential — pre-commit hooks need human verification (interactive git commit). Key research insight: GSAP is NOT on window.gsap in Next.js bundles; test reduced-motion indirectly via DOM state. tsconfig.test.json is mandatory to isolate vitest/globals from Playwright test global.

### 2026-04-11T03:00:00Z | Phase 40 | tags: storybook, jsdoc-audit, auto-gen-script, documentation
Phase 40 is documentation-artifacts-only — no component or build pipeline changes. 3 plans in 2 waves. Wave 1 parallelizes Plan 01 (auto-gen api-docs.ts script + JSDoc sub-component audit across 51 SF files) with Plan 02 (README.md + MIGRATION.md in technical specimen tone). Wave 2 is Plan 03 (Storybook 10 greenfield setup + 40+ stories with branded theme), depends on Plan 01 because Storybook autodocs reads JSDoc. Key finding: lib/api-docs.ts (2203 lines) uses stale @sfux/* import paths that never existed — auto-gen script replaces all 107 hand-curated entries. Storybook is the largest task (45+ story files) but stays in one plan because the .storybook/ config and stories are tightly coupled. gsd-tools init still unavailable.

### 2026-04-11T23:30:00Z | Phase 44 | tags: copy-audit, single-plan, string-edits, playwright
Phase 44 is a pure accuracy pass — 1 plan, 2 tasks, 7 files. Task 1 edits 6 source files (stats-band, hero, marquee-band, page.tsx, init/page.tsx, opengraph-image.tsx) with exact string replacements. Task 2 updates Playwright test assertions last. All requirements (COP-01 through COP-06) fit in a single plan because every edit is a string replacement under 5 lines — no architectural changes, no new dependencies, no build tooling. Research was exceptionally thorough with exact line numbers and before/after strings, making planning near-trivial. Key decision: marquee "SHIP FASTER" replaced with "COMPOSABLE BY DESIGN" per Claude's discretion (research recommended it). gsd-tools init still unavailable.

### 2026-04-11T23:45:00Z | Phase 50.1 | tags: webgl, shader, single-plan, ambient-effect
Phase 50.1 (Datamosh Overlay) planned as single plan with 2 auto tasks + 1 visual checkpoint. Research doc was extremely detailed with complete component skeleton — planning was straightforward pattern-match. WebGL component phases with established codebase patterns (useSignalScene, INT-04, GSAP ticker) are Level 0 discovery and naturally fit 1 plan.

### 2026-04-11T01:00:00Z | Phase 45 | tags: token-rename, css-layer, build-pipeline, breaking-change
Phase 45 (Token Bridge) splits into 2 plans in 2 waves. Plan 01 (Wave 1) handles the CSS-only rename — lib/tokens.css and app/globals.css converted to @theme inline with --color-* aliasing var(--sfx-*), plus the wrap-tokens-layer.ts build script. Plan 02 (Wave 2) handles JS/TS string refs (~50 across 15 files), cd-tokens.css creation, and MIGRATION.md expansion, ending with a visual checkpoint. Key architectural decision: Approach A from research — keep --color-* in @theme for Tailwind utility compat, add --sfx-* as the value source. This avoids renaming ~112 Tailwind class references. The --font-inter/--font-anton/--font-jetbrains vars from next/font must NOT be renamed. globals.css at 1819 lines is the biggest single-file rename — splitting CSS from JS into separate plans prevents context exhaustion.

### 2026-04-12T04:00:00Z | Phase 46 | tags: token-normalization, find-replace, wcag-contrast, duration-prefix
Phase 46 (Tightening Pass) splits into 2 plans in 2 waves. Plan 01 (Wave 1) handles all duration token work: 15 hardcoded Tailwind durations replaced with --sfx-duration-* token refs + Category C prefix fixes (var(--duration-*) -> var(--sfx-duration-*), var(--ease-*) -> var(--sfx-ease-*)) + sf-button alignment to --sfx-duration-fast (TGH-02 + TGH-04). Plan 02 (Wave 2) handles 7 hardcoded color replacements + WCAG AA contrast verification (TGH-03 + TGH-01). Wave 2 forced by single file overlap (copy-button.tsx modified by both duration and color tasks). Research was extremely thorough — exact file+line inventory for every change, making plans maximally specific. Key discovery: unprefixed var(--duration-*) references are a latent bug (no such properties exist), fixing them is both correctness and normalization. TGH-01 is verification-only — contrast already passes at 5.81:1. color-cycle-frame.tsx and page-animations.tsx colors require getComputedStyle approach (GSAP can't read CSS vars natively).
