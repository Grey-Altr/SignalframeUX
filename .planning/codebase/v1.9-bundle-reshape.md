# v1.9 Bundle Reshape — Phase 67 BND-05 + BND-07

> Strictly diagnostic + lock. Findings + chunk-ID baseline.
> Successor to `v1.8-lcp-diagnosis.md` for v1.9 lock state.
> Plan 02 reads §1 outcome summary to choose D-06 outcome ladder branch.

**Captured:** 2026-04-30 (Phase 67, Plan 01)
**Build:** `rm -rf .next/cache .next && ANALYZE=true pnpm build` (production webpack)
**Method:** Programmatic chartData extraction from `.next/analyze/client.html` (autonomous override of manual treemap inspection — same source-of-truth, equivalent fidelity). Per BND-04 standing rule (D-10), every gating measurement was prefixed with `rm -rf .next/cache .next` (stale-chunk guard).

---

## 1. Reshape Vector Outcome Summary

| Vector | Action                                                                                                  | Pre (KB gz) | Post (KB gz) | Delta (KB gz) | D-02 floor (≥2 KB) | AES-04        | Status                |
| ------ | ------------------------------------------------------------------------------------------------------- | ----------- | ------------ | ------------- | ------------------ | ------------- | --------------------- |
| V1     | `@/components/sf` to optimizePackageImports + DCE SFScrollArea/SFNavigationMenu barrel exports          | 258.9       | 187.6        | -71.3         | PASS               | PASS 20/20    | COMMITTED (`c35544e`) |
| V2     | PageAnimations dynamic-import (gsap chunk lazy attempt — RESEARCH-corrects-CONTEXT framing per Task 0)  | 187.6       | 187.6        | 0.0           | FAIL               | N/A           | SKIPPED (D-02 floor)  |
| V3     | TooltipProviderLazy (hydration-gated wrapper)                                                           | 187.6       | 187.6        | 0.0           | FAIL               | N/A           | SKIPPED (D-02 floor)  |
| TOTAL  | —                                                                                                       | 258.9       | 187.6        | -71.3         | —                  | —             | —                     |

Final homepage `/` First Load JS: **187.6 KB gzip** (-71.3 KB / 28% reduction from pre-reshape 258.9 KB).
Plan 02 D-06 outcome ladder branch: **≤200 KB → retire `_path_k_decision` entirely**; restore `BUDGET_BYTES = 200 * 1024` per CLAUDE.md hard constraint.
Anticipated branch: **A (retire path_k)**.

**Vector 2/3 SKIP rationale:**
- **Vector 2** (PageAnimations dynamic-import): GSAP chunks `584bde89-*` (gsap entry, 19.4 KB gz) and `8964-*` (Observer + ScrollTrigger + index, 24.9 KB gz) remain in homepage First-Load JS post-V1. Investigation: `lib/gsap-core` is eagerly consumed by `lenis-provider.tsx:5` (PF-04 contract surface, synchronous `ScrollTrigger.update` at line 52) AND `scale-canvas.tsx:4` (pre-paint critical, `--sf-canvas-scale` math). Wrapping `PageAnimations` (the only OTHER static eager gsap consumer) in `next/dynamic({ ssr: false })` produced a 0 KB delta because the gsap chunks were not its exclusive owners. The PF-04 + pre-paint constraints make `lib/gsap-core` genuinely load-bearing in homepage First-Load JS. Per D-02 cost/risk floor: REVERT.

  This corrects RESEARCH.md §"Vector 2 — CRITICAL CORRECTION" which claimed chunk `8964` was "Next.js runtime auxiliary." Fresh post-Phase-66 build evidence (Task 0 `/tmp/67-pre-chunks.json`) shows `8964` contains `gsap@3.14.2/Observer.js + ScrollTrigger.js + index.js (62.6 KB parsed)`. RESEARCH's attribution was based on stale `v1.8-lcp-diagnosis.md §2b` data; the chunk-ID/package mapping shifted between Phase 57 capture and Phase 67 capture. ScrollSmoother is correctly NOT imported in the codebase (verified `grep -rn "ScrollSmoother" --include="*.ts" --include="*.tsx" lib/ components/ app/` → 0 hits).

- **Vector 3** (TooltipProviderLazy): Vector 1's `optimizePackageImports` + barrel DCE ALREADY removed chunk `7525-*` (TooltipProvider/react-remove-scroll, 26.0 KB gz pre-V1) from the homepage First-Load JS path (chunk dissolved post-V1). With chunk 7525 already lazy post-V1, deferring TooltipProvider mount via lazy wrapper produced no further reduction. The lazy-mount mechanism itself is architecturally correct (defensive against future tooltip consumers re-pulling the chunk into First-Load JS); deferred to a future phase if/when that pressure surfaces. Per D-02 cost/risk floor: REVERT.

---

## 2a. v1.9 stable chunk-ID lock (Phase 67 close)

Cross-referencing v1.8 named chunks (per `v1.8-lcp-diagnosis.md §2a`) against post-Phase-67 state. "v1.8 status" rows below come from the post-Phase-66 / pre-Phase-67 baseline (Task 0); "v1.9 status" rows are the locked Phase 67 close state.

| v1.8 chunk ID | v1.9 status (filename)                | Top packages by size (parsed/gzip)                              | Total chunk size       | Notes                                                                                                                                          |
| ------------- | ------------------------------------- | --------------------------------------------------------------- | ---------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------- |
| `7525`        | DISSOLVED (Vector 1)                  | (was: Radix popper 26.5 / react-remove-scroll 10.9 / tooltip 7.7 / dialog 5.5) | (was: 76.4 KB / 26.0 KB gz) | Removed from homepage `/page` manifest. The TooltipProvider chain moved into per-route lazy chunks via `optimizePackageImports: ["@/components/sf"]` barrel-rewrite. Plan 01 SUMMARY confirms 26.0 KB gzip headroom recovered. |
| `4335`        | DISSOLVED (Vector 1)                  | (was: Radix select 18.5 + scroll-area 12.0 + Radix sub-packages 81.6) | (was: 109.7 KB / 31.1 KB gz) | Removed from homepage `/page` manifest. The Radix-aggregate-via-barrel chain dissolved through DCE + per-file imports. 31.1 KB gzip headroom recovered. |
| `8964`        | PRESERVED — `8964-02d5beb63a80f3f5.js`| `gsap` 61.2 KB (Observer 9.0 + ScrollTrigger 34.3 + index 19.3) | 62.0 KB / 24.9 KB gz   | GSAP core (Observer + ScrollTrigger + gsap entry concatenated). Eagerly consumed by `lenis-provider.tsx` (PF-04 contract) and `scale-canvas.tsx` (pre-paint). Cannot be dynamic-imported without breaking PF-04 sync. **CRITICAL CORRECTION** to RESEARCH.md attribution (§5 Method Notes). |
| `584bde89`    | PRESERVED — `584bde89-478e5bcc7be5ae42.js` | `gsap` 50.4 KB (gsap-core.ts entry index)                  | 50.5 KB / 19.3 KB gz   | Separate gsap entry chunk (split point). Same constraint as 8964 — bound to `lib/gsap-core` static-import chain.                              |
| `2979`        | PRESERVED — `2979-7e3b1be684627f10.js`| `next` 166.5 KB                                                  | 168.4 KB / 44.8 KB gz  | Next.js runtime chunk. Framework floor — not Phase 67 optimization target.                                                                    |
| `5791061e`    | PRESERVED — `5791061e-b51f32ecb5a3272a.js` | `next` 168.9 KB (compiled `react-dom/cjs`)                | 169.0 KB / 53.0 KB gz  | Next.js compiled react-dom. Framework floor.                                                                                                  |
| `8843`        | DISSOLVED (Vector 1)                  | (was: tailwind-merge/dist 28.6 KB)                              | (was: 28.6 KB / 9.2 KB gz) | tailwind-merge moved to lazy chunks. Replaced by NEW chunk `289-*` (8.9 KB gz, see §2b).                                                      |
| `9046`        | DISSOLVED (Vector 1)                  | (was: Radix slider 13.3 KB)                                     | (was: 13.3 KB / 4.9 KB gz) | Radix slider moved to lazy chunks (homepage doesn't consume).                                                                                 |
| `7369`        | DISSOLVED (Vector 1)                  | (was: lenis 17.1 KB)                                            | (was: 17.2 KB / 4.9 KB gz) | Lenis dedicated chunk dissolved into NEW chunk `6309-*` (5.2 KB gz, see §2b).                                                                |
| `4458`        | DISSOLVED (Vector 1)                  | (was: gsap-core.ts 71.2 KB — overlap with 8964 + 584bde89)      | (was: 71.2 KB / 13.1 KB gz) | gsap chunk reshuffle: 4458's content rebalanced across 8964/584bde89 + new 5837 (component-registry).                                         |
| `2307`        | DISSOLVED (Vector 1)                  | (was: copy-button.tsx 20.6 KB)                                  | (was: 20.6 KB / 5.5 KB gz) | Layout-component chunk dissolved into the layout chunk + lazy chunks.                                                                         |
| `3228`        | PRESERVED — `3228-0c5979f2a433adaf.js`| `ScrambleTextPlugin.js` 10.8 / `SplitText.js` 6.9 / `CustomEase.js` 6.2 | 24.0 KB / 9.8 KB gz  | gsap-plugins chunk (ScrambleText, SplitText, CustomEase). Used by hero animations.                                                            |

**11 v1.8 chunks → 5 preserved + 6 dissolved**. Net: 7 dissolved chunks (8843, 9046, 7369, 4458, 2307, 7525, 4335) representing 110.0 KB gzip pre-V1. New chunks introduced post-reshape (see §2b) total ~33 KB gzip. Net First-Load JS reduction = 71.3 KB gzip (matches Vector 1 measurement).

## 2b. New chunks introduced by Phase 67 reshape

Chunks present in post-Phase-67 `/page` manifest that were NOT in the pre-Phase-67 set (intersection: `comm -13 pre.txt post.txt`):

| Chunk filename                       | Size (parsed/gzip)    | Top packages                                              | Origin                                                                                                                                                  |
| ------------------------------------ | --------------------- | --------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `289-21d7e8dcd1977007.js`            | 27.8 KB / 8.9 KB gz   | `tailwind-merge` 25.5 / `clsx` 0.4                        | tailwind-merge + clsx aggregate (replaces dissolved 8843). The `cn()` utility used everywhere keeps these eagerly bundled.                              |
| `9067-c4993fc134c463a8.js`           | 8.3 KB / 3.3 KB gz    | `shared` 4.6 / `client` 3.6                               | Webpack splitChunks shared/client runtime helpers. Auto-emitted by Next 15 webpack config; no source-attribution.                                       |
| `6309-ce962777f8910e27.js`           | 17.7 KB / 5.2 KB gz   | `lenis` 17.1                                              | Lenis chunk (replaces dissolved 7369). Eagerly bundled — `LenisProvider` is at root layout and PF-04 contract surface.                                 |
| `5837-a116bffc877132b7.js`           | 22.1 KB / 6.1 KB gz   | `component-registry.ts` 17.4 / `gsap-core.ts` 0.2 / `gsap-split.ts` 0.2 | NEW: SF component-registry chunk surfaced after barrel reshape. component-registry.ts is the typed inventory used by /inventory and component-detail. |
| `webpack-aa6002c96a1b521d.js`        | 4.4 KB / 2.3 KB gz    | (webpack runtime)                                         | Webpack runtime — re-emitted with new content-hash after splitChunks reshape (every reshape produces a new webpack-* hash).                            |
| `app/page-59e56d4233758045.js`       | 35.3 KB / 9.4 KB gz   | `next` 2.8 / `node_modules` 0.4                           | Homepage entry chunk — re-emitted with new content-hash. Renamed from `app/page-5e497a1e84043ce7.js` (dissolved).                                       |

**Total new-chunk gzip overhead:** ~35.2 KB (8.9 + 3.3 + 5.2 + 6.1 + 2.3 + 9.4). Net offset against 110.0 KB gzip dissolved chunks = -71.3 KB net reduction (matches §1 V1 delta).

## 3. Pre/post homepage First-Load JS comparison

| Stage                                  | Homepage gzip total (KB) | Source                          |
| -------------------------------------- | ------------------------ | ------------------------------- |
| v1.8-pre-Phase-67 (Task 0)             | 258.9                    | `/tmp/67-build-pre-budget.log`  |
| post-Vector-1                          | 187.6                    | `/tmp/67-budget-v1.log`         |
| post-Vector-2 (no-op, REVERTED)        | 187.6                    | `/tmp/67-budget-v2.log`         |
| post-Vector-3 (no-op, REVERTED)        | 187.6                    | `/tmp/67-budget-v3.log`         |
| FINAL (post-reshape lock)              | 187.6                    | `/tmp/67-budget-final.log`      |
| TARGET (CLAUDE.md hard constraint)     | 200.0                    | —                               |
| path_k (v1.8 ratification)             | 260.0                    | `tests/v1.8-phase63-1-bundle-budget.spec.ts:59` |

**Outcome:** Final 187.6 KB is **12.4 KB UNDER** the CLAUDE.md 200 KB hard target — Plan 02 retires `_path_k_decision` entirely (D-06 ladder branch A) and restores `BUDGET_BYTES = 200 * 1024`.

## 4. PF-04 + Single-Ticker Invariant Check

- **`components/layout/lenis-provider.tsx`**: `autoResize: true` preserved (verified `grep -c "autoResize: true" components/layout/lenis-provider.tsx == 1`). `lib/gsap-core` static import at line 5 unchanged — ScrollTrigger.update synchronous sync at line 52 intact.
- **`lib/gsap-core.ts`**: NOT dynamic-imported (verified `grep -c "from \"@/lib/gsap-core\"" components/layout/lenis-provider.tsx == 1`). The static-import path is preserved per PF-04 contract (commit `73311e0` code-of-record).
- **No new rAF call sites** (single-ticker rule, ROADMAP §v1.9 rule 7): verified `grep -rn "requestAnimationFrame" components/providers/ components/layout/page-animations*` returns 0 matches in any new file (Plan 01 created 0 new files in the final lock state — Vector 2 + Vector 3 both reverted).
- **ScrollSmoother absence confirmed**: `grep -rn "ScrollSmoother" --include="*.ts" --include="*.tsx" lib/ components/ app/` returns 0 matches.

## 5. Method Notes

**Build invocation:** `rm -rf .next/cache .next && ANALYZE=true pnpm build` (BND-04 stale-chunk guard, D-10 standing rule). Five gating builds executed in Plan 01: Task 0 baseline, Vector 1 commit (`/tmp/67-build-v1.log`), Vector 2 attempt (`/tmp/67-build-v2.log`), Vector 3 attempt (`/tmp/67-build-v3.log`), Task 5 final (`/tmp/67-build-final.log`).

**Analyzer:** `@next/bundle-analyzer` via Next 15.5.14 (next.config.ts:2,4-6).

**Per-vector AES-04 cadence:** D-08 — every reshape commit verified against `.planning/visual-baselines/v1.8-start/` (strict desktop+ipad) and `.planning/visual-baselines/v1.9-pre/` (cohort mobile+iphone, capture-only with dimensional drift tolerance per Phase 66 ARC-02 pillarbox precedent — see `tests/v1.9-phase66-aes04-diff.spec.ts:176`). Phase 67 spec at `tests/v1.9-phase67-aes04.spec.ts` (5 routes × 4 viewports = 20 captures). Vector 1 PASSED 20/20.

**Vector 2 attribution correction:** RESEARCH.md §"Vector 2 — CRITICAL CORRECTION" claimed chunk `8964` = "Next.js runtime auxiliary" per `v1.8-lcp-diagnosis.md §2b line 82`. **Fresh post-Phase-66 build evidence (Task 0 `/tmp/67-pre-chunks.json`) shows chunk 8964 actually contains `gsap@3.14.2/Observer.js + ScrollTrigger.js + index.js` (62.6 KB parsed / 24.9 KB gzip).** The RESEARCH attribution was based on stale Phase 57 chunk-mapping data; webpack splitChunks reshuffled between Phase 57 and Phase 67 (multiple ratification cycles). The 24.9 KB gzip size is stable, but the package content changed. ScrollSmoother is correctly NOT imported in the codebase (verified empirically). The `_path_k_decision` comment at `tests/v1.8-phase63-1-bundle-budget.spec.ts:43` ("8964 = GSAP ScrollSmoother + ScrollTrigger") is closer to current truth than RESEARCH.md acknowledged — it correctly identifies GSAP, just not the specific plugin.

**CONTEXT D-01 Vector 2 divergence:** CONTEXT.md D-01 named "GSAP ScrollSmoother → dynamic import" as the Vector 2 target. Vector 2 was framed against the actual eager gsap-bearing module (`PageAnimations`). Outcome: SKIPPED at D-02 floor due to `lib/gsap-core` being load-bearing for PF-04 + pre-paint scale-canvas. The chunk-id reshape itself (V1) provided the 71.3 KB win that V2 was designed to contribute toward.

**Plan 02 hand-off:** Reads §1 outcome summary to choose D-06 outcome ladder branch. Final 187.6 KB → branch A (retire `_path_k_decision`, restore 200 KB target). Plan 02 mutates `tests/v1.8-phase63-1-bundle-budget.spec.ts:59` `BUDGET_BYTES = 200 * 1024` and removes the path_k header block.
