---
phase: 05-dx-contract-state
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - components/sf/sf-badge.tsx
  - components/sf/sf-button.tsx
  - components/sf/sf-card.tsx
  - components/sf/sf-checkbox.tsx
  - components/sf/sf-command.tsx
  - components/sf/sf-container.tsx
  - components/sf/sf-dialog.tsx
  - components/sf/sf-dropdown-menu.tsx
  - components/sf/sf-grid.tsx
  - components/sf/sf-input.tsx
  - components/sf/sf-label.tsx
  - components/sf/sf-popover.tsx
  - components/sf/sf-radio-group.tsx
  - components/sf/sf-scroll-area.tsx
  - components/sf/sf-section.tsx
  - components/sf/sf-select.tsx
  - components/sf/sf-separator.tsx
  - components/sf/sf-sheet.tsx
  - components/sf/sf-skeleton.tsx
  - components/sf/sf-slider.tsx
  - components/sf/sf-stack.tsx
  - components/sf/sf-switch.tsx
  - components/sf/sf-table.tsx
  - components/sf/sf-tabs.tsx
  - components/sf/sf-text.tsx
  - components/sf/sf-textarea.tsx
  - components/sf/sf-toggle.tsx
  - components/sf/sf-tooltip.tsx
autonomous: true
requirements: [DX-03]

must_haves:
  truths:
    - "Every SF-wrapped component in sf/ has a JSDoc block with at minimum one usage example that renders correctly"
    - "Sub-exports of compound components have one-liner JSDoc describing their role"
    - "JSDoc blocks follow consistent format: description, @param for variants, @example"
  artifacts:
    - path: "components/sf/sf-button.tsx"
      provides: "JSDoc reference implementation for Pattern A"
      contains: "@example"
    - path: "components/sf/sf-container.tsx"
      provides: "JSDoc reference implementation for Pattern B"
      contains: "@example"
    - path: "components/sf/sf-text.tsx"
      provides: "JSDoc reference implementation for Pattern C"
      contains: "@example"
  key_links:
    - from: "components/sf/*.tsx"
      to: "docs/SCAFFOLDING.md"
      via: "JSDoc format matches documented canonical pattern"
      pattern: "@example"
---

<objective>
Add JSDoc blocks to all 28 exported SF component functions in components/sf/. Every primary export gets a full JSDoc block (description, @param for variant props, @example). Sub-exports of compound components get one-liner JSDoc.

Purpose: Make every SF component self-documenting for developers and AI agents — hover docs show usage without leaving the editor.
Output: 28 component files updated with JSDoc coverage
</objective>

<execution_context>
@.planning/phases/05-dx-contract-state/05-RESEARCH.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-dx-contract-state/05-CONTEXT.md
@components/sf/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: JSDoc sweep — layout primitives + interactive A-L (14 files)</name>
  <files>
    components/sf/sf-container.tsx, components/sf/sf-section.tsx, components/sf/sf-stack.tsx,
    components/sf/sf-grid.tsx, components/sf/sf-text.tsx,
    components/sf/sf-badge.tsx, components/sf/sf-button.tsx, components/sf/sf-card.tsx,
    components/sf/sf-checkbox.tsx, components/sf/sf-command.tsx, components/sf/sf-dialog.tsx,
    components/sf/sf-dropdown-menu.tsx, components/sf/sf-input.tsx, components/sf/sf-label.tsx
  </files>
  <read_first>
    - components/sf/sf-button.tsx (Pattern A reference — read for JSDoc placement on named function)
    - components/sf/sf-container.tsx (Pattern B reference — read for JSDoc placement on const forwardRef)
    - components/sf/sf-text.tsx (Pattern C reference — read for JSDoc placement on polymorphic forwardRef)
    - components/sf/sf-card.tsx (compound component — read for sub-export pattern)
    - components/sf/sf-command.tsx (most sub-exports — read for one-liner pattern)
    - components/sf/sf-dialog.tsx (compound — read for sub-export count)
    - components/sf/sf-dropdown-menu.tsx (compound — read for sub-export count)
    - .planning/phases/05-dx-contract-state/05-RESEARCH.md (JSDoc patterns, pitfall 1 on forwardRef, pitfall 2 on sub-export scope)
    - Each remaining file in this task's file list before adding JSDoc
  </read_first>
  <action>
Add JSDoc blocks to 14 SF component files. Follow this format precisely:

**Primary exports (full block):**
```typescript
/**
 * [One-line description] -- FRAME layer [category] primitive.
 *
 * [1-2 sentences on what it enforces/provides from the token system]
 *
 * @param [variant-prop] - [description]. [allowed values quoted]
 * @param className - Merged via cn() after variant classes
 *
 * @example
 * <SFComponent prop="value">Content</SFComponent>
 */
```

**Sub-exports (one-liner):**
```typescript
/** Sub-component of [Parent] -- renders the [region description]. */
```

Per component:

1. **SFContainer** (Pattern B): "Responsive page container" -- width variant ("wide" | "content" | "full"), enforces max-width tokens and responsive gutters.
2. **SFSection** (Pattern B): "Semantic page section" -- spacing variant, auto-applies data-section, optional data-section-label and data-bg-shift.
3. **SFStack** (Pattern B): "Vertical rhythm container" -- gap variant maps to blessed spacing stops, optional align.
4. **SFGrid** (Pattern B): "Responsive column grid" -- cols variant, responsive breakpoint behavior.
5. **SFText** (Pattern C): "Semantic text primitive" -- variant maps to typography aliases, polymorphic `as` prop.
6. **SFBadge** (Pattern A): "Status indicator badge" -- intent variant.
7. **SFButton** (Pattern A): "Primary action button" -- intent + size variants, asymmetric hover, press transform.
8. **SFCard** (Pattern A, compound): Primary + one-liner on SFCardHeader, SFCardTitle, SFCardDescription, SFCardContent, SFCardFooter.
9. **SFCheckbox** (Pattern A): "Toggle checkbox input" -- inherits Radix CheckboxProps.
10. **SFCommand** (Pattern A, compound): Primary + one-liner on 9 sub-exports.
11. **SFDialog** (Pattern A, compound): Primary "Modal dialog" + one-liner on 7 sub-exports.
12. **SFDropdownMenu** (Pattern A, compound): Primary "Context menu" + one-liner on 7 sub-exports.
13. **SFInput** (Pattern A): "Text input field" -- enforces SF styling contract.
14. **SFLabel** (Pattern A): "Form field label" -- associates with input via htmlFor.

Read each file BEFORE writing JSDoc. Place JSDoc immediately above the export function/const declaration. Do NOT modify any logic or styling. Only add JSDoc comment blocks.

Each @example must use actual props from the component's interface. Keep examples to 1-2 lines.
  </action>
  <acceptance_criteria>
    - AC-1: All 14 files have `/** ... */` JSDoc blocks on primary exports
    - AC-2: All compound component sub-exports have one-liner JSDoc
    - AC-3: Every primary JSDoc block contains `@example` with valid JSX
    - AC-4: No component logic or styling modified -- only JSDoc comments added
  </acceptance_criteria>
  <ac_refs>AC-1, AC-2, AC-3, AC-4</ac_refs>
  <verify>
    <automated>for f in sf-container sf-section sf-stack sf-grid sf-text sf-badge sf-button sf-card sf-checkbox sf-command sf-dialog sf-dropdown-menu sf-input sf-label; do grep -q "@example" "components/sf/${f}.tsx" && echo "PASS: ${f}" || echo "FAIL: ${f}"; done</automated>
  </verify>
  <done>14 SF component files have JSDoc blocks: primary exports with full block (description, @param, @example), sub-exports with one-liners</done>
</task>

<task type="auto">
  <name>Task 2: JSDoc sweep — interactive M-Z (14 files)</name>
  <files>
    components/sf/sf-popover.tsx, components/sf/sf-radio-group.tsx, components/sf/sf-scroll-area.tsx,
    components/sf/sf-select.tsx, components/sf/sf-separator.tsx, components/sf/sf-sheet.tsx,
    components/sf/sf-skeleton.tsx, components/sf/sf-slider.tsx, components/sf/sf-switch.tsx,
    components/sf/sf-table.tsx, components/sf/sf-tabs.tsx, components/sf/sf-textarea.tsx,
    components/sf/sf-toggle.tsx, components/sf/sf-tooltip.tsx
  </files>
  <read_first>
    - Each file in this task's file list before adding JSDoc
    - components/sf/sf-sheet.tsx (compound -- read for sub-export count)
    - components/sf/sf-select.tsx (compound -- read for sub-export count)
    - components/sf/sf-table.tsx (compound -- read for sub-export count)
    - components/sf/sf-tabs.tsx (compound -- read for sub-export count)
    - components/sf/sf-popover.tsx (compound -- read for sub-export count)
    - .planning/phases/05-dx-contract-state/05-RESEARCH.md (JSDoc patterns)
  </read_first>
  <action>
Add JSDoc blocks to remaining 14 SF component files. Same format as Task 1.

Per component:

15. **SFPopover** (Pattern A, compound): Primary "Floating content panel" + one-liner on sub-exports.
16. **SFRadioGroup** (Pattern A): Primary "Radio selection group" + SFRadioGroupItem one-liner.
17. **SFScrollArea** (Pattern A): Primary "Custom scroll container" + SFScrollBar one-liner.
18. **SFSelect** (Pattern A, compound): Primary "Dropdown select input" + one-liner on sub-exports.
19. **SFSeparator** (Pattern A): "Visual divider" -- orientation variant.
20. **SFSheet** (Pattern A, compound): Primary "Slide-out panel" + one-liner on sub-exports.
21. **SFSkeleton** (Pattern A): "Loading placeholder" -- renders animated shimmer.
22. **SFSlider** (Pattern A): "Range input slider" -- inherits Radix SliderProps.
23. **SFSwitch** (Pattern A): "Toggle switch input" -- inherits Radix SwitchProps.
24. **SFTable** (Pattern A, compound): Primary "Data table" + one-liner on sub-exports (Header, Body, Row, Head, Cell, Caption, Footer).
25. **SFTabs** (Pattern A, compound): Primary "Tabbed interface" + one-liner on SFTabsList, SFTabsTrigger, SFTabsContent.
26. **SFTextarea** (Pattern A): "Multi-line text input" -- enforces SF styling contract.
27. **SFToggle** (Pattern A): "Pressable toggle button" -- intent + size variants.
28. **SFTooltip** (Pattern A, compound): Primary "Hover tooltip" + one-liner on sub-exports.

Read each file BEFORE writing JSDoc. Place JSDoc immediately above the export function/const declaration. Do NOT modify any logic or styling. Only add JSDoc comment blocks.

Each @example must use actual props from the component's interface. Keep examples to 1-2 lines.
  </action>
  <acceptance_criteria>
    - AC-5: All 14 files have `/** ... */` JSDoc blocks on primary exports
    - AC-6: All compound component sub-exports have one-liner JSDoc
    - AC-7: Every primary JSDoc block contains `@example` with valid JSX
    - AC-8: No component logic or styling modified -- only JSDoc comments added
    - AC-9: Full sweep complete -- all 28 SF component files have JSDoc coverage
  </acceptance_criteria>
  <ac_refs>AC-5, AC-6, AC-7, AC-8, AC-9</ac_refs>
  <verify>
    <automated>for f in sf-popover sf-radio-group sf-scroll-area sf-select sf-separator sf-sheet sf-skeleton sf-slider sf-switch sf-table sf-tabs sf-textarea sf-toggle sf-tooltip; do grep -q "@example" "components/sf/${f}.tsx" && echo "PASS: ${f}" || echo "FAIL: ${f}"; done && echo "---" && TOTAL=$(grep -rl "@example" components/sf/*.tsx 2>/dev/null | wc -l) && echo "Total files with @example: ${TOTAL}/28"</automated>
  </verify>
  <done>All 28 SF component files have JSDoc coverage -- primary exports with full blocks, sub-exports with one-liners, zero logic changes</done>
</task>

</tasks>

<verification>
- `grep -rl "@example" components/sf/*.tsx | wc -l` returns 28
- `grep -rl "/\*\*" components/sf/*.tsx | wc -l` returns 28
- `npx next build` passes (no JSDoc syntax errors breaking TypeScript)
- No component logic or styling modified in any file
</verification>

<success_criteria>
- AC-1 through AC-9 pass
- All 28 SF component files have JSDoc with @example on primary exports
- Compound component sub-exports have one-liner JSDoc
- Zero logic or styling changes -- documentation only
</success_criteria>

<output>
After completion, create `.planning/phases/05-dx-contract-state/05-02-SUMMARY.md`
</output>
