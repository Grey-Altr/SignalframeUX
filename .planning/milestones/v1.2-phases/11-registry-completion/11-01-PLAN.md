---
phase: 11-registry-completion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - registry.json
  - package.json
  - public/r/
autonomous: true
requirements:
  - DX-04

must_haves:
  truths:
    - "registry.json lists all 33 items (27 existing + 5 layout primitives + sf-theme)"
    - "Every registry item has meta.layer ('frame' or 'signal') and meta.pattern ('A', 'B', or 'C')"
    - "sf-theme is type registry:style with cssVars and no files array"
    - "public/r/ contains a JSON file for every registry item after shadcn build"
    - "pnpm registry:build script exists in package.json and runs without error"
  artifacts:
    - path: "registry.json"
      provides: "Complete registry with 33 items + meta fields"
      contains: "sf-container"
    - path: "registry.json"
      provides: "sf-theme entry"
      contains: "sf-theme"
    - path: "public/r/sf-container.json"
      provides: "Built registry item for sf-container"
    - path: "public/r/sf-theme.json"
      provides: "Built registry item for sf-theme with cssVars"
    - path: "package.json"
      provides: "registry:build script"
      contains: "registry:build"
  key_links:
    - from: "registry.json"
      to: "public/r/*.json"
      via: "shadcn build reads registry.json and generates public/r/ files"
      pattern: "shadcn build"
    - from: "registry.json items[].files[].path"
      to: "components/sf/*.tsx"
      via: "file path references resolved by shadcn build"
      pattern: "components/sf/"
---

<objective>
Complete the shadcn CLI registry so all 33 items (27 existing interactive/animation + 5 layout primitives + sf-theme token entry) are listed in registry.json with meta.layer and meta.pattern fields, and public/r/ is rebuilt via shadcn build.

Purpose: DX-04 — any SF component or the token system can be installed via `pnpm shadcn add [name]` from the registry.
Output: Complete registry.json (33 items with meta), rebuilt public/r/ directory, registry:build script in package.json.
</objective>

<execution_context>
@/Users/greyaltaer/.claude/pde-os/engines/gsd/workflows/execute-plan.md
@/Users/greyaltaer/.claude/pde-os/engines/gsd/templates/summary.md
</execution_context>

<context>
@.planning/project-context.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-registry-completion/11-RESEARCH.md
</context>

<acceptance_criteria>
- AC-1: registry.json contains exactly 33 items in items array (27 existing + sf-container + sf-section + sf-grid + sf-stack + sf-text + sf-theme)
- AC-2: Every item in registry.json has a "meta" object with "layer" (string: "frame" or "signal") and "pattern" (string: "A", "B", or "C")
- AC-3: sf-theme item has type "registry:style", includes cssVars object with theme/light/dark sections, and has NO files array
- AC-4: All 5 layout primitives (sf-container, sf-section, sf-grid, sf-stack, sf-text) have type "registry:ui" and files pointing to components/sf/
- AC-5: package.json contains "registry:build": "shadcn build" in scripts
- AC-6: `pnpm registry:build` exits 0 and public/r/ contains at least 32 JSON files (one per component; sf-theme may or may not produce a file depending on shadcn build behavior with registry:style)
- AC-7: public/r/sf-section.json embedded source contains bgShift typed as string union (not boolean) — confirming Phase 10 fix is captured
</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Complete registry.json with all 33 items and meta fields</name>
  <files>registry.json</files>
  <read_first>
    - registry.json
    - .planning/phases/11-registry-completion/11-RESEARCH.md
    - components/sf/sf-container.tsx
    - components/sf/sf-section.tsx
    - components/sf/sf-grid.tsx
    - components/sf/sf-stack.tsx
    - components/sf/sf-text.tsx
  </read_first>
  <ac_refs>AC-1, AC-2, AC-3, AC-4</ac_refs>
  <boundaries>
    - DO NOT modify any file in components/sf/ or components/animation/
    - DO NOT modify public/r/ (that happens in Task 2 via shadcn build)
    - DO NOT remove or rename any existing registry item
  </boundaries>
  <action>
Update registry.json to contain exactly 33 items with complete meta fields.

**Step 1 — Backfill meta on all 27 existing items.** Add a "meta" object to each existing item using this classification table:

| Name | layer | pattern |
|------|-------|---------|
| sf-button | frame | B |
| sf-card | frame | A |
| sf-input | frame | A |
| sf-badge | frame | B |
| sf-tabs | frame | A |
| sf-table | frame | A |
| sf-separator | frame | A |
| sf-tooltip | frame | A |
| sf-dialog | frame | A |
| sf-sheet | frame | A |
| sf-dropdown-menu | frame | A |
| sf-toggle | frame | B |
| sf-slider | frame | A |
| sf-command | frame | A |
| sf-skeleton | frame | A |
| sf-popover | frame | A |
| sf-scroll-area | frame | A |
| sf-label | frame | A |
| sf-select | frame | A |
| sf-checkbox | frame | A |
| sf-radio-group | frame | A |
| sf-switch | frame | A |
| sf-textarea | frame | A |
| circuit-divider | signal | C |
| vhs-overlay | signal | C |
| hero-mesh | signal | C |
| scramble-text | signal | C |

Pattern key: A = thin SF wrap of shadcn/Radix base, B = CVA-driven with no Radix base, C = animation/standalone with no shadcn dependency.

**Step 2 — Add 5 layout primitive entries** after the existing 27 items:

sf-container: type "registry:ui", dependencies ["class-variance-authority"], files [{"path": "components/sf/sf-container.tsx", "type": "registry:ui"}], meta {"layer": "frame", "pattern": "B"}, title "SF Container", description "Responsive page container enforcing max-width tokens and responsive gutters. FRAME layer."

sf-section: type "registry:ui", files [{"path": "components/sf/sf-section.tsx", "type": "registry:ui"}], meta {"layer": "frame", "pattern": "B"}, title "SF Section", description "Page section with data attributes for animation and bgShift color palette. FRAME layer."

sf-grid: type "registry:ui", dependencies ["class-variance-authority"], files [{"path": "components/sf/sf-grid.tsx", "type": "registry:ui"}], meta {"layer": "frame", "pattern": "B"}, title "SF Grid", description "Responsive grid with blessed column counts and gap tokens. FRAME layer."

sf-stack: type "registry:ui", dependencies ["class-variance-authority"], files [{"path": "components/sf/sf-stack.tsx", "type": "registry:ui"}], meta {"layer": "frame", "pattern": "B"}, title "SF Stack", description "Flex stack with blessed gap tokens for vertical/horizontal layouts. FRAME layer."

sf-text: type "registry:ui", files [{"path": "components/sf/sf-text.tsx", "type": "registry:ui"}], meta {"layer": "frame", "pattern": "B"}, title "SF Text", description "Polymorphic text component with semantic typography aliases. FRAME layer."

Check each component source file to confirm whether it imports CVA — only list "class-variance-authority" in dependencies if the component actually uses it. sf-section and sf-text likely do NOT use CVA.

**Step 3 — Add sf-theme entry** as the final item:

```json
{
  "name": "sf-theme",
  "type": "registry:style",
  "title": "SF Theme",
  "description": "SignalframeUX token system — OKLCH color palette, zero-radius, motion tokens, SIGNAL runtime vars. Token-only installation, no component files.",
  "dependencies": [],
  "cssVars": {
    "theme": {
      "color-background": "oklch(1 0 0)",
      "color-foreground": "oklch(0.145 0 0)",
      "color-card": "oklch(1 0 0)",
      "color-card-foreground": "oklch(0.145 0 0)",
      "color-primary": "oklch(0.65 0.3 350)",
      "color-primary-foreground": "oklch(0.985 0 0)",
      "color-secondary": "oklch(0.970 0.005 298)",
      "color-secondary-foreground": "oklch(0.205 0 0)",
      "color-muted": "oklch(0.930 0.005 298)",
      "color-muted-foreground": "oklch(0.460 0.010 298)",
      "color-accent": "oklch(0.930 0.005 298)",
      "color-accent-foreground": "oklch(0.205 0 0)",
      "color-destructive": "oklch(0.550 0.180 25)",
      "color-border": "oklch(0.205 0 0)",
      "color-input": "oklch(0.205 0 0)",
      "color-ring": "oklch(0.65 0.3 350)",
      "radius": "0px"
    },
    "light": {
      "signal-intensity": "0.5",
      "signal-speed": "1",
      "signal-accent": "0"
    },
    "dark": {
      "color-background": "oklch(0.145 0 0)",
      "color-foreground": "oklch(0.985 0 0)",
      "color-card": "oklch(0.205 0 0)",
      "color-card-foreground": "oklch(0.985 0 0)",
      "color-border": "oklch(0.400 0 0)",
      "color-input": "oklch(0.400 0 0)"
    }
  },
  "meta": {
    "layer": "frame",
    "pattern": "A"
  }
}
```

sf-theme must NOT have a "files" array. It uses "registry:style" type (not "registry:ui").

**Final validation:** Count items array length — must be exactly 33.
  </action>
  <verify>
    <automated>node -e "const r=require('./registry.json'); const n=r.items.length; if(n!==33){console.error('Expected 33 items, got '+n);process.exit(1)} r.items.forEach(i=>{if(!i.meta?.layer||!i.meta?.pattern){console.error('Missing meta on '+i.name);process.exit(1)}}); const sf=r.items.find(i=>i.name==='sf-theme'); if(!sf||sf.type!=='registry:style'||!sf.cssVars||sf.files){console.error('sf-theme invalid');process.exit(1)} const lp=['sf-container','sf-section','sf-grid','sf-stack','sf-text']; lp.forEach(name=>{const it=r.items.find(i=>i.name===name); if(!it){console.error('Missing '+name);process.exit(1)}}); console.log('registry.json valid: 33 items, all meta present, sf-theme correct')"</automated>
  </verify>
  <done>registry.json contains 33 items. All items have meta.layer and meta.pattern. sf-theme is registry:style with cssVars and no files. All 5 layout primitives are present as registry:ui.</done>
</task>

<task type="auto">
  <name>Task 2: Add registry:build script and rebuild public/r/</name>
  <files>package.json, public/r/</files>
  <read_first>
    - package.json
    - registry.json
    - public/r/base.json
  </read_first>
  <ac_refs>AC-5, AC-6, AC-7</ac_refs>
  <boundaries>
    - DO NOT modify registry.json (completed in Task 1)
    - DO NOT modify any component source files
    - DO NOT delete public/r/base.json — back it up before build and restore if shadcn build wipes it
  </boundaries>
  <action>
**Step 1 — Back up base.json.** Copy `public/r/base.json` to a temp location. Research flagged uncertainty about whether `shadcn build` wipes the entire `public/r/` directory or only writes listed items. Safety first.

```bash
cp public/r/base.json /tmp/sf-base-backup.json
```

**Step 2 — Add registry:build script to package.json.** Add to the "scripts" object:

```json
"registry:build": "shadcn build"
```

Place it after the existing "lint" script.

**Step 3 — Run the build.**

```bash
pnpm registry:build
```

If this fails, try `pnpm dlx shadcn build` as fallback (the CLI may not be installed as a devDependency).

**Step 4 — Verify base.json survived.** Check if `public/r/base.json` still exists. If it was deleted by the build, restore from backup:

```bash
cp /tmp/sf-base-backup.json public/r/base.json
```

**Step 5 — Count output files.** Verify `public/r/` has at least 32 component JSON files (33 registry items minus sf-theme which may not produce an output file as a registry:style item).

**Step 6 — Spot-check sf-section.json.** Read `public/r/sf-section.json` and verify the embedded source content contains the string union type for bgShift (e.g., `"white" | "black"`) and NOT `boolean`. This confirms the Phase 10 type fix is captured in the registry build output.

**Step 7 — Spot-check circuit-divider.json.** Verify the built file retains `"gsap"` in its dependencies field.

**Step 8 — Spot-check sf-theme.json.** If it was generated, verify it has cssVars. If it was NOT generated (shadcn build may skip registry:style items), manually create `public/r/sf-theme.json` by extracting the sf-theme entry from registry.json and formatting it as a standalone registry-item JSON file with the $schema field:

```json
{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "sf-theme",
  "type": "registry:style",
  "title": "SF Theme",
  "description": "SignalframeUX token system — OKLCH color palette, zero-radius, motion tokens, SIGNAL runtime vars.",
  "cssVars": { ... },
  "meta": { "layer": "frame", "pattern": "A" }
}
```

Copy the full cssVars object from registry.json's sf-theme entry.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const files=fs.readdirSync('public/r').filter(f=>f.endsWith('.json')&&f!=='registry.json'); console.log('public/r/ JSON count:',files.length); if(files.length<32){console.error('Expected >=32, got '+files.length);process.exit(1)} const sec=JSON.parse(fs.readFileSync('public/r/sf-section.json','utf8')); const src=sec.files?.[0]?.content||''; if(!src.includes('white')||!src.includes('black')){console.error('sf-section bgShift not string union');process.exit(1)} if(fs.existsSync('public/r/base.json')){console.log('base.json preserved')}else{console.error('base.json MISSING');process.exit(1)} console.log('All checks passed')"</automated>
  </verify>
  <done>package.json has registry:build script. public/r/ contains JSON files for all registry items. sf-section.json has correct bgShift string union type. base.json preserved. sf-theme.json exists with cssVars.</done>
</task>

</tasks>

<verification>
1. `node -e "const r=require('./registry.json'); console.log('Items:', r.items.length)"` — prints "Items: 33"
2. `node -e "const r=require('./registry.json'); r.items.forEach(i=>{if(!i.meta?.layer)throw new Error(i.name)}); console.log('All meta present')"` — no error
3. `ls public/r/*.json | wc -l` — at least 33 (32 components + base.json + possibly registry.json)
4. `node -e "const t=require('./public/r/sf-theme.json'); if(!t.cssVars) process.exit(1); console.log('sf-theme valid')"` — prints "sf-theme valid"
5. `grep 'registry:build' package.json` — matches
</verification>

<success_criteria>
- registry.json has 33 items with meta.layer and meta.pattern on every item
- sf-theme is registry:style with cssVars, no files array
- 5 layout primitives are registry:ui pointing to components/sf/
- public/r/ rebuilt with all component JSON files
- sf-section.json reflects Phase 10 bgShift string union fix
- base.json preserved through rebuild
- package.json has registry:build script
</success_criteria>

<output>
After completion, create `.planning/phases/11-registry-completion/11-01-SUMMARY.md`
</output>
