---
phase: 15-documentation-cleanup
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/SCAFFOLDING.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [DOC-01]

must_haves:
  truths:
    - "SCAFFOLDING.md documents the createSignalframeUX factory and useSignalframe hook with correct types"
    - "SFSection JSDoc already reflects bgShift: 'white' | 'black' (verified, no edit needed)"
    - "DOC-01 checkbox in current REQUIREMENTS.md is marked complete"
  artifacts:
    - path: "docs/SCAFFOLDING.md"
      provides: "Section 8: Config Provider API with full useSignalframe contract"
      contains: "useSignalframe"
    - path: ".planning/REQUIREMENTS.md"
      provides: "DOC-01 marked complete"
      contains: "[x] **DOC-01"
  key_links:
    - from: "docs/SCAFFOLDING.md Section 8"
      to: "lib/signalframe-provider.tsx"
      via: "API contract documents actual exported types"
      pattern: "createSignalframeUX"
---

<objective>
Add the useSignalframe() API contract section to SCAFFOLDING.md, verify SFSection JSDoc is already correct, and mark DOC-01 complete in REQUIREMENTS.md.

Purpose: Complete the remaining DOC-01 sub-targets — the API contract documentation and final requirement closure.

Output: Updated SCAFFOLDING.md with Section 8, verified SFSection JSDoc, DOC-01 marked complete.
</objective>

<execution_context>
@/Users/greyaltaer/.claude/pde-os/engines/gsd/workflows/execute-plan.md
@/Users/greyaltaer/.claude/pde-os/engines/gsd/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/15-documentation-cleanup/15-RESEARCH.md

<interfaces>
From lib/signalframe-provider.tsx (source of truth for API contract):
```typescript
export function createSignalframeUX(config?: SignalframeUXConfig): {
  SignalframeProvider: React.FC<{ children: React.ReactNode }>;
  useSignalframe: () => UseSignalframeReturn;
}

interface SignalframeUXConfig {
  defaultTheme?: 'light' | 'dark' | 'system';
  motionPreference?: 'full' | 'reduced' | 'system';
}

interface UseSignalframeReturn {
  theme: 'light' | 'dark';
  setTheme: (theme: 'light' | 'dark') => void;
  motion: SignalframeMotionController;
}

interface SignalframeMotionController {
  pause: () => void;
  resume: () => void;
  prefersReduced: boolean;
}

// Standalone export also available:
export function useSignalframe(): UseSignalframeReturn;
```
</interfaces>
</context>

<acceptance_criteria>
AC-1: docs/SCAFFOLDING.md contains a "## 8. Config Provider API" section documenting createSignalframeUX, useSignalframe, and SignalframeMotionController
AC-2: SCAFFOLDING.md Section 8 includes both factory-returned and standalone import patterns for useSignalframe
AC-3: SCAFFOLDING.md Section 8 documents the SSR constraint (must call from 'use client' wrapper)
AC-4: components/sf/sf-section.tsx line 6 reads `bgShift?: "white" | "black"` (verified, no edit needed)
AC-5: .planning/REQUIREMENTS.md shows `- [x] **DOC-01**` and traceability table shows DOC-01 as Complete
</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Add Config Provider API section to SCAFFOLDING.md</name>
  <files>docs/SCAFFOLDING.md</files>
  <read_first>
    docs/SCAFFOLDING.md (current content, find end of Section 7 for insertion point)
    lib/signalframe-provider.tsx (source of truth for all types and exports)
  </read_first>
  <action>
    Append a new "## 8. Config Provider API" section at the end of docs/SCAFFOLDING.md. Use the Edit tool to insert after the last line.

    The section content is fully specified in the RESEARCH.md "Sub-target 3" section — use it verbatim. It documents:

    1. `createSignalframeUX(config?)` — factory function, parameters, return value
    2. `useSignalframe()` — hook return type (theme, setTheme, motion)
    3. `SignalframeMotionController` — pause, resume, prefersReduced
    4. SSR constraint — must be called from 'use client' wrapper, not Server Component module scope
    5. Both usage patterns: factory-returned hook AND standalone named export from lib/signalframe-provider

    Include the code examples from RESEARCH.md showing:
    - Factory creation in a 'use client' wrapper file
    - Standalone import pattern
    - Property table for UseSignalframeReturn
    - Property table for SignalframeMotionController

    Do NOT modify any existing sections (1-7). Append only.
  </action>
  <verify>
    <automated>grep -c "useSignalframe" /Users/greyaltaer/code/projects/SignalframeUX/docs/SCAFFOLDING.md</automated>
    Must return >= 3 (function name appears in heading, factory return, standalone export, code examples).
    Also: grep "Config Provider API" /Users/greyaltaer/code/projects/SignalframeUX/docs/SCAFFOLDING.md
  </verify>
  <ac_refs>AC-1, AC-2, AC-3</ac_refs>
  <acceptance_criteria>
    - grep "Config Provider API" docs/SCAFFOLDING.md returns a match
    - grep "useSignalframe" docs/SCAFFOLDING.md returns >= 3 matches
    - grep "use client" docs/SCAFFOLDING.md returns >= 1 match (SSR constraint documented)
    - grep "standalone" docs/SCAFFOLDING.md returns >= 1 match (both import patterns)
  </acceptance_criteria>
  <done>SCAFFOLDING.md has a complete Section 8 documenting the Config Provider API with correct types, both usage patterns, and SSR constraint</done>
</task>

<task type="auto">
  <name>Task 2: Verify SFSection JSDoc and mark DOC-01 complete</name>
  <files>
    .planning/REQUIREMENTS.md
  </files>
  <read_first>
    components/sf/sf-section.tsx (verify bgShift type and JSDoc — expect line 6: bgShift?: "white" | "black")
    .planning/REQUIREMENTS.md (find DOC-01 checkbox and traceability row)
  </read_first>
  <action>
    Step 1 — VERIFY (no edit): Read components/sf/sf-section.tsx and confirm:
    - Line ~6: `bgShift?: "white" | "black"` (not boolean)
    - Line ~19: JSDoc says "white" or "black" (not boolean)
    If both are correct, proceed. If either is wrong (unexpected), fix it.

    Step 2 — Mark DOC-01 complete in `.planning/REQUIREMENTS.md`:
    - Change `- [ ] **DOC-01**:` to `- [x] **DOC-01**:`
    - In the Traceability table, update the DOC-01 row:
      - Phase: Phase 15
      - Plan: 15-01, 15-02
      - Status: Complete

    Step 3 — Update ROADMAP.md Phase 15 entry:
    - Read .planning/ROADMAP.md
    - Update Phase 15 plans line: `**Plans:** 2/2 plans complete`
    - Update plan list:
      ```
      Plans:
      - [x] 15-01-PLAN.md — SUMMARY frontmatter normalization + REQUIREMENTS archive checkbox fixes
      - [x] 15-02-PLAN.md — SCAFFOLDING.md API contract + DOC-01 closure
      ```
    - Mark Phase 15 checkbox as complete: `- [x] **Phase 15: Documentation Cleanup**`

    NOTE: This task is sequenced last in the plan so that DOC-01 is only checked off after all documentation work is done.
  </action>
  <verify>
    <automated>grep "\[x\] \*\*DOC-01" /Users/greyaltaer/code/projects/SignalframeUX/.planning/REQUIREMENTS.md</automated>
    Must return a match. Also:
    grep 'bgShift.*white.*black' /Users/greyaltaer/code/projects/SignalframeUX/components/sf/sf-section.tsx
    Must return a match confirming the type is correct.
  </verify>
  <ac_refs>AC-4, AC-5</ac_refs>
  <acceptance_criteria>
    - sf-section.tsx contains bgShift?: "white" | "black" (grep confirms)
    - .planning/REQUIREMENTS.md contains "[x] **DOC-01**" (grep confirms)
    - REQUIREMENTS.md traceability table shows DOC-01 as Complete with Phase 15 attribution
  </acceptance_criteria>
  <done>SFSection JSDoc confirmed correct. DOC-01 marked complete in REQUIREMENTS.md. ROADMAP.md updated with Phase 15 plan list and completion status.</done>
</task>

</tasks>

<verification>
1. `grep "Config Provider API" docs/SCAFFOLDING.md` returns match
2. `grep -c "useSignalframe" docs/SCAFFOLDING.md` returns >= 3
3. `grep "\[x\] \*\*DOC-01" .planning/REQUIREMENTS.md` returns match
4. `grep 'bgShift.*white.*black' components/sf/sf-section.tsx` returns match
5. `pnpm tsc --noEmit` passes (confirms no code regressions)
</verification>

<success_criteria>
SCAFFOLDING.md has complete Config Provider API documentation. SFSection JSDoc verified correct. DOC-01 marked complete with traceability. ROADMAP.md reflects Phase 15 completion.
</success_criteria>

<output>
After completion, create `.planning/phases/15-documentation-cleanup/15-02-SUMMARY.md`
</output>
