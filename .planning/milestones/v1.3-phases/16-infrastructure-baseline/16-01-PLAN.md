---
phase: 16-infrastructure-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/ui/accordion.tsx
  - components/ui/alert-dialog.tsx
  - components/ui/avatar.tsx
  - components/ui/navigation-menu.tsx
  - components/ui/progress.tsx
  - components/ui/collapsible.tsx
  - components/ui/toggle-group.tsx
  - .planning/phases/16-infrastructure-baseline/BASELINE.md
autonomous: true
requirements: [INFRA-02]

must_haves:
  truths:
    - "pnpm build completes with zero errors after all 7 shadcn bases are installed"
    - "tsc --noEmit passes clean with no type errors"
    - "Bundle size baseline is recorded with exact numbers before any v1.3 SF wrapper ships"
    - "Lighthouse LCP and TTI are recorded against production build"
  artifacts:
    - path: "components/ui/accordion.tsx"
      provides: "Radix accordion base"
    - path: "components/ui/alert-dialog.tsx"
      provides: "Radix alert-dialog base"
    - path: "components/ui/avatar.tsx"
      provides: "Radix avatar base"
    - path: "components/ui/navigation-menu.tsx"
      provides: "Radix navigation-menu base"
    - path: "components/ui/progress.tsx"
      provides: "Radix progress base"
    - path: "components/ui/collapsible.tsx"
      provides: "Radix collapsible base"
    - path: "components/ui/toggle-group.tsx"
      provides: "Radix toggle-group base"
    - path: ".planning/phases/16-infrastructure-baseline/BASELINE.md"
      provides: "Numbered performance baseline"
      contains: "## Performance Baseline"
  key_links:
    - from: "components/ui/*.tsx (new bases)"
      to: "pnpm build"
      via: "TypeScript compilation"
      pattern: "tsc --noEmit.*exit 0"
---

<objective>
Install the seven shadcn base components required for v1.3 SF wrappers and capture a numbered performance baseline before any new component ships.

Purpose: Front-load all shadcn base installs so Phase 17-19 authors can immediately begin SF wrapping. Capture the pre-expansion bundle/Lighthouse numbers as a regression gate.
Output: 7 new files in components/ui/, BASELINE.md with measured numbers.
</objective>

<execution_context>
@/Users/greyaltaer/.claude/pde-os/engines/gsd/workflows/execute-plan.md
@/Users/greyaltaer/.claude/pde-os/engines/gsd/templates/summary.md
</execution_context>

<context>
@.planning/project-context.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-infrastructure-baseline/16-RESEARCH.md
</context>

<acceptance_criteria>

**AC-1:** Given the project has 24 shadcn bases installed, when `pnpm dlx shadcn@4.1.2 add accordion alert-dialog avatar navigation-menu progress collapsible toggle-group` completes, then 7 new files exist in `components/ui/` and `pnpm exec tsc --noEmit` exits 0.

**AC-2:** Given a clean build after shadcn install, when `ANALYZE=true pnpm build` runs, then `.next/analyze/client.html` is generated and the initial JS bundle size in KB is extracted and recorded.

**AC-3:** Given the performance baseline file exists, when reading `.planning/phases/16-infrastructure-baseline/BASELINE.md`, then it contains numbered values for: initial JS bundle size (KB), Lighthouse Performance score, LCP (seconds), and TTI (seconds).

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Install 7 shadcn base components and verify build</name>
  <files>components/ui/accordion.tsx, components/ui/alert-dialog.tsx, components/ui/avatar.tsx, components/ui/navigation-menu.tsx, components/ui/progress.tsx, components/ui/collapsible.tsx, components/ui/toggle-group.tsx</files>
  <read_first>
    - next.config.ts (verify bundle analyzer wiring)
    - components/ui/dialog.tsx (reference pattern for how shadcn generates bases)
    - package.json (confirm shadcn@4.1.2 pinned version)
  </read_first>
  <ac_refs>AC-1</ac_refs>
  <action>
1. Run `pnpm dlx shadcn@4.1.2 add accordion alert-dialog avatar navigation-menu progress collapsible toggle-group`. If 4.1.2 fails, fall back to `pnpm dlx shadcn@latest add ...` and note the version used.

2. After install, verify all 7 files exist in `components/ui/`:
   - accordion.tsx
   - alert-dialog.tsx
   - avatar.tsx
   - navigation-menu.tsx
   - progress.tsx
   - collapsible.tsx
   - toggle-group.tsx

3. Run `pnpm exec tsc --noEmit` — must exit 0 with no errors.

4. Run `pnpm build` — must complete successfully.

5. Inspect each generated file and record which ones have `'use client'` at line 1. This information is needed by Phase 17-19 SF wrapper authors. Expected:
   - WILL have 'use client': alert-dialog.tsx, navigation-menu.tsx, toggle-group.tsx, accordion.tsx, collapsible.tsx
   - MAY NOT have 'use client': avatar.tsx, progress.tsx

6. Inspect each generated file for `rounded-*` classes (not `rounded-none`). Record which elements carry them. Expected high-risk:
   - avatar.tsx: `rounded-full` on AvatarRoot, AvatarImage, AvatarFallback
   - progress.tsx: `rounded-full` on ProgressRoot and ProgressIndicator
   - alert-dialog.tsx: `rounded-xl` on AlertDialogContent
   - navigation-menu.tsx: `rounded-md` or `rounded-lg` on content/indicator
   - toggle-group.tsx: `rounded-*` on toggle items

Document findings in a code comment block at the top of the BASELINE.md file created in Task 2.
  </action>
  <verify>
    <automated>cd /Users/greyaltaer/code/projects/SignalframeUX && ls components/ui/accordion.tsx components/ui/alert-dialog.tsx components/ui/avatar.tsx components/ui/navigation-menu.tsx components/ui/progress.tsx components/ui/collapsible.tsx components/ui/toggle-group.tsx && pnpm exec tsc --noEmit</automated>
  </verify>
  <done>All 7 shadcn base files exist in components/ui/, tsc --noEmit exits 0, pnpm build succeeds, 'use client' and rounded-* audit notes recorded</done>
</task>

<task type="auto">
  <name>Task 2: Capture numbered performance baseline</name>
  <files>.planning/phases/16-infrastructure-baseline/BASELINE.md</files>
  <read_first>
    - next.config.ts (confirm ANALYZE=true wiring)
  </read_first>
  <ac_refs>AC-2, AC-3</ac_refs>
  <action>
1. Run `ANALYZE=true pnpm build` to generate bundle analysis. Extract the initial JS bundle size from build output (the "First Load JS" column for the root route `/`). Record the exact KB value.

2. Note: Lighthouse CLI measurement against `pnpm start` may not be available in all environments. If `npx lighthouse` is available, run:
   ```
   pnpm build && pnpm start &
   sleep 3
   npx lighthouse http://localhost:3000 --output=json --chrome-flags="--headless" | grep -E '"(performance|largestContentfulPaint|interactive)"'
   ```
   If Lighthouse CLI is not available, record "Lighthouse: measure manually via browser DevTools > Lighthouse panel against `pnpm start` on localhost:3000" and populate the LCP/TTI/score fields with placeholder instructions.

3. Create `.planning/phases/16-infrastructure-baseline/BASELINE.md` with this structure:

```markdown
# Performance Baseline — v1.3 Pre-Expansion

Captured: {date}
Commit: {git short hash}
After: 7 shadcn bases installed (accordion, alert-dialog, avatar, navigation-menu, progress, collapsible, toggle-group)
Before: Any v1.3 SF wrapper implementation

## Performance Baseline

| Metric | Value | Gate | Hard Limit |
|--------|-------|------|------------|
| Initial JS bundle (First Load JS, `/`) | {N} KB | 150 KB | 200 KB |
| Lighthouse Performance | {N}/100 | 100 | 90 |
| Lighthouse LCP | {N}s | 1.0s | 1.5s |
| Lighthouse TTI | {N}s | 1.5s | 2.0s |

## Bundle Composition

{Paste the Next.js build output showing route sizes}

## shadcn Install Audit

### 'use client' Status
{Table of 7 bases with yes/no for 'use client'}

### rounded-* Classes Requiring SF Override
{Table of bases with specific elements and classes that need rounded-none in SF wrappers}

## Regression Protocol

After each P1 component (Phase 17-18):
1. Run `ANALYZE=true pnpm build`
2. Compare First Load JS against this baseline
3. If delta > 5KB for a FRAME-only component, investigate
4. If total exceeds 150KB gate, stop and optimize before continuing
```

4. Populate all fields with measured values from the build output.
  </action>
  <verify>
    <automated>cd /Users/greyaltaer/code/projects/SignalframeUX && test -f .planning/phases/16-infrastructure-baseline/BASELINE.md && grep -c "Initial JS bundle" .planning/phases/16-infrastructure-baseline/BASELINE.md</automated>
  </verify>
  <done>BASELINE.md exists with measured bundle size, Lighthouse metrics (or instructions to capture), shadcn audit tables for 'use client' and rounded-* classes</done>
</task>

</tasks>

<verification>
- `ls components/ui/accordion.tsx components/ui/alert-dialog.tsx components/ui/avatar.tsx components/ui/navigation-menu.tsx components/ui/progress.tsx components/ui/collapsible.tsx components/ui/toggle-group.tsx` — all 7 exist
- `pnpm exec tsc --noEmit` — exits 0
- `pnpm build` — succeeds
- `cat .planning/phases/16-infrastructure-baseline/BASELINE.md` — contains Performance Baseline table with numbers
</verification>

<success_criteria>
- 7 new shadcn base files in components/ui/ (31 total bases)
- TypeScript compilation clean
- Production build clean
- BASELINE.md records initial JS bundle size, Lighthouse LCP/TTI/score
- 'use client' and rounded-* audit documented for Phase 17-19 reference
</success_criteria>

<output>
After completion, create `.planning/phases/16-infrastructure-baseline/16-01-SUMMARY.md`
</output>
