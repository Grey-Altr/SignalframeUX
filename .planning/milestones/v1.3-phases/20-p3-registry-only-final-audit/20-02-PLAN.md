---
phase: 20-p3-registry-only-final-audit
plan: 02
type: execute
wave: 2
depends_on: [20-01]
files_modified:
  - registry.json
  - SCAFFOLDING.md
  - components/blocks/components-explorer.tsx
autonomous: true
requirements: [REG-01, REG-02]

must_haves:
  truths:
    - "All registry.json entries have correct meta.pattern values (A for Radix-wrapped, B for lazy/P3, C for pure-SF)"
    - "Every registry.json item has a corresponding public/r/ JSON file"
    - "SCAFFOLDING.md component count and pitfall section reflect v1.3 final state"
    - "Shared JS bundle remains under 150KB gate"
    - "SFCalendar and SFMenubar appear in ComponentsExplorer under correct categories"
  artifacts:
    - path: "registry.json"
      provides: "Complete registry with corrected meta.pattern values"
      contains: '"pattern"'
    - path: "SCAFFOLDING.md"
      provides: "Updated component count and resolved pitfall #3"
      contains: "Pattern B"
  key_links:
    - from: "registry.json"
      to: "public/r/"
      via: "pnpm registry:build"
      pattern: "public/r/sf-calendar.json"
    - from: "components/blocks/components-explorer.tsx"
      to: "components/sf/sf-calendar-lazy.tsx"
      via: "preview entry reference"
      pattern: "CALENDAR"
---

<objective>
Final audit of the entire v1.3 component expansion -- fix meta.pattern values across registry, rebuild public/r/, update SCAFFOLDING.md, add ComponentsExplorer entries, verify bundle gate.

Purpose: Close v1.3 with a clean, consistent, complete registry and documentation. Every component shipped in Phases 16-20 is discoverable, installable, and correctly categorized.
Output: Corrected registry.json, rebuilt public/r/, updated SCAFFOLDING.md, ComponentsExplorer entries for P3 components.
</objective>

<execution_context>
@/Users/greyaltaer/.claude/pde-os/engines/gsd/workflows/execute-plan.md
@/Users/greyaltaer/.claude/pde-os/engines/gsd/templates/summary.md
</execution_context>

<context>
@.planning/project-context.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-p3-registry-only-final-audit/20-RESEARCH.md
@.planning/phases/20-p3-registry-only-final-audit/20-01-SUMMARY.md

<interfaces>
<!-- Pattern classification rules from SCAFFOLDING.md -->
Pattern A — Radix Wrap: Thin passthrough over shadcn/Radix base (sf-dialog, sf-button, sf-tabs, sf-sheet, etc.)
Pattern B — Lazy Registry-Only: next/dynamic with ssr:false, NOT in barrel (sf-calendar, sf-menubar ONLY)
Pattern C — Pure SF Construction: No Radix base, semantic HTML + Tailwind (sf-text, sf-container, sf-stack, sf-grid, sf-section, sf-empty-state, sf-status-dot)

<!-- Components needing meta.pattern correction (currently "B" but should be "A" or "C") -->
Known incorrect: sf-button ("B" should be "A"), and likely others from pre-v1.3 entries.
Must audit all 50 entries and correct.

<!-- Missing from public/r/ (Phase 17-18 components) -->
Missing: sf-collapsible, sf-breadcrumb, sf-avatar, sf-alert, sf-alert-dialog, sf-empty-state, sf-status-dot, sf-accordion, sf-progress, sf-toast
</interfaces>
</context>

<acceptance_criteria>
- AC-1: Every registry.json entry has correct `meta.pattern` — "A" for Radix-wrapped, "B" for lazy/P3 (sf-calendar, sf-menubar only), "C" for pure-SF. `grep '"pattern": "B"' registry.json | wc -l` returns exactly 2
- AC-2: `pnpm registry:build` succeeds and every registry.json item name has a corresponding `public/r/{name}.json` file
- AC-3: SCAFFOLDING.md component count updated to v1.3 total; pitfall #3 (meta.pattern inconsistency) marked as resolved
- AC-4: ComponentsExplorer has entries for SFCalendar and SFMenubar under their correct categories with CSS-only preview components
- AC-5: `ANALYZE=true pnpm build` passes; shared JS under 150KB gate
- AC-6: `pnpm build` passes with zero errors after all changes
</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Registry meta.pattern audit + public/r/ rebuild</name>
  <files>registry.json</files>
  <read_first>
    - registry.json (full file — audit every entry's meta.pattern)
    - SCAFFOLDING.md (Pattern A/B/C definitions)
    - components/sf/index.ts (to identify which components are Radix-wrapped vs pure-SF)
  </read_first>
  <action>
1. Read `registry.json` in full. For each of the 50 entries, determine the correct `meta.pattern`:
   - **Pattern A (Radix-wrapped):** Has a `registryDependencies` pointing to a shadcn/Radix base AND the SF wrapper imports from `ui/[name]`. This includes: sf-button, sf-input, sf-badge, sf-tabs, sf-table, sf-separator, sf-tooltip, sf-dialog, sf-sheet, sf-dropdown-menu, sf-toggle, sf-slider, sf-command, sf-skeleton, sf-popover, sf-scroll-area, sf-label, sf-select, sf-checkbox, sf-radio-group, sf-switch, sf-textarea, sf-collapsible, sf-breadcrumb, sf-avatar, sf-alert, sf-alert-dialog, sf-accordion, sf-progress, sf-toast, sf-toggle-group, sf-pagination, sf-stepper, sf-navigation-menu, sf-card
   - **Pattern B (lazy/P3):** ONLY sf-calendar and sf-menubar
   - **Pattern C (pure-SF):** No Radix base — sf-container, sf-section, sf-grid, sf-stack, sf-text, sf-empty-state, sf-status-dot
   - **Non-SF entries (keep existing):** circuit-divider, vhs-overlay, hero-mesh, scramble-text, sf-theme, base — these are not SF-wrapped components; keep whatever pattern they have or omit meta.pattern if not applicable

2. Update every incorrect `meta.pattern` value. The main fix: change all pre-v1.3 entries that say `"B"` to `"A"` (they are Radix-wrapped, not lazy).

3. Run `pnpm registry:build` to regenerate all `public/r/` JSON files.

4. Verify: count of files in `public/r/` matches count of items in registry.json (plus base registry.json itself).
  </action>
  <ac_refs>AC-1, AC-2</ac_refs>
  <verify>
    <automated>cd /Users/greyaltaer/code/projects/SignalframeUX && grep -c '"pattern": "B"' registry.json && echo "should be 2" && pnpm registry:build 2>&1 | tail -3 && ls public/r/*.json | wc -l</automated>
  </verify>
  <done>All meta.pattern values correct (exactly 2 "B" entries for calendar and menubar), all public/r/ JSON files regenerated and in sync with registry.json</done>
</task>

<task type="auto">
  <name>Task 2: SCAFFOLDING.md update + ComponentsExplorer P3 entries + bundle gate</name>
  <files>SCAFFOLDING.md, components/blocks/components-explorer.tsx</files>
  <read_first>
    - SCAFFOLDING.md (full — identify component count, pitfall #3, P3 section)
    - components/blocks/components-explorer.tsx (existing entry structure, category assignment)
  </read_first>
  <action>
1. Update SCAFFOLDING.md:
   - Update component count to reflect all v1.3 additions (should be ~42 SF component files total)
   - Mark pitfall #3 (meta.pattern inconsistency) as RESOLVED with note: "Fixed in Phase 20 final audit — all entries now use correct A/B/C pattern values"
   - Verify P3 lazy component rules section is still accurate
   - Add SFCalendar and SFMenubar to the component inventory/reference if one exists

2. Add ComponentsExplorer entries for P3 components:
   - Add CALENDAR entry under FORMS (or FEEDBACK — match the category system) with:
     - CSS-only PreviewCalendar component (simple grid suggesting a month view, no actual react-day-picker import)
     - Label: "Calendar"
     - Description noting lazy/P3 status
     - Category tag matching the established system
   - Add MENUBAR entry under NAVIGATION with:
     - CSS-only PreviewMenubar component (horizontal bar with labeled sections)
     - Label: "Menubar"
     - Description noting lazy/P3 status
   - Both entries should have version "v1.3.0"
   - Increment the entry index numbers sequentially after the last existing entry

3. Run `ANALYZE=true pnpm build` to verify:
   - Zero build errors
   - Shared JS remains under 150KB gate (baseline: 102-103KB)
   - Calendar/menubar deps appear ONLY in async chunks, not in shared
   - Record the final shared JS size for the SUMMARY

4. Final barrel check: `grep -c "calendar\|menubar" components/sf/index.ts` must return 0.
  </action>
  <ac_refs>AC-3, AC-4, AC-5, AC-6</ac_refs>
  <verify>
    <automated>cd /Users/greyaltaer/code/projects/SignalframeUX && grep -q "RESOLVED\|resolved\|Fixed" SCAFFOLDING.md && grep -c "calendar\|menubar" components/sf/index.ts | grep -q "^0$" && ANALYZE=true pnpm build 2>&1 | grep -E "First Load|shared" | head -5</automated>
  </verify>
  <done>SCAFFOLDING.md updated with v1.3 final state and resolved pitfall, ComponentsExplorer has Calendar and Menubar entries, shared bundle under 150KB gate, build passes clean</done>
</task>

</tasks>

<verification>
- `grep -c '"pattern": "B"' registry.json` returns exactly 2
- `pnpm registry:build` succeeds with no errors
- `public/r/sf-calendar.json` and `public/r/sf-menubar.json` exist
- `ANALYZE=true pnpm build` passes, shared JS under 150KB
- ComponentsExplorer shows Calendar and Menubar entries
- `grep -c "calendar\|menubar" components/sf/index.ts` returns 0
- SCAFFOLDING.md pitfall #3 marked resolved
</verification>

<success_criteria>
The v1.3 registry is complete and consistent. Every component has the correct meta.pattern, every registry entry has a public/r/ file, SCAFFOLDING.md reflects the final state, and the bundle gate passes. Phase 20 and milestone v1.3 are done.
</success_criteria>

<output>
After completion, create `.planning/phases/20-p3-registry-only-final-audit/20-02-SUMMARY.md`
</output>
