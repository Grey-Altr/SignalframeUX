---
phase: 01-frame-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - components/sf/sf-button.tsx
  - components/blocks/shared-code-block.tsx
  - components/blocks/code-section.tsx
  - components/blocks/components-explorer.tsx
  - components/blocks/api-explorer.tsx
  - components/blocks/token-tabs.tsx
autonomous: true
requirements:
  - FRM-01
  - FRM-07
must_haves:
  truths:
    - "Zero non-blessed spacing values (p/m/gap-5, -7, -10) exist in components/sf/, components/blocks/, or components/layout/"
    - "All CVA calls in components/sf/ use intent as the variant prop name and have defaultVariants declared"
  artifacts:
    - path: "components/blocks/components-explorer.tsx"
      provides: "Spacing-corrected component explorer"
      contains: "p-6"
    - path: "components/blocks/token-tabs.tsx"
      provides: "Spacing-corrected token tabs"
      contains: "p-6"
    - path: "components/sf/sf-button.tsx"
      provides: "CVA-compliant button with signal intent documented"
      contains: "defaultVariants"
  key_links:
    - from: "components/sf/sf-button.tsx"
      to: "CVA intent standard"
      via: "cva() call with intent + defaultVariants"
      pattern: "intent.*default"
---

<objective>
Sweep all non-blessed spacing values from component files and verify CVA compliance across SF components.

Purpose: Enforce the blessed spacing stops (4/8/12/16/24/32/48/64/96) across all content spacing. Confirm CVA variant standard is met. This is the mechanical enforcement pass that makes the token system trustworthy.
Output: Component files with only blessed spacing values. CVA audit confirmation.
</objective>

<execution_context>
@/Users/greyaltaer/.claude/pde-os/engines/gsd/workflows/execute-plan.md
@/Users/greyaltaer/.claude/pde-os/engines/gsd/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-frame-foundation/01-CONTEXT.md
@.planning/phases/01-frame-foundation/01-RESEARCH.md

<interfaces>
<!-- Spacing mapping reference from RESEARCH.md -->
Non-blessed to blessed-stop mapping (Tailwind units, each unit = 4px):
  p-5 (20px) → p-6 (24px)
  p-7 (28px) → p-8 (32px)
  p-10 (40px) → p-12 (48px)
  Same for m-, gap-, px-, py-, pt-, pb-, pl-, pr-, mt-, mb-, ml-, mr- prefixes.

Exemptions per CONTEXT.md:
  - components/ui/*.tsx (shadcn base — never modify)
  - Structural measurements: nav-height (83px), border-width, fixed-position offsets (bottom-20 in global-effects.tsx)
  - Only CONTENT spacing uses blessed stops

CVA standard:
  - Variant prop: intent
  - Values: default, primary, secondary, destructive, ghost, outline
  - signal intent on sf-button.tsx: pre-standard extension — keep as documented extension, add comment
  - defaultVariants: { intent: "default" } on every CVA call (sf-button already uses intent: "primary" — keep as-is since it's explicitly declared)
</interfaces>
</context>

<acceptance_criteria>
**AC-1:** Given all component files in sf/, blocks/, and layout/ are scanned, when running grep for non-blessed spacing units (p/m/gap/pt/pb/pl/pr/mt/mb/ml/mr-5, -7, -10), then zero matches are returned (excluding ui/ and structural exemptions)
**AC-2:** Given sf-button.tsx is read, when inspecting the CVA call, then intent is the variant prop name and defaultVariants contains intent
**AC-3:** Given sf-badge.tsx and sf-toggle.tsx are read, when inspecting their CVA calls, then both use intent with defaultVariants declared
**AC-4:** Given sf-button.tsx has a signal intent value, when reading the file, then a code comment documents it as a pre-standard extension of the blessed intent set
</acceptance_criteria>

<tasks>

<task type="auto" risk="high">
  <name>Task 1: Sweep non-blessed spacing values across all component files</name>
  <files>
    components/sf/sf-button.tsx
    components/blocks/shared-code-block.tsx
    components/blocks/code-section.tsx
    components/blocks/components-explorer.tsx
    components/blocks/api-explorer.tsx
    components/blocks/token-tabs.tsx
  </files>
  <read_first>
    components/sf/sf-button.tsx
    components/blocks/shared-code-block.tsx
    components/blocks/code-section.tsx
    components/blocks/components-explorer.tsx
    components/blocks/api-explorer.tsx
    components/blocks/token-tabs.tsx
  </read_first>
  <ac_refs>AC-1</ac_refs>
  <boundaries>
    - DO NOT modify components/ui/*.tsx (shadcn base exempt)
    - DO NOT change structural positioning values (bottom-20 in global-effects.tsx is exempt)
    - DO NOT change border-width, nav-height, or other structural measurements
    - Preserve visual density — if p-5 becomes p-6, the component gets slightly more padding; this is the correct trade
  </boundaries>
  <action>
Replace every non-blessed Tailwind spacing unit in content spacing contexts with the nearest blessed stop. The complete replacement map:

**sf-button.tsx line 21:** `px-10` → `px-12` (40px → 48px, xl size variant)

**shared-code-block.tsx line 16:** `p-5` → `p-6` (20px → 24px)

**code-section.tsx line 66:** `py-10` → `py-12` (40px → 48px)

**components-explorer.tsx:**
- Line 430: `px-5` → `px-6` (20px → 24px)
- Line 443: `px-5` → `px-6`
- Line 449: `px-5` → `px-6`
- Line 477: `p-5` → `p-6`

**api-explorer.tsx:**
- Line 475: `pt-10` → `pt-12` (40px → 48px)
- Line 517: `p-5` → `p-6` (20px → 24px)
- Line 554: `py-10` → `py-12` (40px → 48px)
- Line 721: `p-10` → `p-12` (40px → 48px)
- Line 730: `top-5 left-5` → `top-6 left-6` (20px → 24px)
- Line 780: `p-5` → `p-6`

**token-tabs.tsx:**
- Line 205: `py-10` → `py-12` (40px → 48px)
- Line 219: `mt-5` → `mt-6` (20px → 24px)
- Lines 399, 403, 423: `p-5` → `p-6`
- Lines 490, 493, 509, 537, 540, 562: `p-5` → `p-6`

After all replacements, run the grep verification to confirm zero remaining non-blessed values.
  </action>
  <verify>
    <automated>grep -rEn " (p|px|py|m|mx|my|gap|pt|pb|pl|pr|mt|mb|ml|mr)-(5|7|10)[^0-9]" components/sf/ components/blocks/ components/layout/ --include="*.tsx" 2>/dev/null; echo "EXIT:$?"</automated>
  </verify>
  <done>Zero non-blessed spacing values in components/sf/, components/blocks/, and components/layout/. The grep command returns no matches (exit code 1 = no matches found).</done>
</task>

<task type="auto">
  <name>Task 2: Verify and document CVA compliance across SF components</name>
  <files>components/sf/sf-button.tsx</files>
  <read_first>
    components/sf/sf-button.tsx
    components/sf/sf-badge.tsx
    components/sf/sf-toggle.tsx
  </read_first>
  <ac_refs>AC-2, AC-3, AC-4</ac_refs>
  <boundaries>
    - DO NOT change sf-badge.tsx or sf-toggle.tsx (already compliant)
    - DO NOT remove the signal intent from sf-button.tsx — document it as extension
  </boundaries>
  <action>
1. Read all three CVA-using SF components and confirm compliance:
   - sf-badge.tsx: uses `intent` with `defaultVariants: { intent: "default" }` — COMPLIANT, no changes needed
   - sf-toggle.tsx: uses `intent` with `defaultVariants: { intent: "default" }` — COMPLIANT, no changes needed
   - sf-button.tsx: uses `intent` with `defaultVariants: { intent: "primary" }` — COMPLIANT (primary is a valid explicit default)

2. In sf-button.tsx, add a comment above the `signal` intent value documenting it as a pre-standard extension:
```typescript
      intent: {
        default: "...",
        primary: "...",
        secondary: "...",
        destructive: "...",
        ghost: "...",
        outline: "...",
        // signal: pre-standard extension — kept for SignalframeUX brand accent usage.
        // Blessed intent set: default, primary, secondary, destructive, ghost, outline.
        signal: "...",
      },
```

3. Scan ALL other sf/*.tsx files for any undiscovered CVA usage:
   `grep -l "cva" components/sf/*.tsx`
   If any additional CVA-using component is found beyond the three known ones, apply the same intent + defaultVariants standard.
  </action>
  <verify>
    <automated>grep -A2 "defaultVariants" components/sf/sf-button.tsx components/sf/sf-badge.tsx components/sf/sf-toggle.tsx && grep -c "pre-standard extension" components/sf/sf-button.tsx</automated>
  </verify>
  <done>All three CVA-using SF components have intent as variant prop and defaultVariants declared. sf-button.tsx signal intent is documented with "pre-standard extension" comment. No undiscovered CVA components.</done>
</task>

</tasks>

<verification>
- `grep -rEn " (p|px|py|m|mx|my|gap|pt|pb|pl|pr|mt|mb|ml|mr)-(5|7|10)[^0-9]" components/sf/ components/blocks/ components/layout/ --include="*.tsx"` returns zero matches
- `grep -l "cva" components/sf/*.tsx` returns exactly: sf-badge.tsx, sf-button.tsx, sf-toggle.tsx
- `grep "defaultVariants" components/sf/sf-button.tsx components/sf/sf-badge.tsx components/sf/sf-toggle.tsx` returns 3 matches
- `npx next build` completes without errors
</verification>

<success_criteria>
All content spacing in sf/, blocks/, and layout/ directories uses only blessed stops. All CVA-using SF components are compliant with intent + defaultVariants standard. The signal intent is explicitly documented.
</success_criteria>

<output>
After completion, create `.planning/phases/01-frame-foundation/01-02-SUMMARY.md`
</output>
