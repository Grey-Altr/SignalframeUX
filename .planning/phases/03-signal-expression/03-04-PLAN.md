---
phase: 03-signal-expression
plan: "04"
type: execute
wave: 2
depends_on: ["03-01", "03-02", "03-03"]
files_modified:
  - .planning/phases/03-signal-expression/SIGNAL-SPEC.md
autonomous: true
requirements: [SIG-10, SIG-06, SIG-07, SIG-08]

must_haves:
  truths:
    - "SIGNAL-SPEC.md documents every SIGNAL effect with timing, easing, mobile behavior, CSS fallback, and reduced-motion behavior"
    - "SIG-06 (audio), SIG-07 (haptic), SIG-08 (idle) are listed as DEFERRED in SIGNAL-SPEC.md with rationale"
    - "Mobile behavior is explicitly defined per effect: which collapse, which persist, what the static fallback looks like"
  artifacts:
    - path: ".planning/phases/03-signal-expression/SIGNAL-SPEC.md"
      provides: "Complete SIGNAL layer specification document"
      contains: "DEFERRED"
  key_links:
    - from: ".planning/phases/03-signal-expression/SIGNAL-SPEC.md"
      to: "components/layout/page-animations.tsx"
      via: "Documents timing values that page-animations.tsx implements"
      pattern: "34ms|100ms|400ms|40ms"
---

<objective>
Create the SIGNAL-SPEC.md specification document that defines every SIGNAL layer effect's timing, easing, mobile behavior, CSS fallback, and reduced-motion behavior. Also formally documents SIG-06/07/08 as deferred to post-v1.0.

Purpose: The spec is the single source of truth for SIGNAL behavior. It codifies what Plans 01-03 implemented, documents mobile collapse/persist decisions, and marks deferred features. Future phases (ATF, DX) reference this spec.

Output: SIGNAL-SPEC.md in the phase directory.
</objective>

<execution_context>
@/Users/greyaltaer/.claude/pde-os/engines/gsd/workflows/execute-plan.md
@/Users/greyaltaer/.claude/pde-os/engines/gsd/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-signal-expression/03-CONTEXT.md
@.planning/phases/03-signal-expression/03-RESEARCH.md
@.planning/phases/03-signal-expression/03-01-SUMMARY.md
@.planning/phases/03-signal-expression/03-02-SUMMARY.md
@.planning/phases/03-signal-expression/03-03-SUMMARY.md
</context>

<acceptance_criteria>
AC-1: Given SIGNAL-SPEC.md exists, when read, then it contains a table or section for EACH of these effects: ScrambleText, asymmetric hover, hard-cut transition, staggered grid entry, [data-anim] fallback, canvas cursor, VHS overlay, circuit divider, hero mesh.

AC-2: Given each effect entry, then it lists: timing (in ms with token reference), easing (token name), mobile behavior (collapse/persist + fallback description), CSS fallback (what renders without JS), reduced-motion behavior.

AC-3: Given SIGNAL-SPEC.md, when searched for "SIG-06", then it appears with status "DEFERRED — post-v1.0" and a one-line rationale.

AC-4: Given SIGNAL-SPEC.md, when searched for "SIG-07", then it appears with status "DEFERRED — post-v1.0" and a one-line rationale.

AC-5: Given SIGNAL-SPEC.md, when searched for "SIG-08", then it appears with status "DEFERRED — post-v1.0" and a one-line rationale.

AC-6: Given the mobile behavior section, then it explicitly lists effects that COLLAPSE on mobile (cursor, VHS grain, circuit dividers, hero-mesh) and effects that PERSIST on mobile (ScrambleText, hover timing, staggered grid, hard-cut transitions).

AC-7: Given SIGNAL-SPEC.md, then it references the motion token names (--duration-instant, --duration-fast, --duration-slow) with their ms values.
</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Create SIGNAL-SPEC.md with complete effect specifications</name>
  <files>.planning/phases/03-signal-expression/SIGNAL-SPEC.md</files>
  <read_first>.planning/phases/03-signal-expression/03-CONTEXT.md, .planning/phases/03-signal-expression/03-01-SUMMARY.md, .planning/phases/03-signal-expression/03-02-SUMMARY.md, .planning/phases/03-signal-expression/03-03-SUMMARY.md</read_first>
  <ac_refs>AC-1, AC-2, AC-3, AC-4, AC-5, AC-6, AC-7</ac_refs>
  <action>
Create `.planning/phases/03-signal-expression/SIGNAL-SPEC.md` with the following structure:

**Header:**
```markdown
# SIGNAL Layer Specification — SignalframeUX v1.0

**Created:** {date}
**Phase:** 03 — SIGNAL Expression
**Status:** Active
```

**Section 1: Motion Tokens Reference**
Table of token name, CSS variable, ms value, and usage:
- `--duration-instant` = 34ms (hard cuts, active press)
- `--duration-fast` = 100ms (hover-in, snap interactions)
- `--duration-normal` = 200ms (standard transitions)
- `--duration-slow` = 400ms (hover-out, return transitions)
- `--duration-glacial` = 600ms (dramatic reveals)
- `--ease-default` = cubic-bezier(0.16, 1, 0.3, 1)
- `--ease-hover` = cubic-bezier(0.33, 1, 0.68, 1)
- `--ease-spring` = cubic-bezier(0.34, 1.56, 0.64, 1)

**Section 2: Effect Specifications**

For each effect, use this template:

```markdown
### {Effect Name} — {SIG-ID}

| Property | Value |
|----------|-------|
| Timing | {ms} ({token name}) |
| Easing | {token name} / {value} |
| Trigger | {how it fires} |
| Mobile | {Collapse / Persist} — {fallback} |
| CSS Fallback | {what renders without JS} |
| Reduced Motion | {what replaces it} |
| File | {source file} |
```

**Effects to document (with values from implementation):**

1. **ScrambleText (SIG-01)**: 800ms, power default, ScrollTrigger onEnter once: true, PERSISTS on mobile, CSS fallback: original text visible, reduced-motion: text shown instantly (no scramble)
2. **Asymmetric Hover (SIG-02)**: 100ms in (--duration-fast) / 400ms out (--duration-slow), --ease-hover in / --ease-default out, CSS transition (no JS needed), PERSISTS on mobile, CSS fallback: N/A (pure CSS), reduced-motion: transitions suppressed
3. **Hard-Cut Section Transition (SIG-03)**: 34ms (--duration-instant), ease: none, ScrollTrigger onEnter, PERSISTS on mobile, CSS fallback: section visible (opacity:1 catch-all), reduced-motion: section visible immediately
4. **Staggered Grid Entry (SIG-04)**: 400ms per item with 40ms stagger, power2.out, ScrollTrigger.batch, PERSISTS on mobile, CSS fallback: children visible (catch-all + reduced-motion reset), reduced-motion: children visible immediately
5. **[data-anim] Progressive Enhancement (SIG-05)**: N/A timing (CSS rule), catch-all opacity:1, PERSISTS everywhere, CSS fallback: the entire point — content always visible, reduced-motion: opacity:1 !important
6. **Canvas Cursor (SIG-09)**: rAF loop (~16ms per frame), N/A easing, IntersectionObserver on [data-cursor], COLLAPSES on mobile (pointer: coarse), CSS fallback: system cursor (canvas hidden), reduced-motion: system cursor only
7. **VHS Overlay (existing)**: GSAP timeline, multiple layers, COLLAPSES on mobile (pointer: coarse hides it), CSS fallback: hidden if GSAP fails, reduced-motion: frozen or hidden
8. **Circuit Dividers (existing)**: DrawSVGPlugin scrub, COLLAPSES on mobile to static SVG, CSS fallback: static SVG visible, reduced-motion: static SVG
9. **Hero Mesh (existing)**: GSAP timeline, COLLAPSES on mobile to static first frame, CSS fallback: static background, reduced-motion: static frame

**Section 3: Mobile Behavior Matrix**
Table with columns: Effect, Desktop, Mobile (<md), Fallback
Two groups: "Persists on Mobile" and "Collapses on Mobile" per CONTEXT.md locked decisions.

**Section 4: Deferred Effects**

```markdown
## Deferred to Post-v1.0

### Audio Feedback Palette — SIG-06
**Status:** DEFERRED — post-v1.0
**Rationale:** Web Audio API synthesized tones require careful UX design for gesture-gating and volume control. Deferred to avoid scope creep in the expression layer.

### Haptic Feedback — SIG-07
**Status:** DEFERRED — post-v1.0
**Rationale:** Vibration API has limited browser support and requires device-specific tuning. Not critical for portfolio SOTD evaluation.

### Idle State Animation — SIG-08
**Status:** DEFERRED — post-v1.0
**Rationale:** IdleOverlay component exists in global-effects.tsx but grain drift / color pulse refinement deferred. Current implementation shows basic idle indicator. Full refinement is post-v1.0 polish.
```

**Section 5: Progressive Enhancement Guarantee**
Summary paragraph explaining the SIG-05 model: CSS sets content visible by default, GSAP sets hidden state on init, animations reveal. No JS = all content visible. Reduced motion = all content visible, no animation.
  </action>
  <verify>
    <automated>cd /Users/greyaltaer/code/projects/SignalframeUX && grep -c "DEFERRED" .planning/phases/03-signal-expression/SIGNAL-SPEC.md && grep -c "SIG-0" .planning/phases/03-signal-expression/SIGNAL-SPEC.md && grep "duration-instant\|duration-fast\|duration-slow" .planning/phases/03-signal-expression/SIGNAL-SPEC.md | head -5</automated>
  </verify>
  <done>SIGNAL-SPEC.md exists with all 9+ effects documented (timing, easing, mobile, CSS fallback, reduced-motion), SIG-06/07/08 marked DEFERRED with rationale, mobile collapse/persist matrix present, motion tokens referenced.</done>
</task>

</tasks>

<verification>
1. `grep "DEFERRED" .planning/phases/03-signal-expression/SIGNAL-SPEC.md` — returns 3+ matches (SIG-06, 07, 08)
2. `grep "SIG-01\|SIG-02\|SIG-03\|SIG-04\|SIG-05\|SIG-09\|SIG-10" .planning/phases/03-signal-expression/SIGNAL-SPEC.md` — all IDs present
3. `grep "Collapse\|Persist" .planning/phases/03-signal-expression/SIGNAL-SPEC.md` — mobile behavior documented
4. `grep "duration-instant\|duration-fast\|duration-slow" .planning/phases/03-signal-expression/SIGNAL-SPEC.md` — token references present
</verification>

<success_criteria>
- SIGNAL-SPEC.md exists with complete effect specifications
- Every implemented SIGNAL effect has timing, easing, mobile, fallback, reduced-motion
- SIG-06, SIG-07, SIG-08 marked DEFERRED with rationale
- Mobile behavior matrix separates collapse vs persist
- Motion token names and ms values referenced throughout
</success_criteria>

<output>
After completion, create `.planning/phases/03-signal-expression/03-04-SUMMARY.md`
</output>
