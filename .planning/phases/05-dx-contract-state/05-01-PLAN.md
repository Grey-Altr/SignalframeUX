---
phase: 05-dx-contract-state
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/SCAFFOLDING.md
  - .planning/DX-SPEC.md
  - components/animation/color-cycle-frame.tsx
autonomous: true
requirements: [DX-01, DX-02, DX-04, DX-05, STP-01, STP-02]

must_haves:
  truths:
    - "A developer can scaffold a new SF component using only SCAFFOLDING.md without asking clarifying questions"
    - "The FRAME/SIGNAL import boundary is documented with layer rules and bridge pattern"
    - "Deferred items (DX-04, DX-05, STP-01) have interface sketches in DX-SPEC.md"
    - "Theme toggle during active GSAP animation produces no OKLCH/inline color conflicts"
  artifacts:
    - path: "docs/SCAFFOLDING.md"
      provides: "SF component scaffolding spec with import boundary"
      contains: "Canonical Pattern"
    - path: ".planning/DX-SPEC.md"
      provides: "Interface sketches for deferred DX-04, DX-05, STP-01"
      contains: "registry.json"
  key_links:
    - from: "docs/SCAFFOLDING.md"
      to: "components/sf/"
      via: "documented file structure and patterns"
      pattern: "sf/.*\\.tsx"
    - from: "docs/SCAFFOLDING.md"
      to: "components/animation/"
      via: "import boundary documentation"
      pattern: "data-anim"
---

<objective>
Create the DX documentation foundation: SCAFFOLDING.md (canonical SF component pattern + import boundary), DX-SPEC.md (deferred item interface sketches), and STP-02 theme toggle GSAP audit.

Purpose: Give developers and AI agents a single reference for scaffolding SF components correctly, document the FRAME/SIGNAL layer boundary, capture deferred item designs for post-v1.0, and verify theme toggle safety.
Output: docs/SCAFFOLDING.md, .planning/DX-SPEC.md, STP-02 audit findings
</objective>

<execution_context>
@.planning/phases/05-dx-contract-state/05-RESEARCH.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-dx-contract-state/05-CONTEXT.md
@components/sf/index.ts
@components/sf/sf-button.tsx
@components/sf/sf-container.tsx
@components/sf/sf-text.tsx
@components/sf/sf-section.tsx
@lib/theme.ts
@app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create docs/SCAFFOLDING.md — SF component scaffolding spec and import boundary</name>
  <files>docs/SCAFFOLDING.md</files>
  <read_first>
    - components/sf/sf-button.tsx (Pattern A — Radix-based wrapper)
    - components/sf/sf-container.tsx (Pattern B — native element + forwardRef)
    - components/sf/sf-text.tsx (Pattern C — polymorphic forwardRef)
    - components/sf/sf-section.tsx (data attributes reference)
    - components/sf/index.ts (barrel export pattern)
    - .planning/phases/05-dx-contract-state/05-RESEARCH.md (canonical patterns, SCAFFOLDING.md structure)
  </read_first>
  <action>
Create `docs/` directory. Write `docs/SCAFFOLDING.md` with these 7 sections:

**1. File Structure** — Where SF files live (`components/sf/sf-{name}.tsx`), naming convention (sf-kebab filename, SFPascal export), one component per file (compound components co-located).

**2. Canonical Pattern** — Document the three sub-patterns extracted from the codebase:
  - Pattern A (Radix-based wrapper): SFButton-style. Import from `@/components/ui/{base}`, CVA variants, named function export. Show annotated sf-button.tsx structure.
  - Pattern B (Native element + forwardRef): SFContainer-style. No Radix base, `React.forwardRef`, `displayName`. Show annotated sf-container.tsx structure.
  - Pattern C (Polymorphic forwardRef): SFText-style. `as` prop, `React.Ref<any>` cast (documented TypeScript limitation). Show annotated sf-text.tsx structure.
  - Mark Pattern A as default for new Radix wrappers, Pattern B for new layout primitives.

**3. CVA Shape** — `intent` as standard variant prop name, `defaultVariants` required on every CVA call, `VariantProps<typeof sfXVariants>` in interface.

**4. Required Props** — `className` always accepted, merged via `cn()` (appended, never replaces base classes), `forwardRef` required on Pattern B/C, `...props` spread last.

**5. Data Attributes** — Table of `data-section`, `data-section-label`, `data-bg-shift`, `data-anim` with type, applied-by, read-by columns. Explain `[data-anim]` as FRAME-to-SIGNAL handoff.

**6. Import Boundary** — `sf/` = FRAME layer (deterministic, stable API). `animation/` = SIGNAL layer (generative, GSAP-driven). `ui/` = shadcn base (never import directly in consumer code). Bridge via `[data-anim]` attribute. Consumer code imports from `@/components/sf` barrel only. No runtime enforcement per user decision.

**7. Barrel Export** — Named exports only in `index.ts`. Layout Primitives section separated. New components added alphabetically within their section.

Keep every section tightly scoped to "how to create a new SF component." Do not expand into style guidelines.
  </action>
  <acceptance_criteria>
    - AC-1: File `docs/SCAFFOLDING.md` exists
    - AC-2: Contains all 7 section headings (File Structure, Canonical Pattern, CVA Shape, Required Props, Data Attributes, Import Boundary, Barrel Export)
    - AC-3: Contains annotated code examples for all three sub-patterns (Pattern A, Pattern B, Pattern C)
    - AC-4: Import boundary section documents sf/ = FRAME, animation/ = SIGNAL, [data-anim] bridge
    - AC-5: CVA section specifies `intent` as standard variant prop name
  </acceptance_criteria>
  <ac_refs>AC-1, AC-2, AC-3, AC-4, AC-5</ac_refs>
  <verify>
    <automated>test -f docs/SCAFFOLDING.md && grep -c "## " docs/SCAFFOLDING.md | grep -q "[7-9]" && grep -q "Pattern A" docs/SCAFFOLDING.md && grep -q "Pattern B" docs/SCAFFOLDING.md && grep -q "Pattern C" docs/SCAFFOLDING.md && grep -q "Import Boundary" docs/SCAFFOLDING.md && grep -q "intent" docs/SCAFFOLDING.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>SCAFFOLDING.md exists in docs/ with all 7 required sections, three annotated sub-patterns, import boundary documentation, and CVA intent standard</done>
</task>

<task type="auto">
  <name>Task 2: Create .planning/DX-SPEC.md — deferred item interface sketches</name>
  <files>.planning/DX-SPEC.md</files>
  <read_first>
    - .planning/phases/05-dx-contract-state/05-CONTEXT.md (deferred item list)
    - .planning/phases/05-dx-contract-state/05-RESEARCH.md (DX-SPEC.md structure, pitfall 4 on over-specification)
    - .planning/REQUIREMENTS.md (DX-04, DX-05, STP-01 descriptions)
  </read_first>
  <action>
Write `.planning/DX-SPEC.md` with interface sketches for three deferred items:

**DX-04: registry.json** — TypeScript interface for the registry shape (components array with name, file, description, dependencies, variants). Annotate with open questions: distribution format (JSON vs package), versioning strategy, shadcn compatibility. Do NOT write implementation — shape only.

**DX-05: createSignalframeUX + useSignalframe** — TypeScript interface for config object (`createSignalframeUX(config)`) and hook return type (`useSignalframe()` returning tokens, theme, motion controllers). Annotate open questions: provider pattern vs global singleton, SSR hydration strategy, motion controller scope. Do NOT write function bodies.

**STP-01: Session State Persistence** — TypeScript interface for session state shape (component browser filters, token explorer tab, scroll positions). Annotate open questions: storage backend (sessionStorage vs zustand), hydration timing, state reset policy. Do NOT write implementation.

Each section: heading, one-paragraph rationale, TypeScript interface block, "Open Questions" list. Keep interfaces minimal (shape, not implementation detail). Follow Research pitfall 4: annotate with open questions, do not lock in decisions.
  </action>
  <acceptance_criteria>
    - AC-6: File `.planning/DX-SPEC.md` exists
    - AC-7: Contains interface sketch for DX-04 (registry.json) with TypeScript interface
    - AC-8: Contains interface sketch for DX-05 (createSignalframeUX/useSignalframe) with TypeScript interface
    - AC-9: Contains interface sketch for STP-01 (session state) with TypeScript interface
    - AC-10: Each section has "Open Questions" list (not locked decisions)
  </acceptance_criteria>
  <ac_refs>AC-6, AC-7, AC-8, AC-9, AC-10</ac_refs>
  <verify>
    <automated>test -f .planning/DX-SPEC.md && grep -q "DX-04" .planning/DX-SPEC.md && grep -q "DX-05" .planning/DX-SPEC.md && grep -q "STP-01" .planning/DX-SPEC.md && grep -q "Open Questions" .planning/DX-SPEC.md && grep -q "interface" .planning/DX-SPEC.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>DX-SPEC.md exists with TypeScript interface sketches and open questions for all three deferred items (DX-04, DX-05, STP-01)</done>
</task>

<task type="auto">
  <name>Task 3: STP-02 audit — theme toggle GSAP color conflict verification</name>
  <files>components/animation/color-cycle-frame.tsx</files>
  <read_first>
    - lib/theme.ts (sf-no-transition mechanism)
    - components/animation/color-cycle-frame.tsx (--color-primary mutation via setProperty)
    - components/animation/hero-mesh.tsx (canvas rgba — expected theme-neutral)
    - components/animation/vhs-overlay.tsx (GSAP opacity — expected non-color)
    - app/globals.css (sf-no-transition CSS rule, lines ~799-806)
    - .planning/phases/05-dx-contract-state/05-RESEARCH.md (STP-02 analysis, expected no-change outcome)
  </read_first>
  <action>
Perform the STP-02 theme toggle GSAP guard audit:

1. **Grep audit**: Search all files under `components/animation/` for GSAP color mutations: `gsap.to|gsap.from|gsap.fromTo|gsap.set` with color/backgroundColor/fill/stroke properties. Search for any `style.setProperty` calls that set color-bearing CSS variables.

2. **Verify sf-no-transition mechanism**: Confirm `lib/theme.ts` applies `sf-no-transition` class before theme toggle and removes after 2 rAF ticks. Confirm `globals.css` backs this with `transition: none !important` and `animation-duration: 0.01ms !important`.

3. **Assess color-cycle-frame.tsx**: This component calls `document.documentElement.style.setProperty("--color-primary", ...)`. Determine if this conflicts with theme toggle. Per research: CSS variable mutation is instant (not transition-dependent), so `sf-no-transition` suppression is not needed for `setProperty` calls. However, if a theme toggle occurs DURING a color cycle animation, the cycling could overwrite the theme's intended `--color-primary` value. If this conflict exists, add a guard: check for `sf-no-transition` class presence before calling `setProperty`, and skip the setProperty call if the class is present. This is a minimal 2-line guard.

4. **Document findings**: Add a JSDoc comment block at the top of color-cycle-frame.tsx documenting the theme toggle interaction: what's guarded, what's safe, why canvas components are exempt.

If the grep audit finds no GSAP color mutations (expected per research), document the null finding as a comment in color-cycle-frame.tsx. The audit itself IS the deliverable.
  </action>
  <acceptance_criteria>
    - AC-11: Grep audit completed — no undocumented GSAP color mutations exist in animation/ files
    - AC-12: color-cycle-frame.tsx has a documented theme toggle interaction comment
    - AC-13: If setProperty/sf-no-transition conflict exists, guard added; if no conflict, null finding documented
  </acceptance_criteria>
  <ac_refs>AC-11, AC-12, AC-13</ac_refs>
  <verify>
    <automated>grep -rn "gsap\.\(to\|from\|fromTo\|set\).*\(color\|background\|fill\|stroke\)" components/animation/ ; grep -q "theme toggle" components/animation/color-cycle-frame.tsx && echo "PASS: audit documented" || echo "FAIL: missing audit documentation"</automated>
  </verify>
  <done>STP-02 audit complete — all GSAP/theme color conflict surfaces identified and documented, color-cycle-frame.tsx has theme interaction comment, guard added if needed</done>
</task>

</tasks>

<verification>
- `docs/SCAFFOLDING.md` exists with 7 sections, 3 annotated patterns, import boundary
- `.planning/DX-SPEC.md` exists with 3 deferred item interface sketches
- STP-02 grep audit finds no undocumented GSAP color conflicts
- `color-cycle-frame.tsx` has theme toggle interaction documentation
- No new components, tokens, or runtime enforcement added
</verification>

<success_criteria>
- AC-1 through AC-13 pass
- A developer can scaffold a new SF component using only SCAFFOLDING.md
- Deferred items have interface sketches with open questions (not locked decisions)
- Theme toggle during GSAP animation is verified safe or guarded
</success_criteria>

<output>
After completion, create `.planning/phases/05-dx-contract-state/05-01-SUMMARY.md`
</output>
