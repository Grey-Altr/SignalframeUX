---
phase: 10-foundation-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/globals.css
  - components/sf/sf-section.tsx
autonomous: true
requirements:
  - FND-01
  - FND-02
must_haves:
  truths:
    - "SignalOverlay renders correct colors on first page load — no magenta flash from missing CSS var defaults"
    - "bgShift prop accepts only 'white' or 'black' string values, not boolean"
    - "TypeScript compilation passes with zero errors after bgShift type change"
  artifacts:
    - path: "app/globals.css"
      provides: "--signal-intensity, --signal-speed, --signal-accent defaults in :root"
      contains: "--signal-intensity: 0.5"
    - path: "components/sf/sf-section.tsx"
      provides: "Typed bgShift prop with value passthrough"
      contains: "bgShift?: \"white\" | \"black\""
  key_links:
    - from: "components/animation/signal-overlay.tsx"
      to: "app/globals.css"
      via: "CSS custom property defaults read before SignalOverlay mounts"
      pattern: "--signal-intensity"
    - from: "components/layout/page-animations.tsx"
      to: "components/sf/sf-section.tsx"
      via: "getAttribute('data-bg-shift') reads typed string value"
      pattern: "data-bg-shift"
---

<objective>
Add missing SIGNAL runtime CSS custom property defaults to globals.css and fix the SFSection bgShift prop from boolean to typed string union.

Purpose: Eliminate magenta flash on first frame (missing --signal-* defaults) and make bgShift prop type-safe so GSAP scroll animations receive explicit palette keys instead of empty strings. These are prerequisites for Phase 12 SIGNAL wiring.

Output: Two corrected source files — globals.css with signal defaults, sf-section.tsx with typed bgShift.
</objective>

<execution_context>
@/Users/greyaltaer/.claude/pde-os/engines/gsd/workflows/execute-plan.md
@/Users/greyaltaer/.claude/pde-os/engines/gsd/templates/summary.md
</execution_context>

<context>
@.planning/project-context.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-foundation-fixes/10-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From components/sf/sf-section.tsx (CURRENT — to be changed):
```typescript
interface SFSectionProps extends React.ComponentProps<"section"> {
  label?: string;
  bgShift?: boolean;  // CHANGE TO: "white" | "black"
  spacing?: "8" | "12" | "16" | "24";
}
// Render: data-bg-shift={bgShift ? "" : undefined}  // CHANGE TO: data-bg-shift={bgShift}
```

From components/animation/signal-overlay.tsx (read-only reference):
```typescript
// DEFAULTS constant (lines 34-38):
const DEFAULTS = { intensity: 50, speed: 50, accent: 0 };
// Maps to CSS: --signal-intensity: 0.5, --signal-speed: 1, --signal-accent: 0
```

From components/layout/page-animations.tsx (read-only reference):
```typescript
// applyBgShift reads: getAttribute("data-bg-shift") as string key into palette object
// palette has keys: "white" and "black"
```
</interfaces>
</context>

<acceptance_criteria>
**AC-1:** Given globals.css is loaded in the browser, when the page renders before SignalOverlay mounts, then `getComputedStyle(document.documentElement).getPropertyValue('--signal-intensity')` returns `0.5` (not empty string)
**AC-2:** Given globals.css is loaded, when inspecting :root CSS vars, then `--signal-speed` defaults to `1` and `--signal-accent` defaults to `0`
**AC-3:** Given sf-section.tsx is compiled, when a consumer passes `bgShift="white"`, then TypeScript compiles without errors
**AC-4:** Given sf-section.tsx is compiled, when a consumer passes `bgShift={true}`, then TypeScript reports a type error (boolean no longer accepted)
**AC-5:** Given SFSection renders with `bgShift="black"`, when the DOM is inspected, then `data-bg-shift` attribute value is `"black"` (not empty string)
**AC-6:** Given all changes are applied, when `pnpm tsc --noEmit` runs, then exit code is 0
</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Add SIGNAL runtime CSS custom property defaults to globals.css</name>
  <files>app/globals.css</files>
  <read_first>
    - app/globals.css (current :root block, lines 103-155 — insertion point after --sf-vhs-noise-opacity on line 150)
    - components/animation/signal-overlay.tsx (DEFAULTS constant lines 34-38, handleReset lines 152-159 — confirms exact default values)
  </read_first>
  <action>
In `app/globals.css`, inside the `:root` block that starts at line 103 (the "SignalframeUX Extension Variables" block), insert the following 5 lines AFTER line 150 (`--sf-vhs-noise-opacity: 0.015;`) and BEFORE line 152 (`/* -- Spacing Tokens`):

```css
  /* ── SIGNAL Runtime Defaults ── */
  /* Read by color-resolve.ts and WebGL uniforms. SignalOverlay overrides at runtime. */
  --signal-intensity: 0.5;
  --signal-speed: 1;
  --signal-accent: 0;
```

These values are derived from SignalOverlay's DEFAULTS constant: intensity=50/100=0.5, speed=50/50=1.0, accent=0. Do NOT place these inside a `@theme` block — they are runtime-settable vars that JavaScript writes via `setProperty()`, not Tailwind design tokens.
  </action>
  <ac_refs>AC-1, AC-2</ac_refs>
  <verify>
    <automated>grep -n "signal-intensity\|signal-speed\|signal-accent" /Users/greyaltaer/code/projects/SignalframeUX/app/globals.css</automated>
  </verify>
  <acceptance_criteria>
    - globals.css contains the line `--signal-intensity: 0.5;`
    - globals.css contains the line `--signal-speed: 1;`
    - globals.css contains the line `--signal-accent: 0;`
    - All three lines appear inside a `:root` block (not inside `@theme`)
    - The comment `/* ── SIGNAL Runtime Defaults ── */` appears before the three vars
  </acceptance_criteria>
  <done>Three --signal-* CSS custom properties declared with correct defaults in the :root block of globals.css</done>
</task>

<task type="auto">
  <name>Task 2: Fix SFSection bgShift prop type from boolean to string union</name>
  <files>components/sf/sf-section.tsx</files>
  <read_first>
    - components/sf/sf-section.tsx (full file — 51 lines, interface at line 4-8, render at line 38, JSDoc at lines 10-27)
    - components/layout/page-animations.tsx (applyBgShift function — confirms it reads getAttribute("data-bg-shift") as a string palette key)
  </read_first>
  <action>
In `components/sf/sf-section.tsx`, make these three changes:

1. **Interface (line 6):** Change `bgShift?: boolean;` to `bgShift?: "white" | "black";`

2. **Render expression (line 38):** Change `data-bg-shift={bgShift ? "" : undefined}` to `data-bg-shift={bgShift}`

3. **JSDoc updates:**
   - Line 19: Change `@param bgShift - Enables data-bg-shift presence attribute for GSAP scroll targeting` to `@param bgShift - Background shift value for GSAP scroll targeting. "white" or "black".`
   - Line 24: Change `<SFSection label="Work" spacing="24" bgShift>` to `<SFSection label="Work" spacing="24" bgShift="white">`

Do NOT use `boolean | "white" | "black"` union — the boolean mode produced empty strings that GSAP couldn't use. Clean break to string-only.

After editing, run `pnpm tsc --noEmit` to confirm zero TypeScript errors. Research confirmed zero call sites use `bgShift=` prop (all existing usage passes `data-bg-shift` as a spread HTML attribute), so no consumer files need updating.
  </action>
  <ac_refs>AC-3, AC-4, AC-5, AC-6</ac_refs>
  <verify>
    <automated>cd /Users/greyaltaer/code/projects/SignalframeUX && grep -n 'bgShift' components/sf/sf-section.tsx && pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - sf-section.tsx contains `bgShift?: "white" | "black"` (exact string, no boolean in union)
    - sf-section.tsx contains `data-bg-shift={bgShift}` (no ternary, no empty string)
    - sf-section.tsx JSDoc contains `bgShift="white"` in the @example block
    - sf-section.tsx JSDoc @param bgShift line contains the word "white" or "black"
    - `pnpm tsc --noEmit` exits with code 0
  </acceptance_criteria>
  <done>SFSection bgShift prop typed as "white" | "black", render passes value directly to data-bg-shift attribute, JSDoc updated, tsc clean</done>
</task>

</tasks>

<verification>
1. `grep -n "signal-intensity\|signal-speed\|signal-accent" app/globals.css` — shows all three vars with correct values
2. `grep -n "bgShift" components/sf/sf-section.tsx` — shows string union type, direct passthrough render, updated JSDoc
3. `pnpm tsc --noEmit` — exits 0, confirming no type errors introduced
4. `pnpm build` — full build succeeds (catches module-level issues beyond tsc)
</verification>

<success_criteria>
- globals.css has --signal-intensity: 0.5, --signal-speed: 1, --signal-accent: 0 in :root block
- sf-section.tsx bgShift typed as "white" | "black" with direct attribute passthrough
- pnpm tsc --noEmit exits 0
- pnpm build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/10-foundation-fixes/10-01-SUMMARY.md`
</output>
