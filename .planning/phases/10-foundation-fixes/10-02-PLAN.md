---
phase: 10-foundation-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/reference/page.tsx
  - app/start/page.tsx
autonomous: true
requirements:
  - INT-01
must_haves:
  truths:
    - "Reference page content starts below the fixed nav bar — no overlap"
    - "NEXT_CARDS grid on the start page is wrapped in a semantic SFSection element"
  artifacts:
    - path: "app/reference/page.tsx"
      provides: "Nav-height margin on main element"
      contains: "mt-[var(--nav-height)]"
    - path: "app/start/page.tsx"
      provides: "NEXT_CARDS wrapped in SFSection"
      contains: "SFSection label=\"NEXT STEPS\""
  key_links:
    - from: "app/reference/page.tsx"
      to: "app/globals.css"
      via: "CSS var --nav-height consumed by mt-[var(--nav-height)]"
      pattern: "mt-\\[var\\(--nav-height\\)\\]"
    - from: "app/start/page.tsx"
      to: "components/sf/sf-section.tsx"
      via: "SFSection wrapper around NEXT_CARDS grid"
      pattern: "SFSection.*NEXT STEPS"
---

<objective>
Fix reference page nav clearance and wrap the NEXT_CARDS grid in SFSection for semantic consistency.

Purpose: The reference page content currently renders behind the fixed nav bar. The NEXT_CARDS grid on the start page is the only major content section not wrapped in SFSection, breaking semantic consistency and GSAP targeting.

Output: Two corrected page files — reference/page.tsx with nav margin, start/page.tsx with SFSection-wrapped NEXT_CARDS.
</objective>

<execution_context>
@/Users/greyaltaer/.claude/pde-os/engines/gsd/workflows/execute-plan.md
@/Users/greyaltaer/.claude/pde-os/engines/gsd/templates/summary.md
</execution_context>

<context>
@.planning/project-context.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-foundation-fixes/10-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From app/reference/page.tsx (CURRENT — line 17):
```tsx
<main id="main-content" data-cursor>
// NEEDS: className="mt-[var(--nav-height)]"
```

From app/start/page.tsx (CURRENT — line 305):
```tsx
<div data-anim="stagger" className="grid grid-cols-1 sm:grid-cols-3 border-b-[3px] border-foreground">
// NEEDS: SFSection wrapper with label="NEXT STEPS" className="py-0"
```

From app/start/page.tsx (line 4 — SFSection already imported):
```tsx
import { SFButton, SFBadge, SFSection } from "@/components/sf";
```

From app/globals.css (line 171 — nav-height already declared):
```css
--nav-height: 83px;
```

From app/start/page.tsx line 188 (correct pattern to replicate for reference page):
```tsx
<main id="main-content" data-cursor className="mt-[var(--nav-height)]">
```
</interfaces>
</context>

<acceptance_criteria>
**AC-1:** Given the reference page is rendered in a browser, when the user navigates to /reference, then the page content starts below the 83px fixed nav bar (no overlap)
**AC-2:** Given app/reference/page.tsx is inspected, when the `<main>` element is examined, then it has `className="mt-[var(--nav-height)]"` matching the pattern used by app/start/page.tsx
**AC-3:** Given the start page is rendered, when the NEXT_CARDS section is inspected in the DOM, then it is wrapped in a `<section>` element with `data-section-label="NEXT STEPS"`
**AC-4:** Given the SFSection wrapper has `className="py-0"`, when the page renders, then no extra vertical padding is added around the NEXT_CARDS grid (cards retain their own py-10 spacing)
**AC-5:** Given all changes are applied, when `pnpm tsc --noEmit` runs, then exit code is 0
</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Add nav-height margin to reference page and wrap NEXT_CARDS in SFSection</name>
  <files>app/reference/page.tsx, app/start/page.tsx</files>
  <read_first>
    - app/reference/page.tsx (full file — 27 lines, main tag at line 17 missing mt-[var(--nav-height)])
    - app/start/page.tsx (lines 1-7 for imports, lines 295-340 for NEXT_CARDS section boundary)
    - app/start/page.tsx (line 188 — correct nav-height pattern to replicate)
  </read_first>
  <action>
**Change 1 — app/reference/page.tsx line 17:**

Change:
```tsx
<main id="main-content" data-cursor>
```
To:
```tsx
<main id="main-content" data-cursor className="mt-[var(--nav-height)]">
```

This matches the exact pattern already used in `app/start/page.tsx` line 188. The `--nav-height: 83px` CSS var is already declared in globals.css line 171.

**Change 2 — app/start/page.tsx lines 305-333:**

Wrap the existing NEXT_CARDS `<div>` (line 305) in an SFSection. The closing `</div>` is at line 333.

Change:
```tsx
        {/* ═══ NEXT STEPS ═══ */}
        <div data-anim="stagger" className="grid grid-cols-1 sm:grid-cols-3 border-b-[3px] border-foreground">
```
To:
```tsx
        {/* ═══ NEXT STEPS ═══ */}
        <SFSection label="NEXT STEPS" className="py-0">
          <div data-anim="stagger" className="grid grid-cols-1 sm:grid-cols-3 border-b-[3px] border-foreground">
```

And after the closing `</div>` at line 333, add:
```tsx
        </SFSection>
```

CRITICAL: Use `className="py-0"` on SFSection to override the default `py-16` padding. The inner cards have their own `py-10` spacing. Without `py-0`, you get 128px extra padding. SFSection is already imported at line 4.

After both changes, run `pnpm tsc --noEmit` to confirm zero TypeScript errors.
  </action>
  <ac_refs>AC-1, AC-2, AC-3, AC-4, AC-5</ac_refs>
  <verify>
    <automated>cd /Users/greyaltaer/code/projects/SignalframeUX && grep -n "mt-\[var(--nav-height)\]" app/reference/page.tsx && grep -n "NEXT STEPS" app/start/page.tsx && pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - app/reference/page.tsx contains `className="mt-[var(--nav-height)]"` on the main element
    - app/start/page.tsx contains `SFSection label="NEXT STEPS"` wrapping the NEXT_CARDS grid
    - app/start/page.tsx SFSection has `className="py-0"` (no extra padding)
    - The inner grid div with `data-anim="stagger"` is preserved unchanged inside the SFSection
    - `pnpm tsc --noEmit` exits with code 0
  </acceptance_criteria>
  <done>Reference page has nav clearance margin, NEXT_CARDS grid wrapped in SFSection with py-0 override, tsc clean</done>
</task>

</tasks>

<verification>
1. `grep -n "mt-\[var(--nav-height)\]" app/reference/page.tsx` — shows margin class on main
2. `grep -n "SFSection.*NEXT STEPS" app/start/page.tsx` — shows SFSection wrapper
3. `grep -n "py-0" app/start/page.tsx` — confirms padding override
4. `pnpm tsc --noEmit` — exits 0
5. `pnpm build` — full build succeeds
</verification>

<success_criteria>
- Reference page main element has mt-[var(--nav-height)] class
- NEXT_CARDS grid is inside SFSection with label="NEXT STEPS" and className="py-0"
- pnpm tsc --noEmit exits 0
- pnpm build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/10-foundation-fixes/10-02-SUMMARY.md`
</output>
