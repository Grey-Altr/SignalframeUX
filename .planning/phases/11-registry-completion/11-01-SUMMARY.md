---
phase: 11-registry-completion
plan: 01
subsystem: registry
tags: [registry, shadcn, dx, meta-fields, layout-primitives]
requires: [10-01]
provides: [registry.json complete, public/r/ built, registry:build script]
affects: [DX-04, components/sf/, public/r/]
tech-stack:
  added: []
  patterns: [shadcn registry build, registry:style cssVars, meta.layer/pattern classification]
key-files:
  created:
    - public/r/sf-container.json
    - public/r/sf-section.json
    - public/r/sf-grid.json
    - public/r/sf-stack.json
    - public/r/sf-text.json
    - public/r/sf-theme.json
    - public/r/sf-label.json
    - public/r/sf-select.json
    - public/r/sf-checkbox.json
    - public/r/sf-radio-group.json
    - public/r/sf-switch.json
    - public/r/sf-textarea.json
    - public/r/circuit-divider.json
    - public/r/vhs-overlay.json
    - public/r/hero-mesh.json
    - public/r/scramble-text.json
  modified:
    - registry.json
    - package.json
decisions:
  - "sf-section and sf-text have no CVA dependency in registry entry — confirmed by source inspection"
  - "shadcn build generates sf-theme.json even for registry:style type — no manual creation needed"
  - "base.json preserved through shadcn build (not listed in registry.json items)"
metrics:
  duration: 2 minutes
  completed: 2026-04-06T11:20:07Z
  tasks_completed: 2
  files_created: 16
  files_modified: 2
requirements:
  - DX-04
requirements_completed: [DX-04]
---

# Phase 11 Plan 01: Registry Completion Summary

**One-liner:** shadcn CLI registry completed with 33 items (27 existing + 5 layout primitives + sf-theme), meta.layer/pattern backfilled on all items, public/r/ rebuilt via `pnpm registry:build`.

## What Was Built

### Task 1: Complete registry.json (33 items + meta)

- Backfilled `meta.layer` and `meta.pattern` on all 27 existing registry items
- Added 5 layout primitive entries (sf-container, sf-section, sf-grid, sf-stack, sf-text) as `registry:ui`
- Added sf-theme as `registry:style` with full cssVars (theme/light/dark sections, OKLCH palette, zero-radius, SIGNAL runtime vars)
- CVA dependency included only where component actually uses it: sf-container, sf-grid, sf-stack (YES); sf-section, sf-text (NO — confirmed by source read)

### Task 2: registry:build script + public/r/ rebuild

- Added `"registry:build": "shadcn build"` to package.json scripts
- `pnpm registry:build` exits 0 — generates all 33 component JSON files
- 16 new public/r/ files created (10 previously missing + 6 new layout/theme entries)
- base.json preserved (not in registry.json items list; shadcn build leaves it untouched)
- sf-theme.json was auto-generated by shadcn build (registry:style type handled correctly)

## Acceptance Criteria Results

| AC | Result | Notes |
|----|--------|-------|
| AC-1: 33 items in registry.json | PASS | Verified by node count |
| AC-2: All items have meta.layer + meta.pattern | PASS | Verified programmatically |
| AC-3: sf-theme is registry:style with cssVars, no files | PASS | Auto-generated correctly |
| AC-4: 5 layout primitives as registry:ui pointing to components/sf/ | PASS | All 5 present |
| AC-5: package.json has registry:build script | PASS | "registry:build": "shadcn build" |
| AC-6: pnpm registry:build exits 0, >=32 JSON files | PASS | 34 component JSON files |
| AC-7: sf-section.json bgShift is string union not boolean | PASS | Phase 10 fix confirmed |

## Deviations from Plan

None — plan executed exactly as written.

## Self-Check

- registry.json: 33 items ✓
- public/r/ JSON count: 35 files (34 component files + registry.json) ✓
- sf-section.json bgShift string union: confirmed ✓
- circuit-divider.json gsap dependency: confirmed ✓
- sf-theme.json cssVars.theme/light/dark: confirmed ✓
- base.json preserved: confirmed ✓
- package.json registry:build: confirmed ✓

## Self-Check: PASSED
