# Phase 17: P1 Non-Animated Components - Context

**Gathered:** 2026-04-06
**Status:** Ready for planning

<domain>
## Phase Boundary

Seven FRAME-only SF-wrapped components are live in the system — SFAvatar, SFBreadcrumb, SFEmptyState, SFAlertDialog, SFAlert, SFCollapsible, SFStatusDot. All follow the SCAFFOLDING.md 9-point checklist. No SIGNAL layer involvement except SFStatusDot (GSAP pulse on active state) and SFEmptyState (optional ScrambleText slot).

</domain>

<decisions>
## Implementation Decisions

### Visual Design Language
- SFEmptyState gets full glitch/noise texture treatment — Bayer dither pattern background, monospace text, high visual tension matching hero SIGNAL aesthetic
- SFAlert color mapping uses existing tokens: info=primary, warning=accent, destructive=destructive, success=success — no new tokens
- SFStatusDot is 8px square (matches 4px grid), solid fill, no border
- SFAvatar fallback icon is Lucide User at 60% container size, foreground color

### Component API Patterns
- SFAlertDialog loading state via `loading` boolean prop on confirm trigger — disables button + shows spinner
- SFEmptyState uses `action` slot + `title` prop with optional `scramble` boolean for ScrambleText treatment
- SFBreadcrumb separator is forward slash `/` in monospace — DU/TDR minimalism
- SFCollapsible uses `asChild` on trigger (Radix default) — user provides their own button

### Integration & Showcase
- New components added to ComponentsExplorer under their category groups — no separate demo pages
- SFStatusDot GSAP pulse uses `--duration-normal` (200ms) with `--ease-default` — subtle, not distracting
- Registry meta.pattern: Pattern A (Radix-wrapped) for Avatar, Breadcrumb, AlertDialog, Alert, Collapsible; Pattern C (pure-SF) for EmptyState and StatusDot

### Claude's Discretion
- Internal implementation details for each wrapper (cn() class composition, prop forwarding)
- JSDoc content beyond required fields
- ComponentsExplorer entry descriptions and ordering within categories

</decisions>

<code_context>
## Existing Code Insights

### Reusable Assets
- SFButton pattern: CVA with `intent` key, cn() class merging, Omit<> for prop override, JSDoc block
- SCAFFOLDING.md: 9-point checklist for all new wrappers
- ScrambleText component exists in `components/animation/` for EmptyState integration
- Lucide React for icons (Avatar fallback, Alert icons)
- shadcn bases installed in Phase 16: accordion, alert-dialog, avatar, collapsible, progress, navigation-menu, toggle-group

### Established Patterns
- SF wrapping: thin passthrough with cn() and rounded-none enforcement
- Barrel export: sf/index.ts directive-free, 'use client' in individual files
- CVA for visual variants with `intent` as primary key
- Registry entries in registry.json with meta.layer and meta.pattern

### Integration Points
- sf/index.ts barrel export (category comments: Forms, Feedback, Navigation, Data Display, Layout, Generative)
- registry.json for shadcn CLI install
- ComponentsExplorer COMPONENTS array for showcase entries
- public/r/ JSON files generated by `pnpm registry:build`

</code_context>

<specifics>
## Specific Ideas

- SFEmptyState should feel like a "design moment" — not a gray placeholder. The Bayer dither pattern should reference the hero's ordered dither aesthetic.
- SFBreadcrumb must be a Server Component with zero client-side JS.
- SFStatusDot GSAP pulse must be suppressed by prefers-reduced-motion per SCAFFOLDING.md rule 6.

</specifics>

<deferred>
## Deferred Ideas

None — discussion stayed within phase scope.

</deferred>
