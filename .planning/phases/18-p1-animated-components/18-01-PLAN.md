---
phase: 18-p1-animated-components
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - components/sf/sf-accordion.tsx
  - components/sf/sf-progress.tsx
  - components/sf/index.ts
  - registry.json
  - package.json
autonomous: true
requirements: [FD-01, FD-03]

must_haves:
  truths:
    - "User can expand/collapse accordion panels with GSAP stagger on children"
    - "Accordion collapses with reverse stagger"
    - "User sees progress fill animate via GSAP tween when value changes"
    - "Both components degrade to instant state changes under prefers-reduced-motion"
  artifacts:
    - path: "components/sf/sf-accordion.tsx"
      provides: "SFAccordion, SFAccordionItem, SFAccordionTrigger, SFAccordionContent"
      contains: "gsap.fromTo"
    - path: "components/sf/sf-progress.tsx"
      provides: "SFProgress with GSAP fill tween"
      contains: "gsap.to"
    - path: "components/sf/index.ts"
      provides: "Barrel exports for SFAccordion and SFProgress"
      contains: "sf-accordion"
    - path: "registry.json"
      provides: "Registry entries with meta.layer: signal"
      contains: "sf-accordion"
  key_links:
    - from: "components/sf/sf-accordion.tsx"
      to: "@/lib/gsap-core"
      via: "import { gsap }"
      pattern: "gsap.*fromTo"
    - from: "components/sf/sf-progress.tsx"
      to: "@/lib/gsap-core"
      via: "import { gsap }"
      pattern: "gsap\\.to"
    - from: "components/sf/index.ts"
      to: "components/sf/sf-accordion.tsx"
      via: "export"
      pattern: "sf-accordion"
---

<objective>
Ship SFAccordion (GSAP stagger) and SFProgress (GSAP fill tween) as Pattern A SF wrappers with barrel exports and registry entries. Install Sonner dependency for Plan 02.

Purpose: Deliver two of three SIGNAL-eligible P1 components with animation, prefers-reduced-motion guards, and DU/TDR sharp-edged aesthetic.
Output: Two working animated SF components, Sonner installed for next plan.
</objective>

<execution_context>
@/Users/greyaltaer/.claude/pde-os/engines/gsd/workflows/execute-plan.md
@/Users/greyaltaer/.claude/pde-os/engines/gsd/templates/summary.md
</execution_context>

<context>
@.planning/project-context.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-p1-animated-components/18-CONTEXT.md
@.planning/phases/18-p1-animated-components/18-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From components/ui/accordion.tsx (base -- DO NOT modify):
```typescript
"use client"
import { Accordion as AccordionPrimitive } from "radix-ui"
// Exports: Accordion, AccordionItem, AccordionTrigger, AccordionContent
// AccordionContent has: data-open:animate-accordion-down data-closed:animate-accordion-up
// AccordionItem has: not-last:border-b
// AccordionTrigger has: rounded-lg (MUST override with rounded-none)
```

From components/ui/progress.tsx (base -- DO NOT modify):
```typescript
"use client"
import { Progress as ProgressPrimitive } from "radix-ui"
// Root has: rounded-full (MUST override with rounded-none)
// Indicator has: transition-all (MUST remove -- conflicts with GSAP)
// Indicator style: translateX(-${100 - (value || 0)}%)
```

From components/sf/sf-status-dot.tsx (GSAP animation pattern precedent):
```typescript
"use client";
import { useRef, useEffect } from "react";
import { gsap } from "@/lib/gsap-core";
// Pattern: useRef + useEffect, reduced-motion guard BEFORE tween, tween.kill() in cleanup
```

From components/sf/index.ts (barrel -- directive-free):
```typescript
// Feedback section exists at line ~105
// Navigation section at ~126
// Data Display section at ~137
```
</interfaces>
</context>

<acceptance_criteria>
AC-1: SFAccordion opens a panel and GSAP-staggers child content elements at 50ms per child; closing reverses stagger; prefers-reduced-motion shows instant expand with no motion.
AC-2: SFAccordion sub-components (SFAccordionItem, SFAccordionTrigger, SFAccordionContent) all apply rounded-none, overriding base rounded-lg/rounded-full.
AC-3: SFProgress fill width animates via GSAP gsap.to() tween when value prop changes; reduced-motion sets position instantly via gsap.set(); the base transition-all class is removed.
AC-4: Both components are exported from sf/index.ts under Feedback section without adding 'use client' to the barrel.
AC-5: registry.json has entries for sf-accordion and sf-progress with meta.layer: "signal" and meta.pattern: "A".
AC-6: Sonner is installed as a project dependency.
AC-7: pnpm build completes with zero errors.
</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Install Sonner + create SFAccordion with GSAP stagger</name>
  <read_first>
    - components/ui/accordion.tsx
    - components/sf/sf-status-dot.tsx
    - lib/gsap-core.ts
    - SCAFFOLDING.md (SF Wrapper Creation Checklist section)
  </read_first>
  <files>components/sf/sf-accordion.tsx, package.json</files>
  <action>
1. Install Sonner for Plan 02: `pnpm add sonner`

2. Create `components/sf/sf-accordion.tsx` as a Pattern A wrapper:
   - `'use client'` directive at top
   - Import base Accordion components from `@/components/ui/accordion`
   - Import `{ gsap }` from `@/lib/gsap-core`
   - Import `{ cn }` from `@/lib/utils`
   - Import `useRef, useEffect` from `react`

3. SFAccordion (root): Thin passthrough of Accordion base. No class changes needed on root.

4. SFAccordionItem: Override className to add `rounded-none border-b border-foreground/20`. Remove base `not-last:border-b` by overriding fully.

5. SFAccordionTrigger: Override className to replace `rounded-lg` with `rounded-none`. Keep all other base classes (flex, outline, hover:underline, focus-visible ring). Use `cn()` to merge.

6. SFAccordionContent: This is the animated component.
   - Accept a `ref` via `useRef` on the inner content `<div>`.
   - On expand (detect via Radix `data-state="open"` or use a controlled `open` state), run GSAP stagger on `contentRef.current.children`:
     ```
     gsap.fromTo(children,
       { opacity: 0, y: 8 },
       { opacity: 1, y: 0, duration: 0.2, stagger: 0.05, ease: "power2.out" }
     )
     ```
   - Use a `MutationObserver` or `ResizeObserver` on the content wrapper to detect when Radix renders the content (since AccordionContent unmounts/mounts children).
   - Alternatively: Use `useEffect` with a dependency that triggers on open state. Since the base uses `data-open`/`data-closed` CSS animations for height, the children stagger is independent. Attach `useEffect` that runs when the SFAccordionContent mounts its inner div. The content is only in the DOM when open (Radix's `forceMount` is not used by default).
   - Reduced-motion guard: Check `window.matchMedia('(prefers-reduced-motion: reduce)').matches` BEFORE creating any tween. If true, skip stagger entirely (Radix CSS handles show/hide).
   - Cleanup: `tween.kill()` in useEffect return.
   - Override: Remove or keep `animate-accordion-down`/`animate-accordion-up` classes per research recommendation -- keep them for container height animation, GSAP only staggers the children inside.
   - Apply `rounded-none` on the content container.

7. Export all four: SFAccordion, SFAccordionItem, SFAccordionTrigger, SFAccordionContent.

8. JSDoc on SFAccordion with @example showing basic usage.
  </action>
  <ac_refs>AC-1, AC-2</ac_refs>
  <verify>
    <automated>cd /Users/greyaltaer/code/projects/SignalframeUX && grep -c "gsap.fromTo" components/sf/sf-accordion.tsx && grep -c "rounded-none" components/sf/sf-accordion.tsx && grep -c "prefers-reduced-motion" components/sf/sf-accordion.tsx && grep -c "stagger" components/sf/sf-accordion.tsx && pnpm list sonner</automated>
  </verify>
  <done>SFAccordion renders with GSAP stagger on expand (50ms per child), reverse on collapse, reduced-motion guard skips animation, all sub-components have rounded-none, Sonner installed.</done>
</task>

<task type="auto">
  <name>Task 2: Create SFProgress with GSAP fill tween + barrel + registry</name>
  <read_first>
    - components/ui/progress.tsx
    - components/sf/sf-status-dot.tsx
    - components/sf/index.ts
    - registry.json
  </read_first>
  <files>components/sf/sf-progress.tsx, components/sf/index.ts, registry.json</files>
  <action>
1. Create `components/sf/sf-progress.tsx` as a Pattern A wrapper:
   - `'use client'` directive
   - Import `{ Progress as ProgressPrimitive }` from `radix-ui` (rewrite Radix structure directly, do NOT wrap base -- need ref access to indicator)
   - Import `{ gsap }` from `@/lib/gsap-core`
   - Import `{ cn }` from `@/lib/utils`, `useRef, useEffect` from `react`

2. SFProgress component:
   - Render Radix ProgressPrimitive.Root with `data-slot="progress"`, classes: `relative flex h-1 w-full items-center overflow-x-hidden rounded-none bg-muted` (note: `rounded-none` replaces base `rounded-full`)
   - Render ProgressPrimitive.Indicator with ref, `data-slot="progress-indicator"`, classes: `size-full flex-1 rounded-none bg-primary` (NO `transition-all` -- GSAP is sole animation driver)
   - Set initial inline style: `transform: translateX(-${100 - (value || 0)}%)`

3. GSAP tween in useEffect, dependency on `value`:
   - If reduced-motion: `gsap.set(indicatorRef.current, { xPercent: -(100 - (value || 0)) })` and return
   - Otherwise: `gsap.to(indicatorRef.current, { xPercent: -(100 - (value || 0)), duration: 0.2, ease: "power2.out" })`
   - Cleanup: `tween.kill()`

4. Export `SFProgress`.

5. JSDoc with @example showing `<SFProgress value={60} />`.

6. Update `components/sf/index.ts` -- add under `// Feedback` section:
   ```
   export { SFAccordion, SFAccordionItem, SFAccordionTrigger, SFAccordionContent } from "./sf-accordion";
   export { SFProgress } from "./sf-progress";
   ```
   DO NOT add 'use client' to barrel.

7. Update `registry.json` -- add two entries:
   - sf-accordion: type registry:ui, dependencies ["gsap"], registryDependencies ["accordion"], files [components/sf/sf-accordion.tsx], meta { layer: "signal", pattern: "A" }
   - sf-progress: type registry:ui, dependencies ["gsap"], registryDependencies ["progress"], files [components/sf/sf-progress.tsx], meta { layer: "signal", pattern: "A" }

8. Verify build: `pnpm build` must pass with zero errors.
  </action>
  <ac_refs>AC-3, AC-4, AC-5, AC-6, AC-7</ac_refs>
  <verify>
    <automated>cd /Users/greyaltaer/code/projects/SignalframeUX && grep -c "rounded-none" components/sf/sf-progress.tsx && grep "transition-all" components/sf/sf-progress.tsx; echo "exit:$?" && grep "sf-accordion" components/sf/index.ts && grep "sf-progress" components/sf/index.ts && grep "'use client'" components/sf/index.ts; echo "barrel-directive:$?" && grep "sf-accordion" registry.json && grep "sf-progress" registry.json && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <done>SFProgress animates fill via GSAP tween, reduced-motion uses gsap.set(), no transition-all class, both components in barrel (no directive contamination), both in registry with meta.layer: "signal", build passes.</done>
</task>

</tasks>

<verification>
- `pnpm build` passes with zero errors
- `grep "'use client'" components/sf/index.ts` returns nothing (barrel stays directive-free)
- `grep "rounded-none" components/sf/sf-accordion.tsx` returns >= 3 matches (item, trigger, content)
- `grep "rounded-none" components/sf/sf-progress.tsx` returns >= 2 matches (root, indicator)
- `grep "transition-all" components/sf/sf-progress.tsx` returns 0 matches (removed)
- `grep "prefers-reduced-motion" components/sf/sf-accordion.tsx` returns >= 1 match
- `grep "prefers-reduced-motion" components/sf/sf-progress.tsx` returns >= 1 match
- `pnpm list sonner` shows sonner installed
</verification>

<success_criteria>
SFAccordion and SFProgress are functional Pattern A wrappers with GSAP animation, reduced-motion guards, rounded-none everywhere, exported from barrel without directive contamination, registered in registry.json with signal/A metadata, and build passes.
</success_criteria>

<output>
After completion, create `.planning/phases/18-p1-animated-components/18-01-SUMMARY.md`
</output>
