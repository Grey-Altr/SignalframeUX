---
phase: 20-p3-registry-only-final-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/ui/calendar.tsx
  - components/ui/menubar.tsx
  - components/sf/sf-calendar.tsx
  - components/sf/sf-calendar-lazy.tsx
  - components/sf/sf-menubar.tsx
  - components/sf/sf-menubar-lazy.tsx
  - registry.json
autonomous: true
requirements: [REG-01, REG-02]

must_haves:
  truths:
    - "SFCalendar renders a date picker with zero border-radius on all sub-elements"
    - "SFCalendar loads lazily via next/dynamic and does not appear in the initial shared bundle"
    - "SFMenubar renders a desktop menubar with zero border-radius on all sub-elements"
    - "SFMenubar loads lazily via next/dynamic and is NOT exported from sf/index.ts"
    - "Neither SFCalendar nor SFMenubar appear in sf/index.ts barrel"
  artifacts:
    - path: "components/sf/sf-calendar.tsx"
      provides: "SF-wrapped calendar with rounded-none overrides"
      contains: "rounded-none"
    - path: "components/sf/sf-calendar-lazy.tsx"
      provides: "next/dynamic lazy loader with SFSkeleton fallback"
      contains: "ssr: false"
    - path: "components/sf/sf-menubar.tsx"
      provides: "SF-wrapped menubar with rounded-none overrides"
      contains: "rounded-none"
    - path: "components/sf/sf-menubar-lazy.tsx"
      provides: "next/dynamic lazy loader"
      contains: "ssr: false"
  key_links:
    - from: "components/sf/sf-calendar-lazy.tsx"
      to: "components/sf/sf-calendar.tsx"
      via: "dynamic import"
      pattern: "import.*sf-calendar"
    - from: "components/sf/sf-menubar-lazy.tsx"
      to: "components/sf/sf-menubar.tsx"
      via: "dynamic import"
      pattern: "import.*sf-menubar"
    - from: "registry.json"
      to: "components/sf/sf-calendar.tsx"
      via: "registry files entry"
      pattern: "sf-calendar"
---

<objective>
Create SFCalendar and SFMenubar as lazy-loaded Pattern B components with registry entries.

Purpose: Deliver the two heavy-dep P3 components that consumers install via shadcn CLI but that never enter the main bundle. This is the last component work in v1.3.
Output: Two SF wrappers, two lazy loaders, two registry.json entries with meta.heavy: true.
</objective>

<execution_context>
@/Users/greyaltaer/.claude/pde-os/engines/gsd/workflows/execute-plan.md
@/Users/greyaltaer/.claude/pde-os/engines/gsd/templates/summary.md
</execution_context>

<context>
@.planning/project-context.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-p3-registry-only-final-audit/20-RESEARCH.md

<interfaces>
<!-- Existing lazy loader pattern to follow exactly -->

From components/animation/signal-overlay-lazy.tsx:
```typescript
"use client";
import dynamic from "next/dynamic";

const SignalOverlayDynamic = dynamic(
  () =>
    import("@/components/animation/signal-overlay").then((m) => ({
      default: m.SignalOverlay,
    })),
  { ssr: false }
);

export function SignalOverlayLazy() {
  return <SignalOverlayDynamic />;
}
```

From components/sf/index.ts:
```typescript
// P3 components are NEVER added here
// SFCalendar and SFMenubar are imported directly via their -lazy.tsx files
```

From registry.json (existing entry structure):
```json
{
  "name": "sf-navigation-menu",
  "type": "registry:ui",
  "title": "SF Navigation Menu",
  "description": "...",
  "registryDependencies": ["navigation-menu", "sheet"],
  "files": [{ "path": "components/sf/sf-navigation-menu.tsx", "type": "registry:ui" }],
  "meta": { "layer": "frame", "pattern": "A" }
}
```
</interfaces>
</context>

<acceptance_criteria>
- AC-1: `pnpm dlx shadcn@4.1.2 add calendar` installs react-day-picker and date-fns; `components/ui/calendar.tsx` exists
- AC-2: `pnpm dlx shadcn@4.1.2 add menubar` installs base; `components/ui/menubar.tsx` exists
- AC-3: `components/sf/sf-calendar.tsx` wraps ui/calendar with `rounded-none` on root, day cells, nav buttons, and month grid — zero visible border-radius
- AC-4: `components/sf/sf-calendar-lazy.tsx` uses `next/dynamic` with `ssr: false` and `SFSkeleton` fallback; file starts with `'use client'`
- AC-5: `components/sf/sf-menubar.tsx` wraps ui/menubar sub-components with `rounded-none` on trigger, content, item, sub-trigger, sub-content
- AC-6: `components/sf/sf-menubar-lazy.tsx` uses `next/dynamic` with `ssr: false`; file starts with `'use client'`
- AC-7: Neither `sf-calendar` nor `sf-menubar` appear in `sf/index.ts` — `grep -c "calendar\|menubar" components/sf/index.ts` returns 0
- AC-8: `registry.json` has entries for both `sf-calendar` and `sf-menubar` with `meta.heavy: true` and `meta.pattern: "B"`
- AC-9: `pnpm build` passes with zero errors
</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Install shadcn bases + create SFCalendar (wrapper + lazy loader)</name>
  <files>components/ui/calendar.tsx, components/ui/menubar.tsx, components/sf/sf-calendar.tsx, components/sf/sf-calendar-lazy.tsx</files>
  <read_first>
    - components/animation/signal-overlay-lazy.tsx (lazy loader pattern)
    - components/ui/calendar.tsx (after shadcn install — inspect for rounded-* classes)
    - SCAFFOLDING.md (Pattern B definition, checklist)
  </read_first>
  <action>
1. Install shadcn bases (pinned version):
   ```bash
   pnpm dlx shadcn@4.1.2 add calendar
   pnpm dlx shadcn@4.1.2 add menubar
   ```

2. Read the generated `components/ui/calendar.tsx` and identify EVERY `rounded-*` class. These must all be overridden with `rounded-none` in the SF wrapper.

3. Create `components/sf/sf-calendar.tsx`:
   - `'use client'` directive (react-day-picker uses hooks)
   - Import Calendar and CalendarProps from `@/components/ui/calendar`
   - Import `cn` from `@/lib/utils`
   - Export `SFCalendar` function component
   - Apply `rounded-none` on the root className
   - Use the `classNames` prop to override every sub-element that has `rounded-*`:
     - day cells, nav_button, head_cell, cell, day (exact prop names depend on generated code — read ui/calendar.tsx first)
   - Apply `border-2 border-border` for DU/TDR industrial styling
   - JSDoc with `@example` showing import from `sf-calendar-lazy`
   - Comment: "P3 lazy component: import via sf-calendar-lazy.tsx, NOT from sf/index.ts"

4. Create `components/sf/sf-calendar-lazy.tsx`:
   - `'use client'` at line 1
   - `import dynamic from "next/dynamic"`
   - `import { SFSkeleton } from "@/components/sf"`
   - Dynamic import of `sf-calendar` with `.then((m) => ({ default: m.SFCalendar }))`
   - `ssr: false`
   - `loading: () => <SFSkeleton className="h-[350px] w-[280px]" />`
   - Export as `SFCalendarLazy`

5. Do NOT add anything to `sf/index.ts`.
  </action>
  <ac_refs>AC-1, AC-3, AC-4, AC-7</ac_refs>
  <verify>
    <automated>cd /Users/greyaltaer/code/projects/SignalframeUX && test -f components/ui/calendar.tsx && test -f components/sf/sf-calendar.tsx && test -f components/sf/sf-calendar-lazy.tsx && grep -q "ssr: false" components/sf/sf-calendar-lazy.tsx && grep -q "rounded-none" components/sf/sf-calendar.tsx && grep -q "use client" components/sf/sf-calendar-lazy.tsx && echo "PASS"</automated>
  </verify>
  <done>SFCalendar wrapper has rounded-none on all sub-elements, lazy loader uses next/dynamic with ssr:false and SFSkeleton fallback, both shadcn bases installed</done>
</task>

<task type="auto">
  <name>Task 2: Create SFMenubar (wrapper + lazy loader) + registry entries + build gate</name>
  <files>components/sf/sf-menubar.tsx, components/sf/sf-menubar-lazy.tsx, registry.json</files>
  <read_first>
    - components/ui/menubar.tsx (inspect for rounded-* classes and sub-component exports)
    - components/sf/sf-dropdown-menu.tsx (reference pattern for multi-export Radix wraps)
    - registry.json (append new entries at end of items array)
  </read_first>
  <action>
1. Read `components/ui/menubar.tsx` and identify all exported sub-components and all `rounded-*` classes.

2. Create `components/sf/sf-menubar.tsx`:
   - `'use client'` directive
   - Import all sub-components from `@/components/ui/menubar`
   - Import `cn` from `@/lib/utils`
   - Re-export each sub-component with SF prefix and `rounded-none` override:
     - SFMenubar (root)
     - SFMenubarMenu
     - SFMenubarTrigger (rounded-none + sf-focusable)
     - SFMenubarContent (rounded-none + border-2 border-foreground shadow-none)
     - SFMenubarItem (rounded-none)
     - SFMenubarSeparator
     - SFMenubarLabel
     - SFMenubarShortcut
     - SFMenubarGroup
     - SFMenubarSub
     - SFMenubarSubTrigger (rounded-none)
     - SFMenubarSubContent (rounded-none + border-2 border-foreground shadow-none)
     - SFMenubarCheckboxItem (rounded-none)
     - SFMenubarRadioGroup
     - SFMenubarRadioItem (rounded-none)
   - Mirror the approach from sf-dropdown-menu.tsx for the wrapping pattern
   - JSDoc with P3 lazy import note
   - Comment: "P3 lazy component: import via sf-menubar-lazy.tsx, NOT from sf/index.ts"

3. Create `components/sf/sf-menubar-lazy.tsx`:
   - `'use client'` at line 1
   - Dynamic import of `sf-menubar` with default export pointing to SFMenubar
   - `ssr: false`
   - `loading: () => <SFSkeleton className="h-10 w-full" />`
   - Export as `SFMenubarLazy`

4. Add two registry.json entries at the end of the `items` array:

   SFCalendar entry:
   ```json
   {
     "name": "sf-calendar",
     "type": "registry:ui",
     "title": "SF Calendar",
     "description": "Lazy-loaded date calendar with react-day-picker. Import via next/dynamic only — NOT in sf/index.ts barrel.",
     "dependencies": ["react-day-picker", "date-fns"],
     "registryDependencies": ["calendar", "button"],
     "files": [
       { "path": "components/sf/sf-calendar.tsx", "type": "registry:ui" },
       { "path": "components/sf/sf-calendar-lazy.tsx", "type": "registry:ui" }
     ],
     "meta": { "layer": "frame", "pattern": "B", "heavy": true }
   }
   ```

   SFMenubar entry:
   ```json
   {
     "name": "sf-menubar",
     "type": "registry:ui",
     "title": "SF Menubar",
     "description": "Desktop application menubar with Radix primitives. Import via next/dynamic only — NOT in sf/index.ts barrel.",
     "registryDependencies": ["menubar"],
     "files": [
       { "path": "components/sf/sf-menubar.tsx", "type": "registry:ui" },
       { "path": "components/sf/sf-menubar-lazy.tsx", "type": "registry:ui" }
     ],
     "meta": { "layer": "frame", "pattern": "B", "heavy": true }
   }
   ```

5. Do NOT add anything to `sf/index.ts`.

6. Run `pnpm build` to verify zero errors and no bundle regression.
  </action>
  <ac_refs>AC-2, AC-5, AC-6, AC-7, AC-8, AC-9</ac_refs>
  <verify>
    <automated>cd /Users/greyaltaer/code/projects/SignalframeUX && test -f components/sf/sf-menubar.tsx && test -f components/sf/sf-menubar-lazy.tsx && grep -q "ssr: false" components/sf/sf-menubar-lazy.tsx && grep -q "rounded-none" components/sf/sf-menubar.tsx && grep -c "calendar\|menubar" components/sf/index.ts | grep -q "^0$" && grep -q '"heavy": true' registry.json && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <done>SFMenubar wrapper re-exports all sub-components with rounded-none, lazy loader works, both registry entries have meta.heavy: true and meta.pattern: "B", sf/index.ts unchanged, pnpm build passes</done>
</task>

</tasks>

<verification>
- `pnpm build` passes with zero errors
- `grep -c "calendar\|menubar" components/sf/index.ts` returns 0
- `grep -c '"heavy": true' registry.json` returns 2
- Both lazy files contain `'use client'` and `ssr: false`
- Both wrappers contain `rounded-none`
</verification>

<success_criteria>
SFCalendar and SFMenubar exist as Pattern B lazy components with registry entries. Neither pollutes the barrel or initial bundle. Build passes clean.
</success_criteria>

<output>
After completion, create `.planning/phases/20-p3-registry-only-final-audit/20-01-SUMMARY.md`
</output>
