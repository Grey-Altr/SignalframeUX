---
phase: 22-token-finalization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/globals.css
  - lib/color-resolve.ts
autonomous: true
requirements:
  - TK-01
  - TK-04

must_haves:
  truths:
    - "SFAlert intent=success renders a visible green border (bg-success utility is generated)"
    - "SFStatusDot status=active renders a green dot (bg-success utility is generated)"
    - "SFToast success and warning variants render visible border colors"
    - "SignalMesh and GLSLHero render correctly after globals.css changes — no magenta, no black"
    - "color-resolve.ts requires zero code changes — audit confirms safety"
  artifacts:
    - path: "app/globals.css"
      provides: "success/warning tokens in @theme block"
      contains: "--color-success: oklch(0.85 0.25 145)"
    - path: "app/globals.css"
      provides: "success/warning removed from :root"
  key_links:
    - from: "app/globals.css @theme"
      to: "Tailwind utility generation"
      via: "@theme --color-success generates bg-success, text-success, border-success"
      pattern: "--color-success.*oklch"
    - from: "lib/color-resolve.ts"
      to: "signal-mesh.tsx, glsl-hero.tsx"
      via: "resolveColorToken for --color-primary only — success/warning never resolved by WebGL"
      pattern: "resolveColorToken"
---

<objective>
Move --color-success and --color-warning from the :root block into the @theme block in globals.css so Tailwind v4 generates the bg-success, text-success, border-success (and warning equivalents) utility classes. Then audit color-resolve.ts to confirm the WebGL color bridge is unaffected.

Purpose: TK-01 is a functional correctness fix — four components (SFAlert, SFStatusDot, SFToast, ComponentsExplorer) reference bg-success/border-success/border-warning utilities that currently resolve to nothing because the tokens are declared in :root (invisible to Tailwind v4 utility generation) instead of @theme. TK-04 confirms the WebGL pipeline is safe before any OKLCH values move.
Output: globals.css with correct @theme placement; color-resolve.ts audit documented (no code changes).
</objective>

<execution_context>
@/Users/greyaltaer/.claude/pde-os/engines/gsd/workflows/execute-plan.md
@/Users/greyaltaer/.claude/pde-os/engines/gsd/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/22-token-finalization/22-RESEARCH.md
@app/globals.css
@lib/color-resolve.ts

<interfaces>
<!-- Key files the executor needs to understand. -->

From app/globals.css (lines 26-95 — @theme block):
The @theme block ends at line 95. --color-destructive is at line 42.
--color-border is at line 43. success/warning must go BETWEEN these two lines.

From app/globals.css (lines 102-108 — :root extension block):
```css
:root {
  --sf-yellow: oklch(0.91 0.18 98);
  --sf-green: oklch(0.85 0.25 145);
  --sf-clock: oklch(0.145 0 0);
  --color-success: oklch(0.85 0.25 145);   /* REMOVE */
  --color-warning: oklch(0.91 0.18 98);    /* REMOVE */
  --sf-tracking-label: 0.15em;
```

From lib/color-resolve.ts:
```typescript
export function resolveColorToken(cssVar: string, options?: ResolveColorOptions): RGB
export function resolveColorAsThreeColor(cssVar: string, options?: ResolveColorOptions): THREE.Color
```
Callers only pass: --color-primary, --color-foreground, --color-background. Never success/warning.
</interfaces>
</context>

<acceptance_criteria>
**AC-1:** Given globals.css is loaded by Tailwind v4, when the build runs, then `bg-success`, `text-success`, `border-success`, `bg-warning`, `text-warning`, and `border-warning` utility classes are generated (tokens declared inside `@theme` block).

**AC-2:** Given the :root block in globals.css, when inspected after the migration, then `--color-success` and `--color-warning` are absent from :root (no duplicate declarations).

**AC-3:** Given --sf-yellow and --sf-green aliases exist in :root, when the migration completes, then both aliases remain untouched in the :root block.

**AC-4:** Given color-resolve.ts is called only with --color-primary, --color-foreground, and --color-background, when the audit completes, then the file requires zero code changes and no callers reference success/warning tokens.

**AC-5:** Given the globals.css changes, when `pnpm tsc --noEmit` runs, then zero TypeScript errors are reported.
</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Move success/warning tokens from :root to @theme and remove :root fallbacks</name>
  <files>app/globals.css</files>
  <read_first>
    - app/globals.css (full file — executor must see current @theme block ending at line 95 and :root block at lines 102-108)
    - components/sf/sf-alert.tsx (consumer — uses border-success bg-success/10 text-success)
    - components/sf/sf-status-dot.tsx (consumer — uses bg-success)
    - components/sf/sf-toast.tsx (consumer — uses border-success, border-warning)
  </read_first>
  <action>
Edit app/globals.css to perform these exact changes:

1. **Add to @theme block** — Insert two new lines AFTER `--color-destructive: oklch(0.550 0.180 25);` (currently line 42) and BEFORE `--color-border: oklch(0.205 0 0);` (currently line 43):

```css
  /* ── Extended Palette — status/feedback colors ── */
  --color-success: oklch(0.85 0.25 145);
  --color-warning: oklch(0.91 0.18 98);
```

2. **Remove from :root block** — Delete these two lines from the `/* ── SignalframeUX Extension Variables ── */` :root block (currently lines 107-108):

```css
  --color-success: oklch(0.85 0.25 145);
  --color-warning: oklch(0.91 0.18 98);
```

3. **DO NOT touch** `--sf-yellow` and `--sf-green` — they remain in :root. They are sf-namespaced decorative aliases used via `var(--sf-yellow)` in inline styles, not as Tailwind utilities.

4. **DO NOT add dark mode overrides** — the green (L=0.85) and yellow (L=0.91) values have sufficient contrast against the dark background (L=0.145). No `.dark {}` override needed per research.

5. Run `pnpm tsc --noEmit` to confirm zero TypeScript errors.

6. Run `pnpm build` to confirm the build succeeds and the utilities are generated. Verify by checking the built CSS output for the presence of `bg-success` class selector (look in `.next/static/css/*.css` files).
  </action>
  <ac_refs>AC-1, AC-2, AC-3, AC-5</ac_refs>
  <verify>
    <automated>cd /Users/greyaltaer/code/projects/SignalframeUX && grep -n "color-success" app/globals.css && grep -n "color-warning" app/globals.css && pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep "@theme" -A 50 app/globals.css` shows `--color-success` and `--color-warning` within the @theme block (between --color-destructive and --color-border)
    - `grep "SignalframeUX Extension" -A 10 app/globals.css` does NOT contain `--color-success` or `--color-warning`
    - `--sf-yellow` and `--sf-green` still present in :root (grep confirms)
    - `pnpm tsc --noEmit` exits 0
    - `pnpm build` exits 0
  </acceptance_criteria>
  <done>--color-success and --color-warning are in @theme, removed from :root, build passes, and Tailwind generates bg-success/border-success/text-success utilities</done>
</task>

<task type="auto">
  <name>Task 2: Audit color-resolve.ts WebGL bridge — confirm no code changes needed</name>
  <files>lib/color-resolve.ts</files>
  <read_first>
    - lib/color-resolve.ts (full file — the audit target)
    - components/animation/signal-mesh.tsx (caller — grep for resolveColorToken)
    - components/animation/glsl-hero.tsx (caller — grep for resolveColorToken)
    - components/animation/token-viz.tsx (caller — grep for resolveColorToken or resolveColorAsThreeColor)
  </read_first>
  <action>
Perform an audit of color-resolve.ts and its callers. This is a verification task — NO code changes to color-resolve.ts.

1. **Read color-resolve.ts** and confirm:
   - Uses 1x1 canvas probe via `ctx.fillStyle` (browser's native CSS parser handles OKLCH)
   - Fallback on empty string returns magenta `{ r: 255, g: 0, b: 128 }`
   - Cache uses MutationObserver on `:root` class/style attributes
   - Alpha syntax handled by browser natively (not a custom parser)

2. **Grep all callers** to confirm the token list:
   - `signal-mesh.tsx`: resolves `--color-primary` (opaque)
   - `glsl-hero.tsx`: resolves `--color-primary` with TTL 2000ms (opaque)
   - `token-viz.tsx`: resolves `--color-foreground`, `--color-background`, and core 5 colors (all opaque)
   - NO caller resolves `--color-success` or `--color-warning`

3. **Verify WebGL scenes render** after the globals.css changes from Task 1:
   - Run `pnpm dev` and navigate to homepage — SignalMesh icosahedron renders (not magenta)
   - Navigate to any page with GLSLHero — noise field renders with primary color tint
   - These are visual smoke tests confirming no regression

4. **Document audit findings** — No code changes required. The bridge resolves only opaque core tokens; success/warning tokens moving from :root to @theme has zero impact on WebGL rendering.

Run `grep -rn "resolveColor" components/ lib/ --include="*.tsx" --include="*.ts"` to produce the complete caller list as evidence.
  </action>
  <ac_refs>AC-4, AC-5</ac_refs>
  <verify>
    <automated>cd /Users/greyaltaer/code/projects/SignalframeUX && grep -rn "resolveColor" components/ lib/ --include="*.tsx" --include="*.ts" && grep -rn "color-success\|color-warning" components/animation/ --include="*.tsx" --include="*.ts" || echo "No success/warning references in animation components — PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "color-success\|color-warning" components/animation/` returns zero results
    - `grep -rn "resolveColor" components/ lib/` returns only signal-mesh.tsx, glsl-hero.tsx, token-viz.tsx, color-resolve.ts itself, and canvas-cursor.tsx — no new callers
    - lib/color-resolve.ts has zero modifications (git diff shows no changes)
    - `pnpm tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>color-resolve.ts audit complete — confirmed safe, zero code changes, no caller references success/warning tokens, WebGL scenes render correctly post-migration</done>
</task>

</tasks>

<verification>
- `pnpm tsc --noEmit` exits 0
- `pnpm build` exits 0
- grep confirms --color-success and --color-warning are inside @theme, absent from :root
- grep confirms --sf-yellow and --sf-green remain in :root
- grep confirms no animation component references success/warning tokens
- Visual: SFAlert success variant shows green border, SFStatusDot active shows green dot
</verification>

<success_criteria>
Tailwind v4 generates bg-success, text-success, border-success, bg-warning, text-warning, border-warning utility classes. The four affected components (SFAlert, SFStatusDot, SFToast, ComponentsExplorer) render correct colors. WebGL bridge audit confirms zero impact. Build passes clean.
</success_criteria>

<output>
After completion, create `.planning/phases/22-token-finalization/22-01-SUMMARY.md`
</output>
