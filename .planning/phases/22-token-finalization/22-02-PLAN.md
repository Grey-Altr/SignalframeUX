---
phase: 22-token-finalization
plan: 02
type: execute
wave: 2
depends_on:
  - 22-01
files_modified:
  - app/globals.css
  - SCAFFOLDING.md
autonomous: true
requirements:
  - TK-02
  - TK-03

must_haves:
  truths:
    - "globals.css contains an explicit comment block documenting the absence of elevation tokens as a DU/TDR design decision"
    - "SCAFFOLDING.md documents sidebar and chart tokens as deferred groups with a recommendation to avoid until respective milestones"
    - "SCAFFOLDING.md documents the elevation policy with reference to the three sf-* press tokens"
  artifacts:
    - path: "app/globals.css"
      provides: "Elevation policy comment block"
      contains: "ELEVATION POLICY"
    - path: "SCAFFOLDING.md"
      provides: "Elevation Policy section"
      contains: "## Elevation Policy"
    - path: "SCAFFOLDING.md"
      provides: "Deferred Token Groups section"
      contains: "## Deferred Token Groups"
  key_links:
    - from: "app/globals.css elevation comment"
      to: "SCAFFOLDING.md Elevation Policy section"
      via: "Both document the same architectural decision — no shadow/elevation scale"
      pattern: "ELEVATION POLICY|Elevation Policy"
---

<objective>
Document the intentional absence of elevation tokens and the deferred status of sidebar/chart token groups. This is pure documentation work — no behavioral code changes.

Purpose: TK-02 ensures future contributors understand that the lack of box-shadow/elevation tokens is a deliberate DU/TDR aesthetic decision, not an omission. TK-03 ensures sidebar and chart tokens (present in @theme for shadcn compat) are documented as deferred with explicit "do not use" guidance.
Output: globals.css elevation comment block; SCAFFOLDING.md with Elevation Policy and Deferred Token Groups sections.
</objective>

<execution_context>
@/Users/greyaltaer/.claude/pde-os/engines/gsd/workflows/execute-plan.md
@/Users/greyaltaer/.claude/pde-os/engines/gsd/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/22-token-finalization/22-RESEARCH.md
@app/globals.css
@SCAFFOLDING.md

<interfaces>
<!-- Key locations in files the executor needs. -->

From app/globals.css:
- Lines 14-24: COLOR TIERS comment block (the elevation comment goes after this, before the @theme opening)
- Lines 26-95: @theme block (contains sidebar tokens at lines 47-55, chart tokens at lines 57-62)
- Lines 111-113: --sf-inset-shadow, --sf-deboss-light, --sf-deboss-shadow (the three press-effect tokens referenced in the elevation policy)

From SCAFFOLDING.md:
- Line 125-131: "## Known Pitfalls" section (the last content section — new sections append after this)
- Total 130 lines currently
</interfaces>
</context>

<acceptance_criteria>
**AC-1:** Given globals.css is opened, when searching for "ELEVATION POLICY", then a multi-line comment block is found documenting: (a) no box-shadow scale, (b) no z-elevation variables, (c) this is intentional DU/TDR, (d) the three sf-* press tokens are NOT an elevation scale.

**AC-2:** Given SCAFFOLDING.md is opened, when searching for "Elevation Policy", then a section exists with: (a) a statement that depth is expressed through spacing/hierarchy/layout/contrast/motion, (b) a list of the three --sf-* press tokens and their purpose.

**AC-3:** Given SCAFFOLDING.md is opened, when searching for "Deferred Token Groups", then a section exists documenting: (a) sidebar tokens present for shadcn compat, SFSidebar deferred to cdOS, (b) chart tokens present for shadcn compat, SFChart out of scope, (c) explicit "do not use" recommendation for both groups in v1.4.

**AC-4:** Given both files are edited, when `pnpm tsc --noEmit` runs, then zero TypeScript errors are reported.
</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Add elevation policy comment to globals.css and Elevation Policy section to SCAFFOLDING.md</name>
  <files>app/globals.css, SCAFFOLDING.md</files>
  <read_first>
    - app/globals.css (full file — find the exact insertion point between the COLOR TIERS comment and @theme opening)
    - SCAFFOLDING.md (full file — find the end of Known Pitfalls section for appending)
  </read_first>
  <action>
**1. Add elevation policy comment to globals.css**

Insert the following comment block AFTER the COLOR TIERS comment block (which ends at line 24 with the closing `───`) and BEFORE the `@theme {` opening (currently line 26). Place it at line 25 (between the two):

```css
/* ── ELEVATION POLICY ─────────────────────────────────────────
   INTENTIONALLY ABSENT: No box-shadow scale, no z-elevation variables.

   Depth is expressed through spacing, hierarchy, layout, contrast,
   and motion. This is a deliberate DU/TDR aesthetic decision.

   DO NOT add a shadow elevation scale to this system.

   Existing sf-* shadow tokens (--sf-inset-shadow, --sf-deboss-light,
   --sf-deboss-shadow) are micro-feedback press-effect utilities only —
   they are not an elevation scale.
   ─────────────────────────────────────────────────────────── */
```

**2. Add Elevation Policy section to SCAFFOLDING.md**

Append the following section AFTER the "## Known Pitfalls" section (after line 131):

```markdown
## Elevation Policy

The SignalframeUX token system has **no box-shadow elevation scale** and **no z-elevation variables**. This is a deliberate DU/TDR aesthetic decision — depth is expressed through spacing, hierarchy, layout, contrast, and motion only.

**DO NOT** add a shadow elevation scale to this system.

Three `--sf-*` shadow tokens exist in `:root` for micro-feedback press effects. They are NOT an elevation scale:

| Token | Purpose |
|-------|---------|
| `--sf-inset-shadow` | `inset 0 1px 2px oklch(0 0 0 / 0.08)` — subtle press-in feedback |
| `--sf-deboss-light` | `0 1px 0 oklch(1 0 0 / 0.1)` — light deboss highlight |
| `--sf-deboss-shadow` | `0 -1px 0 oklch(0 0 0 / 0.15)` — dark deboss shadow |
```

**3. Run `pnpm tsc --noEmit`** to confirm zero errors.
  </action>
  <ac_refs>AC-1, AC-2, AC-4</ac_refs>
  <verify>
    <automated>cd /Users/greyaltaer/code/projects/SignalframeUX && grep -n "ELEVATION POLICY" app/globals.css && grep -n "Elevation Policy" SCAFFOLDING.md && grep -n "sf-inset-shadow" SCAFFOLDING.md && pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep "ELEVATION POLICY" app/globals.css` returns at least one match
    - `grep "DO NOT add a shadow elevation scale" app/globals.css` returns a match
    - `grep "## Elevation Policy" SCAFFOLDING.md` returns a match
    - `grep "sf-inset-shadow" SCAFFOLDING.md` returns a match (press token documented)
    - `grep "sf-deboss-light" SCAFFOLDING.md` returns a match
    - `grep "sf-deboss-shadow" SCAFFOLDING.md` returns a match
    - `pnpm tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Elevation absence documented in both globals.css (comment block) and SCAFFOLDING.md (Elevation Policy section with three press-effect token descriptions)</done>
</task>

<task type="auto">
  <name>Task 2: Add Deferred Token Groups section to SCAFFOLDING.md for sidebar and chart tokens</name>
  <files>SCAFFOLDING.md</files>
  <read_first>
    - SCAFFOLDING.md (full file — must see the Elevation Policy section just added by Task 1, append after it)
    - app/globals.css (reference — sidebar tokens at lines 47-55, chart tokens at lines 57-62)
  </read_first>
  <action>
Append the following section to SCAFFOLDING.md AFTER the "## Elevation Policy" section added by Task 1:

```markdown
## Deferred Token Groups

The following token groups are declared in `@theme` for shadcn CLI compatibility but have **no SF consumer components** in v1.4. Do not build against these tokens until their respective milestones.

### Sidebar Tokens

Eight tokens: `--color-sidebar`, `--color-sidebar-foreground`, `--color-sidebar-primary`, `--color-sidebar-primary-foreground`, `--color-sidebar-accent`, `--color-sidebar-accent-foreground`, `--color-sidebar-border`, `--color-sidebar-ring`. Dark overrides exist in `.dark {}`.

**Status:** SFSidebar is deferred to the cdOS milestone. These tokens exist solely so `pnpm dlx shadcn add sidebar` resolves without errors. Do not reference sidebar tokens in v1.4 components.

### Chart Tokens

Five tokens: `--color-chart-1` through `--color-chart-5`. No dark overrides (by design — chart colors are consistent across themes).

**Status:** SFChart is out of scope for v1.4 (recharts ~50KB dependency, no current consumer). These tokens exist solely for shadcn CLI compatibility. Do not reference chart tokens in v1.4 components.

### Recommendation

Avoid consuming sidebar or chart tokens in any SF component until the consuming SF wrapper (SFSidebar, SFChart) is authored in its respective milestone. Using these tokens creates undocumented component contracts that may break when the consuming component is eventually built.
```

Run `pnpm tsc --noEmit` to confirm zero errors (CSS/markdown changes should not affect TypeScript).
  </action>
  <ac_refs>AC-3, AC-4</ac_refs>
  <verify>
    <automated>cd /Users/greyaltaer/code/projects/SignalframeUX && grep -n "Deferred Token Groups" SCAFFOLDING.md && grep -n "Sidebar Tokens" SCAFFOLDING.md && grep -n "Chart Tokens" SCAFFOLDING.md && grep -n "cdOS milestone" SCAFFOLDING.md && pnpm tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep "## Deferred Token Groups" SCAFFOLDING.md` returns a match
    - `grep "### Sidebar Tokens" SCAFFOLDING.md` returns a match
    - `grep "### Chart Tokens" SCAFFOLDING.md` returns a match
    - `grep "cdOS milestone" SCAFFOLDING.md` returns a match (SFSidebar deferred reference)
    - `grep "recharts" SCAFFOLDING.md` returns a match (SFChart out-of-scope note)
    - `grep "Do not reference sidebar tokens" SCAFFOLDING.md` returns a match
    - `grep "Do not reference chart tokens" SCAFFOLDING.md` returns a match
    - `pnpm tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Sidebar and chart token groups documented in SCAFFOLDING.md with deferred status, consumer absence, and explicit "do not use in v1.4" recommendation</done>
</task>

</tasks>

<verification>
- `grep "ELEVATION POLICY" app/globals.css` returns a match
- `grep "## Elevation Policy" SCAFFOLDING.md` returns a match
- `grep "## Deferred Token Groups" SCAFFOLDING.md` returns a match
- `grep "### Sidebar Tokens" SCAFFOLDING.md` returns a match
- `grep "### Chart Tokens" SCAFFOLDING.md` returns a match
- `pnpm tsc --noEmit` exits 0
- `pnpm build` exits 0
</verification>

<success_criteria>
globals.css contains the ELEVATION POLICY comment block. SCAFFOLDING.md contains three new sections: Elevation Policy (with three press-token descriptions), Deferred Token Groups (with Sidebar Tokens and Chart Tokens subsections), and a Recommendation paragraph. Build passes clean.
</success_criteria>

<output>
After completion, create `.planning/phases/22-token-finalization/22-02-SUMMARY.md`
</output>
