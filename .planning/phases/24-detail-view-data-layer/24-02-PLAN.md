---
phase: 24-detail-view-data-layer
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/api-docs.ts
autonomous: true
requirements:
  - DV-02

must_haves:
  truths:
    - "api-docs.ts has a ComponentDoc entry for every component in the 49-item registry"
    - "Every new entry has at least one PropDef with name, type, default, and desc fields"
    - "All new entries follow UPPERCASE convention for description and desc strings"
    - "All existing 27 entries remain intact and unmodified"
    - "New entries use real import paths (@/components/sf) not fictional @sfux/* paths"
  artifacts:
    - path: "lib/api-docs.ts"
      provides: "Complete component documentation for all ~49 registry items"
      contains: "sfButton"
  key_links:
    - from: "lib/component-registry.ts"
      to: "lib/api-docs.ts"
      via: "docId string key lookup"
      pattern: "API_DOCS\\["
---

<objective>
Extend lib/api-docs.ts with ~36 new ComponentDoc entries to achieve full coverage of all registry items. This ensures every component referenced by component-registry.ts docId has a matching entry with props, usage examples, and accessibility notes.

Purpose: Phase 25 ComponentDetail panel props table and code tab consume API_DOCS entries directly. Missing entries = empty props tables in the detail view.
Output: api-docs.ts extended from 27 to ~63 entries covering all SF components, layout primitives, and animation components.
</objective>

<execution_context>
@.planning/phases/24-detail-view-data-layer/24-CONTEXT.md
@.planning/phases/24-detail-view-data-layer/24-RESEARCH.md
</execution_context>

<context>
@.planning/project-context.md
@.planning/ROADMAP.md
@.planning/STATE.md

@lib/api-docs.ts
@components/sf/index.ts
@public/r/registry.json
@components/blocks/components-explorer.tsx

<interfaces>
<!-- Existing ComponentDoc interface from api-docs.ts — new entries MUST match this exactly -->

```typescript
export interface PropDef {
  name: string;
  type: string;
  default: string;
  desc: string;
  required?: boolean;
}

export interface UsageExample {
  label: string;
  code: string;
}

export interface ComponentDoc {
  id: string;
  name: string;
  layer: "FRAME" | "SIGNAL" | "CORE" | "TOKEN" | "HOOK";
  version: string;
  status: "STABLE" | "BETA" | "EXPERIMENTAL";
  description: string;
  importPath: string;
  importName: string;
  props: PropDef[];
  usage: UsageExample[];
  a11y: string[];
  preview?: PreviewHud;  // OPTIONAL — do NOT add preview for new entries
}
```

Existing API_DOCS keys (27, DO NOT modify these):
createSignalframeUX, useSignalframe, SFUXProvider, defineTheme,
input, card, modal, table, tabs, toast, dropdown, drawer, badge,
noisebg, particlemesh, glitchtext, waveform, reactivecanvas,
colors, spacing, typography, motion, elevation,
useSignalEffect, useToken, useMotion, useBreakpoint

PREVIEW_DATA block (lines 788-897) — DO NOT modify. New entries go into API_DOCS only (before line 785 closing `};`).
</interfaces>
</context>

<acceptance_criteria>
**AC-1:** Given lib/api-docs.ts is extended, when all API_DOCS keys are counted, then the total is at least 49 entries (27 existing + at least 22 new for grid items).

**AC-2:** Given each new ComponentDoc entry, when its `props` array is inspected, then it contains at least 1 PropDef with non-empty `name`, `type`, `default`, and `desc` fields.

**AC-3:** Given each new ComponentDoc entry, when its `description` and PropDef `desc` fields are inspected, then all strings are UPPERCASE (matching the existing DU/TDR convention).

**AC-4:** Given the existing 27 entries, when the file is diffed before and after, then lines 44-785 are unchanged — all existing entries preserved exactly.

**AC-5:** Given new entries for SF components, when their `importPath` is inspected, then they use `@/components/sf` (barrel) for Pattern A, direct paths for Pattern B (e.g., `@/components/sf/sf-drawer-lazy`), and `@/components/animation/*` for Pattern C.

**AC-6:** Given each new entry has a `usage` array, when inspected, then it contains at least 1 UsageExample with non-empty `label` and `code` fields.

**AC-7:** Given each new entry has an `a11y` array, when inspected, then it contains at least 2 UPPERCASE strings describing accessibility behavior.
</acceptance_criteria>

<tasks>

<task type="auto" risk="high">
  <name>Task 1: Extend api-docs.ts with all missing ComponentDoc entries</name>
  <files>lib/api-docs.ts</files>
  <read_first>
    - lib/api-docs.ts (FULL file — understand existing structure, key naming, description style, where to insert new entries before line 785)
    - components/sf/index.ts (all exported component names and their sub-exports)
    - components/sf/sf-button.tsx (reference for prop types and CVA variants — example of intent/size pattern)
    - components/sf/sf-accordion.tsx (reference for SIGNAL layer component with GSAP)
    - components/sf/sf-input-group.tsx (newest component — reference for latest conventions)
    - components/sf/sf-avatar.tsx (reference for component with fallback pattern)
    - components/sf/sf-alert.tsx (reference for intent variant pattern)
    - components/sf/sf-progress.tsx (reference for GSAP-animated SIGNAL component)
    - components/sf/sf-status-dot.tsx (reference for GSAP pulse)
    - components/sf/sf-stepper.tsx (reference for composite component)
    - components/sf/sf-navigation-menu.tsx (reference for complex multi-part component)
    - components/sf/sf-toggle-group.tsx (reference for toggle group)
    - components/sf/sf-hover-card.tsx (reference for newest FRAME component)
    - components/sf/sf-input-otp.tsx (reference for OTP input)
    - components/sf/sf-container.tsx (reference for layout primitive)
    - components/sf/sf-section.tsx (reference for layout primitive with bgShift)
    - components/sf/sf-text.tsx (reference for polymorphic layout primitive)
    - components/sf/sf-grid.tsx (reference for layout primitive)
    - components/sf/sf-stack.tsx (reference for layout primitive)
    - public/r/registry.json (meta.layer and meta.pattern for each component)
    - .planning/phases/24-detail-view-data-layer/24-RESEARCH.md (Pattern 2: api-docs.ts New Entry Shape section, Gap Analysis section)
  </read_first>
  <ac_refs>AC-1, AC-2, AC-3, AC-4, AC-5, AC-6, AC-7</ac_refs>
  <boundaries>
    - DO NOT modify lines 1-785 of api-docs.ts (existing API_DOCS entries)
    - DO NOT modify lines 787-902 (PREVIEW_DATA block and merge loop)
    - DO NOT add preview/PreviewHud data for new entries
  </boundaries>
  <action>
Insert new ComponentDoc entries into the `API_DOCS` object BEFORE the closing `};` at line 785. Add a section comment before the new entries:

```typescript
  /* ═══════════════════════════════════════
     SF COMPONENTS — PHASE 24 EXTENSION
     ═══════════════════════════════════════ */
```

Create entries for these components, grouped by category. For each entry, read the actual SF component source file to extract:
- Real prop names and types from the component's interface/props
- CVA variant values (intent options, size options)
- Default values from defaultVariants or parameter defaults
- Accessibility attributes from the JSX (aria-*, role, etc.)

**New entry keys (camelCase SF component name):**

FORMS (8 entries):
- `sfButton` — intent: primary/ghost/signal, size: sm/md/lg/xl, asChild
- `sfInput` — DO NOT create if key "input" already exists. Instead create `sfInput` as a NEW key with real import paths and accurate props from sf-input.tsx (the old "input" key uses fictional paths)
- `sfToggle` — pressed prop, intent variants
- `sfSlider` — min, max, step, value, onValueChange
- `sfToggleGroup` — type: single/multiple, intent, size
- `sfCalendar` — mode, selected, onSelect, disabled (Pattern B, importPath: @/components/sf/sf-calendar-lazy)
- `sfInputOTP` — maxLength, pattern, render
- `sfInputGroup` — composite: SFInputGroupAddon, SFInputGroupButton, SFInputGroupInput, SFInputGroupText, SFInputGroupTextarea

LAYOUT (4 entries):
- `sfCard` — DO NOT create if "card" already exists. Create `sfCard` as new key with real props
- `sfDialog` — open, onOpenChange, modal (maps to grid item "006" MODAL)
- `sfDrawer` — open, onOpenChange, direction (Pattern B, importPath: @/components/sf/sf-drawer-lazy)
- `sfHoverCard` — openDelay, closeDelay

NAVIGATION (6 entries):
- `sfTabs` — DO NOT create if "tabs" exists. Create `sfTabs` as new key
- `sfPagination` — Server Component, no interactive props
- `sfAvatar` — src, alt, fallback
- `sfBreadcrumb` — Server Component, composition-based
- `sfNavigationMenu` — orientation, delayDuration
- `sfMenubar` — Pattern B, importPath: @/components/sf/sf-menubar-lazy

FEEDBACK (10 entries):
- `sfBadge` — DO NOT create if "badge" exists. Create `sfBadge` as new key
- `sfToastFrame` — title, description, intent (Sonner-based, FRAME layer)
- `sfAlert` — intent: info/warning/destructive/success
- `sfAlertDialog` — open, onOpenChange, loading state on action button
- `sfCollapsible` — open, onOpenChange, disabled
- `sfEmptyState` — title, description, icon, action slot
- `sfAccordion` — type: single/multiple, collapsible, GSAP stagger (SIGNAL layer)
- `sfProgress` — value (0-100), GSAP fill tween (SIGNAL layer)
- `sfToastSignal` — same as sfToastFrame but SIGNAL layer variant
- `sfStepper` — steps array, activeStep, orientation (SIGNAL layer, uses SFProgress as connector)

DATA_DISPLAY (2 entries):
- `sfTable` — DO NOT create if "table" exists. Create `sfTable` as new key
- `sfStatusDot` — status: active/idle/offline, GSAP pulse on active (SIGNAL layer)

GENERATIVE (4 entries — these already exist as noisebg, particlemesh, glitchtext, waveform but create NEW keys with sf-prefixed camelCase):
- `noiseBg` — opacity, animate, speed
- `waveform` — frequency, amplitude, color
- `glitchText` — text, intensity, interval
- `particleMesh` — particleCount, mouseRadius, mouseForce

LAYOUT PRIMITIVES (5 entries):
- `sfContainer` — maxWidth: content/wide/full, as
- `sfSection` — bgShift: white/black, as, data-section
- `sfGrid` — columns, gap, as
- `sfStack` — direction, gap, align, as
- `sfText` — variant: heading-1/heading-2/heading-3/body/small/code, as

NON-GRID UTILITY COMPONENTS (entries for registry items not in the explorer grid but needed for DV-02 full coverage):
- `sfSeparator` — orientation, decorative
- `sfTooltip` — delayDuration, side
- `sfSheet` — open, onOpenChange, side
- `sfDropdownMenu` — open, onOpenChange
- `sfCommand` — filter, loop
- `sfSkeleton` — className (animation shimmer)
- `sfPopover` — open, onOpenChange, align
- `sfScrollArea` — orientation, type
- `sfLabel` — htmlFor
- `sfSelect` — value, onValueChange, placeholder
- `sfCheckbox` — checked, onCheckedChange, disabled
- `sfRadioGroup` — value, onValueChange
- `sfSwitch` — checked, onCheckedChange
- `sfTextarea` — placeholder, rows

**Conventions for ALL new entries:**
- `description`: UPPERCASE string, 1-2 sentences describing what the component is and its key characteristics
- `importPath`: `"@/components/sf"` for Pattern A barrel components, direct path for Pattern B
- `importName`: Exact export name from sf/index.ts (e.g., "SFButton", "SFAccordion")
- `version`: Match the version from ComponentsExplorer or registry.json. Use "v1.4.0" for Phase 23 components (InputGroup, HoverCard, InputOTP, Drawer), "v1.3.0" for Phase 17-19 components, "v2.1.0" for Phase 1-5 components, "v1.0.0" for generative components
- `status`: "STABLE" for all
- `layer`: "FRAME" or "SIGNAL" — match subcategory from ComponentsExplorer
- `props`: Read from actual component source. At minimum 1 PropDef. Use UPPERCASE for `desc`. For composite components (Card, Dialog, Tabs, etc.), document the main container's props.
- `usage`: At minimum 1 UsageExample with UPPERCASE label. Show real import + JSX.
- `a11y`: At minimum 2 UPPERCASE strings. Reference actual ARIA attributes, keyboard behavior, or screen reader semantics from the component source.
- Do NOT add `preview` field to new entries

After adding all entries, verify:
```bash
pnpm tsc --noEmit
```
  </action>
  <verify>
    <automated>cd /Users/greyaltaer/code/projects/SignalframeUX && pnpm tsc --noEmit && node -e "const fs=require('fs');const c=fs.readFileSync('lib/api-docs.ts','utf8');const m=c.match(/^\s{2}\w+:\s*\{$/gm);console.log('total API_DOCS entries:',m?m.length:0)" && grep -c "layer:" lib/api-docs.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm tsc --noEmit` passes with zero errors
    - Total API_DOCS entry count is at least 49 (27 existing + at least 22 new)
    - All new entries have UPPERCASE `description` strings
    - All new entries have at least 1 PropDef in `props` array
    - All new entries have at least 1 UsageExample in `usage` array
    - All new entries have at least 2 strings in `a11y` array
    - Lines 787-902 (PREVIEW_DATA block) remain unmodified
    - `grep -c "sfButton" lib/api-docs.ts` returns at least 1 (new entry exists)
    - `grep -c "sfAccordion" lib/api-docs.ts` returns at least 1 (new entry exists)
    - `grep -c "sfContainer" lib/api-docs.ts` returns at least 1 (layout primitive entry exists)
  </acceptance_criteria>
  <done>api-docs.ts has ComponentDoc entries for all ~49+ registry items. All existing entries preserved. All new entries follow UPPERCASE convention with real import paths, valid props, usage examples, and a11y notes. TypeScript compiles cleanly.</done>
</task>

</tasks>

<verification>
After task completes:
1. `pnpm tsc --noEmit` — zero errors
2. `pnpm build` — full build passes
3. Count total API_DOCS entries — at least 49
4. Spot-check 5 random new entries for: UPPERCASE desc, real importPath, at least 1 prop, at least 1 usage, at least 2 a11y strings
5. Verify existing entries unchanged (lines 44-785 diff = 0 changes)
</verification>

<success_criteria>
- api-docs.ts extended with full coverage for all registry items
- All new entries follow existing UPPERCASE convention
- All new entries have accurate props derived from actual component source files
- Zero TypeScript errors
- DV-02 requirement satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/24-detail-view-data-layer/24-02-SUMMARY.md`
</output>
