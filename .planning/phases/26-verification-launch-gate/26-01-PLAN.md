---
phase: 26-verification-launch-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [VF-01]

must_haves:
  truths:
    - "Shared JS bundle (rootMainFiles) is under 150 KB gzip"
    - "ComponentDetail is in an async chunk, not in the shared bundle"
    - "shiki does not appear in any client chunk"
    - "SFCalendar and SFMenubar are in separate async chunks, not in the shared bundle"
  artifacts:
    - path: ".next/build-manifest.json"
      provides: "rootMainFiles array with 4 chunks"
      contains: "rootMainFiles"
  key_links:
    - from: "next.config.ts"
      to: "@next/bundle-analyzer"
      via: "ANALYZE=true env var"
      pattern: "ANALYZE.*true"
---

<objective>
Verify the shared JS bundle gate (VF-01) by running a fresh production build with bundle analysis.

Purpose: Confirm that all v1.4 additions (ComponentDetail, P3 components, shiki) remain isolated from the shared bundle and the rootMainFiles total is under the 150 KB gate.
Output: Build confirmation with measured gzip sizes. No files modified -- this is a verification-only plan.
</objective>

<execution_context>
@/Users/greyaltaer/.claude/pde-os/engines/gsd/workflows/execute-plan.md
@/Users/greyaltaer/.claude/pde-os/engines/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-verification-launch-gate/26-RESEARCH.md
</context>

<acceptance_criteria>
**AC-1:** Given a fresh `pnpm build` completes successfully, when the rootMainFiles from `.next/build-manifest.json` are measured with gzip compression, then the total is under 150 KB.

**AC-2:** Given `ANALYZE=true pnpm build` produces client treemap output, when the treemap is inspected, then `component-detail` appears only in a numbered async chunk (not in shared chunks), `shiki` does not appear in any client chunk, and `sf-calendar`/`sf-menubar` appear only in async chunks.

**AC-3:** Given the Playwright test suite runs against the local dev server, when all spec files execute, then all tests pass with zero failures (regression gate).
</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Run production build and measure shared bundle gate</name>
  <files></files>
  <ac_refs>AC-1, AC-2</ac_refs>
  <read_first>
    next.config.ts
    .next/build-manifest.json
  </read_first>
  <action>
1. Run `ANALYZE=true pnpm build` from the project root. This triggers @next/bundle-analyzer and produces treemap HTML files plus a fresh `.next/` output directory.

2. After build completes, measure the shared bundle gate programmatically:
```bash
python3 -c "
import json, gzip, os
with open('.next/build-manifest.json') as f:
    d = json.load(f)
total = 0
for path in d['rootMainFiles']:
    filepath = f'.next/{path}'
    with open(filepath, 'rb') as f:
        data = f.read()
    gz = gzip.compress(data, compresslevel=9)
    total += len(gz)
    print(f'{os.path.basename(path)}: {len(gz)/1024:.1f} KB gzip')
print(f'Total: {total/1024:.1f} KB (gate: 150 KB, margin: {150 - total/1024:.1f} KB)')
"
```

3. The total MUST be under 150 KB. Expected value based on Phase 27 build: ~100 KB gzip with ~50 KB margin.

4. Verify lazy-load isolation by checking the build output:
   - Search the `.next/` client chunks for `component-detail` -- it must appear only in an async numbered chunk, NOT in any rootMainFiles chunk
   - Search for `shiki` in client chunks -- it must be ABSENT entirely (server-only)
   - Search for `sf-calendar` and `sf-menubar` -- must be in separate async chunks only

5. If the gate passes, record the exact measurements. If the gate fails (>150 KB), diagnose which module leaked into the shared bundle and report the issue.
  </action>
  <verify>
    <automated>python3 -c "import json,gzip,os; f=open('.next/build-manifest.json'); d=json.load(f); t=sum(len(gzip.compress(open(f'.next/{p}','rb').read(),9)) for p in d['rootMainFiles']); print(f'{t/1024:.1f} KB'); assert t/1024 < 150, f'GATE FAILED: {t/1024:.1f} KB > 150 KB'"</automated>
  </verify>
  <done>rootMainFiles total is under 150 KB gzip. ComponentDetail, shiki, SFCalendar, and SFMenubar confirmed absent from shared bundle chunks.</done>
</task>

<task type="auto">
  <name>Task 2: Run Playwright regression suite</name>
  <files></files>
  <ac_refs>AC-3</ac_refs>
  <read_first>
    playwright.config.ts
    tests/phase-25-detail-view.spec.ts
    tests/phase-27-integration-bugs.spec.ts
  </read_first>
  <action>
1. Start the dev server if not already running: `pnpm dev` (background process on localhost:3000).

2. Run the full Playwright test suite:
```bash
pnpm exec playwright test
```

3. Both spec files must pass:
   - `tests/phase-25-detail-view.spec.ts` (10 tests)
   - `tests/phase-27-integration-bugs.spec.ts` (8 tests)

4. All tests must pass with zero failures. If any test fails, investigate whether it is a regression from Phase 27 fixes or a pre-existing issue, and report findings.

5. Record test results (pass count, fail count, duration).
  </action>
  <verify>
    <automated>pnpm exec playwright test 2>&1 | tail -5</automated>
  </verify>
  <done>All Playwright tests pass with zero failures. No regressions detected from Phase 27 integration bug fixes.</done>
</task>

</tasks>

<verification>
- `pnpm build` completes with zero errors
- rootMainFiles gzip total < 150 KB
- ComponentDetail, shiki, SFCalendar, SFMenubar confirmed isolated from shared bundle
- All Playwright tests pass
</verification>

<success_criteria>
VF-01 is satisfied: the shared JS bundle (rootMainFiles) is confirmed under the 150 KB gate with all v1.4 lazy-load and server-only isolation strategies verified working.
</success_criteria>

<output>
After completion, create `.planning/phases/26-verification-launch-gate/26-01-SUMMARY.md`
</output>
