---
phase: 28-route-infrastructure
plan: 02
type: execute
wave: 2
depends_on: [28-01]
files_modified:
  - components/layout/nav.tsx
  - components/layout/footer.tsx
  - components/layout/command-palette.tsx
  - components/blocks/hero.tsx
  - components/blocks/manifesto-band.tsx
  - app/init/page.tsx
  - app/sitemap.ts
  - lib/component-registry.ts
  - tests/phase-25-detail-view.spec.ts
autonomous: true
requirements: [RA-04]
must_haves:
  truths:
    - "Zero grep hits for old route strings (/components, /tokens, /start as hrefs) outside next.config.ts"
    - "Nav links point to /inventory, /system, /init"
    - "Footer links point to /inventory, /system, /init"
    - "Command palette routes point to /inventory, /system, /init"
    - "Sitemap URLs use /inventory, /system, /init"
    - "Phase 25 detail view tests run against /inventory"
  artifacts:
    - path: "components/layout/nav.tsx"
      provides: "Updated NAV_LINKS array with new routes"
      contains: "/inventory"
    - path: "components/layout/footer.tsx"
      provides: "Updated footer hrefs"
      contains: "/inventory"
    - path: "components/layout/command-palette.tsx"
      provides: "Updated NAV_ITEMS with new routes"
      contains: "/inventory"
    - path: "app/sitemap.ts"
      provides: "Updated sitemap URLs"
      contains: "/inventory"
    - path: "lib/component-registry.ts"
      provides: "Updated code sample href"
      contains: 'href="/inventory"'
    - path: "tests/phase-25-detail-view.spec.ts"
      provides: "Test URLs updated to /inventory"
  key_links:
    - from: "components/layout/nav.tsx"
      to: "/inventory, /system, /init"
      via: "NAV_LINKS array href values"
      pattern: 'href.*"/inventory"'
    - from: "app/sitemap.ts"
      to: "/inventory, /system, /init"
      via: "sitemap URL strings"
      pattern: "inventory.*system.*init"
---

<objective>
Update every internal link, nav item, footer link, command palette route, sitemap entry, and test URL to reference the new route names.

Purpose: Complete the route rename by ensuring zero references to old paths remain anywhere in the codebase (excluding the redirect config itself). This is the link surgery that makes the rename fully consistent.
Output: All 9 files updated, grep-to-zero verification passes, all tests reference correct URLs.
</objective>

<execution_context>
@/Users/greyaltaer/.claude/pde-os/engines/gsd/workflows/execute-plan.md
@/Users/greyaltaer/.claude/pde-os/engines/gsd/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-route-infrastructure/28-CONTEXT.md
@.planning/phases/28-route-infrastructure/28-RESEARCH.md
@.planning/phases/28-route-infrastructure/28-01-SUMMARY.md
@components/layout/nav.tsx
@components/layout/footer.tsx
@components/layout/command-palette.tsx
@components/blocks/hero.tsx
@components/blocks/manifesto-band.tsx
@app/init/page.tsx
@app/sitemap.ts
@lib/component-registry.ts
@tests/phase-25-detail-view.spec.ts
</context>

<acceptance_criteria>
AC-1: In `components/layout/nav.tsx`, the NAV_LINKS array contains `/inventory` (not `/components`), `/system` (not `/tokens`), and `/init` (not `/start`).
AC-2: In `components/layout/footer.tsx`, all Link hrefs reference `/inventory`, `/system`, `/init` — zero occurrences of `"/components"`, `"/tokens"`, `"/start"` as href values.
AC-3: In `components/layout/command-palette.tsx`, NAV_ITEMS routes use `/inventory`, `/system`, `/init`.
AC-4: In `app/sitemap.ts`, URL strings use `/inventory`, `/system`, `/init`.
AC-5: In `components/blocks/hero.tsx`, the CTA href is `"/init"` (not `"/start"`).
AC-6: In `components/blocks/manifesto-band.tsx`, all route hrefs updated to `/init`, `/inventory`, `/system`.
AC-7: In `app/init/page.tsx`, internal links point to `/inventory` and `/system` (not `/components` and `/tokens`).
AC-8: In `lib/component-registry.ts`, the JSX code sample string at ~line 396 uses `href="/inventory"` (not `href="/components"`).
AC-9: In `tests/phase-25-detail-view.spec.ts`, all `page.goto()` calls use `"/inventory"` (not `"/components"`). Comment text also updated.
AC-10: Running `grep -rn '"/components"\|"/tokens"\|"/start"' --include="*.ts" --include="*.tsx" . | grep -v "next.config.ts" | grep -v "node_modules"` returns zero results (excluding redirect config).
</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Update all navigation and layout link references</name>
  <files>components/layout/nav.tsx, components/layout/footer.tsx, components/layout/command-palette.tsx, app/sitemap.ts</files>
  <read_first>components/layout/nav.tsx, components/layout/footer.tsx, components/layout/command-palette.tsx, app/sitemap.ts</read_first>
  <ac_refs>AC-1, AC-2, AC-3, AC-4</ac_refs>
  <boundaries>Do NOT modify component styling, structure, or logic. Only change route string values. Do NOT touch `components/layout/nav-overlay.tsx` — it receives links as props, no hardcoded routes.</boundaries>
  <action>
In each file, find and replace ONLY route href strings (not import paths):

**`components/layout/nav.tsx`** — In the `NAV_LINKS` array:
- `"/components"` -> `"/inventory"`
- `"/tokens"` -> `"/system"`
- `"/start"` -> `"/init"`
- Also update any display labels if they reference old names (e.g., "Components" -> "Inventory", "Tokens" -> "System", "Start" -> "Init")

**`components/layout/footer.tsx`** — In Link href attributes:
- `"/start"` -> `"/init"`
- `"/components"` -> `"/inventory"` (appears twice per research)
- `"/tokens"` -> `"/system"`

**`components/layout/command-palette.tsx`** — In `NAV_ITEMS` array:
- `"/components"` -> `"/inventory"`
- `"/tokens"` -> `"/system"`
- `"/start"` -> `"/init"`

**`app/sitemap.ts`** — In URL strings:
- `${BASE}/components` -> `${BASE}/inventory`
- `${BASE}/tokens` -> `${BASE}/system`
- `${BASE}/start` -> `${BASE}/init`

CRITICAL: Only change quoted route strings like `"/components"`, `href="/components"`, `href: "/components"`. NEVER change `@/components/` import paths or `components/` directory references.
  </action>
  <verify>
    <automated>grep -n '"/components"\|"/tokens"\|"/start"' components/layout/nav.tsx components/layout/footer.tsx components/layout/command-palette.tsx app/sitemap.ts | grep -v "node_modules" | wc -l | xargs test 0 -eq && echo "PASS"</automated>
  </verify>
  <done>All 4 navigation/layout files reference only new route names. Zero old route strings remain in these files.</done>
</task>

<task type="auto">
  <name>Task 2: Update block components, init page, registry, and tests</name>
  <files>components/blocks/hero.tsx, components/blocks/manifesto-band.tsx, app/init/page.tsx, lib/component-registry.ts, tests/phase-25-detail-view.spec.ts</files>
  <read_first>components/blocks/hero.tsx, components/blocks/manifesto-band.tsx, app/init/page.tsx, lib/component-registry.ts, tests/phase-25-detail-view.spec.ts</read_first>
  <ac_refs>AC-5, AC-6, AC-7, AC-8, AC-9, AC-10</ac_refs>
  <boundaries>Do NOT modify `tests/phase-27-integration-bugs.spec.ts` — its `@/components/animation/waveform` reference is a source import path, NOT a route. Do NOT change any `@/components/` import paths in any file.</boundaries>
  <action>
**`components/blocks/hero.tsx`** — Line ~99:
- `href="/start"` -> `href="/init"`

**`components/blocks/manifesto-band.tsx`** — In the links/items data:
- `href: "/start"` (x2) -> `href: "/init"` (x2)
- `href: "/components"` -> `href: "/inventory"`
- `href: "/tokens"` -> `href: "/system"`

**`app/init/page.tsx`** (was `app/start/page.tsx`, moved in Plan 01) — Lines ~136, ~138, ~366:
- `href: "/components"` -> `href: "/inventory"`
- `href: "/tokens"` -> `href: "/system"`
- `href="/components"` -> `href="/inventory"`

**`lib/component-registry.ts`** — Line ~396, inside a JSX code sample string:
- `href="/components"` -> `href="/inventory"`
- This is the ONLY change in this file. Do NOT touch any `@/components/sf` or `@/components/animation` import paths.

**`tests/phase-25-detail-view.spec.ts`** — In test code:
- `page.goto("/components")` (x4) -> `page.goto("/inventory")` (x4)
- Comment text referencing `/components` route -> `/inventory`
- Do NOT change any `@/components/` import path assertions — those are source paths, not routes.

After all changes, run the grep-to-zero verification:
```bash
grep -rn '"/components"\|"/tokens"\|"/start"' --include="*.ts" --include="*.tsx" . | grep -v "next.config.ts" | grep -v "node_modules"
```
This MUST return zero lines.
  </action>
  <verify>
    <automated>grep -rn '"/components"\|"/tokens"\|"/start"' --include="*.ts" --include="*.tsx" . | grep -v "next.config.ts" | grep -v "node_modules" | wc -l | xargs test 0 -eq && echo "PASS"</automated>
  </verify>
  <done>All 5 files updated. Grep-to-zero verification passes — no old route strings exist anywhere in the codebase outside next.config.ts redirects.</done>
</task>

</tasks>

<verification>
- `grep -rn '"/components"\|"/tokens"\|"/start"' --include="*.ts" --include="*.tsx" . | grep -v "next.config.ts" | grep -v "node_modules"` returns zero results
- `grep -c '"/inventory"' components/layout/nav.tsx` returns >= 1
- `grep -c '"/system"' components/layout/nav.tsx` returns >= 1
- `grep -c '"/init"' components/layout/nav.tsx` returns >= 1
- `grep -c '"/inventory"' app/sitemap.ts` returns >= 1
- `grep -c 'goto("/inventory")' tests/phase-25-detail-view.spec.ts` returns >= 1
- `pnpm exec tsc --noEmit` passes
- `pnpm exec playwright test tests/phase-28-route-infra.spec.ts tests/phase-25-detail-view.spec.ts` passes
</verification>

<success_criteria>
- All 9 files updated with new route names
- Grep-to-zero verification passes (zero old route strings outside redirect config)
- TypeScript compilation clean
- Phase 28 smoke tests pass (routes + redirects)
- Phase 25 detail view tests pass against /inventory
</success_criteria>

<output>
After completion, create `.planning/phases/28-route-infrastructure/28-02-SUMMARY.md`
</output>
