---
phase: 31-thesis-section
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/thesis-manifesto.ts
  - tests/phase-31-thesis.spec.ts
autonomous: false
requirements:
  - TH-05
must_haves:
  truths:
    - "lib/thesis-manifesto.ts exports a typed array named THESIS_MANIFESTO containing exactly 6 manifesto statements transcribed verbatim from wiki/analyses/v1.5-thesis-copy-draft.md (Grey-approved 2026-04-07)"
    - "All 6 entries have size='anchor'; pillars present are a superset of {signal-frame, enhanced-flat, biophilia} (the three TH-05 required pillars) with optional extensions culture-infrastructure and memetic-engineering"
    - "Every statement text is between 2 and 8 words and avoids hedge words (might, could, perhaps, probably, maybe, tends, somewhat)"
    - "Manifesto copy is the locked wiki source — user review gate is pre-cleared by the 2026-04-07 wiki lock and the 2026-04-08 'drop magenta' decision"
    - "tests/phase-31-thesis.spec.ts exists with TH-05 content-coverage tests that grep-assert the three required pillar tokens and the exact 6-statement count"
  artifacts:
    - path: "lib/thesis-manifesto.ts"
      provides: "Typed manifesto content + type definitions consumed by components/blocks/thesis-section.tsx in Plan 02"
      exports: ["THESIS_MANIFESTO", "ManifestoStatementData", "ManifestoAnchor", "ManifestoSize"]
    - path: "tests/phase-31-thesis.spec.ts"
      provides: "Playwright test file scaffold covering TH-05 content assertions (Plan 02 extends this same file)"
      contains: "TH-05"
  key_links:
    - from: "lib/thesis-manifesto.ts"
      to: "components/blocks/thesis-section.tsx"
      via: "typed array import (Plan 02 consumes)"
      pattern: "THESIS_MANIFESTO"
    - from: "tests/phase-31-thesis.spec.ts"
      to: "lib/thesis-manifesto.ts"
      via: "grep assertion (fs.readFileSync + toContain pattern matching Phase 29 test style)"
      pattern: "thesis-manifesto"
---

<objective>
Resolve the Phase 31 content blocker (B-01, manifesto copy) by transcribing the 6-statement Grey-approved wiki copy into a typed TypeScript constant at `lib/thesis-manifesto.ts`, and scaffold the Phase 31 Playwright test file with the TH-05 content-coverage tests.

**B-01 is pre-cleared.** The locked wiki copy at `wiki/analyses/v1.5-thesis-copy-draft.md` (Grey-approved 2026-04-07) supersedes the researcher's draft in `31-RESEARCH.md` lines 400-473. User decided 2026-04-08 to use the wiki copy with foreground-only color (no magenta on "SIGNAL" in S6 — enforces D-16). The researcher draft is preserved at HEAD~1 for history.

Purpose: The manifesto is the content dependency for every engineering task in Plan 02. Separating it into its own plan enforces a content-integrity boundary before any React / GSAP code is written. The transcription must match the wiki source verbatim — any deviation would re-open B-01.

Output:
- `lib/thesis-manifesto.ts` (new) — typed array constant containing the 6 wiki statements verbatim, all `size: "anchor"`, pillar-annotated, wiki-source-traced
- `tests/phase-31-thesis.spec.ts` (new, scaffold) — Playwright file containing TH-05 content-coverage tests; Plan 02 extends with TH-01 through TH-04 and TH-06 browser tests
</objective>

<execution_context>
@/Users/greyaltaer/.claude/pde-os/engines/gsd/workflows/execute-plan.md
@/Users/greyaltaer/.claude/pde-os/engines/gsd/templates/summary.md
</execution_context>

<context>
@.planning/project-context.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/31-thesis-section/31-CONTEXT.md
@.planning/phases/31-thesis-section/31-RESEARCH.md
@.planning/phases/31-thesis-section/31-VALIDATION.md
@tests/phase-29-infra.spec.ts
@CLAUDE.md

<interfaces>
<!-- Contract this plan CREATES (Plan 02 will consume these exports verbatim) -->

```typescript
// lib/thesis-manifesto.ts — CREATED by this plan

// Size is kept as a discriminator for future extension, but all 6 statements
// in the locked wiki copy are "anchor". No "connector" tier exists.
export type ManifestoSize = "anchor" | "connector";

export type ManifestoPillar =
  | "signal-frame"          // TH-05 required (DETERMINISTIC STRUCTURE. / THE FRAME HOLDS.)
  | "enhanced-flat"         // TH-05 required (ZERO RADIUS.)
  | "culture-infrastructure" // wiki-copy extension (CULTURE AS INFRASTRUCTURE.)
  | "biophilia"             // TH-05 required (STRUCTURE THAT BREATHES.)
  | "memetic-engineering";  // wiki-copy extension (FORMAT IS STRATEGY.)

export interface ManifestoAnchor {
  top?: string;
  left?: string;
  right?: string;
  bottom?: string;
}

export interface ManifestoStatementData {
  text: string;
  size: ManifestoSize;
  anchor: ManifestoAnchor;            // desktop anchor
  mobileAnchor?: ManifestoAnchor;     // optional mobile override (D-33)
  pillar: ManifestoPillar;            // required — every wiki statement has a pillar
  wikiSource?: string;                // trace back to wiki/concepts/*.md origin
}

export const THESIS_MANIFESTO: readonly ManifestoStatementData[];
```

<!-- Reference pattern for Playwright grep-based source assertion -->
<!-- From tests/phase-29-infra.spec.ts lines 18-43 -->

```typescript
import { test, expect } from "@playwright/test";
import * as fs from "fs";
import * as path from "path";

const ROOT = path.resolve(__dirname, "..");

test.describe("Phase 31: THESIS Section", () => {
  test("TH-05: ...", () => {
    const src = fs.readFileSync(path.resolve(ROOT, "lib/thesis-manifesto.ts"), "utf-8");
    expect(src).toContain("...");
  });
});
```
</interfaces>
</context>

<acceptance_criteria>

**AC-1:** Given the locked wiki copy at `wiki/analyses/v1.5-thesis-copy-draft.md` and the 2026-04-08 "drop magenta, foreground-only" user decision, When the executor transcribes the 6 statements into `lib/thesis-manifesto.ts`, Then every statement text matches the wiki source verbatim (ALL CAPS, punctuation preserved, no paraphrase).

**AC-2:** Given the transcribed manifesto text, When `lib/thesis-manifesto.ts` is written, Then the file exports `ManifestoSize`, `ManifestoPillar`, `ManifestoAnchor`, `ManifestoStatementData`, and `THESIS_MANIFESTO` with the exact type names specified in the Plan 01 `<interfaces>` block, AND `grep -c "^export" lib/thesis-manifesto.ts` reports 5 or more matches.

**AC-3:** Given the THESIS_MANIFESTO constant, When its contents are inspected, Then it contains exactly 6 statement objects, AND every object has `size: "anchor"`, AND the set of `pillar` values is a superset of `{"signal-frame", "enhanced-flat", "biophilia"}` (the three TH-05 required pillars) and may also include `"culture-infrastructure"` and `"memetic-engineering"` (wiki extensions).

**AC-4:** Given the manifesto content, When any statement's `text` field is word-counted by splitting on whitespace, Then every statement has between 2 and 8 words inclusive, AND no statement contains any of the hedge tokens `might`, `could`, `perhaps`, `probably`, `maybe`, `tends`, or `somewhat` (case-insensitive).

**AC-5:** Given all 6 statements are anchors, When their `text` values are read, Then every statement asserts (not explains) the pillar position — i.e. the text contains no conjunction opener (`because`, `since`, `therefore`), no hedging verb, and is written in ALL CAPS with period-terminated clauses. The three paired-clause statements (S1 "DETERMINISTIC STRUCTURE. GENERATIVE EXPRESSION.", S2 "ZERO RADIUS. ZERO COMPROMISE.", S6 "THE FRAME HOLDS. THE SIGNAL MOVES.") follow the period-separated pair register established in the wiki copy.

**AC-6:** Given `lib/thesis-manifesto.ts`, When `pnpm exec tsc --noEmit` is run, Then the compiler reports zero errors in `lib/thesis-manifesto.ts` (type shape matches the `<interfaces>` contract, no `any` types).

**AC-7:** Given the test file `tests/phase-31-thesis.spec.ts` created by this plan, When `pnpm exec playwright test tests/phase-31-thesis.spec.ts -g "TH-05"` is executed against the transcribed manifesto, Then all TH-05 content-coverage tests pass (>= 1 test case).

**AC-8:** Given the SIGNAL/FRAME ordering rule from `CLAUDE.md` and user memory, When the manifesto is inspected for layer naming order, Then no statement uses `FRAME/SIGNAL`, `FRAME and SIGNAL`, or `FRAME first then SIGNAL` slash-ordering. Statement S6 "THE FRAME HOLDS. THE SIGNAL MOVES." is accepted as a paired-clause exception — both clauses are independent sentences with parallel structure, and the wiki copy locks this phrasing. No other FRAME-first single-mention usage is permitted.

</acceptance_criteria>

<tasks>

<task type="checkpoint:decision" gate="resolved">
  <name>Task 1: B-01 manifesto blocker resolution (PRE-CLEARED)</name>
  <ac_refs>AC-1, AC-5, AC-8</ac_refs>
  <status>RESOLVED 2026-04-08. No action required. The user chose the locked wiki copy with foreground-only color. This task is kept in the plan for audit trail only.</status>
  <resolution>
    The user decided on 2026-04-08 (conversation log) to use the 6-statement Grey-approved wiki copy at `wiki/analyses/v1.5-thesis-copy-draft.md` (locked 2026-04-07) as the canonical source, with the wiki's magenta-on-"SIGNAL" accent dropped to enforce CONTEXT D-16 (foreground-only). The researcher's 8-statement draft in `31-RESEARCH.md` is preserved at HEAD~1 for history but is not the implementation source.

    **Approved statements (verbatim from wiki copy):**

    - **S1** (anchor, signal-frame) — "DETERMINISTIC STRUCTURE. GENERATIVE EXPRESSION." — wiki source: `concepts/frame-signal-architecture.md`, `concepts/cybernetic-biophilia.md`
    - **S2** (anchor, enhanced-flat) — "ZERO RADIUS. ZERO COMPROMISE." — wiki source: `concepts/enhanced-flat-design.md` (`--radius: 0px` everywhere)
    - **S3** (anchor, culture-infrastructure) — "CULTURE AS INFRASTRUCTURE." — wiki source: `entities/culture-division.md`, `concepts/cd-event-system.md`
    - **S4** (anchor, biophilia) — "STRUCTURE THAT BREATHES." — wiki source: `concepts/cybernetic-biophilia.md`, `moodboards/generative-systems.md`
    - **S5** (anchor, memetic-engineering) — "FORMAT IS STRATEGY." — wiki source: `concepts/memetic-engineering.md`
    - **S6** (anchor, signal-frame) — "THE FRAME HOLDS. THE SIGNAL MOVES." — wiki source: `concepts/frame-signal-architecture.md`

    **Coverage check:**
    - 6 anchors (every statement) — over-satisfies TH-03's 3-moment floor
    - 6 total statements — matches locked wiki count (CONTEXT D-02 revised)
    - Word range 2-7 per statement — within revised D-02 2-8 range
    - TH-05 required pillars (signal-frame, enhanced-flat, biophilia) all present
    - Wiki extensions (culture-infrastructure, memetic-engineering) included — user approved
    - All trace to specific wiki sources — D-01 authenticity
    - ALL CAPS, period-terminated clauses — D-03 register and D-05 case
    - No hedge words, no conjunctions, no marketing cadence

    **SIGNAL/FRAME ordering note:** S6 "THE FRAME HOLDS. THE SIGNAL MOVES." is accepted as a paired-clause exception to the "SIGNAL first" ordering rule. The two clauses are independent parallel sentences naming each layer equally; the wiki copy locks this phrasing as the closing statement before PROOF. No single-mention FRAME-first usage exists anywhere in the 6-statement set.
  </resolution>
  <boundaries>
    This task is informational. Task 2 (writing `lib/thesis-manifesto.ts`) is free to fire immediately without waiting for a new user decision.
  </boundaries>
  <done>User decision recorded in this file and in the commit history (3281e45 preserves the obsolete draft; the next commit transcribes the wiki copy).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write lib/thesis-manifesto.ts transcribing the wiki copy</name>
  <ac_refs>AC-1, AC-2, AC-3, AC-4, AC-5, AC-6, AC-8</ac_refs>
  <read_first>
    - lib/thesis-manifesto.ts (this file is being created — confirm it does not already exist)
    - wiki/analyses/v1.5-thesis-copy-draft.md (the canonical 6-statement source — at `~/Library/Mobile Documents/iCloud~md~obsidian/Documents/second-brain/wiki/analyses/v1.5-thesis-copy-draft.md`)
    - .planning/phases/31-thesis-section/31-CONTEXT.md (lines 22-50 — revised decisions D-01 through D-05 + override preamble)
    - CLAUDE.md (TypeScript strict rules, SIGNAL/FRAME ordering)
    - tsconfig.json (verify strict mode is on — all type annotations must be explicit, no implicit `any`)
  </read_first>
  <behavior>
    - Behavior 1: Runtime import is not the test vehicle (TypeScript cannot be required without a transpile step); the test reads the source file as text via `fs.readFileSync` matching the Phase 29 pattern.
    - Behavior 2: The file exports exactly these named symbols: `ManifestoSize` (type alias), `ManifestoPillar` (type alias), `ManifestoAnchor` (interface), `ManifestoStatementData` (interface), `THESIS_MANIFESTO` (const array).
    - Behavior 3: `THESIS_MANIFESTO` has a literal length of exactly 6 — the test counts `text:` fields inside the array literal.
    - Behavior 4: The file contains the three TH-05 required pillar strings `"signal-frame"`, `"enhanced-flat"`, `"biophilia"` each assigned to at least one object whose `size` is `"anchor"`. Two additional wiki-extension pillars (`"culture-infrastructure"`, `"memetic-engineering"`) are also present.
    - Behavior 5: No hedge tokens present (case-insensitive): `might`, `could`, `perhaps`, `probably`, `maybe`, `tends`, `somewhat`.
    - Behavior 6: `pnpm exec tsc --noEmit` returns zero errors.
    - Behavior 7: Every statement `text` field is ALL CAPS (matches wiki copy rendering).
  </behavior>
  <action>
    Create `lib/thesis-manifesto.ts` by transcribing the 6 statements from `wiki/analyses/v1.5-thesis-copy-draft.md` verbatim. The file has three parts:

    **Part A — Type definitions** (place at top, before the constant):

    ```typescript
    export type ManifestoSize = "anchor" | "connector";

    export type ManifestoPillar =
      | "signal-frame"
      | "enhanced-flat"
      | "culture-infrastructure"
      | "biophilia"
      | "memetic-engineering";

    export interface ManifestoAnchor {
      top?: string;
      left?: string;
      right?: string;
      bottom?: string;
    }

    export interface ManifestoStatementData {
      text: string;
      size: ManifestoSize;
      anchor: ManifestoAnchor;
      mobileAnchor?: ManifestoAnchor;
      pillar: ManifestoPillar;
      wikiSource?: string;
    }
    ```

    **Part B — The constant.** Transcribe the 6 Grey-approved wiki statements EXACTLY in ALL CAPS. Every object has `text`, `size: "anchor"`, `anchor`, `pillar`, and `wikiSource` keys. The `anchor` coordinates below are first-draft asymmetric positions — Plan 02 is free to refine them during layout choreography.

    ```typescript
    export const THESIS_MANIFESTO: readonly ManifestoStatementData[] = [
      {
        // S1 — signal-frame thesis opener — paired clauses, two-line stack at biggest scale
        text: "DETERMINISTIC STRUCTURE. GENERATIVE EXPRESSION.",
        size: "anchor",
        pillar: "signal-frame",
        anchor: { top: "22vh", left: "8vw" },
        wikiSource: "concepts/frame-signal-architecture.md, concepts/cybernetic-biophilia.md",
      },
      {
        // S2 — enhanced-flat design rule — hard declarative, the aesthetic as manifesto
        text: "ZERO RADIUS. ZERO COMPROMISE.",
        size: "anchor",
        pillar: "enhanced-flat",
        anchor: { top: "30vh", right: "6vw" },
        wikiSource: "concepts/enhanced-flat-design.md (--radius: 0px)",
      },
      {
        // S3 — culture-as-infrastructure — operational philosophy, wiki-copy extension
        text: "CULTURE AS INFRASTRUCTURE.",
        size: "anchor",
        pillar: "culture-infrastructure",
        anchor: { bottom: "34vh", left: "12vw" },
        wikiSource: "entities/culture-division.md, concepts/cd-event-system.md",
      },
      {
        // S4 — cybernetic biophilia — founding principle compressed to three words
        text: "STRUCTURE THAT BREATHES.",
        size: "anchor",
        pillar: "biophilia",
        anchor: { top: "50vh", right: "18vw" },
        wikiSource: "concepts/cybernetic-biophilia.md, moodboards/generative-systems.md",
      },
      {
        // S5 — memetic engineering — container-as-strategy, wiki-copy extension
        text: "FORMAT IS STRATEGY.",
        size: "anchor",
        pillar: "memetic-engineering",
        anchor: { bottom: "18vh", right: "8vw" },
        wikiSource: "concepts/memetic-engineering.md",
      },
      {
        // S6 — signal-frame closer — paired symmetric clauses (FRAME-first accepted per AC-8 wiki-lock exception)
        text: "THE FRAME HOLDS. THE SIGNAL MOVES.",
        size: "anchor",
        pillar: "signal-frame",
        anchor: { bottom: "28vh", left: "10vw" },
        wikiSource: "concepts/frame-signal-architecture.md",
      },
    ] as const;
    ```

    **Part C — No default export.** Named exports only. This file is consumed by `components/blocks/thesis-section.tsx` in Plan 02 via `import { THESIS_MANIFESTO, type ManifestoStatementData } from "@/lib/thesis-manifesto";`.

    **After writing:** Run `pnpm exec tsc --noEmit` and fix any type errors. Do NOT use `any`. Do NOT use `@ts-ignore`. If the `readonly` + `as const` combination causes issues when Plan 02 consumes the array, downgrade to a plain `ManifestoStatementData[]` without `as const` — flag the change in the commit message.

    Commit message: `Feat(31): transcribe thesis manifesto from locked wiki copy (lib/thesis-manifesto.ts)` — include the 6-line summary of statement texts in the commit body for easy git blame lookup.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit 2>&1 | grep -E "lib/thesis-manifesto\.ts" | head -5 ; test -f lib/thesis-manifesto.ts && grep -c "^export" lib/thesis-manifesto.ts && grep -c "text:" lib/thesis-manifesto.ts && grep -c "size: \"anchor\"" lib/thesis-manifesto.ts</automated>
  </verify>
  <boundaries>
    Do NOT modify any file other than `lib/thesis-manifesto.ts`. Do NOT create `components/blocks/thesis-section.tsx` — that is Plan 02's responsibility. Do NOT edit `app/page.tsx`. Do NOT add tests yet — Task 3 adds the content-coverage test. Do NOT import `lib/thesis-manifesto.ts` from any component file — Plan 02 handles all consumers.
  </boundaries>
  <done>
    - `lib/thesis-manifesto.ts` exists on disk
    - `grep -c "^export" lib/thesis-manifesto.ts` reports >= 5 (ManifestoSize, ManifestoPillar, ManifestoAnchor, ManifestoStatementData, THESIS_MANIFESTO)
    - `grep -c "text:" lib/thesis-manifesto.ts` reports exactly 6
    - `grep -c "size: \"anchor\"" lib/thesis-manifesto.ts` reports exactly 6
    - `grep -E "\"(signal-frame|enhanced-flat|biophilia)\"" lib/thesis-manifesto.ts` matches all three required pillar strings at least once
    - `grep -iE "\\b(might|could|perhaps|probably|maybe|tends|somewhat)\\b" lib/thesis-manifesto.ts` returns zero matches inside `text:` values (executor verifies against the 6 extracted text literals specifically)
    - `pnpm exec tsc --noEmit` reports zero errors for `lib/thesis-manifesto.ts`
    - File committed to git with `Feat(31):` prefix
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create tests/phase-31-thesis.spec.ts with TH-05 content-coverage test</name>
  <ac_refs>AC-7</ac_refs>
  <read_first>
    - tests/phase-31-thesis.spec.ts (this file is being created — confirm absent)
    - tests/phase-29-infra.spec.ts (full file — canonical pattern for source-reading grep tests)
    - lib/thesis-manifesto.ts (the file written in Task 2 — confirm content matches expected shape)
    - playwright.config.ts (verify test discovery pattern, no new config needed)
    - .planning/phases/31-thesis-section/31-RESEARCH.md (lines 517-630 — Validation Architecture, per-requirement test specs)
  </read_first>
  <behavior>
    - Test 1: `TH-05: manifesto file exports 6 statements` — reads `lib/thesis-manifesto.ts` via fs, counts occurrences of `text:` inside the THESIS_MANIFESTO array literal, asserts exactly 6.
    - Test 2: `TH-05: manifesto covers all three TH-05 pillars with anchor statements` — reads the file, asserts each of `"signal-frame"`, `"enhanced-flat"`, `"biophilia"` appears at least once as a `pillar:` value.
    - Test 3: `TH-05: manifesto has exactly 6 anchor statements` — counts `size: "anchor"` occurrences, asserts exactly 6 (all statements are anchors in the wiki copy).
    - Test 4: `TH-05: manifesto text contains zero hedge words` — extracts every `text: "..."` string literal from the source, asserts none contain hedge tokens (case-insensitive).
    - Test 5: `TH-05: manifesto file exports typed interfaces` — asserts the source contains `export type ManifestoSize`, `export type ManifestoPillar`, `export interface ManifestoAnchor`, `export interface ManifestoStatementData`, and `export const THESIS_MANIFESTO`.
    - Test 6: `TH-05: every statement is 2-8 words` — extracts `text: "..."` literals, asserts each has a whitespace-split word count in [2, 8].
  </behavior>
  <action>
    Create `tests/phase-31-thesis.spec.ts`. This is the Wave 0 test scaffold for Phase 31 — Plan 02 will extend the SAME file (not create a new file) with TH-01 through TH-04 and TH-06 browser-level tests.

    File structure — use the exact pattern from `tests/phase-29-infra.spec.ts`:

    ```typescript
    /**
     * Phase 31 — THESIS Section
     * Plan 01 adds TH-05 content-coverage tests that read the manifesto source file.
     * Plan 02 extends this file with TH-01, TH-02, TH-03, TH-04, TH-06 browser-level tests.
     *
     * Source-level tests use fs.readFileSync matching the Phase 29 pattern.
     * Browser tests require the dev server on http://localhost:3000 (Phase 30 homepage must be rendered).
     */
    import { test, expect } from "@playwright/test";
    import * as fs from "fs";
    import * as path from "path";

    const ROOT = path.resolve(__dirname, "..");

    test.describe("Phase 31: THESIS Section", () => {
      // ── TH-05: Manifesto content coverage (source-file grep) ──────────────────

      test("TH-05: manifesto file exists and exports typed interfaces", () => {
        const src = fs.readFileSync(path.resolve(ROOT, "lib/thesis-manifesto.ts"), "utf-8");
        expect(src).toContain("export type ManifestoSize");
        expect(src).toContain("export type ManifestoPillar");
        expect(src).toContain("export interface ManifestoAnchor");
        expect(src).toContain("export interface ManifestoStatementData");
        expect(src).toContain("export const THESIS_MANIFESTO");
      });

      test("TH-05: manifesto contains exactly 6 statements", () => {
        const src = fs.readFileSync(path.resolve(ROOT, "lib/thesis-manifesto.ts"), "utf-8");
        // Slice to the array literal to avoid counting `text:` inside comments or types
        const arrayStart = src.indexOf("THESIS_MANIFESTO");
        expect(arrayStart).toBeGreaterThan(-1);
        const arraySlice = src.slice(arrayStart);
        const textMatches = arraySlice.match(/\btext:\s*"/g) ?? [];
        expect(textMatches.length).toBe(6);
      });

      test("TH-05: manifesto has exactly 6 anchor statements", () => {
        const src = fs.readFileSync(path.resolve(ROOT, "lib/thesis-manifesto.ts"), "utf-8");
        const anchorMatches = src.match(/size:\s*"anchor"/g) ?? [];
        expect(anchorMatches.length).toBe(6);
      });

      test("TH-05: manifesto covers all three TH-05 required pillars", () => {
        const src = fs.readFileSync(path.resolve(ROOT, "lib/thesis-manifesto.ts"), "utf-8");
        // The three TH-05 required pillars must each appear as a `pillar:` value
        expect(src).toMatch(/pillar:\s*"signal-frame"/);
        expect(src).toMatch(/pillar:\s*"enhanced-flat"/);
        expect(src).toMatch(/pillar:\s*"biophilia"/);
      });

      test("TH-05: manifesto text contains no hedge words", () => {
        const src = fs.readFileSync(path.resolve(ROOT, "lib/thesis-manifesto.ts"), "utf-8");
        const textLiterals = Array.from(
          src.matchAll(/\btext:\s*"([^"]+)"/g),
          (m) => m[1]
        );
        expect(textLiterals.length).toBe(6);

        const hedgePattern = /\b(might|could|perhaps|probably|maybe|tends|somewhat)\b/i;
        for (const literal of textLiterals) {
          expect(literal, `Statement "${literal}" contains a hedge word`).not.toMatch(hedgePattern);
        }
      });

      test("TH-05: every statement is 2-8 words", () => {
        const src = fs.readFileSync(path.resolve(ROOT, "lib/thesis-manifesto.ts"), "utf-8");
        const textLiterals = Array.from(
          src.matchAll(/\btext:\s*"([^"]+)"/g),
          (m) => m[1]
        );
        for (const literal of textLiterals) {
          const wordCount = literal.trim().split(/\s+/).length;
          expect(wordCount, `Statement "${literal}" has ${wordCount} words`).toBeGreaterThanOrEqual(2);
          expect(wordCount, `Statement "${literal}" has ${wordCount} words`).toBeLessThanOrEqual(8);
        }
      });

      // Plan 02 will append TH-01, TH-02, TH-03, TH-04, TH-06 browser-level tests below this line.
    });
    ```

    **No `test.beforeAll` or `test.beforeEach` needed** — all TH-05 tests are pure synchronous file reads. The dev server is only required for Plan 02's browser tests.

    After writing the test file, run the verify command below to confirm all 6 TH-05 tests pass against the manifesto file authored in Task 2.

    Commit message: `Feat(31): add TH-05 content-coverage tests (tests/phase-31-thesis.spec.ts)` — mention that Plan 02 will extend this same file.
  </action>
  <verify>
    <automated>pnpm exec playwright test tests/phase-31-thesis.spec.ts --reporter=list 2>&1 | tee /tmp/ph31-plan01-verify.log ; grep -E "TH-05.*(passed|failed)" /tmp/ph31-plan01-verify.log</automated>
  </verify>
  <boundaries>
    Do NOT write tests for TH-01, TH-02, TH-03, TH-04, or TH-06 in this task — those belong to Plan 02 and require the `ThesisSection` component to exist. Do NOT add a `beforeAll` that launches the dev server. Do NOT import from `lib/thesis-manifesto.ts` directly — use `fs.readFileSync` grep assertions (matching Phase 29 pattern and matching RESEARCH's fallback recommendation at line 632). Do NOT modify `playwright.config.ts`. Do NOT modify `tests/phase-29-infra.spec.ts` or any other existing test file.
  </boundaries>
  <done>
    - `tests/phase-31-thesis.spec.ts` exists on disk
    - Running `pnpm exec playwright test tests/phase-31-thesis.spec.ts --reporter=list` reports all 6 TH-05 tests passing
    - `grep -c "TH-05" tests/phase-31-thesis.spec.ts` returns >= 6
    - `grep -c "TH-01\\|TH-02\\|TH-03\\|TH-04\\|TH-06" tests/phase-31-thesis.spec.ts` returns 0 (those tests are Plan 02's scope)
    - File committed to git with `Feat(31):` prefix
  </done>
</task>

</tasks>

<verification>
Overall Plan 01 verification sequence (run after all tasks complete):

1. **File existence:**
   ```bash
   test -f lib/thesis-manifesto.ts && echo "manifesto OK"
   test -f tests/phase-31-thesis.spec.ts && echo "test file OK"
   ```

2. **Type check:**
   ```bash
   pnpm exec tsc --noEmit 2>&1 | grep -E "lib/thesis-manifesto\\.ts" ; echo "tsc exit: $?"
   ```
   Expected: zero errors referencing `lib/thesis-manifesto.ts`.

3. **TH-05 tests green:**
   ```bash
   pnpm exec playwright test tests/phase-31-thesis.spec.ts --reporter=list
   ```
   Expected: 6 passed, 0 failed (all TH-05 tests).

4. **Content coverage grep audit:**
   ```bash
   grep -c "text:" lib/thesis-manifesto.ts       # expect 6
   grep -c "size: \"anchor\"" lib/thesis-manifesto.ts  # expect 6
   grep -cE "pillar: \"(signal-frame|enhanced-flat|biophilia)\"" lib/thesis-manifesto.ts  # expect >= 3
   ```

5. **SIGNAL/FRAME ordering audit (manual scan):**
   ```bash
   grep -n "FRAME/SIGNAL\\|FRAME and SIGNAL\\|FRAME then SIGNAL" lib/thesis-manifesto.ts
   ```
   Expected: zero matches. S6 "THE FRAME HOLDS. THE SIGNAL MOVES." is a paired-clause exception (two independent sentences, not a slash-ordering) and is wiki-locked.

6. **Wiki-source trace receipt:**
   Every statement object has a `wikiSource` field tracing back to a `concepts/*.md`, `analyses/*.md`, `entities/*.md`, or `moodboards/*.md` file.
</verification>

<success_criteria>
Plan 01 is complete when:
- [x] B-01 resolved via locked wiki copy (pre-cleared 2026-04-08)
- [ ] `lib/thesis-manifesto.ts` exists, exports 5 required symbols, contains exactly 6 statements all at `size: "anchor"`, covers the three TH-05 required pillars, contains zero hedge words in statement text literals, passes `pnpm exec tsc --noEmit`
- [ ] `tests/phase-31-thesis.spec.ts` exists, contains 6 TH-05 content-coverage tests, and all 6 tests pass
- [ ] Both files committed with `Feat(31):` prefix
- [ ] `requirements_completed: [TH-05]` (partial — TH-05 is split across content coverage here and text-register verification via Plan 02 integration tests; this plan closes the content-file portion)
- [ ] B-01 manifesto blocker marked RESOLVED in STATE.md
- [ ] Plan 02 is unblocked to begin Wave 2

**Plan 02 hard dependency:** Plan 02 Task 0 (forwardRef upgrade to PinnedSection) can technically run in parallel with this plan because it touches a different file, but per `depends_on: ["01"]` on Plan 02's frontmatter, execution is strictly sequenced.
</success_criteria>

<output>
After completion, create `.planning/phases/31-thesis-section/31-01-SUMMARY.md` following the standard summary template. The summary MUST include:

1. **Decision Log** — record the B-01 resolution (wiki-copy verbatim with foreground-only override per 2026-04-08 user decision) and the final 6-statement text block.
2. **File Manifest** — list `lib/thesis-manifesto.ts` and `tests/phase-31-thesis.spec.ts` with line counts.
3. **Plan 02 Handoff Notes** — confirm:
   - The manifesto constant is available for import as `@/lib/thesis-manifesto`
   - The test file exists and Plan 02 will extend it (append, not replace)
   - B-01 manifesto blocker is resolved in STATE.md (Plan 02 updates STATE.md as part of its work)
4. **Statement Index** — list the 6 statements in order with pillar attribution, default desktop anchor coordinates, and wiki-source trace. This is the Plan 02 engineer's quick reference for type scale and positioning.
5. **Coverage Check** — restate pillar coverage (3 TH-05 required + 2 wiki extensions), word-count range (2-7 observed within the 2-8 AC-4 bound), all-anchor structure, and ALL CAPS verification.
</output>