---
phase: 31-thesis-section
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/thesis-manifesto.ts
  - tests/phase-31-thesis.spec.ts
autonomous: false
requirements:
  - TH-05
must_haves:
  truths:
    - "lib/thesis-manifesto.ts exports a typed array named THESIS_MANIFESTO containing 7 to 9 manifesto statements"
    - "At least 3 entries have size='anchor' and together cover the three pillars signal-frame, enhanced-flat, biophilia"
    - "Every statement text is between 3 and 15 words and avoids hedge words (might, could, perhaps, probably)"
    - "The manifesto text has been reviewed and approved by the user before Plan 02 engineering begins"
    - "tests/phase-31-thesis.spec.ts exists with a TH-05 content coverage test that grep-asserts the three pillar tokens and the 7-9 statement range"
  artifacts:
    - path: "lib/thesis-manifesto.ts"
      provides: "Typed manifesto content + type definitions consumed by components/blocks/thesis-section.tsx in Plan 02"
      exports: ["THESIS_MANIFESTO", "ManifestoStatementData", "ManifestoAnchor", "ManifestoSize"]
    - path: "tests/phase-31-thesis.spec.ts"
      provides: "Playwright test file scaffold covering TH-05 content assertions (Plan 02 extends this same file)"
      contains: "TH-05"
  key_links:
    - from: "lib/thesis-manifesto.ts"
      to: "components/blocks/thesis-section.tsx"
      via: "typed array import (Plan 02 consumes)"
      pattern: "THESIS_MANIFESTO"
    - from: "tests/phase-31-thesis.spec.ts"
      to: "lib/thesis-manifesto.ts"
      via: "grep assertion (fs.readFileSync + toContain pattern matching Phase 29 test style)"
      pattern: "thesis-manifesto"
---

<objective>
Resolve the Phase 31 content blocker (B-01, manifesto copy) by authoring the 8-statement thesis manifesto as a typed TypeScript constant in `lib/thesis-manifesto.ts`, and scaffold the Phase 31 Playwright test file with the TH-05 content coverage test. This plan enforces the D-01 / B-01 user-review gate at the plan boundary — Plan 02 engineering tasks cannot fire until this plan's manifesto file is reviewed and approved by the user.

Purpose: The manifesto is the content dependency for every engineering task in Plan 02. Separating it into its own plan enforces a user-review checkpoint before any React / GSAP code is written against the content. The researcher already drafted the 8-statement manifesto in `.planning/phases/31-thesis-section/31-RESEARCH.md` lines 400-473 — this plan surfaces that draft to the user, converts it into a typed file, and ensures the file can be verified by the test suite.

Output:
- `lib/thesis-manifesto.ts` (new) — typed array constant, final text approved by user
- `tests/phase-31-thesis.spec.ts` (new, scaffold) — Playwright file containing the TH-05 content coverage test; Plan 02 extends with TH-01 through TH-04 and TH-06 tests
</objective>

<execution_context>
@/Users/greyaltaer/.claude/pde-os/engines/gsd/workflows/execute-plan.md
@/Users/greyaltaer/.claude/pde-os/engines/gsd/templates/summary.md
</execution_context>

<context>
@.planning/project-context.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/31-thesis-section/31-CONTEXT.md
@.planning/phases/31-thesis-section/31-RESEARCH.md
@.planning/phases/31-thesis-section/31-VALIDATION.md
@tests/phase-29-infra.spec.ts
@CLAUDE.md

<interfaces>
<!-- Contract this plan CREATES (Plan 02 will consume these exports verbatim) -->

```typescript
// lib/thesis-manifesto.ts — CREATED by this plan

export type ManifestoSize = "anchor" | "connector";

export interface ManifestoAnchor {
  top?: string;
  left?: string;
  right?: string;
  bottom?: string;
}

export interface ManifestoStatementData {
  text: string;
  size: ManifestoSize;
  anchor: ManifestoAnchor;            // desktop anchor
  mobileAnchor?: ManifestoAnchor;     // optional mobile override (D-33)
  pillar?: "signal-frame" | "enhanced-flat" | "biophilia" | "connector";
}

export const THESIS_MANIFESTO: ManifestoStatementData[];
```

<!-- Reference pattern for Playwright grep-based source assertion -->
<!-- From tests/phase-29-infra.spec.ts lines 18-43 -->

```typescript
import { test, expect } from "@playwright/test";
import * as fs from "fs";
import * as path from "path";

const ROOT = path.resolve(__dirname, "..");

test.describe("Phase 31: THESIS Section", () => {
  test("TH-05: ...", () => {
    const src = fs.readFileSync(path.resolve(ROOT, "lib/thesis-manifesto.ts"), "utf-8");
    expect(src).toContain("...");
  });
});
```
</interfaces>
</context>

<acceptance_criteria>

**AC-1:** Given the researcher's draft in `31-RESEARCH.md` "Manifesto Copy Strategy" section, When the executor presents the 8-statement draft to the user via a decision checkpoint with explicit questions from RESEARCH lines 493-499, Then the user either approves the draft verbatim or supplies revisions, and the executor records the final text before writing any TypeScript.

**AC-2:** Given the approved manifesto text, When `lib/thesis-manifesto.ts` is written, Then the file exports `ManifestoSize`, `ManifestoAnchor`, `ManifestoStatementData`, and `THESIS_MANIFESTO` with the exact type names specified in the Plan 01 `<interfaces>` block, AND `grep -c "export" lib/thesis-manifesto.ts` reports 4 or more matches.

**AC-3:** Given the THESIS_MANIFESTO constant, When its contents are inspected, Then it contains between 7 and 9 statement objects (inclusive), AND at least 3 objects have `size: "anchor"`, AND the set of `pillar` values on anchor entries is a superset of `{"signal-frame", "enhanced-flat", "biophilia"}`.

**AC-4:** Given the manifesto content, When any statement's `text` field is word-counted by splitting on whitespace, Then every statement has between 3 and 15 words inclusive, AND no statement contains any of the hedge tokens `might`, `could`, `perhaps`, `probably`, `maybe`, `tends`, or `somewhat` (case-insensitive).

**AC-5:** Given the three anchor statements, When their `text` values are read, Then each anchor statement covers exactly one of the three pillars via its `pillar` field AND the text asserts (not explains) the pillar position — i.e. the text contains no conjunction opener (`because`, `since`, `therefore`), no hedging verb, and terminates in either a period, no punctuation, or a period-separated pair (matching draft S2 "FRAME carries. SIGNAL breathes." register).

**AC-6:** Given `lib/thesis-manifesto.ts`, When `pnpm exec tsc --noEmit` is run, Then the compiler reports zero errors in `lib/thesis-manifesto.ts` (type shape matches the `<interfaces>` contract, no `any` types).

**AC-7:** Given the test file `tests/phase-31-thesis.spec.ts` created by this plan, When `pnpm exec playwright test tests/phase-31-thesis.spec.ts -g "TH-05"` is executed against the authored manifesto, Then all TH-05 content-coverage tests pass (>= 1 test case).

**AC-8:** Given the SIGNAL/FRAME ordering rule from `CLAUDE.md` and user memory, When the manifesto is inspected for layer naming order, Then any statement that mentions both layers names SIGNAL first (e.g. `SIGNAL/FRAME`, `SIGNAL and FRAME`, or mirrored clauses where SIGNAL appears first) AND no statement writes `FRAME/SIGNAL`, `FRAME and SIGNAL`, or `FRAME first then SIGNAL`. Draft S2 "FRAME carries. SIGNAL breathes." is a paired-clause exception because both clauses are symmetric — but if the user approves that line, the executor must confirm symmetry is intact and flag any other FRAME-first single-mention usage.

</acceptance_criteria>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Present manifesto draft to user for review (B-01 resolution)</name>
  <ac_refs>AC-1, AC-5, AC-8</ac_refs>
  <read_first>
    - .planning/phases/31-thesis-section/31-RESEARCH.md (lines 373-511 — Manifesto Copy Strategy, Draft, Coverage Check, User Review Questions, Authoring Trace)
    - .planning/phases/31-thesis-section/31-CONTEXT.md (lines 22-28 — D-01 through D-05 locked decisions)
    - CLAUDE.md (SIGNAL/FRAME ordering rule, Enhanced Flat Design style rules)
    - .planning/STATE.md (lines 116-117 — B-01 manifesto blocker constraint)
  </read_first>
  <decision>
    Approve the 8-statement manifesto draft from RESEARCH.md lines 400-473, or supply targeted revisions. Each statement is reproduced below with its pillar, anchor position, and source trace. The user's response unblocks Plan 02 engineering.
  </decision>
  <context>
    The Phase 31 THESIS section requires a 7-9 statement manifesto covering (a) SIGNAL/FRAME thesis, (b) Enhanced Flat Design, (c) cybernetic biophilia — as declarative statements, not explanations. The researcher drafted 8 statements grounded in cdSB wiki sources (`culture-division-operating-principles.md`, `frame-signal-intellectual-lineage.md`, `cybernetic-biophilia.md`, `enhanced-flat-design.md`, `frame-signal-architecture.md`). Every line traces back to a wiki source. The draft satisfies every formal constraint (3 anchors, 8 total, all phrases 5-10 words, three pillars covered). What remains is the user's creative judgment on register and conceptual accuracy.

    **DRAFT (full text, from RESEARCH.md lines 400-473):**

    - **S1** (connector, opening) — "A system has two layers." — anchor { top: 18vh, left: 8vw } — trace: frame-signal-architecture.md "Two distinct layers that coexist"
    - **S2** (anchor, signal-frame) — "FRAME carries. SIGNAL breathes." — anchor { top: 32vh, left: 6vw } — trace: FRAME structural / SIGNAL generative paired clauses
    - **S3** (connector) — "Noise is the material, not the enemy." — anchor { top: 48vh, right: 10vw } — trace: frame-signal-intellectual-lineage.md §1 Shannon inversion
    - **S4** (anchor, enhanced-flat) — "Depth is spacing. Depth is hierarchy. Not shadow." — anchor { top: 30vh, right: 4vw } — trace: enhanced-flat-design.md depth techniques
    - **S5** (connector) — "Structure is not decoration removed. Structure is the decision." — anchor { bottom: 24vh, left: 12vw } — trace: enhanced-flat-design.md rules
    - **S6** (anchor, biophilia) — "A machine that breathes. A body that computes." — anchor { bottom: 18vh, right: 6vw } — trace: cybernetic-biophilia.md direct lift
    - **S7** (connector) — "The tension does not resolve. The tension is the field." — anchor { bottom: 12vh, left: 30vw } — trace: cybernetic-biophilia.md "Why It Matters"
    - **S8** (connector, closing) — "You control how much life bleeds through." — anchor { top: 50vh, left: 22vw } — trace: frame-signal-architecture.md direct lift

    **Coverage check (RESEARCH lines 476-491):**
    - 3 anchors (S2, S4, S6) — TH-03 floor satisfied
    - 8 total statements — within D-02 7-9 range
    - Every phrase <=10 words — within D-02 5-15 range
    - Every pillar has >=1 anchor — D-04 satisfied
    - All trace to specific wiki sources — D-01 authenticity
    - No marketing cadence, no hedge words, no explanation mode — D-03 register

    **SIGNAL/FRAME ordering note:** S2 "FRAME carries. SIGNAL breathes." begins with FRAME for the paired-clause rhythm. The CLAUDE.md rule is "SIGNAL/FRAME ordering, never FRAME/SIGNAL" — this refers to single-mention listings. Paired symmetric clauses where both layers are named equally are an acceptable exception. The executor must confirm with the user that this exception is intentional. If the user prefers strict SIGNAL-first, the line can be revised to "SIGNAL breathes. FRAME carries."
  </context>
  <options>
    <option id="approve-draft-verbatim">
      <name>Approve the draft verbatim (all 8 statements unchanged, including the FRAME-first paired clause in S2)</name>
      <pros>Preserves the researcher's direct wiki traces and the S2 paired-clause rhythm; zero content churn; engineering can start immediately.</pros>
      <cons>Commits to S2's FRAME-first clause order as an intentional symmetric exception.</cons>
    </option>
    <option id="approve-with-s2-signal-first">
      <name>Approve draft but reverse S2 to "SIGNAL breathes. FRAME carries." (SIGNAL-first across the whole manifesto)</name>
      <pros>Enforces strict SIGNAL/FRAME ordering from CLAUDE.md and user memory feedback_signal_frame_order.md.</pros>
      <cons>Subtly changes the sonic rhythm of the anchor statement; S2 becomes a SIGNAL-lead declaration which is arguably stronger anyway.</cons>
    </option>
    <option id="revise-specific-lines">
      <name>User supplies targeted revisions to specific statements (list which lines to change and the new text)</name>
      <pros>Fine-grained creative control; user preserves any lines that land and revises only the ones that don't.</pros>
      <cons>Executor must verify that revised lines still satisfy AC-3 (pillar coverage), AC-4 (word count + no hedges), AC-5 (anchor register), and AC-8 (SIGNAL/FRAME ordering) before proceeding to Task 2.</cons>
    </option>
    <option id="rewrite-fully">
      <name>User rewrites the manifesto from scratch (supply all 7-9 statements with pillar assignments and desktop anchor positions)</name>
      <pros>Full authorial control if the draft's register is off.</pros>
      <cons>Largest scope; executor still validates against AC-3, AC-4, AC-5, AC-8 before writing the typed file.</cons>
    </option>
  </options>
  <boundaries>
    Do NOT write `lib/thesis-manifesto.ts` in this task. Do NOT modify any other file. This task's only output is a recorded user decision (approved text, whatever form). The text is handed to Task 2 via the executor's working memory or a temporary note file.
  </boundaries>
  <resume-signal>Reply with `approve-draft-verbatim`, `approve-with-s2-signal-first`, `revise-specific-lines: [text]`, or `rewrite-fully: [text]`.</resume-signal>
  <done>User has recorded a decision. The approved final text (8 statements with pillars and anchors) is available to Task 2. No file has been written yet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write lib/thesis-manifesto.ts with approved content</name>
  <ac_refs>AC-2, AC-3, AC-4, AC-5, AC-6, AC-8</ac_refs>
  <read_first>
    - lib/thesis-manifesto.ts (this file is being created — confirm it does not already exist)
    - .planning/phases/31-thesis-section/31-RESEARCH.md (lines 236-258 — type shape, lines 400-473 — draft content as fallback reference)
    - .planning/phases/31-thesis-section/31-CONTEXT.md (lines 22-28 — D-01 through D-05)
    - CLAUDE.md (TypeScript strict rules, SIGNAL/FRAME ordering)
    - tsconfig.json (verify strict mode is on — all type annotations must be explicit, no implicit `any`)
  </read_first>
  <behavior>
    - Behavior 1: Running `node -e "const m = require('./lib/thesis-manifesto.ts'); console.log(m.THESIS_MANIFESTO.length)"` is not the test vehicle (TypeScript cannot be required at runtime without a transpile step); instead the test reads the source file as text via `fs.readFileSync` (matching Phase 29 pattern).
    - Behavior 2: The file exports exactly these named symbols: `ManifestoSize` (type alias), `ManifestoAnchor` (interface), `ManifestoStatementData` (interface), `THESIS_MANIFESTO` (const array).
    - Behavior 3: `THESIS_MANIFESTO` has a literal length between 7 and 9 (inclusive) — the test counts top-level `text:` fields or counts opening braces for statement objects.
    - Behavior 4: The file contains the three pillar strings `"signal-frame"`, `"enhanced-flat"`, `"biophilia"` each assigned to at least one object whose `size` is `"anchor"`.
    - Behavior 5: No hedge tokens present (case-insensitive): `might`, `could`, `perhaps`, `probably`, `maybe`, `tends`, `somewhat`.
    - Behavior 6: `pnpm exec tsc --noEmit` returns zero errors.
  </behavior>
  <action>
    Create `lib/thesis-manifesto.ts` with the exact approved content from Task 1. The file has three parts:

    **Part A — Type definitions** (place at top, before the constant):

    ```typescript
    export type ManifestoSize = "anchor" | "connector";

    export interface ManifestoAnchor {
      top?: string;
      left?: string;
      right?: string;
      bottom?: string;
    }

    export interface ManifestoStatementData {
      text: string;
      size: ManifestoSize;
      anchor: ManifestoAnchor;
      mobileAnchor?: ManifestoAnchor;
      pillar?: "signal-frame" | "enhanced-flat" | "biophilia" | "connector";
    }
    ```

    **Part B — The constant.** Use the Task 1 approved text. If the user approved `approve-draft-verbatim`, transcribe the 8 statements from 31-RESEARCH.md lines 400-473 EXACTLY. Every object has `text`, `size`, `anchor`, and `pillar` keys minimum. Include inline `//` comments tracing each line to its wiki source (per RESEARCH lines 501-512) — these comments document authorial intent and allow the Plan 02 engineer to understand why the text reads as it does.

    The expected structure for the default (approve-draft-verbatim) case:

    ```typescript
    export const THESIS_MANIFESTO: readonly ManifestoStatementData[] = [
      {
        // S1 — opening connector — frame-signal-architecture.md "Two distinct layers that coexist"
        text: "A system has two layers.",
        size: "connector",
        pillar: "connector",
        anchor: { top: "18vh", left: "8vw" },
      },
      {
        // S2 — ANCHOR — signal-frame thesis — paired symmetric clauses (SIGNAL/FRAME exception by user approval)
        text: "FRAME carries. SIGNAL breathes.",
        size: "anchor",
        pillar: "signal-frame",
        anchor: { top: "32vh", left: "6vw" },
      },
      {
        // S3 — connector — frame-signal-intellectual-lineage.md §1 Shannon inversion
        text: "Noise is the material, not the enemy.",
        size: "connector",
        pillar: "connector",
        anchor: { top: "48vh", right: "10vw" },
      },
      {
        // S4 — ANCHOR — enhanced-flat-design.md depth techniques (three-beat rhythm, final denial)
        text: "Depth is spacing. Depth is hierarchy. Not shadow.",
        size: "anchor",
        pillar: "enhanced-flat",
        anchor: { top: "30vh", right: "4vw" },
      },
      {
        // S5 — connector — enhanced-flat-design.md structure as active position
        text: "Structure is not decoration removed. Structure is the decision.",
        size: "connector",
        pillar: "connector",
        anchor: { bottom: "24vh", left: "12vw" },
      },
      {
        // S6 — ANCHOR — cybernetic-biophilia.md direct lift "machines that breathe / organisms that compute"
        text: "A machine that breathes. A body that computes.",
        size: "anchor",
        pillar: "biophilia",
        anchor: { bottom: "18vh", right: "6vw" },
      },
      {
        // S7 — connector — cybernetic-biophilia.md "tension as source of energy"
        text: "The tension does not resolve. The tension is the field.",
        size: "connector",
        pillar: "connector",
        anchor: { bottom: "12vh", left: "30vw" },
      },
      {
        // S8 — closing connector — frame-signal-architecture.md direct lift, user as governor
        text: "You control how much life bleeds through.",
        size: "connector",
        pillar: "signal-frame",
        anchor: { top: "50vh", left: "22vw" },
      },
    ] as const;
    ```

    **Part C — No default export.** Named exports only. This file is consumed by `components/blocks/thesis-section.tsx` in Plan 02 via `import { THESIS_MANIFESTO, type ManifestoStatementData } from "@/lib/thesis-manifesto";`.

    **If user chose `approve-with-s2-signal-first`:** Replace S2's text with `"SIGNAL breathes. FRAME carries."` and update the comment to remove the "FRAME-first exception" note. All other lines unchanged.

    **If user chose `revise-specific-lines` or `rewrite-fully`:** Use the user's approved text verbatim. Preserve the `pillar` attribution and a reasonable anchor position (use RESEARCH's positions as defaults for any position the user did not specify). Every statement must still include a wiki trace comment — if the user supplied new text, the executor writes `// user-supplied — no wiki trace` rather than inventing a source.

    **After writing:** Run `pnpm exec tsc --noEmit` and fix any type errors. Do NOT use `any`. Do NOT use `@ts-ignore`. If the `readonly` + `as const` combination causes issues when Plan 02 consumes the array, downgrade to a plain `ManifestoStatementData[]` without `as const` — flag the change in the commit message.

    Commit message: `Feat(31): author thesis manifesto content (lib/thesis-manifesto.ts)` — include the 8-line summary of statement texts in the commit body for easy git blame lookup.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit 2>&1 | grep -E "lib/thesis-manifesto\.ts" | head -5 ; test -f lib/thesis-manifesto.ts && grep -c "^export" lib/thesis-manifesto.ts && grep -c "text:" lib/thesis-manifesto.ts && grep -c "size: \"anchor\"" lib/thesis-manifesto.ts</automated>
  </verify>
  <boundaries>
    Do NOT modify any file other than `lib/thesis-manifesto.ts`. Do NOT create `components/blocks/thesis-section.tsx` — that is Plan 02's responsibility. Do NOT edit `app/page.tsx`. Do NOT add tests yet — Task 3 adds the content-coverage test. Do NOT import `lib/thesis-manifesto.ts` from any component file — Plan 02 handles all consumers.
  </boundaries>
  <done>
    - `lib/thesis-manifesto.ts` exists on disk
    - `grep -c "^export" lib/thesis-manifesto.ts` reports >= 4 (ManifestoSize, ManifestoAnchor, ManifestoStatementData, THESIS_MANIFESTO)
    - `grep -c "text:" lib/thesis-manifesto.ts` reports between 7 and 9 inclusive
    - `grep -c "size: \"anchor\"" lib/thesis-manifesto.ts` reports >= 3
    - `grep -E "\"(signal-frame|enhanced-flat|biophilia)\"" lib/thesis-manifesto.ts` matches all three pillar strings at least once
    - `grep -iE "\\b(might|could|perhaps|probably|maybe|tends|somewhat)\\b" lib/thesis-manifesto.ts` returns zero matches (hedge-word guard — NB: this regex may match comment words, so executor verifies no matches inside `text:` values specifically)
    - `pnpm exec tsc --noEmit` reports zero errors for `lib/thesis-manifesto.ts`
    - File committed to git with `Feat(31):` prefix
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create tests/phase-31-thesis.spec.ts with TH-05 content-coverage test</name>
  <ac_refs>AC-7</ac_refs>
  <read_first>
    - tests/phase-31-thesis.spec.ts (this file is being created — confirm absent)
    - tests/phase-29-infra.spec.ts (full file — canonical pattern for source-reading grep tests)
    - lib/thesis-manifesto.ts (the file written in Task 2 — confirm content matches expected shape)
    - playwright.config.ts (verify test discovery pattern, no new config needed)
    - .planning/phases/31-thesis-section/31-RESEARCH.md (lines 517-630 — Validation Architecture, per-requirement test specs)
  </read_first>
  <behavior>
    - Test 1: `TH-05: manifesto file exports 7-9 statements` — reads `lib/thesis-manifesto.ts` via fs, counts occurrences of `text:`, asserts the count is between 7 and 9 inclusive.
    - Test 2: `TH-05: manifesto covers all three pillars with anchor statements` — reads the file, asserts each of `"signal-frame"`, `"enhanced-flat"`, `"biophilia"` appears at least once in proximity (+/- 3 lines) to a `size: "anchor"` declaration.
    - Test 3: `TH-05: manifesto has three or more anchor statements` — counts `size: "anchor"` occurrences, asserts >= 3.
    - Test 4: `TH-05: manifesto text contains zero hedge words` — extracts every `text: "..."` string literal from the source, asserts none contain hedge tokens (case-insensitive).
    - Test 5: `TH-05: manifesto file exports typed interfaces` — asserts the source contains `export type ManifestoSize`, `export interface ManifestoAnchor`, `export interface ManifestoStatementData`, and `export const THESIS_MANIFESTO`.
  </behavior>
  <action>
    Create `tests/phase-31-thesis.spec.ts`. This is the Wave 0 test scaffold for Phase 31 — Plan 02 will extend the SAME file (not create a new file) with TH-01 through TH-04 and TH-06 browser-level tests.

    File structure — use the exact pattern from `tests/phase-29-infra.spec.ts`:

    ```typescript
    /**
     * Phase 31 — THESIS Section
     * Plan 01 adds TH-05 content-coverage tests that read the manifesto source file.
     * Plan 02 extends this file with TH-01, TH-02, TH-03, TH-04, TH-06 browser-level tests.
     *
     * Source-level tests use fs.readFileSync matching the Phase 29 pattern.
     * Browser tests require the dev server on http://localhost:3000 (Phase 30 homepage must be rendered).
     */
    import { test, expect } from "@playwright/test";
    import * as fs from "fs";
    import * as path from "path";

    const ROOT = path.resolve(__dirname, "..");

    test.describe("Phase 31: THESIS Section", () => {
      // ── TH-05: Manifesto content coverage (source-file grep) ──────────────────

      test("TH-05: manifesto file exists and exports typed interfaces", () => {
        const src = fs.readFileSync(path.resolve(ROOT, "lib/thesis-manifesto.ts"), "utf-8");
        expect(src).toContain("export type ManifestoSize");
        expect(src).toContain("export interface ManifestoAnchor");
        expect(src).toContain("export interface ManifestoStatementData");
        expect(src).toContain("export const THESIS_MANIFESTO");
      });

      test("TH-05: manifesto contains 7-9 statements", () => {
        const src = fs.readFileSync(path.resolve(ROOT, "lib/thesis-manifesto.ts"), "utf-8");
        // Count occurrences of `text:` inside the THESIS_MANIFESTO array
        // Slice the file to the array literal to avoid counting any other `text:` in comments or types
        const arrayStart = src.indexOf("THESIS_MANIFESTO");
        expect(arrayStart).toBeGreaterThan(-1);
        const arraySlice = src.slice(arrayStart);
        const textMatches = arraySlice.match(/\btext:\s*"/g) ?? [];
        expect(textMatches.length).toBeGreaterThanOrEqual(7);
        expect(textMatches.length).toBeLessThanOrEqual(9);
      });

      test("TH-05: manifesto has at least 3 anchor statements", () => {
        const src = fs.readFileSync(path.resolve(ROOT, "lib/thesis-manifesto.ts"), "utf-8");
        const anchorMatches = src.match(/size:\s*"anchor"/g) ?? [];
        expect(anchorMatches.length).toBeGreaterThanOrEqual(3);
      });

      test("TH-05: manifesto covers all three pillars via anchor statements", () => {
        const src = fs.readFileSync(path.resolve(ROOT, "lib/thesis-manifesto.ts"), "utf-8");
        // Each anchor statement must be attributed to one of the three pillars
        expect(src).toMatch(/pillar:\s*"signal-frame"/);
        expect(src).toMatch(/pillar:\s*"enhanced-flat"/);
        expect(src).toMatch(/pillar:\s*"biophilia"/);

        // Additional semantic coverage check — the three pillar identifiers must exist in the file at all
        expect(src).toContain("signal-frame");
        expect(src).toContain("enhanced-flat");
        expect(src).toContain("biophilia");
      });

      test("TH-05: manifesto text contains no hedge words", () => {
        const src = fs.readFileSync(path.resolve(ROOT, "lib/thesis-manifesto.ts"), "utf-8");
        // Extract every `text: "..."` literal value (not keys, not comments)
        const textLiterals = Array.from(
          src.matchAll(/\btext:\s*"([^"]+)"/g),
          (m) => m[1]
        );
        expect(textLiterals.length).toBeGreaterThanOrEqual(7);

        const hedgePattern = /\b(might|could|perhaps|probably|maybe|tends|somewhat)\b/i;
        for (const literal of textLiterals) {
          expect(literal, `Statement "${literal}" contains a hedge word`).not.toMatch(hedgePattern);
        }
      });

      test("TH-05: every statement is 3-15 words", () => {
        const src = fs.readFileSync(path.resolve(ROOT, "lib/thesis-manifesto.ts"), "utf-8");
        const textLiterals = Array.from(
          src.matchAll(/\btext:\s*"([^"]+)"/g),
          (m) => m[1]
        );
        for (const literal of textLiterals) {
          const wordCount = literal.trim().split(/\s+/).length;
          expect(wordCount, `Statement "${literal}" has ${wordCount} words`).toBeGreaterThanOrEqual(3);
          expect(wordCount, `Statement "${literal}" has ${wordCount} words`).toBeLessThanOrEqual(15);
        }
      });

      // Plan 02 will append TH-01, TH-02, TH-03, TH-04, TH-06 browser-level tests below this line.
    });
    ```

    **No `test.beforeAll` or `test.beforeEach` needed** — all TH-05 tests are pure synchronous file reads. The dev server is only required for Plan 02's browser tests.

    After writing the test file, run the verify command below to confirm all 6 TH-05 tests pass against the manifesto file authored in Task 2.

    Commit message: `Feat(31): add TH-05 content-coverage tests (tests/phase-31-thesis.spec.ts)` — mention that Plan 02 will extend this same file.
  </action>
  <verify>
    <automated>pnpm exec playwright test tests/phase-31-thesis.spec.ts --reporter=list 2>&1 | tee /tmp/ph31-plan01-verify.log ; grep -E "TH-05.*(passed|failed)" /tmp/ph31-plan01-verify.log</automated>
  </verify>
  <boundaries>
    Do NOT write tests for TH-01, TH-02, TH-03, TH-04, or TH-06 in this task — those belong to Plan 02 and require the `ThesisSection` component to exist. Do NOT add a `beforeAll` that launches the dev server. Do NOT import from `lib/thesis-manifesto.ts` directly — use `fs.readFileSync` grep assertions (matching Phase 29 pattern and matching RESEARCH's fallback recommendation at line 632). Do NOT modify `playwright.config.ts`. Do NOT modify `tests/phase-29-infra.spec.ts` or any other existing test file.
  </boundaries>
  <done>
    - `tests/phase-31-thesis.spec.ts` exists on disk
    - Running `pnpm exec playwright test tests/phase-31-thesis.spec.ts --reporter=list` reports all 6 TH-05 tests passing
    - `grep -c "TH-05" tests/phase-31-thesis.spec.ts` returns >= 6
    - `grep -c "TH-01\\|TH-02\\|TH-03\\|TH-04\\|TH-06" tests/phase-31-thesis.spec.ts` returns 0 (those tests are Plan 02's scope)
    - File committed to git with `Feat(31):` prefix
  </done>
</task>

</tasks>

<verification>
Overall Plan 01 verification sequence (run after all tasks complete):

1. **File existence:**
   ```bash
   test -f lib/thesis-manifesto.ts && echo "manifesto OK"
   test -f tests/phase-31-thesis.spec.ts && echo "test file OK"
   ```

2. **Type check:**
   ```bash
   pnpm exec tsc --noEmit 2>&1 | grep -E "lib/thesis-manifesto\\.ts" ; echo "tsc exit: $?"
   ```
   Expected: zero errors referencing `lib/thesis-manifesto.ts`.

3. **TH-05 tests green:**
   ```bash
   pnpm exec playwright test tests/phase-31-thesis.spec.ts --reporter=list
   ```
   Expected: 6 passed, 0 failed (all TH-05 tests).

4. **Content coverage grep audit:**
   ```bash
   grep -c "text:" lib/thesis-manifesto.ts       # expect 7-9
   grep -c "size: \"anchor\"" lib/thesis-manifesto.ts  # expect >= 3
   grep -cE "pillar: \"(signal-frame|enhanced-flat|biophilia)\"" lib/thesis-manifesto.ts  # expect >= 3
   ```

5. **SIGNAL/FRAME ordering audit (manual scan):**
   ```bash
   grep -n "FRAME/SIGNAL\\|FRAME and SIGNAL\\|FRAME then SIGNAL" lib/thesis-manifesto.ts
   ```
   Expected: zero matches (unless user explicitly approved a FRAME-first exception in Task 1 decision).

6. **User review receipt:**
   Task 1 must have recorded the user's decision (either a chat approval or a note in the commit message for Task 2). The executor verifies that Task 2's commit body references the Task 1 decision.
</verification>

<success_criteria>
Plan 01 is complete when:
- [ ] User has reviewed and approved the manifesto draft (Task 1 checkpoint cleared with a recorded decision)
- [ ] `lib/thesis-manifesto.ts` exists, exports the 4 required symbols, contains 7-9 statements, at least 3 anchors, covers all three pillars, contains zero hedge words in statement text literals, passes `pnpm exec tsc --noEmit`
- [ ] `tests/phase-31-thesis.spec.ts` exists, contains 6 TH-05 content-coverage tests, and all 6 tests pass
- [ ] Both files committed with `Feat(31):` prefix
- [ ] `requirements_completed: [TH-05]` (partial — TH-05 is split across content coverage here and text-register verification via Plan 02 integration tests; this plan closes the content-file portion)
- [ ] B-01 manifesto blocker is RESOLVED in STATE.md (Plan 02 executor updates STATE.md as part of its output)
- [ ] Plan 02 is unblocked to begin Wave 2

**Plan 02 hard dependency:** Plan 02 Task 0 (forwardRef upgrade to PinnedSection) can technically run in parallel with this plan because it touches a different file, but per the --gaps-style dependency enforcement in `depends_on: ["01"]` on Plan 02's frontmatter, execution is strictly sequenced. This plan MUST complete and the user MUST approve before Plan 02 begins.
</success_criteria>

<output>
After completion, create `.planning/phases/31-thesis-section/31-01-SUMMARY.md` following the standard summary template. The summary MUST include:

1. **Decision Log** — record exactly which manifesto option the user chose in Task 1 (approve-draft-verbatim, approve-with-s2-signal-first, revise-specific-lines, or rewrite-fully) and the final 8-statement text block.
2. **File Manifest** — list `lib/thesis-manifesto.ts` and `tests/phase-31-thesis.spec.ts` with line counts.
3. **Plan 02 Handoff Notes** — confirm:
   - The manifesto constant is available for import as `@/lib/thesis-manifesto`
   - The test file exists and Plan 02 will extend it (append, not replace)
   - B-01 manifesto blocker is resolved in STATE.md (Plan 02 updates STATE.md as part of its work)
4. **Anchor Statement Index** — list the 3 anchor statements in order with their pillar attribution and default desktop anchor coordinates. This is the Plan 02 engineer's quick reference for type scale and positioning.
5. **Coverage Check** — restate the 8-point coverage audit from 31-RESEARCH.md lines 476-491 with final file values filled in.
</output>
</content>
</invoke>