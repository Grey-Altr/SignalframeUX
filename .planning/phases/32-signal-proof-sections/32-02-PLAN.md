---
phase: 32-signal-proof-sections
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - components/animation/glsl-signal.tsx
  - components/blocks/signal-section.tsx
  - app/page.tsx
  - tests/phase-32-signal-proof.spec.ts
autonomous: false
requirements:
  - SG-01
  - SG-02
  - SG-03
  - SG-04
  - SG-05
must_haves:
  truths:
    - "The SIGNAL section (#signal) has data-section='signal' and occupies between 1.45 and 1.55 times the viewport height of scroll distance on desktop 1440x900 (SG-02 150vh target with rounding tolerance)"
    - "The SIGNAL section renders a full-viewport WebGL scene at uIntensity locked to 1.0 — the generative noise field at maximum SIGNAL intensity, not the geometric lattice branch used in PROOF (SG-01)"
    - "The SIGNAL section contains zero or one text elements — grep-counted text nodes inside #signal are <= 1 (SG-03 minimal/no text)"
    - "Scroll into the SIGNAL section sets document.documentElement.style --signal-intensity to '1.0' via ScrollTrigger onEnter (SG-04 SIGNAL parameter animation), and the value persists on onLeave (no restore to lower value — SIGNAL ends at full intensity, handing off to ACQUISITION)"
    - "Scroll through SIGNAL applies a translateY parallax of 0 to -40px on the scene's container div (not the singleton canvas) — the SignalCanvas scissor/viewport follows getBoundingClientRect automatically (SG-04 scroll-driven parameter animation)"
    - "With prefers-reduced-motion: reduce active, the SIGNAL section renders a static fallback frame (no canvas, no ScrollTrigger, no parallax) — grep-counted #signal canvas is 0 under reduced motion (SG-05)"
    - "The ScrollTrigger on SIGNAL uses scrub:2 (slow atmospheric parallax, NOT scrub:1), start:'top bottom', end:'bottom top', and logs trigger.start + trigger.end to console.debug on creation (RESEARCH pitfall 1 — zero-range trap mitigation)"
    - "The 150vh height is applied via min-height: 150vh OR height: 150vh inline style on the section root so that the ScrollTrigger range at creation time is a stable 1.5 * window.innerHeight and does not collapse under fonts-loading or Lenis init"
    - "The SignalCanvas singleton now hosts exactly 3 concurrent scenes: ENTRY (GLSLHero), PROOF (ProofShader from Plan 01), and SIGNAL (GLSLSignal) — confirmed by no WebGL context limit errors in the console on desktop or on a physical iOS Safari device"
    - "No text elements 'compete' with the shader — grep audit of signal-section.tsx confirms either zero text JSX string literals or exactly one monospaced data point (SG-03 + SG-05 visual judgment)"
  artifacts:
    - path: "components/animation/glsl-signal.tsx"
      provides: "A 'use client' fork of GLSLHero with uIntensity locked to 1.0 (no CSS-var observer), no uMouse interaction, no ScrollTrigger inside the shader (parent owns the scroll), no grid lines (pure FBM noise field for atmospheric breathing). Registers with SignalCanvas singleton as the 3rd concurrent scene."
      exports: ["GLSLSignal"]
      contains: "uIntensity"
    - path: "components/blocks/signal-section.tsx"
      provides: "The SIGNAL block — 'use client', 150vh section with GLSLSignal as a child, owns the ScrollTrigger scrub:2 parallax that moves a wrapper div via translateY, sets --signal-intensity:1.0 on onEnter (persists on onLeave), renders a reduced-motion static fallback."
      exports: ["SignalSection"]
      min_lines: 80
    - path: "tests/phase-32-signal-proof.spec.ts"
      provides: "Extended with SG-01, SG-02, SG-03, SG-04, SG-05 Playwright browser tests appended to the existing Plan 01 test file (no new test file created — same pattern as Phase 31 where Plan 02 extended Plan 01's spec)."
      contains: "SG-01"
  key_links:
    - from: "components/blocks/signal-section.tsx"
      to: "components/animation/glsl-signal.tsx"
      via: "direct React import; the GLSLSignal canvas wrapper ref is passed up to the parent so the parent ScrollTrigger can mutate its transform for parallax"
      pattern: "GLSLSignal"
    - from: "components/animation/glsl-signal.tsx"
      to: "hooks/use-signal-scene.ts"
      via: "useSignalScene registration of the scene with the SignalCanvas singleton (3rd concurrent WebGL scene after ENTRY and PROOF)"
      pattern: "useSignalScene"
    - from: "app/page.tsx"
      to: "components/blocks/signal-section.tsx"
      via: "direct import replacing the current <h2>SIGNAL</h2> stub inside the #signal SFSection"
      pattern: "SignalSection"
    - from: "tests/phase-32-signal-proof.spec.ts"
      to: "components/blocks/signal-section.tsx"
      via: "Playwright browser tests (boundingBox, canvas selector, style.getPropertyValue) + source-level fs.readFileSync grep for pattern compliance"
      pattern: "signal-section"
---

<objective>
Deliver the SIGNAL homepage section — a 150vh full-viewport atmospheric parallax section running the SignalCanvas singleton's 3rd concurrent WebGL scene at maximum intensity (uIntensity: 1.0). ScrollTrigger drives a slow scrub:2 parallax (translateY 0 to -40px) on the scene wrapper div, while onEnter sets the global `--signal-intensity` CSS custom property to `1.0`. The section has zero or one text elements — pure visual/generative experience referencing Ikeda's perceptual threshold work.

**SIGNAL is the lower-risk half of Phase 32**, building directly on the patterns proven in Plan 01 (PROOF). The locked decisions:
- 150vh height (not 100vh) — atmospheric breathing section
- scrub:2 (not scrub:1 or no scrub) — slow parallax
- onEnter sets --signal-intensity: 1.0 on documentElement, persists on onLeave (correct handoff to ACQUISITION)
- No text competing with shader (zero or one monospaced data point maximum)
- Reduced-motion: static frame fallback, no animation, no parallax, no canvas

The 3rd concurrent WebGL scene lands safely because the SignalCanvas singleton scissor/viewport pattern already handles ENTRY + PROOF simultaneously after Plan 01. SIGNAL simply adds a third registered element whose bounding rect drives its own scissor region.

Purpose: SIGNAL closes the atmospheric pillar of the v1.5 homepage. The quality bar is Ikeda-level — "would someone walking past stop and look?" — not cognitive, not explanatory. Pure perception.

Output:
- `components/animation/glsl-signal.tsx` — GLSLHero-style fork with uIntensity locked, no uMouse, no grid lines, pure FBM noise field, registers 3rd concurrent scene with SignalCanvas
- `components/blocks/signal-section.tsx` — the 150vh block composing GLSLSignal with a ScrollTrigger scrub:2 parallax on a wrapper div, --signal-intensity:1.0 onEnter, reduced-motion static fallback
- `app/page.tsx` — imports and renders `<SignalSection />` inside the existing #signal SFSection, replacing the stub
- `tests/phase-32-signal-proof.spec.ts` — extended with SG-01 through SG-05 tests appended to the Plan 01 describe block
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/32-signal-proof-sections/32-CONTEXT.md
@.planning/phases/32-signal-proof-sections/32-RESEARCH.md
@.planning/phases/32-signal-proof-sections/32-VALIDATION.md
@.planning/phases/32-signal-proof-sections/32-01-PLAN.md
@components/animation/glsl-hero.tsx
@components/animation/proof-shader.tsx
@lib/signal-canvas.tsx
@hooks/use-signal-scene.ts
@app/page.tsx
@tests/phase-32-signal-proof.spec.ts
@CLAUDE.md

<interfaces>
<!-- Contracts this plan CREATES — consumed downstream by page.tsx and the test file -->

```typescript
// components/animation/glsl-signal.tsx — CREATED by Task 1
// 'use client'; fork of GLSLHero with these differences:
// 1. No uMouse uniform (no pointer interaction)
// 2. No CSS var observer (no --signal-* reading — uIntensity is locked to 1.0 at build time)
// 3. No ScrollTrigger inside the shader (parent SignalSection owns the scroll lifecycle)
// 4. FRAGMENT_SHADER omits grid lines (pure FBM noise field — cleaner atmospheric output)
// 5. Registered element is the inner containerRef; parent moves this ref's transform for parallax

export function GLSLSignal(): JSX.Element;
```

```typescript
// components/blocks/signal-section.tsx — CREATED by Task 2
// 'use client' block. Single exported component.

export function SignalSection(): JSX.Element;
```

<!-- Reference pattern: Plan 01's ProofShader structure (section-scoped observer, fallback, useSignalScene) -->
<!-- From Plan 01's components/animation/proof-shader.tsx (not yet on disk at planning time — will exist when Plan 02 executes since depends_on: ["01"]) -->

<!-- Reference pattern: SIGNAL parallax from RESEARCH.md pattern 4 -->
```typescript
const trigger = ScrollTrigger.create({
  trigger: sectionRef.current,
  start: 'top bottom',
  end: 'bottom top',
  scrub: 2,  // SLOW atmospheric scrub — NOT scrub:1
  onEnter: () => {
    document.documentElement.style.setProperty('--signal-intensity', '1.0');
  },
  onUpdate: (self) => {
    const parallaxOffset = self.progress * 40;
    if (canvasWrapRef.current) {
      canvasWrapRef.current.style.transform = `translateY(${-parallaxOffset}px)`;
    }
  },
});
console.debug('[SIGNAL ST]', 'start:', trigger.start, 'end:', trigger.end);
```

<!-- Critical: the parallax target is the SCENE WRAPPER DIV (the one registered with useSignalScene), NOT the singleton canvas -->
<!-- The SignalCanvas singleton uses getBoundingClientRect() on the registered element to drive scissor/viewport -->
<!-- Moving the registered element via translateY shifts the rect, which shifts the scissor in the next ticker frame -->
<!-- From lib/signal-canvas.tsx lines 71-82 (scissor follows getBoundingClientRect) -->
```typescript
const rect = entry.element.getBoundingClientRect();
renderer.setScissor(rect.left, canvasY, rect.width, rect.height);
renderer.setViewport(rect.left, canvasY, rect.width, rect.height);
```
</interfaces>

</context>

<acceptance_criteria>

**AC-1 (SG-02 scroll distance):** Given the homepage renders at desktop 1440x900, When the `#signal` SFSection (or its inner SignalSection root) is measured, Then its boundingBox().height / viewport.height is in the range [1.45, 1.55] — 150vh with rounding tolerance. Mobile (375x667) MAY differ due to address bar behavior — only desktop is enforced in CI.

**AC-2 (SG-01 full-viewport WebGL scene):** Given the SIGNAL section is in-viewport and prefers-reduced-motion is NOT set, When the SignalCanvas singleton is inspected, Then it has 3 registered scenes (ENTRY + PROOF + SIGNAL) AND the SIGNAL section's inner container div has been registered via `useSignalScene`. Playwright assertion: `page.locator('#signal [data-signal-scene]').count() >= 1` AND the section produces visible output (canvas is the global fixed canvas; verify via checking that the SIGNAL element rect is non-zero and has data-signal-scene attribute).

**AC-3 (SG-03 minimal or no text):** Given the SIGNAL section is rendered, When its text content is measured (innerText on `#signal`), Then the visible text content length is <= 20 characters (allows one short monospaced data point OR zero text). Playwright assertion: `innerText.trim().length <= 20`.

**AC-4 (SG-04 scroll-driven parameter animation):** Given the SIGNAL section and a Playwright browser context, When the page scrolls from just-above SIGNAL to just-below SIGNAL, Then `document.documentElement.style.getPropertyValue('--signal-intensity')` equals exactly `'1.0'` after the onEnter callback fires (scroll into view) AND equals `'1.0'` (NOT lower) after scrolling past (onLeave persists the value).

**AC-5 (SG-04 parallax translateY):** Given the SIGNAL section and a Playwright browser context, When the page scrolls through SIGNAL (from progress 0 to progress 1), Then the scene wrapper div's inline style `transform` contains a `translateY(...)` value that changes with scroll progress — specifically: at progress 0 the translateY is approximately `0px`, at progress 1 (or near it) the translateY is approximately `-40px` (±5px tolerance). Read via `window.getComputedStyle(wrapper).transform` or the inline `wrapper.style.transform` string.

**AC-6 (SG-05 reduced motion):** Given Playwright context with `reducedMotion: 'reduce'`, When the SIGNAL section is inspected, Then `#signal canvas` matches 0 elements (no canvas scene registered) AND a static fallback div is visible with non-zero width and height AND no ScrollTrigger is created (no console.debug output `[SIGNAL ST]`).

**AC-7 (zero-range trap mitigation):** Given the SignalSection ScrollTrigger.create call, When the component mounts, Then it logs `[SIGNAL ST] start: <number> end: <number>` to `console.debug` AND `start !== end` (the range is non-zero). The test MAY capture the console.debug output and assert the two values differ by approximately `1.5 * window.innerHeight` (1350px at 900px viewport). If equal: the zero-range trap is active and the plan must diagnose fonts/Lenis init order before merging.

**AC-8 (no new npm packages):** Given `package.json`, When diffed against HEAD before this plan, Then zero new dependencies or devDependencies are added. All WebGL via existing Three.js, all scroll via existing GSAP ScrollTrigger, no new animation libraries (PF-05 compliance).

**AC-9 (tsc clean):** Given all new and modified files, When `pnpm exec tsc --noEmit` runs, Then it reports zero errors in any file touched by this plan.

**AC-10 (SIGNAL/FRAME ordering):** Given all files created or modified by this plan, When their comments, JSDoc, data attributes, and UI text are inspected, Then no occurrence of `FRAME/SIGNAL`, `FRAME and SIGNAL`, or `FRAME first then SIGNAL` exists.

**AC-11 (GLSLSignal purity):** Given `components/animation/glsl-signal.tsx`, When its source is inspected, Then it contains zero `uMouse` references (no pointer uniform — SIGNAL is non-interactive), zero `--signal-intensity` MutationObserver patterns (uIntensity is locked at build time to `1.0`), zero `ScrollTrigger.create` calls (parent owns scroll lifecycle), and zero `grid` / `gridDensity` / `gridX` / `gridY` references (no grid lines — pure FBM noise field for cleaner atmospheric output).

**AC-12 (3 concurrent scenes):** Given the page is loaded with all three sections mounted (ENTRY + PROOF + SIGNAL), When `window.__sf_signal_canvas.entries.size` (or equivalent singleton introspection) is read via `page.evaluate`, Then it reports exactly 3 registered scenes. If the singleton API differs, adjust the assertion to count `[data-signal-scene]` or similar marker attributes from each registered component's container div.

**AC-13 (blessed spacing stops):** Given all spacing values in `components/blocks/signal-section.tsx`, When padding/gap/margin classes are inspected, Then every value maps to a blessed stop `{4, 8, 12, 16, 24, 32, 48, 64, 96}px` (Tailwind classes `{1,2,3,4,6,8,12,16,24}`). Layout dimensions (like 150vh section height) are exempt as structural, not spacing.

**AC-14 (Playwright SG-01..05 tests green):** Given `tests/phase-32-signal-proof.spec.ts` extended with SG tests, When `pnpm exec playwright test tests/phase-32-signal-proof.spec.ts --reporter=list` runs, Then all SG-01, SG-02, SG-03, SG-04, SG-05 automated tests pass AND all PR-01..PR-06 tests from Plan 01 still pass (no regression).

**AC-15 (physical device verification):** Given the SIGNAL section is deployed to the dev server, When it is tested on a physical iOS Safari device, Then (a) the atmospheric FBM noise field renders without WebGL context errors (ENTRY + PROOF + SIGNAL all visible on the same scroll session), (b) the 150vh parallax feels slow and breathing (not jarring), and (c) no text competes with the shader. Manual checkpoint.

</acceptance_criteria>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create GLSLSignal (GLSLHero fork, uIntensity locked, no uMouse, no grid, pure FBM)</name>
  <ac_refs>AC-2, AC-9, AC-11, AC-12</ac_refs>
  <files>
    - components/animation/glsl-signal.tsx
  </files>
  <read_first>
    - components/animation/glsl-hero.tsx (full file — structural template for useSignalScene, buildScene, ResizeObserver, useGSAP ticker)
    - components/animation/proof-shader.tsx (Plan 01 output — reference for a minimal fork of GLSLHero without pointer/observer)
    - hooks/use-signal-scene.ts (confirm the hook signature and lifecycle)
    - lib/signal-canvas.tsx (lines 60-90 — confirm 3rd concurrent scene is safe and how scissor follows the element rect)
    - .planning/phases/32-signal-proof-sections/32-RESEARCH.md (Pattern 5 SignalCanvas Singleton 3rd Scene, Pattern 4 SIGNAL Parallax)
  </read_first>
  <behavior>
    - Behavior 1: The component registers with `useSignalScene` passing its inner `containerRef` — when mounted alongside ENTRY's GLSLHero and PROOF's ProofShader, the singleton now has 3 concurrent scenes.
    - Behavior 2: The FRAGMENT_SHADER has a `uniform float uIntensity;` that the buildScene initializes to `1.0` and NEVER updates from any CSS custom property observer — it stays locked at `1.0` for the lifetime of the component.
    - Behavior 3: The FRAGMENT_SHADER contains zero grid line logic — no `gridX`, `gridY`, `uGridDensity`, `step(fract(uv.x * ...`. Pure FBM noise field output only.
    - Behavior 4: The component contains zero `uMouse` references, zero `MutationObserver` references, and zero `ScrollTrigger.create` calls.
    - Behavior 5: Under `prefers-reduced-motion: reduce`, the component returns a static fallback div (no canvas, no scene registration, no ticker).
    - Behavior 6: The inner container div carries `data-signal-scene` attribute so the parent (SignalSection) can locate it for parallax ref forwarding, AND so the AC-12 test can count registered scenes by selector.
  </behavior>
  <action>
    Create `components/animation/glsl-signal.tsx` by taking the GLSLHero structure as a template and stripping everything PROOF and ENTRY specifically need. The result is a minimal FBM-only atmospheric scene.

    **File structure:**

    ```typescript
    "use client";

    import { useRef, useEffect, useState, forwardRef, useImperativeHandle } from "react";
    import * as THREE from "three";
    import { useSignalScene } from "@/hooks/use-signal-scene";
    import { resolveColorAsThreeColor } from "@/lib/color-resolve";
    import { gsap, useGSAP } from "@/lib/gsap-core";
    import { cn } from "@/lib/utils";

    // ── WebGL availability (copy from glsl-hero.tsx) ────────────────────────

    function checkWebGL(): boolean {
      if (typeof window === "undefined") return false;
      try {
        const canvas = document.createElement("canvas");
        return !!(canvas.getContext("webgl2") || canvas.getContext("webgl"));
      } catch {
        return false;
      }
    }

    // ── Vertex shader (identical to GLSLHero pass-through) ─────────────────

    const VERTEX_SHADER = /* glsl */ `
      void main() {
        gl_Position = vec4(position, 1.0);
      }
    `;

    // ── Fragment shader — pure FBM noise field, no grid, no dither overlay
    // uIntensity is a constant 1.0 (locked at buildScene time) — no observer.

    const FRAGMENT_SHADER = /* glsl */ `
      precision highp float;

      uniform float uTime;
      uniform vec3  uColor;
      uniform vec2  uResolution;
      uniform float uIntensity;

      float hash(vec2 p) {
        return fract(sin(dot(p, vec2(127.1, 311.7))) * 43758.5453);
      }

      float noise(vec2 p) {
        vec2 i = floor(p);
        vec2 f = fract(p);
        vec2 u = f * f * (3.0 - 2.0 * f);
        return mix(
          mix(hash(i + vec2(0.0, 0.0)), hash(i + vec2(1.0, 0.0)), u.x),
          mix(hash(i + vec2(0.0, 1.0)), hash(i + vec2(1.0, 1.0)), u.x),
          u.y
        );
      }

      float fbm(vec2 p) {
        float value = 0.0;
        float amplitude = 0.5;
        float frequency = 1.0;
        for (int i = 0; i < 4; i++) {
          value += amplitude * noise(p * frequency);
          amplitude *= 0.5;
          frequency *= 2.0;
        }
        return value;
      }

      void main() {
        vec2 uv = gl_FragCoord.xy / uResolution;

        // Pure FBM noise field — atmospheric breathing, no grid overlay
        float fbmOffsetX = uTime * 0.08;
        float fbmOffsetY = uTime * 0.05;
        float n = fbm(uv * 4.0 + vec2(fbmOffsetX, fbmOffsetY));

        // uIntensity is locked to 1.0 — full noise amplitude (no lattice blend)
        float signal = n * (0.5 + uIntensity * 0.5);

        // Output — monochrome using --color-primary
        gl_FragColor = vec4(uColor * signal, 1.0);
      }
    `;

    // ── Reduced-motion static fallback ──────────────────────────────────────

    function GLSLSignalFallback() {
      return (
        <div
          data-signal-scene="static-fallback"
          className="absolute inset-0 z-0 rounded-none"
          aria-hidden="true"
          style={{ backgroundColor: "var(--color-primary)", opacity: 0.08 }}
        />
      );
    }

    // ── Component — forwardRef so parent SignalSection can drive parallax ─

    export const GLSLSignal = forwardRef<HTMLDivElement>(function GLSLSignal(_, forwardedRef) {
      const containerRef = useRef<HTMLDivElement>(null);
      const [hasWebGL] = useState(() => checkWebGL());
      const [isReducedMotion, setIsReducedMotion] = useState(() => {
        if (typeof window === "undefined") return false;
        return window.matchMedia("(prefers-reduced-motion: reduce)").matches;
      });

      // Forward the inner container ref so SignalSection can mutate its transform
      useImperativeHandle(
        forwardedRef,
        () => containerRef.current as HTMLDivElement,
        []
      );

      // Update reduced-motion state on change (accessibility)
      useEffect(() => {
        if (typeof window === "undefined") return;
        const mq = window.matchMedia("(prefers-reduced-motion: reduce)");
        const handler = () => setIsReducedMotion(mq.matches);
        mq.addEventListener("change", handler);
        return () => mq.removeEventListener("change", handler);
      }, []);

      // Uniforms ref — accessible from ticker closure
      const uniformsRef = useRef<{
        uTime:       THREE.IUniform<number>;
        uColor:      THREE.IUniform<THREE.Color>;
        uResolution: THREE.IUniform<THREE.Vector2>;
        uIntensity:  THREE.IUniform<number>;
      } | null>(null);

      // ResizeObserver for uResolution
      useEffect(() => {
        if (!hasWebGL || isReducedMotion) return;
        const container = containerRef.current;
        if (!container) return;
        const observer = new ResizeObserver(() => {
          if (!uniformsRef.current) return;
          uniformsRef.current.uResolution.value.set(
            container.clientWidth,
            container.clientHeight
          );
        });
        observer.observe(container);
        return () => observer.disconnect();
      }, [hasWebGL, isReducedMotion]);

      // buildScene — called once by useSignalScene on mount
      const buildScene = () => {
        const container = containerRef.current!;
        const camera = new THREE.OrthographicCamera(-1, 1, 1, -1, 0, 1);
        const scene = new THREE.Scene();
        const primaryColor = resolveColorAsThreeColor("--color-primary", {
          ttl: 2000,
        });

        const uniforms = {
          uTime:       { value: 0 },
          uColor:      { value: primaryColor },
          uResolution: {
            value: new THREE.Vector2(
              container.clientWidth,
              container.clientHeight
            ),
          },
          uIntensity: { value: 1.0 }, // LOCKED — never updated
        };
        uniformsRef.current = uniforms;

        const geo = new THREE.PlaneGeometry(2, 2);
        const material = new THREE.ShaderMaterial({
          vertexShader: VERTEX_SHADER,
          fragmentShader: FRAGMENT_SHADER,
          uniforms,
          transparent: true,
          depthWrite: false,
        });

        const mesh = new THREE.Mesh(geo, material);
        scene.add(mesh);
        return { scene, camera };
      };

      // Only register the scene if WebGL is available and reduced-motion is not active.
      // Passing a null-returning buildScene would break the singleton; instead conditionally
      // call the hook only in the "normal" render path by placing it before the early return.
      useSignalScene(
        containerRef as React.RefObject<HTMLElement | null>,
        hasWebGL && !isReducedMotion ? buildScene : () => ({
          scene: new THREE.Scene(),
          camera: new THREE.OrthographicCamera(-1, 1, 1, -1, 0, 1),
        })
      );

      // Ticker — accumulate uTime for FBM drift (only under normal motion)
      useGSAP(
        () => {
          if (!hasWebGL || isReducedMotion) return;

          const tickerFn = () => {
            if (!uniformsRef.current) return;
            uniformsRef.current.uTime.value += 0.016;
          };

          gsap.ticker.remove(tickerFn);
          gsap.ticker.add(tickerFn);

          return () => {
            gsap.ticker.remove(tickerFn);
          };
        },
        { scope: containerRef, dependencies: [hasWebGL, isReducedMotion] }
      );

      // Reduced-motion or no-WebGL: render static fallback
      if (!hasWebGL || isReducedMotion) {
        return <GLSLSignalFallback />;
      }

      return (
        <div
          ref={containerRef}
          data-signal-scene="glsl-signal"
          className="absolute inset-0 z-0 rounded-none"
          aria-hidden="true"
        />
      );
    });

    GLSLSignal.displayName = "GLSLSignal";
    ```

    **Notes for the executor:**
    - The `useSignalScene` unconditional call is a React rules-of-hooks concern: hooks must be called in the same order every render. We call it unconditionally but pass a no-op buildScene when the scene should not render. A cleaner alternative is to extract the WebGL path into a separate inner component that is only mounted when `hasWebGL && !isReducedMotion`, and return the fallback from the outer component. If the unconditional-hook-with-noop-buildScene pattern causes issues in signal-canvas.tsx (e.g., empty scenes producing console warnings), switch to the inner-component split pattern. This is an ADR-level decision the executor can make on-the-fly — document it in the commit message.
    - The `data-signal-scene="glsl-signal"` attribute is load-bearing for the AC-12 scene count assertion AND for SignalSection to locate the wrapper for parallax (though the `forwardRef` is the primary mechanism).
    - NO `uMouse`, NO `MutationObserver`, NO grid line helpers, NO `uScroll`, NO `uAccent`, NO Bayer dither. Pure FBM with uIntensity locked to 1.0.

    Commit message: `Feat(32): add GLSLSignal — 3rd concurrent scene, uIntensity locked 1.0, pure FBM`
  </action>
  <verify>
    <automated>test -f components/animation/glsl-signal.tsx && grep -c "uMouse" components/animation/glsl-signal.tsx && grep -c "MutationObserver" components/animation/glsl-signal.tsx && grep -c "ScrollTrigger.create" components/animation/glsl-signal.tsx && grep -c "uGridDensity\|gridX\|gridY" components/animation/glsl-signal.tsx && grep -c "uIntensity" components/animation/glsl-signal.tsx && grep -c "value: 1.0" components/animation/glsl-signal.tsx && grep -c "data-signal-scene" components/animation/glsl-signal.tsx && grep -c "useSignalScene" components/animation/glsl-signal.tsx && grep -c "forwardRef" components/animation/glsl-signal.tsx && pnpm exec tsc --noEmit 2>&1 | grep -E "components/animation/glsl-signal\.tsx" ; echo "tsc-filter-exit:$?"</automated>
  </verify>
  <boundaries>
    Do NOT modify `components/animation/glsl-hero.tsx` — it must stay intact for ENTRY. Do NOT modify `components/animation/proof-shader.tsx` — it must stay intact for PROOF. Do NOT create a second `WebGLRenderer` — use `useSignalScene`. Do NOT add any CSS custom property observer (uIntensity is locked at buildScene time). Do NOT add a ScrollTrigger inside this file — the parent SignalSection owns the scroll lifecycle. Do NOT add any text JSX in the fallback other than `aria-hidden="true"`.
  </boundaries>
  <done>
    - `components/animation/glsl-signal.tsx` exists
    - `grep -c "uMouse" components/animation/glsl-signal.tsx` is exactly 0
    - `grep -c "MutationObserver" components/animation/glsl-signal.tsx` is exactly 0
    - `grep -c "ScrollTrigger.create" components/animation/glsl-signal.tsx` is exactly 0
    - `grep -c "uGridDensity\|gridX\|gridY" components/animation/glsl-signal.tsx` is exactly 0
    - `grep -c "uIntensity" components/animation/glsl-signal.tsx` >= 3 (uniform declaration, GLSL usage, value: 1.0)
    - `grep -c "value: 1.0" components/animation/glsl-signal.tsx` >= 1 (uIntensity locked)
    - `grep -c "data-signal-scene" components/animation/glsl-signal.tsx` >= 1
    - `grep -c "useSignalScene" components/animation/glsl-signal.tsx` >= 1
    - `grep -c "forwardRef" components/animation/glsl-signal.tsx` >= 1
    - `pnpm exec tsc --noEmit` reports zero errors in this file
    - File committed with `Feat(32):` prefix
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create SignalSection block — 150vh, scrub:2 parallax, --signal-intensity:1.0 onEnter, reduced-motion fallback</name>
  <ac_refs>AC-1, AC-3, AC-4, AC-5, AC-6, AC-7, AC-9, AC-10, AC-13</ac_refs>
  <files>
    - components/blocks/signal-section.tsx
  </files>
  <read_first>
    - components/animation/glsl-signal.tsx (Task 1 output — the inner scene component)
    - components/blocks/proof-section.tsx (Plan 01 output — reference for a client block with useGSAP + ScrollTrigger cleanup)
    - components/blocks/thesis-section.tsx (reference — how a 2.5x-scrollDistance section composes pinning; SIGNAL does NOT pin so the pattern is only structurally similar)
    - components/animation/pinned-section.tsx (reference — NOT used here, SIGNAL is not pinned)
    - .planning/phases/32-signal-proof-sections/32-RESEARCH.md (Pattern 4 SIGNAL Parallax ScrollTrigger, Pitfall 1 Zero-Range Trap)
    - CLAUDE.md (SIGNAL/FRAME ordering, blessed spacing, rounded-none)
  </read_first>
  <behavior>
    - Behavior 1: The root element is a `<section>` with `data-section="signal"`, `data-anim`, inline style `{ height: "150vh", position: "relative", overflow: "hidden" }`. NO id attribute (the parent SFSection owns `id="signal"` in page.tsx).
    - Behavior 2: Inside the section, a wrapper div hosts the GLSLSignal component. The wrapper has `absolute inset-0` and receives the parallax translateY on its inline style from the ScrollTrigger onUpdate.
    - Behavior 3: The SignalSection holds a ref to the GLSLSignal forwarded element (the scene wrapper) — this is the element whose `style.transform` is mutated by the parallax, NOT the singleton canvas.
    - Behavior 4: A useGSAP hook creates a ScrollTrigger with `trigger: sectionRef.current`, `start: 'top bottom'`, `end: 'bottom top'`, `scrub: 2`, `invalidateOnRefresh: true`, `onEnter` that sets `document.documentElement.style.setProperty('--signal-intensity', '1.0')`, `onUpdate(self)` that sets `canvasWrapRef.current.style.transform = translateY(${-self.progress * 40}px)`, and NO `onLeave` (the value persists at 1.0).
    - Behavior 5: The ScrollTrigger.create call is followed by `console.debug('[SIGNAL ST]', 'start:', trigger.start, 'end:', trigger.end)` to surface the zero-range trap diagnostic (RESEARCH pitfall 1).
    - Behavior 6: Under `prefers-reduced-motion: reduce`, the hook returns early — no ScrollTrigger, no parallax, no intensity setter. The JSX still renders the GLSLSignal component which will render its own reduced-motion static fallback internally.
    - Behavior 7: The section contains zero or one text elements (SG-03). If a data point is included, it is JetBrains Mono monospaced text, positioned in a corner (e.g., bottom-right or top-right), at a small size (text-2xs or text-xs), and its content is <= 20 characters total.
    - Behavior 8: Cleanup revert pattern — `gsap.context` or `trigger.kill()` in the return of useGSAP ensures no leaks on unmount.
  </behavior>
  <action>
    Create `components/blocks/signal-section.tsx`.

    **File structure:**

    ```typescript
    "use client";

    import { useEffect, useRef } from "react";
    import { gsap, ScrollTrigger, useGSAP } from "@/lib/gsap-core";
    import { cn } from "@/lib/utils";
    import { GLSLSignal } from "@/components/animation/glsl-signal";

    /**
     * SignalSection — 150vh atmospheric parallax section.
     *
     * Full-viewport WebGL scene at maximum SIGNAL intensity (uIntensity locked 1.0),
     * slow scrub:2 parallax translating the scene wrapper -40px over the scroll range,
     * --signal-intensity:1.0 set on documentElement on onEnter and persisted on onLeave
     * (hands off to ACQUISITION at full intensity).
     *
     * No text competes with the shader (SG-03). No pin (it is not a pinned section).
     * Reduced-motion: renders GLSLSignal's static fallback, no ScrollTrigger created.
     */
    export function SignalSection() {
      const sectionRef = useRef<HTMLElement>(null);
      const canvasWrapRef = useRef<HTMLDivElement>(null);

      useGSAP(
        () => {
          const section = sectionRef.current;
          const wrap = canvasWrapRef.current;
          if (!section || !wrap) return;

          // Reduced-motion: no ScrollTrigger, no parallax, no intensity setter.
          // GLSLSignal renders its own static fallback internally.
          if (window.matchMedia("(prefers-reduced-motion: reduce)").matches) return;

          const trigger = ScrollTrigger.create({
            trigger: section,
            start: "top bottom",
            end: "bottom top",
            scrub: 2, // SLOW atmospheric scrub (RESEARCH-locked, NOT scrub:1)
            invalidateOnRefresh: true,
            onEnter: () => {
              // SIGNAL sets the GLOBAL --signal-intensity to 1.0
              // (PROOF's section-scoped value does not conflict — CSS cascade)
              document.documentElement.style.setProperty(
                "--signal-intensity",
                "1.0"
              );
            },
            onUpdate: (self) => {
              // Parallax: translate the scene wrapper -40px over full range
              // The SignalCanvas singleton scissor follows getBoundingClientRect
              // automatically on the next ticker frame.
              const parallaxOffset = self.progress * 40;
              wrap.style.transform = `translateY(${-parallaxOffset}px)`;
            },
            // No onLeave handler — --signal-intensity persists at 1.0.
            // This is the locked handoff behavior to ACQUISITION.
          });

          // Zero-range trap diagnostic (RESEARCH Pitfall 1)
          console.debug(
            "[SIGNAL ST]",
            "start:",
            trigger.start,
            "end:",
            trigger.end
          );

          return () => {
            trigger.kill();
          };
        },
        { scope: sectionRef, dependencies: [] }
      );

      return (
        <section
          ref={sectionRef as React.RefObject<HTMLElement>}
          data-section="signal"
          data-signal-root
          data-anim
          style={{ height: "150vh", position: "relative", overflow: "hidden" }}
          className="rounded-none"
        >
          {/* Scene wrapper — its transform is mutated by the parallax ScrollTrigger */}
          <div
            ref={canvasWrapRef}
            data-signal-wrap
            className="absolute inset-0 rounded-none"
            style={{ transform: "translateY(0px)" }}
          >
            <GLSLSignal />
          </div>
          {/* SG-03: no competing text. One optional monospaced data point in the
              bottom-right corner — content is <= 20 characters. Omit entirely to
              satisfy the strictest reading of "minimal or no text". */}
        </section>
      );
    }
    ```

    **Decision: ship with ZERO text inside SignalSection.** The CONTEXT.md says "Minimal or no text — pure visual/generative experience." and "Single optional monospaced data point only" — we choose the stricter "no text" interpretation. This trivially passes AC-3 (`innerText <= 20`) and matches the Ikeda reference (perceptual threshold, not informational). If Phase 34 visual language audit wants to add a `SIGNAL//FIELD` label, it can be added then.

    **Critical invariants (grep-verified):**
    - `data-section="signal"` exactly once
    - `scrub: 2` exactly once (NOT scrub:1 or scrub:true or no scrub)
    - `pin: true` exactly 0 times (SIGNAL is not pinned)
    - `onEnter` exactly 1 time, `onLeave` exactly 0 times (value persists)
    - `--signal-intensity` set to `'1.0'` exactly once
    - `console.debug("[SIGNAL ST]"` exactly 1 time (zero-range diagnostic)
    - `150vh` appears at least once (section height)
    - `parallaxOffset` or `self.progress * 40` appears exactly once
    - Zero text JSX string literals (no `<span>text</span>` inside the section)

    Commit message: `Feat(32): add SignalSection block — 150vh scrub:2 parallax, --signal-intensity:1.0 onEnter`
  </action>
  <verify>
    <automated>test -f components/blocks/signal-section.tsx && grep -c "data-section=\"signal\"" components/blocks/signal-section.tsx && grep -c "scrub: 2" components/blocks/signal-section.tsx && grep -c "pin: true" components/blocks/signal-section.tsx && grep -c "onEnter" components/blocks/signal-section.tsx && grep -c "onLeave" components/blocks/signal-section.tsx && grep -c "'--signal-intensity',\s*\"1\.0\"\|'--signal-intensity',\s*'1\.0'" components/blocks/signal-section.tsx && grep -c "console.debug(\"\[SIGNAL ST\]\|console.debug('\[SIGNAL ST\]" components/blocks/signal-section.tsx && grep -c "150vh" components/blocks/signal-section.tsx && grep -c "FRAME/SIGNAL" components/blocks/signal-section.tsx && pnpm exec tsc --noEmit 2>&1 | grep -E "components/blocks/signal-section\.tsx" ; echo "tsc-filter-exit:$?"</automated>
  </verify>
  <boundaries>
    Do NOT add `pin: true` to the ScrollTrigger. Do NOT use `scrub: 1` or `scrub: true` — only `scrub: 2`. Do NOT add `onLeave` or `onLeaveBack` callbacks (value persists). Do NOT modify the GLSLSignal component. Do NOT add any text content inside the SIGNAL section. Do NOT set `--signal-intensity` to any value other than `'1.0'`. Do NOT call `window.scrollTo` anywhere. Do NOT modify `app/page.tsx` in this task — Task 3 handles the wiring.
  </boundaries>
  <done>
    - `components/blocks/signal-section.tsx` exists
    - Grep counts match verify: scrub: 2 == 1, pin: true == 0, onEnter == 1, onLeave == 0, --signal-intensity '1.0' == 1, console.debug [SIGNAL ST] == 1, 150vh >= 1, FRAME/SIGNAL == 0
    - `pnpm exec tsc --noEmit` reports zero errors in this file
    - File committed with `Feat(32):` prefix
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire SignalSection into app/page.tsx + extend Playwright tests with SG-01..05</name>
  <ac_refs>AC-1, AC-2, AC-3, AC-4, AC-5, AC-6, AC-7, AC-12, AC-14</ac_refs>
  <files>
    - app/page.tsx
    - tests/phase-32-signal-proof.spec.ts
  </files>
  <read_first>
    - app/page.tsx (the current state — confirm the stub exists at the #signal SFSection)
    - components/blocks/signal-section.tsx (Task 2 output — the import path and name)
    - tests/phase-32-signal-proof.spec.ts (Plan 01 state — contains PR-01..06 tests; Plan 02 appends SG-01..05 inside the same describe block)
    - components/blocks/proof-section.tsx (Plan 01 — reference for the wiring pattern in app/page.tsx)
    - .planning/phases/32-signal-proof-sections/32-RESEARCH.md (lines 556-628 — exact SG test patterns)
    - .planning/phases/32-signal-proof-sections/32-VALIDATION.md (Per-Task Verification Map for the SG test names)
  </read_first>
  <behavior>
    - Behavior 1: `app/page.tsx` imports `SignalSection` from `@/components/blocks/signal-section`.
    - Behavior 2: The existing `<SFSection ... id="signal" ...>` has its stub `<h2>SIGNAL</h2>` replaced with `<SignalSection />`. The SFSection className loses `min-h-screen flex items-center justify-center` and keeps only `py-0`.
    - Behavior 3: The test file `tests/phase-32-signal-proof.spec.ts` is EXTENDED (not replaced). New SG-01..05 tests are appended inside the existing `test.describe("Phase 32: SIGNAL + PROOF Sections", ...)` block at the Plan 02 marker.
    - Behavior 4: All 11 Phase 32 tests (PR-01..06 + SG-01..05) pass in a single Playwright run.
    - Behavior 5: The SG tests follow the same pattern as PR tests from Plan 01 — browser tests with `page.goto('/')`, `page.waitForLoadState('networkidle')`, reducedMotion context for SG-05.
  </behavior>
  <action>
    **Part A — Modify `app/page.tsx`:**

    Add import near the ThesisSection/ProofSection imports:

    ```typescript
    import { SignalSection } from "@/components/blocks/signal-section";
    ```

    Replace the #signal SFSection block. BEFORE:

    ```tsx
    <SFSection
      label="SIGNAL"
      bgShift="black"
      id="signal"
      data-section="signal"
      className="py-0 min-h-screen flex items-center justify-center"
    >
      <h2 className="font-mono text-sm text-muted-foreground uppercase tracking-widest">
        SIGNAL
      </h2>
    </SFSection>
    ```

    AFTER:

    ```tsx
    <SFSection
      label="SIGNAL"
      bgShift="black"
      id="signal"
      data-section="signal"
      className="py-0"
    >
      <SignalSection />
    </SFSection>
    ```

    The same nested-id concern from Plan 01 Task 4 applies — the parent SFSection owns `id="signal"`, and the child SignalSection's inner `<section>` does NOT set an id (already handled in Task 2 — its root has no id, only `data-section="signal"` and `data-signal-root`).

    **Part B — Extend `tests/phase-32-signal-proof.spec.ts` with SG-01..05:**

    Locate the Plan 02 marker comment inside the existing describe block:
    ```
    // Plan 02 (SIGNAL section) will append SG-01 through SG-05 tests below this marker.
    // ------------------------------------------------------------------------------
    ```

    Append the following tests INSIDE the same `test.describe("Phase 32: SIGNAL + PROOF Sections", ...)` block, below the marker:

    ```typescript
      // ── SG-01: SIGNAL section has a registered WebGL scene ─────────────────

      test("SG-01: SIGNAL section full-viewport WebGL scene registered", async ({ page }) => {
        await page.setViewportSize({ width: 1440, height: 900 });
        await page.goto("/");
        await page.waitForLoadState("networkidle");
        await page.locator("#signal").scrollIntoViewIfNeeded();
        await page.waitForTimeout(400); // let ScrollTrigger onEnter fire + scene register

        // Under normal motion: GLSLSignal renders data-signal-scene="glsl-signal" inside the section
        const scene = page.locator("#signal [data-signal-scene=\"glsl-signal\"]");
        await expect(scene).toHaveCount(1);
      });

      // ── SG-02: SIGNAL section is 150vh ────────────────────────────────────

      test("SG-02: SIGNAL section scroll distance is ~150vh", async ({ page }) => {
        await page.setViewportSize({ width: 1440, height: 900 });
        await page.goto("/");
        await page.waitForLoadState("networkidle");

        // Target the inner SignalSection root (data-signal-root) — the SFSection wrapper
        // may have different intrinsic height behavior
        const signalRoot = page.locator("#signal [data-signal-root]").first();
        await expect(signalRoot).toHaveCount(1);
        const box = await signalRoot.boundingBox();
        expect(box).not.toBeNull();
        const vh = page.viewportSize()!.height;
        expect(box!.height / vh).toBeGreaterThanOrEqual(1.45);
        expect(box!.height / vh).toBeLessThanOrEqual(1.55);
      });

      // ── SG-03: SIGNAL has minimal or no text (<= 20 chars) ────────────────

      test("SG-03: SIGNAL section has minimal/no text content", async ({ page }) => {
        await page.setViewportSize({ width: 1440, height: 900 });
        await page.goto("/");
        await page.waitForLoadState("networkidle");
        await page.locator("#signal").scrollIntoViewIfNeeded();
        await page.waitForTimeout(200);

        // Read innerText of the inner SignalSection root (exclude SectionIndicator HUD etc)
        const signalRoot = page.locator("#signal [data-signal-root]").first();
        const text = (await signalRoot.innerText()).trim();
        expect(text.length).toBeLessThanOrEqual(20);
      });

      // ── SG-04: --signal-intensity set to 1.0 onEnter (documentElement) ────

      test("SG-04: scroll into SIGNAL sets --signal-intensity to 1.0", async ({ page }) => {
        await page.setViewportSize({ width: 1440, height: 900 });
        await page.goto("/");
        await page.waitForLoadState("networkidle");
        await page.locator("#signal").scrollIntoViewIfNeeded();
        await page.waitForTimeout(400); // onEnter + lerp settle

        const intensity = await page.evaluate(() => {
          return document.documentElement.style.getPropertyValue("--signal-intensity");
        });
        // The value is either set explicitly by SignalSection's onEnter or has been
        // touched by earlier sections. The assertion is exact equality to "1.0" at
        // the moment SIGNAL is in view, per the SG-04 requirement.
        expect(intensity.trim()).toBe("1.0");
      });

      // ── SG-05: reduced-motion renders static fallback, no canvas scene ────

      test("SG-05: SIGNAL reduced-motion static fallback (no canvas scene)", async ({ browser }) => {
        const ctx = await browser.newContext({ reducedMotion: "reduce" });
        const page = await ctx.newPage();
        await page.setViewportSize({ width: 1440, height: 900 });
        await page.goto("/");
        await page.waitForLoadState("networkidle");
        await page.locator("#signal").scrollIntoViewIfNeeded();
        await page.waitForTimeout(200);

        // Under reduced motion, GLSLSignal returns the static fallback with
        // data-signal-scene="static-fallback" — NOT "glsl-signal"
        const liveScene = page.locator("#signal [data-signal-scene=\"glsl-signal\"]");
        await expect(liveScene).toHaveCount(0);
        const fallback = page.locator("#signal [data-signal-scene=\"static-fallback\"]");
        await expect(fallback).toHaveCount(1);

        await ctx.close();
      });
    ```

    **Part C — Run the full Phase 32 test suite and confirm GREEN:**

    ```bash
    pnpm exec playwright test tests/phase-32-signal-proof.spec.ts --reporter=list
    ```

    Expected: all 11 tests pass (PR-01..06 from Plan 01 + SG-01..05 from this task). If any fail:

    - **SG-02 fail (wrong box height):** The SFSection parent wrapper may inflate or collapse the measured height. Diagnose by logging the outer SFSection bounding box and the inner signal-root bounding box separately. If SFSection is the problem, target `#signal` itself and measure its height — 150vh of child content should push SFSection to 150vh since it has no min-height.
    - **SG-04 fail (value not '1.0'):** Check that PROOF's onLeave restored `--signal-intensity` to 1.0 on its SECTION-scoped property (not documentElement). PROOF never touches documentElement, so the documentElement value is whatever the most recent section with a documentElement setter left it — that is SIGNAL. If the value is empty string, scroll might be too fast — add a longer waitForTimeout or scroll in multiple steps.
    - **SG-05 fail (scene count wrong):** Verify GLSLSignal's reduced-motion branch returns the fallback with `data-signal-scene="static-fallback"` not the live scene.

    **Part D — Physical iOS Safari verification (fold into Plan 01's Task 5 checkpoint OR add a separate micro-checkpoint):**

    Since Plan 01's Task 5 already requires physical iOS testing, add the SIGNAL verification steps to THAT checkpoint rather than creating a new one. Specifically, Plan 01 Task 5's "how-to-verify" should be updated in this task (as a small edit) to also check:
    1. Scroll past PROOF into SIGNAL.
    2. Confirm the atmospheric FBM noise field renders with no WebGL context errors in the device console (Remote Inspector).
    3. Confirm the parallax feels slow and "breathing" — not jarring.
    4. Confirm no text competes with the shader.
    5. Confirm 3 concurrent scenes (ENTRY + PROOF + SIGNAL) all render on the same scroll session without flicker.

    However, because Plan 01 has ALREADY been executed and its checkpoint may already be closed by the time Plan 02 runs, the cleanest approach is to bundle SIGNAL verification into this task's own checkpoint or defer the physical test to Phase 32 close (phase-level verify step). The chosen approach: include a lightweight human-verify checkpoint at the END of Plan 02 (Task 4) that covers ONLY the SIGNAL-specific verification AND the 3-scene concurrency check.

    Commit messages (one per logical change):
    - Commit 1: `Feat(32): mount SignalSection in homepage SIGNAL section (replaces stub)`
    - Commit 2: `Feat(32): add SG-01..05 Playwright tests (extends phase-32 spec)`

    Or combine into one commit if they feel tightly coupled.
  </action>
  <verify>
    <automated>grep -c "SignalSection" app/page.tsx && grep -c "<h2 className=\"font-mono text-sm text-muted-foreground uppercase tracking-widest\">\s*SIGNAL" app/page.tsx && grep -c "SG-0[1-5]" tests/phase-32-signal-proof.spec.ts && pnpm exec playwright test tests/phase-32-signal-proof.spec.ts --reporter=list 2>&1 | tee /tmp/ph32-plan02-verify.log ; grep -E "(PR-0[1-6]|SG-0[1-5]).*(passed|failed)" /tmp/ph32-plan02-verify.log ; tail -5 /tmp/ph32-plan02-verify.log</automated>
  </verify>
  <boundaries>
    Do NOT remove or rewrite any Plan 01 PR tests. Do NOT create a new test file — extend the existing Plan 01 file. Do NOT modify the ENTRY, THESIS, PROOF, INVENTORY, or ACQUISITION stubs in page.tsx. Do NOT touch SFSection component internals. Do NOT add SG tests to any other spec file.
  </boundaries>
  <done>
    - `app/page.tsx` imports `SignalSection` from `@/components/blocks/signal-section`
    - The old `<h2>SIGNAL</h2>` stub is gone from the #signal SFSection
    - `tests/phase-32-signal-proof.spec.ts` contains SG-01, SG-02, SG-03, SG-04, SG-05 test names (grep SG-0[1-5] returns >= 5)
    - `pnpm exec playwright test tests/phase-32-signal-proof.spec.ts --reporter=list` reports: 11/11 passed (6 PR + 5 SG)
    - `pnpm exec tsc --noEmit` reports zero errors in files touched by this plan
    - Changes committed with `Feat(32):` prefix
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: SIGNAL visual verification + 3-concurrent-scene check (Chrome DevTools MCP + physical iOS Safari)</name>
  <ac_refs>AC-5, AC-7, AC-15</ac_refs>
  <what-built>
    SignalSection with:
    - 150vh atmospheric parallax section
    - GLSLSignal as the 3rd concurrent WebGL scene (after ENTRY GLSLHero and PROOF ProofShader)
    - ScrollTrigger scrub:2 translateY parallax (0 to -40px over scroll range)
    - --signal-intensity set to 1.0 on onEnter, persists on onLeave
    - Reduced-motion static fallback with no canvas, no parallax
    - Zero text content competing with the shader
  </what-built>
  <how-to-verify>
    **Desktop verification (Chrome DevTools MCP at 1440x900):**
    1. Open http://localhost:3000 in Chrome DevTools MCP.
    2. Scroll down through ENTRY, THESIS, PROOF. Observe: no WebGL context errors in the console (3 scenes coexisting).
    3. Continue scrolling into SIGNAL. Observe:
       - A full-viewport atmospheric FBM noise field appears.
       - As you scroll through the 150vh range, the shader wrapper gently translates up (-40px total) — the motion is SLOW (scrub:2), not snappy.
       - No text competes with the shader.
       - The section feels like Ikeda's perceptual threshold work — "would someone walking past stop?"
    4. Open DevTools console and type: `document.documentElement.style.getPropertyValue('--signal-intensity')` — while SIGNAL is in view, the value should be exactly `1.0`.
    5. Scroll past SIGNAL into ACQUISITION. Re-check the value — it should STILL be `1.0` (persists on onLeave, not restored).
    6. Scroll back up through SIGNAL to PROOF. Move the mouse horizontally in PROOF — confirm the PROOF section-scoped --signal-intensity still works (layer separation visible). This is the cascade check — SIGNAL's documentElement setter must NOT interfere with PROOF's section-scoped setter.
    7. Check the browser console for `[SIGNAL ST] start: XXX end: YYY` — confirm start !== end AND the range is approximately 1350 (900px viewport * 1.5). If start === end: the zero-range trap is active and must be fixed before merging (force ScrollTrigger.refresh after Lenis init, or wait for fonts.ready — consult RESEARCH Pitfall 1).

    **Reduced-motion verification (macOS):**
    1. macOS: System Settings -> Accessibility -> Display -> Reduce Motion (toggle on).
    2. Reload http://localhost:3000/.
    3. Scroll to SIGNAL. Observe: no atmospheric motion, no parallax — a static fallback div (low-opacity foreground color) is visible where the canvas would be. The 150vh height is still present.
    4. Open the console — no `[SIGNAL ST]` log should appear (ScrollTrigger was not created).
    5. Toggle Reduce Motion off.

    **Physical iOS Safari verification (REQUIRED per STATE.md — Phase 32 is the mandatory testing phase):**
    1. Start dev server with LAN access if not already: `pnpm dev -- --hostname 0.0.0.0`
    2. On physical iPhone Safari, navigate to `http://<lan-ip>:3000`
    3. Scroll through the full homepage: ENTRY -> THESIS -> PROOF -> INVENTORY -> SIGNAL -> ACQUISITION.
    4. In SIGNAL specifically:
       - Confirm the atmospheric FBM noise field renders (no black screen, no WebGL error).
       - Confirm the parallax feels slow and breathing.
       - Open the Safari Remote Inspector (Mac: Safari -> Develop -> <device> -> <tab>) and check the console for:
         - 0 WebGL context limit errors
         - `[SIGNAL ST] start: ... end: ...` with a non-zero range
         - 3 scenes registered with the SignalCanvas singleton
    5. Confirm no address bar resize glitches in SIGNAL (Phase 29 autoResize:false should prevent this).
    6. Confirm no layout shift (CLS) as you scroll into SIGNAL.

    **Ikeda quality test:**
    - Would someone walking past stop and look at this section for 2 seconds? Not because they understood it, but because it looked unlike any website they'd seen.
    - If yes: approve.
    - If no: describe what fell short (too muted, too fast, too slow, too geometric, too textured, too much text, etc.) and I will remediate.
  </how-to-verify>
  <resume-signal>Type "approved" once: (1) desktop visual + console checks pass (including zero-range trap confirmation and 3-scene concurrency), (2) reduced-motion static fallback renders correctly, (3) physical iOS Safari session confirms all three scenes render without errors, (4) Ikeda quality test returns "yes". If any issue: describe it.</resume-signal>
  <done>
    - Desktop Chrome DevTools MCP verification passed (3 scenes, parallax, intensity=1.0, zero-range trap clear)
    - Reduced-motion fallback verified on macOS
    - Physical iOS Safari session completed — all 3 scenes render, no WebGL errors, no CLS
    - Ikeda quality test returns approved
    - User returns "approved"
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser (client) <-> WebGL context | Shader program runs on GPU; no user input flows to server |
| Scroll events | Lenis normalizes scroll; ScrollTrigger proxies via Lenis; no data transmission |
| CSS custom properties | --signal-intensity set on documentElement is readable by any stylesheet/script on the page (standard web platform scope) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-06 | Denial of Service | 3rd concurrent WebGL scene on Safari iOS (2-8 context limit) | mitigate | Use SignalCanvas singleton scissor/viewport (one renderer, multiple scissor regions). AC-2 + AC-12 verify 3 scenes coexist. Physical iOS test in Task 4 is the final gate. |
| T-32-07 | Denial of Service | ScrollTrigger left running after unmount | mitigate | Cleanup function in useGSAP return calls `trigger.kill()`. Pattern verified in Plan 01 for ProofSection; replicated here. |
| T-32-08 | Tampering | --signal-intensity global set by SIGNAL interfering with PROOF's section-scoped value | mitigate | CSS cascade guarantees the more specific (element-scoped) setter wins inside PROOF's subtree. Task 4 includes a manual re-check: scroll back into PROOF after SIGNAL and confirm layer separation still works. |
| T-32-09 | Denial of Service | Zero-range ScrollTrigger trap (trigger.start === trigger.end) | mitigate | console.debug logs start/end on creation (AC-7). Manual review in Task 4 checkpoint. `invalidateOnRefresh: true` forces re-measurement on Lenis resize. 150vh inline style ensures stable layout height. |

**Security domain: pure client-side visual interaction — no authentication, no user data storage, no server transmission, no sensor input (SIGNAL has no pointer/touch/gyroscope). Zero ASVS categories apply. No new packages introduced (AC-8).**
</threat_model>

<verification>
Overall Plan 02 verification sequence (run after all tasks complete):

1. **File existence:**
   ```bash
   test -f components/animation/glsl-signal.tsx
   test -f components/blocks/signal-section.tsx
   ```

2. **Type check:**
   ```bash
   pnpm exec tsc --noEmit 2>&1 | grep -E "glsl-signal\.tsx|signal-section\.tsx" ; echo "exit: $?"
   ```
   Expected: zero errors referencing either path.

3. **Playwright tests green (all 11 Phase 32 tests):**
   ```bash
   pnpm exec playwright test tests/phase-32-signal-proof.spec.ts --reporter=list
   ```
   Expected: PR-01..06 (6) + SG-01..05 (5) = 11 passed, 0 failed.

4. **GLSLSignal purity audit (AC-11):**
   ```bash
   grep -n "uMouse\|MutationObserver\|ScrollTrigger.create\|uGridDensity\|gridX\|gridY" components/animation/glsl-signal.tsx
   ```
   Expected: zero matches.

5. **ScrollTrigger configuration audit (AC-5, AC-7):**
   ```bash
   grep -n "scrub: 2\|pin: true\|onEnter\|onLeave" components/blocks/signal-section.tsx
   ```
   Expected: `scrub: 2` == 1, `pin: true` == 0, `onEnter` == 1, `onLeave` == 0.

6. **Zero-range diagnostic present (AC-7):**
   ```bash
   grep -n "console.debug.*SIGNAL ST" components/blocks/signal-section.tsx
   ```
   Expected: exactly 1 match.

7. **SIGNAL/FRAME ordering audit:**
   ```bash
   grep -rn "FRAME/SIGNAL\|FRAME and SIGNAL\|FRAME first then SIGNAL" components/blocks/signal-section.tsx components/animation/glsl-signal.tsx
   ```
   Expected: zero matches.

8. **No new npm packages:**
   ```bash
   git diff HEAD -- package.json pnpm-lock.yaml | wc -l
   ```
   Expected: 0.

9. **Bundle size sanity:**
   ```bash
   pnpm build 2>&1 | grep -E "signal|First Load JS"
   ```
   Expected: homepage First Load JS delta is small (<5 KB gzip — the signal scene shares Three.js with GLSLHero and ProofShader).

10. **Physical device verification (Task 4 checkpoint):**
    - User returns "approved" with observations recorded in summary.
</verification>

<success_criteria>
Plan 02 is complete when:
- [ ] `components/animation/glsl-signal.tsx` exists: uIntensity locked to 1.0, no uMouse, no MutationObserver, no ScrollTrigger inside, no grid lines, forwardRef for parallax ref forwarding, data-signal-scene attribute on both live and fallback variants
- [ ] `components/blocks/signal-section.tsx` exists: 150vh inline style, data-section="signal", data-signal-root, ScrollTrigger scrub:2 with onEnter setting --signal-intensity:1.0, onUpdate driving translateY parallax, NO onLeave (value persists), NO pin, console.debug zero-range diagnostic, reduced-motion early return
- [ ] `app/page.tsx` renders `<SignalSection />` inside the existing #signal SFSection (no stub `<h2>SIGNAL</h2>`)
- [ ] `tests/phase-32-signal-proof.spec.ts` extended with SG-01, SG-02, SG-03, SG-04, SG-05 tests (NOT a new file)
- [ ] `pnpm exec playwright test tests/phase-32-signal-proof.spec.ts --reporter=list` reports 11/11 passed (6 PR + 5 SG)
- [ ] `pnpm exec tsc --noEmit` reports zero errors in any file touched
- [ ] Zero new npm packages in package.json
- [ ] Task 4 human-verify checkpoint returns "approved" (desktop + reduced-motion + physical iOS Safari + Ikeda quality test + 3-scene concurrency + zero-range trap clear)
- [ ] Requirements SG-01, SG-02, SG-03, SG-04, SG-05 closed
- [ ] Phase 32 is COMPLETE — all 11 requirements (PR-01..06 + SG-01..05) closed across both plans
</success_criteria>

<output>
After completion, create `.planning/phases/32-signal-proof-sections/32-02-SUMMARY.md` following the standard summary template. The summary MUST include:

1. **Decision Log** — record the uIntensity-locked-1.0 build-time approach (no CSS var observer in GLSLSignal), scrub:2 slow parallax, 40px translateY maximum, --signal-intensity:1.0 persistence on onLeave, zero-text content policy for SG-03.
2. **File Manifest** — list all 4 created/modified files with line counts.
3. **Phase 32 Close Notes** — confirm:
   - All 11 Phase 32 requirements closed (PR-01..06 from Plan 01 + SG-01..05 from Plan 02)
   - 3 concurrent WebGL scenes (ENTRY + PROOF + SIGNAL) proven safe on desktop + physical iOS Safari
   - Zero-range ScrollTrigger trap mitigated — `[SIGNAL ST] start: X end: Y` values recorded with X !== Y
   - Bundle delta recorded (expect <5KB gzip from the new shader+block files)
   - Phase 33 (INVENTORY + ACQUISITION) is unblocked
4. **Physical Device Verification** — iOS Safari version, model, 3-scene concurrency observation, Ikeda quality test result, any UX friction notes from scrolling ENTRY->SIGNAL in a single session.
5. **CSS Custom Property Cascade Audit** — confirm that PROOF's section-scoped --signal-intensity still works correctly after scrolling back from SIGNAL (SIGNAL's documentElement setter does not leak into PROOF's subtree). Record the test steps executed.
6. **SignalCanvas Singleton State** — record the final count of registered scenes (expected: 3) and confirm no WebGL context leaks after repeated scroll in/out cycles.
</output>
