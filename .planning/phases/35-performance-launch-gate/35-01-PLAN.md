---
phase: 35-performance-launch-gate
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - .planning/ROADMAP.md
  - tests/phase-35-init.spec.ts
  - tests/phase-35-reference.spec.ts
  - tests/phase-35-bundle-gate.spec.ts
  - tests/phase-35-lcp-homepage.spec.ts
  - tests/phase-35-cls-all-routes.spec.ts
autonomous: false
requirements: [PF-01, PF-03]

must_haves:
  truths:
    - "ROADMAP.md lines 480-481 no longer reference stale 29-01/29-02 plan files"
    - "ROADMAP.md Phase 35 plan list matches the 5 PLAN files this phase produces"
    - "Vercel CLI is upgraded to 50.42.0+ and `vercel whoami` matches signalframeux team"
    - "tests/phase-35-init.spec.ts contains the Gap 2 reduced-motion h1/nav overlap test at 375x667"
    - "tests/phase-35-reference.spec.ts contains the Gap 2 reduced-motion h1/nav overlap test at 375x667"
    - "tests/phase-35-bundle-gate.spec.ts, phase-35-lcp-homepage.spec.ts, phase-35-cls-all-routes.spec.ts exist as RED stubs ready for Wave 1 assertions"
    - "Wave 0 seed tests have been run against current code and their GREEN/RED status is recorded"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Corrected Phase 35 plan list"
      contains: "35-01-PLAN.md"
    - path: "tests/phase-35-init.spec.ts"
      provides: "EDGE-2 reduced-motion h1/nav boundingBox test seed"
      contains: "emulateMedia({ reducedMotion: \"reduce\" })"
    - path: "tests/phase-35-reference.spec.ts"
      provides: "EDGE-2 reduced-motion h1/nav boundingBox test seed"
      contains: "boundingBox"
    - path: "tests/phase-35-bundle-gate.spec.ts"
      provides: "PF-01 test file stub"
      contains: "@phase35"
    - path: "tests/phase-35-lcp-homepage.spec.ts"
      provides: "PF-03 test file stub"
      contains: "largest-contentful-paint"
    - path: "tests/phase-35-cls-all-routes.spec.ts"
      provides: "PF-04 test file stub"
      contains: "layout-shift"
  key_links:
    - from: ".planning/ROADMAP.md"
      to: "35-01-PLAN.md through 35-05-PLAN.md"
      via: "Phase 35 plan list"
      pattern: "35-0[1-5]-PLAN"
---

<objective>
Wave 0 scaffolding for Phase 35 Launch Gate. Three atomic deliverables that unblock every subsequent wave:
1. Fix the stale ROADMAP.md Phase 35 plan list (lines 480-481 currently reference deleted 29-01/29-02 files) as part of the brain-wins commit that lands these PLAN files.
2. Upgrade Vercel CLI to 50.42.0+ so Wave 3 production deploy has the correct flag surface.
3. Land the mandatory Gap 2 Day-1 test (reduced-motion h1/nav boundingBox overlap at 375x667) from the Phase 34 shipped compile-back, plus RED stub files for the Wave 1a CI-blocking gates (bundle, LCP, CLS). Stubs are tagged `@phase35` and register with Playwright but may fail initially — RED-before-GREEN is the contract.

Purpose: The pre-brief at `wiki/analyses/v1.5-phase35-brief.md` §Compile-Back Contract mandates a brain-wins commit that rewrites PLAN prose AND lands Wave 0 RED test assertions BEFORE any Wave 1 execution. This plan is that commit's contents. Without it, Wave 1 agents have no RED baseline to triage against, the stale ROADMAP residue poisons planning lookups, and Wave 3 deploy may hit deprecated-flag issues.

Output: ROADMAP.md corrected, Vercel CLI upgraded + team verified, 5 test files landed (2 with the Gap 2 test body verbatim from the brief, 3 as stub skeletons that Wave 1 plan 35-02 will flesh out).
</objective>

<execution_context>
@/Users/greyaltaer/.claude/pde-os/engines/gsd/workflows/execute-plan.md
@/Users/greyaltaer/.claude/pde-os/engines/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-performance-launch-gate/35-RESEARCH.md
@.planning/phases/35-performance-launch-gate/35-VALIDATION.md
@/Users/greyaltaer/Library/Mobile Documents/iCloud~md~obsidian/Documents/second-brain/wiki/analyses/v1.5-phase35-brief.md
@tests/phase-34-visual-language-subpage.spec.ts
@playwright.config.ts

<interfaces>
Gap 2 test body (LOCKED verbatim from brief §Test Carry-Overs §Gap 2 — copy exactly, do not paraphrase):

```typescript
test("EDGE-2: reduced-motion h1 does not overlap nav at 375x667", async ({ page }) => {
  await page.emulateMedia({ reducedMotion: "reduce" });
  await page.setViewportSize({ width: 375, height: 667 });
  for (const route of ["/init", "/reference", "/system"]) {
    await page.goto(route);
    await expect(page.locator("body")).toHaveAttribute("data-nav-visible", "true");
    const h1Box = await page.locator("h1").first().boundingBox();
    const navBox = await page.locator("nav").first().boundingBox();
    expect(h1Box).not.toBeNull();
    expect(navBox).not.toBeNull();
    expect(h1Box!.y).toBeGreaterThanOrEqual(navBox!.y + navBox!.height);
  }
});
```

This test block goes into BOTH tests/phase-35-init.spec.ts AND tests/phase-35-reference.spec.ts (agents 3 and 4 own it per brief §Visual-QA Wave 1 table). It may be RED against current code — that's acceptable at Wave 0, it becomes a Wave 3 triage item if it fails.
</interfaces>
</context>

<acceptance_criteria>
**AC-1:** Given `.planning/ROADMAP.md` after this plan runs, when `sed -n '466,485p' .planning/ROADMAP.md | grep '29-0[12]-PLAN'` is executed, then it returns zero matches (stale Phase 29 residue is absent from the Phase 35 block). When `sed -n '466,485p' .planning/ROADMAP.md | grep '35-0[1-5]-PLAN'` is executed, it returns at least 5 matches corresponding to the five Phase 35 plan files. The fix is IDEMPOTENT — already landed at plan-time as part of the brain-wins commit.

**AC-2:** Given the global Vercel CLI, when `vercel --version` is executed, then the output starts with `Vercel CLI 50.42` or higher, and when `vercel whoami` is executed it returns a user/team consistent with the `team_uY4PqYooW9d80d9fTlGHAMB3` orgId recorded in `.vercel/project.json`.

**AC-3:** Given `tests/phase-35-init.spec.ts` and `tests/phase-35-reference.spec.ts` exist, when each file is grepped for the three-condition stack, then each file contains all of: `emulateMedia({ reducedMotion: "reduce" })`, `setViewportSize({ width: 375, height: 667 })`, and `boundingBox` — the non-negotiable EDGE-2 signature from the brief.

**AC-4:** Given the five Phase 35 test files exist on disk, when `npx playwright test tests/phase-35-*.spec.ts --list` is executed, then every file registers at least one test and every test name contains the `@phase35` tag so `pnpm test -g "@phase35"` picks them up.

**AC-5:** Given the Gap 2 test body, when both spec files are grepped for the exact locked phrase `reduced-motion h1 does not overlap nav at 375x667`, then both files contain it verbatim (not paraphrased).

**AC-6:** Given the `metadataBase` OPEN item and the LR-01 credits attribution OPEN item, when `.planning/phases/35-performance-launch-gate/OPEN-ITEMS.md` is read, then it lists both items with explicit "Grey to decide" status, linked back to brief §Pre-Phase Checklist.
</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Fix stale ROADMAP.md Phase 35 plan list + create OPEN-ITEMS.md</name>
  <files>.planning/ROADMAP.md, .planning/phases/35-performance-launch-gate/OPEN-ITEMS.md</files>
  <ac_refs>AC-1, AC-6</ac_refs>
  <read_first>
    .planning/ROADMAP.md (focus lines 466-485 — the entire Phase 35 block)
    /Users/greyaltaer/Library/Mobile Documents/iCloud~md~obsidian/Documents/second-brain/wiki/analyses/v1.5-phase35-brief.md (§Scope Correction, §Pre-Phase Checklist — the two OPEN items)
  </read_first>
  <boundaries>
    <!-- Do NOT modify any phase block other than Phase 35. Do NOT modify the Progress table rows. -->
    <!-- Do NOT touch ROADMAP.md content outside the Phase 35 block at lines 466-485. -->
    <!-- Do NOT touch components/blocks/entry-section.tsx lines 43-58 (VL-05 slash sibling status-quo lock). -->
  </boundaries>
  <action>
**Note:** ROADMAP.md Phase 35 block was already fixed at PLANNING TIME as part of the brain-wins commit that landed this PLAN file. The stale `29-01-PLAN` / `29-02-PLAN` references have already been replaced with the 5 real Phase 35 entries. This task is IDEMPOTENT — verify the fix holds, and take no action if it does.

1. **Verify ROADMAP.md is already fixed** (idempotent guard):
   ```bash
   grep -c "29-0[12]-PLAN" .planning/ROADMAP.md  # must return 2 — only inside Phase 29's own block, NOT Phase 35's
   grep -c "35-0[1-5]-PLAN" .planning/ROADMAP.md  # must return at least 5
   sed -n '466,485p' .planning/ROADMAP.md | grep "29-0[12]-PLAN"  # must return NO matches (stale residue is gone from Phase 35 block)
   ```
   If all three checks pass, ROADMAP is correct — skip to step 2. If any check fails, fix the Phase 35 block at lines 466-485 to contain exactly the 5 `[ ] 35-0N-PLAN.md` entries (keeping Phase 29's own `[x] 29-0N-PLAN.md` entries at lines 374-375 untouched).

2. Create `.planning/phases/35-performance-launch-gate/OPEN-ITEMS.md` with this exact structure (literal markdown, not a template):

```markdown
# Phase 35 — Open Items Requiring Grey's Decision

> Per the brief at `wiki/analyses/v1.5-phase35-brief.md` §Pre-Phase Checklist, two items are
> preserved as OPEN — the planner MUST NOT default-resolve these. They land in specific
> tasks (LR-01 credits = plan 35-04 Task X, LR-02 metadataBase = plan 35-03 Task X) that
> block on Grey's written decision before executing.

## OPEN-1: LR-01 Awwwards credits attribution

**Brief reference:** §LR-01 "Credits attribution — OPEN (Grey to decide)"

Choose ONE or BOTH:
- [ ] Culture Division (studio attribution only)
- [ ] Grey Altaer (personal attribution only)
- [ ] Both (Culture Division as studio, Grey Altaer as designer + developer)

**Brief default suggestion:** both. **Brief final word:** "this is a branding decision Grey owns outright."

**Blocks:** 35-04 Task "Author Awwwards description deck" — the credits line cannot be
written until Grey decides.

## OPEN-2: LR-02 metadataBase production URL

**Brief reference:** §LR-02 "metadataBase is the single pre-ship lock that needs a real URL"

Current best candidate: `https://signalframeux.vercel.app` (from `app/sitemap.ts:3`).

Grey confirms final v1.5 production URL before Phase 35 close commit. Default stays
as the sitemap URL unless Grey chooses a custom domain.

**Blocks:** 35-03 Task "Wire metadataBase in app/layout.tsx" — the literal URL string
that ships in production requires written confirmation.
```
  </action>
  <verify>
    <automated>! sed -n '466,485p' .planning/ROADMAP.md | grep -q '29-0[12]-PLAN' && sed -n '466,485p' .planning/ROADMAP.md | grep -c '35-0[1-5]-PLAN' | awk '{exit !($1 >= 5)}' && test -f .planning/phases/35-performance-launch-gate/OPEN-ITEMS.md && grep -q 'OPEN-1' .planning/phases/35-performance-launch-gate/OPEN-ITEMS.md && grep -q 'OPEN-2' .planning/phases/35-performance-launch-gate/OPEN-ITEMS.md</automated>
  </verify>
  <done>ROADMAP.md Phase 35 block is free of `29-0X` residue. Plan count reads `5 plans`. Five real Phase 35 plan entries are listed with requirement tags. OPEN-ITEMS.md exists with both OPEN-1 and OPEN-2 documented.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Upgrade global Vercel CLI to 50.42.0+</name>
  <ac_refs>AC-2</ac_refs>
  <what-built>Claude cannot self-upgrade a global npm package that lives in the system's Node toolchain — this is one of the rare legitimate human-action checkpoints. The CLI upgrade is a prerequisite for plan 35-05 (`vercel --prod` production deploy), so it lands early in Wave 0 rather than blocking Wave 4.</what-built>
  <how-to-verify>
Run these four commands in a shell. Do NOT proceed until `vercel --version` reports 50.42.0 or higher.

```bash
# 1. Upgrade
pnpm add -g vercel@latest
# or if pnpm global install is not configured:
npm i -g vercel@latest

# 2. Confirm version >= 50.42.0
vercel --version

# 3. Confirm team match
vercel whoami

# 4. Confirm project link still works
cat .vercel/project.json
# expected: {"projectId":"prj_gwmf7fNhZiJqQ7AYL606u3xWsMGJ","orgId":"team_uY4PqYooW9d80d9fTlGHAMB3","projectName":"signalframeux"}
```

If `vercel whoami` reports a team that doesn't match `team_uY4PqYooW9d80d9fTlGHAMB3`, run `vercel teams switch` to select the correct team. Do NOT run `vercel link` — that would overwrite the existing `.vercel/project.json`.
  </how-to-verify>
  <resume-signal>Paste the output of `vercel --version` + `vercel whoami` here, then reply "vercel ready" to continue.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Create tests/phase-35-init.spec.ts + tests/phase-35-reference.spec.ts with Gap 2 test body</name>
  <files>tests/phase-35-init.spec.ts, tests/phase-35-reference.spec.ts</files>
  <ac_refs>AC-3, AC-5</ac_refs>
  <read_first>
    /Users/greyaltaer/Library/Mobile Documents/iCloud~md~obsidian/Documents/second-brain/wiki/analyses/v1.5-phase35-brief.md (§Test Carry-Overs §Gap 2 — the locked test body)
    tests/phase-34-visual-language-subpage.spec.ts (existing Phase 34 spec — model for import style, describe structure, grep patterns)
    playwright.config.ts (confirms baseURL + chromium project + headless + SwiftShader WebGL args)
  </read_first>
  <boundaries>
    <!-- components/blocks/entry-section.tsx lines 43-58 — VL-05 slash sibling status-quo lock. This task creates test files; do not modify any entry-section.tsx code even if tests are RED against it. -->
    <!-- Do NOT add any other Phase 35 assertions here — this task only lands the Gap 2 seed. Plan 35-02 Wave 1 fleshes out the rest. -->
    <!-- Do NOT paraphrase the test body. Copy the locked block verbatim from the brief. -->
  </boundaries>
  <action>
Create two new Playwright spec files, each a minimal skeleton containing the verbatim Gap 2 locked test body.

**File 1: `tests/phase-35-init.spec.ts`**

```typescript
import { test, expect } from "@playwright/test";

/**
 * Phase 35 /init tests — Wave 0 seed.
 *
 * Contains the mandatory Gap 2 EDGE-2 test carried over from Phase 34 shipped
 * compile-back. Per pre-brief at wiki/analyses/v1.5-phase35-brief.md §Test
 * Carry-Overs §Gap 2, this test MUST land Day 1 of Wave 0 before any other
 * Phase 35 layout work. The three-condition stack (375x667 + reducedMotion +
 * boundingBox overlap) is non-negotiable.
 *
 * Wave 1 (plan 35-02) will add the full /init assertion suite to this file.
 */

test.describe("@phase35 /init", () => {
  test("EDGE-2: reduced-motion h1 does not overlap nav at 375x667", async ({ page }) => {
    await page.emulateMedia({ reducedMotion: "reduce" });
    await page.setViewportSize({ width: 375, height: 667 });
    for (const route of ["/init", "/reference", "/system"]) {
      await page.goto(route);
      await expect(page.locator("body")).toHaveAttribute("data-nav-visible", "true");
      const h1Box = await page.locator("h1").first().boundingBox();
      const navBox = await page.locator("nav").first().boundingBox();
      expect(h1Box).not.toBeNull();
      expect(navBox).not.toBeNull();
      expect(h1Box!.y).toBeGreaterThanOrEqual(navBox!.y + navBox!.height);
    }
  });
});
```

**File 2: `tests/phase-35-reference.spec.ts`** — identical describe block, same test body verbatim, only the `test.describe` label changes:

```typescript
import { test, expect } from "@playwright/test";

/**
 * Phase 35 /reference tests — Wave 0 seed.
 *
 * Contains the mandatory Gap 2 EDGE-2 test (duplicated from phase-35-init.spec.ts
 * per brief §Visual-QA Wave 1 table: agents 3 AND 4 both own this assertion).
 * Wave 1 (plan 35-02) will add the full /reference assertion suite here.
 */

test.describe("@phase35 /reference", () => {
  test("EDGE-2: reduced-motion h1 does not overlap nav at 375x667", async ({ page }) => {
    await page.emulateMedia({ reducedMotion: "reduce" });
    await page.setViewportSize({ width: 375, height: 667 });
    for (const route of ["/init", "/reference", "/system"]) {
      await page.goto(route);
      await expect(page.locator("body")).toHaveAttribute("data-nav-visible", "true");
      const h1Box = await page.locator("h1").first().boundingBox();
      const navBox = await page.locator("nav").first().boundingBox();
      expect(h1Box).not.toBeNull();
      expect(navBox).not.toBeNull();
      expect(h1Box!.y).toBeGreaterThanOrEqual(navBox!.y + navBox!.height);
    }
  });
});
```

Run the spec headless against the dev server (or against `pnpm build && pnpm start`) to record its initial GREEN or RED status. Whatever the result, log it in the git commit message so Wave 3 triage knows whether the bug is already fixed or still present:

```bash
pnpm exec playwright test tests/phase-35-init.spec.ts tests/phase-35-reference.spec.ts --reporter=list
```

If the test is RED, do NOT fix the underlying component — that's a Wave 3 triage item. The RED state is valuable signal.
  </action>
  <verify>
    <automated>grep -c "reduced-motion h1 does not overlap nav at 375x667" tests/phase-35-init.spec.ts tests/phase-35-reference.spec.ts | grep -v ":0$" | wc -l | tr -d ' ' | grep -q "^2$" && grep -l "emulateMedia.*reducedMotion.*reduce" tests/phase-35-init.spec.ts tests/phase-35-reference.spec.ts | wc -l | tr -d ' ' | grep -q "^2$" && grep -l "setViewportSize.*375.*667" tests/phase-35-init.spec.ts tests/phase-35-reference.spec.ts | wc -l | tr -d ' ' | grep -q "^2$"</automated>
  </verify>
  <done>Both files exist, both contain the exact locked test name, both contain the three-condition signature (reducedMotion + 375x667 + boundingBox). Test run status (GREEN or RED) is captured in the commit message for downstream triage.</done>
</task>

<task type="auto">
  <name>Task 4: Create CI-blocking gate stub files (bundle, LCP, CLS)</name>
  <files>tests/phase-35-bundle-gate.spec.ts, tests/phase-35-lcp-homepage.spec.ts, tests/phase-35-cls-all-routes.spec.ts</files>
  <ac_refs>AC-4</ac_refs>
  <read_first>
    /Users/greyaltaer/Library/Mobile Documents/iCloud~md~obsidian/Documents/second-brain/wiki/analyses/v1.5-phase35-brief.md (§PF-01 bundle gate, §PF-03 LCP Playwright test, §PF-04 CLS parameterized sweep)
    .planning/phases/35-performance-launch-gate/35-RESEARCH.md (§Code Examples §1 bundle measurement script pattern)
  </read_first>
  <boundaries>
    <!-- These are STUBS. The full locked assertion bodies land in plan 35-02 Wave 1. This task only creates the files so Plan 35-02 has registered test targets and so the brain-wins commit delivers everything the brief's Compile-Back Contract checklist requires. -->
    <!-- Do NOT include per-route bundle ceilings — brief §PF-01 explicitly drops those. -->
    <!-- Do NOT include ANY fix suggestions or "optimize X" language — these are measurement gates. -->
  </boundaries>
  <action>
Create three stub spec files, each containing a `test.describe` block with a single `test.skip` placeholder that references the brief section where the locked assertion lives. Plan 35-02 will remove the `.skip` and paste the locked test body.

**File 1: `tests/phase-35-bundle-gate.spec.ts`**

```typescript
import { test } from "@playwright/test";

/**
 * Phase 35 PF-01 bundle gate — Wave 0 stub.
 *
 * Locked assertion: sum gzipped shared-chunk file sizes from .next/build-manifest.json
 * (or .next/app-build-manifest.json). Two assertions:
 *   1. Total shared JS gzip < 150_000 bytes.
 *   2. Grep root entrypoint chunk list for "three", "SignalCanvas", "useSignalScene",
 *      "SignalMesh", "TokenViz", "GLSLHero", "signal-canvas" — must all be absent.
 *
 * Per brief §PF-01, per-route ceilings are explicitly dropped. The Three.js async-only
 * assertion IS the failure-mode coverage for sync-regression.
 *
 * Wave 1 (plan 35-02 Task owned by Agent 1) fleshes this out.
 */

test.describe("@phase35 PF-01 bundle gate", () => {
  test.skip("PF-01: shared JS < 150 KB gzip and Three.js async-only (Wave 1 fills)", async () => {
    // Wave 0 stub — locked assertion body lands in plan 35-02
  });
});
```

**File 2: `tests/phase-35-lcp-homepage.spec.ts`**

```typescript
import { test } from "@playwright/test";

/**
 * Phase 35 PF-03 LCP gate — Wave 0 stub.
 *
 * Locked assertion: navigate to / and measure LCP via PerformanceObserver
 * subscribing to `largest-contentful-paint` with `buffered: true`. Assert
 * lastEntry.startTime < 1000.
 *
 * STATE.md D-08: LCP element must use opacity: 0.01 (not 0) as fade-in start
 * state, else Lighthouse and PerformanceObserver cannot measure it. The
 * homepage <h1>SIGNALFRAME//UX</h1> is the LCP element; any animation
 * applied to it must start at 0.01 or use clip-path, never 0.
 *
 * Brief §PF-03 requires running against `pnpm build && pnpm start`, NOT `pnpm dev`.
 * React dev-mode double-renders inflate LCP by ~2x.
 *
 * Wave 1 (plan 35-02 Task owned by Agent 1) fleshes this out.
 */

test.describe("@phase35 PF-03 LCP homepage", () => {
  test.skip("PF-03: LCP < 1.0s on homepage (Wave 1 fills)", async () => {
    // Wave 0 stub — locked assertion body lands in plan 35-02
  });
});
```

**File 3: `tests/phase-35-cls-all-routes.spec.ts`**

```typescript
import { test } from "@playwright/test";

/**
 * Phase 35 PF-04 CLS gate — Wave 0 stub.
 *
 * Locked assertion: parameterized over all 5 routes (/, /system, /init,
 * /reference, /inventory). For each route, subscribe PerformanceObserver
 * to "layout-shift" with buffered: true, scroll to bottom then back, sum
 * entry values (skipping hadRecentInput), expect total < 0.001.
 *
 * The < 0.001 tolerance accounts for floating-point noise in the layout-shift
 * API's value computation. True zero is brittle; 0.001 is effectively zero with headroom.
 *
 * Wave 1 (plan 35-02 Task owned by Agent 2) fleshes this out.
 */

test.describe("@phase35 PF-04 CLS all routes", () => {
  test.skip("PF-04: CLS ~ 0 on all 5 routes (Wave 1 fills)", async () => {
    // Wave 0 stub — locked assertion body lands in plan 35-02
  });
});
```

Run `pnpm exec playwright test tests/phase-35-bundle-gate.spec.ts tests/phase-35-lcp-homepage.spec.ts tests/phase-35-cls-all-routes.spec.ts --list` to confirm Playwright registers all three files and the three skipped tests.
  </action>
  <verify>
    <automated>test -f tests/phase-35-bundle-gate.spec.ts && test -f tests/phase-35-lcp-homepage.spec.ts && test -f tests/phase-35-cls-all-routes.spec.ts && grep -q "@phase35 PF-01" tests/phase-35-bundle-gate.spec.ts && grep -q "@phase35 PF-03" tests/phase-35-lcp-homepage.spec.ts && grep -q "@phase35 PF-04" tests/phase-35-cls-all-routes.spec.ts && npx playwright test tests/phase-35-bundle-gate.spec.ts tests/phase-35-lcp-homepage.spec.ts tests/phase-35-cls-all-routes.spec.ts --list 2>&1 | grep -c "PF-0" | awk '{exit !($1 >= 3)}'</automated>
  </verify>
  <done>All three stub files exist, each containing a `@phase35` describe block and a `test.skip` placeholder. Playwright `--list` confirms registration. Plan 35-02 now has three anchored files to extend.</done>
</task>

</tasks>

<verification>
Wave 0 sign-off requires:
- ROADMAP.md Phase 35 block contains zero stale 29-XX references and 5 real 35-XX entries
- Vercel CLI reports >= 50.42.0 and team matches `team_uY4PqYooW9d80d9fTlGHAMB3`
- tests/phase-35-init.spec.ts + tests/phase-35-reference.spec.ts contain the verbatim Gap 2 test
- tests/phase-35-{bundle-gate,lcp-homepage,cls-all-routes}.spec.ts exist as registered stubs
- .planning/phases/35-performance-launch-gate/OPEN-ITEMS.md documents both OPEN user decisions
- All changes committed to a single brain-wins commit per brief §Compile-Back Contract
</verification>

<success_criteria>
Plan 35-01 is complete when:
1. `grep -c "29-0[12]-PLAN" .planning/ROADMAP.md` returns 0
2. `vercel --version` returns 50.42.0 or higher
3. Both Gap 2 spec files exist with the locked three-condition test body
4. All three CI-gate stub files exist with `@phase35` tag
5. OPEN-ITEMS.md exists with OPEN-1 and OPEN-2
6. Git log shows one brain-wins commit containing ROADMAP.md + tests/phase-35-*.spec.ts + OPEN-ITEMS.md
</success_criteria>

<output>
After completion, create `.planning/phases/35-performance-launch-gate/35-01-SUMMARY.md` documenting:
- Gap 2 test initial status (GREEN or RED against current code)
- Vercel CLI version before and after upgrade
- Commit SHA of the brain-wins commit
- Any deviations from the locked test body (there should be none)
</output>
