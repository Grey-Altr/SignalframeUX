---
phase: 35-performance-launch-gate
plan: 02
type: execute
wave: 1
depends_on: [35-01]
files_modified:
  - tests/phase-35-homepage.spec.ts
  - tests/phase-35-system.spec.ts
  - tests/phase-35-init.spec.ts
  - tests/phase-35-reference.spec.ts
  - tests/phase-35-inventory.spec.ts
  - tests/phase-35-bundle-gate.spec.ts
  - tests/phase-35-lcp-homepage.spec.ts
  - tests/phase-35-cls-all-routes.spec.ts
  - .planning/phases/35-performance-launch-gate/visual-qa/agent-1-homepage.md
  - .planning/phases/35-performance-launch-gate/visual-qa/agent-2-system.md
  - .planning/phases/35-performance-launch-gate/visual-qa/agent-3-init.md
  - .planning/phases/35-performance-launch-gate/visual-qa/agent-4-reference.md
  - .planning/phases/35-performance-launch-gate/visual-qa/agent-5-inventory.md
autonomous: true
requirements: [PF-01, PF-03, LR-04]

must_haves:
  truths:
    - "tests/phase-35-bundle-gate.spec.ts asserts shared JS < 150 KB gzip + Three.js async-only"
    - "tests/phase-35-lcp-homepage.spec.ts asserts LCP < 1000ms on homepage via PerformanceObserver"
    - "tests/phase-35-cls-all-routes.spec.ts asserts CLS < 0.001 across all 5 routes"
    - "Each of the 5 route spec files runs its full assertion suite at 1440x900, 768x1024, and 375x667 viewports"
    - "Every route spec file uses the tightened nav-visible initial-state pattern: waitUntil 'domcontentloaded' + timeout 500ms"
    - "Homepage spec enforces VL-05 magenta lower-bound (hero moment present) without modifying the component"
    - "Agent reports exist at .planning/phases/35-performance-launch-gate/visual-qa/agent-{1-5}-{slug}.md with BLOCK/FLAG/PASS severity tags"
    - "Every locked brain assertion from brief §Visual-QA Wave 1 table is encoded as a Playwright assertion"
  artifacts:
    - path: "tests/phase-35-homepage.spec.ts"
      provides: "Agent 1 — homepage full suite (PF-01 bundle, PF-03 LCP, LR-02 OG snapshot, HUD, GhostLabel, magenta budget, nav-reveal)"
    - path: "tests/phase-35-system.spec.ts"
      provides: "Agent 2 — /system full suite (PF-04 parameterized across all 5 routes, specimen ladder, HUD label)"
    - path: "tests/phase-35-init.spec.ts"
      provides: "Agent 3 — /init full suite extending the Gap 2 test landed in 35-01"
    - path: "tests/phase-35-reference.spec.ts"
      provides: "Agent 4 — /reference full suite extending the Gap 2 test landed in 35-01"
    - path: "tests/phase-35-inventory.spec.ts"
      provides: "Agent 5 — /inventory full suite (SP-05 nav-reveal coverage gap, 12-row breadth, ComponentDetail expand-in-place)"
  key_links:
    - from: "tests/phase-35-*.spec.ts"
      to: "pnpm test -g @phase35"
      via: "Playwright describe tags"
      pattern: "@phase35"
    - from: "tests/phase-35-bundle-gate.spec.ts"
      to: ".next/build-manifest.json"
      via: "zlib.gzipSync of shared chunks"
      pattern: "gzipSync|gzip"
---

<objective>
Land the full Wave 1 Playwright test suite — 5 route spec files and 3 CI-blocking gate files — that encodes every locked brain assertion from the pre-brief as a grep-verifiable Playwright assertion. This is the PRIMARY phase deliverable: the tests are the durable lock mechanism per `feedback_brief_handoff` memory ("tests are the only durable lock mechanism in the cdBrain -> Cursor handoff").

Purpose: Wave 1 is where the brain locks become enforced code. Phase 34's final sign-off evidence (3-of-5 UAT items needed real fixes) proves that visual QA is a test-gap detector — this plan closes those gaps preemptively by authoring the tests BEFORE running the site through chrome-devtools MCP. Every assertion is either GREEN (brain lock already held) or RED (Wave 3 triage item). There are no "measurements to take later."

Output: 8 test files, each containing locked assertion bodies from brief §§PF-01, PF-03, PF-04, LR-04, and Visual-QA Wave 1 table. 5 agent report files documenting BLOCK/FLAG/PASS status per assertion. No production code changes — this plan is test-authoring only. Any RED tests become Wave 3 triage inputs for plan 35-04.
</objective>

<execution_context>
@/Users/greyaltaer/.claude/pde-os/engines/gsd/workflows/execute-plan.md
@/Users/greyaltaer/.claude/pde-os/engines/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/35-performance-launch-gate/35-RESEARCH.md
@.planning/phases/35-performance-launch-gate/35-VALIDATION.md
@.planning/phases/35-performance-launch-gate/35-01-SUMMARY.md
@/Users/greyaltaer/Library/Mobile Documents/iCloud~md~obsidian/Documents/second-brain/wiki/analyses/v1.5-phase35-brief.md
@tests/phase-34-visual-language-subpage.spec.ts
@tests/phase-35-init.spec.ts
@tests/phase-35-reference.spec.ts
@playwright.config.ts

<interfaces>
LOCKED test bodies — copy verbatim. Do NOT paraphrase.

**§PF-01 bundle gate locked assertion pattern (brief §PF-01):**
1. Read `.next/build-manifest.json` or `.next/app-build-manifest.json`
2. Identify shared-chunk file sizes (equivalent to "First Load JS shared by all" line)
3. Gzip each chunk with `zlib.gzipSync`, sum, assert < 150_000
4. Grep root entrypoint chunk list for these substrings — ALL must be absent:
   - `three`
   - `SignalCanvas`
   - `useSignalScene`
   - `SignalMesh`
   - `TokenViz`
   - `GLSLHero`
   - `signal-canvas`

**§PF-03 LCP locked assertion body (brief §PF-03):**
```typescript
test("PF-03: LCP < 1.0s on homepage", async ({ page }) => {
  await page.goto("/");
  const lcp = await page.evaluate(() => new Promise<number>((resolve) => {
    new PerformanceObserver((list) => {
      const entries = list.getEntries();
      const last = entries[entries.length - 1];
      resolve(last.startTime);
    }).observe({ type: "largest-contentful-paint", buffered: true });
  }));
  expect(lcp).toBeLessThan(1000);
});
```
With inline comment preserved verbatim:
```
// STATE.md D-08: LCP element must use opacity: 0.01 (not 0) as fade-in start
// state, else Lighthouse and PerformanceObserver cannot measure it. The
// homepage <h1>SIGNALFRAME//UX</h1> is the LCP element; any animation
// applied to it must start at 0.01 or use clip-path, never 0.
```

**§PF-04 CLS locked assertion body (brief §PF-04):**
```typescript
for (const route of ["/", "/system", "/init", "/reference", "/inventory"]) {
  test(`PF-04: CLS ~ 0 on ${route}`, async ({ page }) => {
    await page.goto(route);
    const cls = await page.evaluate(() => new Promise<number>((resolve) => {
      let total = 0;
      new PerformanceObserver((list) => {
        for (const entry of list.getEntries()) {
          if (!(entry as any).hadRecentInput) {
            total += (entry as any).value;
          }
        }
      }).observe({ type: "layout-shift", buffered: true });
      window.scrollTo(0, document.documentElement.scrollHeight);
      setTimeout(() => {
        window.scrollTo(0, 0);
        setTimeout(() => resolve(total), 500);
      }, 500);
    }));
    expect(cls).toBeLessThan(0.001);
  });
}
```

**§LR-04 viewport iteration pattern (brief §LR-04):**
```typescript
const VIEWPORTS = [
  { width: 1440, height: 900, name: "desktop" },
  { width: 768, height: 1024, name: "tablet" },
  { width: 375, height: 667, name: "mobile" },
];

for (const vp of VIEWPORTS) {
  test.describe(`${vp.name} -- ${vp.width}x${vp.height}`, () => {
    test.use({ viewport: { width: vp.width, height: vp.height } });
    // ... assertions ...
  });
}
```

**§Gap 1 tightened nav-visible pattern (brief §Test Carry-Overs §Gap 1):**
```typescript
// Phase 35 tightened pattern — replace EVERY nav-visible initial-state assertion
await page.goto(route, { waitUntil: "domcontentloaded" });
await expect(page.locator("body")).toHaveAttribute("data-nav-visible", "false", { timeout: 500 });
```
NOT the old Phase 34 pattern (which uses default goto + timeout 2000).

**§Visual-QA Wave 1 agent slice table (brief §Visual-QA Pass Shape Wave 1):**
- Agent 1 (`/`): PF-01 bundle gate, PF-03 LCP, LR-02 OG image snapshot, HUD shape + 5-field set, GhostLabel THESIS location lock, nav-reveal contract, magenta budget (upper AND lower bound), reduced-motion, all 3 viewports
- Agent 2 (`/system`): PF-04 CLS parameterized across all 5 routes, specimen ladder readability, HUD `[SYS//TOK]` label, magenta budget, 3 viewports
- Agent 3 (`/init`): Gap 2 (already landed in 35-01), bringup-sequence voice register DOM check, HUD `[INIT//SYS]` label, 3 viewports
- Agent 4 (`/reference`): Gap 2 (already landed in 35-01), schematic register density check (row count >= threshold per category, monospaced font verification on headings), HUD `[REF//API]` label, 3 viewports
- Agent 5 (`/inventory`): SP-05 nav-reveal contract (Phase 34 coverage gap), 12-row breadth sample integrity (exactly 2 per category, GEN rows at positions 11-12), ComponentDetail fixed-overlay on homepage contrast with expand-in-place on /inventory, 3 viewports

**Phase 34 magenta audit proxy pattern (STATE.md Phase 34-01 note):**
Magenta audit is a CSS-rule count proxy (grep hits on `text-primary` or `var\\(--color-primary\\)`), capped at 5 per target file. Lower-bound hero moment is a separate scroll-driven assertion — VL-05 commit fcc811d scroll-drives signal-intensity on homepage. Do NOT modify entry-section.tsx lines 43-58; only VERIFY the slash sibling still exists.
</interfaces>
</context>

<acceptance_criteria>
**AC-1:** Given `.next/build-manifest.json` exists after `pnpm build`, when `tests/phase-35-bundle-gate.spec.ts` runs, then it computes the sum of gzipped shared-chunk sizes via `zlib.gzipSync` and asserts the total is strictly less than 150_000 bytes. When `grep -c "gzipSync" tests/phase-35-bundle-gate.spec.ts` runs, it returns at least 1.

**AC-2:** Given the same bundle-gate spec, when it greps the root entrypoint chunk list for the 7 forbidden substrings (`three`, `SignalCanvas`, `useSignalScene`, `SignalMesh`, `TokenViz`, `GLSLHero`, `signal-canvas`), then the test asserts each substring has zero matches. When `grep -c "SignalCanvas\\|useSignalScene\\|GLSLHero" tests/phase-35-bundle-gate.spec.ts` runs, it returns at least 3.

**AC-3:** Given `tests/phase-35-lcp-homepage.spec.ts` runs against `pnpm build && pnpm start`, when the test subscribes PerformanceObserver to `largest-contentful-paint` with `buffered: true` on `/`, then it asserts the resolved `startTime < 1000`. When the file is grepped for the D-08 inline comment phrase `STATE.md D-08`, it returns exactly 1 match.

**AC-4:** Given `tests/phase-35-cls-all-routes.spec.ts`, when it runs, then it parameterizes over exactly these 5 routes: `/`, `/system`, `/init`, `/reference`, `/inventory`. Each test subscribes `layout-shift` PerformanceObserver, scrolls to bottom and back with 500ms settles, sums entry values (skipping `hadRecentInput`), and asserts `total < 0.001`. When `grep -c 'route of \\["/"' tests/phase-35-cls-all-routes.spec.ts` runs, it returns at least 1.

**AC-5:** Given each of the 5 route spec files (`phase-35-homepage.spec.ts`, `phase-35-system.spec.ts`, `phase-35-init.spec.ts`, `phase-35-reference.spec.ts`, `phase-35-inventory.spec.ts`), when grepped for the viewport iteration constant `VIEWPORTS = [`, then each file contains the exact constant with `width: 1440`, `width: 768`, `width: 375`. All three values must be present in every file.

**AC-6:** Given the 4 subpage/homepage spec files (`homepage`, `system`, `init`, `reference`), when grepped for the tightened nav-visible pattern, then each file contains BOTH `waitUntil: "domcontentloaded"` AND `timeout: 500` within a nav-visible assertion block. The old `timeout: 2000` pattern must be ABSENT from all four files.

**AC-7:** Given `tests/phase-35-homepage.spec.ts`, when grepped, it contains assertions for: (a) homepage renders GhostLabel on `THESIS` section — `grep -q "data-ghost-label" tests/phase-35-homepage.spec.ts || grep -q "GhostLabel" tests/phase-35-homepage.spec.ts`; (b) InstrumentHUD mounts with `[01//ENTRY]` readout somewhere on first paint; (c) VL-05 hero magenta lower-bound (the scroll-driven hero moment still exists in `components/blocks/entry-section.tsx` lines 43-58 — this is a READ-ONLY source check, NOT a modification).

**AC-8:** Given `tests/phase-35-system.spec.ts`, when grepped, it contains the PF-04 parameterized CLS sweep per brief §Visual-QA Wave 1 table (Agent 2 owns PF-04 across all 5 routes in addition to /system-specific checks). The file references specimen-ladder assertions and the `[SYS//TOK]` HUD label.

**AC-9:** Given `tests/phase-35-init.spec.ts` (already contains Gap 2 from 35-01), when this plan extends it, then the file additionally contains: the tightened Gap 1 nav-visible pattern, a DOM check that `/init` renders the locked bringup-sequence terminal string `[OK] SYSTEM READY`, and the `[INIT//SYS]` HUD label assertion. The Gap 2 test body from 35-01 is preserved verbatim.

**AC-10:** Given `tests/phase-35-reference.spec.ts` (already contains Gap 2 from 35-01), when extended, then the file additionally contains: tightened Gap 1 nav-visible, a schematic register density check (`grep` for monospaced font class on category headings, row count >= locked threshold per category), the `[REF//API]` HUD label.

**AC-11:** Given `tests/phase-35-inventory.spec.ts`, when grepped, it contains SP-05 nav-reveal coverage for `/inventory` (the Phase 34 coverage gap called out in brief §Visual-QA Wave 1 table row 5), 12-row breadth integrity (exactly 2 per category, GEN rows at positions 11-12), and the ComponentDetail expand-in-place contrast (/inventory uses expand-in-place; homepage uses fixed-overlay — this plan only asserts the /inventory side).

**AC-12:** Given `.planning/phases/35-performance-launch-gate/visual-qa/`, when the directory is listed, then exactly 5 agent report files exist (agent-1-homepage.md through agent-5-inventory.md). Each file contains a summary table listing every test in its corresponding spec file with a BLOCK / FLAG / PASS tag. Tests that are currently RED in Wave 1 are marked BLOCK or FLAG per severity; tests that are GREEN are marked PASS.

**AC-13:** Given `components/blocks/entry-section.tsx`, when grepped after this plan runs, then lines 43-58 (the VL-05 hero slash sibling) are UNCHANGED from the current state — specifically the block containing `data-anim="hero-slash-moment"`, `mix-blend-mode: screen`, and `opacity: 0.25` remains intact. This is a status-quo-lock assertion against the plan itself.
</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Fill PF-01 bundle gate + PF-03 LCP + PF-04 CLS locked assertions</name>
  <files>tests/phase-35-bundle-gate.spec.ts, tests/phase-35-lcp-homepage.spec.ts, tests/phase-35-cls-all-routes.spec.ts</files>
  <ac_refs>AC-1, AC-2, AC-3, AC-4</ac_refs>
  <read_first>
    /Users/greyaltaer/Library/Mobile Documents/iCloud~md~obsidian/Documents/second-brain/wiki/analyses/v1.5-phase35-brief.md (§PF-01, §PF-03, §PF-04 — all three locked test bodies)
    .planning/phases/35-performance-launch-gate/35-RESEARCH.md (§Code Examples §1 — bundle measurement script)
    tests/phase-35-bundle-gate.spec.ts (Wave 0 stub from 35-01)
    tests/phase-35-lcp-homepage.spec.ts (Wave 0 stub from 35-01)
    tests/phase-35-cls-all-routes.spec.ts (Wave 0 stub from 35-01)
    .next/build-manifest.json (run `pnpm build` first if missing)
  </read_first>
  <boundaries>
    <!-- components/blocks/entry-section.tsx lines 43-58 — VL-05 slash sibling status-quo lock -->
    <!-- Do NOT add per-route bundle ceilings — brief §PF-01 explicitly drops them -->
    <!-- Do NOT add "fix" language — these are measurement gates, not remediation tasks -->
    <!-- Do NOT run the tests against `pnpm dev` when validating — LCP measurement requires `pnpm build && pnpm start` -->
    <!-- CLS tolerance is `< 0.001` — brief §PF-04 locked; do NOT change to true zero or a looser value -->
  </boundaries>
  <action>
Replace the Wave 0 stubs with the locked assertion bodies. Copy test code verbatim from the brief sections referenced in `<interfaces>`.

**File 1: `tests/phase-35-bundle-gate.spec.ts`**

Import `readFileSync` from `fs`, `gzipSync` from `zlib`, and `join` from `path`. Read `.next/build-manifest.json` (fall back to `.next/app-build-manifest.json` if App Router manifest path differs). Extract the `rootMainFiles` array (shared chunks referenced by every route). For each file path, `readFileSync` → `gzipSync(data, { level: 9 })` → sum `.length`. Assert `total < 150_000`.

Second assertion: concatenate the shared chunk file list into a single string and assert NONE of the 7 forbidden substrings appear: `three`, `SignalCanvas`, `useSignalScene`, `SignalMesh`, `TokenViz`, `GLSLHero`, `signal-canvas`. Use a loop over the substring list; assert `sharedChunkNames.includes(substring) === false` for each.

Note in test comment that per-route ceilings are intentionally absent per brief §PF-01.

Remove the `test.skip` from 35-01; the test is now live. Keep the `@phase35 PF-01` describe label.

**File 2: `tests/phase-35-lcp-homepage.spec.ts`**

Replace the stub with the verbatim locked body from brief §PF-03:

```typescript
import { test, expect } from "@playwright/test";

test.describe("@phase35 PF-03 LCP homepage", () => {
  // STATE.md D-08: LCP element must use opacity: 0.01 (not 0) as fade-in start
  // state, else Lighthouse and PerformanceObserver cannot measure it. The
  // homepage <h1>SIGNALFRAME//UX</h1> is the LCP element; any animation
  // applied to it must start at 0.01 or use clip-path, never 0.
  test("PF-03: LCP < 1.0s on homepage", async ({ page }) => {
    await page.goto("/");
    const lcp = await page.evaluate(() => new Promise<number>((resolve) => {
      new PerformanceObserver((list) => {
        const entries = list.getEntries();
        const last = entries[entries.length - 1];
        resolve(last.startTime);
      }).observe({ type: "largest-contentful-paint", buffered: true });
    }));
    expect(lcp).toBeLessThan(1000);
  });
});
```

Preserve the D-08 comment verbatim. This is the brain memory the brief requires to stay in the file.

**File 3: `tests/phase-35-cls-all-routes.spec.ts`**

Replace the stub with the verbatim locked parameterized body from brief §PF-04. The test loops over exactly `["/", "/system", "/init", "/reference", "/inventory"]`. Use `expect(cls).toBeLessThan(0.001)` — the locked tolerance.

Run all three tests against `pnpm build && pnpm start` (NOT `pnpm dev`) and record initial GREEN/RED status in the commit message. Do NOT fix any RED — that's Wave 3 triage.
  </action>
  <verify>
    <automated>grep -q "gzipSync" tests/phase-35-bundle-gate.spec.ts && grep -q "SignalCanvas" tests/phase-35-bundle-gate.spec.ts && grep -q "GLSLHero" tests/phase-35-bundle-gate.spec.ts && grep -q "PerformanceObserver" tests/phase-35-lcp-homepage.spec.ts && grep -q "STATE.md D-08" tests/phase-35-lcp-homepage.spec.ts && grep -q "largest-contentful-paint" tests/phase-35-lcp-homepage.spec.ts && grep -q "toBeLessThan(1000)" tests/phase-35-lcp-homepage.spec.ts && grep -q "layout-shift" tests/phase-35-cls-all-routes.spec.ts && grep -q "toBeLessThan(0.001)" tests/phase-35-cls-all-routes.spec.ts && grep -q '"/inventory"' tests/phase-35-cls-all-routes.spec.ts</automated>
  </verify>
  <done>All three CI-blocking gate tests contain verbatim locked assertion bodies. D-08 comment preserved in LCP test. CLS tolerance at 0.001. Test run status (GREEN or RED per route) recorded in commit message.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author 5 route spec files with viewport iteration + tightened nav-visible</name>
  <files>tests/phase-35-homepage.spec.ts, tests/phase-35-system.spec.ts, tests/phase-35-init.spec.ts, tests/phase-35-reference.spec.ts, tests/phase-35-inventory.spec.ts</files>
  <ac_refs>AC-5, AC-6, AC-7, AC-8, AC-9, AC-10, AC-11, AC-13</ac_refs>
  <behavior>
    - Test: every route spec file contains the locked VIEWPORTS constant with exactly 3 entries (1440x900, 768x1024, 375x667)
    - Test: every route spec file uses the tightened nav-visible pattern (waitUntil: "domcontentloaded" + timeout: 500)
    - Test: homepage spec enforces VL-05 hero slash sibling PRESENCE via source-read assertion (NOT modification)
    - Test: homepage spec contains `[01//ENTRY]` HUD label assertion + GhostLabel THESIS location lock + magenta budget check
    - Test: system spec contains PF-04 parameterized sweep (Agent 2 owns it per brief table) + `[SYS//TOK]` HUD label
    - Test: init spec preserves the Gap 2 EDGE-2 test from 35-01 + adds `[OK] SYSTEM READY` terminal string + `[INIT//SYS]` HUD label
    - Test: reference spec preserves Gap 2 + adds schematic register density check + `[REF//API]` HUD label
    - Test: inventory spec contains SP-05 nav-reveal (Phase 34 coverage gap) + 12-row breadth integrity + ComponentDetail expand-in-place
  </behavior>
  <read_first>
    /Users/greyaltaer/Library/Mobile Documents/iCloud~md~obsidian/Documents/second-brain/wiki/analyses/v1.5-phase35-brief.md (§Visual-QA Pass Shape Wave 1, §Test Carry-Overs §Gap 1, §LR-04)
    tests/phase-34-visual-language-subpage.spec.ts (precedent: describe structure, source-read pattern using fs.readFileSync, nav-reveal assertion style)
    .planning/phases/34-visual-language-subpage-redesign/34-VERIFICATION.md (v1.5 final state being measured against)
    .planning/STATE.md (Phase 34-01/03 notes — HUD selector `[data-section]`, terminal footer `[OK] SYSTEM READY`, GhostLabel pair lock, magenta audit proxy)
    components/blocks/entry-section.tsx (lines 43-58 — VL-05 slash sibling to assert PRESENCE, not modify)
    tests/phase-35-init.spec.ts (existing Gap 2 test from 35-01 — preserve verbatim, only ADD to the same describe block)
    tests/phase-35-reference.spec.ts (same — preserve Gap 2)
  </read_first>
  <boundaries>
    <!-- components/blocks/entry-section.tsx lines 43-58: VL-05 slash sibling STATUS-QUO LOCK. Read to assert presence; do NOT modify. -->
    <!-- tests/phase-35-init.spec.ts + tests/phase-35-reference.spec.ts: the Gap 2 test body from 35-01 must be PRESERVED VERBATIM. Extend the same describe block; do not replace or rename it. -->
    <!-- Do NOT introduce any test fixtures that modify live components. These are assertion-only specs. -->
    <!-- Do NOT write `expect(magenta_count).toBeGreaterThan(5)` — the magenta cap is 5 upper, 1 lower. Upper-bound is a grep proxy on source files (≤5 hits); lower-bound is a scroll-driven presence check. -->
    <!-- Do NOT touch the VIEWPORTS constant values. 1440x900 / 768x1024 / 375x667 are LOCKED per brief §LR-04. -->
  </boundaries>
  <action>
Each of the 5 files starts with the locked VIEWPORTS constant block (copy verbatim):

```typescript
const VIEWPORTS = [
  { width: 1440, height: 900, name: "desktop" },
  { width: 768, height: 1024, name: "tablet" },
  { width: 375, height: 667, name: "mobile" },
] as const;
```

All assertions run inside nested `test.describe` loops per brief §LR-04 — each viewport gets its own describe with `test.use({ viewport: { width: vp.width, height: vp.height } })`.

**Tightened nav-visible pattern** (brief §Test Carry-Overs §Gap 1) — wherever a nav-visible initial-state assertion is made, use EXACTLY:
```typescript
await page.goto(route, { waitUntil: "domcontentloaded" });
await expect(page.locator("body")).toHaveAttribute("data-nav-visible", "false", { timeout: 500 });
```
The old `timeout: 2000` pattern must NOT appear.

---

**File 1: `tests/phase-35-homepage.spec.ts`** (Agent 1)

Write a new file (not in 35-01). Contents:
- Import block (`test`, `expect` from `@playwright/test`; `readFileSync` from `fs`; `join` from `path`)
- Locked VIEWPORTS constant
- `test.describe("@phase35 homepage / — Agent 1", () => { ... })` outer
- Inside: VIEWPORTS loop producing 3 nested describes

Per-viewport assertions (minimum set from brief §Visual-QA Wave 1 Agent 1 slice):
1. Nav-reveal contract: navigate to `/` with tightened Gap 1 pattern → assert `data-nav-visible="false"` before scroll → scroll 600px → assert `data-nav-visible="true"` within 500ms
2. InstrumentHUD shape: grep-style source assertion using `readFileSync` on `components/layout/instrument-hud.tsx` AND runtime check that `data-section` attribute resolves to `[01//ENTRY]` on first paint
3. GhostLabel THESIS location lock: `readFileSync` on `app/page.tsx` + grep for `GhostLabel` appearance (Phase 34 test locks GhostLabel to app/page.tsx + app/system/page.tsx only — verify this pair still holds)
4. Magenta budget upper-bound: `readFileSync` `app/page.tsx` + count matches of `text-primary` OR `var\\(--color-primary\\)` — assert `<= 5`
5. **Magenta budget lower-bound — VL-05 canonical hero moment**: `readFileSync` on `components/blocks/entry-section.tsx` + assert the file contains `data-anim="hero-slash-moment"` AND `mixBlendMode: "screen"` AND the `opacity: 0.25` VL-05 baseline. This is READ-ONLY — no modification. Comment in the test: `// VL-05 status-quo lock — do not modify entry-section.tsx lines 43-58`
6. Reduced-motion smoke: `emulateMedia({ reducedMotion: "reduce" })` → navigate `/` → assert page renders without layout break
7. LR-02 OG image snapshot: `page.goto("/opengraph-image")` → assert response status 200 (the OG image is authored in plan 35-03, so this assertion is RED until 35-03 lands — acceptable; Wave 3 will re-verify after 35-03)

---

**File 2: `tests/phase-35-system.spec.ts`** (Agent 2)

New file. Same viewport iteration wrapper. Per brief §Visual-QA Wave 1 Agent 2 slice:
1. **PF-04 parameterized CLS sweep across ALL 5 routes** — Agent 2 owns this; duplicate the locked CLS body (already in `tests/phase-35-cls-all-routes.spec.ts`) here, OR import/re-export. Simplest: write the sweep verbatim in both files (the /system spec version loops over all 5 routes because brief §Visual-QA Wave 1 Agent 2 row explicitly says "owns the parameterized test for all 5 routes"). Both copies are fine; they assert the same thing.
2. /system nav-reveal (Gap 1 tightened pattern)
3. /system specimen ladder readability: assert presence of spacing specimens, type specimens, OKLCH color grid via grep-source or DOM selectors (the actual presence checks — not visual)
4. HUD label: assert `data-section` resolves to `[SYS//TOK]` on `/system`
5. /system magenta budget upper-bound: source-grep count ≤ 5 on `app/system/page.tsx` + token-tabs + specimens files

---

**File 3: `tests/phase-35-init.spec.ts`** (Agent 3)

OPEN the existing file from 35-01. Preserve the existing `@phase35 /init` describe block and EDGE-2 test verbatim. ADD:
- The VIEWPORTS constant (at module top)
- A new `test.describe("@phase35 /init — full suite", () => { ... })` block OR extend the existing describe with the viewport iteration loop. Preserve the existing EDGE-2 test unchanged.
- Tightened Gap 1 nav-visible assertion (`/init` nav starts hidden, appears on scroll)
- `[OK] SYSTEM READY` terminal footer DOM check (brief locks this exact string per STATE.md Phase 34-03 note)
- `[INIT//SYS]` HUD label assertion
- Bringup-sequence voice register: assert presence of specific locked phrases from brief/STATE (e.g., `INIT`, `HANDSHAKE`, `LINK`, `TRANSMIT`, `DEPLOY` code labels per STATE.md Phase 34-03 note)

---

**File 4: `tests/phase-35-reference.spec.ts`** (Agent 4)

OPEN the existing file from 35-01. Preserve EDGE-2 test verbatim. ADD:
- VIEWPORTS constant
- Extended describe or new describe with viewport loop
- Gap 1 tightened nav-visible
- Schematic register density check: source-read `app/reference/page.tsx` + assert monospaced font class is applied to category headings (grep for `font-mono` on heading elements) + row count per category `>= 1` (or the precise brief threshold)
- `[REF//API]` HUD label assertion

---

**File 5: `tests/phase-35-inventory.spec.ts`** (Agent 5)

New file. Per brief §Visual-QA Wave 1 Agent 5 slice — THIS IS THE PHASE 34 COVERAGE GAP:
1. SP-05 nav-reveal contract at `/inventory` — the Phase 34 spec at `tests/phase-34-visual-language-subpage.spec.ts:287-313` explicitly excluded /inventory; this test closes that gap with the tightened Gap 1 pattern
2. 12-row breadth integrity: read the component grid source + assert exactly 2 entries per category (FRM/LAY/NAV/FBK/DAT/GEN), total = 12, GEN rows at positions 11 and 12
3. ComponentDetail on /inventory: assert expand-in-place pattern (NOT fixed-overlay — that's homepage behavior per STATE.md v1.4 carry-forward)
4. /inventory magenta budget upper-bound: source-grep on the inventory page + related component files

---

After authoring all 5 files, run:
```bash
pnpm exec playwright test tests/phase-35-*.spec.ts --list
```
to confirm every spec registers tests. Then run against the dev server to capture initial GREEN/RED status. Do not fix any RED — record for Wave 3 triage.

**CRITICAL:** Before marking done, verify `components/blocks/entry-section.tsx` lines 43-58 are byte-identical to their pre-task state: `git diff components/blocks/entry-section.tsx` must return empty. The VL-05 status-quo lock is non-negotiable.
  </action>
  <verify>
    <automated>for f in tests/phase-35-homepage.spec.ts tests/phase-35-system.spec.ts tests/phase-35-init.spec.ts tests/phase-35-reference.spec.ts tests/phase-35-inventory.spec.ts; do test -f "$f" || exit 1; grep -q "VIEWPORTS" "$f" || exit 2; grep -q "width: 1440" "$f" || exit 3; grep -q "width: 768" "$f" || exit 4; grep -q "width: 375" "$f" || exit 5; done && for f in tests/phase-35-homepage.spec.ts tests/phase-35-system.spec.ts tests/phase-35-init.spec.ts tests/phase-35-reference.spec.ts; do grep -q 'waitUntil: "domcontentloaded"' "$f" || exit 6; grep -q 'timeout: 500' "$f" || exit 7; grep -q 'timeout: 2000' "$f" && exit 8; done && grep -q "hero-slash-moment" tests/phase-35-homepage.spec.ts && grep -q "01//ENTRY" tests/phase-35-homepage.spec.ts && grep -q "SYS//TOK" tests/phase-35-system.spec.ts && grep -q "OK. SYSTEM READY" tests/phase-35-init.spec.ts && grep -q "INIT//SYS" tests/phase-35-init.spec.ts && grep -q "REF//API" tests/phase-35-reference.spec.ts && grep -q "reduced-motion h1 does not overlap nav at 375x667" tests/phase-35-init.spec.ts && grep -q "reduced-motion h1 does not overlap nav at 375x667" tests/phase-35-reference.spec.ts && [ -z "$(git diff components/blocks/entry-section.tsx)" ]</automated>
  </verify>
  <done>All 5 route spec files exist with locked VIEWPORTS, tightened Gap 1 nav-visible, route-specific assertions per brief §Visual-QA Wave 1 table. Gap 2 tests from 35-01 preserved verbatim. VL-05 hero slash sibling source is unchanged (status-quo lock holds). Every test registered by Playwright `--list`.</done>
</task>

<task type="auto">
  <name>Task 3: Write Wave 1 agent reports with BLOCK/FLAG/PASS severity tags</name>
  <files>.planning/phases/35-performance-launch-gate/visual-qa/agent-1-homepage.md, .planning/phases/35-performance-launch-gate/visual-qa/agent-2-system.md, .planning/phases/35-performance-launch-gate/visual-qa/agent-3-init.md, .planning/phases/35-performance-launch-gate/visual-qa/agent-4-reference.md, .planning/phases/35-performance-launch-gate/visual-qa/agent-5-inventory.md</files>
  <ac_refs>AC-12</ac_refs>
  <read_first>
    /Users/greyaltaer/Library/Mobile Documents/iCloud~md~obsidian/Documents/second-brain/wiki/analyses/v1.5-phase35-brief.md (§Visual-QA Pass Shape — agent report format: findings severity-tagged BLOCK / FLAG / PASS)
    tests/phase-35-homepage.spec.ts (just authored in Task 2)
    tests/phase-35-system.spec.ts
    tests/phase-35-init.spec.ts
    tests/phase-35-reference.spec.ts
    tests/phase-35-inventory.spec.ts
  </read_first>
  <boundaries>
    <!-- Do NOT author report content speculatively. The BLOCK/FLAG/PASS tagging comes from RUNNING the specs and recording actual results. -->
    <!-- Do NOT fix any RED test — that's Wave 3 triage (plan 35-04). -->
    <!-- Reports are FINDINGS documents, not plans. No remediation suggestions beyond the one-line fix pointer. -->
  </boundaries>
  <action>
After Task 1 and Task 2 complete, run the full Phase 35 test suite and capture the results per spec file:

```bash
pnpm exec playwright test tests/phase-35-bundle-gate.spec.ts tests/phase-35-lcp-homepage.spec.ts tests/phase-35-cls-all-routes.spec.ts tests/phase-35-homepage.spec.ts --reporter=list 2>&1 | tee /tmp/agent-1-raw.log
pnpm exec playwright test tests/phase-35-system.spec.ts --reporter=list 2>&1 | tee /tmp/agent-2-raw.log
pnpm exec playwright test tests/phase-35-init.spec.ts --reporter=list 2>&1 | tee /tmp/agent-3-raw.log
pnpm exec playwright test tests/phase-35-reference.spec.ts --reporter=list 2>&1 | tee /tmp/agent-4-raw.log
pnpm exec playwright test tests/phase-35-inventory.spec.ts --reporter=list 2>&1 | tee /tmp/agent-5-raw.log
```

Run against `pnpm build && pnpm start` (production build — NOT `pnpm dev`). LCP and CLS measurements against dev-mode are meaningless.

For each agent report, write a markdown file with this structure:

```markdown
# Agent {N} — {slug} — Wave 1 Findings

**Date:** {ISO timestamp}
**Spec file:** `tests/phase-35-{slug}.spec.ts`
**Commit:** {sha}
**Run mode:** production (`pnpm build && pnpm start`)

## Summary

| Metric | Value |
|--------|-------|
| Tests passed | {green count} |
| Tests failed | {red count} |
| Tests skipped | 0 |
| BLOCK count | {count of blockers for Wave 3 triage} |
| FLAG count | {count of flags for Wave 3 triage} |

## Findings

| Test | Status | Severity | Notes |
|------|--------|----------|-------|
| PF-01: shared JS < 150 KB gzip | PASS / FAIL | PASS / BLOCK / FLAG | {1 line: what was measured OR what failed} |
| ... | ... | ... | ... |

## Wave 3 Triage Candidates

{List only BLOCK and FLAG findings. One bullet per finding. Format:}
- **[BLOCK/FLAG]** {test name}: {1-sentence finding} — {file:line where fix would land}

{If no Wave 3 candidates, write "No BLOCK or FLAG findings — all Wave 1 assertions GREEN."}
```

**Severity rubric** (brief §Visual-QA Pass Shape Wave 3):
- **BLOCK** — test is failing AND the brain lock it encodes is load-bearing for v1.5 launch. Goes to Wave 3 triage top.
- **FLAG** — test is failing but the brain lock is nice-to-have OR the failure is environmental (network timing, test harness quirk). Goes to Wave 3 but below BLOCK.
- **PASS** — test is green. Include in the findings table for completeness.

Populate all 5 files. If all tests are PASS, the report still gets written — the absence of findings is itself a signal.
  </action>
  <verify>
    <automated>ls .planning/phases/35-performance-launch-gate/visual-qa/agent-*.md | wc -l | tr -d ' ' | grep -q "^5$" && for f in .planning/phases/35-performance-launch-gate/visual-qa/agent-1-homepage.md .planning/phases/35-performance-launch-gate/visual-qa/agent-2-system.md .planning/phases/35-performance-launch-gate/visual-qa/agent-3-init.md .planning/phases/35-performance-launch-gate/visual-qa/agent-4-reference.md .planning/phases/35-performance-launch-gate/visual-qa/agent-5-inventory.md; do grep -q "Findings" "$f" || exit 1; grep -qE "BLOCK|FLAG|PASS" "$f" || exit 2; done</automated>
  </verify>
  <done>5 agent report files exist. Each contains actual test run results tagged BLOCK/FLAG/PASS. Wave 3 triage list is populated (or explicitly empty). No test has been modified to pass — findings reflect honest current-state signals.</done>
</task>

</tasks>

<verification>
Plan 35-02 sign-off requires:
- All 8 test files (5 route + 3 CI gate) contain locked assertion bodies from brief
- VL-05 hero slash sibling in `components/blocks/entry-section.tsx` lines 43-58 is byte-identical to pre-plan state
- `pnpm exec playwright test tests/phase-35-*.spec.ts --list` registers >= 20 tests
- 5 agent reports exist with BLOCK/FLAG/PASS tagging
- Gap 1 tightened nav-visible pattern present in 4 files; old `timeout: 2000` pattern absent
- Gap 2 test preserved verbatim in `/init` and `/reference` specs
</verification>

<success_criteria>
Plan 35-02 is complete when every brain lock in brief §Visual-QA Wave 1 is encoded as a Playwright assertion, every route spec iterates across 3 viewports, every agent report documents actual test status, and the VL-05 status-quo lock on entry-section.tsx holds.
</success_criteria>

<output>
Create `.planning/phases/35-performance-launch-gate/35-02-SUMMARY.md` summarizing:
- Total assertions authored per agent
- BLOCK and FLAG counts entering Wave 3 triage
- Confirmation that VL-05 slash sibling is unchanged
- Commit SHA
</output>
