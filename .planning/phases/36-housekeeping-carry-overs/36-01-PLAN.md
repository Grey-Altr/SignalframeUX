---
phase: 36-housekeeping-carry-overs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/phase-35-reference.spec.ts
  - tests/phase-29-infra.spec.ts
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [CO-01, CO-02, CO-04]

must_haves:
  truths:
    - "Lighthouse Best Practices = 100 on signalframeux.vercel.app (3-run worst score)"
    - "Lighthouse SEO = 100 on signalframeux.vercel.app (3-run worst score)"
    - "T-06 font-mono test reads components/blocks/api-explorer.tsx, not app/reference/page.tsx"
    - "TypeScript tsc --noEmit passes with 0 errors"
    - "REQUIREMENTS.md contains v1.6 requirement IDs (CO-01 through DIST-04)"
    - "ROADMAP.md Phase 31, 35, v1.5 milestone entries are correct"
  artifacts:
    - path: "tests/phase-35-reference.spec.ts"
      provides: "Corrected font-mono test path"
      contains: "components/blocks/api-explorer.tsx"
    - path: "tests/phase-29-infra.spec.ts"
      provides: "TypeScript strict-mode clean test"
      contains: "never[]"
    - path: ".planning/REQUIREMENTS.md"
      provides: "v1.6 requirement definitions"
      contains: "## v1.6 API-Ready"
  key_links:
    - from: "tests/phase-35-reference.spec.ts"
      to: "components/blocks/api-explorer.tsx"
      via: "readFileSync path argument"
      pattern: "readFileSync.*api-explorer\\.tsx"
---

<objective>
Fix v1.5 carry-over items (Lighthouse scores, test path, TS errors), append v1.6 requirements to REQUIREMENTS.md, and verify ROADMAP staleness.

Purpose: Establish a clean baseline before ESLint config (Plan 02) and Next.js 16 migration (Phase 37). Lighthouse 100/100 all categories is a hard gate for v1.6.
Output: Lighthouse 100/100 confirmed, test files corrected, REQUIREMENTS.md extended with v1.6 section, ROADMAP verified.
</objective>

<execution_context>
@/Users/greyaltaer/.claude/pde-os/engines/gsd/workflows/execute-plan.md
@/Users/greyaltaer/.claude/pde-os/engines/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-housekeeping-carry-overs/36-CONTEXT.md
@.planning/phases/36-housekeeping-carry-overs/36-RESEARCH.md
@.planning/phases/35-performance-launch-gate/v1.6-carry-overs.md

<interfaces>
<!-- Key file paths and patterns the executor needs -->

From tests/phase-35-reference.spec.ts:62-66 (T-06 fix target):
```typescript
// CURRENT (wrong path):
const src = readFileSync(join(process.cwd(), "app/reference/page.tsx"), "utf-8");
// CORRECT (font-mono lives in api-explorer.tsx):
const src = readFileSync(join(process.cwd(), "components/blocks/api-explorer.tsx"), "utf-8");
```

From tests/phase-29-infra.spec.ts:117 (TS strict fix):
```typescript
// CURRENT (implicit any[]):
const nonGsapRafComponents = [
// CORRECT:
const nonGsapRafComponents: never[] = [
```

From scripts/launch-gate.ts (Lighthouse runner):
```bash
pnpm tsx scripts/launch-gate.ts --url https://signalframeux.vercel.app
# Output: JSON with .worst.best-practices, .worst.seo, .fullLhr[0].audits
```
</interfaces>
</context>

<acceptance_criteria>
**AC-1:** Given the deployed site at signalframeux.vercel.app, when `pnpm tsx scripts/launch-gate.ts --url https://signalframeux.vercel.app` runs 3 times, then worst-of-3 Best Practices >= 100 and worst-of-3 SEO >= 100
**AC-2:** Given `tests/phase-35-reference.spec.ts`, when the font-mono test executes, then it reads from `components/blocks/api-explorer.tsx` (not `app/reference/page.tsx`)
**AC-3:** Given the full codebase, when `pnpm exec tsc --noEmit` runs, then exit code is 0 with 0 errors
**AC-4:** Given `.planning/REQUIREMENTS.md`, when opened, then it contains a `## v1.6 API-Ready` section with requirement IDs CO-01 through CO-04, MG-01 through MG-03, QA-01 through QA-03, LIB-01 through LIB-03, DOC-01 through DOC-04, DIST-01 through DIST-04
**AC-5:** Given `.planning/ROADMAP.md`, when inspected, then Phase 31 is `[x]`, Phase 35 is `[x]` with `5/5 plans`, and v1.5 milestone is `[x]`
</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Lighthouse diagnostic + fix loop</name>
  <files>scripts/launch-gate.ts</files>
  <read_first>
    scripts/launch-gate.ts
    .planning/phases/35-performance-launch-gate/v1.6-carry-overs.md
    .planning/phases/36-housekeeping-carry-overs/36-RESEARCH.md
  </read_first>
  <action>
1. Run `pnpm tsx scripts/launch-gate.ts --url https://signalframeux.vercel.app` to get a fresh 3-run Lighthouse audit.
2. Parse the JSON output to identify specific failing audits:
   - For Best Practices (currently 96): inspect `.fullLhr[0].categories['best-practices'].auditRefs` for entries with score < 1. Common culprits: `no-unload-listeners`, deprecated JS APIs, `uses-http2`, third-party cookies.
   - For SEO (currently 91): inspect `.fullLhr[0].categories['seo'].auditRefs` for entries with score < 1. Common culprits: missing `canonical` link on subpages, missing or too-long `meta description`, crawlable links without href, `robots.txt` issues.
3. Fix each identified failing audit in the codebase:
   - If SEO canonical is missing: verify Next.js metadata API generates canonical for all 5 routes (`/`, `/inventory`, `/system`, `/init`, `/reference`). Add `alternates: { canonical: '/route' }` to each page's metadata export if missing.
   - If meta descriptions are missing on subpages: add `description` to each page's metadata export.
   - If Best Practices has `no-unload-listeners`: grep for `beforeunload` or `unload` event listeners and remove or guard them.
   - If Best Practices has deprecated APIs: identify and replace the deprecated call.
4. Deploy via `vercel --yes` (or let Vercel auto-deploy on commit).
5. Re-run `pnpm tsx scripts/launch-gate.ts --url https://signalframeux.vercel.app` after deploy completes.
6. Iterate until both Best Practices = 100 AND SEO = 100 (worst of 3 runs). No cycle cap.

Note: The specific fixes depend on what the fresh audit reveals. The audit JSON is the source of truth — do not guess at fixes without reading the actual failing audit IDs.
  </action>
  <ac_refs>AC-1</ac_refs>
  <verify>
    <automated>pnpm tsx scripts/launch-gate.ts --url https://signalframeux.vercel.app 2>&1 | grep -E "best-practices|seo"</automated>
  </verify>
  <done>Lighthouse 3-run worst: Best Practices = 100, SEO = 100. All other categories remain at 100. JSON artifact saved.</done>
</task>

<task type="auto">
  <name>Task 2: Test fixes + TS strict + REQUIREMENTS.md + ROADMAP verify</name>
  <files>tests/phase-35-reference.spec.ts, tests/phase-29-infra.spec.ts, .planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    tests/phase-35-reference.spec.ts
    tests/phase-29-infra.spec.ts
    .planning/REQUIREMENTS.md
    .planning/ROADMAP.md
  </read_first>
  <action>
**T-06 fix (CO-02):**
In `tests/phase-35-reference.spec.ts` line 65, change:
```
const src = readFileSync(join(process.cwd(), "app/reference/page.tsx"), "utf-8");
```
to:
```
const src = readFileSync(join(process.cwd(), "components/blocks/api-explorer.tsx"), "utf-8");
```
No other changes needed — the `expect(src).toMatch(/font-mono/)` assertion remains correct since `api-explorer.tsx` contains `font-mono` at lines 166, 181, 291, 315.

**TypeScript strict fixes:**
In `tests/phase-29-infra.spec.ts` line 117, change:
```
const nonGsapRafComponents = [
```
to:
```
const nonGsapRafComponents: never[] = [
```
This fixes both TS7034 (line 117, implicit any[] variable) and TS7005 (line 147, implicit any[] on use). `never[]` is correct because the array is intentionally always empty (documentary comments only).

**REQUIREMENTS.md append (CO-04):**
Append a new section after the existing `## Future Requirements (Deferred)` section (before `## Out of Scope`). The new section:

```markdown
## v1.6 API-Ready

### Housekeeping & Carry-Overs (Phase 36)
- [ ] **CO-01**: Lighthouse Best Practices = 100 and SEO = 100 on signalframeux.vercel.app (3-run worst score)
- [ ] **CO-02**: T-06 font-mono test path fixed — reads `components/blocks/api-explorer.tsx` not `app/reference/page.tsx`
- [ ] **CO-03**: ESLint config wired with `eslint-config-next` preset, `pnpm lint` passes clean
- [ ] **CO-04**: Toolchain currency verified — Vercel CLI at 50.42.0+, ROADMAP.md stale entries corrected

### Next.js 16 Migration (Phase 37)
- [ ] **MG-01**: Next.js upgraded to ^16.x.x, app builds and starts without errors
- [ ] **MG-02**: All 18 Playwright E2E tests pass on Next 16 with zero regressions
- [ ] **MG-03**: Lighthouse scores >= Phase 36 baseline (100/100 all categories maintained)

### Test & Quality Hardening (Phase 38)
- [ ] **QA-01**: Vitest configured with coverage; SF component utility functions have unit tests
- [ ] **QA-02**: axe-core integrated into Playwright suite — every route passes WCAG AA audit
- [ ] **QA-03**: Pre-commit hooks run `pnpm lint` + `tsc --noEmit`; blocked commits fail visibly

### Library Build Pipeline (Phase 39)
- [ ] **LIB-01**: `package.json` exports field mapping SF components, tokens CSS, and utilities
- [ ] **LIB-02**: `pnpm build:lib` produces ESM + CJS output via tsup in `dist/` with `.d.ts` declarations
- [ ] **LIB-03**: Consumer importing `@signalframe/sf` does NOT bundle GSAP or Three.js (tree-shaking verified)

### API Documentation & DX (Phase 40)
- [ ] **DOC-01**: All exported SF components have JSDoc with description, props table, and usage example
- [ ] **DOC-02**: README.md covers installation, quick start, token system, and FRAME/SIGNAL model
- [ ] **DOC-03**: Storybook runs locally with stories for all SF-wrapped components
- [ ] **DOC-04**: `/reference` route reflects actual exported API (not stale internal-only components)

### Distribution & Launch Gate (Phase 41)
- [ ] **DIST-01**: `npm publish --dry-run` succeeds with correct package contents
- [ ] **DIST-02**: Fresh Next.js 16 app installs SFUX, renders 3+ SF components — builds without errors
- [ ] **DIST-03**: CHANGELOG.md and semver version strategy documented
- [ ] **DIST-04**: Lighthouse 100/100 all categories on deployed site after all v1.6 changes
```

**ROADMAP verify (CO-04):**
Verify these entries in ROADMAP.md are already correct (CONTEXT.md says they were fixed this session):
- Phase 31 line: `[x]` checkbox present
- Phase 35 line: `[x]` checkbox present, `5/5 plans` in plan count
- v1.5 milestone line: `[x]` checkbox present
- Vercel CLI: already at 50.43.0 (no action needed — just verify via `vercel --version`)

If any ROADMAP entry is still stale, fix it. Otherwise, document verification in the summary.
  </action>
  <ac_refs>AC-2, AC-3, AC-4, AC-5</ac_refs>
  <verify>
    <automated>grep "components/blocks/api-explorer.tsx" tests/phase-35-reference.spec.ts && grep "never\[\]" tests/phase-29-infra.spec.ts && pnpm exec tsc --noEmit && grep "## v1.6 API-Ready" .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>
- `tests/phase-35-reference.spec.ts` reads `components/blocks/api-explorer.tsx` for font-mono test
- `tests/phase-29-infra.spec.ts` has `never[]` annotation, `tsc --noEmit` exits 0
- `.planning/REQUIREMENTS.md` contains `## v1.6 API-Ready` section with CO-01 through DIST-04
- ROADMAP Phase 31/35/v1.5 entries verified correct
- Vercel CLI confirmed at 50.42.0+
  </done>
</task>

</tasks>

<verification>
- `pnpm tsx scripts/launch-gate.ts --url https://signalframeux.vercel.app` shows all 4 categories at 100 (worst of 3 runs)
- `pnpm exec tsc --noEmit` exits 0
- `grep "api-explorer" tests/phase-35-reference.spec.ts` matches
- `grep "v1.6 API-Ready" .planning/REQUIREMENTS.md` matches
- `vercel --version` shows 50.42.0+
</verification>

<success_criteria>
Lighthouse 100/100 all categories on deployed URL. TypeScript compiles with 0 errors. T-06 test path corrected. REQUIREMENTS.md extended with all v1.6 requirement IDs. ROADMAP verified clean.
</success_criteria>

<output>
After completion, create `.planning/phases/36-housekeeping-carry-overs/36-01-SUMMARY.md`
</output>
