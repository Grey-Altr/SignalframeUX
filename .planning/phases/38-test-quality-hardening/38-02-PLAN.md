---
phase: 38-test-quality-hardening
plan: 02
type: execute
wave: 2
depends_on: [38-01]
files_modified:
  - tests/phase-38-a11y.spec.ts
  - tests/phase-38-reduced-motion.spec.ts
autonomous: true
requirements: [QA-02]
must_haves:
  truths:
    - "Every route (/, /inventory, /system, /init, /reference) passes WCAG AA axe-core audit"
    - "prefers-reduced-motion: content is visible and positioned correctly on all routes"
    - "axe-core runs after full hydration (networkidle) to avoid false negatives"
  artifacts:
    - path: "tests/phase-38-a11y.spec.ts"
      provides: "WCAG AA accessibility audit for all 5 routes"
      contains: "AxeBuilder"
    - path: "tests/phase-38-reduced-motion.spec.ts"
      provides: "Reduced-motion content visibility assertions"
      contains: "emulateMedia"
  key_links:
    - from: "tests/phase-38-a11y.spec.ts"
      to: "@axe-core/playwright"
      via: "import AxeBuilder"
      pattern: "import.*AxeBuilder.*@axe-core/playwright"
    - from: "tests/phase-38-reduced-motion.spec.ts"
      to: "page.emulateMedia"
      via: "reducedMotion: 'reduce' before navigation"
      pattern: "emulateMedia.*reducedMotion.*reduce"
---

<objective>
Create Playwright E2E specs for WCAG AA accessibility auditing (axe-core) across all 5 routes and prefers-reduced-motion content visibility assertions.

Purpose: QA-02 requires axe-core integrated into the Playwright suite with every route passing WCAG AA audit. The reduced-motion spec verifies that all animated content remains visible and positioned when `prefers-reduced-motion: reduce` is active.

Output: Two new Playwright spec files that run as part of the existing E2E suite.
</objective>

<execution_context>
@/Users/greyaltaer/.claude/pde-os/engines/gsd/workflows/execute-plan.md
@/Users/greyaltaer/.claude/pde-os/engines/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-test-quality-hardening/38-CONTEXT.md
@.planning/phases/38-test-quality-hardening/38-RESEARCH.md

<interfaces>
<!-- Existing Playwright config and test patterns -->

From playwright.config.ts:
```typescript
export default defineConfig({
  testDir: "./tests",
  fullyParallel: false,
  workers: 1,
  use: {
    baseURL: "http://localhost:3000",
    browserName: "chromium",
    headless: true,
  },
});
```

From @axe-core/playwright (RESEARCH Pattern 3):
```typescript
import AxeBuilder from '@axe-core/playwright';
// .withTags(['wcag2a', 'wcag2aa']) — filter to WCAG AA rules
// .analyze() — returns { violations: Array<{ impact, id, description, nodes }> }
```

From signalframe-provider.tsx (RESEARCH):
```typescript
// Sets gsap.globalTimeline.timeScale(0) when prefers-reduced-motion: reduce
// GSAP is NOT attached to window.gsap — test indirectly via DOM state
```
</interfaces>
</context>

<acceptance_criteria>
**AC-1:** Given the a11y spec exists, when `pnpm exec playwright test tests/phase-38-a11y.spec.ts` is run against a live server, then all 5 routes (/, /inventory, /system, /init, /reference) pass WCAG AA audit with 0 critical/serious violations.
**AC-2:** Given the reduced-motion spec exists, when `pnpm exec playwright test tests/phase-38-reduced-motion.spec.ts` is run, then all routes have visible, on-screen content with `reducedMotion: 'reduce'` emulated.
**AC-3:** Given axe-core finds violations, when violations have impact 'critical' or 'serious', then the test fails with a descriptive error message listing violation ID, description, and affected HTML.
</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Create axe-core WCAG AA accessibility spec</name>
  <files>tests/phase-38-a11y.spec.ts</files>
  <read_first>
    - playwright.config.ts (existing config — baseURL, testDir, projects)
    - tests/phase-35-homepage.spec.ts (existing test file — follow naming and structure conventions)
    - .planning/phases/38-test-quality-hardening/38-RESEARCH.md (Pattern 3: axe-core spec, Pitfall 2: networkidle)
  </read_first>
  <action>
    Create `tests/phase-38-a11y.spec.ts`:

    1. Import `test, expect` from `@playwright/test` and `AxeBuilder` from `@axe-core/playwright`.

    2. Define ROUTES array: `['/', '/inventory', '/system', '/init', '/reference']`

    3. Wrap in `test.describe('@phase38 WCAG AA accessibility audit', () => { ... })`

    4. For each route, create a test:
       - Navigate with `waitUntil: 'networkidle'` (CRITICAL — per RESEARCH Pitfall 2, axe must run after hydration)
       - Create AxeBuilder instance: `new AxeBuilder({ page })`
       - Filter to WCAG AA tags: `.withTags(['wcag2a', 'wcag2aa'])`
       - Call `.analyze()`
       - Filter violations to critical + serious impact only
       - Assert filtered violations array has length 0
       - Error message must include: violation impact, id, description, and first 2 affected HTML nodes (for debugging)

    5. Test naming: `WCAG AA: ${route}` for each route.

    NOTE: If axe-core discovers violations on any route, the executor must FIX THE VIOLATIONS in the source components/pages, not skip or disable the rules. Common fixes: missing alt text, missing form labels, color contrast, ARIA roles. The RESEARCH notes that Phase 36 fixed `role="row"` requiring `role="cell"` children — similar issues may exist.
  </action>
  <ac_refs>AC-1, AC-3</ac_refs>
  <verify>
    <automated>pnpm exec playwright test tests/phase-38-a11y.spec.ts 2>&1 | tail -20</automated>
  </verify>
  <done>
    - tests/phase-38-a11y.spec.ts exists with 5 route tests
    - All 5 routes pass WCAG AA audit with 0 critical/serious violations
    - Descriptive error messages are present for any failures
  </done>
</task>

<task type="auto">
  <name>Task 2: Create prefers-reduced-motion Playwright spec</name>
  <files>tests/phase-38-reduced-motion.spec.ts</files>
  <read_first>
    - lib/signalframe-provider.tsx (how reduced-motion is detected and applied — look for gsap.globalTimeline.timeScale, data attributes, or CSS class toggles)
    - tests/phase-35-homepage.spec.ts (existing Playwright patterns — locator conventions, assertion style)
    - .planning/phases/38-test-quality-hardening/38-RESEARCH.md (Pattern 4: reduced-motion spec, Pitfall 3: GSAP not on window)
  </read_first>
  <action>
    Create `tests/phase-38-reduced-motion.spec.ts`:

    1. Import `test, expect` from `@playwright/test`.

    2. Define ROUTES array: `['/', '/inventory', '/system', '/init', '/reference']`

    3. Wrap in `test.describe('@phase38 prefers-reduced-motion', () => { ... })`

    4. For each route, create a test:
       - FIRST: `await page.emulateMedia({ reducedMotion: 'reduce' })` — BEFORE navigation (CRITICAL per RESEARCH Pitfall 3 — media query must be active during hydration)
       - Navigate with `waitUntil: 'networkidle'`
       - Verify content is visible and positioned:
         a. Main content area is visible (`page.locator('main').first()` or `[data-section]` elements)
         b. No element that should be visible has `opacity: 0` (animation start states should not persist)
         c. For homepage (`/`): verify h1 heading is visible and has a bounding box with x >= 0

    5. Test GSAP reduced-motion signal indirectly:
       - Do NOT use `window.gsap` (not available per RESEARCH Pitfall 3)
       - Instead: read `signalframe-provider.tsx` during read_first to find what DOM signal is emitted (data attribute on body, CSS class, etc.)
       - If provider sets a data attribute (e.g., `data-reduced-motion="true"`), assert it exists
       - If no DOM signal exists, verify behavior indirectly: check that `[data-section]` elements are visible and positioned within viewport bounds (no offscreen animation start states)

    6. For homepage specifically:
       - h1 heading must be visible (not stuck at opacity 0.01 animation start state)
       - `[data-section]` elements must have bounding boxes with y values within reasonable document flow (not pushed 1000px offscreen by animation start positions)

    7. Test naming: `reduced-motion content visible: ${route}` for each route.
  </action>
  <ac_refs>AC-2</ac_refs>
  <verify>
    <automated>pnpm exec playwright test tests/phase-38-reduced-motion.spec.ts 2>&1 | tail -20</automated>
  </verify>
  <done>
    - tests/phase-38-reduced-motion.spec.ts exists with 5 route tests
    - All routes pass with reduced-motion emulated
    - Homepage h1 is visible (not stuck at animation start state)
    - No content is positioned offscreen
  </done>
</task>

</tasks>

<verification>
- `pnpm exec playwright test tests/phase-38-a11y.spec.ts` — all 5 routes pass WCAG AA
- `pnpm exec playwright test tests/phase-38-reduced-motion.spec.ts` — all 5 routes pass reduced-motion check
- grep "AxeBuilder" tests/phase-38-a11y.spec.ts returns a match
- grep "emulateMedia" tests/phase-38-reduced-motion.spec.ts returns a match
</verification>

<success_criteria>
- axe-core WCAG AA audit passes on all 5 routes with 0 critical/serious violations
- prefers-reduced-motion content visibility confirmed on all 5 routes
- Both specs follow existing Playwright conventions (naming, structure, assertions)
- Any a11y violations discovered during testing are FIXED in source, not skipped
</success_criteria>

<output>
After completion, create `.planning/phases/38-test-quality-hardening/38-02-SUMMARY.md`
</output>
