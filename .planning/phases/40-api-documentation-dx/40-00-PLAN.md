---
phase: 40-api-documentation-dx
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/phase-40-01-jsdoc-audit.spec.ts
  - tests/phase-40-02-readme.spec.ts
  - tests/phase-40-03-storybook.spec.ts
  - tests/phase-40-04-api-docs.spec.ts
autonomous: true
requirements:
  - DOC-01
  - DOC-02
  - DOC-03
  - DOC-04

must_haves:
  truths:
    - "All 4 test spec files exist before any implementation tasks run"
    - "Each spec file is runnable (imports resolve, test structure valid) even though tests will fail RED until implementation"
  artifacts:
    - path: "tests/phase-40-01-jsdoc-audit.spec.ts"
      provides: "JSDoc coverage verification for all exported symbols"
      min_lines: 30
    - path: "tests/phase-40-02-readme.spec.ts"
      provides: "README section and MIGRATION.md verification"
      min_lines: 20
    - path: "tests/phase-40-03-storybook.spec.ts"
      provides: "Storybook config and build verification"
      min_lines: 15
    - path: "tests/phase-40-04-api-docs.spec.ts"
      provides: "API docs accuracy verification — zero @sfux/, correct importPaths"
      min_lines: 30
  key_links:
    - from: "tests/phase-40-01-jsdoc-audit.spec.ts"
      to: "lib/entry-core.ts"
      via: "fs.readFileSync export parsing"
      pattern: "entry-core"
    - from: "tests/phase-40-04-api-docs.spec.ts"
      to: "lib/api-docs.ts"
      via: "fs.readFileSync content check"
      pattern: "api-docs"
---

<objective>
Create all 4 Playwright test spec files as Wave 0 scaffolds so subsequent plans can reference them in <verify> blocks.

Purpose: Nyquist compliance requires test files to exist BEFORE the tasks they verify. This Wave 0 plan creates all spec files in RED state (tests will fail until implementation plans run). This ensures every plan's <verify> can reference a real test file.

Output: 4 Playwright spec files in tests/ directory.
</objective>

<execution_context>
@/Users/greyaltaer/.claude/pde-os/engines/gsd/workflows/execute-plan.md
@/Users/greyaltaer/.claude/pde-os/engines/gsd/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-api-documentation-dx/40-CONTEXT.md
@.planning/phases/40-api-documentation-dx/40-RESEARCH.md

@lib/entry-core.ts
@lib/entry-animation.ts
@lib/entry-webgl.ts
@lib/api-docs.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create all 4 Playwright test spec scaffolds</name>
  <files>
    tests/phase-40-01-jsdoc-audit.spec.ts,
    tests/phase-40-02-readme.spec.ts,
    tests/phase-40-03-storybook.spec.ts,
    tests/phase-40-04-api-docs.spec.ts
  </files>
  <read_first>
    lib/entry-core.ts, lib/entry-animation.ts, lib/entry-webgl.ts,
    lib/api-docs.ts (first 40 lines — interfaces only),
    tests/ (list existing test files for naming pattern reference)
  </read_first>
  <action>
    Create all 4 test spec files. Each must be syntactically valid and importable — tests will fail RED until implementation plans complete, which is correct for Wave 0.

    **tests/phase-40-01-jsdoc-audit.spec.ts:**
    - Read each entry file (entry-core.ts, entry-animation.ts, entry-webgl.ts)
    - Extract export names via regex on `export { Name }` lines
    - For `export * from` lines, resolve the source file and extract its named exports
    - For each export name, read the source file and verify a `/**` JSDoc block exists before the export declaration
    - Verify each JSDoc block contains an `@example` tag
    - Exclude type-only exports (TextVariant, SignalframeUXConfig, UseSignalframeReturn, SFStatusDotStatus)

    **tests/phase-40-02-readme.spec.ts:**
    - Test 1: README.md exists with 4 required sections — use pattern that matches BOTH orderings: `SIGNAL.*FRAME|FRAME.*SIGNAL`
    - Test 2: README quick start contains `from "signalframeux"` code block
    - Test 3: MIGRATION.md exists and is under 200 lines
    - Test 4: MIGRATION.md contains import mapping for all 3 entry points
    - Test 5: README links to MIGRATION.md
    - No @sfux/ paths in either file (excluding historical "old" column references in migration table)

    **tests/phase-40-03-storybook.spec.ts:**
    - Test 1: .storybook/main.ts, preview.ts, manager.ts exist
    - Test 2: manager.ts contains `brandTitle: "SIGNALFRAME//UX"` and `appBorderRadius: 0`
    - Test 3: Run `pnpm build-storybook` via execFileSync("pnpm", ["build-storybook"]), verify storybook-static/index.html exists
    - Test 4: Count story files in stories/ and stories/flagship/, verify >= 40 total

    **tests/phase-40-04-api-docs.spec.ts:**
    - Read lib/api-docs.ts content as string
    - Verify zero matches for `@sfux/`
    - Verify all importPath values match `signalframeux` | `signalframeux/animation` | `signalframeux/webgl`
    - Read entry files, extract named exports, verify each appears as key in API_DOCS (excluding type-only exports)

    All specs use `import { test, expect } from "@playwright/test"` and `import * as fs from "fs"` and `import * as path from "path"` with `const ROOT = path.resolve(__dirname, "..")`.

    Use `execFileSync` (not `execSync`) for shell commands per project security conventions.
  </action>
  <verify>
    <automated>ls tests/phase-40-01-jsdoc-audit.spec.ts tests/phase-40-02-readme.spec.ts tests/phase-40-03-storybook.spec.ts tests/phase-40-04-api-docs.spec.ts && echo "All 4 spec files exist"</automated>
  </verify>
  <done>All 4 Playwright spec files exist in tests/ with valid TypeScript, proper imports, and test cases covering all DOC-* requirements. Tests are expected to fail RED until implementation plans execute.</done>
</task>

</tasks>

<verification>
- All 4 spec files exist in tests/ directory
- Each file has valid TypeScript syntax (no import errors)
- File count: `ls tests/phase-40-*.spec.ts | wc -l` returns 4
</verification>

<success_criteria>
1. tests/phase-40-01-jsdoc-audit.spec.ts exists with JSDoc coverage tests
2. tests/phase-40-02-readme.spec.ts exists with README/MIGRATION tests
3. tests/phase-40-03-storybook.spec.ts exists with Storybook build tests
4. tests/phase-40-04-api-docs.spec.ts exists with api-docs accuracy tests
5. All files use consistent Playwright test patterns matching existing project specs
</success_criteria>

<output>
After completion, create `.planning/phases/40-api-documentation-dx/40-00-SUMMARY.md`
</output>
