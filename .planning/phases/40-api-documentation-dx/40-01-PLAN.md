---
phase: 40-api-documentation-dx
plan: 01
type: execute
wave: 1
depends_on: ["40-00"]
files_modified:
  - scripts/generate-api-docs.ts
  - lib/api-docs.ts
  - package.json
  - components/sf/sf-card.tsx
  - components/sf/sf-tabs.tsx
  - components/sf/sf-table.tsx
  - components/sf/sf-tooltip.tsx
  - components/sf/sf-dialog.tsx
  - components/sf/sf-sheet.tsx
  - components/sf/sf-dropdown-menu.tsx
  - components/sf/sf-command.tsx
  - components/sf/sf-popover.tsx
  - components/sf/sf-scroll-area.tsx
  - components/sf/sf-select.tsx
  - components/sf/sf-radio-group.tsx
  - components/sf/sf-alert.tsx
  - components/sf/sf-alert-dialog.tsx
  - components/sf/sf-collapsible.tsx
  - components/sf/sf-avatar.tsx
  - components/sf/sf-breadcrumb.tsx
  - components/sf/sf-pagination.tsx
  - components/sf/sf-navigation-menu.tsx
  - components/sf/sf-toggle-group.tsx
  - components/sf/sf-input-group.tsx
  - components/sf/sf-input-otp.tsx
  - components/sf/sf-hover-card.tsx
  - components/sf/sf-accordion.tsx
  - components/sf/sf-stepper.tsx
  - components/sf/sf-empty-state.tsx
autonomous: true
requirements:
  - DOC-01
  - DOC-04

must_haves:
  truths:
    - "Every named export in entry-core/animation/webgl has JSDoc with description, at least one @param (if it accepts props), and @example"
    - "lib/api-docs.ts is auto-generated from source and contains zero @sfux/ import paths"
    - "/reference route shows all library exports with correct signalframeux import paths"
  artifacts:
    - path: "scripts/generate-api-docs.ts"
      provides: "Auto-generation script reading entry files + JSDoc to produce api-docs.ts"
      min_lines: 100
    - path: "lib/api-docs.ts"
      provides: "Auto-generated API docs with signalframeux import paths"
      contains: "signalframeux"
  key_links:
    - from: "scripts/generate-api-docs.ts"
      to: "lib/entry-core.ts"
      via: "file read + export parsing"
      pattern: "entry-core"
    - from: "scripts/generate-api-docs.ts"
      to: "lib/api-docs.ts"
      via: "file write"
      pattern: "api-docs"
    - from: "lib/api-docs.ts"
      to: "components/blocks/api-explorer.tsx"
      via: "import API_DOCS"
      pattern: "API_DOCS"
---

<objective>
Auto-generate api-docs.ts from actual export surface and audit JSDoc coverage on all exported SF components.

Purpose: Eliminate the stale hand-curated api-docs.ts (107 entries with @sfux/* import paths that never existed) and replace it with auto-generated data from entry-core.ts, entry-animation.ts, entry-webgl.ts. Simultaneously audit and fix JSDoc gaps on sub-components so autodocs and the auto-gen script produce complete documentation.

Output: scripts/generate-api-docs.ts, regenerated lib/api-docs.ts, JSDoc fixes on sub-components.
</objective>

<execution_context>
@/Users/greyaltaer/.claude/pde-os/engines/gsd/workflows/execute-plan.md
@/Users/greyaltaer/.claude/pde-os/engines/gsd/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-api-documentation-dx/40-CONTEXT.md
@.planning/phases/40-api-documentation-dx/40-RESEARCH.md

@lib/entry-core.ts
@lib/entry-animation.ts
@lib/entry-webgl.ts
@lib/api-docs.ts
@components/blocks/api-explorer.tsx

<interfaces>
From lib/api-docs.ts (lines 7-38):
```typescript
export interface PropDef {
  name: string;
  type: string;
  default: string;
  desc: string;
  required?: boolean;
}

export interface UsageExample {
  label: string;
  code: string;
}

export interface PreviewHud {
  lines: string[];
  code: string;
}

export interface ComponentDoc {
  id: string;
  name: string;
  layer: "FRAME" | "SIGNAL" | "CORE" | "TOKEN" | "HOOK";
  version: string;
  status: "STABLE" | "BETA" | "EXPERIMENTAL";
  description: string;
  importPath: string;
  importName: string;
  props: PropDef[];
  usage: UsageExample[];
  a11y: string[];
  preview?: PreviewHud;
}

export const API_DOCS: Record<string, ComponentDoc> = { ... };
```

The auto-gen script MUST produce output matching this exact interface. The consumer (api-explorer.tsx) imports API_DOCS and renders it — the interface contract is non-negotiable.
</interfaces>
</context>

<acceptance_criteria>
**AC-1:** Given the 3 entry files (entry-core.ts, entry-animation.ts, entry-webgl.ts), when `pnpm docs:generate` runs, then lib/api-docs.ts is overwritten with entries for every named export and contains zero `@sfux/` strings.

**AC-2:** Given any SF component file in components/sf/*.tsx that has named exports in an entry file, when reading the file, then every exported function/const has a `/**` JSDoc block containing at minimum a description line and an `@example` tag.

**AC-3:** Given the regenerated lib/api-docs.ts, when checking importPath values, then all entries use one of exactly three values: `signalframeux`, `signalframeux/animation`, or `signalframeux/webgl`.

**AC-4:** Given the regenerated lib/api-docs.ts, when `pnpm build` runs, then api-explorer.tsx compiles against the new API_DOCS without errors.

**AC-5:** Given tests/phase-40-01-jsdoc-audit.spec.ts (from Wave 0), when `npx playwright test tests/phase-40-01-jsdoc-audit.spec.ts` runs, then it exits 0.

**AC-6:** Given tests/phase-40-04-api-docs.spec.ts (from Wave 0), when `npx playwright test tests/phase-40-04-api-docs.spec.ts` runs, then it exits 0.
</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: JSDoc audit — card, tabs, table, overlay, dialog components (13 files)</name>
  <files>
    components/sf/sf-card.tsx, components/sf/sf-tabs.tsx, components/sf/sf-table.tsx,
    components/sf/sf-tooltip.tsx, components/sf/sf-dialog.tsx, components/sf/sf-sheet.tsx,
    components/sf/sf-dropdown-menu.tsx, components/sf/sf-command.tsx, components/sf/sf-popover.tsx,
    components/sf/sf-scroll-area.tsx, components/sf/sf-select.tsx, components/sf/sf-alert.tsx,
    components/sf/sf-alert-dialog.tsx
  </files>
  <read_first>
    lib/entry-core.ts, lib/entry-animation.ts, lib/entry-webgl.ts,
    components/sf/sf-button.tsx (reference JSDoc pattern),
    components/sf/sf-card.tsx, components/sf/sf-tabs.tsx, components/sf/sf-table.tsx,
    components/sf/sf-tooltip.tsx, components/sf/sf-dialog.tsx, components/sf/sf-sheet.tsx,
    components/sf/sf-dropdown-menu.tsx, components/sf/sf-command.tsx, components/sf/sf-popover.tsx
  </read_first>
  <ac_refs>AC-2</ac_refs>
  <action>
    Audit the first batch of SF component files (card, tabs, table, overlay, dialog, feedback group). For each file:

    1. Identify ALL exported names (the root component AND sub-components like SFCardHeader, SFCardTitle, etc.)
    2. Check if each exported name has a `/**` JSDoc block with:
       - A description line (first line after `/**`)
       - An `@example` tag with a usage snippet
       - `@param` tags for any props the component accepts (optional for simple sub-components that just forward div props)
    3. If a sub-component is missing JSDoc, add minimal JSDoc following this pattern from RESEARCH.md:

    ```typescript
    /** Card header container — FRAME layer. Receives standard div props.
     * @example
     * <SFCardHeader><SFCardTitle>Title</SFCardTitle></SFCardHeader>
     */
    ```

    Sub-components to audit in this batch:
    - SFCardHeader, SFCardTitle, SFCardDescription, SFCardContent, SFCardFooter
    - SFTabsList, SFTabsTrigger, SFTabsContent
    - SFTableHeader, SFTableHead, SFTableBody, SFTableRow, SFTableCell
    - SFTooltipContent, SFTooltipTrigger
    - SFDialogTrigger, SFDialogClose, SFDialogContent, SFDialogHeader, SFDialogFooter, SFDialogTitle, SFDialogDescription
    - SFSheetTrigger, SFSheetClose, SFSheetContent, SFSheetHeader, SFSheetFooter, SFSheetTitle, SFSheetDescription
    - SFDropdownMenuTrigger, SFDropdownMenuContent, SFDropdownMenuGroup, SFDropdownMenuItem, SFDropdownMenuLabel, SFDropdownMenuSeparator, SFDropdownMenuShortcut
    - SFCommandDialog, SFCommandInput, SFCommandList, SFCommandEmpty, SFCommandGroup, SFCommandItem, SFCommandSeparator, SFCommandShortcut
    - SFPopoverTrigger, SFPopoverContent, SFPopoverHeader, SFPopoverTitle, SFPopoverDescription
    - SFScrollBar
    - SFSelectTrigger, SFSelectContent, SFSelectGroup, SFSelectItem, SFSelectLabel, SFSelectValue
    - SFAlertTitle, SFAlertDescription
    - SFAlertDialogTrigger, SFAlertDialogContent, SFAlertDialogHeader, SFAlertDialogFooter, SFAlertDialogTitle, SFAlertDialogDescription, SFAlertDialogAction, SFAlertDialogCancel

    DO NOT modify component behavior or styling. JSDoc additions only.
  </action>
  <verify>
    <automated>for f in components/sf/sf-card.tsx components/sf/sf-tabs.tsx components/sf/sf-table.tsx components/sf/sf-tooltip.tsx components/sf/sf-dialog.tsx components/sf/sf-sheet.tsx components/sf/sf-dropdown-menu.tsx components/sf/sf-command.tsx components/sf/sf-popover.tsx components/sf/sf-scroll-area.tsx components/sf/sf-select.tsx components/sf/sf-alert.tsx components/sf/sf-alert-dialog.tsx; do echo "$f: exports=$(grep -c '^export' $f) jsdoc=$(grep -c '/\*\*' $f)"; done</automated>
  </verify>
  <done>Every named export across the 13 card/tabs/table/overlay/dialog/feedback files has a /** JSDoc block with description and @example. Zero exports without JSDoc in this batch.</done>
</task>

<task type="auto">
  <name>Task 2: JSDoc audit — form, navigation, input, layout, animation components (13 files)</name>
  <files>
    components/sf/sf-radio-group.tsx, components/sf/sf-collapsible.tsx,
    components/sf/sf-avatar.tsx, components/sf/sf-breadcrumb.tsx, components/sf/sf-pagination.tsx,
    components/sf/sf-navigation-menu.tsx, components/sf/sf-toggle-group.tsx,
    components/sf/sf-input-group.tsx, components/sf/sf-input-otp.tsx, components/sf/sf-hover-card.tsx,
    components/sf/sf-accordion.tsx, components/sf/sf-stepper.tsx, components/sf/sf-empty-state.tsx
  </files>
  <read_first>
    components/sf/sf-button.tsx (reference JSDoc pattern),
    components/sf/sf-radio-group.tsx, components/sf/sf-collapsible.tsx,
    components/sf/sf-avatar.tsx, components/sf/sf-breadcrumb.tsx,
    components/sf/sf-navigation-menu.tsx, components/sf/sf-accordion.tsx
  </read_first>
  <ac_refs>AC-2</ac_refs>
  <action>
    Audit the second batch of SF component files (form, navigation, input, layout, animation group). For each file:

    1. Identify ALL exported names (root + sub-components)
    2. Check if each exported name has a `/**` JSDoc block with description + `@example`
    3. Add minimal JSDoc for any missing sub-components

    Sub-components to audit in this batch:
    - SFRadioGroupItem
    - SFCollapsibleTrigger, SFCollapsibleContent
    - SFAvatarImage, SFAvatarFallback
    - SFBreadcrumbList, SFBreadcrumbItem, SFBreadcrumbLink, SFBreadcrumbPage, SFBreadcrumbSeparator
    - SFPaginationContent, SFPaginationItem, SFPaginationLink, SFPaginationPrevious, SFPaginationNext
    - SFNavigationMenuList, SFNavigationMenuItem, SFNavigationMenuTrigger, SFNavigationMenuContent, SFNavigationMenuLink, SFNavigationMenuViewport, SFNavigationMenuMobile
    - SFToggleGroupItem
    - SFInputGroupAddon, SFInputGroupButton, SFInputGroupText, SFInputGroupInput, SFInputGroupTextarea
    - SFInputOTPGroup, SFInputOTPSlot, SFInputOTPSeparator
    - SFHoverCardTrigger, SFHoverCardContent
    - SFAccordionItem, SFAccordionTrigger, SFAccordionContent
    - SFStep (from sf-stepper.tsx)

    Also verify: lib/signalframe-provider.tsx (createSignalframeUX, useSignalframe), hooks/use-scramble-text.ts, hooks/use-session-state.ts, hooks/use-nav-reveal.ts, hooks/use-signal-scene.ts, lib/color-resolve.ts (resolveColorToken) all have JSDoc with description + @example.

    DO NOT modify component behavior or styling. JSDoc additions only.
  </action>
  <verify>
    <automated>for f in components/sf/sf-radio-group.tsx components/sf/sf-collapsible.tsx components/sf/sf-avatar.tsx components/sf/sf-breadcrumb.tsx components/sf/sf-pagination.tsx components/sf/sf-navigation-menu.tsx components/sf/sf-toggle-group.tsx components/sf/sf-input-group.tsx components/sf/sf-input-otp.tsx components/sf/sf-hover-card.tsx components/sf/sf-accordion.tsx components/sf/sf-stepper.tsx components/sf/sf-empty-state.tsx; do echo "$f: exports=$(grep -c '^export' $f) jsdoc=$(grep -c '/\*\*' $f)"; done</automated>
  </verify>
  <done>Every named export across the 13 form/navigation/input/layout/animation files has a /** JSDoc block with description and @example. Zero exports without JSDoc in this batch.</done>
</task>

<task type="auto">
  <name>Task 3: Auto-generation script + regenerate api-docs.ts</name>
  <files>
    scripts/generate-api-docs.ts, lib/api-docs.ts, package.json
  </files>
  <read_first>
    lib/api-docs.ts (current stale version — preserve ComponentDoc/PropDef/UsageExample/PreviewHud interfaces),
    lib/entry-core.ts, lib/entry-animation.ts, lib/entry-webgl.ts,
    components/blocks/api-explorer.tsx (consumer — understand how API_DOCS is consumed),
    package.json (add docs:generate script)
  </read_first>
  <ac_refs>AC-1, AC-3, AC-4, AC-5, AC-6</ac_refs>
  <action>
    Create `scripts/generate-api-docs.ts` that:

    1. Reads lib/entry-core.ts, lib/entry-animation.ts, lib/entry-webgl.ts
    2. Parses each `export { Name } from "../path"` line using regex to extract:
       - Export names (e.g., SFButton, SFCardHeader)
       - Source file relative path (e.g., ../components/sf/sf-button)
    3. For `export * from "./gsap-core"` etc in entry-animation.ts, uses TypeScript compiler API to resolve the actual exported names from those files. ONLY includes names that have JSDoc `/**` blocks (to filter out low-level GSAP internals without docs).
    4. For each source file, uses TypeScript compiler API (ts.createProgram + ts.getJSDocTags) to extract:
       - Description (first paragraph of /** ... */)
       - @param tags -> PropDef[] entries
       - @example tags -> UsageExample[] entries
    5. Maps entry file to importPath:
       - entry-core.ts -> "signalframeux"
       - entry-animation.ts -> "signalframeux/animation"
       - entry-webgl.ts -> "signalframeux/webgl"
    6. Determines layer from entry file + JSDoc @layer tag (if present):
       - entry-core.ts interactive components -> "FRAME"
       - entry-core.ts hooks -> "HOOK"
       - entry-core.ts utilities (cn, toggleTheme, GRAIN_SVG) -> "CORE"
       - entry-core.ts types (SignalframeUXConfig etc) -> "CORE"
       - entry-animation.ts -> "SIGNAL"
       - entry-webgl.ts -> "SIGNAL"
    7. Writes output to lib/api-docs.ts preserving the EXACT interface definitions (PropDef, UsageExample, PreviewHud, ComponentDoc) at the top of the file, then `export const API_DOCS: Record<string, ComponentDoc> = { ... }` with all entries.
    8. Sets version to "v1.6.0" and status to "STABLE" for all entries.

    Add to package.json scripts:
    ```json
    "docs:generate": "tsx scripts/generate-api-docs.ts"
    ```

    Run `pnpm docs:generate` to produce the new lib/api-docs.ts.

    Verify the output:
    - Zero `@sfux/` strings in the generated file
    - All importPath values are one of: "signalframeux", "signalframeux/animation", "signalframeux/webgl"
    - Every named export from all 3 entry files appears as a key in API_DOCS (except type-only exports like TextVariant, SignalframeUXConfig, UseSignalframeReturn, SFStatusDotStatus)
  </action>
  <verify>
    <automated>pnpm docs:generate && grep -c "@sfux/" lib/api-docs.ts | grep "^0$" && pnpm build && npx playwright test tests/phase-40-01-jsdoc-audit.spec.ts tests/phase-40-04-api-docs.spec.ts</automated>
  </verify>
  <done>
    - scripts/generate-api-docs.ts exists and runs via `pnpm docs:generate`
    - lib/api-docs.ts contains zero @sfux/ strings
    - All entry file exports appear as API_DOCS keys
    - All importPaths use signalframeux package name
    - pnpm build succeeds (api-explorer.tsx compiles against regenerated API_DOCS)
    - Both Playwright spec files pass
  </done>
</task>

</tasks>

<verification>
- `pnpm docs:generate` exits 0 and produces lib/api-docs.ts
- `grep -c "@sfux/" lib/api-docs.ts` returns 0
- `npx playwright test tests/phase-40-01-jsdoc-audit.spec.ts` exits 0
- `npx playwright test tests/phase-40-04-api-docs.spec.ts` exits 0
- `pnpm build` passes (api-explorer.tsx consumes regenerated API_DOCS without error)
</verification>

<success_criteria>
1. Every named export in the 3 entry files has JSDoc with description + @example
2. lib/api-docs.ts is auto-generated with zero @sfux/ paths
3. All import paths use signalframeux/signalframeux-animation/signalframeux-webgl
4. Both Playwright verification specs pass
5. pnpm build succeeds (api-explorer.tsx integration unbroken)
</success_criteria>

<output>
After completion, create `.planning/phases/40-api-documentation-dx/40-01-SUMMARY.md`
</output>
