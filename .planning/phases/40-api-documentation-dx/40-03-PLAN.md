---
phase: 40-api-documentation-dx
plan: 03
type: execute
wave: 2
depends_on: ["40-00", "40-01"]
files_modified:
  - .storybook/main.ts
  - .storybook/preview.ts
  - .storybook/manager.ts
  - stories/sf-button.stories.tsx
  - stories/sf-card.stories.tsx
  - stories/sf-input.stories.tsx
  - stories/sf-badge.stories.tsx
  - stories/sf-tabs.stories.tsx
  - stories/sf-separator.stories.tsx
  - stories/sf-table.stories.tsx
  - stories/sf-tooltip.stories.tsx
  - stories/sf-dialog.stories.tsx
  - stories/sf-sheet.stories.tsx
  - stories/sf-dropdown-menu.stories.tsx
  - stories/sf-toggle.stories.tsx
  - stories/sf-slider.stories.tsx
  - stories/sf-command.stories.tsx
  - stories/sf-skeleton.stories.tsx
  - stories/sf-popover.stories.tsx
  - stories/sf-scroll-area.stories.tsx
  - stories/sf-label.stories.tsx
  - stories/sf-select.stories.tsx
  - stories/sf-checkbox.stories.tsx
  - stories/sf-radio-group.stories.tsx
  - stories/sf-switch.stories.tsx
  - stories/sf-textarea.stories.tsx
  - stories/sf-alert.stories.tsx
  - stories/sf-alert-dialog.stories.tsx
  - stories/sf-collapsible.stories.tsx
  - stories/sf-avatar.stories.tsx
  - stories/sf-breadcrumb.stories.tsx
  - stories/sf-pagination.stories.tsx
  - stories/sf-navigation-menu.stories.tsx
  - stories/sf-toggle-group.stories.tsx
  - stories/sf-input-group.stories.tsx
  - stories/sf-input-otp.stories.tsx
  - stories/sf-hover-card.stories.tsx
  - stories/sf-container.stories.tsx
  - stories/sf-section.stories.tsx
  - stories/sf-stack.stories.tsx
  - stories/sf-grid.stories.tsx
  - stories/sf-text.stories.tsx
  - stories/flagship/sf-accordion.stories.tsx
  - stories/flagship/sf-progress.stories.tsx
  - stories/flagship/sf-status-dot.stories.tsx
  - stories/flagship/sf-stepper.stories.tsx
  - stories/flagship/sf-empty-state.stories.tsx
  - stories/flagship/signal-canvas.stories.tsx
  - stories/flagship/sf-button.stories.tsx
  - stories/flagship/sf-card.stories.tsx
  - stories/flagship/sf-dialog.stories.tsx
  - stories/flagship/sf-dropdown-menu.stories.tsx
  - stories/flagship/sf-container.stories.tsx
  - stories/flagship/sf-navigation-menu.stories.tsx
  - package.json
autonomous: true
requirements:
  - DOC-03

must_haves:
  truths:
    - "Storybook runs locally via pnpm storybook and renders SF components with correct SFUX tokens/styling"
    - "Every exported SF component has at least one story file"
    - "pnpm build-storybook produces storybook-static/ with index.html"
    - "Storybook UI is branded with SFUX identity — dark background, magenta accent, zero border-radius"
    - "At least 10 flagship components have rich stories with interactive controls and 3+ variants"
  artifacts:
    - path: ".storybook/main.ts"
      provides: "Storybook framework config for Next.js + Vite"
      contains: "@storybook/nextjs-vite"
    - path: ".storybook/preview.ts"
      provides: "Global decorators, Tailwind CSS import, dark theme default"
      contains: "globals.css"
    - path: ".storybook/manager.ts"
      provides: "Custom SFUX brand theme"
      contains: "SIGNALFRAME//UX"
  key_links:
    - from: ".storybook/preview.ts"
      to: "app/globals.css"
      via: "CSS import"
      pattern: "globals.css"
    - from: "stories/*.stories.tsx"
      to: "components/sf/*.tsx"
      via: "component import"
      pattern: "from.*components/sf"
---

<objective>
Set up Storybook 10 with branded theme and stories for all exported SF components.

Purpose: Storybook serves dual purpose (D-01): local dev sandbox for component exploration AND published consumer showcase. Every exported SF component gets a basic story with autodocs; 12 flagship components get rich stories with interactive controls and composition examples (D-02 requires 10-15). The Storybook UI itself is branded with SFUX identity (D-03).

Output: .storybook/ config, stories/ directory with 40+ story files, branded theme.
</objective>

<execution_context>
@/Users/greyaltaer/.claude/pde-os/engines/gsd/workflows/execute-plan.md
@/Users/greyaltaer/.claude/pde-os/engines/gsd/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-api-documentation-dx/40-CONTEXT.md
@.planning/phases/40-api-documentation-dx/40-RESEARCH.md
@.planning/phases/40-api-documentation-dx/40-01-SUMMARY.md

@CLAUDE.md
@lib/entry-core.ts
@lib/entry-animation.ts
@lib/entry-webgl.ts
@app/globals.css
@postcss.config.mjs
@tsconfig.json

<interfaces>
From RESEARCH.md — Storybook config patterns:

main.ts: StorybookConfig with framework "@storybook/nextjs-vite", stories glob "../stories/**/*.stories.@(ts|tsx)", addons ["@storybook/addon-essentials", "@storybook/addon-themes"], docs.autodocs "tag"

preview.ts: Import "../app/globals.css", withThemeByClassName decorator (dark default), backgrounds with OKLCH values

manager.ts: create() theme from @storybook/theming/create — base "dark", brandTitle "SIGNALFRAME//UX", appBorderRadius 0, inputBorderRadius 0, hex colors (not OKLCH — Storybook create() doesn't support OKLCH)

Story pattern: Meta<typeof SFComponent> with tags: ["autodocs"], args for default story. Flagship stories add argTypes with control configs.

Server Components: Enable `features: { experimentalRSC: true }` in main.ts OR add "use client" to story files importing layout primitives (SFContainer, SFSection, SFStack, SFGrid, SFText).
</interfaces>
</context>

<acceptance_criteria>
**AC-1:** Given the project root, when running `pnpm storybook`, then Storybook opens in browser and renders SF components with correct SFUX dark theme, OKLCH colors, and zero border-radius.

**AC-2:** Given the stories/ directory, when listing .stories.tsx files, then there is at least one story file for every SF component exported from entry-core.ts and entry-animation.ts (excluding type-only exports and GSAP utility re-exports).

**AC-3:** Given the stories/flagship/ directory, when counting flagship story files, then there are at least 10 flagship stories, each with argTypes with control definitions and at least 3 named story exports (variants/compositions).

**AC-4:** Given `pnpm build-storybook`, when the command completes, then it exits 0 and `storybook-static/index.html` exists.

**AC-5:** Given .storybook/manager.ts, when reading the file, then it contains `brandTitle: "SIGNALFRAME//UX"` and `appBorderRadius: 0`.

**AC-6:** Given tests/phase-40-03-storybook.spec.ts (from Wave 0), when `npx playwright test tests/phase-40-03-storybook.spec.ts` runs, then it exits 0.
</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Install Storybook + configure .storybook/ files + branded theme</name>
  <files>
    .storybook/main.ts, .storybook/preview.ts, .storybook/manager.ts, package.json
  </files>
  <read_first>
    package.json (current deps and scripts),
    app/globals.css (first 50 lines — token imports, @source directives),
    postcss.config.mjs (PostCSS pipeline for Tailwind v4),
    tsconfig.json (path aliases — @/ must resolve in Storybook)
  </read_first>
  <ac_refs>AC-1, AC-4, AC-5</ac_refs>
  <action>
    Install Storybook packages:
    ```bash
    pnpm add -D @storybook/nextjs-vite@latest storybook@latest @storybook/addon-themes @storybook/theming
    ```

    Create `.storybook/main.ts`:
    ```typescript
    import type { StorybookConfig } from "@storybook/nextjs-vite";

    const config: StorybookConfig = {
      stories: ["../stories/**/*.stories.@(ts|tsx)", "../stories/**/*.mdx"],
      addons: [
        "@storybook/addon-essentials",
        "@storybook/addon-themes",
      ],
      framework: {
        name: "@storybook/nextjs-vite",
        options: {},
      },
      features: {
        experimentalRSC: true,
      },
      docs: {
        autodocs: "tag",
      },
    };

    export default config;
    ```

    Create `.storybook/preview.ts`:
    ```typescript
    import type { Preview } from "@storybook/react";
    import { withThemeByClassName } from "@storybook/addon-themes";
    import "../app/globals.css";

    const preview: Preview = {
      decorators: [
        withThemeByClassName({
          themes: {
            dark: "dark",
            light: "",
          },
          defaultTheme: "dark",
        }),
      ],
      parameters: {
        backgrounds: {
          default: "dark",
          values: [
            { name: "dark", value: "oklch(0.145 0 0)" },
            { name: "light", value: "oklch(1 0 0)" },
          ],
        },
        controls: {
          matchers: {
            color: /(background|color)$/i,
            date: /date$/i,
          },
        },
      },
    };

    export default preview;
    ```

    Create `.storybook/manager.ts`:
    ```typescript
    import { addons } from "storybook/manager-api";
    import { create } from "@storybook/theming/create";

    const sfuxTheme = create({
      base: "dark",
      brandTitle: "SIGNALFRAME//UX",
      brandUrl: "https://signalframe.culturedivision.com",
      fontBase: "Inter, sans-serif",
      fontCode: "'JetBrains Mono', monospace",
      appBg: "#1a1a1a",
      appContentBg: "#0a0a0a",
      appBorderColor: "#1a1a1a",
      appBorderRadius: 0,
      colorPrimary: "#e0306c",
      colorSecondary: "#e0306c",
      barBg: "#0a0a0a",
      barTextColor: "#999999",
      barSelectedColor: "#e0306c",
      inputBg: "#1a1a1a",
      inputBorder: "#333333",
      inputBorderRadius: 0,
    });

    addons.setConfig({ theme: sfuxTheme });
    ```

    Add scripts to package.json:
    ```json
    "storybook": "storybook dev -p 6006",
    "build-storybook": "storybook build"
    ```

    Add `storybook-static` to .gitignore if not already present.

    Verify: `pnpm build-storybook` exits 0.

    If Tailwind classes fail to apply in stories, add `@source "../stories"` to app/globals.css @source directives.
  </action>
  <verify>
    <automated>pnpm build-storybook && test -f storybook-static/index.html && echo "PASS"</automated>
  </verify>
  <done>
    - .storybook/main.ts, preview.ts, manager.ts exist with correct config
    - pnpm build-storybook exits 0
    - storybook-static/index.html exists
    - Branded theme has SIGNALFRAME//UX title, zero border-radius, magenta accent
  </done>
</task>

<task type="auto">
  <name>Task 2: Create story files for all SF components + 12 flagship rich stories + verification</name>
  <files>
    stories/sf-button.stories.tsx, stories/sf-card.stories.tsx, stories/sf-input.stories.tsx,
    stories/sf-badge.stories.tsx, stories/sf-tabs.stories.tsx, stories/sf-separator.stories.tsx,
    stories/sf-table.stories.tsx, stories/sf-tooltip.stories.tsx, stories/sf-dialog.stories.tsx,
    stories/sf-sheet.stories.tsx, stories/sf-dropdown-menu.stories.tsx, stories/sf-toggle.stories.tsx,
    stories/sf-slider.stories.tsx, stories/sf-command.stories.tsx, stories/sf-skeleton.stories.tsx,
    stories/sf-popover.stories.tsx, stories/sf-scroll-area.stories.tsx, stories/sf-label.stories.tsx,
    stories/sf-select.stories.tsx, stories/sf-checkbox.stories.tsx, stories/sf-radio-group.stories.tsx,
    stories/sf-switch.stories.tsx, stories/sf-textarea.stories.tsx, stories/sf-alert.stories.tsx,
    stories/sf-alert-dialog.stories.tsx, stories/sf-collapsible.stories.tsx,
    stories/sf-avatar.stories.tsx, stories/sf-breadcrumb.stories.tsx,
    stories/sf-pagination.stories.tsx, stories/sf-navigation-menu.stories.tsx,
    stories/sf-toggle-group.stories.tsx, stories/sf-input-group.stories.tsx,
    stories/sf-input-otp.stories.tsx, stories/sf-hover-card.stories.tsx,
    stories/sf-container.stories.tsx, stories/sf-section.stories.tsx,
    stories/sf-stack.stories.tsx, stories/sf-grid.stories.tsx, stories/sf-text.stories.tsx,
    stories/flagship/sf-accordion.stories.tsx, stories/flagship/sf-progress.stories.tsx,
    stories/flagship/sf-status-dot.stories.tsx, stories/flagship/sf-stepper.stories.tsx,
    stories/flagship/sf-empty-state.stories.tsx, stories/flagship/signal-canvas.stories.tsx,
    stories/flagship/sf-button.stories.tsx, stories/flagship/sf-card.stories.tsx,
    stories/flagship/sf-dialog.stories.tsx, stories/flagship/sf-dropdown-menu.stories.tsx,
    stories/flagship/sf-container.stories.tsx, stories/flagship/sf-navigation-menu.stories.tsx
  </files>
  <read_first>
    .storybook/main.ts (confirm framework config from Task 1),
    components/sf/sf-button.tsx (reference for story pattern — read JSDoc, props, variants),
    components/sf/sf-card.tsx (multi-sub-component story pattern),
    components/sf/sf-accordion.tsx (SIGNAL layer flagship pattern),
    components/sf/sf-container.tsx (layout primitive — Server Component story pattern),
    lib/entry-core.ts (full export list),
    lib/entry-animation.ts (animation exports)
  </read_first>
  <ac_refs>AC-2, AC-3, AC-4, AC-6</ac_refs>
  <action>
    Create stories/ directory with subdirectories: stories/ (basic) and stories/flagship/ (rich).

    **BASIC STORIES** (one per SF component file from entry-core.ts — ~35 files):
    Each basic story file follows this pattern:
    ```typescript
    import type { Meta, StoryObj } from "@storybook/react";
    import { SFButton } from "@/components/sf/sf-button";

    const meta: Meta<typeof SFButton> = {
      title: "Core/SFButton",
      component: SFButton,
      tags: ["autodocs"],
    };
    export default meta;

    type Story = StoryObj<typeof SFButton>;

    export const Default: Story = {
      args: { intent: "primary", children: "LAUNCH" },
    };

    export const Ghost: Story = {
      args: { intent: "ghost", children: "CANCEL" },
    };
    ```

    Title hierarchy for sidebar organization:
    - Layout primitives: `title: "Layout/SFContainer"`, `"Layout/SFSection"`, etc.
    - Interactive components: `title: "Core/SFButton"`, `"Core/SFCard"`, etc.
    - Form components: `title: "Form/SFInput"`, `"Form/SFCheckbox"`, etc.
    - Feedback: `title: "Feedback/SFAlert"`, `"Feedback/SFAlertDialog"`, etc.
    - Navigation: `title: "Navigation/SFBreadcrumb"`, `"Navigation/SFPagination"`, etc.
    - Overlay: `title: "Overlay/SFDialog"`, `"Overlay/SFSheet"`, `"Overlay/SFPopover"`, etc.

    For multi-sub-component exports (SFCard with SFCardHeader etc), compose them in the story:
    ```typescript
    export const Default: Story = {
      render: () => (
        <SFCard>
          <SFCardHeader>
            <SFCardTitle>System Status</SFCardTitle>
            <SFCardDescription>Monitoring active</SFCardDescription>
          </SFCardHeader>
          <SFCardContent>Content here</SFCardContent>
        </SFCard>
      ),
    };
    ```

    **FLAGSHIP STORIES** (12 components in stories/flagship/ — rich stories with controls per D-02 requiring 10-15):

    1. **SFAccordion** — title "SIGNAL/SFAccordion", argTypes for type (single/multiple) and collapsible, 3+ stories (Single, Multiple, Composed)
    2. **SFProgress** — title "SIGNAL/SFProgress", argTypes for value (0-100 slider) and intent, 3+ stories (Default, Signal, Indeterminate)
    3. **SFStatusDot** — title "SIGNAL/SFStatusDot", argTypes for status control (online/offline/busy/away), 4 stories (one per status)
    4. **SFStepper** — title "SIGNAL/SFStepper", composed with SFStep children, 3+ stories (Linear, WithContent, Complete)
    5. **SFEmptyState** — title "SIGNAL/SFEmptyState", argTypes for message/action, 2+ stories
    6. **SignalCanvas** — title "WebGL/SignalCanvas", tags: ["autodocs"] ONLY (no render — WebGL needs real browser context), parameters.docs.description.component with usage instructions, static story export
    7. **SFButton** — title "Flagship/SFButton", argTypes for intent (primary/ghost/signal), size (sm/md/lg/xl), disabled boolean. 4+ stories: Primary, Ghost, Signal, AllSizes
    8. **SFCard** — title "Flagship/SFCard", composed with all sub-components (Header, Title, Description, Content, Footer). 3+ stories: Default, WithFooterActions, CompactCard
    9. **SFDialog** — title "Flagship/SFDialog", argTypes for open state. 3+ stories: Default, WithForm, AlertConfirmation (composed with SFAlertDialog)
    10. **SFDropdownMenu** — title "Flagship/SFDropdownMenu", composed with Menu items, groups, separators. 3+ stories: Default, WithShortcuts, NestedGroups
    11. **SFContainer** — title "Flagship/SFContainer", argTypes for max-width variant, padding. 3+ stories: Default, Wide, FullBleed. (Layout primitive — Server Component; use experimentalRSC or "use client" fallback)
    12. **SFNavigationMenu** — title "Flagship/SFNavigationMenu", composed with MenuList, MenuItem, MenuTrigger, MenuContent. 3+ stories: Default, WithMobileVariant, MultiLevel

    For flagship stories, each file must have:
    - `argTypes` with `control` definitions for key props
    - At least 3 named story exports showing different variants/compositions
    - Composed examples showing sub-component usage patterns

    **Layout primitive stories** (SFContainer, SFSection, SFStack, SFGrid, SFText):
    These are Server Components. With `experimentalRSC: true` in main.ts they should work. If they fail, add `"use client"` at top of their story files as fallback.

    After all stories are created, run `pnpm build-storybook` to verify everything compiles. Then run the Wave 0 spec.
  </action>
  <verify>
    <automated>ls stories/*.stories.tsx stories/flagship/*.stories.tsx 2>/dev/null | wc -l && ls stories/flagship/*.stories.tsx 2>/dev/null | wc -l && pnpm build-storybook && npx playwright test tests/phase-40-03-storybook.spec.ts</automated>
  </verify>
  <done>
    - 40+ story files exist across stories/ and stories/flagship/
    - Every SF component from entry-core.ts and entry-animation.ts has a story
    - 12 flagship stories in stories/flagship/ have argTypes + 3+ named exports each
    - pnpm build-storybook exits 0
    - Playwright spec passes
  </done>
</task>

</tasks>

<verification>
- `pnpm build-storybook` exits 0
- `storybook-static/index.html` exists
- `ls stories/*.stories.tsx stories/flagship/*.stories.tsx | wc -l` >= 40
- `ls stories/flagship/*.stories.tsx | wc -l` >= 10
- `.storybook/manager.ts` contains `SIGNALFRAME//UX` and `appBorderRadius: 0`
- `npx playwright test tests/phase-40-03-storybook.spec.ts` exits 0
</verification>

<success_criteria>
1. Storybook runs locally with all SF components rendering correctly
2. Every exported SF component has at least one story
3. 12 flagship components have rich stories with controls and compositions (meets D-02 minimum of 10)
4. Branded SFUX theme applied — dark, magenta accent, zero border-radius
5. pnpm build-storybook produces static output for deployment
6. Playwright verification spec passes
</success_criteria>

<output>
After completion, create `.planning/phases/40-api-documentation-dx/40-03-SUMMARY.md`
</output>
