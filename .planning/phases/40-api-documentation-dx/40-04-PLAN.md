---
phase: 40-api-documentation-dx
plan: 04
type: execute
wave: 3
depends_on: ["40-03"]
files_modified:
  - vercel-storybook.json
  - .github/workflows/storybook-deploy.yml
autonomous: false
requirements:
  - DOC-03

must_haves:
  truths:
    - "Storybook is deployed as a separate Vercel project with auto-deploy on push"
    - "Build command is pnpm build-storybook and output directory is storybook-static"
  artifacts:
    - path: "vercel-storybook.json"
      provides: "Vercel project config for Storybook deployment"
      contains: "build-storybook"
  key_links:
    - from: "vercel-storybook.json"
      to: "storybook-static/"
      via: "outputDirectory config"
      pattern: "storybook-static"
---

<objective>
Deploy Storybook to Vercel as a separate project with auto-deploy on push.

Purpose: D-04 locks Storybook deployment to a Vercel subdomain (e.g. storybook.signalframeux.com) as a separate Vercel project that auto-deploys on push. Plan 40-03 handles building Storybook locally; this plan handles the deployment infrastructure.

Output: Vercel project configuration for Storybook, deployment verification.
</objective>

<execution_context>
@/Users/greyaltaer/.claude/pde-os/engines/gsd/workflows/execute-plan.md
@/Users/greyaltaer/.claude/pde-os/engines/gsd/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-api-documentation-dx/40-CONTEXT.md
@.planning/phases/40-api-documentation-dx/40-03-SUMMARY.md

@package.json
@.gitignore
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Vercel project config for Storybook deployment</name>
  <files>vercel-storybook.json</files>
  <read_first>
    package.json (verify build-storybook script exists from Plan 40-03),
    .gitignore (verify storybook-static is gitignored)
  </read_first>
  <action>
    Create `vercel-storybook.json` at repo root — a Vercel project configuration file for the Storybook deployment.

    ```json
    {
      "$schema": "https://openapi.vercel.sh/vercel.json",
      "buildCommand": "pnpm build-storybook",
      "outputDirectory": "storybook-static",
      "framework": null,
      "installCommand": "pnpm install"
    }
    ```

    This file will be referenced when creating the separate Vercel project. The Storybook deployment is a static site (no framework) built by `pnpm build-storybook` with output in `storybook-static/`.

    Then attempt to create the Vercel project and link it:

    ```bash
    # Create a new Vercel project for Storybook
    # This links the same repo but with Storybook-specific build settings
    vercel link --yes --project signalframeux-storybook 2>/dev/null || true
    ```

    If `vercel link` requires authentication or interactive setup, this task will create a checkpoint for the user to complete the Vercel project setup manually.

    Configure the project build settings via CLI if possible:
    ```bash
    vercel project ls 2>/dev/null  # Check if project exists
    ```

    If CLI project creation is not possible without interactive auth, document the manual steps clearly for the checkpoint task.
  </action>
  <verify>
    <automated>test -f vercel-storybook.json && grep -q "build-storybook" vercel-storybook.json && grep -q "storybook-static" vercel-storybook.json && echo "PASS"</automated>
  </verify>
  <done>vercel-storybook.json exists with correct build command and output directory for Storybook deployment.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Storybook Vercel deployment configuration (vercel-storybook.json) with build command `pnpm build-storybook` and output directory `storybook-static`.</what-built>
  <how-to-verify>
    1. Go to https://vercel.com/new and create a new project
    2. Import the SignalframeUX repository
    3. Set project name to `signalframeux-storybook` (or similar)
    4. Configure build settings:
       - Framework Preset: Other
       - Build Command: `pnpm build-storybook`
       - Output Directory: `storybook-static`
       - Install Command: `pnpm install`
    5. Deploy and verify the Storybook is accessible at the Vercel URL
    6. Optionally configure a custom subdomain (e.g. storybook.signalframeux.com) under Project Settings > Domains
    7. Push a trivial change to verify auto-deploy triggers on push
  </how-to-verify>
  <resume-signal>Type "deployed" with the Storybook URL, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
- vercel-storybook.json exists with build-storybook command
- Storybook is deployed and accessible at Vercel URL (confirmed by user)
</verification>

<success_criteria>
1. vercel-storybook.json exists with correct Storybook build configuration
2. Separate Vercel project created for Storybook (user-verified)
3. Auto-deploy on push is enabled (Vercel default behavior)
4. Storybook is accessible at deployed URL
</success_criteria>

<output>
After completion, create `.planning/phases/40-api-documentation-dx/40-04-SUMMARY.md`
</output>
