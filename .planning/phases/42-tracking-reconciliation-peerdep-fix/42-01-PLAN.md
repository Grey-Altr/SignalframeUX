---
phase: 42-tracking-reconciliation-peerdep-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/36-housekeeping-carry-overs/36-01-SUMMARY.md
  - .planning/phases/36-housekeeping-carry-overs/36-02-SUMMARY.md
  - .planning/phases/39-library-build-pipeline/39-01-SUMMARY.md
  - .planning/phases/39-library-build-pipeline/39-02-SUMMARY.md
  - package.json
autonomous: true
requirements:
  - CO-01
  - CO-02
  - CO-03
  - CO-04
  - LIB-01
  - LIB-02
  - LIB-03

must_haves:
  truths:
    - "Phase 36 SUMMARYs have requirements-completed frontmatter"
    - "Phase 39 SUMMARYs have requirements-completed frontmatter"
    - "next is in peerDependencies with optional: true in peerDependenciesMeta"
    - "next is NOT in dependencies"
    - "npm publish --dry-run still succeeds"
  artifacts:
    - path: "package.json"
      provides: "Corrected peerDependencies for next"
      contains: "peerDependenciesMeta"
---

<objective>
Close artifact tracking gaps from v1.6 milestone audit: add requirements-completed frontmatter to Phase 36 and 39 SUMMARYs, move `next` from dependencies to peerDependencies.

Purpose: CO-01-04 and LIB-01-03 were implemented but never tracked in SUMMARY frontmatter. The `next` dependency placement causes duplicate installs for consumers.
Output: Updated SUMMARYs, corrected package.json
</objective>

<context>
@.planning/v1.6-MILESTONE-AUDIT.md
@.planning/REQUIREMENTS.md
</context>

<acceptance_criteria>

**AC-1:** Given Phase 36 36-01-SUMMARY.md, when its frontmatter is read, then it contains `requirements-completed: [CO-01, CO-02]`

**AC-2:** Given Phase 36 36-02-SUMMARY.md, when its frontmatter is read, then it contains `requirements-completed: [CO-03, CO-04]`

**AC-3:** Given Phase 39 39-01-SUMMARY.md, when its frontmatter is read, then it contains `requirements-completed: [LIB-02]`

**AC-4:** Given Phase 39 39-02-SUMMARY.md, when its frontmatter is read, then it contains `requirements-completed: [LIB-01, LIB-03]`

**AC-5:** Given package.json, when its peerDependencies is read, then it contains `"next": ">=15.3.0"`

**AC-6:** Given package.json, when its peerDependenciesMeta is read, then it contains `"next": { "optional": true }`

**AC-7:** Given package.json, when its dependencies is read, then it does NOT contain `next`

**AC-8:** Given the project root, when `npm publish --dry-run` is run, then it exits 0

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Add requirements-completed to Phase 36 and 39 SUMMARYs</name>
  <ac_refs>AC-1, AC-2, AC-3, AC-4</ac_refs>
  <read_first>
    - .planning/phases/36-housekeeping-carry-overs/36-01-SUMMARY.md
    - .planning/phases/36-housekeeping-carry-overs/36-02-SUMMARY.md
    - .planning/phases/39-library-build-pipeline/39-01-SUMMARY.md
    - .planning/phases/39-library-build-pipeline/39-02-SUMMARY.md
  </read_first>
  <files>
    .planning/phases/36-housekeeping-carry-overs/36-01-SUMMARY.md,
    .planning/phases/36-housekeeping-carry-overs/36-02-SUMMARY.md,
    .planning/phases/39-library-build-pipeline/39-01-SUMMARY.md,
    .planning/phases/39-library-build-pipeline/39-02-SUMMARY.md
  </files>
  <action>
Add `requirements-completed` to the YAML frontmatter of each SUMMARY:

- **36-01-SUMMARY.md**: Add `requirements-completed: [CO-01, CO-02]` (Lighthouse BP+SEO 100, font-mono test path fixed)
- **36-02-SUMMARY.md**: Add `requirements-completed: [CO-03, CO-04]` (ESLint config, toolchain currency)
- **39-01-SUMMARY.md**: Add `requirements-completed: [LIB-02]` (ESM+CJS output via tsup)
- **39-02-SUMMARY.md**: Add `requirements-completed: [LIB-01, LIB-03]` (exports field, tree-shaking verified)

Insert the line after the existing `tags:` line in the frontmatter. Do not modify any other content.
  </action>
  <verify>
    <automated>grep "requirements-completed" .planning/phases/36-housekeeping-carry-overs/36-01-SUMMARY.md .planning/phases/36-housekeeping-carry-overs/36-02-SUMMARY.md .planning/phases/39-library-build-pipeline/39-01-SUMMARY.md .planning/phases/39-library-build-pipeline/39-02-SUMMARY.md</automated>
  </verify>
  <done>All 4 SUMMARYs have requirements-completed frontmatter</done>
</task>

<task type="auto">
  <name>Task 2: Move next from dependencies to peerDependencies</name>
  <ac_refs>AC-5, AC-6, AC-7, AC-8</ac_refs>
  <read_first>
    - package.json (current dependencies, peerDependencies, peerDependenciesMeta)
  </read_first>
  <files>package.json</files>
  <action>
1. Remove `"next"` from the `dependencies` object
2. Add `"next": ">=15.3.0"` to the `peerDependencies` object
3. Add `"next": { "optional": true }` to the `peerDependenciesMeta` object (create the field if it doesn't exist, or add to existing)
4. Run `npm publish --dry-run` to verify the package still publishes correctly
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(p.dependencies?.next) { console.error('FAIL: next still in dependencies'); process.exit(1); } if(!p.peerDependencies?.next) { console.error('FAIL: next not in peerDependencies'); process.exit(1); } if(!p.peerDependenciesMeta?.next?.optional) { console.error('FAIL: next not optional in peerDependenciesMeta'); process.exit(1); } console.log('PASS: next correctly in peerDependencies with optional:true');"</automated>
  </verify>
  <done>next moved to peerDependencies with optional:true, npm publish --dry-run passes</done>
</task>

</tasks>

<verification>
- All 4 SUMMARY files have requirements-completed in frontmatter
- package.json: next in peerDependencies, not in dependencies, optional:true in meta
- npm publish --dry-run exits 0
</verification>

<success_criteria>
- CO-01-04: Artifact tracking gap closed (SUMMARY frontmatter updated)
- LIB-01-03: Artifact tracking gap closed (SUMMARY frontmatter updated)
- LIB-03 adjacent: next moved to peerDependencies (no duplicate consumer install)
</success_criteria>
