---
phase: 44-copy-audit-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/blocks/stats-band.tsx
  - components/blocks/hero.tsx
  - components/blocks/marquee-band.tsx
  - app/page.tsx
  - app/init/page.tsx
  - app/opengraph-image.tsx
  - tests/phase-35-metadata.spec.ts
autonomous: true
requirements:
  - COP-01
  - COP-02
  - COP-03
  - COP-04
  - COP-05
  - COP-06

must_haves:
  truths:
    - "Component count shows 48 in every location across the site"
    - "Version string shows v1.7 on both hero and OG image"
    - "/init page says BUILT FOR REACT + NEXT.JS, not FRAMEWORK-AGNOSTIC"
    - "Marquee band contains a specific honest claim, not SHIP FASTER"
    - "No instance of 'and growing' filler exists in hero or meta description"
    - "Playwright metadata test passes clean with updated assertions"
  artifacts:
    - path: "components/blocks/stats-band.tsx"
      provides: "Corrected component count in stats band"
      contains: '"48"'
    - path: "components/blocks/hero.tsx"
      provides: "Corrected component count and version tag"
      contains: "48 SF COMPONENTS"
    - path: "components/blocks/marquee-band.tsx"
      provides: "Replaced SHIP FASTER with honest claim"
    - path: "app/page.tsx"
      provides: "Corrected meta description"
      contains: "48 SF components"
    - path: "app/init/page.tsx"
      provides: "Accurate framework claim"
      contains: "BUILT FOR REACT + NEXT.JS"
    - path: "app/opengraph-image.tsx"
      provides: "Corrected version and component count"
      contains: "COMPONENTS:48"
    - path: "tests/phase-35-metadata.spec.ts"
      provides: "Updated assertions matching new copy"
      contains: "COMPONENTS:48"
  key_links:
    - from: "tests/phase-35-metadata.spec.ts"
      to: "app/opengraph-image.tsx"
      via: "readFileSync source assertions"
      pattern: 'toContain.*COMPONENTS:48'
---

<objective>
Fix all false or stale copy claims across the SignalframeUX site. Reconcile component count to 48 (the actual non-lazy SF component count), update version strings from stale v1.5/v2.0.0 to v1.7, replace inaccurate marketing copy, remove filler phrases, and update Playwright test assertions to match.

Purpose: Every public-facing claim must be accurate before any aesthetic work begins in v1.7. False counts and stale versions undermine credibility.
Output: 7 files updated with accurate copy; Playwright metadata test passing green.
</objective>

<execution_context>
@/Users/greyaltaer/.claude/pde-os/engines/gsd/workflows/execute-plan.md
@/Users/greyaltaer/.claude/pde-os/engines/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-copy-audit-fixes/44-CONTEXT.md
@.planning/phases/44-copy-audit-fixes/44-RESEARCH.md
</context>

<acceptance_criteria>

**AC-1:** Given stats-band.tsx is rendered, when the component count stat is displayed, then the value is "48" (not "28").

**AC-2:** Given hero.tsx is rendered, when the component count line is displayed, then it reads "48 SF COMPONENTS" with no "AND GROWING" filler.

**AC-3:** Given hero.tsx is rendered, when the version tag is displayed, then it reads "SF//UX v1.7 · 2026" (not "v2.0.0").

**AC-4:** Given the OG image source is read, when version and component lines are checked, then "v1.7" appears (not "v1.5 — REDESIGN") and "COMPONENTS:48" appears (not "COMPONENTS:54").

**AC-5:** Given app/page.tsx metadata, when the description is read, then it contains "48 SF components" with no "and growing".

**AC-6:** Given app/init/page.tsx STEPS array, when step 05 description is read, then it contains "BUILT FOR REACT + NEXT.JS" (not "FRAMEWORK-AGNOSTIC").

**AC-7:** Given marquee-band.tsx is read, when the MARQUEE_TEXT constant is checked, then "SHIP FASTER" is absent and a specific honest claim is present.

**AC-8:** Given Playwright test phase-35-metadata.spec.ts runs, when the "OG image contains all locked content fields" test executes, then it passes green with assertions for "v1.7", "COMPONENTS:48" (not "v1.5", "REDESIGN", "COMPONENTS:54").

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Fix all copy across 6 source files</name>
  <files>
    components/blocks/stats-band.tsx,
    components/blocks/hero.tsx,
    components/blocks/marquee-band.tsx,
    app/page.tsx,
    app/init/page.tsx,
    app/opengraph-image.tsx
  </files>
  <ac_refs>AC-1, AC-2, AC-3, AC-4, AC-5, AC-6, AC-7</ac_refs>
  <read_first>
    components/blocks/stats-band.tsx,
    components/blocks/hero.tsx,
    components/blocks/marquee-band.tsx,
    app/page.tsx,
    app/init/page.tsx,
    app/opengraph-image.tsx
  </read_first>
  <action>
Edit each file with these exact changes. All copy must remain UPPERCASE in the DU/TDR typographic voice.

**1. `components/blocks/stats-band.tsx` line 1 (COP-01):**
Change `{ value: "28", label: "SF COMPONENTS", accent: false }` to `{ value: "48", label: "SF COMPONENTS", accent: false }`.

**2. `components/blocks/hero.tsx` line 93 (COP-01, COP-05):**
Change `28 SF COMPONENTS AND GROWING` to `48 SF COMPONENTS`. Remove " AND GROWING" entirely.

**3. `components/blocks/hero.tsx` line 121 (COP-02):**
Change `SF//UX v2.0.0 · 2026` to `SF//UX v1.7 · 2026`. Per user decision, hero version string must match OG image milestone label.

**4. `app/page.tsx` line 17 (COP-01, COP-05):**
Change `"A dual-layer design system with 28 SF components and growing. Signal layer for generative expression, Frame layer for deterministic structure."` to `"A dual-layer design system with 48 SF components. Signal layer for generative expression, Frame layer for deterministic structure."`.

**5. `app/init/page.tsx` line 109 (COP-03):**
In STEPS[4].description, change `SIGNALFRAMEUX\u2122 IS FRAMEWORK-AGNOSTIC BUT OPTIMIZED FOR NEXT.JS + VERCEL.` to `SIGNALFRAMEUX\u2122 IS BUILT FOR REACT + NEXT.JS + VERCEL.`. Keep the rest of the string unchanged (tree-shaking and bundle notes).

**6. `components/blocks/marquee-band.tsx` line 1-2 (COP-04):**
In the MARQUEE_TEXT constant, replace `SHIP FASTER` with `COMPOSABLE BY DESIGN`. The full string becomes:
`"SIGNAL//FRAME™ // DESIGN SYSTEM // BUILT FOR ENGINEERS // COMPOSABLE BY DESIGN // ACCEPT THE INTERFACE // "`
Also update the sr-only span text from `Ship faster.` to `Composable by design.` (line 10, inside the `<span className="sr-only">` element).

**7. `app/opengraph-image.tsx` line 64 (COP-02):**
Change `v1.5 — REDESIGN` to `v1.7 — TIGHTENING`. This matches the v1.7 milestone name.

**8. `app/opengraph-image.tsx` line 70 (COP-01):**
Change `COMPONENTS:54` to `COMPONENTS:48`.

Do NOT change the `77/77 ✓` line — it is a Lighthouse score readout that is close to current (78 perf baseline; 77 may reflect a prior production measurement). Do NOT change any other OG image content, layout, or styles. Do NOT introduce a shared copy constants file — the project pattern is inline hardcoded copy.
  </action>
  <verify>
    <automated>cd /Users/greyaltaer/code/projects/SignalframeUX && grep -q '"48"' components/blocks/stats-band.tsx && grep -q '48 SF COMPONENTS' components/blocks/hero.tsx && grep -qv 'AND GROWING' components/blocks/hero.tsx && grep -q 'v1.7' components/blocks/hero.tsx && grep -q '48 SF components' app/page.tsx && grep -qv 'and growing' app/page.tsx && grep -q 'BUILT FOR REACT + NEXT.JS' app/init/page.tsx && grep -qv 'FRAMEWORK-AGNOSTIC' app/init/page.tsx && grep -q 'COMPOSABLE BY DESIGN' components/blocks/marquee-band.tsx && grep -qv 'SHIP FASTER' components/blocks/marquee-band.tsx && grep -q 'COMPONENTS:48' app/opengraph-image.tsx && grep -q 'v1.7' app/opengraph-image.tsx && echo "ALL COPY CHECKS PASS"</automated>
  </verify>
  <acceptance_criteria>
    - stats-band.tsx contains `"48"` as the SF COMPONENTS value
    - hero.tsx line 93 reads `48 SF COMPONENTS` with no `AND GROWING`
    - hero.tsx line 121 reads `SF//UX v1.7 · 2026`
    - app/page.tsx meta description contains `48 SF components` with no `and growing`
    - app/init/page.tsx STEPS[4].description contains `BUILT FOR REACT + NEXT.JS` and does NOT contain `FRAMEWORK-AGNOSTIC`
    - marquee-band.tsx MARQUEE_TEXT contains `COMPOSABLE BY DESIGN` and does NOT contain `SHIP FASTER`
    - opengraph-image.tsx contains `v1.7` (not `v1.5`) and `COMPONENTS:48` (not `COMPONENTS:54`)
  </acceptance_criteria>
  <done>All 6 source files contain accurate, reconciled copy. Component count is 48 everywhere. Version is v1.7 everywhere. No false claims remain.</done>
</task>

<task type="auto">
  <name>Task 2: Update Playwright test assertions to match new copy</name>
  <files>tests/phase-35-metadata.spec.ts</files>
  <ac_refs>AC-8</ac_refs>
  <read_first>
    tests/phase-35-metadata.spec.ts,
    app/opengraph-image.tsx
  </read_first>
  <action>
Update the "LR-02: OG image contains all locked content fields" test (starting at line 32) to assert the new correct strings.

**Changes to assertions (lines 35-38):**

Line 35: `expect(src).toContain("v1.5");` → `expect(src).toContain("v1.7");`

Line 36: `expect(src).toContain("REDESIGN");` → `expect(src).toContain("TIGHTENING");`

Line 37: `expect(src).toContain("COMPONENTS:54");` → `expect(src).toContain("COMPONENTS:48");`

Line 38: `expect(src).toContain("77/77");` → Keep unchanged (value still present in OG source).

**Leave ALL other assertions untouched** — lines 34, 39-42 remain as-is. The structural/format guards (flex-only, inline-style-only, fs-based) in the earlier test are also unchanged.

After editing, run the test to confirm it passes:
`pnpm exec playwright test tests/phase-35-metadata.spec.ts`
  </action>
  <verify>
    <automated>cd /Users/greyaltaer/code/projects/SignalframeUX && grep -q 'toContain("v1.7")' tests/phase-35-metadata.spec.ts && grep -q 'toContain("TIGHTENING")' tests/phase-35-metadata.spec.ts && grep -q 'toContain("COMPONENTS:48")' tests/phase-35-metadata.spec.ts && pnpm exec playwright test tests/phase-35-metadata.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - phase-35-metadata.spec.ts line 35 asserts `toContain("v1.7")` not `toContain("v1.5")`
    - phase-35-metadata.spec.ts line 36 asserts `toContain("TIGHTENING")` not `toContain("REDESIGN")`
    - phase-35-metadata.spec.ts line 37 asserts `toContain("COMPONENTS:48")` not `toContain("COMPONENTS:54")`
    - `pnpm exec playwright test tests/phase-35-metadata.spec.ts` exits 0 with all tests passing
  </acceptance_criteria>
  <done>Playwright metadata test passes green. All assertions match the updated OG image source. No stale string assertions remain.</done>
</task>

</tasks>

<verification>
1. All 6 source files contain accurate copy — verified by grep checks in Task 1
2. Playwright metadata test passes green — verified by running the spec in Task 2
3. `pnpm build` succeeds with no errors (copy changes are string-only, no type/import changes)
</verification>

<success_criteria>
- Component count "48" appears in stats-band, hero, OG image, and meta description (4 locations per COP-01)
- Version "v1.7" appears in hero and OG image (COP-02)
- "FRAMEWORK-AGNOSTIC" is absent from /init, replaced with "BUILT FOR REACT + NEXT.JS" (COP-03)
- "SHIP FASTER" is absent from marquee, replaced with "COMPOSABLE BY DESIGN" (COP-04)
- "and growing" is absent from hero and meta description (COP-05)
- `pnpm exec playwright test tests/phase-35-metadata.spec.ts` exits 0 (COP-06)
</success_criteria>

<output>
After completion, create `.planning/phases/44-copy-audit-fixes/44-01-SUMMARY.md`
</output>
