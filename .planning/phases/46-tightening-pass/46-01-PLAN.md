---
phase: 46-tightening-pass
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/blocks/acquisition-section.tsx
  - components/blocks/acquisition-copy-button.tsx
  - components/blocks/inventory-section.tsx
  - components/blocks/components-explorer.tsx
  - components/layout/copy-button.tsx
  - components/layout/back-to-top.tsx
  - components/layout/global-effects.tsx
  - components/blocks/component-grid.tsx
  - components/sf/sf-button.tsx
  - components/sf/sf-card.tsx
  - components/sf/sf-tabs.tsx
  - components/sf/sf-toggle.tsx
  - components/sf/sf-toggle-group.tsx
  - components/sf/sf-navigation-menu.tsx
  - components/sf/sf-table.tsx
  - components/animation/signal-overlay.tsx
  - components/animation/hover-preview.tsx
  - components/animation/xray-reveal.tsx
  - components/animation/border-draw.tsx
  - components/layout/nav-overlay.tsx
autonomous: true
requirements:
  - TGH-02
  - TGH-04

must_haves:
  truths:
    - "Zero hardcoded animation duration values remain in components/blocks/, components/sf/, components/layout/, or components/animation/ code"
    - "All duration token references use the --sfx-duration-* prefix (not unprefixed --duration-*)"
    - "All easing token references use the --sfx-ease-* prefix (not unprefixed --ease-*)"
    - "sf-button hover uses --sfx-duration-fast (100ms), matching all other SF components"
  artifacts:
    - path: "components/sf/sf-button.tsx"
      provides: "Aligned hover duration"
      contains: "sfx-duration-fast"
    - path: "components/animation/signal-overlay.tsx"
      provides: "Prefixed duration references"
      contains: "sfx-duration-fast"
    - path: "components/layout/global-effects.tsx"
      provides: "Prefixed duration and easing references"
      contains: "sfx-duration-normal"
  key_links:
    - from: "components/sf/sf-button.tsx"
      to: "app/globals.css"
      via: "CSS custom property reference"
      pattern: "var\\(--sfx-duration-fast\\)"
    - from: "components/animation/signal-overlay.tsx"
      to: "app/globals.css"
      via: "CSS custom property reference"
      pattern: "var\\(--sfx-duration-fast\\)"
---

<objective>
Replace all 15 hardcoded animation durations with --sfx-duration-* token references and fix all unprefixed var(--duration-*) / var(--ease-*) references to use the correct --sfx- prefix. Align sf-button hover duration from --duration-normal to --sfx-duration-fast.

Purpose: Normalize all animation timing to the token system. The unprefixed var(--duration-*) references are a latent bug — no such custom properties exist, so they resolve to nothing. Fixing the prefix makes the system work as intended.
Output: 20 files updated with correct token references, zero hardcoded durations remaining.
</objective>

<execution_context>
@/Users/greyaltaer/.claude/pde-os/engines/gsd/workflows/execute-plan.md
@/Users/greyaltaer/.claude/pde-os/engines/gsd/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-tightening-pass/46-RESEARCH.md

<interfaces>
<!-- Token definitions from globals.css :root -->
Duration tokens (defined as --sfx-duration-*):
  --sfx-duration-instant: 34ms
  --sfx-duration-fast: 100ms  (was 67ms, updated to 100ms)
  --sfx-duration-normal: 200ms
  --sfx-duration-slow: 400ms
  --sfx-duration-glacial: 600ms

Easing tokens (defined as --sfx-ease-*):
  --sfx-ease-default: cubic-bezier(0.16, 1, 0.3, 1)
  --sfx-ease-hover: cubic-bezier(0.33, 1, 0.68, 1)
  --sfx-ease-spring: cubic-bezier(0.22, 0.61, 0.36, 1)

Pattern for Tailwind arbitrary values:
  duration-[var(--sfx-duration-fast)]    // 100ms
  duration-[var(--sfx-duration-normal)]  // 200ms
  duration-[var(--sfx-duration-instant)] // 34ms

Pattern for inline style transitions:
  transition: `property var(--sfx-duration-fast) var(--sfx-ease-default)`
</interfaces>
</context>

<acceptance_criteria>
- AC-1: grep for `duration-\[34ms\]` in components/ returns zero matches
- AC-2: grep for `duration-100[^0]` (bare Tailwind duration) in components/blocks/ and components/layout/ returns zero matches (excluding ui/)
- AC-3: grep for `duration-150` in components/blocks/ and components/layout/ returns zero matches (excluding ui/)
- AC-4: grep for `duration-200` in components/blocks/ and components/layout/ returns zero matches (excluding ui/)
- AC-5: grep for `var(--duration-` (unprefixed) in components/ returns zero matches (excluding ui/)
- AC-6: grep for `var(--ease-` (unprefixed, without sfx) in components/ returns zero matches (excluding ui/)
- AC-7: sf-button.tsx contains `sfx-duration-fast` and does NOT contain `duration-normal`
- AC-8: `pnpm build` completes without errors
</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Replace 15 hardcoded durations with token references</name>
  <files>
    components/blocks/acquisition-section.tsx
    components/blocks/acquisition-copy-button.tsx
    components/blocks/inventory-section.tsx
    components/blocks/components-explorer.tsx
    components/layout/copy-button.tsx
    components/layout/back-to-top.tsx
    components/layout/global-effects.tsx
    components/blocks/component-grid.tsx
  </files>
  <read_first>
    components/blocks/acquisition-section.tsx
    components/blocks/acquisition-copy-button.tsx
    components/blocks/inventory-section.tsx
    components/blocks/components-explorer.tsx
    components/layout/copy-button.tsx
    components/layout/back-to-top.tsx
    components/layout/global-effects.tsx
    components/blocks/component-grid.tsx
    .planning/phases/46-tightening-pass/46-RESEARCH.md
  </read_first>
  <ac_refs>AC-1, AC-2, AC-3, AC-4</ac_refs>
  <action>
Replace hardcoded Tailwind duration utilities with CSS custom property references. Follow the exact inventory from RESEARCH.md:

**Category A — `duration-[34ms]` to `duration-[var(--sfx-duration-instant)]`:**
1. acquisition-section.tsx: two instances of `duration-[34ms]`
2. acquisition-copy-button.tsx: one instance of `duration-[34ms]`
3. inventory-section.tsx: two instances of `duration-[34ms]`
4. components-explorer.tsx: two instances of `duration-[34ms]`

**Category B — Tailwind numeric durations to token vars:**
8. copy-button.tsx: `duration-150` -> `duration-[var(--sfx-duration-normal)]`
9. back-to-top.tsx: `duration-200` -> `duration-[var(--sfx-duration-normal)]`
10. global-effects.tsx: `duration-200` -> `duration-[var(--sfx-duration-normal)]`
11. component-grid.tsx: `duration-150` -> `duration-[var(--sfx-duration-fast)]`
12. component-grid.tsx: `duration-200` -> `duration-[var(--sfx-duration-normal)]`
13. components-explorer.tsx: `duration-200` -> `duration-[var(--sfx-duration-normal)]`
14. components-explorer.tsx: `duration-100` -> `duration-[var(--sfx-duration-fast)]`
15. components-explorer.tsx: `duration-150` -> `duration-[var(--sfx-duration-fast)]`

DO NOT modify files in components/ui/ (shadcn base — per project rules).
DO NOT modify GSAP `duration:` values in JS — those are animation choreography, not CSS transitions.
DO NOT modify globals.css @keyframes or animation shorthands — those are the token source of truth.
  </action>
  <verify>
    <automated>cd /Users/greyaltaer/code/projects/SignalframeUX && grep -rn 'duration-\[34ms\]' components/blocks/ components/layout/ && echo "FAIL: hardcoded 34ms found" || echo "PASS: no hardcoded 34ms"; grep -rn 'duration-100\b\|duration-150\|duration-200' components/blocks/ components/layout/ --include='*.tsx' && echo "FAIL: numeric durations found" || echo "PASS: no numeric durations"</automated>
  </verify>
  <done>All 15 hardcoded duration values replaced with --sfx-duration-* token references. Zero hardcoded durations remain in components/blocks/ and components/layout/.</done>
</task>

<task type="auto">
  <name>Task 2: Fix unprefixed var(--duration-*) and var(--ease-*) references + align sf-button</name>
  <files>
    components/sf/sf-button.tsx
    components/sf/sf-card.tsx
    components/sf/sf-tabs.tsx
    components/sf/sf-toggle.tsx
    components/sf/sf-toggle-group.tsx
    components/sf/sf-navigation-menu.tsx
    components/sf/sf-table.tsx
    components/animation/signal-overlay.tsx
    components/animation/hover-preview.tsx
    components/animation/xray-reveal.tsx
    components/animation/border-draw.tsx
    components/layout/global-effects.tsx
    components/layout/nav-overlay.tsx
  </files>
  <read_first>
    components/sf/sf-button.tsx
    components/sf/sf-card.tsx
    components/sf/sf-tabs.tsx
    components/sf/sf-toggle.tsx
    components/sf/sf-toggle-group.tsx
    components/sf/sf-navigation-menu.tsx
    components/sf/sf-table.tsx
    components/animation/signal-overlay.tsx
    components/animation/hover-preview.tsx
    components/animation/xray-reveal.tsx
    components/animation/border-draw.tsx
    components/layout/global-effects.tsx
    components/layout/nav-overlay.tsx
  </read_first>
  <ac_refs>AC-5, AC-6, AC-7, AC-8</ac_refs>
  <action>
Fix all unprefixed CSS custom property references (Category C from RESEARCH.md). These are latent bugs — no `--duration-*` or `--ease-*` properties exist without the `--sfx-` prefix.

**SF components — fix duration prefix:**
- sf-card.tsx: `var(--duration-fast)` -> `var(--sfx-duration-fast)`
- sf-tabs.tsx: `var(--duration-fast)` -> `var(--sfx-duration-fast)`
- sf-toggle.tsx: `var(--duration-fast)` -> `var(--sfx-duration-fast)`
- sf-toggle-group.tsx: `var(--duration-fast)` -> `var(--sfx-duration-fast)`
- sf-navigation-menu.tsx: `var(--duration-fast)` -> `var(--sfx-duration-fast)`
- sf-table.tsx: `var(--duration-fast)` -> `var(--sfx-duration-fast)`

**sf-button.tsx (TGH-04) — fix BOTH prefix AND value:**
- Change `duration-[var(--duration-normal)] ease-[var(--ease-default)]`
- To: `duration-[var(--sfx-duration-fast)] ease-[var(--sfx-ease-default)]`
- This simultaneously fixes the prefix bug and aligns hover to --sfx-duration-fast (100ms) matching all other SF components.

**Animation components — fix duration and easing prefix:**
- signal-overlay.tsx: 3 instances of `var(--duration-fast,67ms)` -> `var(--sfx-duration-fast)` (remove 67ms fallback — --sfx-duration-fast is always defined in :root)
- hover-preview.tsx: `var(--ease-default)` -> `var(--sfx-ease-default)`
- xray-reveal.tsx: `var(--ease-default)` -> `var(--sfx-ease-default)`
- border-draw.tsx: 4 instances of `var(--ease-default)` -> `var(--sfx-ease-default)`

**Layout components — fix duration and easing prefix:**
- global-effects.tsx: `var(--duration-normal)`, `var(--duration-fast)`, `var(--ease-default)` -> `var(--sfx-duration-normal)`, `var(--sfx-duration-fast)`, `var(--sfx-ease-default)`
- nav-overlay.tsx: `var(--ease-default)` -> `var(--sfx-ease-default)`

After all edits, run `pnpm build` to confirm no compilation errors.
  </action>
  <verify>
    <automated>cd /Users/greyaltaer/code/projects/SignalframeUX && grep -rn 'var(--duration-' components/ --include='*.tsx' | grep -v 'sfx-' | grep -v 'components/ui/' && echo "FAIL: unprefixed duration refs" || echo "PASS: all prefixed"; grep -rn 'var(--ease-' components/ --include='*.tsx' | grep -v 'sfx-' | grep -v 'components/ui/' && echo "FAIL: unprefixed ease refs" || echo "PASS: all prefixed"; grep 'sfx-duration-fast' components/sf/sf-button.tsx && echo "PASS: button aligned" || echo "FAIL: button not aligned"; pnpm build 2>&1 | tail -5</automated>
  </verify>
  <done>All var(--duration-*) references updated to var(--sfx-duration-*). All var(--ease-*) references updated to var(--sfx-ease-*). sf-button uses --sfx-duration-fast matching all other SF components. Build passes.</done>
</task>

</tasks>

<verification>
1. `grep -rn 'duration-\[34ms\]\|duration-100\b\|duration-150\|duration-200' components/blocks/ components/layout/ components/sf/ --include='*.tsx' | grep -v 'components/ui/'` returns zero matches
2. `grep -rn 'var(--duration-\|var(--ease-' components/ --include='*.tsx' | grep -v 'sfx-' | grep -v 'components/ui/'` returns zero matches
3. `grep 'sfx-duration-fast' components/sf/sf-button.tsx` returns a match
4. `pnpm build` succeeds
</verification>

<success_criteria>
Zero hardcoded animation durations remain in component code. All CSS custom property duration/easing references use the correct --sfx- prefix. sf-button hover is aligned to --sfx-duration-fast. Build passes without errors.
</success_criteria>

<output>
After completion, create `.planning/phases/46-tightening-pass/46-01-SUMMARY.md`
</output>
