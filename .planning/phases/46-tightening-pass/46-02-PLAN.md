---
phase: 46-tightening-pass
plan: 02
type: execute
wave: 2
depends_on: ["46-01"]
files_modified:
  - app/globals.css
  - components/blocks/shared-code-block.tsx
  - components/layout/copy-button.tsx
  - app/init/page.tsx
  - components/blocks/component-detail.tsx
  - components/animation/color-cycle-frame.tsx
  - components/layout/page-animations.tsx
  - components/animation/glsl-signal.tsx
autonomous: true
requirements:
  - TGH-01
  - TGH-03

must_haves:
  truths:
    - "Light mode --sfx-muted-foreground on bg-muted passes WCAG AA (>= 4.5:1 contrast ratio)"
    - "Zero hardcoded hex color values (#NNNNNN) remain in component style props (excluding Canvas 2D, OG image, error boundary)"
    - "Zero hardcoded oklch() values remain in component Tailwind classes (excluding token source globals.css)"
    - "color-cycle-frame.tsx cover color and page-animations.tsx base colors reference CSS custom properties via getComputedStyle"
  artifacts:
    - path: "app/globals.css"
      provides: "Contrast verification comment"
      contains: "5.81:1"
    - path: "components/blocks/shared-code-block.tsx"
      provides: "Token-based color references"
      contains: "text-muted-foreground"
    - path: "app/init/page.tsx"
      provides: "Token-based color references"
      contains: "text-muted-foreground"
  key_links:
    - from: "components/blocks/shared-code-block.tsx"
      to: "app/globals.css"
      via: "Tailwind utility class resolving to CSS custom property"
      pattern: "text-muted-foreground"
    - from: "components/animation/color-cycle-frame.tsx"
      to: "app/globals.css"
      via: "getComputedStyle reading CSS custom property"
      pattern: "getComputedStyle.*--sfx-"
---

<objective>
Replace all 7 hardcoded color values in component code with CSS custom property references and verify that light mode muted-foreground contrast passes WCAG AA.

Purpose: Eliminate hardcoded colors so the entire UI responds to theme tokens. Confirm the contrast ratio claim from research (5.81:1, already passing AA).
Output: 7 files updated with token-based color references, contrast verification comment added.
</objective>

<execution_context>
@/Users/greyaltaer/.claude/pde-os/engines/gsd/workflows/execute-plan.md
@/Users/greyaltaer/.claude/pde-os/engines/gsd/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-tightening-pass/46-RESEARCH.md

<interfaces>
<!-- Color tokens from globals.css :root -->
Core 5 colors (as CSS custom properties):
  --sfx-background: oklch(0.145 0 0)       // near-black
  --sfx-foreground: oklch(0.985 0 0)        // near-white
  --sfx-primary: oklch(0.65 0.3 350)        // magenta
  --sfx-secondary: oklch(0.250 0.005 298)   // dark gray
  --sfx-accent: oklch(0.65 0.3 350)         // magenta (same as primary)

Extended:
  --sfx-muted: oklch(0.200 0.005 298)
  --sfx-muted-foreground: oklch(0.650 0.010 298)
  --sfx-card: oklch(0.175 0.005 298)

Light mode overrides (in .light / :root without .dark):
  --sfx-muted: oklch(0.930 0.005 298)
  --sfx-muted-foreground: oklch(0.460 0.010 298)  // 5.81:1 on bg-muted = PASSES AA

Tailwind utility classes that map to tokens:
  text-muted-foreground -> var(--sfx-muted-foreground)
  text-foreground -> var(--sfx-foreground)
  bg-card -> var(--sfx-card)
  bg-background -> var(--sfx-background)
  text-background -> var(--sfx-background)
</interfaces>
</context>

<acceptance_criteria>
- AC-1: globals.css contains a comment confirming muted-foreground contrast ratio of 5.81:1 passes WCAG AA
- AC-2: shared-code-block.tsx contains NO inline `style={{ color: "#999999" }}` — uses `text-muted-foreground` class instead
- AC-3: copy-button.tsx contains NO inline `style={{ color: "#000000" }}` — uses a Tailwind class instead
- AC-4: app/init/page.tsx contains NO inline `style={{ color: "#999999" }}` — uses `text-muted-foreground` class instead
- AC-5: component-detail.tsx contains NO `bg-[oklch(` hardcoded background — uses `bg-card` or a CSS custom property reference
- AC-6: color-cycle-frame.tsx cover color reads from CSS custom property via getComputedStyle, not hardcoded oklch
- AC-7: page-animations.tsx base foreground/background colors read from CSS custom properties via getComputedStyle, not hardcoded hex/oklch
- AC-8: glsl-signal.tsx contains NO hardcoded `oklch(0.145` in inline CSS — uses var(--sfx-foreground) or similar token reference
- AC-9: `pnpm build` completes without errors
</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Verify WCAG AA contrast and replace hardcoded colors in static components</name>
  <files>
    app/globals.css
    components/blocks/shared-code-block.tsx
    components/layout/copy-button.tsx
    app/init/page.tsx
    components/blocks/component-detail.tsx
    components/animation/glsl-signal.tsx
  </files>
  <read_first>
    app/globals.css
    components/blocks/shared-code-block.tsx
    components/layout/copy-button.tsx
    app/init/page.tsx
    components/blocks/component-detail.tsx
    components/animation/glsl-signal.tsx
    .planning/phases/46-tightening-pass/46-RESEARCH.md
  </read_first>
  <ac_refs>AC-1, AC-2, AC-3, AC-4, AC-5, AC-8</ac_refs>
  <action>
**TGH-01 — Contrast verification (no color change needed):**
In globals.css, find the existing comment about muted-foreground contrast (around line 612). Update or replace it with an accurate comment:
```
/* TGH-01 verified: light --sfx-muted-foreground oklch(0.460 0.010 298) on --sfx-muted oklch(0.930 0.005 298) = 5.81:1 contrast ratio (PASSES WCAG AA >= 4.5:1) */
```
The existing comment claiming failure is incorrect per RESEARCH.md calculations. The light mode values already pass AA. No color value change is needed.

**TGH-03 — Static color replacements:**

1. **shared-code-block.tsx** (line ~23): Remove `style={{ color: "#999999" }}` and add `text-muted-foreground` to the className instead.

2. **copy-button.tsx** (line ~41): Remove `style={{ color: "#000000" }}` and replace with `text-background` className (or `text-primary-foreground` — whichever is semantically correct for a dark icon on a light button). Check the context of this element to pick the right semantic class.

3. **app/init/page.tsx** (line ~147): Remove `style={{ color: "#999999" }}` and add `text-muted-foreground` to the className instead.

4. **component-detail.tsx** (line ~302): Replace `bg-[oklch(0.12_0_0)]` with `bg-card` (which maps to `--sfx-card: oklch(0.175 0.005 298)` in dark mode). If the darker value is intentional for a code block background, use `bg-[var(--sfx-background)]` instead.

5. **glsl-signal.tsx** (line ~171): Replace hardcoded `oklch(0.145 0 0 / 0.06)` in inline CSS with a reference using `var(--sfx-foreground)`. Since this needs opacity, use: `color: oklch(from var(--sfx-foreground) l c h / 0.06)` or fall back to `background: var(--sfx-background)` with opacity if the relative color syntax isn't supported. If the relative syntax is risky for browser support, use `opacity: 0.06` on the element with `bg-foreground` class instead.

DO NOT modify: global-error.tsx, opengraph-image.tsx, icon.tsx (Satori), hero-mesh.tsx, canvas-cursor.tsx, token-viz.tsx (Canvas 2D), token-tabs.tsx (specimen display), dark-mode-toggle.tsx (intentional literal white).
  </action>
  <verify>
    <automated>cd /Users/greyaltaer/code/projects/SignalframeUX && grep '5.81:1' app/globals.css && echo "PASS: contrast comment" || echo "FAIL: no contrast comment"; grep -n 'style=.*color.*#' components/blocks/shared-code-block.tsx components/layout/copy-button.tsx app/init/page.tsx 2>/dev/null && echo "FAIL: hardcoded hex colors remain" || echo "PASS: no hardcoded hex"; grep -n 'bg-\[oklch' components/blocks/component-detail.tsx && echo "FAIL: hardcoded oklch bg" || echo "PASS: no hardcoded oklch bg"; grep -n 'oklch(0.145' components/animation/glsl-signal.tsx && echo "FAIL: hardcoded oklch in glsl-signal" || echo "PASS: glsl-signal clean"</automated>
  </verify>
  <done>Contrast ratio verified and documented. All 5 static hardcoded color values replaced with CSS custom property references or Tailwind utility classes.</done>
</task>

<task type="auto">
  <name>Task 2: Replace hardcoded colors in JS animation files + build verification</name>
  <files>
    components/animation/color-cycle-frame.tsx
    components/layout/page-animations.tsx
  </files>
  <read_first>
    components/animation/color-cycle-frame.tsx
    components/layout/page-animations.tsx
    .planning/phases/46-tightening-pass/46-RESEARCH.md
  </read_first>
  <ac_refs>AC-6, AC-7, AC-9</ac_refs>
  <action>
These files use hardcoded color values in JS for GSAP animations. Per RESEARCH.md recommendation:

**color-cycle-frame.tsx:**
- The cover color `oklch(0.145 0 0)` at line ~81 should read from `--sfx-foreground` via getComputedStyle:
  ```typescript
  const coverColor = getComputedStyle(document.documentElement).getPropertyValue('--sfx-foreground').trim() || 'oklch(0.145 0 0)';
  ```
- The palette array (lines ~52-58) contains curated artistic color choices. Per RESEARCH.md: leave the palette array as hardcoded (these are intentional artistic choices), but make the base magenta entry reference `--sfx-primary`:
  ```typescript
  const primaryColor = getComputedStyle(document.documentElement).getPropertyValue('--sfx-primary').trim() || 'oklch(0.65 0.3 350)';
  ```
  Then use `primaryColor` in the palette array at the position that currently has `oklch(0.65 0.3 350)`.
- Compute these values once at component mount (inside useEffect or useLayoutEffect), not per animation frame.

**page-animations.tsx:**
- Lines ~457-458 contain hardcoded `"#fff"`, `"oklch(0.145 0 0)"`, and `"oklch(0.65 0.3 350)"`.
- Replace with getComputedStyle reads:
  ```typescript
  const fg = getComputedStyle(document.documentElement).getPropertyValue('--sfx-foreground').trim();
  const bg = getComputedStyle(document.documentElement).getPropertyValue('--sfx-background').trim();
  const primary = getComputedStyle(document.documentElement).getPropertyValue('--sfx-primary').trim();
  ```
- Compute once at function/effect scope, not per animation. Use fallbacks for safety:
  `fg || 'oklch(0.985 0 0)'`, `bg || 'oklch(0.145 0 0)'`, `primary || 'oklch(0.65 0.3 350)'`

After all edits, run `pnpm build` to confirm no compilation errors.
  </action>
  <verify>
    <automated>cd /Users/greyaltaer/code/projects/SignalframeUX && grep -n 'getComputedStyle.*--sfx-' components/animation/color-cycle-frame.tsx && echo "PASS: color-cycle uses tokens" || echo "FAIL: no token read"; grep -n 'getComputedStyle.*--sfx-' components/layout/page-animations.tsx && echo "PASS: page-animations uses tokens" || echo "FAIL: no token read"; pnpm build 2>&1 | tail -5</automated>
  </verify>
  <done>color-cycle-frame.tsx and page-animations.tsx read base/cover/primary colors from CSS custom properties via getComputedStyle. Build passes without errors.</done>
</task>

</tasks>

<verification>
1. `grep -rn 'style=.*color.*#' components/blocks/ components/layout/ app/init/ --include='*.tsx' | grep -v 'components/ui/'` returns zero matches
2. `grep -rn 'bg-\[oklch' components/blocks/ --include='*.tsx'` returns zero matches
3. `grep -n 'getComputedStyle.*--sfx-' components/animation/color-cycle-frame.tsx components/layout/page-animations.tsx` returns matches in both files
4. `grep '5.81:1' app/globals.css` returns a match
5. `pnpm build` succeeds
</verification>

<success_criteria>
Light mode contrast verified and documented (5.81:1 >= 4.5:1 WCAG AA). All 7 hardcoded color values in component code replaced with CSS custom property references. Build passes without errors.
</success_criteria>

<output>
After completion, create `.planning/phases/46-tightening-pass/46-02-SUMMARY.md`
</output>
