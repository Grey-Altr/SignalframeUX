---
phase: 49-grain-idle-escalation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - GRN-01
  - GRN-02
  - GRN-03
  - GRN-04
  - VRG-02

must_haves:
  truths:
    - "REQUIREMENTS.md status for GRN-01 reflects shipped reality (grain baseline 0.03 at intensity 0 via SIG-03 curve at components/layout/global-effects.tsx:36)"
    - "REQUIREMENTS.md status for GRN-02 reflects shipped reality: hook shipped (40e2f0d) but consumer removed by a260238 for PRF-02 Lighthouse gate — Obsolete with rationale"
    - "REQUIREMENTS.md status for GRN-03 reflects shipped reality: relative-offset pattern documented in 40e2f0d commit message but consumer removed by a260238 — Obsolete with rationale"
    - "REQUIREMENTS.md status for GRN-04 reflects shipped reality: temporal gate cannot be satisfied retroactively for already-shipped grain code — Obsolete with rationale"
    - "REQUIREMENTS.md status for VRG-02 reflects shipped reality: temporal gate cannot be satisfied retroactively (same as GRN-04) — Obsolete with rationale"
    - "ROADMAP.md Phase 49 plan list points at 49-01-PLAN.md (not stale 44-01-PLAN.md inherited reference)"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Status annotations for GRN-01..04 and VRG-02 (1 RATIFIED + 4 OBSOLETE)"
      contains: "RATIFIED 2026-04-25"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated traceability matrix rows for Phase 49 requirements"
      contains: "GRN-01 | Phase 49 | Ratified"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 49 plan reference fixed"
      contains: "49-01-PLAN.md — Grain ratification + idle escalation closure"
  key_links:
    - from: "REQUIREMENTS.md GRN-01 line"
      to: "components/layout/global-effects.tsx:36 (grain opacity formula)"
      via: "file:line citation in body status line (same line that ratified SIG-03)"
      pattern: "0.03 + 0.05"
    - from: "REQUIREMENTS.md GRN-02 line"
      to: "hooks/use-idle-escalation.ts:28 (hook exists) + commit a260238 (consumer removed for PRF-02)"
      via: "file:line + commit-hash citation in OBSOLETE annotation"
      pattern: "useIdleEscalation"
---

<objective>
Audit Phase 49's five requirements (GRN-01..04 + VRG-02) against shipped code, ratify GRN-01 against `components/layout/global-effects.tsx:36`, and mark GRN-02 / GRN-03 / GRN-04 / VRG-02 OBSOLETE with explicit rationale citing the architectural decisions that supersede them. Fix the stale `44-01-PLAN.md` reference under Phase 49 in `ROADMAP.md`. No source code changes — pure documentation reconciliation per `feedback_ratify_reality_bias.md` and `project_overdue_lockin_mode.md`.

Purpose: Phase 49 has three distinct artifacts in shipped reality — (1) grain baseline curve `0.03 + 0.05 * Math.log10(...)` at `global-effects.tsx:36` ratified GRN-01 trivially via the same line that already ratified SIG-03, (2) `useIdleEscalation` hook shipped at `hooks/use-idle-escalation.ts:28` in commit `40e2f0d` along with a 3-phase consumer (`IdleOverlay`) inside `global-effects.tsx`, and (3) the `IdleOverlay` consumer was deliberately removed in commit `a260238` ("fix(cleanup): remove heavy SIGNAL effects from GlobalEffects render for performance gate") to ship Lighthouse Performance compliance for PRF-02. The hook persists as library-only code with no consumer. GRN-02 and GRN-03 describe behavior that no longer renders; per `feedback_ratify_reality_bias.md` three-question test, the requirements are superseded by the architectural decision to prioritize launch-gate performance. GRN-04 and VRG-02 are temporal gates ("baselines captured BEFORE grain changes") that cannot be satisfied retroactively for code that has already shipped.

Output: Five body-status flips in `.planning/REQUIREMENTS.md` (1 → Ratified with file:line citation; 4 → Obsolete with supersedence-citation), five matrix-row updates, and one `ROADMAP.md` plan-reference fix.
</objective>

<execution_context>
@/Users/greyaltaer/.claude/pde-os/engines/gsd/workflows/execute-plan.md
@/Users/greyaltaer/.claude/pde-os/engines/gsd/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.handoffs/v1.7-ratification.md
@.planning/phases/48-intensity-bridge/48-01-SUMMARY.md

<scope_decision>
The five Phase 49 requirements split 1-Ratified / 4-Obsolete:

| Req | Verdict | Citation / Rationale |
|-----|---------|----------------------|
| GRN-01 | RATIFY | `components/layout/global-effects.tsx:36` — `const grainOpacity = 0.03 + 0.05 * Math.log10(1 + i * 9);` At i=0 the formula evaluates to 0.03, within spec range 0.03–0.05; the curve is the SIG-03 ratified line, so "escalates dynamically via SIG-03 curve" is also satisfied. Same line, dual purpose. |
| GRN-02 | OBSOLETE | Hook shipped: `hooks/use-idle-escalation.ts:28` (`export function useIdleEscalation(thresholds: IdleThreshold[]): number`) introduced in commit `40e2f0d feat(49): useIdleEscalation hook + 3-phase idle system`. Consumer (`IdleOverlay` with 8s/20s/45s thresholds + relative scanline boost) was wired in the same commit but DELETED in commit `a260238 fix(cleanup): remove heavy SIGNAL effects from GlobalEffects render for performance gate` to ship PRF-02 Lighthouse Performance compliance. Hook is currently library-only with zero consumers. Per `feedback_ratify_reality_bias.md`: shipped reality = hook exists, idle escalation does not run. The performance trade-off was deliberate; PRF-02 is a launch-gate requirement that supersedes GRN-02. |
| GRN-03 | OBSOLETE | Relative-offset pattern (`+0.03` scanline delta) was documented in commit `40e2f0d`'s message and implemented inside the deleted `IdleOverlay` consumer. Pattern persists in commit history as encoded intent for any future re-wiring; no current code path satisfies it because the consumer was removed by `a260238`. Same supersedence as GRN-02. |
| GRN-04 | OBSOLETE | Temporal gate ("Chromatic visual baselines captured before grain changes") cannot be satisfied retroactively. The grain code ships at `global-effects.tsx:36` and predates Chromatic wiring (commits `54db119 chore(48): install Chromatic` + `abe0cfd chore(49): wire Chromatic addon into Storybook config` postdate the grain logic). The forward-looking version of this gate is implicit in VRG-01 (Chromatic infrastructure ratified Phase 48); any future grain change can be gated against a fresh `pnpm chromatic` run. Within v1.7 lock posture per `project_overdue_lockin_mode.md`, no further grain changes are scoped — gate is moot. |
| VRG-02 | OBSOLETE | Same temporal-gate impossibility as GRN-04, scoped at the visual-regression family. Forward-looking baseline capture remains available via VRG-01's ratified Chromatic wiring; the "before Phase 49" temporal phrasing is unsatisfiable retroactively. |

**Key supporting commits (all on `main`):**
- `40e2f0d` — `feat(49): useIdleEscalation hook + 3-phase idle system` — hook + consumer shipped together
- `a260238` — `fix(cleanup): remove heavy SIGNAL effects from GlobalEffects render for performance gate` — consumer removed for PRF-02
- `54db119` — `chore(48): install Chromatic for visual regression tracking`
- `abe0cfd` — `chore(49): wire Chromatic addon into Storybook config`

**Pre-plan verification of GRN-02 supersedence:** `grep -rn "useIdleEscalation" components/ app/ stories/` returns ONLY the hook definition (`hooks/use-idle-escalation.ts:28`) — zero consumers across the entire source tree. The `IdleOverlay` JSDoc reference at `components/layout/global-effects.tsx:201` ("via IdleOverlay's resetIdle") is stale doc-comment residue from before the cleanup commit; it does not name a live identifier.

**Pattern precedent:** Phase 47 split 1-Complete / 2-Ratified / 1-Obsolete (VPT-03 was Obsolete with rationale). This plan follows the same lean ratification pattern. Phase 48 was atypical in landing 6/6 Ratified — Phase 49 returns to mixed-verdict normal, which is the expected v1.7 audit outcome.
</scope_decision>

<existing_state>
REQUIREMENTS.md (lines 268-271 for GRN-01..04, line 321 for VRG-02) all marked `[ ]` Pending.

REQUIREMENTS.md traceability matrix:
- Lines 357-360: GRN-01..04 all `Pending`
- Line 392: VRG-02 `Pending`

ROADMAP.md line 730 (under Phase 49 entry):
```
- [ ] 44-01-PLAN.md — Fix all copy across 6 source files + update Playwright assertions
```
This is a copy-paste artifact from Phase 44 (same lineage as Phases 50, 52, 53, 54, 55, 56 which still carry the same stale ref). Fix only the Phase 49 occurrence.

ROADMAP.md line 506 already shows Phase 49 with `[x]` and "completed 2026-04-11, 1/1 plans" — that line conflates code shipping (yes, grain + hook landed 2026-04-11) with traceability shipping (no, that's this commit). Like Phase 48, the plan-list checkbox at line 730 is the closing artifact.

Not modifying STATE.md in this commit. The roll-up update is a campaign-level concern, handled when all v1.7 phases finish ratifying (or by the `gsd-tools phase complete 49` invocation, separately).
</existing_state>

</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Ratify GRN-01 + mark GRN-02/03/04 + VRG-02 Obsolete in REQUIREMENTS.md (body + matrix)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md:268-271 (GRN-01..04 body lines)
    - .planning/REQUIREMENTS.md:321 (VRG-02 body line)
    - .planning/REQUIREMENTS.md:357-360 (GRN matrix rows)
    - .planning/REQUIREMENTS.md:392 (VRG-02 matrix row)
    - components/layout/global-effects.tsx:36 (proof for GRN-01)
    - hooks/use-idle-escalation.ts:28 (proof of GRN-02 hook artifact)
    - git show --stat a260238 (proof of GRN-02 consumer removal)
  </read_first>
  <action>
    **Edit 1 — body status lines for GRN-01..04 (lines 268-271), replace exactly:**

    ```
    - [ ] **GRN-01**: Grain baseline stays at 0.03-0.05; intensity dial escalates dynamically via SIG-03 curve
    - [ ] **GRN-02**: Idle escalation refactored to `useIdleEscalation(thresholds[])` with 3 phases (8s grain drift, 20s scan emphasis, 45s glitch burst + auto-reset)
    - [ ] **GRN-03**: Escalation targets use relative offsets (currentValue + delta), not absolute values
    - [ ] **GRN-04**: Chromatic visual baselines captured before grain changes
    ```

    With exactly:

    ```
    - [x] **GRN-01**: Grain baseline stays at 0.03-0.05; intensity dial escalates dynamically via SIG-03 curve — RATIFIED 2026-04-25, shipped at `components/layout/global-effects.tsx:36` (`0.03 + 0.05 * Math.log10(1 + i * 9)` evaluates to 0.03 at i=0 — within spec; same line that ratified SIG-03)
    - [x] **GRN-02**: Idle escalation refactored to `useIdleEscalation(thresholds[])` with 3 phases (8s grain drift, 20s scan emphasis, 45s glitch burst + auto-reset) — OBSOLETE 2026-04-25; hook shipped at `hooks/use-idle-escalation.ts:28` (commit `40e2f0d`) but the 3-phase consumer (`IdleOverlay`) was deliberately deleted in commit `a260238` ("remove heavy SIGNAL effects from GlobalEffects render for performance gate") to ship PRF-02 Lighthouse Performance compliance. Hook is currently library-only; zero consumers. Performance gate supersedes idle-escalation animation per v1.7 launch-gate hierarchy.
    - [x] **GRN-03**: Escalation targets use relative offsets (currentValue + delta), not absolute values — OBSOLETE 2026-04-25; relative-offset pattern was implemented in `IdleOverlay` (commit `40e2f0d`'s scanline `+0.03` delta) but the consumer was removed by `a260238`. Pattern persists in commit history as encoded intent for any future re-wiring; same supersedence as GRN-02.
    - [x] **GRN-04**: Chromatic visual baselines captured before grain changes — OBSOLETE 2026-04-25; temporal gate cannot be satisfied retroactively. Grain code (`global-effects.tsx:36`) predates Chromatic wiring (commits `54db119` + `abe0cfd`). Forward-looking baseline capture remains available via VRG-01's ratified Chromatic infrastructure; within v1.7 lock posture no further grain changes are scoped, so the gate is moot.
    ```

    **Edit 2 — body status line for VRG-02 (line 321), replace exactly:**

    ```
    - [ ] **VRG-02**: Visual baselines captured before Phase 49 (grain changes)
    ```

    With exactly:

    ```
    - [x] **VRG-02**: Visual baselines captured before Phase 49 (grain changes) — OBSOLETE 2026-04-25; same temporal-gate impossibility as GRN-04. Forward-looking baseline capture remains available via VRG-01's ratified Chromatic wiring; "before Phase 49" temporal phrasing is unsatisfiable retroactively for already-shipped grain.
    ```

    **Edit 3 — traceability matrix GRN rows (lines 357-360), replace exactly:**

    ```
    | GRN-01 | Phase 49 | Pending |
    | GRN-02 | Phase 49 | Pending |
    | GRN-03 | Phase 49 | Pending |
    | GRN-04 | Phase 49 | Pending |
    ```

    With exactly:

    ```
    | GRN-01 | Phase 49 | Ratified |
    | GRN-02 | Phase 49 | Obsolete |
    | GRN-03 | Phase 49 | Obsolete |
    | GRN-04 | Phase 49 | Obsolete |
    ```

    **Edit 4 — traceability matrix VRG-02 row (line 392), replace exactly:**

    ```
    | VRG-02 | Phase 49 | Pending |
    ```

    With exactly:

    ```
    | VRG-02 | Phase 49 | Obsolete |
    ```

    Do NOT touch any other line in REQUIREMENTS.md. Do NOT modify SIG-* (already Ratified Phase 48), VHS-*, HLF-*, CIR-*, MSH-*, PTL-*, GLT-*, SYM-*, VRG-03, or PRF-* rows — those are different-phase scope.
  </action>
  <verify>
    <automated>grep -c "^- \[x\] \*\*GRN-0[1-4]\*\*" .planning/REQUIREMENTS.md | grep -q "^4$" &amp;&amp; grep -q "^- \[x\] \*\*VRG-02\*\*" .planning/REQUIREMENTS.md &amp;&amp; grep -c "OBSOLETE 2026-04-25" .planning/REQUIREMENTS.md | grep -qE "^[4-9]$|^[1-9][0-9]+$" &amp;&amp; grep -qE "^\| GRN-01 \| Phase 49 \| Ratified \|" .planning/REQUIREMENTS.md &amp;&amp; grep -qE "^\| GRN-02 \| Phase 49 \| Obsolete \|" .planning/REQUIREMENTS.md &amp;&amp; grep -qE "^\| GRN-03 \| Phase 49 \| Obsolete \|" .planning/REQUIREMENTS.md &amp;&amp; grep -qE "^\| GRN-04 \| Phase 49 \| Obsolete \|" .planning/REQUIREMENTS.md &amp;&amp; grep -qE "^\| VRG-02 \| Phase 49 \| Obsolete \|" .planning/REQUIREMENTS.md &amp;&amp; ! grep -qE "^\| GRN-0[1-4] \| Phase 49 \| Pending \|" .planning/REQUIREMENTS.md &amp;&amp; ! grep -qE "^\| VRG-02 \| Phase 49 \| Pending \|" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - All 4 GRN body lines start with `- [x]` (count = 4)
    - VRG-02 body line starts with `- [x]` (count = 1)
    - GRN-01 body line contains literal `RATIFIED 2026-04-25` and cites `components/layout/global-effects.tsx:36`
    - GRN-02 body line contains literal `OBSOLETE 2026-04-25` and cites both `hooks/use-idle-escalation.ts:28` and commit `a260238`
    - GRN-03 body line contains literal `OBSOLETE 2026-04-25` and references commits `40e2f0d` + `a260238`
    - GRN-04 body line contains literal `OBSOLETE 2026-04-25` and explains the temporal-gate rationale
    - VRG-02 body line contains literal `OBSOLETE 2026-04-25` and explains the temporal-gate rationale
    - Matrix row `| GRN-01 | Phase 49 | Ratified |` exists
    - Matrix rows `| GRN-0[2-4] | Phase 49 | Obsolete |` exist (3 rows)
    - Matrix row `| VRG-02 | Phase 49 | Obsolete |` exists
    - Zero matrix rows match `^\| GRN-0[1-4] \| Phase 49 \| Pending \|`
    - Zero matrix rows match `^\| VRG-02 \| Phase 49 \| Pending \|`
    - SIG-*, VHS-*, HLF-*, CIR-*, MSH-*, PTL-*, GLT-*, SYM-*, VRG-03, PRF-* matrix rows unchanged
  </acceptance_criteria>
  <done>
    REQUIREMENTS.md reflects Phase 49 reality: GRN-01 RATIFIED with file:line citation; GRN-02/03/04 + VRG-02 OBSOLETE with explicit supersedence rationale (PRF-02 launch-gate prioritization for GRN-02/03; temporal-gate impossibility for GRN-04/VRG-02). Body status lines and matrix rows are consistent. No future-phase rows touched.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Fix stale Phase 49 plan reference in ROADMAP.md</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md:718-731 (the Phase 49 entry — confirm the stale `44-01-PLAN.md` reference at line 730)
  </read_first>
  <action>
    Edit `.planning/ROADMAP.md` to replace the Phase 49 plan reference. The exact 3-line block under the Phase 49 success-criteria reads:

    ```
    **Plans:** 1 plan
    Plans:
    - [ ] 44-01-PLAN.md — Fix all copy across 6 source files + update Playwright assertions
    ```

    Replace with exactly:

    ```
    **Plans:** 1 plan
    Plans:
    - [x] 49-01-PLAN.md — Grain ratification + idle escalation closure
    ```

    Critical scoping note: this exact 3-line block also appears under Phases 50, 52, 53, 54, 55, 56. DO NOT touch any of those — they are out of scope for this phase. The Edit must be made unique by including surrounding context (use the preceding `### Phase 49: Grain + Idle Escalation + Visual Baseline` heading + the success-criteria block above the plan list as the disambiguator). Pass enough surrounding lines to make `old_string` unique to the Phase 49 occurrence only.

    The plan checkbox is set to `[x]` (not `[ ]`) because by the time this plan commits, the plan IS complete — the ratification IS the work, and it's done in this commit.
  </action>
  <verify>
    <automated>grep -A 20 "### Phase 49: Grain" .planning/ROADMAP.md | grep -q "49-01-PLAN.md — Grain ratification + idle escalation closure" &amp;&amp; grep -A 20 "### Phase 49: Grain" .planning/ROADMAP.md | head -25 | grep -vq "44-01-PLAN.md" &amp;&amp; grep -A 20 "### Phase 50: VHS" .planning/ROADMAP.md | grep -q "44-01-PLAN.md"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -A 20 "### Phase 49: Grain" .planning/ROADMAP.md` contains the line `- [x] 49-01-PLAN.md — Grain ratification + idle escalation closure`
    - The Phase 49 entry no longer contains `44-01-PLAN.md`
    - Phase 50 entry STILL contains `44-01-PLAN.md` (out of scope, must remain unchanged for that phase's own ratification commit)
    - Phases 52, 53, 54, 55, 56 still contain `44-01-PLAN.md` references (out of scope)
    - Total count of `49-01-PLAN.md` occurrences in ROADMAP.md is exactly 1
    - Total count of `44-01-PLAN.md` occurrences in ROADMAP.md decremented by exactly 1 from prior state (was 9 after Phase 48 ratification — should now be 8)
  </acceptance_criteria>
  <done>
    Phase 49 in ROADMAP.md correctly points at this plan's filename with the checkbox ticked. All other phases' identical stale references deliberately preserved — they belong to those phases' future ratification commits.
  </done>
</task>

</tasks>

<verification>
After both tasks land, run from repo root:

```bash
# Task 1 — REQUIREMENTS.md status updates
grep -c "^- \[x\] \*\*GRN-0[1-4]\*\*" .planning/REQUIREMENTS.md  # expect 4
grep -c "^- \[x\] \*\*VRG-02\*\*" .planning/REQUIREMENTS.md       # expect 1
grep -E "^\| GRN-01 \| Phase 49 \| Ratified \|" .planning/REQUIREMENTS.md  # expect 1 row
grep -cE "^\| GRN-0[2-4] \| Phase 49 \| Obsolete \|" .planning/REQUIREMENTS.md  # expect 3
grep -E "^\| VRG-02 \| Phase 49 \| Obsolete \|" .planning/REQUIREMENTS.md      # expect 1 row
grep -cE "^\| (GRN-0[1-4]|VRG-02) \| Phase 49 \| Pending \|" .planning/REQUIREMENTS.md  # expect 0

# Task 2 — ROADMAP plan reference
grep -A 20 "### Phase 49: Grain" .planning/ROADMAP.md | grep "49-01-PLAN.md"
grep -A 20 "### Phase 50: VHS" .planning/ROADMAP.md | grep "44-01-PLAN.md"  # phase 50 stale ref deliberately preserved

# Sanity — no source code mutations
git diff --name-only main...HEAD | grep -vE "^\.planning/|^\.handoffs/" || true  # expect empty (only docs touched on this branch)
```

No Storybook/Playwright/tsc gates needed — this plan touches only `.planning/` markdown files. The `useIdleEscalation` hook itself is not exercised in any test (it has no consumer); the OBSOLETE annotation captures its library-only state.
</verification>

<success_criteria>
- Both tasks' acceptance criteria pass
- 5 requirements (GRN-01..04 + VRG-02) flip from `Pending` to either `Ratified` (1) or `Obsolete` (4) with file:line + commit-hash citations
- ROADMAP.md Phase 49 entry references `49-01-PLAN.md` with `[x]` checkbox
- ROADMAP.md Phases 50+ stale `44-01-PLAN.md` references deliberately preserved
- Zero source-code changes (only `.planning/` markdown edits)
- No new tokens, components, dependencies, or tests added — strictly within v1.0 lock + v1.7 ratification scope
</success_criteria>

<output>
After completion, create `.planning/phases/49-grain-idle-escalation/49-01-SUMMARY.md` matching the structure of `.planning/phases/48-intensity-bridge/48-01-SUMMARY.md`. Sections required:

- YAML frontmatter: `phase: 49-grain-idle-escalation`, `plan: 01`, `status: complete`, `completed: "2026-04-25"`, `commit: <hash>`
- `## What was done` — one-paragraph summary noting the 1-Ratified / 4-Obsolete split and the architectural reason (PRF-02 cleanup commit + temporal-gate impossibility)
- `## Changes made` — three subsections:
  - `**GRN-01 — Ratified:**` — file:line citation (same line as SIG-03)
  - `**GRN-02..04 + VRG-02 — Obsolete:**` — table with commit-hash supersedence chain for each
  - `**Bookkeeping:**` — ROADMAP.md plan reference fix
- `## Verification` — table with each acceptance criterion + outcome
- `## Notes` — call out:
  - The `40e2f0d` → `a260238` ship-then-cleanup pattern as the canonical example of "feature lost to launch gate" — useful precedent for any future case where a v1.7 effect is planned but cuts for performance
  - The `useIdleEscalation` hook is library-only / dead code right now; if a future phase wants to re-introduce idle escalation, the wiring blueprint is preserved at `40e2f0d`'s diff
  - Carry-forward intel for Phase 50 (VHS) ratification: the JSDoc references to `IdleOverlay` at `global-effects.tsx:165-186, 201` are stale comment residue from the cleanup commit — Phase 50 audit may want to flag those for a separate doc-comment cleanup pass (out of scope here)
  - That this is the third ratification in the v1.7 audit campaign (47 → 48 → 49)
</output>
</content>
</invoke>