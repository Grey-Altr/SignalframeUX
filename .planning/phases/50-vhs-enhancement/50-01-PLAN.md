---
phase: 50-vhs-enhancement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - VHS-01
  - VHS-02
  - VHS-03
  - VHS-04
  - VHS-05
  - VHS-06

must_haves:
  truths:
    - "REQUIREMENTS.md status for VHS-01 reflects shipped reality (chromatic aberration RGB offset, intensity-gated > 0.3, scaled via SIG-02 derived property at vhs-overlay.tsx:153-178 + globals.css:2195-2210)"
    - "REQUIREMENTS.md status for VHS-02 reflects shipped reality (sf-vhs-jitter keyframes at globals.css:2213-2225, steps(4, end) tape-dropout register)"
    - "REQUIREMENTS.md status for VHS-03 reflects shipped reality: CSS scaffold at globals.css:2230-2246 + JSX container at vhs-overlay.tsx:236, but runtime activator was tied to cut idle-escalation consumer per a260238 — Obsolete with rationale"
    - "REQUIREMENTS.md status for VHS-04 reflects shipped reality (radial-gradient frame-edge vignette at globals.css:2249-2253)"
    - "REQUIREMENTS.md status for VHS-05 reflects shipped reality (paired backdrop-filter / -webkit-backdrop-filter with literal values at globals.css:2108-2109 + 2125-2126; zero var() refs in any backdrop-filter declaration)"
    - "REQUIREMENTS.md status for VHS-06 reflects shipped reality: process gate cannot be satisfied retroactively — Obsolete with rationale (same precedent as GRN-04 / VRG-02)"
    - "ROADMAP.md Phase 50 plan list points at 50-01-PLAN.md (not stale 44-01-PLAN.md inherited reference)"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Status annotations for VHS-01..06 (4 RATIFIED + 2 OBSOLETE)"
      contains: "RATIFIED 2026-04-25"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated traceability matrix rows for Phase 50 requirements"
      contains: "VHS-01 | Phase 50 | Ratified"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 50 plan reference fixed"
      contains: "50-01-PLAN.md — VHS ratification"
  key_links:
    - from: "REQUIREMENTS.md VHS-01 line"
      to: "components/animation/vhs-overlay.tsx:153-178 (chromatic-opacity derivation) + app/globals.css:2195-2210 (red+cyan layers)"
      via: "file:line citation in body status line; reads SIG-02 derived property"
      pattern: "--sfx-vhs-chromatic-opacity"
    - from: "REQUIREMENTS.md VHS-03 line"
      to: "app/globals.css:2230-2246 (CSS scaffold) + commit a260238 (consumer cut for PRF-02)"
      via: "file:line + commit-hash citation in OBSOLETE annotation"
      pattern: "vhs-dropout"
    - from: "REQUIREMENTS.md VHS-05 line"
      to: "app/globals.css:2108-2109 + 2125-2126 (literal-value backdrop-filter declarations)"
      via: "file:line citation; verified by grep -E 'backdrop-filter:' | grep 'var(' returning zero matches"
      pattern: "-webkit-backdrop-filter"
---

<objective>
Audit Phase 50's six requirements (VHS-01..06) against shipped code, ratify VHS-01 / VHS-02 / VHS-04 / VHS-05 against `components/animation/vhs-overlay.tsx` + `app/globals.css`, and mark VHS-03 + VHS-06 OBSOLETE with explicit rationale citing the architectural decisions that supersede them. Fix the stale `44-01-PLAN.md` reference under Phase 50 in `ROADMAP.md`. No source code changes — pure documentation reconciliation per `feedback_ratify_reality_bias.md` and `project_overdue_lockin_mode.md`.

Purpose: Phase 50 ships as predicted by the v1.7 handoff: VHS code is alive and tuned, with the original `93fa031 feat(50): VHS enhancement` commit followed by 4 fix commits (`f836830` / `05f22ab` / `752075e` plus the perf-gate cleanup `a260238` that cut the dropout-band activator). The visual aberration / jitter / vignette / Safari-prefix layers all ratify cleanly with explicit code comments tagging requirement IDs. VHS-03's dropout bands shipped as a CSS-only scaffold with the activator deferred to "idle phase 2+" wiring that never landed and was specifically cut by `a260238` — same supersedence chain as Phase 49's GRN-02/03. VHS-06's human visual review process gate cannot be satisfied retroactively — same precedent as GRN-04 / VRG-02.

Output: Six body-status flips in `.planning/REQUIREMENTS.md` (4 → Ratified with file:line citations; 2 → Obsolete with supersedence-citation), six matrix-row updates, and one `ROADMAP.md` plan-reference fix.
</objective>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.handoffs/v1.7-ratification.md
@.planning/phases/49-grain-idle-escalation/49-01-SUMMARY.md

<scope_decision>
The six Phase 50 requirements split 4-Ratified / 2-Obsolete:

| Req | Verdict | Citation / Rationale |
|-----|---------|----------------------|
| VHS-01 | RATIFY | `vhs-overlay.tsx:153-178` derives `--sfx-vhs-chromatic-opacity` from `--sfx-vhs-scanline-opacity` (the SIG-02 ratified derived property), gated at intensity > 0.3 then linearly scaled across 0.3–1.0; `globals.css:2195-2210` renders red+cyan layers with opposing ±1.5px translateX and `mix-blend-mode: screen`. Reads the Phase 48 derived intensity bridge — pre-satisfied at the consumer wiring level by SIG-02. |
| VHS-02 | RATIFY | `globals.css:2213-2225` `.vhs-jitter` runs `sf-vhs-jitter 0.15s steps(4, end) infinite` keyframes stepping translateX through −1.2px / +0.8px / −0.5px / +1px values. `steps(4, end)` produces the discrete tape-dropout register (not smooth animation). JSX layer 8 at `vhs-overlay.tsx:230-233`. |
| VHS-03 | OBSOLETE | CSS scaffold ships at `globals.css:2230-2246` + JSX container at `vhs-overlay.tsx:236`, but the runtime activator was tied to the cut idle-escalation consumer per Phase 49's GRN-02 supersedence chain (`a260238 fix(cleanup): remove heavy SIGNAL effects from GlobalEffects render for performance gate`). The original feat commit `93fa031` shipped with band-injection deferred to "idle phase 2+" wiring that never landed. PRF-02 launch gate supersedes; CSS persists as latent surface area identical to `useIdleEscalation`'s library-only / dead-code residue. |
| VHS-04 | RATIFY | `globals.css:2249-2253` `.vhs-vignette` uses `radial-gradient(ellipse at center, transparent 60%, oklch(0 0 0 / 0.15) 100%)`, transparent through the 60% center band and darkening to ≈15% black opacity at the perimeter. JSX layer 10 at `vhs-overlay.tsx:239`. |
| VHS-05 | RATIFY | Both `.vhs-scanline` (`globals.css:2108-2109`) and `.vhs-scanline--slow` (`globals.css:2125-2126`) carry paired `backdrop-filter` / `-webkit-backdrop-filter` declarations with literal `contrast()` / `saturate()` / `brightness()` / `blur()` values. Verified by `grep -nE "backdrop-filter:" app/globals.css | grep "var("` returning zero matches across the file. |
| VHS-06 | OBSOLETE | Process gate cannot be satisfied retroactively. `git log --all --grep "vhs.*review|coherence.*vhs" -i` returns zero matches; the review checkpoint was bypassed during the 5-commit VHS tuning arc (`93fa031` → `f836830` → `05f22ab` → `752075e`) followed immediately by `a260238`'s perf-gate cleanup. Forward-looking visual baseline capture remains available via VRG-01's ratified Chromatic infrastructure; same retroactive-temporal-impossibility as GRN-04 / VRG-02. |

**Key supporting commits (all on `main`):**
- `93fa031` — `feat(50): VHS enhancement — chromatic aberration, jitter, dropout bands, vignette` — original VHS layer additions
- `f836830` / `05f22ab` — VHS layer opacity halving (×2) for visual coherence
- `752075e` — chromatic aberration opacity halving at viewport top/bottom edges
- `a260238` — `fix(cleanup): remove heavy SIGNAL effects from GlobalEffects render for performance gate` — cuts dropout-band activator alongside IdleOverlay

**Pre-plan verification of VHS-03 supersedence:** `grep -rn "vhs-dropout--active\|vhs-dropout__band" components/ app/ hooks/ lib/` returns ONLY the CSS class definitions in `app/globals.css:2237` + `app/globals.css:2240`. Zero JS/TS code toggles `--active` or appends band children. Same library-only / dead-code residue pattern as `useIdleEscalation`.

**Pattern precedent:** Phase 47 split 1-Complete / 2-Ratified / 1-Obsolete; Phase 49 split 1-Ratified / 4-Obsolete; Phase 50 splits 4-Ratified / 2-Obsolete. Phase 48 (6/6 Ratified) remains the atypical clean case. Mixed-verdict ratification IS the v1.7 audit norm.
</scope_decision>

<existing_state>
REQUIREMENTS.md (lines 274-279 for VHS-01..06) all marked `[ ]` Pending at start.

REQUIREMENTS.md traceability matrix lines 361-366: VHS-01..06 all `Pending` at start.

ROADMAP.md line 745 (under Phase 50 entry):
```
- [ ] 44-01-PLAN.md — Fix all copy across 6 source files + update Playwright assertions
```
Same copy-paste artifact as Phases 51, 52, 53, 54, 55, 56. Fix only the Phase 50 occurrence.

ROADMAP.md line 507 already shows Phase 50 with `[x]` and "completed 2026-04-11, 1/1 plans" — that line conflates code shipping (yes, VHS layers landed 2026-04-11) with traceability shipping (no, that's this commit).

Not modifying STATE.md in this commit. Roll-up is a campaign-level concern.
</existing_state>

</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Ratify VHS-01/02/04/05 + mark VHS-03/06 Obsolete in REQUIREMENTS.md (body + matrix)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
    Body status lines 274-279 flipped from `[ ]` to `[x]` with literal `RATIFIED 2026-04-25` (VHS-01/02/04/05) or `OBSOLETE 2026-04-25` (VHS-03, VHS-06) annotations + file:line + commit-hash citations.

    Traceability matrix rows 361-366 updated from `Pending` to `Ratified` (VHS-01/02/04/05) or `Obsolete` (VHS-03, VHS-06).

    Do NOT touch HLF-*, CIR-*, MSH-*, PTL-*, GLT-*, SYM-*, VRG-03, PRF-* rows.
  </action>
  <verify>
    <automated>grep -c "^- \[x\] \*\*VHS-0[1-6]\*\*" .planning/REQUIREMENTS.md | grep -q "^6$" &amp;&amp; grep -cE "^\| VHS-0[1-6] \| Phase 50 \| (Ratified|Obsolete) \|" .planning/REQUIREMENTS.md | grep -q "^6$" &amp;&amp; ! grep -qE "^\| VHS-0[1-6] \| Phase 50 \| Pending \|" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - All 6 VHS body lines start with `- [x]`
    - VHS-01/02/04/05 body lines contain literal `RATIFIED 2026-04-25` with file:line citations
    - VHS-03/06 body lines contain literal `OBSOLETE 2026-04-25` with supersedence rationale
    - Matrix rows `| VHS-01 | Phase 50 | Ratified |`, `| VHS-02 | Phase 50 | Ratified |`, `| VHS-04 | Phase 50 | Ratified |`, `| VHS-05 | Phase 50 | Ratified |` exist
    - Matrix rows `| VHS-03 | Phase 50 | Obsolete |`, `| VHS-06 | Phase 50 | Obsolete |` exist
    - Zero matrix rows match `^\| VHS-0[1-6] \| Phase 50 \| Pending \|`
  </acceptance_criteria>
  <done>
    REQUIREMENTS.md reflects Phase 50 reality: VHS-01/02/04/05 RATIFIED with file:line citations; VHS-03 + VHS-06 OBSOLETE with explicit supersedence rationale (PRF-02 launch-gate prioritization for VHS-03; temporal-gate impossibility for VHS-06). Body status lines and matrix rows are consistent. No future-phase rows touched.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Fix stale Phase 50 plan reference in ROADMAP.md</name>
  <files>.planning/ROADMAP.md</files>
  <action>
    Replace the Phase 50 plan list entry at line 745 from:
    ```
    - [ ] 44-01-PLAN.md — Fix all copy across 6 source files + update Playwright assertions
    ```
    to:
    ```
    - [x] 50-01-PLAN.md — VHS ratification + dropout/coherence-review obsolescence closure
    ```

    Critical scoping: this exact line also appears under Phases 51, 52, 53, 54, 55, 56. DO NOT touch any of those — they are out of scope. Use surrounding `### Phase 50.1: Datamosh` heading as disambiguator.
  </action>
  <verify>
    <automated>grep -A 20 "### Phase 50: VHS" .planning/ROADMAP.md | grep -q "50-01-PLAN.md — VHS ratification" &amp;&amp; grep -A 20 "### Phase 50: VHS" .planning/ROADMAP.md | head -25 | grep -vq "44-01-PLAN.md" &amp;&amp; grep -A 20 "### Phase 51: Halftone" .planning/ROADMAP.md | grep -q "44-01-PLAN.md"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -A 20 "### Phase 50: VHS" .planning/ROADMAP.md` contains line `- [x] 50-01-PLAN.md — VHS ratification + dropout/coherence-review obsolescence closure`
    - Phase 50 entry no longer contains `44-01-PLAN.md`
    - Phase 51 entry STILL contains `44-01-PLAN.md` (out of scope)
    - Total `50-01-PLAN.md` occurrences in ROADMAP.md = 1
    - Total `44-01-PLAN.md` occurrences decremented from 8 → 7
  </acceptance_criteria>
  <done>
    Phase 50 in ROADMAP.md correctly points at this plan's filename with the checkbox ticked. All other phases' identical stale references deliberately preserved.
  </done>
</task>

</tasks>

<verification>
- `grep -c "^- \[x\] \*\*VHS-0[1-6]\*\*" .planning/REQUIREMENTS.md` returns `6`
- `grep -cE "^\| VHS-0[1-6] \| Phase 50 \| (Ratified|Obsolete) \|" .planning/REQUIREMENTS.md` returns `6`
- `grep -c "RATIFIED 2026-04-25" .planning/REQUIREMENTS.md` returns at least `13` (was 9 after Phase 49; +4 = VHS-01/02/04/05)
- `grep -c "OBSOLETE 2026-04-25" .planning/REQUIREMENTS.md` returns at least `7` (was 5 after Phase 49; +2 = VHS-03/06)
- ROADMAP.md Phase 50 plan ref reads `50-01-PLAN.md — VHS ratification + dropout/coherence-review obsolescence closure` with `[x]`
- ROADMAP.md `44-01-PLAN.md` occurrences decremented from 8 → 7
- Zero source code (`app/`, `components/`, `lib/`, `hooks/`, `package.json`) changes
</verification>
