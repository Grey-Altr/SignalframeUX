---
phase: 51-halftone-texture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - HLF-01
  - HLF-02
  - HLF-03
  - HLF-04

must_haves:
  truths:
    - "REQUIREMENTS.md status for HLF-01 reflects shipped reality (CSS-only halftone via radial-gradient at globals.css:1252-1266; mix-blend-mode: multiply substitutes spec's filter+background-blend-mode for performance, outcome equivalent)"
    - "REQUIREMENTS.md status for HLF-02 reflects shipped reality (literal i < 0.4 ? 0 : (i - 0.4) / 0.6 * 0.15 curve at global-effects.tsx:40-41; exact spec match, pre-satisfied via Phase 48 SIG-02 carry-forward)"
    - "REQUIREMENTS.md status for HLF-03 reflects shipped reality (.sf-halftone class consumed only by 4 token-specimen blocks; hero uses dot token inline but not the ambient class)"
    - "REQUIREMENTS.md status for HLF-04 reflects shipped reality: combined-view human visual review process gate cannot be satisfied retroactively — Obsolete with rationale (same precedent as VHS-06 / GRN-04 / VRG-02)"
    - "ROADMAP.md Phase 51 plan list points at 51-01-PLAN.md (not stale 44-01-PLAN.md inherited reference)"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Status annotations for HLF-01..04 (3 RATIFIED + 1 OBSOLETE)"
      contains: "RATIFIED 2026-04-25"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated traceability matrix rows for Phase 51 requirements"
      contains: "HLF-01 | Phase 51 | Ratified"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 51 plan reference fixed"
      contains: "51-01-PLAN.md — Halftone ratification"
  key_links:
    - from: "REQUIREMENTS.md HLF-01 line"
      to: "app/globals.css:1252-1266 (.sf-halftone::before radial-gradient + opacity gate)"
      via: "file:line citation; technique-divergence annotation noting mix-blend-mode substitution"
      pattern: "--sfx-halftone-opacity"
    - from: "REQUIREMENTS.md HLF-02 line"
      to: "components/layout/global-effects.tsx:40-41 (literal threshold curve)"
      via: "file:line citation; pre-satisfied at consumer wiring level by SIG-02"
      pattern: "halftoneOpacity"
    - from: "REQUIREMENTS.md HLF-03 line"
      to: "components/blocks/token-specimens/{spacing,motion,color,type}-specimen.tsx (4 consumers) + components/blocks/hero.tsx:21 (inline non-ambient counter-example)"
      via: "file:line citations across 4 specimen files; grep -rn 'sf-halftone' confirms scoping"
      pattern: "data-halftone"
---

<objective>
Audit Phase 51's four requirements (HLF-01..04) against shipped code, ratify HLF-01 / HLF-02 / HLF-03 against `app/globals.css` + `components/layout/global-effects.tsx` + the 4 token-specimen consumers, and mark HLF-04 OBSOLETE with explicit rationale citing the same retroactive-temporal-impossibility precedent as VHS-06 / GRN-04 / VRG-02. Fix the stale `44-01-PLAN.md` reference under Phase 51 in `ROADMAP.md`. No source code changes — pure documentation reconciliation per `feedback_ratify_reality_bias.md` and `project_overdue_lockin_mode.md`.

Purpose: Phase 51 ships as predicted by the v1.7 handoff. The original feat commit `913fcb1 feat(51): CSS-only halftone texture for specimen sections` landed the `.sf-halftone::before` overlay, the `--sfx-halftone-opacity` derivation in `global-effects.tsx`, and the 4 token-specimen consumers in a single arc. Per Phase 48's SIG-02 carry-forward intel, HLF-02 is pre-satisfied at the wiring level — same pattern that pre-satisfied VHS-01. HLF-01's shipped technique diverges from spec (`mix-blend-mode: multiply` instead of `background-blend-mode` + `filter: contrast()`), but the outcome (CSS-only halftone dot pattern) matches and the simpler form is more performant on a single dot layer. This is the first ratification on this branch where shipped *technique* diverges from spec while shipped *outcome* matches — a genuine "ratify reality" call. HLF-04's combined-view human visual review process gate cannot be satisfied retroactively — same precedent established by VHS-06 / GRN-04 / VRG-02.

Output: Four body-status flips in `.planning/REQUIREMENTS.md` (3 → Ratified with file:line citations; 1 → Obsolete with supersedence-citation), four matrix-row updates, and one `ROADMAP.md` plan-reference fix.
</objective>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.handoffs/v1.7-ratification.md
@.planning/phases/50-vhs-enhancement/50-01-SUMMARY.md

<scope_decision>
The four Phase 51 requirements split 3-Ratified / 1-Obsolete:

| Req | Verdict | Citation / Rationale |
|-----|---------|----------------------|
| HLF-01 | RATIFY (with technique-divergence note) | `app/globals.css:1252-1266` `.sf-halftone::before` ships a CSS-only halftone via `radial-gradient(circle, var(--sfx-halftone-dot) 1px, transparent 1px)` at a 4×4px tile, gated by `opacity: var(--sfx-halftone-opacity, 0)` (the SIG-02 ratified derived property). Compositing uses `mix-blend-mode: multiply` (single-layer overlay) instead of the spec's `background-blend-mode` + `filter: contrast()`; both alternative techniques would be redundant given the single dot layer and already-crisp 1px circle edges, and the simpler form avoids a GPU `filter` pass. Outcome (CSS-only halftone dot pattern with intensity-gated opacity) met; technique simplified for performance — ratify reality per `feedback_ratify_reality_bias.md`. Original feat commit `913fcb1`. |
| HLF-02 | RATIFY | `components/layout/global-effects.tsx:40-41` derives `--sfx-halftone-opacity` via literal `i < 0.4 ? 0 : (i - 0.4) / 0.6 * 0.15` curve, snapping to zero below the 0.4 threshold and ramping linearly across 0.4–1.0 to a 0.15 ceiling. Exact match to the spec's "gated at intensity >= 0.4 via derived property curve" wording. Same SIG-02 carry-forward pattern that pre-satisfied VHS-01. |
| HLF-03 | RATIFY | The `.sf-halftone` ambient-overlay class is consumed only by 4 token-specimen blocks: `spacing-specimen.tsx:18`, `motion-specimen.tsx:49`, `color-specimen.tsx:112`, `type-specimen.tsx:27` — each tagged with `data-halftone`. `components/blocks/hero.tsx:21` uses the `--sfx-halftone-dot` token inside an inline `radial-gradient` for one-off decorative texture but does NOT apply the ambient `.sf-halftone` class — confirmed by `grep -rn "sf-halftone" components/ app/` returning only the four specimen consumers + the CSS rule definitions. Zero global / ambient overlay leakage. |
| HLF-04 | OBSOLETE | Combined-view human visual review process gate cannot be satisfied retroactively. `git log --all --grep "halftone.*review|coherence.*halftone|moir" -i` returns zero process-gate matches against shipping history (the 4 hits are unrelated cdB / primitives / feat-51 commits, not coherence reviews). Same retroactive-temporal-impossibility precedent as VHS-06 / GRN-04 / VRG-02. Forward-looking visual baseline capture remains available via VRG-01's ratified Chromatic infrastructure; within v1.7 lock posture no further halftone changes are scoped, so the gate is moot. |

**Key supporting commits (all on `main`):**
- `913fcb1` — `feat(51): CSS-only halftone texture for specimen sections` — original Phase 51 feat commit (CSS rule + derivation + consumer wiring landed together)

**Pre-plan verification of HLF-03 scoping:** `grep -rn "sf-halftone" components/ app/` returns:
- `app/globals.css:1252` `.sf-halftone {` (rule definition)
- `app/globals.css:1255` `.sf-halftone::before {` (rule definition)
- `app/globals.css:2339` `.sf-halftone::before,` (print-suppression rule)
- 4 specimen consumers under `components/blocks/token-specimens/`

Hero's `hero.tsx:21` line uses the `--sfx-halftone-dot` *token* inside a one-off inline `radial-gradient`, but the element does NOT carry the `.sf-halftone` class — therefore HLF-03's "no ambient overlay" guarantee holds.

**Pattern precedent:** Phase 47 split 1-Complete / 2-Ratified / 1-Obsolete; Phase 49 split 1-Ratified / 4-Obsolete; Phase 50 split 4-Ratified / 2-Obsolete. Phase 48 (6/6 Ratified) remains the atypical clean case. Phase 51 splits 3-Ratified / 1-Obsolete — adds a new sub-pattern: the first technique-divergence ratification (HLF-01) where shipped outcome matches spec but shipped *implementation* simplifies past the spec's listed techniques.
</scope_decision>

<existing_state>
REQUIREMENTS.md (lines 282-285 for HLF-01..04) all marked `[ ]` Pending at start.

REQUIREMENTS.md traceability matrix lines 371-374: HLF-01..04 all `Pending` at start.

ROADMAP.md line 774 (under Phase 51 entry):
```
- [ ] 44-01-PLAN.md — Fix all copy across 6 source files + update Playwright assertions
```
Same copy-paste artifact as Phases 52, 53, 54, 55, 56. Fix only the Phase 51 occurrence.

ROADMAP.md line 509 already shows Phase 51 with `[x]` and "completed 2026-04-11, 1/1 plans" — that line conflates code shipping (yes, halftone landed in `913fcb1`) with traceability shipping (no, that's this commit). Compatible with our `[x]` plan-checkbox flip below; not modifying it.

Not modifying STATE.md in this commit. Roll-up is a campaign-level concern.
</existing_state>

</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Ratify HLF-01/02/03 + mark HLF-04 Obsolete in REQUIREMENTS.md (body + matrix)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
    Body status lines 282-285 flipped from `[ ]` to `[x]` with literal `RATIFIED 2026-04-25` (HLF-01/02/03) or `OBSOLETE 2026-04-25` (HLF-04) annotations + file:line + commit-hash citations.

    Traceability matrix rows 371-374 updated from `Pending` to `Ratified` (HLF-01/02/03) or `Obsolete` (HLF-04).

    Do NOT touch CIR-*, MSH-*, PTL-*, GLT-*, SYM-*, VRG-03, PRF-* rows.
  </action>
  <verify>
    <automated>grep -c "^- \[x\] \*\*HLF-0[1-4]\*\*" .planning/REQUIREMENTS.md | grep -q "^4$" &amp;&amp; grep -cE "^\| HLF-0[1-4] \| Phase 51 \| (Ratified|Obsolete) \|" .planning/REQUIREMENTS.md | grep -q "^4$" &amp;&amp; ! grep -qE "^\| HLF-0[1-4] \| Phase 51 \| Pending \|" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - All 4 HLF body lines start with `- [x]`
    - HLF-01/02/03 body lines contain literal `RATIFIED 2026-04-25` with file:line citations
    - HLF-04 body line contains literal `OBSOLETE 2026-04-25` with supersedence rationale
    - Matrix rows `| HLF-01 | Phase 51 | Ratified |`, `| HLF-02 | Phase 51 | Ratified |`, `| HLF-03 | Phase 51 | Ratified |` exist
    - Matrix row `| HLF-04 | Phase 51 | Obsolete |` exists
    - Zero matrix rows match `^\| HLF-0[1-4] \| Phase 51 \| Pending \|`
  </acceptance_criteria>
  <done>
    REQUIREMENTS.md reflects Phase 51 reality: HLF-01/02/03 RATIFIED with file:line citations (HLF-01 carries an explicit technique-divergence annotation noting `mix-blend-mode: multiply` as the simplified equivalent of the spec's `background-blend-mode` + `filter: contrast()`); HLF-04 OBSOLETE with explicit supersedence rationale (retroactive-temporal-impossibility for combined-view human visual review). Body status lines and matrix rows are consistent. No future-phase rows touched.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Fix stale Phase 51 plan reference in ROADMAP.md</name>
  <files>.planning/ROADMAP.md</files>
  <action>
    Replace the Phase 51 plan list entry at line 774 from:
    ```
    - [ ] 44-01-PLAN.md — Fix all copy across 6 source files + update Playwright assertions
    ```
    to:
    ```
    - [x] 51-01-PLAN.md — Halftone ratification + moiré-review obsolescence closure
    ```

    Critical scoping: this exact line also appears under Phases 52, 53, 54, 55, 56. DO NOT touch any of those — they are out of scope. Use surrounding `### Phase 52: Circuit Overlay` heading as disambiguator.
  </action>
  <verify>
    <automated>grep -A 14 "### Phase 51: Halftone" .planning/ROADMAP.md | grep -q "51-01-PLAN.md — Halftone ratification" &amp;&amp; grep -A 14 "### Phase 51: Halftone" .planning/ROADMAP.md | grep -vq "44-01-PLAN.md" &amp;&amp; grep -A 14 "### Phase 52: Circuit" .planning/ROADMAP.md | grep -q "44-01-PLAN.md"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -A 14 "### Phase 51: Halftone" .planning/ROADMAP.md` contains line `- [x] 51-01-PLAN.md — Halftone ratification + moiré-review obsolescence closure`
    - Phase 51 entry no longer contains `44-01-PLAN.md`
    - Phase 52 entry STILL contains `44-01-PLAN.md` (out of scope)
    - Total `51-01-PLAN.md` occurrences in ROADMAP.md = 1
    - Total `44-01-PLAN.md` occurrences decremented from 7 → 6
  </acceptance_criteria>
  <done>
    Phase 51 in ROADMAP.md correctly points at this plan's filename with the checkbox ticked. All other phases' identical stale references deliberately preserved.
  </done>
</task>

</tasks>

<verification>
- `grep -c "^- \[x\] \*\*HLF-0[1-4]\*\*" .planning/REQUIREMENTS.md` returns `4`
- `grep -cE "^\| HLF-0[1-4] \| Phase 51 \| (Ratified|Obsolete) \|" .planning/REQUIREMENTS.md` returns `4`
- `grep -c "RATIFIED 2026-04-25" .planning/REQUIREMENTS.md` returns at least `16` (was 13 after Phase 50; +3 = HLF-01/02/03)
- `grep -c "OBSOLETE 2026-04-25" .planning/REQUIREMENTS.md` returns at least `8` (was 7 after Phase 50; +1 = HLF-04)
- ROADMAP.md Phase 51 plan ref reads `51-01-PLAN.md — Halftone ratification + moiré-review obsolescence closure` with `[x]`
- ROADMAP.md `44-01-PLAN.md` occurrences decremented from 7 → 6
- Zero source code (`app/`, `components/`, `lib/`, `hooks/`, `package.json`) changes
</verification>
