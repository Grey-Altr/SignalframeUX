---
phase: 52-circuit-overlay
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - CIR-01
  - CIR-02
  - CIR-03

must_haves:
  truths:
    - "REQUIREMENTS.md status for CIR-01 reflects shipped reality (.sf-circuit::after embedded SVG circuit pattern at globals.css:1268-1283 with opacity: var(--sfx-circuit-opacity, 0.03) fallback hitting the 0.02-0.05 range, three live consumers wired in app/page.tsx)"
    - "REQUIREMENTS.md status for CIR-02 reflects shipped reality (literal 0.05 * (1 - i) monotonic-inverse curve at global-effects.tsx:43-46; mutually exclusive with grain at high intensity per spec wording)"
    - "REQUIREMENTS.md status for CIR-03 reflects shipped reality (--sfx-circuit-opacity written by global-effects.tsx:46, read by globals.css:1281, real-time updates via SignalIntensityBridge MutationObserver at global-effects.tsx:264-298)"
    - "ROADMAP.md Phase 52 plan list points at 52-01-PLAN.md (not stale 44-01-PLAN.md inherited reference)"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Status annotations for CIR-01..03 (3 RATIFIED, zero OBSOLETE)"
      contains: "RATIFIED 2026-04-25"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated traceability matrix rows for Phase 52 requirements"
      contains: "CIR-01 | Phase 52 | Ratified"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 52 plan reference fixed"
      contains: "52-01-PLAN.md — Circuit overlay clean ratification"
  key_links:
    - from: "REQUIREMENTS.md CIR-01 line"
      to: "app/globals.css:1268-1283 (.sf-circuit::after embedded SVG + opacity gate) + app/page.tsx:69/81/113 (three consumers)"
      via: "file:line citation; clean spec match — no technique-divergence annotation needed (mix-blend-mode: soft-light is the natural compositing choice for low-contrast section background insertion)"
      pattern: "--sfx-circuit-opacity"
    - from: "REQUIREMENTS.md CIR-02 line"
      to: "components/layout/global-effects.tsx:43-46 (literal 0.05 * (1 - i) monotonic-inverse curve)"
      via: "file:line citation; comment line 43 explicitly states 'INVERSE of intensity ... mutually exclusive with grain'; pre-satisfied at consumer wiring level by SIG-02"
      pattern: "circuitOpacity"
    - from: "REQUIREMENTS.md CIR-03 line"
      to: "components/layout/global-effects.tsx:46 (writer) + app/globals.css:1281 (reader) + global-effects.tsx:264-298 (SignalIntensityBridge MutationObserver)"
      via: "writer-reader-observer chain; same observer mechanism that real-time-updated VHS-01 + HLF-02"
      pattern: "SignalIntensityBridge"
---

<objective>
Audit Phase 52's three requirements (CIR-01..03) against shipped code, ratify all three against `app/globals.css` + `components/layout/global-effects.tsx` + the three consumer panels in `app/page.tsx`. Fix the stale `44-01-PLAN.md` reference under Phase 52 in `ROADMAP.md`. No source code changes — pure documentation reconciliation per `feedback_ratify_reality_bias.md` and `project_overdue_lockin_mode.md`.

Purpose: Phase 52 ships as the cleanest predicted ratification on the v1.7 branch. Per Phase 48's SIG-02 carry-forward intel, `--sfx-circuit-opacity` is already derived in `global-effects.tsx:45-46`; per Phase 51's prediction, the curve was expected to be a monotonic inverse of intensity (mirroring how the halftone curve grows monotonically) — and reality matches: the literal `0.05 * (1 - i)` form ratifies CIR-02 cleanly without HLF-01-style technique-divergence annotation. The CIR-* requirement set contains no process-gate clauses (no "human visual review" or "before changes" wording), so the mixed-verdict streak from Phases 47 / 49 / 50 / 51 is broken — Phase 52 joins Phase 48 as the second clean all-ratified phase on this branch. Three consumers (`app/page.tsx:69/81/113`: PROOF, INVENTORY, ACQUISITION) wire the `.sf-circuit` ambient class on `SFPanel`, satisfying CIR-01's "section background" placement requirement. CIR-03's "real-time updates" guarantee is satisfied by the same `SignalIntensityBridge` `MutationObserver` mechanism (`global-effects.tsx:264-298`) that real-time-updated VHS-01 + HLF-02.

Output: Three body-status flips in `.planning/REQUIREMENTS.md` (3 → Ratified with file:line citations), three matrix-row updates, and one `ROADMAP.md` plan-reference fix.
</objective>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.handoffs/v1.7-ratification.md
@.planning/phases/51-halftone-texture/51-01-SUMMARY.md

<scope_decision>
The three Phase 52 requirements split 3-Ratified / 0-Obsolete:

| Req | Verdict | Citation / Rationale |
|-----|---------|----------------------|
| CIR-01 | RATIFY | `app/globals.css:1268-1283` ships `.sf-circuit::after` carrying an embedded SVG (horizontal/vertical traces, right-angle bends, pads/vias) at `background-size: 120px 120px` + `background-repeat: repeat`, gated by `opacity: var(--sfx-circuit-opacity, 0.03)` (the SIG-02 ratified derived property; fallback hits the 0.02-0.05 spec range exactly). Compositing uses `mix-blend-mode: soft-light` for low-contrast section background insertion. Three live consumers wired at `app/page.tsx:69` (PROOF), `:81` (INVENTORY), `:113` (ACQUISITION) via `className="sf-circuit relative"` on `SFPanel`. Outcome and technique both match spec — no technique-divergence annotation needed (unlike HLF-01). Original feat commit precedes this audit; no code mutation. |
| CIR-02 | RATIFY | `components/layout/global-effects.tsx:43-46` derives `--sfx-circuit-opacity` via the literal `0.05 * (1 - i)` curve. Comment on line 43 reads explicitly: `"Circuit: INVERSE of intensity — visible at low, fades at high (mutually exclusive with grain)"`. At `i=0`: circuit=0.05, grain=0.03 (baseline floor from `global-effects.tsx:36`). At `i=1`: circuit=0, grain≈0.08 (logarithmic ceiling from same line). Mutually exclusive at high intensity per spec wording. Same SIG-02 carry-forward beneficiary pattern that pre-satisfied VHS-01 + HLF-02 — third such beneficiary, predicted by Phase 51's carry-forward intel. |
| CIR-03 | RATIFY | `components/layout/global-effects.tsx:46` writes `--sfx-circuit-opacity` from the intensity-derived value; `app/globals.css:1281` reads `var(--sfx-circuit-opacity, 0.03)`. Real-time updates via the `SignalIntensityBridge` `MutationObserver` at `global-effects.tsx:264-298` — observes `<html>` `style` attribute mutations (set by `signal-overlay.tsx` slider + per-section writers like `proof-section.tsx:68/130/140` + `signal-section.tsx:51`) and re-runs `updateSignalDerivedProps()` on every change. Same observer mechanism that satisfied VHS-01 + HLF-02 in real-time. |

**Key supporting artifacts (all on `main`):**
- `app/globals.css:1268-1283` — `.sf-circuit::after` embedded SVG circuit pattern with `mix-blend-mode: soft-light`
- `components/layout/global-effects.tsx:39-57` — derived-property block including `--sfx-circuit-opacity` at lines 43-46
- `components/layout/global-effects.tsx:264-298` — `SignalIntensityBridge` `MutationObserver` mechanism
- `app/page.tsx:69, :81, :113` — three consumer `SFPanel` instances (PROOF, INVENTORY, ACQUISITION)

**Pre-plan verification (`grep -rn "sf-circuit\|--sfx-circuit" components/ app/`):** returns `.sf-circuit` (rule + 3 page consumers) and `--sfx-circuit-opacity` (1 writer in `global-effects.tsx:46` + 1 reader in `globals.css:1281`). No leakage, no orphan consumers, no dead writers. The `.sf-circuit-path` / `.sf-circuit-dot` / `sf-circuit-draw` rules at `globals.css:1627-1666` are a separate animated SVG drawing primitive (used by `CircuitDivider` reference templates), unrelated to CIR-01/02/03's section-background overlay.

**Pattern precedent:** Phase 47 split 1-Complete / 2-Ratified / 1-Obsolete; Phase 48 (6/6 Ratified) was the first clean case; Phase 49 split 1-Ratified / 4-Obsolete; Phase 50 split 4-Ratified / 2-Obsolete; Phase 51 split 3-Ratified / 1-Obsolete (first technique-divergence ratification). Phase 52 splits 3-Ratified / 0-Obsolete — joins Phase 48 as the second clean all-ratified phase. CIR-* requirements contain no process-gate clauses (no "human visual review", no "before grain changes" temporal gates), so the mixed-verdict streak ends here. The clean ratification was predicted by Phase 51's handoff intel.
</scope_decision>

<existing_state>
REQUIREMENTS.md (lines 287-290 for CIR-01..03) all marked `[ ]` Pending at start.

REQUIREMENTS.md traceability matrix lines 375-377: CIR-01..03 all `Pending` at start.

ROADMAP.md line 786 (under Phase 52 entry):
```
- [ ] 44-01-PLAN.md — Fix all copy across 6 source files + update Playwright assertions
```
Same copy-paste artifact as Phases 53, 54, 55, 56. Fix only the Phase 52 occurrence.

ROADMAP.md line 510 already shows Phase 52 with `[x]` and "completed 2026-04-11, 1/1 plans" — that line conflates code shipping (yes, circuit landed in an earlier feat commit) with traceability shipping (no, that's this commit). Compatible with our `[x]` plan-checkbox flip below; not modifying it.

Not modifying STATE.md in this commit. Roll-up is a campaign-level concern.
</existing_state>

</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Ratify CIR-01/02/03 in REQUIREMENTS.md (body + matrix)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
    Body status lines 287-290 flipped from `[ ]` to `[x]` with literal `RATIFIED 2026-04-25` annotations + file:line citations.

    Traceability matrix rows 375-377 updated from `Pending` to `Ratified`.

    Do NOT touch MSH-*, PTL-*, GLT-*, SYM-*, VRG-03, PRF-* rows.
  </action>
  <verify>
    <automated>grep -c "^- \[x\] \*\*CIR-0[1-3]\*\*" .planning/REQUIREMENTS.md | grep -q "^3$" &amp;&amp; grep -cE "^\| CIR-0[1-3] \| Phase 52 \| Ratified \|" .planning/REQUIREMENTS.md | grep -q "^3$" &amp;&amp; ! grep -qE "^\| CIR-0[1-3] \| Phase 52 \| Pending \|" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - All 3 CIR body lines start with `- [x]`
    - All 3 CIR body lines contain literal `RATIFIED 2026-04-25` with file:line citations
    - Matrix rows `| CIR-01 | Phase 52 | Ratified |`, `| CIR-02 | Phase 52 | Ratified |`, `| CIR-03 | Phase 52 | Ratified |` exist
    - Zero matrix rows match `^\| CIR-0[1-3] \| Phase 52 \| Pending \|`
  </acceptance_criteria>
  <done>
    REQUIREMENTS.md reflects Phase 52 reality: CIR-01/02/03 RATIFIED with file:line citations and zero technique-divergence annotations (clean spec match throughout). Body status lines and matrix rows are consistent. No future-phase rows touched.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Fix stale Phase 52 plan reference in ROADMAP.md</name>
  <files>.planning/ROADMAP.md</files>
  <action>
    Replace the Phase 52 plan list entry at line 786 from:
    ```
    - [ ] 44-01-PLAN.md — Fix all copy across 6 source files + update Playwright assertions
    ```
    to:
    ```
    - [x] 52-01-PLAN.md — Circuit overlay clean ratification (3/3)
    ```

    Critical scoping: this exact line also appears under Phases 53, 54, 55, 56. DO NOT touch any of those — they are out of scope. Use surrounding `### Phase 53: Mesh Gradient` heading as disambiguator.
  </action>
  <verify>
    <automated>sed -n '776,790p' .planning/ROADMAP.md | grep -q "52-01-PLAN.md — Circuit overlay clean ratification" &amp;&amp; sed -n '776,790p' .planning/ROADMAP.md | grep -vq "44-01-PLAN.md" &amp;&amp; grep -A 14 "### Phase 53: Mesh" .planning/ROADMAP.md | grep -q "44-01-PLAN.md"</automated>
  </verify>
  <acceptance_criteria>
    - Phase 52 entry contains line `- [x] 52-01-PLAN.md — Circuit overlay clean ratification (3/3)`
    - Phase 52 entry no longer contains `44-01-PLAN.md`
    - Phase 53 entry STILL contains `44-01-PLAN.md` (out of scope)
    - Total `52-01-PLAN.md` occurrences in ROADMAP.md = 1
    - Total `44-01-PLAN.md` occurrences decremented from 6 → 5
  </acceptance_criteria>
  <done>
    Phase 52 in ROADMAP.md correctly points at this plan's filename with the checkbox ticked. All other phases' identical stale references deliberately preserved.
  </done>
</task>

</tasks>

<verification>
- `grep -c "^- \[x\] \*\*CIR-0[1-3]\*\*" .planning/REQUIREMENTS.md` returns `3`
- `grep -cE "^\| CIR-0[1-3] \| Phase 52 \| Ratified \|" .planning/REQUIREMENTS.md` returns `3`
- `grep -c "RATIFIED 2026-04-25" .planning/REQUIREMENTS.md` returns at least `19` (was 16 after Phase 51; +3 = CIR-01/02/03)
- `grep -c "OBSOLETE 2026-04-25" .planning/REQUIREMENTS.md` returns `8` (unchanged — clean phase)
- ROADMAP.md Phase 52 plan ref reads `52-01-PLAN.md — Circuit overlay clean ratification (3/3)` with `[x]`
- ROADMAP.md `44-01-PLAN.md` occurrences decremented from 6 → 5
- Zero source code (`app/`, `components/`, `lib/`, `hooks/`, `package.json`) changes
</verification>
