---
phase: 53-mesh-gradient
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - MSH-01
  - MSH-02
  - MSH-03

must_haves:
  truths:
    - "REQUIREMENTS.md status for MSH-01 reflects shipped reality (.sf-mesh-gradient rule at globals.css:2280-2292 with position: fixed + z-index: -1 and three layered radial-gradient() OKLCH ellipses at lines 2286-2288, consumed in app/layout.tsx:119)"
    - "REQUIREMENTS.md status for MSH-02 reflects shipped reality (mesh stacks at z-index: -1 line 2283; .sf-grain::after at globals.css:980-987 is position: absolute with no explicit z-index — stacks at z>=0 by default — so any grain that exists composites structurally above mesh; author intent comment at globals.css:2276 explicitly states 'grain composites on top')"
    - "REQUIREMENTS.md status for MSH-03 reflects shipped reality (animation: sf-mesh-drift 60s var(--sfx-ease-default) infinite alternate at globals.css:2290 — literal 60s match to spec '60s+ cycle' wording; @keyframes sf-mesh-drift at globals.css:2298-2310)"
    - "ROADMAP.md Phase 53 plan list points at 53-01-PLAN.md (not stale 44-01-PLAN.md inherited reference at line 798)"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Status annotations for MSH-01..03 (3 RATIFIED, zero OBSOLETE)"
      contains: "RATIFIED 2026-04-25"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated traceability matrix rows for Phase 53 requirements"
      contains: "MSH-01 | Phase 53 | Ratified"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 53 plan reference fixed"
      contains: "53-01-PLAN.md — Mesh gradient clean ratification"
  key_links:
    - from: "REQUIREMENTS.md MSH-01 line"
      to: "app/globals.css:2280-2292 (.sf-mesh-gradient rule with position: fixed + z-index: -1) + app/globals.css:2286-2288 (three layered OKLCH radial-gradient ellipses) + app/layout.tsx:119 (consumer)"
      via: "file:line citation; clean spec match — three layered OKLCH radial-gradients + z:-1 exact, no technique-divergence annotation needed"
      pattern: "sf-mesh-gradient"
    - from: "REQUIREMENTS.md MSH-02 line"
      to: "app/globals.css:2283 (mesh z-index: -1) + app/globals.css:980-987 (.sf-grain::after position: absolute no z-index, stacks at z>=0) + author-intent comment at globals.css:2276 ('grain composites on top')"
      via: "structural z-index ordering — mesh fixed at z:-1 sits below ALL default-stacking content including any grain ::after pseudo-element"
      pattern: "sf-grain"
    - from: "REQUIREMENTS.md MSH-03 line"
      to: "app/globals.css:2290 (animation: sf-mesh-drift 60s ... infinite alternate) + app/globals.css:2298-2310 (@keyframes sf-mesh-drift)"
      via: "file:line citation; literal 60s duration matches spec '60-second or longer' wording verbatim; alternate direction provides 120s round-trip"
      pattern: "sf-mesh-drift"
---

<objective>
Audit Phase 53's three requirements (MSH-01..03) against shipped code, ratify all three against `app/globals.css:2274-2322` (mesh-gradient block) + `app/globals.css:980-987` (grain rule) + `app/layout.tsx:119` (consumer). Fix the stale `44-01-PLAN.md` reference under Phase 53 in `ROADMAP.md` (line 798). No source code changes — pure documentation reconciliation per `feedback_ratify_reality_bias.md` and `project_overdue_lockin_mode.md`.

Purpose: Phase 53 ships as the cleanest predicted ratification on the v1.7 branch — the third clean all-ratified phase after Phase 48 (6/6) + Phase 52 (3/3). Per the v1.7 handoff Phase 53 priming intel, the shipped `.sf-mesh-gradient` rule matches MSH-01..03 spec verbatim: three layered OKLCH radial-gradient ellipses at z-index: -1 (MSH-01), structural grain-composites-above guarantee via z-index ordering (MSH-02), and a literal `60s` animation duration on the `sf-mesh-drift` keyframes (MSH-03). The MSH-* requirement set contains no process-gate clauses (no "human visual review" or "before changes" wording), so the mixed-verdict streak from Phases 47 / 49 / 50 / 51 — already broken by Phase 52 — stays broken: Phase 53 joins Phase 48 + 52 as the third clean all-ratified phase. Notably, Phase 53 does NOT depend on the SIG-02 carry-forward chain (mesh uses `--sfx-theme-hue` directly, not `--signal-intensity`-derived properties), making it the first clean phase on this branch ratified outside the SIG-02 → derived-property pattern that pre-satisfied VHS-01 / HLF-02 / CIR-02.

Output: Three body-status flips in `.planning/REQUIREMENTS.md` (3 → Ratified with file:line citations), three matrix-row updates, and one `ROADMAP.md` plan-reference fix at line 798.
</objective>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.handoffs/v1.7-ratification.md
@.planning/phases/52-circuit-overlay/52-01-SUMMARY.md

<scope_decision>
The three Phase 53 requirements split 3-Ratified / 0-Obsolete:

| Req | Verdict | Citation / Rationale |
|-----|---------|----------------------|
| MSH-01 | RATIFY | `app/globals.css:2280-2292` ships `.sf-mesh-gradient` with `position: fixed`, `inset: 0`, `z-index: -1` (line 2283), `pointer-events: none`. Background is three layered `radial-gradient(ellipse ...)` calls at lines 2286-2288, each using `oklch(...)` color values driven by `--sfx-theme-hue` (so the mesh adapts to theme-hue changes — consistent with `feedback_primary_slot_not_color.md`). Consumed at `app/layout.tsx:119` via `<div className="sf-mesh-gradient" aria-hidden="true" />`. Outcome and technique both clean — no technique-divergence annotation needed. |
| MSH-02 | RATIFY | Structural ratification via z-index ordering. Mesh sits at `z-index: -1` (line 2283); `.sf-grain::after` at `globals.css:980-987` uses `position: absolute` with no explicit z-index — establishes a stacking context at z≥0 within its parent. Therefore any `.sf-grain` consumer (e.g. `components/blocks/manifesto-band.tsx:176`) automatically composites above the mesh layer. Author intent confirmed by the JSDoc comment at `globals.css:2276`: "Fixed behind all content at z:-1; grain composites on top." Note: shipped reality applies grain per-section (manifesto-band only), not as a global ambient overlay; this is a deliberate design choice consistent with `feedback_signal_frame_order.md` and the FRAME/SIGNAL dual-layer model — grain is a scoped SIGNAL surface, not a blanket effect. The MSH-02 spec wording ("Grain composited on top of mesh gradient") is satisfied at the structural level: where grain exists, it composites correctly above mesh. |
| MSH-03 | RATIFY | `app/globals.css:2290` declares `animation: sf-mesh-drift 60s var(--sfx-ease-default) infinite alternate`. Literal `60s` duration matches the spec's "60-second or longer" wording verbatim. The `alternate` direction means the animation runs forward 60s then reverse 60s = 120s round-trip per cycle, exceeding the spec's lower bound. `@keyframes sf-mesh-drift` at lines 2298-2310 defines four position waypoints (0% / 33% / 66% / 100%) producing slow background-position drift across all three radial-gradient ellipses. Reduced-motion override at line 2314 (`animation: none`) for accessibility. Print override at line 2320 (`display: none !important`) for performance. |

**Key supporting artifacts (all on `main`):**
- `app/globals.css:2274-2322` — mesh-gradient block (rule + light-mode override + keyframes + reduced-motion + print)
- `app/globals.css:980-987` — `.sf-grain::after` rule (absolute-positioned grain pseudo-element, no z-index = z≥0 stacking)
- `app/layout.tsx:119` — single global consumer `<div className="sf-mesh-gradient" aria-hidden="true" />`

**Pre-plan verification (`grep -rn "sf-mesh-gradient" app/ components/`):** returns the rule definition (`globals.css:2280, 2294, 2314, 2320`) and exactly ONE consumer (`app/layout.tsx:119`). Single global instance — clean wiring, no duplication, no orphan rules. The separate `components/animation/signal-mesh.tsx` + `signal-mesh-lazy.tsx` WebGL mesh on `app/inventory/page.tsx:6` is a distinct decorative element, not the global atmospheric mesh-gradient layer; MSH-* refers exclusively to the CSS path per the spec wording "Layered radial-gradient() using OKLCH color values".

**Pattern precedent:** Phase 47 split 1-Complete / 2-Ratified / 1-Obsolete; Phase 48 (6/6 Ratified) was the first clean case; Phase 49 split 1-Ratified / 4-Obsolete; Phase 50 split 4-Ratified / 2-Obsolete; Phase 51 split 3-Ratified / 1-Obsolete (first technique-divergence ratification); Phase 52 (3/3 Ratified) was the second clean case. Phase 53 splits 3-Ratified / 0-Obsolete — joins Phase 48 + 52 as the third clean all-ratified phase. MSH-* requirements contain no process-gate clauses, so the post-Phase-52 streak holds.

**First non-SIG-02-chain clean ratification.** Phases 48 / 50 / 51 / 52 all leveraged the `global-effects.tsx:39-57` derived-property carry-forward (VHS-01, HLF-02, CIR-02 each pre-satisfied at the wiring level by SIG-02). Phase 53 ratifies entirely outside that chain — the mesh gradient consumes `--sfx-theme-hue` (the brand-hue slot) directly, not any intensity-derived property. This is consistent with the FRAME/SIGNAL split: mesh is a FRAME-layer atmospheric backdrop driven by theme-hue, not a SIGNAL-layer expressive effect driven by intensity. The clean ratification was predicted by the v1.7 handoff Phase 53 priming intel.
</scope_decision>

<existing_state>
REQUIREMENTS.md (lines 293-295 for MSH-01..03) all marked `[ ]` Pending at start.

REQUIREMENTS.md traceability matrix lines 378-380: MSH-01..03 all `Pending` at start.

ROADMAP.md line 798 (under Phase 53 entry):
```
- [ ] 44-01-PLAN.md — Fix all copy across 6 source files + update Playwright assertions
```
Same copy-paste artifact remaining at lines 811 (Phase 54), 823 (Phase 55), 840 (Phase 56). Fix only the Phase 53 occurrence.

ROADMAP.md line 511 already shows Phase 53 with `[x]` and "completed 2026-04-11, 1/1 plans" — that line conflates code shipping (yes, mesh landed in an earlier feat commit) with traceability shipping (no, that's this commit). Compatible with our `[x]` plan-checkbox flip below; not modifying it.

Not modifying STATE.md in this commit. Roll-up is a campaign-level concern.
</existing_state>

</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Ratify MSH-01/02/03 in REQUIREMENTS.md (body + matrix)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
    Body status lines 293-295 flipped from `[ ]` to `[x]` with literal `RATIFIED 2026-04-25` annotations + file:line citations.

    Traceability matrix rows 378-380 updated from `Pending` to `Ratified`.

    Do NOT touch PTL-*, GLT-*, SYM-*, VRG-03, PRF-* rows.
  </action>
  <verify>
    <automated>grep -c "^- \[x\] \*\*MSH-0[1-3]\*\*" .planning/REQUIREMENTS.md | grep -q "^3$" &amp;&amp; grep -cE "^\| MSH-0[1-3] \| Phase 53 \| Ratified \|" .planning/REQUIREMENTS.md | grep -q "^3$" &amp;&amp; ! grep -qE "^\| MSH-0[1-3] \| Phase 53 \| Pending \|" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - All 3 MSH body lines start with `- [x]`
    - All 3 MSH body lines contain literal `RATIFIED 2026-04-25` with file:line citations
    - Matrix rows `| MSH-01 | Phase 53 | Ratified |`, `| MSH-02 | Phase 53 | Ratified |`, `| MSH-03 | Phase 53 | Ratified |` exist
    - Zero matrix rows match `^\| MSH-0[1-3] \| Phase 53 \| Pending \|`
  </acceptance_criteria>
  <done>
    REQUIREMENTS.md reflects Phase 53 reality: MSH-01/02/03 RATIFIED with file:line citations and zero technique-divergence annotations (clean spec match throughout). Body status lines and matrix rows are consistent. No future-phase rows touched.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Fix stale Phase 53 plan reference in ROADMAP.md</name>
  <files>.planning/ROADMAP.md</files>
  <action>
    Replace the Phase 53 plan list entry at line 798 from:
    ```
    - [ ] 44-01-PLAN.md — Fix all copy across 6 source files + update Playwright assertions
    ```
    to:
    ```
    - [x] 53-01-PLAN.md — Mesh gradient clean ratification (3/3)
    ```

    Critical scoping: this exact line also appears under Phases 54, 55, 56 (lines 811, 823, 840). DO NOT touch any of those — they are out of scope. Use surrounding `### Phase 54: Particle Field` heading as disambiguator.
  </action>
  <verify>
    <automated>sed -n '789,800p' .planning/ROADMAP.md | grep -q "53-01-PLAN.md — Mesh gradient clean ratification" &amp;&amp; sed -n '789,800p' .planning/ROADMAP.md | grep -vq "44-01-PLAN.md" &amp;&amp; grep -A 14 "### Phase 54: Particle" .planning/ROADMAP.md | grep -q "44-01-PLAN.md"</automated>
  </verify>
  <acceptance_criteria>
    - Phase 53 entry contains line `- [x] 53-01-PLAN.md — Mesh gradient clean ratification (3/3)`
    - Phase 53 entry no longer contains `44-01-PLAN.md`
    - Phase 54 entry STILL contains `44-01-PLAN.md` (out of scope)
    - Total `53-01-PLAN.md` occurrences in ROADMAP.md = 1
    - Total `44-01-PLAN.md` occurrences decremented from 5 → 4
  </acceptance_criteria>
  <done>
    Phase 53 in ROADMAP.md correctly points at this plan's filename with the checkbox ticked. All other phases' identical stale references deliberately preserved.
  </done>
</task>

</tasks>

<verification>
- `grep -c "^- \[x\] \*\*MSH-0[1-3]\*\*" .planning/REQUIREMENTS.md` returns `3`
- `grep -cE "^\| MSH-0[1-3] \| Phase 53 \| Ratified \|" .planning/REQUIREMENTS.md` returns `3`
- `grep -c "RATIFIED 2026-04-25" .planning/REQUIREMENTS.md` returns at least `22` (was 19 after Phase 52; +3 = MSH-01/02/03)
- `grep -c "OBSOLETE 2026-04-25" .planning/REQUIREMENTS.md` returns `8` (unchanged — clean phase)
- ROADMAP.md Phase 53 plan ref reads `53-01-PLAN.md — Mesh gradient clean ratification (3/3)` with `[x]`
- ROADMAP.md `44-01-PLAN.md` occurrences decremented from 5 → 4
- Zero source code (`app/`, `components/`, `lib/`, `hooks/`, `package.json`) changes
</verification>
