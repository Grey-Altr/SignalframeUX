---
phase: 54-particle-field
plan: 01
status: complete
completed: "2026-04-25"
commit: "48f5f2c"
---

# Summary — Phase 54 Plan 01: Particle field ratification

## What was done

Four Phase 54 requirements (PTL-01..04) audited against shipped code. Verdict: 3 Ratified / 1 Obsolete — mixed-verdict pattern resumes after the Phase 48 / 52 / 53 clean-ratification streak. The clean run ended because PTL-03 contains the canonical process-gate clause "physical iOS Safari device test passes (no context loss, no black canvas)" — same retroactive-temporal-impossibility precedent as VHS-06 / GRN-04 / HLF-04 / VRG-02.

PTL-01 ratifies via dual-mode: structural ratification (MSH-02 precedent) for the `ParticleField` WebGL artifact at `components/animation/particle-field.tsx:135` (`useSignalScene(containerRef as React.RefObject<HTMLElement | null>, buildScene)` — singleton enforcement via the canonical hook used by 12 sibling animation primitives), and technique-divergence ratification (HLF-01 precedent) for the live `ParticleFieldHQ` Canvas2D variant at `components/animation/particle-field-hq.tsx`. The WebGL flavor is `@status reference-template` per the lockdown audit (`particle-field.tsx:4-8`) — no live consumer wires it up — but the artifact exists and structurally satisfies the spec wording. The Canvas2D variant definitionally creates zero WebGL contexts (header comment line 10: "Canvas2D renderer (no additional WebGL context needed)"), strictly satisfying the "no second WebGL context" invariant via API substitution. Live consumer chain: `app/builds/page.tsx:36` + `app/builds/[slug]/page.tsx:151` → `sf-signal-composer-lazy.tsx:11` → `sf-signal-composer.tsx:113` → `ParticleFieldHQ`.

PTL-02 ratifies cleanly: `particle-field.tsx:37-43` ships `getParticleCount()` with literal exact-spec stepping (`cores <= 2` → 0, `cores <= 4` → 2000, else 5000) using `navigator.hardwareConcurrency`. The HQ variant at `particle-field-hq.tsx:53-55` consumes the canonical `getQualityTier()` from `@/lib/effects` — the quality-tier consumption mandated by `feedback_consume_quality_tier.md` — and pulls a tier-scaled count from `getPreset("particle")` at line 66-67. Both flavors satisfy "stepped count based on device capability"; the WebGL flavor matches the literal 0/2000/5000 numbers via direct hardware-concurrency thresholding, the HQ flavor uses the broader `QualityTier` abstraction (which encapsulates hardware-concurrency + GPU-tier + memory checks under one canonical API).

PTL-04 ratifies: `particle-field.tsx:142-144` early-returns from the GSAP ticker effect when `window.matchMedia("(prefers-reduced-motion: reduce)").matches`. The SignalCanvas singleton already renders the initial built scene frame (per the inline comment at line 142 "SignalCanvas already handles static frame — skip ticker"), so the visible result is one static frame of distributed particles with zero animation. The HQ Canvas2D variant at `particle-field-hq.tsx:59` early-returns from the entire `useEffect` hook before the `requestAnimationFrame` loop starts — the `<canvas>` element renders empty/transparent (equivalent to a static single empty frame).

PTL-03 obsoletes on retroactive-temporal-impossibility: no iOS Safari test artifacts exist in `tests/` (verified via `ls tests/ | grep -i particle` returning empty), and no git commit references iOS particle testing (verified via `git log --all --grep "particle.*iOS\|particle.*Safari"`). The `particle-field.tsx` source DOES contain iOS-Safari-aware defensive code — header line 23 ("iOS Safari: no Float32Array resizing, proper disposal, minimal draw calls"); inline line 93 ("Pre-allocate all buffers once — no resizing (iOS Safari stability)"); the buffer pre-allocation at lines 94-95 implements the documented stability strategy literally — but defensive code is not a test-passed artifact. The spec requires a test outcome (no context loss, no black canvas), not a defensive code property. Forward-looking iOS verification remains available via the Chromatic infrastructure (VRG-01 ratified) and manual `pnpm dev` iOS Safari sessions, but cannot retroactively satisfy the spec.

Zero source-code mutations — pure documentation reconciliation per `feedback_ratify_reality_bias.md` and `project_overdue_lockin_mode.md`.

## Changes made

**PTL-01 / PTL-02 / PTL-04 — Ratified · PTL-03 — Obsolete** (single REQUIREMENTS.md commit `61d3d3b`):

| Req | Citation | What's there |
|-----|----------|--------------|
| PTL-01 | `components/animation/particle-field.tsx:135` (singleton call) + `:19` (header comment 'no second WebGL context') + `:4-8` (@status reference-template) + `components/animation/particle-field-hq.tsx:10` ('Canvas2D renderer (no additional WebGL context needed)') + `components/animation/sf-signal-composer.tsx:113` (live consumer) + `app/builds/page.tsx:36` + `app/builds/[slug]/page.tsx:151` | `useSignalScene(containerRef as React.RefObject<HTMLElement \| null>, buildScene)` registers with the SignalCanvas singleton — same pattern as 12 sibling animation primitives (`glitch-pass.tsx:157`, `signal-mesh.tsx:290`, `displace-field.tsx:154`, `feedback-field.tsx:145`, `proof-shader.tsx:359`, `bloom-pass.tsx:152`, `glsl-hero.tsx:333`, `glsl-signal.tsx:247`, etc.). The WebGL flavor is `@status reference-template` per the lockdown audit — no live consumer — but the artifact structurally satisfies the spec. Live shipping uses `ParticleFieldHQ` (Canvas2D), which definitionally creates zero WebGL contexts. Both flavors honor the spec invariant via different mechanisms: structural ratification (singleton enforcement) for the WebGL flavor, technique-divergence ratification (Canvas2D substitution) for the live consumer chain. |
| PTL-02 | `components/animation/particle-field.tsx:37-43` (getParticleCount) + `components/animation/particle-field-hq.tsx:16` (getQualityTier import) + `:53-55` (getQualityTier consumption) + `:66-67` (preset count) | `getParticleCount()` body: `cores = navigator.hardwareConcurrency \|\| 2; if (cores <= 2) return 0; if (cores <= 4) return 2000; return 5000`. Literal exact-spec match. HQ Canvas2D variant uses `getQualityTier()` from `@/lib/effects` (the canonical quality-tier API mandated by `feedback_consume_quality_tier.md`) and pulls `pCount = countOverride ?? preset.count` from `getPreset("particle")`. Tier `"fallback"` cleanly maps to "0 particles" via the early return at `particle-field-hq.tsx:58` + `:166` (returns `null`). Both flavors satisfy "stepped count based on device capability". |
| PTL-03 | OBSOLETE — process-gate retroactive-temporal-impossibility | Same precedent as VHS-06 / GRN-04 / HLF-04 / VRG-02. No iOS Safari test artifacts in `tests/` (verified empty via `ls tests/ \| grep -i particle`); no git commit references iOS particle testing (verified empty via `git log --all --grep "particle.*iOS\|particle.*Safari"`). Defensive iOS-Safari-aware code at `particle-field.tsx:23` (header) + `:93-95` (buffer pre-allocation) implements the documented stability strategy but is not a test-passed artifact. Spec wording requires a test outcome, not a defensive code property. |
| PTL-04 | `components/animation/particle-field.tsx:142-144` (matchMedia early-return) + `components/animation/particle-field-hq.tsx:59` (matchMedia early-return) | `particle-field.tsx`: `if (typeof window !== "undefined" && window.matchMedia("(prefers-reduced-motion: reduce)").matches) return;` — early-returns from the GSAP ticker effect; SignalCanvas singleton already renders the initial built scene frame (per inline comment line 142 "SignalCanvas already handles static frame — skip ticker"); visible result is one static frame with zero animation. `particle-field-hq.tsx:59`: `if (window.matchMedia("(prefers-reduced-motion: reduce)").matches) return;` — early-returns from the entire `useEffect` hook before the `requestAnimationFrame` loop starts; `<canvas>` element renders empty/transparent. Both flavors achieve "static single-frame render". |

REQUIREMENTS.md body status lines (298-301) updated to `[x]`/`[-]` with literal `RATIFIED 2026-04-25` / `OBSOLETE 2026-04-25` annotations + file:line citations. Traceability matrix rows 381-384 updated.

**Bookkeeping** (single ROADMAP.md commit `48f5f2c`):
- Line 811 corrected: `- [ ] 44-01-PLAN.md — Fix all copy across 6 source files...` → `- [x] 54-01-PLAN.md — Particle field ratification (3 Ratified / 1 Obsolete)`. Checkbox ticked because the plan IS complete by the time it commits.
- Phases 55, 56 still carry the same stale `44-01-PLAN.md` reference (3 remaining occurrences). Deliberately preserved.

## Verification

| Check | Outcome |
|-------|---------|
| `grep -c "^- \[x\] \*\*PTL-0[124]\*\*" .planning/REQUIREMENTS.md` returns 3 | PASS |
| `grep -c "^- \[-\] \*\*PTL-03\*\*" .planning/REQUIREMENTS.md` returns 1 | PASS |
| PTL-01 body line contains `RATIFIED 2026-04-25` + cites `particle-field.tsx:135` + `useSignalScene` + `particle-field-hq.tsx` + `sf-signal-composer.tsx:113` | PASS |
| PTL-02 body line contains `RATIFIED 2026-04-25` + cites `particle-field.tsx:37-43` + `getQualityTier` + `getPreset("particle")` | PASS |
| PTL-03 body line contains `OBSOLETE 2026-04-25` + cites `process-gate retroactive-temporal-impossibility` + `VHS-06 / GRN-04 / HLF-04 / VRG-02` | PASS |
| PTL-04 body line contains `RATIFIED 2026-04-25` + cites `particle-field.tsx:142-144` + `prefers-reduced-motion` + `particle-field-hq.tsx:59` | PASS |
| Matrix rows `\| PTL-0[124] \| Phase 54 \| Ratified \|` exist (3 rows) | PASS |
| Matrix row `\| PTL-03 \| Phase 54 \| Obsolete \|` exists | PASS |
| Zero matrix rows match `^\| PTL-0[0-9] \| Phase 54 \| Pending \|` | PASS |
| `RATIFIED 2026-04-25` count in REQUIREMENTS.md = 25 (was 22 after Phase 53; +3 PTL) | PASS |
| `OBSOLETE 2026-04-25` count in REQUIREMENTS.md = 9 (was 8 after Phase 53; +1 PTL) | PASS |
| ROADMAP.md Phase 54 plan ref reads `54-01-PLAN.md — Particle field ratification (3 Ratified / 1 Obsolete)` | PASS |
| ROADMAP.md Phase 54 plan checkbox is `[x]` | PASS |
| Phase 55, 56 entries STILL contain `44-01-PLAN.md` (out of scope, must remain) | PASS |
| `54-01-PLAN.md` occurrence count in ROADMAP.md = 1 | PASS |
| `44-01-PLAN.md` occurrence count decremented from 4 → 3 | PASS |
| Zero source code (`app/`, `components/`, `lib/`, `hooks/`, `package.json`) changes | PASS |

All 2 task acceptance criteria met. No regression in adjacent matrix rows (SIG-* / GRN-* / VHS-* / DTM-* / HLF-* / CIR-* / MSH-* still Ratified/Obsolete/Complete from Phases 48-53; GLT-/SYM-/VRG-03/PRF- untouched).

## Notes

**Mixed-verdict streak resumes.** Phase 47 had 1 Complete + 2 Ratified + 1 Obsolete; Phase 48 had 6/6 Ratified (first clean case); Phase 49 had 1 Ratified + 4 Obsolete; Phase 50 had 4 Ratified + 2 Obsolete; Phase 51 had 3 Ratified + 1 Obsolete; Phase 52 had 3/3 Ratified (second clean case); Phase 53 had 3/3 Ratified (third clean case). Phase 54 returns to mixed verdict (3 Ratified + 1 Obsolete). The pattern lock holds: requirement sets containing process-gate clauses ("human visual review", "physical device test passes", "before changes" temporal gates) produce predictable Obsolete annotations regardless of how cleanly the non-gate requirements ratify. PTL-03's "physical iOS Safari device test passes" is the canonical fifth process-gate obsolescence on this branch (after VHS-06 / GRN-04 / HLF-04 / VRG-02).

**PTL-01 introduces dual-mode ratification.** Phases 47-53 ratified via single-mode patterns: clean spec match (most), structural ratification (MSH-02), technique-divergence (HLF-01). Phase 54 PTL-01 is the **first dual-mode ratification on this branch** — the WebGL `ParticleField` artifact ratifies structurally (singleton enforcement via `useSignalScene` line 135 + 12-sibling-primitive pattern), AND the live Canvas2D `ParticleFieldHQ` variant ratifies via technique-divergence (Canvas2D substitution preserving the "no second WebGL context" invariant). Both modes apply because the codebase contains both flavors and the live-vs-reference distinction matters for the audit. Documenting this explicitly: future ratifications of dual-flavor requirements (e.g. SYM-* if the symbol system ships both a sprite-sheet and an inline-SVG variant) should consider whether single-mode or dual-mode covers the surface area.

**SIG-02 carry-forward chain breaks here.** Phases 48 / 50 / 51 / 52 leveraged the `global-effects.tsx:39-57` derived-property carry-forward — VHS-01, HLF-02, CIR-02 each pre-satisfied at the wiring level by the SIG-02 intensity bridge. The priming intel for Phase 54 flagged `--sfx-fx-particle-opacity` (derived at `global-effects.tsx:57`) as the predicted "fourth carry-forward beneficiary candidate". Reality: the chain breaks. Particle code reads `--sfx-signal-intensity` directly (`particle-field.tsx:54` + `particle-field-hq.tsx:38`), bypassing the derived `--sfx-fx-particle-opacity` slot entirely. The slot is declared in CSS at `globals.css:282` (with default `0`) and written by the bridge at `global-effects.tsx:57` (`Math.round(i * 0.6 * tm * 1000) / 1000`), but no consumer reads it. This is a subtle architectural divergence: VHS / halftone / circuit consume their derived slots via CSS `var(--sfx-vhs-noise-opacity)` etc., while particle consumers cache the upstream `--sfx-signal-intensity` in JS for per-frame computation. The derivation curve at line 57 is dead code at the time of audit — flagged for potential cleanup or future consumer wiring.

**Eighth ratification in the v1.7 audit campaign.** Phase 47 (commits `709e8e9` / `e1dcf8f` / `fa7e70f` / `0ff2e38`) → Phase 48 (`b0d7a51` / `99e83d6` / `40895ad`) → Phase 49 (`efe6aad` / `c2294c1` / `a3e1eee`) → Phase 50 (`5b2e0e5` / `cff3923` / `b856405`) → Phase 51 (`9f36b66` / `9fd9f85` / `a8e5a52`) → Phase 52 (`f289e91` / `16d3759` / `844e5c3`) → Phase 53 (`77b4d73` / `ecfe305` / `cac8298`) → Phase 54 (`61d3d3b` / `48f5f2c` / this commit). Pattern locked: REQUIREMENTS first, ROADMAP second, PLAN+SUMMARY third.

**Carry-forward intel for Phase 55 (Glitch Transition) audit.** Per the v1.7 handoff Phase 55 priming (still pending re-write): GLT-01 / GLT-02 / GLT-03. Likely consumer files: search for `glitch-pass.tsx` (already grep-confirmed at `useSignalScene` line 157 — same singleton pattern as PTL-01 WebGL flavor). The `--sfx-fx-glitch-rate` slot is derived at `global-effects.tsx:56` (literal `Math.round(i * 0.15 * tm * 1000) / 1000`) — needs grep against actual glitch consumers to determine whether SIG-02 chain re-establishes for GLT or remains broken (PTL-* precedent suggests breaks may continue). GLT-02's "triggered by idle escalation Phase 3 (45s)" is dependency-tied to Phase 49's GRN-02/03 obsolescence chain (idle escalation activator removed by `a260238`) — predicted partial-obsolete. GLT-03 is a subjective "feels like signal dropout, not decoration" gate — predicted obsolete (process-gate-equivalent for subjective feel; same family as VHS-06's "human visual coherence review"). Predicted Phase 55 verdict: 1 Ratified (GLT-01 if CSS clip-path animation exists) + 2 Obsolete (GLT-02 dependency-obsolete, GLT-03 subjective-gate-obsolete). Mixed-verdict streak likely continues.

**Stale doc-comment cleanup carry-forward (still deferred).** Phase 49 flagged `global-effects.tsx:165-186, 201` as `IdleOverlay` JSDoc residue from `a260238`. Phases 50, 51, 52, 53, 54 audits did not touch those lines (PTL-* grep targets hit `global-effects.tsx:57` for the `--sfx-fx-particle-opacity` derivation — well above the JSDoc residue block — and that line's consumer turned out to be empty). Carry forward to Phase 55. The cleanup may need its own dedicated commit at the end of the v1.7 audit campaign. Phase 54 surfaces a second cleanup candidate: `global-effects.tsx:57` itself is dead code (the derived `--sfx-fx-particle-opacity` slot is unconsumed). Both items can be addressed in a single end-of-campaign cleanup commit.
