---
phase: 57-diagnosis-pass-aesthetic-of-record-lock-in
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/codebase/AESTHETIC-OF-RECORD.md
autonomous: true
requirements:
  - AES-01
  - AES-02
  - AES-03
  - AES-04
must_haves:
  truths:
    - "AESTHETIC-OF-RECORD.md exists at .planning/codebase/AESTHETIC-OF-RECORD.md and is committed."
    - "Doc cites LOCKDOWN.md sections by heading text + line number (no orphan section anchors)."
    - "Doc enumerates the four standing rules AES-01..AES-04 by name."
    - "Doc lists the three trademark primitive families (T1 pixel-sort, T2 navbar glyph, T3 cube-tile) with verified file paths from CONTEXT canonical_refs."
    - "Doc cites app/globals.css :root token block by line range (121-386)."
    - "Doc length is approximately 150 lines (target per CONTEXT D-01)."
  artifacts:
    - path: ".planning/codebase/AESTHETIC-OF-RECORD.md"
      provides: "Standing-rules + canonical-references entry point for v1.8 perf phases"
      contains: "AES-01, AES-02, AES-03, AES-04, LOCKDOWN.md §, app/globals.css, components/blocks/entry-section.tsx, components/layout/nav.tsx, components/blocks/inventory-section.tsx, --sfx-cube-hue"
  key_links:
    - from: ".planning/codebase/AESTHETIC-OF-RECORD.md"
      to: ".planning/LOCKDOWN.md"
      via: "section heading text + line-number citations (drift-resistant per Pitfall E)"
      pattern: "LOCKDOWN\\.md §[0-9]"
    - from: ".planning/codebase/AESTHETIC-OF-RECORD.md"
      to: "app/globals.css"
      via: ":root token block reference"
      pattern: "app/globals\\.css:121-386"
    - from: "Phases 58-62 (consumers)"
      to: ".planning/codebase/AESTHETIC-OF-RECORD.md"
      via: "AES-04 per-phase pixel-diff gate; cohort review trigger; no-rebaseline rule"
      pattern: "AESTHETIC-OF-RECORD\\.md"
---

<objective>
Create `.planning/codebase/AESTHETIC-OF-RECORD.md` as the perf-phase entry-point document that codifies the four AES standing rules (AES-01..04) and points downstream agents at the right slice of LOCKDOWN.md, `app/globals.css`, and trademark primitive component code.

Purpose: Phases 58-62 read this doc to know what perf changes must NOT violate. CONTEXT D-01 locks the doc as ~150 lines of standing rules + canonical references — NOT a re-extraction (LOCKDOWN.md already is the extraction).

Output: One new committed markdown file. Zero application code change. Smoke-grep-verified citation integrity (Pitfall E mitigation).
</objective>

<execution_context>
@/Users/greyaltaer/.claude/pde-os/engines/gsd/workflows/execute-plan.md
@/Users/greyaltaer/.claude/pde-os/engines/gsd/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/LOCKDOWN.md
@.planning/phases/57-diagnosis-pass-aesthetic-of-record-lock-in/57-CONTEXT.md
@.planning/phases/57-diagnosis-pass-aesthetic-of-record-lock-in/57-RESEARCH.md
@.planning/phases/57-diagnosis-pass-aesthetic-of-record-lock-in/57-VALIDATION.md
@CLAUDE.md
@app/globals.css

<interfaces>
<!-- Verified LOCKDOWN.md section heading + line numbers extracted via `grep -n "^##\|^###" .planning/LOCKDOWN.md`. -->
<!-- Cite by heading text + line number per Pitfall E (Cross-Reference Drift mitigation). -->

LOCKDOWN.md section headings (verified 2026-04-25):

§1 · TRADEMARK PRIMITIVES (line 27)
  T1 · PIXEL-SORT — the signal trademark (line 31)
  T2 · GLYPH LANGUAGE — the mark trademark (line 43)
  T3 · CUBE-TILE BOX — the container trademark (line 55)
  T4 · `//` SEPARATOR — the coded-naming trademark (line 72)

§2 · COLOR SYSTEM (line 83)
  2.1 Slot-typed, two-hue adaptive model (line 85)
  2.2 Saturation asymmetry — R-40 (line 100)
  2.3 Feedback tier exception — R-40-exception (line 105)
  2.4 OKLCH matrix (line 110)
  2.5 Borderless-first — R-60 (line 114)

§3 · TYPOGRAPHY (line 131)
  3.1 Four-voice system (line 133)
  3.2 Scale (minor third, base 13) (line 145)
  3.3 Display-lockup rules (line 149)
  3.4 UPPERCASE mono register (line 157)

§4 · SPACING + LAYOUT (line 163)
  4.1 Blessed 9 stops (line 165)
  4.2 Layout tokens (line 168)
  4.3 Zero border-radius, system-wide (line 175)
  4.4 Panel model — R-63 (line 178)

§5 · MOTION + ANIMATION (line 208)
  5.1 Duration tokens (line 210)
  5.2 Easing tokens (line 214)
  5.3 Signal-leads-frame staging (line 220)
  5.4 Scroll-trigger types (the three modes) (line 227)
  5.5 Reduced-motion fallbacks — MANDATORY (line 234)

§6 · SIGNAL LAYER — HIG (line 239)
§7 · FRAME CHROME — HIG (line 291)
§8 · CONTENT GRAMMAR (line 323)
§9 · INTERACTION — HIG (line 362)

Trademark primitive component paths (from CONTEXT.md `<canonical_refs>`, verified):

T1 · Pixel-sort signal:
  components/blocks/entry-section.tsx
  components/dossier/pointcloud-ring.tsx
  components/dossier/pointcloud-ring-worker.ts
  components/dossier/iris-cloud.tsx
  components/dossier/iris-cloud-worker.ts

T2 · Navbar glyph style:
  components/layout/nav.tsx
  components/layout/nav-overlay.tsx
  components/layout/nav-reveal-mount.tsx
  components/layout/frame-navigation.tsx

T3 · Cube-tile box:
  components/blocks/inventory-section.tsx (semantic)
  components/layout/instrument-hud.tsx (cube-fill consumer)
  components/layout/cd-corner-panel.tsx (cube-fill consumer)
  app/globals.css (`--sfx-cube-hue` slot)

Token source-of-truth:
  app/globals.css:121-386 (`:root` block; `--sf-*` sizing/canvas + `--sfx-*` color/theme/duration/spacing)
</interfaces>
</context>

<threat_model>
**ASVS L1 surface for Plan 01:**

| Vector | Surface | Threat | Mitigation |
|--------|---------|--------|------------|
| Network | None | n/a | No network calls; pure markdown authoring |
| Auth/Authz | None | n/a | No auth/secret handling |
| Input handling | None | n/a | No user input parsing; doc is statically authored |
| Secrets | None | n/a | No env vars, no API keys, no tokens committed |
| Code execution | None | n/a | Markdown text only; no scripts, no eval |
| Repo hygiene | New file in `.planning/codebase/` | None — sibling docs (ARCHITECTURE.md, CONCERNS.md, etc.) already in this dir | Atomic commit; descriptive message |

**Net new attack surface: ZERO.** Plan 01 commits one read-only diagnostic markdown artifact. No production code, no runtime change, no auth/network/secrets touched.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Author AESTHETIC-OF-RECORD.md (standing rules + canonical citations)</name>

  <read_first>
    - .planning/LOCKDOWN.md (full file — to confirm section heading text matches the line numbers in `<interfaces>` above; if any heading text drifted since 2026-04-25 grep, use the current heading text)
    - .planning/phases/57-diagnosis-pass-aesthetic-of-record-lock-in/57-CONTEXT.md (decisions D-01 scope; canonical_refs trademark paths)
    - .planning/phases/57-diagnosis-pass-aesthetic-of-record-lock-in/57-RESEARCH.md (§Architecture Patterns Example 4 citation pattern target style; §Pitfall E cite-by-line-number rule)
    - app/globals.css (lines 1-50 to confirm `:root` block exists at line ~121; if line range has shifted, use current line range and update `<interfaces>` reference accordingly in the doc)
    - .planning/codebase/ARCHITECTURE.md (FIRST 50 lines — for prose-voice + heading-style precedent)
    - CLAUDE.md (§Token System policy header)
  </read_first>

  <files>.planning/codebase/AESTHETIC-OF-RECORD.md</files>

  <action>
Create `.planning/codebase/AESTHETIC-OF-RECORD.md` with EXACTLY the following structure. Target ~150 lines. Tone: terse, declarative, no marketing voice — match LOCKDOWN.md and ARCHITECTURE.md precedent.

**File header (frontmatter-style top block):**
```
# AESTHETIC-OF-RECORD — v1.8 Speed of Light

> Perf-phase entry point. Codifies the four AES standing rules + points
> downstream phases at the canonical aesthetic invariant sources.
> AUTHORITATIVE source for invariants: LOCKDOWN.md.
> This document CITES — does NOT duplicate.

**Created:** 2026-04-25 (Phase 57, AES-01)
**Owner:** v1.8 milestone — read by Phases 58-62
**Sibling:** .planning/LOCKDOWN.md (v1.0 LOCKED 2026-04-23, R-01..R-64 + DECIDEs D-01..D-11)
```

**Section 1: PURPOSE (~10 lines).** Why this doc exists. Three bullets:
- "Phases 58-62 read AES-01..04 from here, not LOCKDOWN.md, so the standing-rule surface is one entry point."
- "LOCKDOWN.md remains the AUTHORITATIVE invariant set. AESTHETIC-OF-RECORD.md is the citation map."
- "Lock-in mode (`feedback_lockin_before_execute.md`): no re-derivation. Every claim cites shipped code."

**Section 2: STANDING RULES (~50 lines).** Four numbered subsections, one per AES requirement. Each subsection MUST contain (a) the rule statement verbatim from REQUIREMENTS.md, (b) at least one citation to LOCKDOWN.md or `app/globals.css`, (c) the enforcement scope (which phase(s) read this rule).

  **AES-01 — Extract from shipped code; never re-derive.**
  Statement: ".planning/codebase/AESTHETIC-OF-RECORD.md created at v1.8 start, extracted from shipped code (lock-in mode per `feedback_lockin_before_execute.md`). Every phase reads from it; no re-derivation per-phase."
  Authoritative sources (cite, do NOT duplicate):
    - LOCKDOWN.md §1 "TRADEMARK PRIMITIVES" (line 27) — T1 PIXEL-SORT, T2 GLYPH LANGUAGE, T3 CUBE-TILE BOX, T4 `//` SEPARATOR
    - LOCKDOWN.md §2 "COLOR SYSTEM" (line 83) — slot-typed two-hue model, R-40 saturation asymmetry, OKLCH matrix
    - LOCKDOWN.md §3 "TYPOGRAPHY" (line 131) — four-voice system, scale, display-lockup rules
    - LOCKDOWN.md §4.1 "Blessed 9 stops" (line 165) — spacing tokens
    - LOCKDOWN.md §4.4 "Panel model — R-63" (line 178)
    - LOCKDOWN.md §5 "MOTION + ANIMATION" (line 208) — duration tokens, easing tokens, signal-leads-frame staging, reduced-motion mandatory
    - LOCKDOWN.md §6 "SIGNAL LAYER — HIG" (line 239)
    - LOCKDOWN.md §9.7 "Keyboard model — R-64" (line 388)
    - `app/globals.css:121-386` — `:root` token block (`--sf-*` sizing/canvas/nav-state + `--sfx-*` color/theme/duration/spacing). Includes `--sfx-cube-hue`, `--sfx-primary-foreground`, motion easing tokens.
  Trademark primitive component paths (must remain visible after any aesthetic-affecting change per `feedback_trademark_primitives.md`):
    - **T1 Pixel-sort signal:** `components/blocks/entry-section.tsx`, `components/dossier/pointcloud-ring.tsx`, `components/dossier/pointcloud-ring-worker.ts`, `components/dossier/iris-cloud.tsx`, `components/dossier/iris-cloud-worker.ts`.
    - **T2 Navbar glyph style:** `components/layout/nav.tsx`, `components/layout/nav-overlay.tsx`, `components/layout/nav-reveal-mount.tsx`, `components/layout/frame-navigation.tsx`.
    - **T3 Cube-tile box:** `components/blocks/inventory-section.tsx` (semantic), `components/layout/instrument-hud.tsx` + `components/layout/cd-corner-panel.tsx` (cube-fill consumers), `app/globals.css` (`--sfx-cube-hue` slot).
  Enforcement: Every Phase 58-62 plan MUST read this section before authoring any change.

  **AES-02 — No Chromatic re-baseline for perf changes.**
  Statement: "No Chromatic re-baseline for perf changes. Only documented exception: Anton `optional` → `swap` migration in CRT-03." (REQUIREMENTS.md line 55)
  Documented exception: CRT-03 (Phase 59) ratifies Anton `font-display: optional → swap` migration with tuned `size-adjust` + `ascent-override` + `descent-override` + `line-gap-override` against `Impact, Helvetica Neue Condensed, Arial Black` fallback. The visible/invisible-by-design exception per `feedback_ratify_reality_bias.md`.
  Anti-pattern: "Just approve the new Chromatic baseline" emerging as a phase pattern (PITFALLS Pitfall #15). Re-baselining is reserved for deliberate aesthetic decisions, never for perf side-effects.
  Enforcement: Phases 58, 59 (except CRT-03), 60, 61, 62 — Chromatic diff is read-only verification.

  **AES-03 — Mid-milestone cohort review by external eye.**
  Statement: "Mid-milestone cohort review by external eye after Phase 60 against `.planning/visual-baselines/v1.8-start/`. 'Feels different' without specific code-change cause escalates." (REQUIREMENTS.md line 56)
  Mechanism: chrome-devtools MCP scroll-test (per `feedback_visual_verification.md`) + side-by-side review of v1.8-start baselines vs. current state.
  Trigger: After Phase 60 (LCP intervention) ships and before Phase 61 begins. Real-device cohort review per Pitfall #10 (mobile emulation pass != real-device pass).
  Enforcement: Phase 60 close-out checklist; STATE.md notes when AES-03 fires.

  **AES-04 — Per-phase pixel-diff <=0.5%.**
  Statement: "Pixel-diff vs v1.8-start snapshot at every phase end; >0.5% diff flagged for human review." (REQUIREMENTS.md line 57)
  Mechanism: Same Playwright harness as DGN-03 baseline capture (Plan 02). Run WITHOUT `--update-snapshots` to compare against committed `.planning/visual-baselines/v1.8-start/`.
  Threshold: 0.5% pixel-diff ratio. Greater diff blocks phase sign-off pending human review.
  Threshold-automation deferred: CI gate vs human-review-only is Phase 58 (CIB) or Phase 62 (final-gate) decision per CONTEXT deferred-ideas.
  Enforcement: Every Phase 58-62 close-out gate.

**Section 3: AUTHORITATIVE SOURCE MAP (~30 lines).** A reference table that downstream agents read first. Format as a markdown table:

| Concern | Source | Line/Section | Notes |
|---------|--------|--------------|-------|
| Trademark primitives | LOCKDOWN.md §1 | line 27 | T1-T4; never modify without explicit milestone scope |
| Color system | LOCKDOWN.md §2 | line 83 | OKLCH; slot-typed; R-40 saturation asymmetry |
| Typography | LOCKDOWN.md §3 | line 131 | Four-voice; minor-third scale; UPPERCASE mono |
| Spacing | LOCKDOWN.md §4.1 | line 165 | Blessed 9 stops — no arbitrary values |
| Panel model | LOCKDOWN.md §4.4 | line 178 | R-63 |
| Motion | LOCKDOWN.md §5 | line 208 | Duration + easing tokens; signal-leads-frame; reduced-motion mandatory |
| SIGNAL HIG | LOCKDOWN.md §6 | line 239 | Three-layer composition; intensity hierarchy; rAF discipline; quality-tier |
| FRAME chrome | LOCKDOWN.md §7 | line 291 | Telemetry, route-ID grammar, breadcrumbs |
| Content grammar | LOCKDOWN.md §8 | line 323 | Coded naming; component category codes |
| Interaction | LOCKDOWN.md §9 | line 362 | Hover/active register; focus rings; touch targets; keyboard model R-64 |
| Tokens | app/globals.css | lines 121-386 | `:root` block; `--sf-*` + `--sfx-*` split |
| Trademark T1 path | components/blocks/entry-section.tsx + 4 dossier files | n/a | Pixel-sort signal |
| Trademark T2 path | components/layout/nav*.tsx (4 files) | n/a | Navbar glyph |
| Trademark T3 path | components/blocks/inventory-section.tsx + 2 layout files + globals.css | n/a | Cube-tile box; `--sfx-cube-hue` slot |

**Section 4: CAPTURED-STATE NOTE (Q3 resolution placeholder, ~15 lines).** Reserve a section titled "Captured-state definition (per DGN-03 baseline)" with placeholder text:

```
## Captured-state definition (per DGN-03 baseline)

The 20 v1.8-start baseline images at `.planning/visual-baselines/v1.8-start/`
represent the WARM-state Anton outcome (per Q1 resolution: planner forces
warm-state via `document.fonts.load('700 100px Anton')` in
`tests/v1.8-baseline-capture.spec.ts`). The reduced-motion path
(`page.emulateMedia({ reducedMotion: 'reduce' })`) is enabled to short-circuit
GSAP scroll-triggered reveals, so the captured state is the
"reduced-motion + warm-Anton" variant.

Rendering backend: SwiftShader (headless Chromium software WebGL), per
`playwright.config.ts:24-26`. Real-GPU visual review is the AES-03 cohort
review job, NOT the AES-04 pixel-diff gate.

Hero opacity at capture: TBD by Plan 02 Task 3 (one-shot probe of captured
PNG). If hero `<h1>` chars at `entry-section.tsx:124-129` are at `opacity:
0.01` (sf-hero-deferred start state per STATE.md D-08), the captured baseline
shows them as effectively invisible — this is the canonical reduced-motion
state and is correct.

(This section is finalized by Plan 02 Task 3 after the first baseline-capture
run.)
```

**Section 5: ENFORCEMENT MATRIX (~15 lines).** A short table mapping each downstream phase to which AES rules it must read:

| Phase | Reads | Enforcement Trigger |
|-------|-------|--------------------|
| 58 (LHCI + RUM) | AES-01, AES-02, AES-04 | Pixel-diff vs v1.8-start at phase end (AES-04) |
| 59 (Critical-path) | AES-01, AES-02 (+ documented Anton exception), AES-04 | Per-PR pixel-diff; CRT-03 is the AES-02 exception ratification |
| 60 (LCP intervention) | AES-01, AES-03, AES-04 | Mid-milestone cohort review (AES-03) fires after Phase 60 |
| 61 (Bundle hygiene) | AES-01, AES-02, AES-04 | Pixel-diff at phase end |
| 62 (Verification) | AES-01..04 (all) | Final-gate audit; cumulative drift check |

**Section 6: CHANGE LOG (~5 lines).** A bottom block for tracking edits to this doc only.

```
---

## Change Log

| Date | Change | By |
|------|--------|----|
| 2026-04-25 | AES-01 deliverable — standing rules + canonical refs codified at v1.8 start | Phase 57, Plan 01 |
```

**Critical authoring rules:**
1. EVERY citation to LOCKDOWN.md uses `LOCKDOWN.md §X.X "Heading text" (line N)` format. The heading text is what protects against silent anchor drift (Pitfall E).
2. EVERY trademark file path is exactly as listed in CONTEXT.md `<canonical_refs>` lines 68-71 — do not invent paths.
3. NO new aesthetic decisions. NO ranking of LCP-02 candidates. NO Phase-60 plan-shape suggestions. This doc is REFERENCE, not directive.
4. Target ~150 lines. If your draft is over 200 lines, you are duplicating LOCKDOWN.md content — cut.
5. Use `pnpm exec prettier --write .planning/codebase/AESTHETIC-OF-RECORD.md` is OPTIONAL; not required if the file already conforms to repo prose conventions.
  </action>

  <verify>
    <automated>test -f .planning/codebase/AESTHETIC-OF-RECORD.md && [ "$(grep -cE 'LOCKDOWN\.md §' .planning/codebase/AESTHETIC-OF-RECORD.md)" -ge 8 ] && grep -q 'app/globals.css:121-386' .planning/codebase/AESTHETIC-OF-RECORD.md && grep -q 'AES-01' .planning/codebase/AESTHETIC-OF-RECORD.md && grep -q 'AES-02' .planning/codebase/AESTHETIC-OF-RECORD.md && grep -q 'AES-03' .planning/codebase/AESTHETIC-OF-RECORD.md && grep -q 'AES-04' .planning/codebase/AESTHETIC-OF-RECORD.md</automated>
  </verify>

  <acceptance_criteria>
    - File exists: `test -f .planning/codebase/AESTHETIC-OF-RECORD.md` returns 0.
    - File length is between 100 and 220 lines: `wc -l .planning/codebase/AESTHETIC-OF-RECORD.md` reports value in range (target ~150 per D-01; tolerance band).
    - File cites LOCKDOWN.md sections by heading text + line number at least 8 times: `grep -cE 'LOCKDOWN\.md §' .planning/codebase/AESTHETIC-OF-RECORD.md` returns >= 8.
    - File cites token source: `grep -q 'app/globals.css:121-386' .planning/codebase/AESTHETIC-OF-RECORD.md` returns 0.
    - File names all four AES rules: `grep -cE 'AES-0[1-4]' .planning/codebase/AESTHETIC-OF-RECORD.md` returns >= 4.
    - File names trademark T1 path: `grep -q 'components/blocks/entry-section.tsx' .planning/codebase/AESTHETIC-OF-RECORD.md` returns 0.
    - File names trademark T2 path: `grep -q 'components/layout/nav.tsx' .planning/codebase/AESTHETIC-OF-RECORD.md` returns 0.
    - File names trademark T3 path: `grep -q 'components/blocks/inventory-section.tsx' .planning/codebase/AESTHETIC-OF-RECORD.md` returns 0.
    - File names cube-tile slot: `grep -q -- '--sfx-cube-hue' .planning/codebase/AESTHETIC-OF-RECORD.md` returns 0.
    - File names AES-02 rule semantics: `grep -iE 'no.*re-baseline|optional.*swap' .planning/codebase/AESTHETIC-OF-RECORD.md` matches at least once.
    - File names AES-03 rule semantics: `grep -iE 'cohort review|external eye' .planning/codebase/AESTHETIC-OF-RECORD.md` matches at least once.
    - File names AES-04 rule semantics: `grep -iE '0\.5%|pixel-diff' .planning/codebase/AESTHETIC-OF-RECORD.md` matches at least once.
  </acceptance_criteria>

  <done>
AESTHETIC-OF-RECORD.md exists at `.planning/codebase/AESTHETIC-OF-RECORD.md`, ~150 lines, codifies AES-01..04 standing rules with verifiable LOCKDOWN.md heading-text + line-number citations, lists all three trademark primitive families with shipped paths, cites `app/globals.css:121-386` token block. Citation-integrity smoke grep returns >= 8 LOCKDOWN.md references and all AES IDs appear at least once. Captured-state section reserved for Plan 02 Task 3 finalization.
  </done>
</task>

<task type="auto">
  <name>Task 2: Smoke-citation integrity grep + atomic commit</name>

  <read_first>
    - .planning/codebase/AESTHETIC-OF-RECORD.md (newly authored)
    - .planning/LOCKDOWN.md (full file — to confirm every cited heading text is still present at the cited line number)
  </read_first>

  <files>(no new files; this task verifies + commits Task 1 output)</files>

  <action>
Run a citation-integrity smoke check, then commit AESTHETIC-OF-RECORD.md atomically. Pitfall E (Cross-Reference Drift) demands every cited LOCKDOWN.md heading text exist in the LOCKDOWN.md HEAD.

**Step 1: Citation-integrity grep.** For every line in AESTHETIC-OF-RECORD.md matching `LOCKDOWN.md §`, extract the quoted heading text and confirm it appears in LOCKDOWN.md. Run:

```bash
# Extract every quoted heading text from AESTHETIC-OF-RECORD.md citations.
# Format expected: LOCKDOWN.md §X.X "Heading text" (line N)
grep -oE 'LOCKDOWN\.md §[0-9.]+\s+"[^"]+"' .planning/codebase/AESTHETIC-OF-RECORD.md \
  | sed -E 's/.*"([^"]+)".*/\1/' \
  | sort -u \
  > /tmp/aesthetic-cites.txt

# Confirm each cited heading text appears in LOCKDOWN.md.
MISSING=0
while IFS= read -r heading; do
  if ! grep -qF "$heading" .planning/LOCKDOWN.md; then
    echo "MISSING: $heading"
    MISSING=$((MISSING+1))
  fi
done < /tmp/aesthetic-cites.txt
test "$MISSING" -eq 0
```

If any cited heading is MISSING from LOCKDOWN.md HEAD, do NOT commit. Update AESTHETIC-OF-RECORD.md to reflect the current heading text in LOCKDOWN.md (per `feedback_ratify_reality_bias.md` — ratify reality, don't revert to stale).

**Step 2: Trademark path existence check.** Confirm every trademark primitive file path in AESTHETIC-OF-RECORD.md exists on disk:

```bash
for path in \
  components/blocks/entry-section.tsx \
  components/dossier/pointcloud-ring.tsx \
  components/dossier/pointcloud-ring-worker.ts \
  components/dossier/iris-cloud.tsx \
  components/dossier/iris-cloud-worker.ts \
  components/layout/nav.tsx \
  components/layout/nav-overlay.tsx \
  components/layout/nav-reveal-mount.tsx \
  components/layout/frame-navigation.tsx \
  components/blocks/inventory-section.tsx \
  components/layout/instrument-hud.tsx \
  components/layout/cd-corner-panel.tsx \
  app/globals.css; do
  test -f "$path" || { echo "MISSING PATH: $path"; exit 1; }
done
```

If any path is missing on disk, the trademark file enumeration is stale — update AESTHETIC-OF-RECORD.md or the canonical_refs source.

**Step 3: Atomic commit.** Use plain `git add` + `git commit` per project precedent (CLAUDE.md §Git: `Feat:` / `Fix:` / `Chore:` prefixes; no `gsd-tools.cjs` wrapper — that helper is not part of this project's commit pipeline).

```bash
git add .planning/codebase/AESTHETIC-OF-RECORD.md
git commit -m "Feat(57-01): AESTHETIC-OF-RECORD.md — AES-01..04 standing rules + LOCKDOWN.md citation map"
```

Confirm commit landed: `git log -1 --oneline | grep -q '57-01'`.
  </action>

  <verify>
    <automated>grep -oE 'LOCKDOWN\.md §[0-9.]+\s+"[^"]+"' .planning/codebase/AESTHETIC-OF-RECORD.md | sed -E 's/.*"([^"]+)".*/\1/' | sort -u | while read h; do grep -qF "$h" .planning/LOCKDOWN.md || { echo "MISSING: $h"; exit 1; }; done && for p in components/blocks/entry-section.tsx components/layout/nav.tsx components/blocks/inventory-section.tsx app/globals.css; do test -f "$p" || exit 1; done && git log -1 --oneline | grep -q '57-01'</automated>
  </verify>

  <acceptance_criteria>
    - Every quoted heading text in `LOCKDOWN.md §X.X "Heading"` citations within AESTHETIC-OF-RECORD.md exists verbatim somewhere in `.planning/LOCKDOWN.md`. Grep loop in `<action>` Step 1 exits 0.
    - Every trademark primitive file path enumerated in AESTHETIC-OF-RECORD.md exists on disk. Loop in `<action>` Step 2 exits 0.
    - A new commit landed with message containing `57-01` and the file `.planning/codebase/AESTHETIC-OF-RECORD.md` is in the commit: `git log -1 --name-only | grep -q 'AESTHETIC-OF-RECORD.md'`.
    - `git status` shows clean tree (no AESTHETIC-OF-RECORD.md modifications uncommitted): `git status --porcelain | grep -q 'AESTHETIC-OF-RECORD.md' && exit 1; exit 0`.
    - Commit was made via plain `git commit` (not `gsd-tools.cjs commit`) per CLAUDE.md §Git precedent. (No verification needed beyond the commit landing — the executor follows the action block.)
  </acceptance_criteria>

  <done>
Citation integrity smoke grep passes (every cited LOCKDOWN.md heading exists; every trademark path exists on disk). AESTHETIC-OF-RECORD.md committed atomically with descriptive message naming Phase-57-Plan-01 and AES-01 deliverable. Working tree clean for the doc.
  </done>
</task>

</tasks>

<verification>
**Plan 01 phase-level checks:**

1. `test -f .planning/codebase/AESTHETIC-OF-RECORD.md` — file exists.
2. `wc -l .planning/codebase/AESTHETIC-OF-RECORD.md` — between 100 and 220 lines (target ~150).
3. `grep -cE 'LOCKDOWN\.md §' .planning/codebase/AESTHETIC-OF-RECORD.md` — >= 8 LOCKDOWN.md citations.
4. `grep -cE 'AES-0[1-4]' .planning/codebase/AESTHETIC-OF-RECORD.md` — all four AES IDs appear.
5. Citation-integrity loop (Task 2 Step 1) — every cited heading exists in LOCKDOWN.md HEAD.
6. Trademark-path loop (Task 2 Step 2) — every cited path exists on disk.
7. `git log -1 --oneline` — commit landed; message mentions 57-01.
</verification>

<success_criteria>
- AES-01 deliverable shipped: `.planning/codebase/AESTHETIC-OF-RECORD.md` committed and discoverable to Phases 58-62.
- AES-02 standing rule documented in §AES-02 of the new doc with the Anton optional→swap exception called out.
- AES-03 standing rule documented in §AES-03 with cohort-review trigger.
- AES-04 standing rule documented in §AES-04 with the 0.5% pixel-diff threshold.
- Plan 01 introduces ZERO application code change (frontmatter `files_modified` lists only the new doc).
- Atomic commit with message naming the phase + plan + AES-01 deliverable.
- Plan 03 (LCP diagnosis) and Plan 02 (baseline capture) consume Plan 01's AESTHETIC-OF-RECORD.md placeholder; both are wave 2 (this plan is wave 1, the sole wave-1 plan).
</success_criteria>

<output>
After completion, create `.planning/phases/57-diagnosis-pass-aesthetic-of-record-lock-in/57-01-SUMMARY.md` documenting:
- Final line count of AESTHETIC-OF-RECORD.md.
- Number of LOCKDOWN.md citations.
- Whether the captured-state section was finalized in this plan or left for Plan 02 Task 3 (expected: left for Plan 02).
- Commit SHA + message.
</output>
</output>
