---
phase: 57-diagnosis-pass-aesthetic-of-record-lock-in
plan: 03
type: execute
wave: 2
depends_on:
  - 57-02
files_modified:
  - tests/v1.8-lcp-diagnosis.spec.ts
  - .planning/codebase/v1.8-lcp-diagnosis.md
  - .planning/codebase/v1.8-lcp-evidence.json
autonomous: false
requirements:
  - DGN-01
  - DGN-02
must_haves:
  truths:
    - "Running `tests/v1.8-lcp-diagnosis.spec.ts` against `pnpm build && pnpm start` captures LCP element identity (selector + startTime + size + url) for `/` at mobile-360x800 AND desktop-1440x900."
    - "Spec captures BOTH cold-state (no font preload, simulating Anton optional skip) AND warm-state (after `document.fonts.load`) LCP candidates per viewport (Pitfall A bimodal LCP)."
    - "`rm -rf .next && ANALYZE=true pnpm build` produces `.next/analyze/client.html` with the v1.8 chunk visualization."
    - "`v1.8-lcp-diagnosis.md` exists with three sections: §1 LCP Element Identity (DGN-01), §2 Per-Chunk Owner Attribution (DGN-02), §3 Diagnostic-only — no Phase-60 ranking (D-02)."
    - "Chunk attribution table looks for v1.7 chunk IDs `3302`, `e9a6067a`, `74c6194b`, `7525` AND lists ALL initial chunks > 50KB (Pitfall D — chunk-ID drift fallback)."
    - "Diagnosis doc distinguishes `public/sf-canvas-sync.js` external render-blocker from inline IIFE in `app/layout.tsx` (RESEARCH finding 6) so Phase 59 CRT-01 doesn't conflate them."
  artifacts:
    - path: "tests/v1.8-lcp-diagnosis.spec.ts"
      provides: "LCP element identity capture harness (cold + warm) per viewport"
      exports: "test.describe block running 4 cases (2 viewports × 2 font states)"
      min_lines: 60
    - path: ".planning/codebase/v1.8-lcp-diagnosis.md"
      provides: "DGN-01 LCP element identity table + DGN-02 chunk attribution table — strictly diagnostic"
      contains: "## 1. LCP Element Identity, ## 2. Per-Chunk Owner Attribution, ## 3. Diagnostic notes — no intervention ranking, public/sf-canvas-sync.js, app/layout.tsx inline scaleScript"
      min_lines: 80
    - path: ".planning/codebase/v1.8-lcp-evidence.json"
      provides: "Machine-readable LCP capture output from the spec — cited by v1.8-lcp-diagnosis.md"
      contains: "[{viewport, fontState, selector, startTime, size, url}, ...] (4 entries minimum)"
  key_links:
    - from: "tests/v1.8-lcp-diagnosis.spec.ts"
      to: ".planning/codebase/v1.8-lcp-evidence.json"
      via: "Spec writes JSON output to disk via fs.writeFile"
      pattern: "v1\\.8-lcp-evidence\\.json"
    - from: ".planning/codebase/v1.8-lcp-diagnosis.md"
      to: ".planning/codebase/v1.8-lcp-evidence.json"
      via: "Markdown table embeds + cites the JSON evidence"
      pattern: "v1\\.8-lcp-evidence\\.json"
    - from: ".planning/codebase/v1.8-lcp-diagnosis.md"
      to: ".next/analyze/client.html"
      via: "DGN-02 chunk attribution table is hand-extracted from analyzer HTML per CONTEXT D-04"
      pattern: "ANALYZE=true pnpm build"
    - from: "Phase 59 CRT-01 plan-phase (consumer)"
      to: ".planning/codebase/v1.8-lcp-diagnosis.md §3"
      via: "Sync-script disambiguation: external file vs inline IIFE"
      pattern: "public/sf-canvas-sync.js|inline.*scaleScript"
    - from: "Phase 60 plan-phase (consumer)"
      to: ".planning/codebase/v1.8-lcp-diagnosis.md §1"
      via: "Reads cold-vs-warm LCP identity to pick from LCP-02 candidates a/b/c"
      pattern: "LCP element identity"
    - from: "Phase 61 plan-phase (consumer)"
      to: ".planning/codebase/v1.8-lcp-diagnosis.md §2"
      via: "Reads chunk attribution to populate optimizePackageImports additions"
      pattern: "optimizePackageImports"
---

<objective>
Author `tests/v1.8-lcp-diagnosis.spec.ts` (LCP element identity capture for `/` at mobile-360x800 + desktop-1440x900, both cold-state and warm-state Anton), run it, run `rm -rf .next && ANALYZE=true pnpm build` for chunk attribution, then write `.planning/codebase/v1.8-lcp-diagnosis.md` containing the LCP table + chunk attribution table + diagnostic notes.

Purpose: DGN-01 + DGN-02. Phase 59 reads §3 (sync-script disambiguation), Phase 60 reads §1 (LCP identity → pick from LCP-02 a/b/c), Phase 61 reads §2 (chunk attribution → optimizePackageImports additions). CONTEXT D-02 locks the doc as STRICTLY DIAGNOSTIC — no intervention ranking.

Output: One new spec file + one JSON evidence artifact + one diagnosis markdown doc. Zero application code change.

Wave 2 dependency: Plan 02 must ship first because (a) it confirms the production server lifecycle works, (b) Plan 03 reuses the same `pnpm build && pnpm start` pattern, (c) the chunk-attribution `rm -rf .next` step is independent BUT the LCP spec runs against the same prod build Plan 02 verified.

Note on `autonomous: false`: Task 4 requires interactive HTML treemap inspection (CONTEXT D-04 + VALIDATION.md Manual-Only Verifications). An autonomous LLM executor cannot hover an interactive `.next/analyze/client.html` treemap to extract chunk → package mappings. Tasks 1-3 (LCP spec author + capture + analyzer build) and Task 5 (commit) remain `type="auto"`. Task 4 is `type="checkpoint:human-verify"`. The plan-level `autonomous: false` flag is the clarifying signal that this plan mixes auto + checkpoint task types.
</objective>

<execution_context>
@/Users/greyaltaer/.claude/pde-os/engines/gsd/workflows/execute-plan.md
@/Users/greyaltaer/.claude/pde-os/engines/gsd/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/research/PITFALLS.md
@.planning/phases/57-diagnosis-pass-aesthetic-of-record-lock-in/57-CONTEXT.md
@.planning/phases/57-diagnosis-pass-aesthetic-of-record-lock-in/57-RESEARCH.md
@.planning/phases/57-diagnosis-pass-aesthetic-of-record-lock-in/57-VALIDATION.md
@playwright.config.ts
@next.config.ts
@tests/phase-35-lcp-homepage.spec.ts
@app/layout.tsx
@components/blocks/entry-section.tsx
@components/animation/ghost-label.tsx
@components/layout/scale-canvas.tsx
@public/sf-canvas-sync.js
@CLAUDE.md

<interfaces>
<!-- Verified at planning time, 2026-04-25 -->

PerformanceObserver LCP element capture pattern (extension of `tests/phase-35-lcp-homepage.spec.ts:17-23`):
  Returns the entry's `.element`, `.startTime`, `.size`, `.url`, `.renderTime`.
  Selector composition: tag + id + class chain (deterministic across runs).

Cold vs warm Anton state (Pitfall A, RESEARCH §Q1):
  Cold: do NOT call `document.fonts.load`; let Anton's `display: optional` race the 100ms window naturally.
  Warm: `await page.evaluate(() => document.fonts.load('700 100px "Anton"'))` before LCP measurement.

  Cold-state determinism note: Playwright's per-test `BrowserContext` is fresh by default
  (each `test()` call gets a brand-new context unless `test.use({ storageState })` is set).
  A fresh context means an empty HTTP cache AND an empty font cache, so the cold-state run
  truly does experience Anton's `display: optional` race against the 100ms window — no need
  to manually clear caches between runs. See playwright.config.ts (no shared-context fixtures
  declared) and the per-test `async ({ page })` signature below.

Two viewports per DGN-01 (ROADMAP.md:881 explicit mobile 360 + desktop 1440):
  { name: "mobile-360x800", width: 360, height: 800 }
  { name: "desktop-1440x900", width: 1440, height: 900 }

Bundle analyzer wiring (next.config.ts:4-6, verified):
  withBundleAnalyzer({ enabled: process.env.ANALYZE === "true" })
  Output: .next/analyze/{client.html, nodejs.html, edge.html}
  Build script (package.json:59): "next build" → webpack (NOT --turbopack; Turbopack is dev-only)

Stale-chunk guard (BND-04, Pitfall #13):
  rm -rf .next && ANALYZE=true pnpm build

v1.7 named chunk IDs to look for (per CONTEXT phase_specifics):
  3302, e9a6067a, 74c6194b, 7525
  Pitfall D: chunk IDs may have shifted in v1.8. Diagnosis must accept "no match found" + list all initial chunks > 50KB as completeness fallback.

Sync-script disambiguation (RESEARCH finding 6):
  External file: public/sf-canvas-sync.js (loaded as <script src="/sf-canvas-sync.js" /> from components/layout/scale-canvas.tsx:143)
  Inline IIFE: app/layout.tsx:90-117 — inline `scaleScript` const injected via React's HTML-string injection prop into a <script> tag in <head>
  These are TWO DIFFERENT THINGS. Phase 59 CRT-01 targets the external file. The inline IIFE is the same logic but pre-existing inline (not part of CRT-01 scope).

LCP candidate elements (verified by source read):
  Homepage <h1>SIGNALFRAME//UX</h1> at components/blocks/entry-section.tsx:124-129 (controlled by `sf-hero-deferred` class with opacity: 0.01 start state per STATE.md D-08 + globals.css:694, 1701, 1715)
  THESIS GhostLabel at components/animation/ghost-label.tsx:18 (clamp 200-400px Anton, opacity 3-5%, aria-hidden — may not qualify as LCP candidate due to low opacity)
</interfaces>
</context>

<threat_model>
**ASVS L1 surface for Plan 03:**

| Vector | Surface | Threat | Mitigation |
|--------|---------|--------|------------|
| Network | localhost:3000 (production-mode dev server) | None | All requests stay on `127.0.0.1` |
| Auth/Authz | None | n/a | Public homepage route only |
| Input handling | None | n/a | Spec is deterministic; no untrusted input |
| Secrets | None | n/a | No env vars consumed except `ANALYZE=true` (build-time flag, not a secret) |
| Code execution | New TypeScript spec + new markdown doc + JSON artifact | Standard test code; no eval, no dynamic require | Reviewed alongside existing `tests/phase-35-*.spec.ts` precedent |
| Repo hygiene | New JSON evidence file in `.planning/codebase/` | None — sibling docs already in this dir | Atomic commit |
| Build artifacts | `.next/` rebuilt with ANALYZE flag | Stale chunks (Pitfall #13) | `rm -rf .next` BEFORE build per BND-04 |

**Net new attack surface: ZERO.** Plan 03 commits a Playwright spec + JSON evidence + diagnostic markdown. No production code, no auth/network/secrets, no runtime change.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Author tests/v1.8-lcp-diagnosis.spec.ts (cold + warm LCP capture)</name>

  <read_first>
    - tests/phase-35-lcp-homepage.spec.ts (full file — base PerformanceObserver pattern)
    - .planning/research/PITFALLS.md §Pitfall #1 (Anton bimodal LCP, lines 12-37) — informs cold/warm distinction
    - .planning/phases/57-diagnosis-pass-aesthetic-of-record-lock-in/57-RESEARCH.md §Pattern 2 (PerformanceObserver LCP Element Capture, lines 203-243) — extension pattern
    - .planning/phases/57-diagnosis-pass-aesthetic-of-record-lock-in/57-RESEARCH.md §Pitfall A (lines 291-301) — both states must be captured
    - components/blocks/entry-section.tsx (lines 124-160 — to confirm what selector PerformanceObserver should report for LCP)
    - app/layout.tsx (lines 40-55 — to confirm Anton family-name string for the warm-state load)
    - tests/v1.8-baseline-capture.spec.ts (Plan 02 output — for spec file structural precedent)
  </read_first>

  <files>tests/v1.8-lcp-diagnosis.spec.ts</files>

  <action>
Create `tests/v1.8-lcp-diagnosis.spec.ts`. Spec runs 4 tests: 2 viewports × 2 font states (cold = no fonts.load forcing; warm = fonts.load forced). Each test captures LCP element identity (selector + startTime + size + url) and writes incrementally to `.planning/codebase/v1.8-lcp-evidence.json`.

**File header:**
```typescript
import { test, expect } from "@playwright/test";
import path from "node:path";
import { promises as fs } from "node:fs";

/**
 * Phase 57 DGN-01 — v1.8 LCP element identity capture.
 *
 * MUST run against `pnpm build && pnpm start` (production build), NOT `pnpm dev`.
 * Reasons identical to phase-35-lcp-homepage.spec.ts:5-8 — React dev double-render
 * inflates LCP timing; Next.js dev overlay markup pollutes element selection.
 *
 * Pitfall A (Anton bimodal LCP): Anton ships display: optional. On a cold visit
 * Anton may not paint (skipped after 100ms window). The LCP element under cold
 * state may differ from warm state. This spec captures BOTH so Phase 60 plan-phase
 * has the full picture.
 *
 * COLD-STATE DETERMINISM: Playwright spawns a fresh BrowserContext per test()
 * by default (no shared `storageState` declared in playwright.config.ts; default
 * `async ({ page })` fixture pattern). A fresh context = empty HTTP cache AND
 * empty font cache, so the cold-state runs truly experience Anton's
 * `display: optional` race against the 100ms window. No manual cache eviction
 * between cold and warm runs is required.
 *
 * Output: .planning/codebase/v1.8-lcp-evidence.json (4 entries — 2 viewports × 2 font states)
 * Re-run command: pnpm exec playwright test tests/v1.8-lcp-diagnosis.spec.ts --project=chromium
 */
```

**Constants block:**
```typescript
const VIEWPORTS = [
  { name: "mobile-360x800", width: 360, height: 800 },
  { name: "desktop-1440x900", width: 1440, height: 900 },
] as const;

const FONT_STATES = [
  { name: "cold", forceLoad: false },
  { name: "warm", forceLoad: true },
] as const;

const EVIDENCE_PATH = path.resolve(
  process.cwd(),
  ".planning/codebase/v1.8-lcp-evidence.json"
);

type LcpEntry = {
  viewport: string;
  fontState: string;
  startTime: number;
  selector: string | null;
  elementSize: number;
  resourceUrl: string;
  capturedAt: string;
};
```

**Helper to merge results into the JSON file (append-on-each-test for resilience):**
```typescript
async function appendEvidence(entry: LcpEntry): Promise<void> {
  let existing: LcpEntry[] = [];
  try {
    const raw = await fs.readFile(EVIDENCE_PATH, "utf8");
    existing = JSON.parse(raw) as LcpEntry[];
  } catch {
    // First write — file doesn't exist yet.
  }
  // Replace entry for the same viewport/fontState if it already exists,
  // otherwise append. This makes re-runs idempotent.
  const filtered = existing.filter(
    (e) => !(e.viewport === entry.viewport && e.fontState === entry.fontState)
  );
  filtered.push(entry);
  await fs.mkdir(path.dirname(EVIDENCE_PATH), { recursive: true });
  await fs.writeFile(EVIDENCE_PATH, JSON.stringify(filtered, null, 2) + "\n");
}
```

**Test body:**
```typescript
test.describe("@v1.8-diagnosis LCP element identity (DGN-01)", () => {
  for (const vp of VIEWPORTS) {
    for (const state of FONT_STATES) {
      test(`/ @ ${vp.name} (${state.name}-Anton)`, async ({ page }) => {
        // Cold-state: clear browser's font cache by setting Cache-Control header
        // bypass via the storage state. Playwright's per-test page is a fresh
        // context already, so font cache is naturally empty for the first request.
        // For warm-state, we explicitly force the font to load before measurement.
        await page.setViewportSize({ width: vp.width, height: vp.height });

        // Do NOT enable reduced-motion here — diagnosis captures the LIVE
        // first-paint state, not the curated reduced-motion variant. Plan 02
        // owns the reduced-motion baseline; Plan 03 owns the diagnosis.

        await page.goto("/", { waitUntil: "load" });

        if (state.forceLoad) {
          // Warm-state: force Anton to be present.
          await page.evaluate(() => document.fonts.load('700 100px "Anton"'));
          await page.evaluate(() => document.fonts.ready);
        } else {
          // Cold-state: let display: optional play out. Wait for any LCP
          // to fire by giving the page 2.5s (LCP detection window upper bound).
          await page.waitForTimeout(2500);
        }

        // Capture LCP entry with element identity.
        const lcpInfo = await page.evaluate(() => new Promise<{
          startTime: number;
          selector: string | null;
          elementSize: number;
          resourceUrl: string;
        }>((resolve) => {
          new PerformanceObserver((list) => {
            const entries = list.getEntries();
            const last = entries[entries.length - 1] as PerformanceEntry & {
              element?: Element;
              size?: number;
              url?: string;
            };
            const el = last.element ?? null;
            const selector = el
              ? `${el.tagName.toLowerCase()}${el.id ? "#" + el.id : ""}${
                  el.className && typeof el.className === "string"
                    ? "." + el.className.split(/\s+/).filter(Boolean).join(".")
                    : ""
                }`
              : null;
            resolve({
              startTime: last.startTime,
              selector,
              elementSize: last.size ?? 0,
              resourceUrl: last.url ?? "",
            });
          }).observe({ type: "largest-contentful-paint", buffered: true });
        }));

        // Persist to evidence JSON.
        await appendEvidence({
          viewport: vp.name,
          fontState: state.name,
          startTime: lcpInfo.startTime,
          selector: lcpInfo.selector,
          elementSize: lcpInfo.elementSize,
          resourceUrl: lcpInfo.resourceUrl,
          capturedAt: new Date().toISOString(),
        });

        // Sanity: an LCP entry must have been captured.
        expect(lcpInfo.startTime).toBeGreaterThan(0);
      });
    }
  }
});
```

**Critical rules:**
1. Spec writes evidence to `.planning/codebase/v1.8-lcp-evidence.json` directly via `fs.writeFile`. The diagnosis markdown (Task 4) reads this JSON to populate the LCP table.
2. Cold-state does NOT call `document.fonts.load`. Cold means "let `display: optional` race the 100ms window naturally."
3. Warm-state forces Anton with `document.fonts.load` to guarantee Anton is painting at LCP time.
4. Use `waitUntil: "load"` (NOT `networkidle` — diagnosis captures real first-paint timing, not idle-stable).
5. Do NOT enable `reducedMotion: "reduce"` — Plan 03 captures live first-paint, Plan 02 owns the reduced-motion baseline.
6. The spec is idempotent: re-running it replaces evidence entries for the same `(viewport, fontState)` rather than appending duplicates.
7. The cold-state-determinism header comment is REQUIRED — it documents why no manual cache eviction is needed (per Playwright's per-test fresh BrowserContext guarantee). Future maintainers reading the spec must see this rationale to avoid adding redundant `context.clearCookies()` / `context.clearPermissions()` calls.
  </action>

  <verify>
    <automated>test -f tests/v1.8-lcp-diagnosis.spec.ts && grep -q "PerformanceObserver" tests/v1.8-lcp-diagnosis.spec.ts && grep -q "FONT_STATES" tests/v1.8-lcp-diagnosis.spec.ts && grep -q '"cold"' tests/v1.8-lcp-diagnosis.spec.ts && grep -q '"warm"' tests/v1.8-lcp-diagnosis.spec.ts && grep -q 'document.fonts.load' tests/v1.8-lcp-diagnosis.spec.ts && grep -q 'v1.8-lcp-evidence.json' tests/v1.8-lcp-diagnosis.spec.ts && grep -qiE 'fresh BrowserContext|per-test.*context|empty font cache' tests/v1.8-lcp-diagnosis.spec.ts && pnpm exec tsc --noEmit -p tsconfig.json 2>&1 | grep "tests/v1.8-lcp-diagnosis.spec.ts" | grep -i "error" && exit 1; exit 0</automated>
  </verify>

  <acceptance_criteria>
    - File exists: `test -f tests/v1.8-lcp-diagnosis.spec.ts` returns 0.
    - File defines 2 viewports: `grep -cE 'mobile-360x800|desktop-1440x900' tests/v1.8-lcp-diagnosis.spec.ts` returns >= 2.
    - File defines 2 font states: `grep -cE '"cold"|"warm"' tests/v1.8-lcp-diagnosis.spec.ts` returns >= 2.
    - File uses PerformanceObserver: `grep -q 'PerformanceObserver' tests/v1.8-lcp-diagnosis.spec.ts` returns 0.
    - File observes type: largest-contentful-paint: `grep -q 'largest-contentful-paint' tests/v1.8-lcp-diagnosis.spec.ts` returns 0.
    - File forces warm-state Anton: `grep -q 'document.fonts.load' tests/v1.8-lcp-diagnosis.spec.ts` returns 0.
    - File writes to evidence JSON: `grep -q 'v1.8-lcp-evidence.json' tests/v1.8-lcp-diagnosis.spec.ts` returns 0.
    - File header documents production-build gate: `grep -iE 'pnpm build && pnpm start|production build' tests/v1.8-lcp-diagnosis.spec.ts` matches at least once.
    - File header documents bimodal-LCP rationale: `grep -iE 'pitfall A|bimodal|cold.*warm|optional' tests/v1.8-lcp-diagnosis.spec.ts` matches at least once.
    - File header documents cold-state determinism via fresh BrowserContext: `grep -iE 'fresh BrowserContext|per-test.*context|empty font cache' tests/v1.8-lcp-diagnosis.spec.ts` matches at least once.
    - TypeScript clean: `pnpm exec tsc --noEmit 2>&1 | grep -E 'tests/v1\.8-lcp-diagnosis\.spec\.ts.*error'` returns no matches.
    - Playwright lists 4 tests: `pnpm exec playwright test tests/v1.8-lcp-diagnosis.spec.ts --list 2>&1 | grep -c '@ '` returns >= 4.
  </acceptance_criteria>

  <done>
`tests/v1.8-lcp-diagnosis.spec.ts` exists, declares 4 tests (2 viewports × 2 font states), captures LCP element identity via PerformanceObserver, writes evidence to `.planning/codebase/v1.8-lcp-evidence.json`, is TypeScript-clean. Header comment documents the per-test fresh BrowserContext guarantee that makes cold-state runs deterministic without manual cache eviction. Playwright `--list` shows 4 tests.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run LCP diagnosis spec — produce v1.8-lcp-evidence.json</name>

  <read_first>
    - tests/v1.8-lcp-diagnosis.spec.ts (newly authored)
    - .planning/visual-baselines/v1.8-start/ (verify Plan 02 output exists — sanity check that Plan 02 ran successfully before Plan 03 piggybacks on the prod-server lifecycle)
  </read_first>

  <files>.planning/codebase/v1.8-lcp-evidence.json</files>

  <action>
Same prod-server lifecycle pattern as Plan 02 Task 2. Reuses production build (no need to rebuild if Plan 02 already did) but starts a fresh `pnpm start` so the LCP cold-state really is cold.

**Step 0: Delete stale evidence (idempotent re-run guard).** The `appendEvidence` helper in the spec preserves entries by `(viewport, fontState)` key — meaning a partially-failed prior run could leave stale entries that mask current-run failures. Delete before every Task 2 invocation:

```bash
rm -f .planning/codebase/v1.8-lcp-evidence.json
```

This guarantees the post-run `grep -c '"capturedAt"' >= 4` check reflects ONLY entries written by the current invocation. Without this, a re-run that captures only 3 of 4 cases could still appear to pass (the 4th stale entry remaining from a previous run).

**Step 1: Verify Plan 02 output exists** (sanity gate against running Plan 03 standalone before Plan 02):
```bash
[ "$(ls .planning/visual-baselines/v1.8-start/*.png 2>/dev/null | wc -l)" -eq 20 ] || { echo "Plan 02 output missing — run Plan 02 first"; exit 1; }
```

**Step 2: Build production bundle** (if not already built; safe to re-run):
```bash
pnpm build
```

**Step 3: Start production server in the background:**
```bash
pnpm start &
SERVER_PID=$!
for i in $(seq 1 30); do
  if curl -sf http://localhost:3000/ > /dev/null; then break; fi
  sleep 1
done
```

**Step 4: Run the diagnosis spec.** Run with `--workers=1` (already enforced by `playwright.config.ts:8` but explicit is safer for cold-state determinism):
```bash
pnpm exec playwright test tests/v1.8-lcp-diagnosis.spec.ts --project=chromium --reporter=list
```

If any of the 4 tests fail, surface but do NOT delete `.planning/codebase/v1.8-lcp-evidence.json` — partial output is diagnostic data.

**Step 5: Stop production server:**
```bash
kill "$SERVER_PID" || true
wait "$SERVER_PID" 2>/dev/null || true
```

**Step 6: Verify v1.8-lcp-evidence.json exists with 4 entries:**
```bash
test -f .planning/codebase/v1.8-lcp-evidence.json
[ "$(jq '. | length' .planning/codebase/v1.8-lcp-evidence.json)" -eq 4 ]
# Verify each viewport × fontState combination is present
for vp in mobile-360x800 desktop-1440x900; do
  for state in cold warm; do
    jq -e ".[] | select(.viewport == \"$vp\" and .fontState == \"$state\")" \
      .planning/codebase/v1.8-lcp-evidence.json > /dev/null \
      || { echo "Missing entry: $vp / $state"; exit 1; }
  done
done
```

If `jq` is not available (CI image differs), use grep fallback:
```bash
grep -q '"viewport": "mobile-360x800"' .planning/codebase/v1.8-lcp-evidence.json
grep -q '"viewport": "desktop-1440x900"' .planning/codebase/v1.8-lcp-evidence.json
grep -q '"fontState": "cold"' .planning/codebase/v1.8-lcp-evidence.json
grep -q '"fontState": "warm"' .planning/codebase/v1.8-lcp-evidence.json
```
  </action>

  <verify>
    <automated>test -f .planning/codebase/v1.8-lcp-evidence.json && grep -q '"viewport": "mobile-360x800"' .planning/codebase/v1.8-lcp-evidence.json && grep -q '"viewport": "desktop-1440x900"' .planning/codebase/v1.8-lcp-evidence.json && grep -q '"fontState": "cold"' .planning/codebase/v1.8-lcp-evidence.json && grep -q '"fontState": "warm"' .planning/codebase/v1.8-lcp-evidence.json && [ "$(grep -c '"capturedAt"' .planning/codebase/v1.8-lcp-evidence.json)" -ge 4 ]</automated>
  </verify>

  <acceptance_criteria>
    - Stale evidence was deleted before the spec run (Step 0): the post-run `capturedAt` count of >= 4 therefore reflects ONLY the current invocation, not a mix of current + stale entries.
    - File exists: `test -f .planning/codebase/v1.8-lcp-evidence.json` returns 0.
    - File contains 4 entries (one per viewport × fontState combination): `grep -c '"capturedAt"' .planning/codebase/v1.8-lcp-evidence.json` returns >= 4.
    - All 4 viewport/fontState combinations present: 4 grep checks in `<action>` Step 6 all return 0.
    - JSON is valid: `node -e "JSON.parse(require('fs').readFileSync('.planning/codebase/v1.8-lcp-evidence.json','utf8'))"` exits 0.
    - Each entry has non-zero `startTime` (sanity: LCP actually fired): `grep -E '"startTime": [0-9]+\.?[0-9]*' .planning/codebase/v1.8-lcp-evidence.json | grep -v '"startTime": 0' | wc -l` returns >= 4.
    - Production server stopped cleanly: `lsof -i :3000 -t 2>/dev/null` returns no PIDs (or only PIDs unrelated to our spawned `pnpm start`).
  </acceptance_criteria>

  <done>
`.planning/codebase/v1.8-lcp-evidence.json` exists with 4 entries (mobile-360x800/cold, mobile-360x800/warm, desktop-1440x900/cold, desktop-1440x900/warm) — all written by the current invocation (Step 0 cleared stale entries). Each entry has a captured selector + non-zero startTime + elementSize + resourceUrl. JSON is valid and parseable. The cold-vs-warm divergence (if any) is the diagnostic data Phase 60 reads.
  </done>
</task>

<task type="auto">
  <name>Task 3: Run bundle analyzer — produce .next/analyze/client.html chunk attribution data</name>

  <read_first>
    - next.config.ts (full file — confirm withBundleAnalyzer wiring at lines 4-6)
    - .planning/research/PITFALLS.md §Pitfall #13 (stale-chunk guard, lines 316-326)
    - .planning/phases/57-diagnosis-pass-aesthetic-of-record-lock-in/57-RESEARCH.md §Pattern 3 (Bundle-Analyzer Chunk Attribution, lines 244-265) and §Pitfall D (chunk-ID drift, lines 322-333)
  </read_first>

  <files>(no committed files; produces .next/analyze/client.html as a build artifact, then Task 4 transcribes findings into v1.8-lcp-diagnosis.md)</files>

  <action>
BND-04 stale-chunk guard demands `rm -rf .next` BEFORE the analyzer build. Plan 02's prod build is now stale data — start from scratch.

**Step 1: Stale-chunk guard.** Wipe build cache + analyzer output:
```bash
rm -rf .next
```

**Step 2: ANALYZE-flag build:**
```bash
ANALYZE=true pnpm build
```

This emits `.next/analyze/client.html`, `.next/analyze/nodejs.html`, `.next/analyze/edge.html`. The webpack-bundle-analyzer HTML auto-opens in browser by default (analyzer config); on a headless run it just writes to disk and exits.

**Step 3: Verify analyzer output exists and is non-trivial:**
```bash
test -f .next/analyze/client.html
[ "$(wc -c < .next/analyze/client.html)" -gt 100000 ]  # > 100KB indicates real treemap data
```

**Step 4: Capture build size table for cross-reference.** Next.js prints a route + chunk size table at end of `pnpm build`. Plan 02 already saw it; re-running ANALYZE=true should also print it. Capture the output for use in Task 4:

```bash
ANALYZE=true pnpm build 2>&1 | tee .next/build-output.txt
# (Already done in Step 2; this just ensures we have it captured)
```

If the analyzer auto-opens a browser tab on macOS, that's fine — close it manually. The committed artifact is the markdown table extracted by Task 4, not the HTML itself (HTML is `.next/`-relative and rebuilt-on-demand; not committed to repo).

**Step 5: Note current chunk file names.** Inspect the static chunks dir to enumerate what's actually emitted (Pitfall D — chunk IDs may have shifted from v1.7's `3302/e9a6067a/74c6194b/7525`):

```bash
ls -lh .next/static/chunks/*.js 2>/dev/null | sort -k5 -h -r | head -20 > /tmp/v18-chunks.txt
cat /tmp/v18-chunks.txt
```

This gives a size-sorted list of static chunks. Task 4 will reference this when writing the attribution table.

**Step 6: Verify .next/build-output.txt or build log shows the size table.** It should contain a table like "Route (app)" with route + size columns:
```bash
grep -qE "Route \(app\)|First Load JS" .next/build-output.txt 2>/dev/null \
  || pnpm build 2>&1 | grep -qE "Route \(app\)|First Load JS"
```
  </action>

  <verify>
    <automated>test -f .next/analyze/client.html && [ "$(wc -c < .next/analyze/client.html)" -gt 100000 ] && [ "$(ls .next/static/chunks/*.js 2>/dev/null | wc -l)" -gt 0 ]</automated>
  </verify>

  <acceptance_criteria>
    - `.next/analyze/client.html` exists: `test -f .next/analyze/client.html` returns 0.
    - Analyzer HTML is non-trivial size (real treemap data): `wc -c < .next/analyze/client.html` returns > 100000.
    - Static chunks emitted: `ls .next/static/chunks/*.js | wc -l` returns > 0.
    - Build was clean (no stale chunks): `.next` was deleted before build per BND-04. Verifiable by inspecting `.next/build-output.txt` for ".next/cache" warnings — there should be none.
    - Chunk size enumeration captured: `/tmp/v18-chunks.txt` exists and lists real chunk filenames.
  </acceptance_criteria>

  <done>
`.next/analyze/client.html` exists with real treemap data. Static chunks enumerated for Task 4 to attribute. `rm -rf .next` was run BEFORE `ANALYZE=true pnpm build` per BND-04. Build artifacts are NOT committed (`.next/` is gitignored); the diagnostic value is transcribed into `v1.8-lcp-diagnosis.md` by Task 4.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Manual chunk-attribution extraction + diagnosis-doc author (DGN-01 + DGN-02)</name>

  <read_first>
    - .planning/codebase/v1.8-lcp-evidence.json (Task 2 output — 4 LCP entries)
    - .next/analyze/client.html (Task 3 output — open in browser to read chunk → package mappings; CONTEXT D-04 mandates manual extraction)
    - /tmp/v18-chunks.txt (Task 3 output — size-sorted chunk list)
    - .planning/research/PITFALLS.md §Pitfall #13 + §Pitfall D
    - .planning/phases/57-diagnosis-pass-aesthetic-of-record-lock-in/57-RESEARCH.md §Summary (lines 8-14, especially the sync-script disambiguation finding)
    - public/sf-canvas-sync.js (full file — short, single IIFE)
    - app/layout.tsx (lines 90-117 — to confirm the inline `scaleScript` IIFE structure)
    - components/layout/scale-canvas.tsx (lines 110-145 — confirms the `<script src="/sf-canvas-sync.js" />` mount)
  </read_first>

  <what-built>
Plan 03 Tasks 1-3 produced:
- `tests/v1.8-lcp-diagnosis.spec.ts` (LCP capture spec)
- `.planning/codebase/v1.8-lcp-evidence.json` (4 LCP entries)
- `.next/analyze/client.html` (interactive webpack-bundle-analyzer treemap)
- `/tmp/v18-chunks.txt` (size-sorted chunk listing)

Task 4 is the **manual extraction + author step** that turns those raw artifacts into the `v1.8-lcp-diagnosis.md` deliverable. CONTEXT D-04 explicitly mandates manual extraction from the analyzer HTML treemap (no JSON artifact, no `source-map-explorer`). VALIDATION.md Manual-Only Verifications row #1 confirms: "human reads the visualization to extract chunk → package mapping."

This step requires a human to:
1. Open `.next/analyze/client.html` in a browser.
2. Hover/click chunk boxes in the treemap to read package composition + size.
3. Cross-reference against v1.7's named chunk IDs (`3302`, `e9a6067a`, `74c6194b`, `7525`) — recording "no match found" per Pitfall D when applicable.
4. Author `.planning/codebase/v1.8-lcp-diagnosis.md` with the extracted data.

An autonomous LLM executor cannot perform steps 1-3. The plan-level `autonomous: false` flag and this checkpoint are the clarifying signals.
  </what-built>

  <how-to-verify>
**Step A: Open the analyzer treemap.**
```bash
open .next/analyze/client.html
```
(macOS — opens in default browser. Linux: `xdg-open`. Or drag the file into a browser tab.)

**Step B: Extract chunk → package mappings.** For each of the four v1.7 named chunk IDs (`3302`, `e9a6067a`, `74c6194b`, `7525`):
1. Search the treemap visualization (browser Find / Cmd+F on the chunk filename, or scan visually).
2. If found: hover the chunk box to see its module composition. Click to drill down. Record the top 3 packages by size + total chunk size.
3. If not found: record "no match found in current build" (Pitfall D — chunk IDs may have shifted).

**Step C: Pitfall D fallback sweep.** From `/tmp/v18-chunks.txt` (Task 3 output), list ALL initial-chunk modules > 50KB regardless of whether they match v1.7 IDs. Hover each in the treemap, record top packages.

**Step D: Author `.planning/codebase/v1.8-lcp-diagnosis.md`** with EXACTLY four sections. Strictly diagnostic per CONTEXT D-02 — NO ranking of LCP-02 candidates a/b/c, NO Phase-60 plan suggestions, NO Phase-59 sync-script intervention recommendations.

**File header:**
```
# v1.8 LCP Diagnosis — Phase 57 DGN-01 + DGN-02

> Strictly diagnostic. Findings only.
> Phase 60 reads §1 to pick from LCP-02 candidates a/b/c.
> Phase 61 reads §2 to extend `optimizePackageImports`.
> Phase 59 reads §3 for sync-script disambiguation.

**Captured:** YYYY-MM-DD (Phase 57, Plan 03)
**Build:** `rm -rf .next && ANALYZE=true pnpm build` (production webpack; pnpm build = next build, no --turbopack per package.json:59)
**Spec:** tests/v1.8-lcp-diagnosis.spec.ts
**Evidence:** .planning/codebase/v1.8-lcp-evidence.json
**Server lifecycle:** pnpm build && pnpm start (NOT pnpm dev)
```

**§1 — LCP Element Identity (DGN-01).** Markdown table populated from v1.8-lcp-evidence.json. One row per viewport × fontState (4 rows). Plus a paragraph distinguishing cold-vs-warm divergence (or convergence) per Pitfall A.

```
## 1. LCP Element Identity (DGN-01)

| Viewport | Anton state | Selector | startTime (ms) | size | resource URL | File:line evidence |
|----------|-------------|----------|----------------|------|--------------|---------------------|
| mobile-360x800 | cold | [paste from evidence.json] | [paste] | [paste] | [paste] | [look up source — e.g., components/blocks/entry-section.tsx:124-129] |
| mobile-360x800 | warm | [paste] | [paste] | [paste] | [paste] | [look up source] |
| desktop-1440x900 | cold | [paste] | [paste] | [paste] | [paste] | [look up source] |
| desktop-1440x900 | warm | [paste] | [paste] | [paste] | [paste] | [look up source] |

**Cold vs warm divergence (Pitfall A — Anton bimodal LCP):**
[One paragraph stating whether the cold-state selector matches the warm-state selector per viewport.
If they differ — e.g., cold shows fallback-rendered text, warm shows Anton-styled <h1> — note that
explicitly; this is the core data Phase 60 needs.]

**File:line evidence map:**
- Homepage <h1>SIGNALFRAME//UX</h1>: components/blocks/entry-section.tsx:124-129
- Homepage hero opacity controlled by sf-hero-deferred class: app/globals.css:694, 1701, 1715 (opacity: 0.01 start state per STATE.md D-08)
- Anton font-face declaration: app/layout.tsx:45 (display: "optional")
- THESIS GhostLabel (alternative LCP candidate, may not qualify due to opacity 3-5%): components/animation/ghost-label.tsx:18

**Method:** PerformanceObserver({ type: "largest-contentful-paint", buffered: true })
in tests/v1.8-lcp-diagnosis.spec.ts. See file for selector composition logic.
Headless Chromium WebGL backend: SwiftShader (playwright.config.ts:24-26) — see §3 caveat.
```

For the file:line column, look at the captured selector. If it's `h1.something` or `span.sf-display`,
map to the corresponding source location from the read_first list above. If the selector matches no known
candidate, write "selector unmatched — investigate" — this IS diagnostic data, not a failure.

**§2 — Per-Chunk Owner Attribution (DGN-02).** Markdown table extracted from `.next/analyze/client.html`.
First subsection looks for the v1.7 named chunk IDs; second subsection lists ALL initial chunks > 50KB
for completeness (Pitfall D fallback). Emit "no match" for any v1.7 ID not found in the v1.8 build.

```
## 2. Per-Chunk Owner Attribution (DGN-02)

### 2a. v1.7 named chunk IDs (CONTEXT phase_specifics)

| v1.7 chunk ID | v1.8 status | Top packages by size (parsed/gzip) | Total chunk size | Notes |
|---------------|-------------|-------------------------------------|------------------|-------|
| 3302 | [found / no match found in current build] | [if found: list top 3 packages from analyzer hover-tooltip] | [size] | [notes] |
| e9a6067a | [found / no match found] | [...] | [...] | [...] |
| 74c6194b | [found / no match found] | [...] | [...] | [...] |
| 7525 | [found / no match found] | [...] | [...] | [...] |

### 2b. All initial chunks > 50KB (Pitfall D fallback — chunk-ID drift)

Webpack splitChunks may rename chunks across builds (Pitfall D). Below is the complete
list of initial-chunk modules above the 50KB threshold from this build, regardless of
whether the chunk ID matches v1.7's set.

| Chunk filename (.next/static/chunks/) | Size (parsed) | Top packages | Notes |
|---------------------------------------|---------------|--------------|-------|
| [chunk-XYZ.js] | [size] | [packages from analyzer] | [v1.7 match? new finding?] |
| ... | ... | ... | ... |

**Build invocation:** `rm -rf .next && ANALYZE=true pnpm build` (BND-04 stale-chunk guard).
**Analyzer:** @next/bundle-analyzer ^16.2.2 (next.config.ts:4-6).
**Method:** Manual extraction from .next/analyze/client.html treemap (CONTEXT D-04 — no JSON artifact, no source-map-explorer).
**Caveat:** Chunk IDs and packages reflect this specific build. Subsequent v1.8 phase builds may shift IDs.

**NEW_FINDING flag:** [If any chunk > 50KB has no v1.7 counterpart, list it here with a `NEW_FINDING:` prefix. Phase 61 plan-phase will triage.]
```

**§3 — Diagnostic notes (no intervention ranking — D-02).** This section addresses the sync-script disambiguation per CONTEXT phase_specifics + RESEARCH finding 6, and notes the SwiftShader headless caveat per RESEARCH §Q2.

```
## 3. Diagnostic notes — no intervention ranking (D-02)

**Sync-script disambiguation (RESEARCH finding 6):**

The codebase has TWO distinct CLS-prevention mechanisms that look superficially similar:

1. **External render-blocker:** public/sf-canvas-sync.js
   - Loaded as `<script src="/sf-canvas-sync.js" />` from components/layout/scale-canvas.tsx:143
   - Phase 59 CRT-01 targets this file for inline + delete (REQUIREMENTS.md:25).
   - Currently render-blocking by design (commit f6225b4 contract).

2. **Inline IIFE:** const scaleScript in app/layout.tsx:90-117
   - Used as an inline <script> tag in <head> via React's HTML-string injection prop.
   - Pre-existing inline script; computes initial canvas scale before first paint.
   - NOT part of CRT-01 scope. Conflating these two would break CRT-01 measurement.

These are two different things. Phase 59 CRT-01 plan-phase MUST disambiguate.

**Headless WebGL caveat (RESEARCH §Q2):**

LCP measurements in this diagnosis were captured under SwiftShader software WebGL
(playwright.config.ts:24-26). Real-GPU LCP timing on a physical device may differ.
Phase 62 VRF-04 (real-device verification) is the source of truth for production
LCP; Plan 03 captures the synthetic-CI baseline.

**Reduced-motion path:**

Plan 03 spec does NOT enable reduced-motion (in contrast to Plan 02 baseline-capture
spec). LCP measurement reflects the LIVE first-paint state, not the curated reduced-motion
variant. This is intentional — Plan 02 owns the "what does it look like" capture;
Plan 03 owns the "what's the LCP target identity" capture.

**No intervention ranking:**

Per CONTEXT D-02, this document does NOT rank the three Phase-60 LCP-02 candidate
paths (a/b/c). Ranking lives in Phase 60 plan-phase RESEARCH.md, where intervention
tradeoffs are properly contextualized against measurement, LHCI median (Phase 58
output), Phase 59 ship state, and real-device divergence.
```

**§4 — Cross-references.** A short closing block:

```
## 4. Cross-references

- Phase 57 ROADMAP: .planning/ROADMAP.md lines 875-886
- Phase 57 CONTEXT: .planning/phases/57-diagnosis-pass-aesthetic-of-record-lock-in/57-CONTEXT.md
- Phase 57 RESEARCH: .planning/phases/57-diagnosis-pass-aesthetic-of-record-lock-in/57-RESEARCH.md
- LCP evidence (machine-readable): .planning/codebase/v1.8-lcp-evidence.json
- Spec (DGN-01): tests/v1.8-lcp-diagnosis.spec.ts
- Aesthetic invariants entry-point: .planning/codebase/AESTHETIC-OF-RECORD.md
- v1.0 lock authority: .planning/LOCKDOWN.md
```

**Critical authoring rules:**
1. Diagnostic only. NO recommendations. NO "we should fix X by Y."
2. Cite file:line for every claim about source. The diagnosis doc IS the evidence map.
3. If a v1.7 chunk ID has no match in the v1.8 build, RECORD that as data — do not silently omit the row.
4. The "NEW_FINDING:" flag in §2b is the explicit handoff to Phase 61. Use it.
5. Section §3 sync-script disambiguation is REQUIRED — Phase 59 CRT-01 plan-phase fails downstream if these two mechanisms are conflated.
  </how-to-verify>

  <resume-signal>Type "approved" once you have (1) opened `.next/analyze/client.html` and extracted chunk → package mappings, (2) authored `.planning/codebase/v1.8-lcp-diagnosis.md` with all four sections per the template above, (3) verified the doc passes the acceptance grep checks. Or describe issues (e.g., "v1.7 chunk IDs all returned no-match — recorded as such per Pitfall D fallback").</resume-signal>

  <files>.planning/codebase/v1.8-lcp-diagnosis.md</files>

  <action>
After human treemap inspection (per `<how-to-verify>` Steps A-C above), author `.planning/codebase/v1.8-lcp-diagnosis.md` with the four-section template per `<how-to-verify>` Step D. Populate the §1 LCP table from `v1.8-lcp-evidence.json`, the §2 chunk attribution table from the manual treemap extraction, the §3 diagnostic notes from the established sync-script + SwiftShader content, and the §4 cross-references. The acceptance grep checks below validate the structural integrity of the authored document; the human is responsible for the semantic accuracy of the chunk-attribution data.
  </action>

  <verify>
    <automated>test -f .planning/codebase/v1.8-lcp-diagnosis.md && grep -q '## 1. LCP Element Identity' .planning/codebase/v1.8-lcp-diagnosis.md && grep -q '## 2. Per-Chunk Owner Attribution' .planning/codebase/v1.8-lcp-diagnosis.md && grep -q '## 3. Diagnostic notes' .planning/codebase/v1.8-lcp-diagnosis.md && grep -q 'public/sf-canvas-sync.js' .planning/codebase/v1.8-lcp-diagnosis.md && grep -qE 'inline.*scaleScript|app/layout\.tsx.*scaleScript|inline IIFE' .planning/codebase/v1.8-lcp-diagnosis.md && grep -qE 'mobile-360x800|desktop-1440x900' .planning/codebase/v1.8-lcp-diagnosis.md && grep -qE 'cold|warm' .planning/codebase/v1.8-lcp-diagnosis.md && grep -qE '3302|no match found' .planning/codebase/v1.8-lcp-diagnosis.md && grep -q 'rm -rf .next' .planning/codebase/v1.8-lcp-diagnosis.md && grep -q 'ANALYZE=true pnpm build' .planning/codebase/v1.8-lcp-diagnosis.md</automated>
  </verify>

  <acceptance_criteria>
    - File exists: `test -f .planning/codebase/v1.8-lcp-diagnosis.md` returns 0.
    - File length is at least 80 lines: `wc -l .planning/codebase/v1.8-lcp-diagnosis.md` returns >= 80.
    - File has §1 LCP Element Identity heading: `grep -q '## 1. LCP Element Identity' .planning/codebase/v1.8-lcp-diagnosis.md` returns 0.
    - File has §2 Per-Chunk Owner Attribution heading: `grep -q '## 2. Per-Chunk Owner Attribution' .planning/codebase/v1.8-lcp-diagnosis.md` returns 0.
    - File has §3 Diagnostic notes heading: `grep -q '## 3. Diagnostic notes' .planning/codebase/v1.8-lcp-diagnosis.md` returns 0.
    - File names both viewports: `grep -cE 'mobile-360x800|desktop-1440x900' .planning/codebase/v1.8-lcp-diagnosis.md` returns >= 2.
    - File names both font states: `grep -cE 'cold|warm' .planning/codebase/v1.8-lcp-diagnosis.md` returns >= 2.
    - File references all 4 v1.7 chunk IDs OR explicitly notes no-match per ID: `grep -cE '3302|e9a6067a|74c6194b|7525' .planning/codebase/v1.8-lcp-diagnosis.md` returns >= 4. (The token "no match found" is the acceptable per-ID outcome but the ID itself must still appear in the table.)
    - File documents sync-script disambiguation: `grep -q 'public/sf-canvas-sync.js' .planning/codebase/v1.8-lcp-diagnosis.md` AND `grep -qE 'inline.*scaleScript|app/layout\.tsx.*scaleScript|inline IIFE' .planning/codebase/v1.8-lcp-diagnosis.md` BOTH return 0.
    - File documents BND-04 stale-chunk guard: `grep -q 'rm -rf .next' .planning/codebase/v1.8-lcp-diagnosis.md` AND `grep -q 'ANALYZE=true pnpm build' .planning/codebase/v1.8-lcp-diagnosis.md` BOTH return 0.
    - File contains NO Phase-60 LCP-02 ranking language: `grep -iE 'recommend.*candidate|prefer (THESIS|ghost-label|hero h1)|Phase 60 should' .planning/codebase/v1.8-lcp-diagnosis.md` returns no matches (D-02 strictly diagnostic).
    - File cites file:line evidence for at least the homepage h1: `grep -q 'components/blocks/entry-section.tsx:' .planning/codebase/v1.8-lcp-diagnosis.md` returns 0.
  </acceptance_criteria>

  <done>
`.planning/codebase/v1.8-lcp-diagnosis.md` exists with §1 LCP identity table (4 rows from evidence.json), §2 chunk attribution table (v1.7 ID lookup + > 50KB completeness sweep — extracted manually from the analyzer treemap), §3 sync-script disambiguation + SwiftShader caveat + no-intervention-ranking statement, §4 cross-references. Pitfall A bimodal LCP, Pitfall D chunk-ID drift, RESEARCH finding 6 sync-script disambiguation all addressed. NO Phase-60 ranking. Phase 59/60/61 plan-phases can each consume the relevant slice.
  </done>
</task>

<task type="auto">
  <name>Task 5: Atomic commit (spec + evidence + diagnosis doc)</name>

  <read_first>
    - tests/v1.8-lcp-diagnosis.spec.ts
    - .planning/codebase/v1.8-lcp-evidence.json
    - .planning/codebase/v1.8-lcp-diagnosis.md
  </read_first>

  <files>(no new files; commits the three Task 1-4 outputs)</files>

  <action>
Single atomic commit for all Plan 03 artifacts. Commit message follows project precedent (`Feat:` per CLAUDE.md §Git):

**Step 1: Stage the three artifacts:**
```bash
git add tests/v1.8-lcp-diagnosis.spec.ts \
        .planning/codebase/v1.8-lcp-evidence.json \
        .planning/codebase/v1.8-lcp-diagnosis.md
```

Confirm `.next/` is NOT staged (it's gitignored, but verify):
```bash
git diff --cached --name-only | grep -E '^\.next/' && exit 1; exit 0
```

**Step 2: Commit:**
```bash
git commit -m "Feat(57-03): DGN-01 + DGN-02 — LCP element identity (cold + warm) + chunk attribution; v1.8-lcp-diagnosis.md committed"
```
(Per CLAUDE.md §Git: `Feat:` prefix, atomic commit, no `gsd-tools.cjs` wrapper — project precedent uses plain `git commit`. Matches Plans 01 + 02.)

**Step 3: Verify the commit landed:**
```bash
git log -1 --oneline | grep -q '57-03'
git log -1 --name-only | grep -q 'v1.8-lcp-diagnosis.md'
git log -1 --name-only | grep -q 'v1.8-lcp-evidence.json'
git log -1 --name-only | grep -q 'tests/v1.8-lcp-diagnosis.spec.ts'
```

**Step 4: Verify clean tree** (no Plan 03 files left dangling):
```bash
git status --porcelain | grep -qE 'v1\.8-lcp-(evidence|diagnosis)|tests/v1\.8-lcp-diagnosis' \
  && exit 1; exit 0
```
  </action>

  <verify>
    <automated>git log -1 --name-only | grep -q 'v1.8-lcp-diagnosis.md' && git log -1 --name-only | grep -q 'v1.8-lcp-evidence.json' && git log -1 --name-only | grep -q 'tests/v1.8-lcp-diagnosis.spec.ts' && git log -1 --oneline | grep -q '57-03' && (git status --porcelain | grep -qE 'v1\.8-lcp-(evidence|diagnosis)|tests/v1\.8-lcp-diagnosis' && exit 1 || exit 0)</automated>
  </verify>

  <acceptance_criteria>
    - One commit landed mentioning `57-03`: `git log -1 --oneline | grep -q '57-03'` returns 0.
    - Commit includes the spec: `git log -1 --name-only | grep -q 'tests/v1.8-lcp-diagnosis.spec.ts'` returns 0.
    - Commit includes the evidence JSON: `git log -1 --name-only | grep -q 'v1.8-lcp-evidence.json'` returns 0.
    - Commit includes the diagnosis doc: `git log -1 --name-only | grep -q 'v1.8-lcp-diagnosis.md'` returns 0.
    - No build artifacts staged: `git diff HEAD~1..HEAD --name-only | grep -E '^\.next/'` returns no matches.
    - Working tree clean for Plan 03 files: `git status --porcelain | grep -E 'v1\.8-lcp-(evidence|diagnosis)|tests/v1\.8-lcp-diagnosis'` returns no matches.
  </acceptance_criteria>

  <done>
Atomic commit shipped with the spec, the JSON evidence, and the diagnosis markdown. Working tree clean. Phase 59/60/61 plan-phases can now `cat .planning/codebase/v1.8-lcp-diagnosis.md` to consume their respective slices.
  </done>
</task>

</tasks>

<verification>
**Plan 03 phase-level checks:**

1. `test -f tests/v1.8-lcp-diagnosis.spec.ts` — spec exists.
2. `test -f .planning/codebase/v1.8-lcp-evidence.json` — evidence exists with 4 entries.
3. `test -f .planning/codebase/v1.8-lcp-diagnosis.md` — diagnosis doc exists.
4. `grep -q "## 1. LCP Element Identity" .planning/codebase/v1.8-lcp-diagnosis.md` AND `grep -q "## 2. Per-Chunk Owner Attribution" .planning/codebase/v1.8-lcp-diagnosis.md` AND `grep -q "## 3. Diagnostic notes" .planning/codebase/v1.8-lcp-diagnosis.md` — all three required sections present.
5. `grep -qE '3302|e9a6067a|74c6194b|7525' .planning/codebase/v1.8-lcp-diagnosis.md` — v1.7 chunk IDs addressed (matched or noted as no-match).
6. `grep -q 'public/sf-canvas-sync.js' .planning/codebase/v1.8-lcp-diagnosis.md` AND `grep -qE 'inline.*scaleScript|inline IIFE' .planning/codebase/v1.8-lcp-diagnosis.md` — sync-script disambiguation present.
7. `git log -1 --name-only | grep -qE 'v1\.8-lcp-(diagnosis|evidence)'` — atomic commit landed.
</verification>

<success_criteria>
- DGN-01 deliverable shipped: `v1.8-lcp-diagnosis.md` §1 contains the 4-row LCP identity table with cold-vs-warm divergence documented per Pitfall A.
- DGN-02 deliverable shipped: `v1.8-lcp-diagnosis.md` §2 contains v1.7 chunk-ID lookup AND > 50KB completeness sweep per Pitfall D fallback.
- Sync-script disambiguation in §3 prevents Phase 59 CRT-01 from conflating `public/sf-canvas-sync.js` with the inline `scaleScript` IIFE.
- Strictly diagnostic — no LCP-02 ranking, no Phase 60 plan-shape suggestions (D-02 lock).
- `tests/v1.8-lcp-diagnosis.spec.ts` is reusable for any phase that wants to re-capture LCP identity (Phase 59 will likely re-run after Anton swap).
- Plan 03 introduces ZERO application code change.
- Atomic commit lands all three artifacts.
</success_criteria>

<output>
After completion, create `.planning/phases/57-diagnosis-pass-aesthetic-of-record-lock-in/57-03-SUMMARY.md` documenting:
- LCP element identity table snapshot (cold-vs-warm divergence yes/no per viewport).
- Chunk attribution result: how many of v1.7's 4 named IDs matched the v1.8 build; how many NEW_FINDING entries.
- Sync-script disambiguation status: confirmed both mechanisms documented.
- Spec + JSON + doc commit SHA.
- Phase 57 phase-end status: 7/7 requirement IDs covered (DGN-01..03 in Plans 02-03; AES-01..04 in Plan 01).
</output>
</output>
