---
phase: 58-lighthouse-ci-real-device-telemetry
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - .lighthouseci/lighthouserc.json
autonomous: true
requirements:
  - CIB-01
  - CIB-02
notes:
  - "OQ-04 (mobile-only vs mobile+desktop LHCI): Phase 57 evidence — mobile LCP = THESIS GhostLabel, desktop LCP = VL-05 magenta `//` overlay. Per phase_specifics §4 OQ-04: dual-run config (mobile + desktop), preview gate covers both viewports. Locked."
  - "OQ-05 (warmup): `numberOfRuns: 5` with `aggregationMethod: median-run` is the variance discipline. CDN warmth is supplied by Vercel's deployment lifecycle itself — by the time `deployment_status:success` fires, the preview URL has already been hit by Vercel's readiness probe, so the edge is warm before the first LHCI run. The workflow (Plan 02) lists the target URL EXACTLY ONCE per emulation; LHCI's per-URL run multiplication does the rest. The earlier URL-duplication-warmup pattern (research recommendation A) was retracted during checker review — it would have multiplied to 15 runs per emulation against `numberOfRuns: 5`, blowing the CI budget. Locked."
  - "OQ-02 (LHCI status posting): `temporaryPublicStorage: true` in lighthouserc upload target; zero secret setup required for v0.1. Inline PR status comments via `LHCI_GITHUB_APP_TOKEN` are deferred — workflow step (Plan 02) does not pass the secret. Locked."
  - "OQ-06 (RUM durable storage): explicitly out of scope for Phase 58 — handled in Plan 02 RUM endpoint as `console.log` only. Vercel KV/Postgres for v1.8.1 if VRF-05 trending needed. Backlog seeded via plan notes."
  - "Two-Lighthouse-version coexistence: existing devDep `lighthouse@^13.1.0` (used by `scripts/launch-gate.ts`) and new `@lhci/cli@^0.15.1` (which bundles Lighthouse 12.x internally and forks a child process — no Node API import collision with host project's node_modules). Confirmed safe by RESEARCH §CIB-01."
must_haves:
  truths:
    - "@lhci/cli@^0.15.1 is installed as a devDependency and `npx lhci --version` exits 0 from the repo root."
    - ".lighthouseci/lighthouserc.json exists and parses against the LHCI 0.15.x schema (assert dry-run exits 0)."
    - "lighthouserc.json encodes the cold-start variance discipline: `numberOfRuns: 5` with median-run aggregation; CDN warmth provided by Vercel deployment lifecycle (deployment_status:success fires post-readiness-probe); perf ≥0.97, LCP ≤1000ms, CLS ≤0, TBT ≤200ms."
    - "lighthouserc.json runs against BOTH mobile and desktop emulation (per OQ-04 lock — Phase 57 cross-viewport LCP divergence)."
  artifacts:
    - path: "package.json"
      provides: "@lhci/cli devDep declaration"
      contains: '"@lhci/cli": "^0.15.1"'
    - path: ".lighthouseci/lighthouserc.json"
      provides: "LHCI assertion config (mobile + desktop)"
      exports: ["ci.collect", "ci.assert", "ci.upload"]
  key_links:
    - from: ".lighthouseci/lighthouserc.json"
      to: "@lhci/cli"
      via: "lhci assert --config=.lighthouseci/lighthouserc.json --dry-run"
      pattern: "ci\\.assert\\.assertions"
    - from: "package.json"
      to: "pnpm-lock.yaml"
      via: "pnpm add -D @lhci/cli@^0.15.1"
      pattern: "@lhci/cli.*0\\.15"
---

<objective>
Install `@lhci/cli@^0.15.1` as a devDependency and ship `.lighthouseci/lighthouserc.json` encoding the v1.8 cold-start variance discipline (`numberOfRuns: 5`, median-run aggregation, perf ≥0.97, LCP ≤1000ms, CLS ≤0, TBT ≤200ms — both mobile and desktop emulation).

**Purpose:** Stand up the local-runnable half of the Phase 58 gate. Plan 02 wires this into GitHub Actions; this plan must land first so the workflow has a valid config to invoke. No application code is touched; no third-party SaaS is introduced; no runtime npm dep is added.

**Output:** A devDep entry, a lockfile delta, and one new JSON config file. `lhci assert --dry-run` against the new config must exit 0 (schema-valid) before this plan is closed.
</objective>

<execution_context>
@/Users/greyaltaer/.claude/pde-os/engines/gsd/workflows/execute-plan.md
@/Users/greyaltaer/.claude/pde-os/engines/gsd/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/58-lighthouse-ci-real-device-telemetry/58-RESEARCH.md
@.planning/phases/58-lighthouse-ci-real-device-telemetry/58-VALIDATION.md
@./CLAUDE.md
@package.json
@scripts/launch-gate.ts

<interfaces>
<!-- Existing devDep contract that LHCI must coexist with: -->

From package.json:
```json
"devDependencies": {
  "lighthouse": "^13.1.0"   // used by scripts/launch-gate.ts (Node API import)
  // @lhci/cli will be added; ships its own Lighthouse 12.x internally,
  // invoked as child process — no version collision.
}
```

From scripts/launch-gate.ts (READ-ONLY for Phase 58 — CIB-04 lock):
```typescript
import lighthouse from "lighthouse";          // Node API import
const RUNS = 3;                                // 3 runs, worst score
const TARGET_SCORE = 100;                      // strict prod gate
```

From RESEARCH.md §Implementation References — lighthouserc shape:
```json
{
  "ci": {
    "collect": { "numberOfRuns": 5, "settings": { "emulatedFormFactor": "mobile", "throttlingMethod": "simulate", "throttling": {...} } },
    "assert":  { "assertions": { "categories:performance": ["error", { "minScore": 0.97, "aggregationMethod": "median-run" }], ... } },
    "upload":  { "target": "temporary-public-storage" }
  }
}
```

OQ-04 lock (this plan): dual-run config — mobile + desktop. Implementation pattern: TWO lighthouserc collect/assert blocks merged into a single config via the LHCI 0.15 `[<configPath>]` flag pattern OR ship two URL entries with per-URL settings overrides via `collect.urls[]` + `--collect.url-replacement-patterns`. Cleanest 0.15.x approach: ship `lighthouserc.json` (mobile, default) plus `lighthouserc.desktop.json` (desktop override). The workflow in Plan 02 invokes lhci twice with different `--config` args; mobile is the primary, desktop is the secondary. Both assert against the same thresholds.
</interfaces>
</context>

<threat_model>
**Phase 58 Plan 01 threat surface:** local devDep + local JSON config; no network exposure, no runtime code, no user-input parsing.

| ID | Severity | Threat | Mitigation in this plan |
|----|----------|--------|-------------------------|
| T-DEP-01 | low | Supply-chain risk on `@lhci/cli@^0.15.1` (npm registry compromise / typosquat). | Pin to caret-major `^0.15.1` (allows patch + minor within 0.15+, but not 1.x). Verify `pnpm-lock.yaml` integrity hashes after install. Confirm package author is `googlechrome` on the npm registry page before merging. |
| T-CFG-01 | low | Config drift: lighthouserc thresholds loosened over time, silently degrading the gate. | Lock all numeric thresholds in this plan's `<action>` blocks (verbatim from RESEARCH.md §Implementation References). Phase verification re-greps each numeric value via `jq` to catch tampering. |

**ASVS L1:** N/A — this plan ships no runtime code. Auth/input-validation surfaces are zero. **No high-severity threats; no blocking conditions.**
</threat_model>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Install @lhci/cli@^0.15.1 as devDep</name>
  <files>package.json, pnpm-lock.yaml</files>
  <read_first>
    - package.json (verify lighthouse@^13.1.0 already present in devDependencies; confirm no existing @lhci/cli entry to avoid duplicate)
    - .planning/phases/58-lighthouse-ci-real-device-telemetry/58-RESEARCH.md (§CIB-01 — confirm coexistence rationale: LHCI invokes Lighthouse as child process, no Node API collision with the host project's `lighthouse@^13.1.0`)
    - ./CLAUDE.md (Hard Constraints — confirm devDep is permitted, no runtime dep added)
  </read_first>
  <action>
    Run `pnpm add -D @lhci/cli@^0.15.1` from the repo root.

    Expected post-state:
    - package.json `devDependencies` includes the line `"@lhci/cli": "^0.15.1",` (alphabetical placement between `@gsap/react` and `@next/bundle-analyzer`).
    - pnpm-lock.yaml contains a new `@lhci/cli` resolved entry with `version: '0.15.x'` (where x ≥ 1) and a populated `integrity` SHA-512 hash.
    - `node_modules/.bin/lhci` symlink exists and is executable.

    Do NOT modify `lighthouse@^13.1.0` — it stays as-is for `scripts/launch-gate.ts` (CIB-04). Do NOT remove or replace it.

    Do NOT add `web-vitals`, `@vercel/speed-insights`, `@vercel/analytics`, `@vercel/kv`, or any other npm package. Only `@lhci/cli` is permitted in this plan.
  </action>
  <behavior>
    - `pnpm add -D @lhci/cli@^0.15.1` exits 0
    - `cat package.json | jq -r '.devDependencies["@lhci/cli"]'` returns a string matching `^0\.15\.[0-9]+`
    - `cat package.json | jq -r '.devDependencies.lighthouse'` still returns `^13.1.0` (untouched)
    - `node_modules/.bin/lhci --version` exits 0 and prints a version starting with `0.15.`
  </behavior>
  <verify>
    <automated>cat package.json | jq -e '.devDependencies["@lhci/cli"] | test("^\\^0\\.15\\.")' && cat package.json | jq -e '.devDependencies.lighthouse == "^13.1.0"' && node_modules/.bin/lhci --version</automated>
  </verify>
  <acceptance_criteria>
    - [ ] `package.json` contains `"@lhci/cli": "^0.15.1"` (or higher 0.15 patch) in `devDependencies`
    - [ ] `package.json` still contains `"lighthouse": "^13.1.0"` in `devDependencies` (CIB-04 — no replacement)
    - [ ] `pnpm-lock.yaml` includes a new `@lhci/cli` resolution with an `integrity` hash starting with `sha512-`
    - [ ] `node_modules/.bin/lhci --version` exits 0 and prints a string matching `^0\.15\.[0-9]+`
    - [ ] No other npm dependencies were added or removed (diff `package.json` against HEAD shows exactly one added line)
  </acceptance_criteria>
  <done>
    `@lhci/cli@^0.15.1` is installed alongside the existing `lighthouse@^13.1.0`, both coexist, `npx lhci --version` works, and the diff is minimal (one devDep line + lockfile additions).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Write .lighthouseci/lighthouserc.json (mobile primary + desktop variant)</name>
  <files>.lighthouseci/lighthouserc.json, .lighthouseci/lighthouserc.desktop.json</files>
  <read_first>
    - .planning/phases/58-lighthouse-ci-real-device-telemetry/58-RESEARCH.md (§Implementation References — copy the JSON shape verbatim; §CIB-02 — note on `aggregationMethod: "median-run"` vs `"optimistic"`; §Open Questions OQ-04 — lock dual-run; OQ-05 — `numberOfRuns: 5` with median-run; CDN warmth supplied by Vercel readiness probe, no URL-duplication needed)
    - .planning/codebase/v1.8-lcp-diagnosis.md (§1 — confirm cross-viewport divergence justifies dual-run)
  </read_first>
  <action>
    Create the directory `.lighthouseci/` (it does not exist yet — verify with `ls -d .lighthouseci 2>&1 | grep "No such"`).

    Write `.lighthouseci/lighthouserc.json` with this EXACT content (mobile-emulation primary; the workflow in Plan 02 injects the Vercel preview URL via `--collect.url`, listing the URL EXACTLY ONCE per emulation since LHCI's `numberOfRuns: 5` does the per-URL run multiplication):

    ```json
    {
      "ci": {
        "collect": {
          "numberOfRuns": 5,
          "settings": {
            "emulatedFormFactor": "mobile",
            "screenEmulation": {
              "mobile": true,
              "width": 375,
              "height": 667,
              "deviceScaleFactor": 2,
              "disabled": false
            },
            "throttlingMethod": "simulate",
            "throttling": {
              "rttMs": 40,
              "throughputKbps": 10240,
              "cpuSlowdownMultiplier": 4
            },
            "disableStorageReset": false,
            "skipAudits": ["uses-http2"]
          }
        },
        "assert": {
          "assertions": {
            "categories:performance": [
              "error",
              { "minScore": 0.97, "aggregationMethod": "median-run" }
            ],
            "categories:accessibility": [
              "error",
              { "minScore": 0.97, "aggregationMethod": "median-run" }
            ],
            "categories:best-practices": [
              "error",
              { "minScore": 0.97, "aggregationMethod": "median-run" }
            ],
            "categories:seo": [
              "error",
              { "minScore": 1.0, "aggregationMethod": "median-run" }
            ],
            "largest-contentful-paint": [
              "error",
              { "maxNumericValue": 1000, "aggregationMethod": "median-run" }
            ],
            "cumulative-layout-shift": [
              "error",
              { "maxNumericValue": 0, "aggregationMethod": "median-run" }
            ],
            "total-blocking-time": [
              "error",
              { "maxNumericValue": 200, "aggregationMethod": "median-run" }
            ]
          }
        },
        "upload": {
          "target": "temporary-public-storage"
        }
      }
    }
    ```

    Note on form-factor pinning: this config does NOT use the LHCI `preset` field. An earlier draft used `"preset": "desktop"` together with `"emulatedFormFactor": "mobile"`, which produces a hybrid form factor — `preset` sets baseline screenEmulation/throttling defaults that aren't fully overridden by the layered settings. The current shape skips `preset` entirely and pins both `emulatedFormFactor: "mobile"` AND an explicit `screenEmulation` block (375×667 @ 2× DPR — Lighthouse's canonical Moto-G-class mobile dims). This guarantees the emitted run is unambiguously mobile.

    Then write `.lighthouseci/lighthouserc.desktop.json` (desktop variant, same thresholds, different emulation):

    ```json
    {
      "ci": {
        "collect": {
          "numberOfRuns": 5,
          "settings": {
            "preset": "desktop",
            "emulatedFormFactor": "desktop",
            "screenEmulation": {
              "mobile": false,
              "width": 1440,
              "height": 900,
              "deviceScaleFactor": 1,
              "disabled": false
            },
            "throttlingMethod": "simulate",
            "throttling": {
              "rttMs": 40,
              "throughputKbps": 10240,
              "cpuSlowdownMultiplier": 1
            },
            "disableStorageReset": false,
            "skipAudits": ["uses-http2"]
          }
        },
        "assert": {
          "assertions": {
            "categories:performance": [
              "error",
              { "minScore": 0.97, "aggregationMethod": "median-run" }
            ],
            "categories:accessibility": [
              "error",
              { "minScore": 0.97, "aggregationMethod": "median-run" }
            ],
            "categories:best-practices": [
              "error",
              { "minScore": 0.97, "aggregationMethod": "median-run" }
            ],
            "categories:seo": [
              "error",
              { "minScore": 1.0, "aggregationMethod": "median-run" }
            ],
            "largest-contentful-paint": [
              "error",
              { "maxNumericValue": 1000, "aggregationMethod": "median-run" }
            ],
            "cumulative-layout-shift": [
              "error",
              { "maxNumericValue": 0, "aggregationMethod": "median-run" }
            ],
            "total-blocking-time": [
              "error",
              { "maxNumericValue": 200, "aggregationMethod": "median-run" }
            ]
          }
        },
        "upload": {
          "target": "temporary-public-storage"
        }
      }
    }
    ```

    Both files use 2-space indentation, trailing newline. No comments (JSON does not support them).

    Validate with a dry run:
    ```bash
    npx lhci assert --config=.lighthouseci/lighthouserc.json --dry-run
    npx lhci assert --config=.lighthouseci/lighthouserc.desktop.json --dry-run
    ```
    Both must exit 0 with no "unknown assertion" warnings.
  </action>
  <behavior>
    - `.lighthouseci/lighthouserc.json` exists, parses as valid JSON, asserts mobile emulation (375×667 @ 2× DPR) WITHOUT a `preset` field
    - `.lighthouseci/lighthouserc.desktop.json` exists, parses as valid JSON, asserts desktop emulation (1440×900)
    - `numberOfRuns` = 5 in both files
    - `categories:performance` minScore = 0.97 with `aggregationMethod: "median-run"` in both
    - `largest-contentful-paint` maxNumericValue = 1000 in both
    - `cumulative-layout-shift` maxNumericValue = 0 (strict — milestone contract) in both
    - `total-blocking-time` maxNumericValue = 200 in both
    - `upload.target` = `"temporary-public-storage"` (OQ-02 lock — no `LHCI_GITHUB_APP_TOKEN` required for v0.1) in both
    - `npx lhci assert --config=.lighthouseci/lighthouserc.json --dry-run` exits 0
    - `npx lhci assert --config=.lighthouseci/lighthouserc.desktop.json --dry-run` exits 0
  </behavior>
  <verify>
    <automated>jq -e '.ci.collect.numberOfRuns == 5' .lighthouseci/lighthouserc.json && jq -e '.ci.assert.assertions["categories:performance"][1].minScore == 0.97' .lighthouseci/lighthouserc.json && jq -e '.ci.assert.assertions["categories:performance"][1].aggregationMethod == "median-run"' .lighthouseci/lighthouserc.json && jq -e '.ci.assert.assertions["largest-contentful-paint"][1].maxNumericValue == 1000' .lighthouseci/lighthouserc.json && jq -e '.ci.assert.assertions["cumulative-layout-shift"][1].maxNumericValue == 0' .lighthouseci/lighthouserc.json && jq -e '.ci.assert.assertions["total-blocking-time"][1].maxNumericValue == 200' .lighthouseci/lighthouserc.json && jq -e '.ci.upload.target == "temporary-public-storage"' .lighthouseci/lighthouserc.json && jq -e '.ci.collect.settings.emulatedFormFactor == "mobile"' .lighthouseci/lighthouserc.json && jq -e '.ci.collect.settings.preset == null' .lighthouseci/lighthouserc.json && jq -e '.ci.collect.settings.screenEmulation.mobile == true' .lighthouseci/lighthouserc.json && jq -e '.ci.collect.settings.screenEmulation.width == 375' .lighthouseci/lighthouserc.json && jq -e '.ci.collect.settings.emulatedFormFactor == "desktop"' .lighthouseci/lighthouserc.desktop.json && jq -e '.ci.collect.settings.screenEmulation.width == 1440' .lighthouseci/lighthouserc.desktop.json && npx lhci assert --config=.lighthouseci/lighthouserc.json --dry-run && npx lhci assert --config=.lighthouseci/lighthouserc.desktop.json --dry-run</automated>
  </verify>
  <acceptance_criteria>
    - [ ] `.lighthouseci/lighthouserc.json` exists and is valid JSON (`jq . .lighthouseci/lighthouserc.json` exits 0)
    - [ ] `.lighthouseci/lighthouserc.desktop.json` exists and is valid JSON
    - [ ] All 7 assertions present in both files (perf, a11y, best-practices, seo, LCP, CLS, TBT) with `aggregationMethod: "median-run"`
    - [ ] CLS assertion is `maxNumericValue: 0` exactly (not 0.01, not 0.1 — the milestone contract is CLS=0)
    - [ ] Mobile config has `emulatedFormFactor: "mobile"`, NO `preset` field (`jq -e '.ci.collect.settings.preset == null'` succeeds), AND an explicit `screenEmulation: { mobile: true, width: 375, height: 667, deviceScaleFactor: 2 }` block — guaranteeing unambiguous mobile form factor
    - [ ] Desktop config has `emulatedFormFactor: "desktop"` and `screenEmulation.width: 1440`
    - [ ] `npx lhci assert --config=<each file> --dry-run` exits 0 for both files
    - [ ] No `urls` field present in either file (the workflow injects URL at invocation time)
    - [ ] No `LHCI_GITHUB_APP_TOKEN` referenced in either file (OQ-02 lock — temporary-public-storage only)
  </acceptance_criteria>
  <done>
    Both lighthouserc JSON files are committed, schema-valid (lhci assert --dry-run passes), encode the v1.8 cold-start variance discipline (`numberOfRuns: 5`, median-run aggregation, perf ≥0.97, LCP ≤1000ms, CLS ≤0, TBT ≤200ms), pin form factors unambiguously (mobile config drops `preset` entirely + adds explicit `screenEmulation`; desktop config keeps `preset: "desktop"` since it aligns with the form factor), and cover BOTH mobile and desktop emulation per OQ-04 lock.
  </done>
</task>

</tasks>

<verification>
**Plan-level verification (run after both tasks complete):**

1. **Schema validation (V-01 from RESEARCH §Validation Architecture):**
   ```bash
   npx lhci assert --config=.lighthouseci/lighthouserc.json --dry-run
   npx lhci assert --config=.lighthouseci/lighthouserc.desktop.json --dry-run
   ```
   Both exit 0; no "unknown assertion" or "unknown audit" warnings.

2. **Threshold integrity (config drift mitigation T-CFG-01):**
   ```bash
   for f in .lighthouseci/lighthouserc.json .lighthouseci/lighthouserc.desktop.json; do
     jq -e '
       .ci.collect.numberOfRuns == 5 and
       .ci.assert.assertions["categories:performance"][1].minScore == 0.97 and
       .ci.assert.assertions["largest-contentful-paint"][1].maxNumericValue == 1000 and
       .ci.assert.assertions["cumulative-layout-shift"][1].maxNumericValue == 0 and
       .ci.assert.assertions["total-blocking-time"][1].maxNumericValue == 200
     ' "$f" || (echo "FAIL: $f" && exit 1)
   done
   ```

3. **Lock-file integrity (T-DEP-01 mitigation):**
   ```bash
   grep -A1 "'@lhci/cli@" pnpm-lock.yaml | grep -c "integrity: sha512-"
   # Expected: ≥ 1
   ```

4. **No regression in existing devDeps:**
   ```bash
   git diff HEAD -- package.json | grep -E "^[+-]" | grep -v "^[+-]{3}" | grep -v "@lhci/cli" | grep -v "^[+-]\s*$"
   # Expected: zero output (only @lhci/cli line added; nothing else changed)
   ```

5. **CIB-04 untouched (sanity check — `scripts/launch-gate.ts` MUST not appear in this plan's diff):**
   ```bash
   git diff HEAD -- scripts/launch-gate.ts
   # Expected: zero output
   ```

6. **Wave 0 — no Wave 0 needed.** This plan's automated verification is exclusively `jq` + `lhci assert --dry-run`; no test framework scaffolding required.
</verification>

<success_criteria>
- [ ] `@lhci/cli@^0.15.1` in `devDependencies`; `lighthouse@^13.1.0` retained
- [ ] `.lighthouseci/lighthouserc.json` (mobile, NO `preset` field, explicit `screenEmulation` 375×667 @ 2× DPR) and `.lighthouseci/lighthouserc.desktop.json` (desktop) committed
- [ ] Both configs pass `npx lhci assert --dry-run`
- [ ] `numberOfRuns: 5`, `aggregationMethod: "median-run"`, perf ≥0.97, LCP ≤1000ms, CLS ≤0, TBT ≤200ms in both
- [ ] `upload.target: "temporary-public-storage"` (no GH App token required — OQ-02 lock)
- [ ] `scripts/launch-gate.ts` untouched (CIB-04 lock — verified by `git diff` on the file showing zero changes)
- [ ] All 4 OQ resolutions (OQ-02, OQ-04, OQ-05, OQ-06) documented in plan `notes` block
- [ ] Pixel-diff is N/A for this plan (no app code touched — Plan 02 owns the pixel-diff verification)
</success_criteria>

<output>
After completion, create `.planning/phases/58-lighthouse-ci-real-device-telemetry/58-01-SUMMARY.md` documenting:
- The two files added and their schema-validated thresholds
- Confirmation that `lighthouse@^13.1.0` was NOT touched
- The dry-run output proving schema validity (paste exit code + last 5 lines)
- Any pnpm warnings encountered during install (peer-dep mismatches, etc.)
- A short "downstream" note: "Plan 02 (Wave 2) consumes both lighthouserc files via `treosh/lighthouse-ci-action@v12` `configPath:` input."
</output>
