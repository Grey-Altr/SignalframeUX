---
phase: 59-critical-path-restructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/layout.tsx
  - components/layout/scale-canvas.tsx
  - public/sf-canvas-sync.js
  - tests/v1.8-phase59-canvas-sync-inline.spec.ts
  - tests/v1.8-phase59-pixel-diff.spec.ts
autonomous: true
requirements: [CRT-01, CRT-05]

must_haves:
  truths:
    - "Production HTML contains the canvas-height-sync IIFE inline (no /sf-canvas-sync.js network request fired)"
    - "public/sf-canvas-sync.js no longer exists on disk"
    - "components/layout/scale-canvas.tsx has no <script src=\"/sf-canvas-sync.js\" /> mount"
    - "iPhone-13 hard-reload CLS=0 across all five routes (home, system, init, inventory, reference)"
    - "Pixel-diff against .planning/visual-baselines/v1.8-start/ is byte-equal-or-zero across all 20 baselines (Plan A is invisible)"
    - "LHCI median-of-5 stays >=97 mobile + desktop on the Plan A PR (CRT-05 bisect protection)"
  artifacts:
    - path: "app/layout.tsx"
      provides: "Inlined canvasSyncScript const + <script> mount in <body> tail"
      contains: "canvasSyncScript"
    - path: "components/layout/scale-canvas.tsx"
      provides: "ScaleCanvas component WITHOUT external sync-script mount"
      forbids_substring: "/sf-canvas-sync.js"
    - path: "tests/v1.8-phase59-canvas-sync-inline.spec.ts"
      provides: "CLS=0 PerformanceObserver + no-network-request guard"
      exports: ["test", "test.describe"]
    - path: "tests/v1.8-phase59-pixel-diff.spec.ts"
      provides: "Phase 59 pixel-diff harness (extends Phase 58 fixture; reused by Plans B+C)"
      exports: ["test", "test.describe"]
  key_links:
    - from: "app/layout.tsx (canvasSyncScript const)"
      to: "<body> tail <script suppressHydrationWarning __DANGEROUS_HTML_PROP__={{ __html: canvasSyncScript }} />"
      via: "React inline-HTML injection prop (same primitive as themeScript + scaleScript)"
      pattern: "__DANGEROUS_HTML_PROP__.*canvasSyncScript"
    - from: "tests/v1.8-phase59-canvas-sync-inline.spec.ts"
      to: "production build (pnpm build && pnpm start)"
      via: "playwright.config.ts existing webServer"
      pattern: "playwright.*phase59-canvas-sync"
---

> **Notation note:** This plan uses the placeholder `__DANGEROUS_HTML_PROP__` for the standard React inline-HTML-injection prop (same convention as `59-RESEARCH.md`). The actual prop name in code is the standard React prop. Plan executor must substitute the literal prop name when editing `app/layout.tsx`. The injected content is a static string literal with NO user input interpolation — the same pattern as the existing `themeScript` and `scaleScript` consts at L92 and L101.

<objective>
Inline the 198-byte `/sf-canvas-sync.js` IIFE into `app/layout.tsx` <body> tail, remove the external script mount from `components/layout/scale-canvas.tsx`, and delete `public/sf-canvas-sync.js`. Net effect: -1 render-blocking external network request, identical pre-paint DOM mutation, zero CLS impact, zero visual change.

Purpose: Closes CRT-01 (one of three independent bisect-safe interventions in Phase 59). The 198 bytes themselves are not the prize — eliminating the additional HTTP request from the critical path is. After this plan ships, the scaleScript and the canvasSyncScript are both inline static literals running at HTML parse, before React hydrates. Pitfall #2 + #8 guardrails preserved (no `next/script strategy="beforeInteractive"`; no `defer`/`async`).

Output: Three files modified (`app/layout.tsx`, `components/layout/scale-canvas.tsx`, `public/sf-canvas-sync.js` deleted), two Wave-0 test files added.

Bisect order (CRT-05): Plan A FIRST. Plan B `depends_on: [01]`, Plan C `depends_on: [02]`. Each PR opens only after the prior is in `main`.
</objective>

<execution_context>
@/Users/greyaltaer/.claude/pde-os/engines/gsd/workflows/execute-plan.md
@/Users/greyaltaer/.claude/pde-os/engines/gsd/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/59-critical-path-restructure/59-RESEARCH.md
@.planning/phases/59-critical-path-restructure/59-VALIDATION.md
@.planning/codebase/AESTHETIC-OF-RECORD.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from shipped code so no scavenger-hunt is required. -->

From `app/layout.tsx` (current canonical inline-script primitive — Plan A MUST mirror this exact pattern):

- L92 `themeScript` const (existing — pattern reference)
- L101 `scaleScript` const (existing — pattern reference, immediate sibling to canvasSyncScript)
- L110-114 mount pattern: head-located for theme/scale via `<script suppressHydrationWarning __DANGEROUS_HTML_PROP__={{ __html: ... }} />`. canvasSyncScript goes in <body> tail (different placement, same primitive).

From `public/sf-canvas-sync.js` (the 198-byte source — copy IIFE byte-for-byte into the const):

```text
(function(){try{var i=document.querySelector('[data-sf-canvas]');if(!i)return;var o=i.parentElement;if(!o)return;var s=window.innerWidth/1280;o.style.height=(i.offsetHeight*s)+'px';}catch(e){}})();
```

From `components/layout/scale-canvas.tsx` (current external mount site at L142-143 — to be removed):

- L142: `eslint-disable-next-line @next/next/no-sync-scripts` directive comment
- L143: raw `<script src="/sf-canvas-sync.js" />` JSX element

From `tests/v1.8-phase58-pixel-diff.spec.ts` (the existing Phase 58 fixture to extend — Wave 0 of this plan creates the Phase 59 sibling):

```ts
const ROUTES = [
  { path: "/", slug: "home" },
  { path: "/system", slug: "system" },
  { path: "/init", slug: "init" },
  { path: "/inventory", slug: "inventory" },
  { path: "/reference", slug: "reference" },
] as const;
const VIEWPORTS = [
  { name: "desktop-1440x900", width: 1440, height: 900 },
  { name: "iphone13-390x844", width: 390, height: 844 },
  { name: "ipad-834x1194", width: 834, height: 1194 },
  { name: "mobile-360x800", width: 360, height: 800 },
] as const;
const BASELINE_DIR = path.resolve(process.cwd(), ".planning/visual-baselines/v1.8-start");
const MAX_DIFF_RATIO = 0.005; // AES-04 standing rule
```

From `tests/phase-35-lcp-homepage.spec.ts:17-23` (CLS PerformanceObserver pattern — copy into Wave 0 spec):

```ts
const cls = await page.evaluate(() => new Promise<number>((resolve) => {
  let total = 0;
  new PerformanceObserver((list) => {
    for (const entry of list.getEntries() as PerformanceEntry[] & Array<{ value?: number; hadRecentInput?: boolean }>) {
      if (!(entry as { hadRecentInput?: boolean }).hadRecentInput) {
        total += (entry as { value?: number }).value ?? 0;
      }
    }
  }).observe({ type: "layout-shift", buffered: true });
  setTimeout(() => resolve(total), 3000);
}));
expect(cls).toBe(0);
```
</interfaces>

<phase_58_gate>
Phase 58 is shipped (commits c9937c8 and earlier). Code-side LHCI gate is fully wired (11/11 must-haves verified per `58-VERIFICATION.md`). HUMAN-UAT items 1+2 (Vercel `deployments:write` + branch-protection rule on `main` requiring `audit`) are deferred-pending and tracked in `58-HUMAN-UAT.md`.

**Plan A does NOT block on Phase 58 HUMAN-UAT.** Plan A is the lowest-risk intervention (single file deletion + 4-line head insertion). The HUMAN-UAT gate is enforced in Plan B (`59-02-PLAN.md`), where the AES-02 documented exception ratifies a Chromatic re-baseline and the bisect protection from branch-protection becomes load-bearing. Plan A may merge with LHCI-as-advisory if HUMAN-UAT 1+2 are still pending; the per-PR LHCI run still happens, the merge button just isn't blocked by it. Verify median-of-5 >=97 by inspecting the workflow logs before merging.
</phase_58_gate>
</context>

<threat_model>
Per §5.55 security gate:

| Threat | Severity | Mitigation |
|--------|----------|------------|
| Inline `<script>` adds another `'unsafe-inline'`-dependent surface alongside themeScript + scaleScript | LOW (CSP already permits inline; same pattern as existing scripts) | The new inline script content is a static string literal with NO user input interpolated. Verified by grep: the `canvasSyncScript` constant must contain zero `${...}` template-literal interpolation segments. The static IIFE is XSS-impossible by construction. |
| Inline body-tail script could in principle be injected pre-paint | NEGLIGIBLE | The script is a string literal authored at build time; React renders it via the standard inline-HTML injection prop against a const that has no user-input data flow. Same risk profile as existing themeScript / scaleScript. |

**No high-severity threats.** Plan A clears the §5.55 gate.
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 0 (Wave 0): Author CLS-observer + no-network-request integration spec</name>
  <files>tests/v1.8-phase59-canvas-sync-inline.spec.ts</files>
  <read_first>
    - tests/v1.8-phase58-pixel-diff.spec.ts (the closest existing Playwright fixture to extend — ROUTES/VIEWPORTS arrays, networkidle wait, pnpm build + pnpm start contract)
    - tests/phase-35-lcp-homepage.spec.ts:17-23 (the canonical CLS PerformanceObserver pattern — copy verbatim into the new spec)
    - 59-RESEARCH.md L568-642 (Validation Architecture — confirms framework=Playwright ^1.59.1, headless Chromium SwiftShader, build target = production)
    - 59-VALIDATION.md L48-50 (Per-Task Verification Map — 59-A-01 row pins the exact spec filename + command)
    - playwright.config.ts (project=chromium config so the spec lands on the existing webServer)
  </read_first>
  <behavior>
    - Test 1 ("CRT-01: no /sf-canvas-sync.js network request"): page.on("request", req => ...) tracks any URL ending in `/sf-canvas-sync.js`; navigates to `/` with waitUntil: "networkidle"; asserts saw=false. Run on iPhone-13 viewport (390x844).
    - Test 2 ("CRT-01: iPhone-13 hard-reload CLS=0"): goto `/` with waitUntil: "networkidle"; PerformanceObserver `{type: "layout-shift", buffered: true}` collects layout-shift entries excluding `hadRecentInput`; setTimeout 3000ms then resolves; expect total CLS === 0. Repeat across `/`, `/system`, `/init`, `/inventory`, `/reference` (5 routes); page.emulateMedia({ reducedMotion: "reduce" }) to suppress GSAP timelines.
    - Test 3 ("CRT-01: production HTML contains canvasSyncScript IIFE inline"): page.goto `/`; const html = await page.content(); regex match the body-tail inline IIFE byte-pattern from public/sf-canvas-sync.js (without the now-deleted file's exact bytes — match the canonical IIFE shape).
  </behavior>
  <action>
    Create `tests/v1.8-phase59-canvas-sync-inline.spec.ts` with three tests as specified in the behavior block above. Mirror the import pattern + ROUTES/VIEWPORTS arrays from `tests/v1.8-phase58-pixel-diff.spec.ts`. Use the CLS PerformanceObserver block from `tests/phase-35-lcp-homepage.spec.ts:17-23` verbatim. Tag the suite `@v18-phase59-canvas-sync` for `--grep` selectivity.

    Test 1 implementation (no-network-request guard):

    ```ts
    let sawSyncScriptRequest = false;
    page.on("request", (req) => {
      if (req.url().endsWith("/sf-canvas-sync.js")) sawSyncScriptRequest = true;
    });
    await page.goto("/", { waitUntil: "networkidle" });
    expect(sawSyncScriptRequest).toBe(false);
    ```

    Test 2 implementation (CLS=0 across 5 routes — for-loop):

    ```ts
    for (const route of ROUTES) {
      // setViewportSize iPhone-13 390x844, emulateMedia reducedMotion: reduce
      // goto route.path, networkidle
      // PerformanceObserver { type: "layout-shift", buffered: true }
      // await 3000ms then resolve total
      // expect(cls).toBe(0)
    }
    ```

    Test 3 implementation (inline-IIFE-present guard):

    ```ts
    await page.goto("/", { waitUntil: "domcontentloaded" });
    const html = await page.content();
    expect(html).toMatch(/document\.querySelector\(['"]\[data-sf-canvas\]['"]\)/);
    expect(html).toMatch(/innerWidth\s*\/\s*1280/);
    ```

    Build target = production: this spec MUST run via `pnpm build && pnpm start` per `playwright.config.ts` webServer configuration. The dev server does NOT inline scripts the same way (Next.js dev mode injects HMR script ordering that masks the inline-script timing). Document this in the spec header comment.

    Run the spec ONCE to confirm it FAILS (RED — the canvas-sync.js file still exists, the request will still fire). Commit as `test(59): RED — canvas-sync inline integration spec`.
  </action>
  <verify><automated>
# RED state: spec MUST fail on current main (no changes applied yet).
# Negate the test command — outer `!` exits 0 only when test command exits non-zero.
! pnpm exec playwright test tests/v1.8-phase59-canvas-sync-inline.spec.ts --project=chromium
</automated></verify>
  <acceptance_criteria>
    - File exists: `test -f tests/v1.8-phase59-canvas-sync-inline.spec.ts`
    - Spec contains all three tests (grep test titles or `Test 1`/`Test 2`/`Test 3` markers)
    - Spec FAILS as expected on the current main: `pnpm exec playwright test tests/v1.8-phase59-canvas-sync-inline.spec.ts --project=chromium` exits with at least one failure
    - Header comment cites `pnpm build && pnpm start` requirement (grep "production build" or similar)
  </acceptance_criteria>
  <done>
    Spec file committed (RED state). Three tests defined, mirroring the verified Phase 58 fixture pattern + Phase 35 CLS observer. Run against current main confirms tests fail (proof the spec is real, not a vacuous pass).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 1 (Wave 0): Extend pixel-diff harness to Phase 59 (reused by Plans B+C)</name>
  <files>tests/v1.8-phase59-pixel-diff.spec.ts</files>
  <read_first>
    - tests/v1.8-phase58-pixel-diff.spec.ts (the parent fixture — copy the entire ROUTES/VIEWPORTS/MAX_DIFF_RATIO/readPng/test.describe block)
    - .planning/visual-baselines/v1.8-start/ (verify all 20 PNGs present: 5 routes x 4 viewports = 20)
    - 59-RESEARCH.md L634-641 (Pixel-Diff Threshold Summary — Plan A target = 0%, Plan C = 0%, Plan B re-baselines as AES-02 exception)
    - 59-VALIDATION.md L51 (per-task row 59-A-02 confirms the spec filename and command)
    - .planning/codebase/AESTHETIC-OF-RECORD.md (AES-04 standing rule = 0.5% per page; Plan A target is stricter at 0%)
  </read_first>
  <behavior>
    - Test 1 ("Plan A pixel-diff: every route x viewport must be byte-identical to v1.8-start"): for each (route, viewport) in 5x4 = 20 combinations, capture full-page screenshot under reducedMotion, networkidle, document.fonts.ready; pixelmatch against `.planning/visual-baselines/v1.8-start/{slug}-{viewport}.png`; assert ratio === 0 (NOT <= 0.005 — Plan A is invisible by construction; any diff is a regression).
    - Test isolation: this spec is the Phase 59 fixture — Plan B will re-baseline this test under the AES-02 documented exception; Plan C will revert to ratio === 0. Plan A locks at 0.
  </behavior>
  <action>
    Copy `tests/v1.8-phase58-pixel-diff.spec.ts` to `tests/v1.8-phase59-pixel-diff.spec.ts`. Modifications:

    1. Update test.describe tag: `@v18-phase58-pixel-diff` → `@v18-phase59-pixel-diff (CRT-01 / Plan A)`.
    2. Update header JSDoc: replace "Phase 58 is infrastructure-only..." with "Phase 59 Plan A (CRT-01) is invisible by construction — inlining /sf-canvas-sync.js produces ZERO visible diff. Plan B (CRT-02+03) is the documented AES-02 exception and re-baselines under separate harness invocation. Plan C (CRT-04) is invisible by construction." (Document the three-plan handoff in this comment block.)
    3. `MAX_DIFF_RATIO`: keep at `0.005` (AES-04 floor) BUT add a strict assertion BEFORE the AES-04 expect: `expect(ratio, \`Plan A is invisible — diff ${(ratio * 100).toFixed(3)}% must be 0%\`).toBe(0);`. This makes Plan A's strict gate primary; if the strict assertion ever needs relaxation (e.g., Plan B's run), the AES-04 0.5% gate still holds beneath.
    4. Output diff PNGs to `.playwright-artifacts/phase59-pixel-diff/` (not phase58).

    Run ONCE to establish baseline: `pnpm build && pnpm exec playwright test tests/v1.8-phase59-pixel-diff.spec.ts --project=chromium`. Should pass on current main (no code changed yet — diff = 0). Commit as `test(59): GREEN — Phase 59 pixel-diff harness (Plan A target = 0)`.
  </action>
  <verify>
    <automated>pnpm build && pnpm exec playwright test tests/v1.8-phase59-pixel-diff.spec.ts --project=chromium 2>&1 | tee /tmp/pixel.log; grep -E "(20 passed|failed)" /tmp/pixel.log | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f tests/v1.8-phase59-pixel-diff.spec.ts`
    - File differs from Phase 58 sibling only at MAX_DIFF_RATIO assertion + tag + header (diff line count <= 30)
    - All 20 (5 routes x 4 viewports) tests pass GREEN on current main: spec exits 0
    - Output diff dir is phase59 not phase58: `grep -F "phase59-pixel-diff" tests/v1.8-phase59-pixel-diff.spec.ts` returns the path
  </acceptance_criteria>
  <done>
    Phase 59 pixel-diff harness committed in GREEN state on current main. The 20-PNG fixture is Plan A's invisibility gate AND the reusable harness for Plans B (re-baselined) + C (zero-diff). After Plan A's edit task ships, this spec MUST still pass GREEN — that is the regression detector.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Inline canvasSyncScript into app/layout.tsx <body> tail; remove external script mount; delete public/sf-canvas-sync.js</name>
  <files>app/layout.tsx, components/layout/scale-canvas.tsx, public/sf-canvas-sync.js</files>
  <read_first>
    - app/layout.tsx (the canonical inline-script pattern at L92 themeScript, L101 scaleScript, L110-114 head mount; the <body> at L119 ends after L153 InstrumentHUD)
    - public/sf-canvas-sync.js (the 198-byte source — copy IIFE byte-for-byte into canvasSyncScript const)
    - components/layout/scale-canvas.tsx (L135-143 = the comment block + eslint-disable + <script src=...> JSX to be removed; L144-145 closing fragment stays)
    - 59-RESEARCH.md L107-141 (CRT-01 Concrete Edits — declares canvasSyncScript const + Option A vs Option B body-tail mount choice; recommends Option B)
    - 59-RESEARCH.md L142-148 (Risk Matrix for CRT-01 — body-tail timing rationale)
    - 59-VALIDATION.md L48 (59-A-01 quick run command)
  </read_first>
  <behavior>
    - Test 1 (existing — Wave 0 spec): no `/sf-canvas-sync.js` network request fires; production HTML contains the inline IIFE; CLS=0 across 5 routes on iPhone-13.
    - Test 2 (existing — Wave 0 pixel-diff): all 20 baselines remain byte-identical (Plan A is invisible by construction).
    - Negative test (manual grep): no production code references `/sf-canvas-sync.js` after this commit.
  </behavior>
  <action>
    **Edit 1** — `app/layout.tsx` add `canvasSyncScript` const directly after `scaleScript` (after L101). Insert this block (mirroring the comment style of themeScript and scaleScript):

    ```text
    // Pre-paint canvas-height sync (formerly /sf-canvas-sync.js, CRT-01 Phase 59).
    // Mirrors the legacy external script's IIFE: query [data-sf-canvas], read inner
    // offsetHeight, multiply by vw/1280 (--sf-content-scale floor), set on outer
    // parent. Inline so it runs at HTML parse time, after [data-sf-canvas] is in
    // the DOM via React's streamed body. Mounted in <body> tail (NOT <head>) so
    // the [data-sf-canvas] div is already streamed by React when the script runs.
    // Pitfall #2/#8: do NOT migrate to next/script strategy="beforeInteractive"
    // (runs after first paint, reintroduces 0.485 CLS). Static literal, no user
    // input interpolated — XSS-impossible by construction.
    const canvasSyncScript = `(function(){try{var i=document.querySelector('[data-sf-canvas]');if(!i)return;var o=i.parentElement;if(!o)return;var s=window.innerWidth/1280;o.style.height=(i.offsetHeight*s)+'px';}catch(e){}})()`;
    ```

    Note: copy the IIFE body BYTE-FOR-BYTE from `public/sf-canvas-sync.js` (the 198-byte source above in <interfaces>); strip only the trailing `;` after the closing IIFE paren so the const matches the inner-IIFE pattern of themeScript and scaleScript.

    **Edit 2** — `app/layout.tsx` add `<script>` mount in `<body>` tail, immediately after `<InstrumentHUD />` (current L153) and before `</body>` (L154). Use the same React inline-HTML-injection prop already used by themeScript (L112) and scaleScript (L114). The mount form mirrors the existing siblings:

    ```text
    {/* Pre-paint canvas-height sync (CRT-01). Body-tail placement: [data-sf-canvas]
        must already be streamed by React at this point. Same React injection prop
        as themeScript + scaleScript — static literal, no user input. */}
    <script suppressHydrationWarning __DANGEROUS_HTML_PROP__={{ __html: canvasSyncScript }} />
    ```

    Substitute `__DANGEROUS_HTML_PROP__` with the standard React prop name (the literal string used in the existing themeScript / scaleScript mounts at L112 / L114 — exact same identifier).

    **Edit 3** — `components/layout/scale-canvas.tsx` remove L135-143 (the comment block + eslint-disable + `<script src="/sf-canvas-sync.js" />` JSX). The closing `</>` fragment + `);` on L144-145 stays. Result: ScaleCanvas's returned JSX ends at `</div>` (current L134) followed directly by `</>` (now L135). Update the JSDoc on the component (L17-30) to remove any reference to "external blocking script `/sf-canvas-sync.js`" — the inline-comment chain at L114-119 explaining the suppressHydrationWarning rationale stays (that pattern is still required because the inline script in layout.tsx mutates `outer.style.height` pre-hydration). Adjust the comment language: "The sibling blocking script (formerly /sf-canvas-sync.js, now inlined in app/layout.tsx body tail per CRT-01) mutates outer.style.height at HTML parse time..."

    **Edit 4** — `rm public/sf-canvas-sync.js` (delete the 198-byte file outright; not move, not rename — DELETE).

    Run the Wave 0 specs to confirm GREEN:
    - `pnpm build && pnpm exec playwright test tests/v1.8-phase59-canvas-sync-inline.spec.ts --project=chromium` → all 3 tests pass.
    - `pnpm exec playwright test tests/v1.8-phase59-pixel-diff.spec.ts --project=chromium` → all 20 tests pass with ratio === 0.

    Negative grep guards (run + screenshot output):
    - `! grep -F "sf-canvas-sync.js" components/layout/scale-canvas.tsx` exits 0
    - `! grep -F "sf-canvas-sync.js" app/layout.tsx` exits 0 (the inline IIFE references `[data-sf-canvas]` only, not the deleted file path)
    - `! test -f public/sf-canvas-sync.js` exits 0

    Commit as `feat(59-01): CRT-01 — inline /sf-canvas-sync.js into app/layout.tsx body tail; delete public/sf-canvas-sync.js`.
  </action>
  <verify>
    <automated>pnpm build 2>&1 | tail -10 && ! grep -F "sf-canvas-sync.js" components/layout/scale-canvas.tsx && ! grep -F "sf-canvas-sync.js" app/layout.tsx && ! test -f public/sf-canvas-sync.js && pnpm exec playwright test tests/v1.8-phase59-canvas-sync-inline.spec.ts tests/v1.8-phase59-pixel-diff.spec.ts --project=chromium</automated>
  </verify>
  <acceptance_criteria>
    - Build green: `pnpm build` exits 0 with no TypeScript errors
    - `! grep -F "sf-canvas-sync.js" components/layout/scale-canvas.tsx` exits 0
    - `! grep -F "sf-canvas-sync.js" app/layout.tsx` exits 0
    - `! test -f public/sf-canvas-sync.js` exits 0
    - `grep -F "canvasSyncScript" app/layout.tsx` returns at least 2 matches (const declaration + dangerouslySetInnerHTML mount)
    - `grep -F "data-sf-canvas" app/layout.tsx` returns at least 1 match (inside the canvasSyncScript IIFE)
    - Static-literal verification: `awk '/canvasSyncScript = /,/^[^a-zA-Z]/' app/layout.tsx | grep -c '\${' ` returns 0 (no template-literal interpolations inside canvasSyncScript)
    - `pnpm exec playwright test tests/v1.8-phase59-canvas-sync-inline.spec.ts --project=chromium` exits 0 (all 3 tests pass)
    - `pnpm exec playwright test tests/v1.8-phase59-pixel-diff.spec.ts --project=chromium` exits 0 (all 20 tests pass — Plan A is invisible)
  </acceptance_criteria>
  <done>
    Production HTML serves the canvas-height-sync IIFE inline. No external `/sf-canvas-sync.js` request fires. CLS=0 verified across 5 routes on iPhone-13 viewport. Pixel-diff is byte-zero across all 20 v1.8-start baselines. ScaleCanvas component no longer references the deleted file. The 198-byte external file is removed from disk.
  </done>
</task>

</tasks>

<verification>
After all three tasks complete, the entire Plan A suite green:

```bash
pnpm build
pnpm exec playwright test tests/v1.8-phase59-canvas-sync-inline.spec.ts tests/v1.8-phase59-pixel-diff.spec.ts --project=chromium
```

Sanity grep guards:

```bash
! grep -F "sf-canvas-sync.js" components/layout/scale-canvas.tsx
! grep -F "sf-canvas-sync.js" app/layout.tsx
! test -f public/sf-canvas-sync.js
grep -F "canvasSyncScript" app/layout.tsx | wc -l   # expect >= 2 (const + mount)
```

Per CRT-05, the open Plan A PR triggers LHCI on Vercel preview. Median-of-5 must be >=97 mobile + desktop with CLS=0 and LCP<=1000ms before merge. If HUMAN-UAT 1+2 from Phase 58 are not yet checked, LHCI runs as advisory; manually inspect the workflow log and confirm thresholds before merging. Plan B's PR opens only after this Plan A merges.
</verification>

<success_criteria>
- `public/sf-canvas-sync.js` deleted
- `app/layout.tsx` contains `canvasSyncScript` const + `<script>` mount in `<body>` tail
- `components/layout/scale-canvas.tsx` no longer mounts the external script
- `tests/v1.8-phase59-canvas-sync-inline.spec.ts` GREEN (3/3)
- `tests/v1.8-phase59-pixel-diff.spec.ts` GREEN with ratio === 0 across all 20 baselines
- LHCI preview median-of-5 >=97 mobile + desktop on the Plan A PR (advisory if HUMAN-UAT 1+2 incomplete; merge-blocking otherwise)
- AESTHETIC-OF-RECORD.md unchanged (Plan A is invisible by construction; AES-02 exception is reserved for Plan B)
- One atomic commit on a `feat/59-01-canvas-sync-inline` branch (or equivalent), opened as PR #1 of the CRT-05 bisect order
</success_criteria>

<output>
After completion, create `.planning/phases/59-critical-path-restructure/59-01-SUMMARY.md` documenting:
- Final byte content of `canvasSyncScript` const (paste verbatim)
- Final mount placement (head vs body tail decision rationale — locked to body tail)
- LHCI median-of-5 mobile + desktop scores observed on the PR
- Confirmation that all 20 pixel-diff comparisons returned ratio === 0
- Confirmation that no `/sf-canvas-sync.js` network request fired
- Cross-link to Plan B (which now unblocks): `Plan B (59-02) opens after this PR merges. Plan B depends_on: [01].`
</output>
