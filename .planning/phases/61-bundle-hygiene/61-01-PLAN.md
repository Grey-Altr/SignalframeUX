---
phase: 61
plan: "01"
title: "Eager-path packages (radix-ui + input-otp)"
type: execute
wave: 1
depends_on: []
files_modified:
  - next.config.ts
  - .planning/phases/61-bundle-hygiene/61-01-RESEARCH-LOG.md
autonomous: true
requirements: [BND-02]
nyquist_compliant: true
must_haves:
  truths:
    - "next.config.ts optimizePackageImports array contains both \"radix-ui\" and \"input-otp\""
    - "61-01-RESEARCH-LOG.md exists with three populated rows: baseline (Build 0), Build A (radix-ui), Build B (input-otp)"
    - "Each row records Shared by all (KB), / First Load JS (KB), and /_not-found First Load JS (KB) values from the Route (app) stdout table"
    - "pnpm typecheck exits 0 after each addition (no type regression)"
    - "Stale-chunk guard `rm -rf .next/cache .next` is documented in the 61-01-RESEARCH-LOG.md header per BND-04"
  artifacts:
    - path: "next.config.ts"
      provides: "Build-time barrel-import transform list extended with eager-path packages"
      contains: "\"radix-ui\""
    - path: ".planning/phases/61-bundle-hygiene/61-01-RESEARCH-LOG.md"
      provides: "Per-package measurement log for builds 0/A/B"
      contains: "Build A | radix-ui"
  key_links:
    - from: "next.config.ts experimental.optimizePackageImports"
      to: "production webpack barrel-rewrite transform"
      via: "Next.js 15 build pipeline"
      pattern: "optimizePackageImports.*radix-ui.*input-otp"
    - from: "61-01-RESEARCH-LOG.md"
      to: "Route (app) stdout table from `pnpm build`"
      via: "manual paste of Shared by all / / / /_not-found rows"
      pattern: "Build (0|A|B).*\\d+ kB"
---

<objective>
Extend `next.config.ts` `optimizePackageImports` from `["lucide-react"]` to `["lucide-react", "radix-ui", "input-otp"]` in two sequential commits, each followed by a stale-chunk-guarded `ANALYZE=true pnpm build` measurement logged to `61-01-RESEARCH-LOG.md`.

Purpose: `radix-ui` (148.1 KB across chunk 3302 + 53.3 KB across chunk 7525) and `input-otp` (9.1 KB on chunk 3302) are the two confirmed eager-path packages on the homepage's initial First Load JS per DGN-02. They are the only packages in the BND-02 candidate list that contribute measurable savings to the "Shared by all" floor. Lazy packages are deferred to Plan 02; verification + final gate is Plan 03.

Output:
- `next.config.ts` with `optimizePackageImports: ["lucide-react", "radix-ui", "input-otp"]`
- `.planning/phases/61-bundle-hygiene/61-01-RESEARCH-LOG.md` with three measurement rows (baseline, Build A, Build B) populated from the Route (app) stdout table
- Two atomic commits (one per package addition) for clean bisect
</objective>

<execution_context>
@/Users/greyaltaer/.claude/pde-os/engines/gsd/workflows/execute-plan.md
@/Users/greyaltaer/.claude/pde-os/engines/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/61-bundle-hygiene/61-RESEARCH.md
@.planning/phases/61-bundle-hygiene/61-VALIDATION.md
@.planning/codebase/v1.8-lcp-diagnosis.md
@next.config.ts

<interfaces>
<!-- Current next.config.ts state (line 8-12). Executor must preserve `useCache: true` and the `redirects()` block verbatim. -->

```ts
const nextConfig: NextConfig = {
  experimental: {
    optimizePackageImports: ["lucide-react"],
    useCache: true,
  },
  // ... redirects() block unchanged
};
```

<!-- Target after Build A: -->
```ts
optimizePackageImports: ["lucide-react", "radix-ui"],
```

<!-- Target after Build B: -->
```ts
optimizePackageImports: ["lucide-react", "radix-ui", "input-otp"],
```

<!-- Build invocation (BND-04 stale-chunk guard mandatory): -->
```bash
rm -rf .next/cache .next && ANALYZE=true pnpm build
```

<!-- Source of measurement: Route (app) stdout table. Read three values per build:
1. "First Load JS shared by all" line (the floor)
2. "/" row "First Load JS" column (homepage delta)
3. "/_not-found" row "First Load JS" column (proxy for shared-only floor)

FORBIDDEN gating source: .next/analyze/client.html chartData (per ROADMAP success criterion 1 + 61-RESEARCH §4 Risks #2). -->
</interfaces>
</context>

<threat_model>
  <surface>build-config (next.config.ts) + dev-only research-log file additions</surface>
  <auth_changes>none</auth_changes>
  <network_surface>none</network_surface>
  <data_handling>none — no user data, no PII, no storage</data_handling>
  <supply_chain>
    optimizePackageImports adds two packages (`radix-ui` ^1.4.3, `input-otp` ^1.4.2) to a Next.js compile-time barrel-rewrite transform list. Both are already direct dependencies in package.json with locked semver. No new packages installed.
    Risk: low. Mitigation: pnpm-lock.yaml is the source of truth; transform fails loudly at build time if package internal paths shift.
  </supply_chain>
  <severity>low — bundle hygiene is invisible by construction</severity>
</threat_model>

<tasks>

<task type="auto" id="61-01-00">
  <name>Task 0 (Wave 0): Create 61-01-RESEARCH-LOG.md template + capture baseline (Build 0)</name>
  <files>.planning/phases/61-bundle-hygiene/61-01-RESEARCH-LOG.md</files>
  <read_first>
    - .planning/phases/61-bundle-hygiene/61-RESEARCH.md (sections §4 Measurement Protocol, §6 Plan Ordering)
    - .planning/phases/61-bundle-hygiene/61-VALIDATION.md (task 61-01-03 row)
    - .planning/codebase/v1.8-lcp-diagnosis.md §2c (current Route (app) baseline numbers)
    - next.config.ts (current state with `optimizePackageImports: ["lucide-react"]`)
  </read_first>
  <action>
    Create the file `.planning/phases/61-bundle-hygiene/61-01-RESEARCH-LOG.md` with this exact structure:

    ```markdown
    # 61-01 Research Log — Eager-path packages (radix-ui + input-otp)

    **Plan:** 61-01
    **Phase:** 61 — Bundle Hygiene
    **Build invocation:** `rm -rf .next/cache .next && ANALYZE=true pnpm build`
    **Stale-chunk guard:** Mandatory before every measurement (BND-04). The `.next/cache` and `.next` directories are deleted between builds to prevent webpack incremental-cache contamination of cross-commit measurements.
    **Gating source:** "Route (app)" stdout table from `pnpm build`. The `.next/analyze/client.html` chartData is FORBIDDEN as a gating source (ROADMAP success criterion 1).

    ## Per-build measurement table

    | Build | Package added | Shared by all (KB) | / First Load (KB) | /_not-found First Load (KB) | Delta vs Build 0 (KB) | Notes |
    |-------|---------------|--------------------|-------------------|----------------------------|-----------------------|-------|
    | 0     | (baseline — lucide-react only) | TBD | TBD | TBD | 0 | Captured before any 61-01 changes |
    | A     | radix-ui      | TBD | TBD | TBD | TBD | optimizePackageImports: ["lucide-react", "radix-ui"] |
    | B     | input-otp     | TBD | TBD | TBD | TBD | optimizePackageImports: ["lucide-react", "radix-ui", "input-otp"] |

    ## Build 0 — Baseline (lucide-react only)

    Timestamp: TBD
    next.config.ts state: `optimizePackageImports: ["lucide-react"]`

    Route (app) snapshot (relevant rows):
    ```
    Route (app)                        Size     First Load JS
    /                                  TBD      TBD
    /_not-found                        TBD      TBD
    + First Load JS shared by all      TBD
    ```

    ## Build A — radix-ui added

    (populated in Task 2)

    ## Build B — input-otp added

    (populated in Task 4)
    ```

    Then run the baseline build (Build 0):

    ```bash
    rm -rf .next/cache .next && ANALYZE=true pnpm build 2>&1 | tee /tmp/phase61-build-0.txt
    ```

    Extract these three values from the Route (app) stdout table at the end of the build output:
    1. "First Load JS shared by all" KB value
    2. "/" row "First Load JS" KB value
    3. "/_not-found" row "First Load JS" KB value

    Update the table row 0 with measured values; populate the "Build 0 — Baseline" snapshot block with the verbatim Route (app) header + `/` + `/_not-found` + "First Load JS shared by all" lines.

    Commit with message: `docs(61-01): create RESEARCH-LOG and capture Build 0 baseline`
  </action>
  <verify>
    <automated>test -f .planning/phases/61-bundle-hygiene/61-01-RESEARCH-LOG.md && grep -E "^\| 0 *\|" .planning/phases/61-bundle-hygiene/61-01-RESEARCH-LOG.md | grep -v "TBD" && grep -E "rm -rf \\.next/cache \\.next" .planning/phases/61-bundle-hygiene/61-01-RESEARCH-LOG.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f .planning/phases/61-bundle-hygiene/61-01-RESEARCH-LOG.md` exits 0
    - Header documents stale-chunk guard: `grep -E "rm -rf \\.next/cache \\.next" .planning/phases/61-bundle-hygiene/61-01-RESEARCH-LOG.md` returns ≥1 match
    - Build 0 row is populated (no `TBD` in the row 0 KB columns): `grep -E "^\| 0 *\|" .planning/phases/61-bundle-hygiene/61-01-RESEARCH-LOG.md | grep -v "TBD"` returns 1 line
    - Build 0 snapshot block contains the literal string "First Load JS shared by all" copied from stdout
    - Commit exists with message matching `docs(61-01): create RESEARCH-LOG and capture Build 0 baseline`
  </acceptance_criteria>
  <done>
    61-01-RESEARCH-LOG.md exists with templated table + populated Build 0 row capturing the pre-change baseline. The file documents the BND-04 stale-chunk guard in its header. One commit landed.
  </done>
</task>

<task type="auto" id="61-01-01">
  <name>Task 1 (Wave 1): Add "radix-ui" to optimizePackageImports + measure Build A</name>
  <files>
    next.config.ts
    .planning/phases/61-bundle-hygiene/61-01-RESEARCH-LOG.md
  </files>
  <read_first>
    - .planning/phases/61-bundle-hygiene/61-RESEARCH.md (§1 per-package compatibility matrix; §2 import surface map; §6 Plan 01 entry)
    - .planning/phases/61-bundle-hygiene/61-01-RESEARCH-LOG.md (current state with Build 0 populated)
    - next.config.ts (current state)
    - .planning/codebase/v1.8-lcp-diagnosis.md §2 (chunk 3302 + 7525 attribution)
  </read_first>
  <action>
    Edit `next.config.ts`. The `optimizePackageImports` array on line 10 currently reads:

    ```ts
    optimizePackageImports: ["lucide-react"],
    ```

    Replace with the literal string:

    ```ts
    optimizePackageImports: ["lucide-react", "radix-ui"],
    ```

    Do NOT add `@radix-ui/react-*` scoped packages. Per 61-RESEARCH §1, all 27 files in this repo import from the `radix-ui` meta-package (^1.4.3); zero direct `@radix-ui/react-*` imports exist. The meta-package form is the correct entry.

    Do NOT change `useCache: true` or the `redirects()` block.

    Commit before measurement with message: `Chore(61-01): add radix-ui to optimizePackageImports`

    Then run Build A:

    ```bash
    rm -rf .next/cache .next && ANALYZE=true pnpm build 2>&1 | tee /tmp/phase61-build-A.txt
    ```

    Extract the three values (Shared by all / / / /_not-found) from the Route (app) stdout table.

    Update `61-01-RESEARCH-LOG.md`:
    1. Populate row A with the three KB values + delta vs row 0 (Shared by all delta).
    2. Add a "Build A — radix-ui added" detail block with the verbatim Route (app) header + `/` + `/_not-found` + "First Load JS shared by all" lines.
    3. Run a sanity check: `pnpm typecheck` must exit 0. Note pass/fail in the row's Notes column.

    Commit the log update with message: `docs(61-01): record Build A measurement (radix-ui)`
  </action>
  <verify>
    <automated>grep -F '"radix-ui"' next.config.ts && grep -E "^\| A *\|" .planning/phases/61-bundle-hygiene/61-01-RESEARCH-LOG.md | grep -v "^| A *| radix-ui *| TBD" && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `next.config.ts` contains the literal string `"radix-ui"` inside the optimizePackageImports array: `grep -F '"radix-ui"' next.config.ts` returns ≥1 match
    - `next.config.ts` does NOT contain any `"@radix-ui/react-` substring (would indicate scoped-package addition error): `grep -F '"@radix-ui/react-' next.config.ts` exits 1
    - `useCache: true` line preserved: `grep -F "useCache: true" next.config.ts` returns ≥1 match
    - 61-01-RESEARCH-LOG.md row A is populated (no `TBD` in the KB columns): `grep -E "^\| A *\| radix-ui" .planning/phases/61-bundle-hygiene/61-01-RESEARCH-LOG.md | grep -v "TBD"` returns 1 line
    - 61-01-RESEARCH-LOG.md "Build A — radix-ui added" detail block exists with the literal string "First Load JS shared by all"
    - `pnpm typecheck` exits 0
    - Two commits exist: `Chore(61-01): add radix-ui to optimizePackageImports` AND `docs(61-01): record Build A measurement (radix-ui)`
  </acceptance_criteria>
  <done>
    `next.config.ts` includes `"radix-ui"` in `optimizePackageImports`. Build A succeeded; the three Route (app) values are recorded in row A of the log with computed delta vs Build 0. Type-check still passes. Two atomic commits landed in bisect order.
  </done>
</task>

<task type="auto" id="61-01-02">
  <name>Task 2 (Wave 1): Add "input-otp" to optimizePackageImports + measure Build B + sanity-check eager-path components</name>
  <files>
    next.config.ts
    .planning/phases/61-bundle-hygiene/61-01-RESEARCH-LOG.md
  </files>
  <read_first>
    - .planning/phases/61-bundle-hygiene/61-RESEARCH.md (§1 per-package compatibility for input-otp; §2 barrel chain; Risks §7)
    - .planning/phases/61-bundle-hygiene/61-01-RESEARCH-LOG.md (state with Build 0 + Build A populated)
    - next.config.ts (current state with radix-ui added)
    - components/sf/index.ts (verify SFInputOTP eager export at lines 177-181)
    - components/sf/sf-input-otp.tsx (the eager wrapper that pulls input-otp into shared bundle)
  </read_first>
  <action>
    Edit `next.config.ts`. The `optimizePackageImports` array currently reads (after Task 1):

    ```ts
    optimizePackageImports: ["lucide-react", "radix-ui"],
    ```

    Replace with the literal string:

    ```ts
    optimizePackageImports: ["lucide-react", "radix-ui", "input-otp"],
    ```

    Commit before measurement with message: `Chore(61-01): add input-otp to optimizePackageImports`

    Then run Build B:

    ```bash
    rm -rf .next/cache .next && ANALYZE=true pnpm build 2>&1 | tee /tmp/phase61-build-B.txt
    ```

    Extract the three values from the Route (app) stdout table.

    Update `61-01-RESEARCH-LOG.md`:
    1. Populate row B with the three KB values + delta vs row 0 (Shared by all delta).
    2. Add a "Build B — input-otp added" detail block with the verbatim Route (app) header + `/` + `/_not-found` + "First Load JS shared by all" lines.
    3. Run `pnpm typecheck` — must exit 0. Note pass/fail.

    Sanity check (no runtime regression on eager-path components — radix-ui-backed + input-otp-backed):

    Run any pre-existing Playwright spec that exercises a Radix-backed SF component (SFPanel via dialog primitive, SFNavigationMenu via navigation-menu primitive) OR SFInputOTP. Use the Phase 59 pixel-diff spec as the canary because it loads the homepage at four viewports and the homepage transitively pulls SFPanel + SFNavigationMenu via the sf barrel.

    `playwright.config.ts` only auto-starts the production server under `CI=true` — without that flag the spec will hit ECONNREFUSED on `localhost:3000`. Prepend the flag (autoStart will run `pnpm build && pnpm start` for the spec):

    ```bash
    CI=true pnpm exec playwright test tests/v1.8-phase59-pixel-diff.spec.ts --project=chromium 2>&1 | tail -10
    ```

    Note in row B Notes column whether the canary spec exits 0 (final pixel-diff gate is in Plan 03; this is a defensive in-plan check, NOT the final gate). If the spec fails, do not block the plan — record the failure mode in row B Notes and continue; Plan 03 owns the gating verdict.

    Commit the log update with message: `docs(61-01): record Build B measurement (input-otp)`
  </action>
  <verify>
    <automated>grep -F '"input-otp"' next.config.ts && grep -F '"radix-ui"' next.config.ts && grep -E "^\| B *\| input-otp" .planning/phases/61-bundle-hygiene/61-01-RESEARCH-LOG.md | grep -v "TBD" && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `next.config.ts` contains both `"radix-ui"` AND `"input-otp"` literal strings in optimizePackageImports: `grep -F '"radix-ui"' next.config.ts && grep -F '"input-otp"' next.config.ts` both return ≥1 match
    - The lucide-react entry is preserved (not deleted): `grep -F '"lucide-react"' next.config.ts` returns ≥1 match
    - 61-01-RESEARCH-LOG.md row B is populated (no `TBD` in KB columns): `grep -E "^\| B *\| input-otp" .planning/phases/61-bundle-hygiene/61-01-RESEARCH-LOG.md | grep -v "TBD"` returns 1 line
    - 61-01-RESEARCH-LOG.md "Build B — input-otp added" detail block exists with the literal string "First Load JS shared by all"
    - `pnpm typecheck` exits 0
    - Phase 59 pixel-diff canary spec exits 0 (or, if it shows non-zero diff, the row B Notes column documents the diff for Plan 03 escalation): `pnpm exec playwright test tests/v1.8-phase59-pixel-diff.spec.ts --project=chromium` is invoked and its result is recorded
    - Two commits exist in bisect order: `Chore(61-01): add input-otp to optimizePackageImports` AND `docs(61-01): record Build B measurement (input-otp)`
  </acceptance_criteria>
  <done>
    `next.config.ts` `optimizePackageImports` array equals `["lucide-react", "radix-ui", "input-otp"]`. Build B succeeded; row B values + delta vs Build 0 + detail block are committed to the research log. Type-check passes. Canary spec result (pass/fail) recorded for Plan 03 final-gate context. Two atomic commits in bisect order.
  </done>
</task>

</tasks>

<verification>
End-of-plan checks (the executor MUST run all of these before declaring the plan complete):

1. `grep -F '"radix-ui"' next.config.ts && grep -F '"input-otp"' next.config.ts && grep -F '"lucide-react"' next.config.ts` — all three return ≥1 match
2. `grep -F '"@radix-ui/react-' next.config.ts` — exits 1 (zero matches; meta-package form only)
3. `test -f .planning/phases/61-bundle-hygiene/61-01-RESEARCH-LOG.md` — exits 0
4. `grep -E "^\| (0|A|B) *\|" .planning/phases/61-bundle-hygiene/61-01-RESEARCH-LOG.md | grep -v "TBD"` — returns ≥3 lines (rows 0, A, B all populated, no TBDs left)
5. `grep -E "rm -rf \\.next/cache \\.next" .planning/phases/61-bundle-hygiene/61-01-RESEARCH-LOG.md` — returns ≥1 match (BND-04 stale-chunk guard documented in log header)
6. `pnpm typecheck` — exits 0
7. Git log shows 5 commits in this plan's range: 1 docs (Build 0), 1 chore (radix-ui add), 1 docs (Build A), 1 chore (input-otp add), 1 docs (Build B). Bisect order preserved (chore before docs).
</verification>

<success_criteria>
1. `next.config.ts` `optimizePackageImports` array is exactly `["lucide-react", "radix-ui", "input-otp"]`.
2. `61-01-RESEARCH-LOG.md` exists with all three measurement rows (0, A, B) populated and three "Build N" detail blocks each containing a Route (app) snapshot.
3. Each measurement was preceded by the BND-04 stale-chunk guard `rm -rf .next/cache .next`.
4. `pnpm typecheck` exits 0 after Build A and Build B.
5. The Phase 59 pixel-diff canary spec was run after Build B; its result (pass/fail) is recorded in row B Notes for Plan 03 escalation context.
6. All BND-02 partial coverage for the eager-path packages (radix-ui + input-otp) is recorded with quantitative deltas.
</success_criteria>

<output>
After completion, create `.planning/phases/61-bundle-hygiene/61-01-SUMMARY.md` with:
- Final state of `next.config.ts` `optimizePackageImports` array (verbatim quoted)
- Three measurement rows from `61-01-RESEARCH-LOG.md` (0, A, B) with absolute KB values + deltas
- Pixel-diff canary spec result from Build B (pass / non-zero diff with route+viewport)
- Notes for Plan 02 (any unexpected build behavior, any rows that warrant escalation in Plan 03 final gate)
- Commit hashes for each of the 5 commits (3 docs + 2 chore)
</output>
