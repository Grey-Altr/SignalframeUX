---
phase: 61
plan: "02"
title: "Lazy-path packages (cmdk + vaul + sonner + react-day-picker)"
type: execute
wave: 2
depends_on: ["61-01"]
files_modified:
  - next.config.ts
  - .planning/phases/61-bundle-hygiene/61-02-RESEARCH-LOG.md
autonomous: true
requirements: [BND-02]
nyquist_compliant: true
must_haves:
  truths:
    - "next.config.ts optimizePackageImports array contains all four lazy packages: cmdk, vaul, sonner, react-day-picker"
    - "61-02-RESEARCH-LOG.md exists with three populated rows: baseline (carry-over from Plan 01 Build B), Build C (cmdk+vaul), Build D (sonner+react-day-picker)"
    - "61-02-RESEARCH-LOG.md documents the date-fns SKIP decision with rationale (already in Next.js 15 default-optimized list)"
    - "pnpm typecheck exits 0 after each addition"
    - "Lazy-path zero-delta on Shared by all is documented as expected (NOT a regression) per 61-RESEARCH §Risks #5"
  artifacts:
    - path: "next.config.ts"
      provides: "Build-time barrel-import transform list extended with four lazy-path packages"
      contains: "cmdk"
    - path: ".planning/phases/61-bundle-hygiene/61-02-RESEARCH-LOG.md"
      provides: "Per-batch measurement log for builds C and D + date-fns skip decision"
      contains: "Build C | cmdk+vaul"
  key_links:
    - from: "next.config.ts experimental.optimizePackageImports"
      to: "lazy-loaded chunks (CommandPaletteLazy, sf-drawer-lazy, sf-toast-lazy, SFCalendarLazy)"
      via: "Next.js 15 build pipeline barrel-rewrite"
      pattern: "optimizePackageImports.*cmdk.*vaul.*sonner.*react-day-picker"
---

<objective>
Extend `next.config.ts` `optimizePackageImports` from Plan 01's end-state ["lucide-react", "radix-ui", "input-otp"] to the full ["lucide-react", "radix-ui", "input-otp", "cmdk", "vaul", "sonner", "react-day-picker"] in two sequential batched commits, each followed by a stale-chunk-guarded `ANALYZE=true pnpm build` measurement logged to `61-02-RESEARCH-LOG.md`.

Purpose: cmdk (CommandPaletteLazy), vaul (sf-drawer-lazy), sonner (sf-toast-lazy), and react-day-picker (SFCalendarLazy) are all behind `next/dynamic` wrappers per 61-RESEARCH §2. They do NOT contribute to the homepage's "Shared by all" floor. Adding them is defensive: it reduces their lazy-chunk size (no harm) and produces a complete `optimizePackageImports` list for v1.8 lock. `date-fns` is SKIPPED — already in the Next.js 15 default-optimized list per 61-RESEARCH §1.

Sequencing: Plan 02 is wave 2 because `next.config.ts` mutations cannot run parallel with Plan 01. Sequential commits preserve git bisect attribution.

Output:
- `next.config.ts` with all 6 non-skip BND-02 packages in `optimizePackageImports`
- `.planning/phases/61-bundle-hygiene/61-02-RESEARCH-LOG.md` with three measurement rows (baseline carry-over, Build C, Build D) + date-fns skip rationale
- Two atomic commits (one per batch addition) for clean bisect
</objective>

<execution_context>
@/Users/greyaltaer/.claude/pde-os/engines/gsd/workflows/execute-plan.md
@/Users/greyaltaer/.claude/pde-os/engines/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/61-bundle-hygiene/61-RESEARCH.md
@.planning/phases/61-bundle-hygiene/61-VALIDATION.md
@.planning/phases/61-bundle-hygiene/61-01-PLAN.md
@.planning/phases/61-bundle-hygiene/61-01-RESEARCH-LOG.md
@.planning/codebase/v1.8-lcp-diagnosis.md
@next.config.ts

<interfaces>
Plan 01 end-state of next.config.ts (entry being extended):

```ts
optimizePackageImports: ["lucide-react", "radix-ui", "input-otp"],
```

Target after Build C (cmdk + vaul batched):

```ts
optimizePackageImports: ["lucide-react", "radix-ui", "input-otp", "cmdk", "vaul"],
```

Target after Build D (sonner + react-day-picker batched):

```ts
optimizePackageImports: ["lucide-react", "radix-ui", "input-otp", "cmdk", "vaul", "sonner", "react-day-picker"],
```

date-fns SKIP rationale (61-RESEARCH §1): "date-fns v4 is in [Next.js 15.x's] default optimization list. Adding it to optimizePackageImports is redundant but harmless." Skip is the cleaner choice.

Build invocation (BND-04 stale-chunk guard mandatory):

```bash
rm -rf .next/cache .next && ANALYZE=true pnpm build
```

Expected delta on "Shared by all" for these four lazy packages: ~0 KB (per 61-RESEARCH §Risks #5). Zero delta is the expected result, NOT a failure. The lazy-chunk-internal size may shrink; this plan does not measure lazy chunks individually.
</interfaces>
</context>

<threat_model>
  <surface>build-config (next.config.ts) + dev-only research-log file additions</surface>
  <auth_changes>none</auth_changes>
  <network_surface>none</network_surface>
  <data_handling>none — no user data, no PII, no storage</data_handling>
  <supply_chain>
    optimizePackageImports adds four packages (`cmdk` ^1.1.1, `vaul` ^1.1.2, `sonner` ^2.0.7, `react-day-picker` ^9.14.0) to a Next.js compile-time barrel-rewrite transform list. All four are direct dependencies in package.json with locked semver. No new packages installed.
    Risk: low. Mitigation: pnpm-lock.yaml is the source of truth.
  </supply_chain>
  <severity>low — bundle hygiene is invisible by construction</severity>
</threat_model>

<tasks>

<task type="auto" id="61-02-00">
  <name>Task 0 (Wave 0): Create 61-02-RESEARCH-LOG.md template + carry-over Build B as baseline + document date-fns skip</name>
  <files>.planning/phases/61-bundle-hygiene/61-02-RESEARCH-LOG.md</files>
  <read_first>
    - .planning/phases/61-bundle-hygiene/61-RESEARCH.md (sections 4 measurement protocol, 6 Plan 02 entry, Risks #5 lazy-zero-delta, Risks #6 date-fns skip)
    - .planning/phases/61-bundle-hygiene/61-01-RESEARCH-LOG.md (read Build B row to use as Plan 02 baseline reference)
    - .planning/phases/61-bundle-hygiene/61-VALIDATION.md (task 61-02-03 row)
    - next.config.ts (current state with radix-ui + input-otp added by Plan 01)
  </read_first>
  <action>
    Create `.planning/phases/61-bundle-hygiene/61-02-RESEARCH-LOG.md` with this structure (use literal headings and table columns; the body of each "Build N" detail block is populated in later tasks):

    File header: title "61-02 Research Log — Lazy-path packages (cmdk + vaul + sonner + react-day-picker)".

    Header metadata block (mandatory content):
    - "Plan: 61-02"
    - "Phase: 61 — Bundle Hygiene"
    - "Build invocation: `rm -rf .next/cache .next && ANALYZE=true pnpm build`"
    - "Stale-chunk guard: Mandatory before every measurement (BND-04). The `.next/cache` and `.next` directories are deleted between builds."
    - "Gating source: Route (app) stdout table from `pnpm build`. The `.next/analyze/client.html` chartData is FORBIDDEN as a gating source (ROADMAP success criterion 1)."
    - "Expected behavior: The four lazy-loaded packages do NOT contribute to the homepage's initial First Load JS per 61-RESEARCH §2. The Shared by all delta for builds C and D is expected to be approximately 0 KB. Zero delta is the expected result, NOT a regression."

    Section: "## date-fns SKIP decision" — must contain literal string "date-fns SKIP" and explain: `date-fns` ^4.1.0 is in the Next.js 15 default-optimized package list (61-RESEARCH §1). Zero direct `date-fns` imports exist in this repo (only transitive via react-day-picker). Decision: SKIP. The final array contains six entries (lucide-react, radix-ui, input-otp, cmdk, vaul, sonner, react-day-picker), not seven.

    Per-build measurement table with columns: Build | Packages added | Shared by all (KB) | / First Load (KB) | /_not-found First Load (KB) | Delta vs Build B (KB) | Notes

    Pre-fill three rows:
    - Row "B (carry-over from 61-01)" — populate the three KB columns by copying values from `.planning/phases/61-bundle-hygiene/61-01-RESEARCH-LOG.md` row B; Notes = "Copied from 61-01-RESEARCH-LOG.md row B"
    - Row "C" — Packages added column = "cmdk + vaul"; KB columns = "TBD"; Notes = "optimizePackageImports: [..., \"cmdk\", \"vaul\"]; expected ~0 KB delta on Shared by all (lazy packages)"
    - Row "D" — Packages added column = "sonner + react-day-picker"; KB columns = "TBD"; Notes = "optimizePackageImports: [..., \"sonner\", \"react-day-picker\"]; expected ~0 KB delta on Shared by all (lazy packages)"

    Sections: "## Build B carry-over (baseline reference)" — paste the Build B Route (app) snapshot block verbatim from 61-01-RESEARCH-LOG.md.
    "## Build C — cmdk + vaul added" — placeholder text "(populated in Task 1)".
    "## Build D — sonner + react-day-picker added" — placeholder text "(populated in Task 2)".

    Commit with message: `docs(61-02): create RESEARCH-LOG with Build B carry-over and date-fns skip rationale`
  </action>
  <verify>
    <automated>test -f .planning/phases/61-bundle-hygiene/61-02-RESEARCH-LOG.md && grep -E "rm -rf \\.next/cache \\.next" .planning/phases/61-bundle-hygiene/61-02-RESEARCH-LOG.md && grep -F "date-fns SKIP" .planning/phases/61-bundle-hygiene/61-02-RESEARCH-LOG.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f .planning/phases/61-bundle-hygiene/61-02-RESEARCH-LOG.md` exits 0
    - Stale-chunk guard documented: `grep -E "rm -rf \\.next/cache \\.next" .planning/phases/61-bundle-hygiene/61-02-RESEARCH-LOG.md` returns ≥1 match
    - date-fns skip rationale documented: `grep -F "date-fns SKIP" .planning/phases/61-bundle-hygiene/61-02-RESEARCH-LOG.md` returns ≥1 match
    - Build B carry-over row populated (no TBD in KB columns): grep returns line with "B (carry-over" and three numeric KB values
    - "Build C — cmdk + vaul added" heading exists: `grep -F "Build C — cmdk + vaul added" .planning/phases/61-bundle-hygiene/61-02-RESEARCH-LOG.md` returns ≥1 match
    - "Build D — sonner + react-day-picker added" heading exists: `grep -F "Build D — sonner + react-day-picker added" .planning/phases/61-bundle-hygiene/61-02-RESEARCH-LOG.md` returns ≥1 match
    - Commit exists with message matching `docs(61-02): create RESEARCH-LOG with Build B carry-over and date-fns skip rationale`
  </acceptance_criteria>
  <done>
    61-02-RESEARCH-LOG.md exists with templated table, populated baseline (Build B carry-over), and the date-fns skip decision documented. One commit landed.
  </done>
</task>

<task type="auto" id="61-02-01">
  <name>Task 1 (Wave 1): Add "cmdk" + "vaul" to optimizePackageImports + measure Build C</name>
  <files>
    next.config.ts
    .planning/phases/61-bundle-hygiene/61-02-RESEARCH-LOG.md
  </files>
  <read_first>
    - .planning/phases/61-bundle-hygiene/61-RESEARCH.md (sections 1 cmdk + vaul compatibility, 2 lazy load posture)
    - .planning/phases/61-bundle-hygiene/61-02-RESEARCH-LOG.md (current state with Build B carry-over)
    - next.config.ts (current state — Plan 01 end-state with lucide-react + radix-ui + input-otp)
    - components/ui/command.tsx (the only file importing from "cmdk")
    - components/ui/drawer.tsx (the only file importing from "vaul")
  </read_first>
  <action>
    Edit `next.config.ts`. The `optimizePackageImports` array currently reads (Plan 01 end-state):

    ```ts
    optimizePackageImports: ["lucide-react", "radix-ui", "input-otp"],
    ```

    Replace with the literal string:

    ```ts
    optimizePackageImports: ["lucide-react", "radix-ui", "input-otp", "cmdk", "vaul"],
    ```

    Do NOT change `useCache: true` or the `redirects()` block. Do NOT remove any prior package.

    Commit before measurement with message: `Chore(61-02): add cmdk and vaul to optimizePackageImports`

    Then run Build C:

    ```bash
    rm -rf .next/cache .next && ANALYZE=true pnpm build 2>&1 | tee /tmp/phase61-build-C.txt
    ```

    Extract the three KB values (Shared by all / / / /_not-found) from the Route (app) stdout table.

    Update `61-02-RESEARCH-LOG.md`:
    1. Populate row C with the three KB values + delta vs Build B (Shared by all delta).
    2. Replace the "Build C — cmdk + vaul added" placeholder with the verbatim Route (app) header + `/` + `/_not-found` + "First Load JS shared by all" lines from stdout.
    3. Run `pnpm typecheck` — must exit 0. Note pass/fail in row C Notes column.
    4. If "Shared by all" delta is approximately 0 KB (within ±0.5 KB), note "expected lazy-package no-delta confirmed" in Notes. If non-zero, note the magnitude — but do NOT treat this as a failure (Plan 03 owns the final gate).

    Commit the log update with message: `docs(61-02): record Build C measurement (cmdk + vaul)`
  </action>
  <verify>
    <automated>grep -F '"cmdk"' next.config.ts && grep -F '"vaul"' next.config.ts && grep -F '"radix-ui"' next.config.ts && grep -F '"input-otp"' next.config.ts && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `next.config.ts` contains `"cmdk"` and `"vaul"`: `grep -F '"cmdk"' next.config.ts && grep -F '"vaul"' next.config.ts` both return ≥1 match
    - All Plan 01 entries preserved: `grep -F '"lucide-react"' next.config.ts && grep -F '"radix-ui"' next.config.ts && grep -F '"input-otp"' next.config.ts` all return ≥1 match
    - `useCache: true` line preserved: `grep -F "useCache: true" next.config.ts` returns ≥1 match
    - 61-02-RESEARCH-LOG.md row C populated (no TBD in KB columns): grep returns line starting with "| C " containing numeric KB values, not "TBD"
    - 61-02-RESEARCH-LOG.md "Build C — cmdk + vaul added" detail block contains the literal string "First Load JS shared by all"
    - `pnpm typecheck` exits 0
    - Two commits exist: `Chore(61-02): add cmdk and vaul to optimizePackageImports` AND `docs(61-02): record Build C measurement (cmdk + vaul)`
  </acceptance_criteria>
  <done>
    `next.config.ts` `optimizePackageImports` array equals `["lucide-react", "radix-ui", "input-otp", "cmdk", "vaul"]`. Build C succeeded; row C values + delta + detail block recorded. Type-check passes. Two atomic commits landed in bisect order.
  </done>
</task>

<task type="auto" id="61-02-02">
  <name>Task 2 (Wave 1): Add "sonner" + "react-day-picker" to optimizePackageImports + measure Build D</name>
  <files>
    next.config.ts
    .planning/phases/61-bundle-hygiene/61-02-RESEARCH-LOG.md
  </files>
  <read_first>
    - .planning/phases/61-bundle-hygiene/61-RESEARCH.md (sections 1 sonner + react-day-picker compatibility, 2 lazy load posture)
    - .planning/phases/61-bundle-hygiene/61-02-RESEARCH-LOG.md (state with Build B + Build C populated)
    - next.config.ts (current state with cmdk + vaul added by Task 1)
    - components/sf/sf-toast.tsx (the only file importing from "sonner")
    - components/ui/calendar.tsx (the only file importing from "react-day-picker")
  </read_first>
  <action>
    Edit `next.config.ts`. The `optimizePackageImports` array currently reads (after Task 1):

    ```ts
    optimizePackageImports: ["lucide-react", "radix-ui", "input-otp", "cmdk", "vaul"],
    ```

    Replace with the literal string (final v1.8-lock end-state for BND-02):

    ```ts
    optimizePackageImports: ["lucide-react", "radix-ui", "input-otp", "cmdk", "vaul", "sonner", "react-day-picker"],
    ```

    Commit before measurement with message: `Chore(61-02): add sonner and react-day-picker to optimizePackageImports`

    Then run Build D:

    ```bash
    rm -rf .next/cache .next && ANALYZE=true pnpm build 2>&1 | tee /tmp/phase61-build-D.txt
    ```

    Extract the three KB values from the Route (app) stdout table.

    Update `61-02-RESEARCH-LOG.md`:
    1. Populate row D with the three KB values + delta vs Build B.
    2. Replace the "Build D — sonner + react-day-picker added" placeholder with the verbatim Route (app) header + `/` + `/_not-found` + "First Load JS shared by all" lines from stdout.
    3. Run `pnpm typecheck` — must exit 0. Note pass/fail in row D Notes.
    4. If "Shared by all" delta is approximately 0 KB (within ±0.5 KB) vs Build B, note "expected lazy-package no-delta confirmed" in Notes.

    Commit the log update with message: `docs(61-02): record Build D measurement (sonner + react-day-picker)`
  </action>
  <verify>
    <automated>grep -F '"sonner"' next.config.ts && grep -F '"react-day-picker"' next.config.ts && grep -F '"cmdk"' next.config.ts && grep -F '"vaul"' next.config.ts && grep -F '"radix-ui"' next.config.ts && grep -F '"input-otp"' next.config.ts && grep -F '"lucide-react"' next.config.ts && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `next.config.ts` contains all six non-skip BND-02 packages: `"lucide-react"`, `"radix-ui"`, `"input-otp"`, `"cmdk"`, `"vaul"`, `"sonner"`, `"react-day-picker"` (the final v1.8-lock state)
    - `next.config.ts` does NOT contain `"date-fns"` (skip decision honored): `grep -F '"date-fns"' next.config.ts` exits 1
    - `next.config.ts` does NOT contain `"@radix-ui/react-` substring: `grep -F '"@radix-ui/react-' next.config.ts` exits 1
    - 61-02-RESEARCH-LOG.md row D populated (no TBD in KB columns): grep returns line starting with "| D " containing numeric KB values, not "TBD"
    - 61-02-RESEARCH-LOG.md "Build D — sonner + react-day-picker added" detail block contains the literal string "First Load JS shared by all"
    - `pnpm typecheck` exits 0
    - Two commits exist: `Chore(61-02): add sonner and react-day-picker to optimizePackageImports` AND `docs(61-02): record Build D measurement (sonner + react-day-picker)`
  </acceptance_criteria>
  <done>
    `next.config.ts` `optimizePackageImports` array equals `["lucide-react", "radix-ui", "input-otp", "cmdk", "vaul", "sonner", "react-day-picker"]` — the final v1.8-lock BND-02 end-state. Build D succeeded; row D values + delta + detail block recorded. Type-check passes. Two atomic commits landed in bisect order.
  </done>
</task>

</tasks>

<verification>
End-of-plan checks (executor MUST run all before declaring complete):

1. `grep -F '"lucide-react"' next.config.ts && grep -F '"radix-ui"' next.config.ts && grep -F '"input-otp"' next.config.ts && grep -F '"cmdk"' next.config.ts && grep -F '"vaul"' next.config.ts && grep -F '"sonner"' next.config.ts && grep -F '"react-day-picker"' next.config.ts` — all seven return ≥1 match
2. `grep -F '"date-fns"' next.config.ts` — exits 1 (skip decision honored)
3. `grep -F '"@radix-ui/react-' next.config.ts` — exits 1
4. `test -f .planning/phases/61-bundle-hygiene/61-02-RESEARCH-LOG.md` — exits 0
5. `grep -F "date-fns SKIP" .planning/phases/61-bundle-hygiene/61-02-RESEARCH-LOG.md` — returns ≥1 match
6. `grep -E "rm -rf \\.next/cache \\.next" .planning/phases/61-bundle-hygiene/61-02-RESEARCH-LOG.md` — returns ≥1 match
7. All three rows (B carry-over, C, D) in the table have populated KB values (no TBD in numeric columns)
8. `pnpm typecheck` — exits 0
9. Git log shows 5 commits in this plan's range: 1 docs (template + carry-over), 1 chore (cmdk+vaul add), 1 docs (Build C), 1 chore (sonner+react-day-picker add), 1 docs (Build D). Bisect order preserved (chore before docs).
</verification>

<success_criteria>
1. `next.config.ts` `optimizePackageImports` array contains exactly seven entries in this order: `["lucide-react", "radix-ui", "input-otp", "cmdk", "vaul", "sonner", "react-day-picker"]`.
2. `61-02-RESEARCH-LOG.md` exists with all three measurement rows populated (B carry-over, C, D) and three "Build N" detail blocks each containing a Route (app) snapshot.
3. The date-fns SKIP decision is documented with rationale referencing 61-RESEARCH §1.
4. Each measurement was preceded by the BND-04 stale-chunk guard.
5. `pnpm typecheck` exits 0 after Build C and Build D.
6. Lazy-package zero-delta on "Shared by all" is documented as expected behavior, not flagged as a regression.
</success_criteria>

<output>
After completion, create `.planning/phases/61-bundle-hygiene/61-02-SUMMARY.md` with:
- Final state of `next.config.ts` `optimizePackageImports` array (verbatim quoted)
- Three measurement rows from `61-02-RESEARCH-LOG.md` (B carry-over, C, D) with absolute KB values + deltas
- date-fns skip decision restated for Plan 03 final-gate context
- Notes for Plan 03 (any unexpected non-zero deltas on lazy adds, build behavior anomalies)
- Commit hashes for each of the 5 commits (3 docs + 2 chore)
</output>
