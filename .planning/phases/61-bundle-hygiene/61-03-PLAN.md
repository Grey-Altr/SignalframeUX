---
phase: 61
plan: "03"
title: "Verification + final gate (BND-01, BND-03, BND-04, AES-04)"
type: execute
wave: 3
depends_on: ["61-02"]
files_modified:
  - tests/v1.8-phase61-bundle-hygiene.spec.ts
  - .planning/phases/61-bundle-hygiene/61-03-FINAL-GATE.md
autonomous: true
requirements: [BND-01, BND-03, BND-04]
nyquist_compliant: true
must_haves:
  truths:
    - "tests/v1.8-phase61-bundle-hygiene.spec.ts exists and exits 0 against `pnpm build && pnpm start` production server"
    - "Final pnpm build Shared by all KB value is recorded and verified <= 102 KB (BND-01)"
    - "components/sf/index.ts contains zero `use client` directive matches (BND-03 v1.3 barrel-discipline preserved)"
    - "BND-04 stale-chunk guard is documented in 61-RESEARCH.md AND replicated in headers of 61-01-RESEARCH-LOG.md, 61-02-RESEARCH-LOG.md"
    - "61-03-FINAL-GATE.md contains the 119 KiB unused-JS reduction percentage computed from chunk 3302 + 7525 sums delta between Build 0 (61-01) and final build, asserting >= 80%"
    - "Pixel-diff vs .planning/visual-baselines/v1.8-start/ is 0% (MAX_DIFF_RATIO = 0) across 4 viewports x 5 routes (AES-04 invisible-by-construction)"
  artifacts:
    - path: "tests/v1.8-phase61-bundle-hygiene.spec.ts"
      provides: "Phase 61 pixel-diff harness + (optional companion sub-spec) Shared-by-all KB parser"
      contains: "MAX_DIFF_RATIO = 0"
    - path: ".planning/phases/61-bundle-hygiene/61-03-FINAL-GATE.md"
      provides: "BND-01 + BND-03 + BND-04 final-gate evidence with measured numbers and computed reduction percentage"
      contains: "Shared by all"
  key_links:
    - from: "tests/v1.8-phase61-bundle-hygiene.spec.ts"
      to: ".planning/visual-baselines/v1.8-start/"
      via: "pixelmatch comparison at MAX_DIFF_RATIO = 0"
      pattern: "MAX_DIFF_RATIO\\s*=\\s*0"
    - from: "61-03-FINAL-GATE.md"
      to: "61-01-RESEARCH-LOG.md Build 0 + final pnpm build stdout"
      via: "manual paste of Shared by all + chunk 3302/7525 sums + reduction% formula"
      pattern: "(Shared by all|3302|7525|reduction)"
---

<objective>
Close Phase 61 with three verification gates that confirm the Plan 01 + Plan 02 changes met the BND requirements and did not regress visual output:

1. **BND-03 verify-only:** Confirm `components/sf/index.ts` is directive-free (zero `use client` matches). This is a pre-audit-confirmed PASS that requires a logged verification step.
2. **BND-04 doc-gate:** Confirm the stale-chunk guard `rm -rf .next/cache .next` is documented in 61-RESEARCH.md (already present per pre-audit) and replicate the documentation in the headers of 61-01-RESEARCH-LOG.md and 61-02-RESEARCH-LOG.md (already mandated by their plans; this task verifies presence).
3. **BND-01 final gate:** Run the final stale-chunk-guarded `ANALYZE=true pnpm build`; record the "Shared by all" KB value; assert <= 102 KB; compute the 119 KiB unused-JS reduction percentage from chunk 3302 + 7525 sums delta between Build 0 (61-01-RESEARCH-LOG.md) and this final build; assert >= 80%.
4. **AES-04 invisible-by-construction:** Create `tests/v1.8-phase61-bundle-hygiene.spec.ts` from the Phase 59 template with `MAX_DIFF_RATIO = 0` against `.planning/visual-baselines/v1.8-start/` baselines (20 PNGs at 4 viewports x 5 routes). Run against `pnpm build && pnpm start` production server; assert exit 0.

Output:
- `tests/v1.8-phase61-bundle-hygiene.spec.ts` (new file, cloned from `tests/v1.8-phase59-pixel-diff.spec.ts` with strict MAX_DIFF_RATIO = 0)
- `.planning/phases/61-bundle-hygiene/61-03-FINAL-GATE.md` (new file, contains BND-01/03/04 final-gate evidence + computed reduction percentage)
- Final commit closes Phase 61.
</objective>

<execution_context>
@/Users/greyaltaer/.claude/pde-os/engines/gsd/workflows/execute-plan.md
@/Users/greyaltaer/.claude/pde-os/engines/gsd/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/61-bundle-hygiene/61-RESEARCH.md
@.planning/phases/61-bundle-hygiene/61-VALIDATION.md
@.planning/phases/61-bundle-hygiene/61-01-PLAN.md
@.planning/phases/61-bundle-hygiene/61-01-RESEARCH-LOG.md
@.planning/phases/61-bundle-hygiene/61-02-PLAN.md
@.planning/phases/61-bundle-hygiene/61-02-RESEARCH-LOG.md
@.planning/codebase/v1.8-lcp-diagnosis.md
@components/sf/index.ts
@tests/v1.8-phase59-pixel-diff.spec.ts
@playwright.config.ts

<interfaces>
Phase 59 pixel-diff template — relevant fields the new spec must replicate:

```ts
const ROUTES = [
  { path: "/", slug: "home" },
  { path: "/system", slug: "system" },
  { path: "/init", slug: "init" },
  { path: "/inventory", slug: "inventory" },
  { path: "/reference", slug: "reference" },
] as const;

const VIEWPORTS = [
  { name: "desktop-1440x900", width: 1440, height: 900 },
  { name: "iphone13-390x844", width: 390, height: 844 },
  { name: "ipad-834x1194", width: 834, height: 1194 },
  { name: "mobile-360x800", width: 360, height: 800 },
] as const;

const BASELINE_DIR = path.resolve(process.cwd(), ".planning/visual-baselines/v1.8-start");
const MAX_DIFF_RATIO = 0; // STRICT — Phase 61 is invisible by construction
```

Playwright project config (`playwright.config.ts`):
- Single project: chromium with SwiftShader (`--use-gl=swiftshader --enable-webgl`)
- baseURL: http://localhost:3000
- webServer in CI: `pnpm build && pnpm start`; locally requires manual `pnpm build && pnpm start` first
- Invocation pattern (matches Phase 59 plan precedent): `pnpm exec playwright test tests/v1.8-phase61-bundle-hygiene.spec.ts --project=chromium`

Reduction percentage formula (BND-01 quantitative gate):

Let:
- B0_3302 = chunk 3302 size at Build 0 (parsed KB) per DGN-02 §2a = 159.3 KB
- B0_7525 = chunk 7525 size at Build 0 (parsed KB) per DGN-02 §2a = 75.1 KB
- B0_unused = 119 KiB (the v1.8-start unused-JS budget per ROADMAP success criterion 1)
- Bf_3302 = chunk 3302 size at final build (parsed KB) — measured in this plan from .next/analyze/client.html chartData (allowed for this informational reduction% calculation; FORBIDDEN as a gating source for Shared by all per ROADMAP)
- Bf_7525 = chunk 7525 size at final build (parsed KB) — measured similarly

Note: chunk IDs may drift after Phase 61's optimizePackageImports changes per 61-RESEARCH §Risks #8. If 3302/7525 chunk filenames change, locate the successor chunks by their dominant package signatures (radix-ui aggregate + popper-floating cluster respectively) and use those.

Reduction% formula:
- delta = (B0_3302 + B0_7525) - (Bf_3302 + Bf_7525)
- reduction_percentage = (delta / B0_unused) * 100

Assertion: reduction_percentage >= 80.

BND-01 primary gate (ROADMAP success criterion 1) is "Shared by all" <= 102 KB from the Route (app) stdout table — this is the binding gate. The reduction percentage is a secondary informational metric also required by the ROADMAP success criterion ("the 119 KiB unused JS measured at v1.8 start is reduced by >=80%").
</interfaces>
</context>

<threat_model>
  <surface>dev-only test file additions + planning documentation; zero production code changes</surface>
  <auth_changes>none</auth_changes>
  <network_surface>none (Playwright spec hits localhost:3000 only)</network_surface>
  <data_handling>none — no user data, no PII, no storage</data_handling>
  <supply_chain>
    No new packages added. Reuses pixelmatch ^7.1.0 + pngjs ^7.0.0 + @playwright/test ^1.59.1, all already devDeps.
    Risk: low.
  </supply_chain>
  <severity>low — verification-only plan</severity>
</threat_model>

<tasks>

<task type="auto" id="61-03-00">
  <name>Task 0 (Wave 0): Create tests/v1.8-phase61-bundle-hygiene.spec.ts from Phase 59 template with MAX_DIFF_RATIO = 0</name>
  <files>tests/v1.8-phase61-bundle-hygiene.spec.ts</files>
  <read_first>
    - tests/v1.8-phase59-pixel-diff.spec.ts (the template to clone)
    - playwright.config.ts (verify chromium-only project + webServer behavior)
    - .planning/visual-baselines/v1.8-start/ (verify 20 baseline PNGs present at 4 viewports x 5 routes)
    - .planning/phases/61-bundle-hygiene/61-RESEARCH.md (section 5 visual regression verification)
    - .planning/phases/61-bundle-hygiene/61-VALIDATION.md (task 61-03-04 row)
  </read_first>
  <action>
    Create `tests/v1.8-phase61-bundle-hygiene.spec.ts` by copying `tests/v1.8-phase59-pixel-diff.spec.ts` verbatim and applying these exact diffs:

    1. Update the file header docblock to read:

    ```
    /**
     * Phase 61 Bundle Hygiene — Pixel-diff against v1.8-start baselines (AES-04 gate).
     *
     * Phase 61 is invisible by construction — adding packages to optimizePackageImports
     * is a build-time barrel-rewrite transform with NO visual surface. Any non-zero
     * pixel diff is a regression.
     *
     * STRICT gate: MAX_DIFF_RATIO === 0. AES-04 floor (0.5%) is the global standing
     * rule but does not apply here; Phase 61 has no documented re-baseline exception.
     *
     * MUST run against `pnpm build && pnpm start` (production), NOT `pnpm dev`.
     * Reuses Phase 57 warmup discipline from tests/v1.8-baseline-capture.spec.ts.
     *
     * Baseline source: .planning/visual-baselines/v1.8-start/ (20 PNGs at 4 viewports x 5 routes).
     */
    ```

    2. Change the constant on the line currently reading `const MAX_DIFF_RATIO = 0.005;` to:

    ```ts
    const MAX_DIFF_RATIO = 0; // STRICT — Phase 61 is invisible by construction
    ```

    3. Update the test.describe label from `"@v18-phase59-pixel-diff (CRT-01 / Plan A)"` to `"@v18-phase61-bundle-hygiene (BND-01 / AES-04)"`.

    4. Update the per-test name template from `${route.slug} @ ${vp.name} matches v1.8-start within 0% (Plan A invisible)` to `${route.slug} @ ${vp.name} matches v1.8-start within 0% (Phase 61 invisible)`.

    5. Update the assertion failure message from `"Pixel diff ${(ratio * 100).toFixed(3)}% exceeds AES-04 0.5% gate"` to `"Pixel diff ${(ratio * 100).toFixed(3)}% exceeds Phase 61 strict 0% gate (bundle hygiene must be invisible)"`.

    6. Update the artifact directory from `.playwright-artifacts/phase59-pixel-diff` to `.playwright-artifacts/phase61-bundle-hygiene`.

    7. Update the `expect(ratio, ...).toBeLessThanOrEqual(MAX_DIFF_RATIO)` line to remain unchanged in semantics but ensure the comment block above it reflects the strict-zero gate. Keep `pixelmatch` `threshold: 0.1` parameter unchanged (this is the per-pixel YIQ threshold, not the ratio gate).

    Do NOT change the ROUTES array, VIEWPORTS array, BASELINE_DIR, or the page.goto/setViewportSize/screenshot orchestration. Those are template-correct.

    Commit with message: `test(61-03): clone Phase 59 pixel-diff spec to Phase 61 with strict 0% gate`
  </action>
  <verify>
    <automated>test -f tests/v1.8-phase61-bundle-hygiene.spec.ts && grep -E "const MAX_DIFF_RATIO\\s*=\\s*0\\b" tests/v1.8-phase61-bundle-hygiene.spec.ts && grep -F "@v18-phase61-bundle-hygiene" tests/v1.8-phase61-bundle-hygiene.spec.ts && grep -F "v1.8-start" tests/v1.8-phase61-bundle-hygiene.spec.ts && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f tests/v1.8-phase61-bundle-hygiene.spec.ts` exits 0
    - MAX_DIFF_RATIO is exactly 0 (not 0.005): `grep -E "const MAX_DIFF_RATIO\\s*=\\s*0\\b" tests/v1.8-phase61-bundle-hygiene.spec.ts` returns ≥1 match AND `grep -F "MAX_DIFF_RATIO = 0.005" tests/v1.8-phase61-bundle-hygiene.spec.ts` exits 1 (no match)
    - test.describe label updated: `grep -F "@v18-phase61-bundle-hygiene" tests/v1.8-phase61-bundle-hygiene.spec.ts` returns ≥1 match
    - BASELINE_DIR points at v1.8-start: `grep -F "v1.8-start" tests/v1.8-phase61-bundle-hygiene.spec.ts` returns ≥1 match
    - All 5 ROUTES preserved: `grep -E "(home|system|init|inventory|reference)" tests/v1.8-phase61-bundle-hygiene.spec.ts | wc -l` returns ≥5
    - All 4 VIEWPORTS preserved: `grep -E "(desktop-1440x900|iphone13-390x844|ipad-834x1194|mobile-360x800)" tests/v1.8-phase61-bundle-hygiene.spec.ts | wc -l` returns ≥4
    - `pnpm typecheck` exits 0 (the new spec compiles against existing devDeps)
    - Commit exists with message matching `test(61-03): clone Phase 59 pixel-diff spec to Phase 61 with strict 0% gate`
  </acceptance_criteria>
  <done>
    `tests/v1.8-phase61-bundle-hygiene.spec.ts` exists with MAX_DIFF_RATIO = 0, the Phase 61 describe label, baseline pointing at v1.8-start, 4 viewports x 5 routes preserved. Type-check passes.
  </done>
</task>

<task type="auto" id="61-03-01">
  <name>Task 1 (Wave 1): BND-03 verify-only + BND-04 doc-gate verification + create 61-03-FINAL-GATE.md skeleton</name>
  <files>.planning/phases/61-bundle-hygiene/61-03-FINAL-GATE.md</files>
  <read_first>
    - components/sf/index.ts (verify zero `use client` directives)
    - .planning/phases/61-bundle-hygiene/61-RESEARCH.md (verify §4 contains stale-chunk guard string `rm -rf .next/cache .next`)
    - .planning/phases/61-bundle-hygiene/61-01-RESEARCH-LOG.md (verify header documents stale-chunk guard)
    - .planning/phases/61-bundle-hygiene/61-02-RESEARCH-LOG.md (verify header documents stale-chunk guard)
    - .planning/phases/61-bundle-hygiene/61-VALIDATION.md (tasks 61-03-01, 61-03-02 rows)
    - .planning/REQUIREMENTS.md (BND-03 + BND-04 definitions)
  </read_first>
  <action>
    Create `.planning/phases/61-bundle-hygiene/61-03-FINAL-GATE.md` with this exact structure:

    File header: title "61-03 Final Gate — BND-01, BND-03, BND-04, AES-04".

    Section: "## BND-03 — sf barrel directive-free (verify-only)"

    Run the assertion:
    ```bash
    grep -n "use client" components/sf/index.ts
    ```

    Expected: exit code 1 (zero matches). This is the v1.3 barrel-discipline rule from CLAUDE.md and REQUIREMENTS.md BND-03.

    Record in this section:
    - The literal command run
    - The exit code observed
    - PASS/FAIL verdict

    Section: "## BND-04 — Stale-chunk guard documentation"

    Run three assertions:
    ```bash
    grep -E "rm -rf \\.next/cache \\.next" .planning/phases/61-bundle-hygiene/61-RESEARCH.md
    grep -E "rm -rf \\.next/cache \\.next" .planning/phases/61-bundle-hygiene/61-01-RESEARCH-LOG.md
    grep -E "rm -rf \\.next/cache \\.next" .planning/phases/61-bundle-hygiene/61-02-RESEARCH-LOG.md
    ```

    Each must return ≥1 match. Record each command + observed match count in this section.

    Section: "## BND-01 — Final gate (populated in Task 2)"
    Placeholder: "(populated by Task 2 after final stale-chunk-guarded build)"

    Section: "## AES-04 — Pixel-diff result (populated in Task 2)"
    Placeholder: "(populated by Task 2 after running tests/v1.8-phase61-bundle-hygiene.spec.ts)"

    Commit with message: `docs(61-03): create FINAL-GATE doc with BND-03 + BND-04 verifications`
  </action>
  <verify>
    <automated>test -f .planning/phases/61-bundle-hygiene/61-03-FINAL-GATE.md && (grep -n "use client" components/sf/index.ts; test $? -eq 1) && grep -E "rm -rf \\.next/cache \\.next" .planning/phases/61-bundle-hygiene/61-RESEARCH.md && grep -E "rm -rf \\.next/cache \\.next" .planning/phases/61-bundle-hygiene/61-01-RESEARCH-LOG.md && grep -E "rm -rf \\.next/cache \\.next" .planning/phases/61-bundle-hygiene/61-02-RESEARCH-LOG.md && grep -F "BND-03" .planning/phases/61-bundle-hygiene/61-03-FINAL-GATE.md && grep -F "BND-04" .planning/phases/61-bundle-hygiene/61-03-FINAL-GATE.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f .planning/phases/61-bundle-hygiene/61-03-FINAL-GATE.md` exits 0
    - BND-03 PASS: `grep -n "use client" components/sf/index.ts` exits 1 (zero matches in the barrel)
    - BND-04 PASS — 61-RESEARCH.md: `grep -E "rm -rf \\.next/cache \\.next" .planning/phases/61-bundle-hygiene/61-RESEARCH.md` returns ≥1 match
    - BND-04 PASS — 61-01-RESEARCH-LOG.md: `grep -E "rm -rf \\.next/cache \\.next" .planning/phases/61-bundle-hygiene/61-01-RESEARCH-LOG.md` returns ≥1 match
    - BND-04 PASS — 61-02-RESEARCH-LOG.md: `grep -E "rm -rf \\.next/cache \\.next" .planning/phases/61-bundle-hygiene/61-02-RESEARCH-LOG.md` returns ≥1 match
    - 61-03-FINAL-GATE.md contains "BND-03" + "BND-04" section headings + the literal verification commands run + observed PASS verdicts
    - Commit exists with message matching `docs(61-03): create FINAL-GATE doc with BND-03 + BND-04 verifications`
  </acceptance_criteria>
  <done>
    BND-03 verified PASS (zero `use client` matches in sf/index.ts). BND-04 verified PASS (stale-chunk guard documented in 61-RESEARCH.md and replicated in both 61-01 / 61-02 RESEARCH-LOGs). 61-03-FINAL-GATE.md skeleton committed with BND-03 + BND-04 sections populated and BND-01 + AES-04 placeholders for Task 2.
  </done>
</task>

<task type="auto" id="61-03-02">
  <name>Task 2 (Wave 2): BND-01 final gate + AES-04 pixel-diff + close 61-03-FINAL-GATE.md</name>
  <files>.planning/phases/61-bundle-hygiene/61-03-FINAL-GATE.md</files>
  <read_first>
    - .planning/phases/61-bundle-hygiene/61-03-FINAL-GATE.md (current state with BND-03 + BND-04 populated)
    - .planning/phases/61-bundle-hygiene/61-01-RESEARCH-LOG.md (read Build 0 row for baseline reduction% calc)
    - .planning/phases/61-bundle-hygiene/61-02-RESEARCH-LOG.md (read Build D row for final state context)
    - .planning/codebase/v1.8-lcp-diagnosis.md §2a (read B0_3302 = 159.3 KB, B0_7525 = 75.1 KB)
    - tests/v1.8-phase61-bundle-hygiene.spec.ts (the spec to run)
    - playwright.config.ts (webServer behavior)
  </read_first>
  <action>
    Run the final gating production build (BND-04 stale-chunk guard mandatory):

    ```bash
    rm -rf .next/cache .next && ANALYZE=true pnpm build 2>&1 | tee /tmp/phase61-build-final.txt
    ```

    Extract from the Route (app) stdout table:
    - "First Load JS shared by all" KB value (call this `final_shared`)
    - "/" row "First Load JS" KB value
    - "/_not-found" row "First Load JS" KB value

    Extract from `.next/analyze/client.html` chartData (one-time use; NOT a gating source — this is for the informational 119 KiB reduction% calc per ROADMAP success criterion 1):
    - chunk 3302 successor size at final build: locate the chunk whose dominant package signature is `@radix-ui/*` aggregate (~145+ KB parsed). Note the filename + parsed KB value as `Bf_3302`.
    - chunk 7525 successor size at final build: locate the chunk whose dominant package signature is the popper-floating cluster (`@radix-ui/react-popper`, `react-remove-scroll`, `aria-hidden`). Note filename + parsed KB value as `Bf_7525`.

    If either chunk ID drifted (per 61-RESEARCH §Risks #8 — expected behavior), record both the new chunk filename AND the dominant-package signature used for attribution.

    Compute the reduction percentage:
    - `B0_3302 = 159.3` (from DGN-02 §2a)
    - `B0_7525 = 75.1` (from DGN-02 §2a)
    - `B0_unused = 119` (the v1.8-start unused-JS budget per ROADMAP)
    - `delta = (B0_3302 + B0_7525) - (Bf_3302 + Bf_7525)` (in KB; if Bf measurements are in KiB, normalize to KB)
    - `reduction_percentage = (delta / B0_unused) * 100`

    Run the AES-04 pixel-diff spec (build the production server first; spec runs against http://localhost:3000):

    ```bash
    pnpm build && pnpm start &
    SERVER_PID=$!
    sleep 8
    pnpm exec playwright test tests/v1.8-phase61-bundle-hygiene.spec.ts --project=chromium 2>&1 | tee /tmp/phase61-pixel-diff-output.txt
    SPEC_EXIT=$?
    kill $SERVER_PID
    test $SPEC_EXIT -eq 0
    ```

    Note: if `pnpm build` was already run for the BND-01 final gate immediately above, the second `pnpm build` may reuse `.next/`; this is acceptable for the spec run because we have already captured the gating measurement. Do NOT delete `.next/` between the BND-01 measurement and the AES-04 spec run — they share the same build artifacts.

    Update `.planning/phases/61-bundle-hygiene/61-03-FINAL-GATE.md`:

    Section: "## BND-01 — Final gate" (replace the placeholder)
    Record:
    - Timestamp of the final build
    - Verbatim Route (app) header + `/` + `/_not-found` + "First Load JS shared by all" lines
    - The literal value of `final_shared` (KB)
    - PASS/FAIL: `final_shared <= 102` (PASS if true, FAIL if false)
    - The reduction percentage calculation:
      - "B0_3302 + B0_7525 = 159.3 + 75.1 = 234.4 KB (Build 0 baseline from DGN-02 §2a)"
      - "Bf_3302 + Bf_7525 = X.X + Y.Y = Z.Z KB (final build, chunks <filenames>)"
      - "delta = 234.4 - Z.Z = D.D KB"
      - "reduction_percentage = (D.D / 119) * 100 = R.R%"
      - PASS/FAIL: `R.R >= 80` (PASS if true, FAIL if false)
    - If chunk IDs drifted (61-RESEARCH §Risks #8), document the successor chunk filenames + the dominant-package signatures used for attribution.

    Section: "## AES-04 — Pixel-diff result" (replace the placeholder)
    Record:
    - The literal command run: `pnpm exec playwright test tests/v1.8-phase61-bundle-hygiene.spec.ts --project=chromium`
    - Observed exit code (must be 0)
    - Observed total / passed / failed test count from the reporter output (expected: 20/20/0)
    - PASS/FAIL: spec exit 0 AND no test reported a non-zero diff

    Section: "## Phase 61 final verdict"
    - Single-line summary: "BND-01 [PASS|FAIL], BND-03 [PASS|FAIL] (Task 1), BND-04 [PASS|FAIL] (Task 1), AES-04 [PASS|FAIL]"
    - If all four PASS: "Phase 61 closes."
    - If any FAIL: "Phase 61 BLOCKED — see [section name] above."

    Commit with message: `docs(61-03): close FINAL-GATE with BND-01 + AES-04 results`
  </action>
  <verify>
    <automated>grep -F "Shared by all" .planning/phases/61-bundle-hygiene/61-03-FINAL-GATE.md && grep -F "reduction_percentage" .planning/phases/61-bundle-hygiene/61-03-FINAL-GATE.md && grep -F "Phase 61 final verdict" .planning/phases/61-bundle-hygiene/61-03-FINAL-GATE.md && pnpm exec playwright test tests/v1.8-phase61-bundle-hygiene.spec.ts --project=chromium 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - 61-03-FINAL-GATE.md "BND-01 — Final gate" section is populated (no "(populated by Task 2..." placeholder remains): `grep -F "(populated by Task 2" .planning/phases/61-bundle-hygiene/61-03-FINAL-GATE.md` exits 1
    - The final "Shared by all" KB value appears in 61-03-FINAL-GATE.md as a numeric value (not "TBD"); if the value is > 102 KB, the FAIL verdict is recorded — but the executor must escalate to the user before declaring the plan complete
    - The reduction percentage formula is recorded with all four numeric values (B0_3302, B0_7525, Bf_3302, Bf_7525) and the computed reduction_percentage. The literal token "reduction_percentage" appears in the file: `grep -F "reduction_percentage" .planning/phases/61-bundle-hygiene/61-03-FINAL-GATE.md` returns ≥1 match
    - AES-04 pixel-diff spec executed and exits 0: `pnpm exec playwright test tests/v1.8-phase61-bundle-hygiene.spec.ts --project=chromium` exit code = 0 (recorded in /tmp/phase61-pixel-diff-output.txt and quoted in 61-03-FINAL-GATE.md)
    - "Phase 61 final verdict" section exists: `grep -F "Phase 61 final verdict" .planning/phases/61-bundle-hygiene/61-03-FINAL-GATE.md` returns ≥1 match
    - Commit exists with message matching `docs(61-03): close FINAL-GATE with BND-01 + AES-04 results`

    BLOCKERS (any of these = task incomplete; escalate):
    - `final_shared > 102` KB → BND-01 fail; do NOT close phase
    - `reduction_percentage < 80` → BND-01 secondary criterion fail; escalate
    - Pixel-diff spec exits non-zero on any of 20 tests → AES-04 fail; bisect (revert Plan 02 sonner+react-day-picker first, then cmdk+vaul, then Plan 01 input-otp, then radix-ui)

    REDUCTION% FALSE-PASS GUARD (per checker Warning #3):
    If `Bf_3302` or `Bf_7525` successor chunk cannot be located by package signature (chunk vanished, or analyzer chartData has no chunk with `@radix-ui/*` aggregate ≥80 KB parsed, or no chunk matches the popper-floating cluster signature), default to the conservative interpretation `Bf_X = B0_X` for that chunk (zero reduction credit). Do NOT interpret a missing chunk as "size = 0" — that would inflate the reduction% to a false PASS. Record the missing-chunk attribution in 61-03-FINAL-GATE.md with the exact analyzer chartData query attempted.
  </acceptance_criteria>
  <done>
    `.planning/phases/61-bundle-hygiene/61-03-FINAL-GATE.md` is fully populated: BND-03 PASS (Task 1), BND-04 PASS (Task 1), BND-01 PASS (final_shared <= 102 KB AND reduction_percentage >= 80%), AES-04 PASS (spec exits 0, 20/20 pixel-diff tests pass against v1.8-start at MAX_DIFF_RATIO = 0). The "Phase 61 final verdict" section reads "Phase 61 closes." Commit landed.
  </done>
</task>

</tasks>

<verification>
End-of-plan checks (executor MUST run all before declaring complete):

1. `test -f tests/v1.8-phase61-bundle-hygiene.spec.ts` — exits 0
2. `grep -E "const MAX_DIFF_RATIO\\s*=\\s*0\\b" tests/v1.8-phase61-bundle-hygiene.spec.ts` — returns ≥1 match
3. `grep -F "MAX_DIFF_RATIO = 0.005" tests/v1.8-phase61-bundle-hygiene.spec.ts` — exits 1 (no match)
4. `test -f .planning/phases/61-bundle-hygiene/61-03-FINAL-GATE.md` — exits 0
5. `grep -n "use client" components/sf/index.ts` — exits 1 (BND-03 PASS)
6. `grep -E "rm -rf \\.next/cache \\.next" .planning/phases/61-bundle-hygiene/61-RESEARCH.md` — ≥1 match (BND-04)
7. `grep -E "rm -rf \\.next/cache \\.next" .planning/phases/61-bundle-hygiene/61-01-RESEARCH-LOG.md` — ≥1 match (BND-04)
8. `grep -E "rm -rf \\.next/cache \\.next" .planning/phases/61-bundle-hygiene/61-02-RESEARCH-LOG.md` — ≥1 match (BND-04)
9. `grep -F "Phase 61 final verdict" .planning/phases/61-bundle-hygiene/61-03-FINAL-GATE.md` — ≥1 match
10. `grep -F "(populated by Task 2" .planning/phases/61-bundle-hygiene/61-03-FINAL-GATE.md` — exits 1 (no placeholders remain)
11. `pnpm build && pnpm start` (backgrounded) followed by `pnpm exec playwright test tests/v1.8-phase61-bundle-hygiene.spec.ts --project=chromium` — exits 0
12. `pnpm typecheck` — exits 0
13. Git log shows 3 commits in this plan: 1 test (spec creation), 1 docs (FINAL-GATE skeleton with BND-03 + BND-04), 1 docs (FINAL-GATE close with BND-01 + AES-04)
</verification>

<success_criteria>
1. `tests/v1.8-phase61-bundle-hygiene.spec.ts` exists with `MAX_DIFF_RATIO = 0` (strict) and exits 0 against `pnpm build && pnpm start` production server.
2. `.planning/phases/61-bundle-hygiene/61-03-FINAL-GATE.md` exists with all four gates recorded:
   - BND-01: final "Shared by all" KB value <= 102 + reduction_percentage >= 80% (computed from chunk 3302+7525 sums delta)
   - BND-03: zero `use client` matches in components/sf/index.ts (verify-only PASS)
   - BND-04: stale-chunk guard documented in 61-RESEARCH.md AND replicated in 61-01-RESEARCH-LOG.md AND 61-02-RESEARCH-LOG.md
   - AES-04: pixel-diff spec exits 0 (zero diff at MAX_DIFF_RATIO = 0 across 4 viewports x 5 routes)
3. The "Phase 61 final verdict" section in 61-03-FINAL-GATE.md reads "Phase 61 closes." (or, if any gate failed, escalates with BLOCKED and bisect direction).
4. `pnpm typecheck` exits 0 with all Phase 61 plan changes applied.
</success_criteria>

<output>
After completion, create `.planning/phases/61-bundle-hygiene/61-03-SUMMARY.md` with:
- Phase 61 final verdict line copied verbatim from 61-03-FINAL-GATE.md
- Final `next.config.ts` `optimizePackageImports` array (verbatim quoted; should match Plan 02 end-state)
- BND-01 numerics: baseline Shared by all (Build 0), final Shared by all, delta KB
- Reduction percentage: B0 chunk sum, Bf chunk sum (with chunk filenames), delta, reduction_percentage
- BND-03 verify command output (exit code 1 expected)
- BND-04 three grep hits documented
- AES-04 spec output: exit code, total/passed/failed test count
- Commit hashes for each of the 3 Plan 03 commits + a roll-up of all Phase 61 commits (Plan 01 + Plan 02 + Plan 03) for milestone closure tracking
</output>

<phase_must_haves>
The Phase 61 phase-level must_haves (specified in the planning context) are owned by this final plan because Plan 03 is the verification plan. Repeated here for executor convenience:

1. `optimizePackageImports` array in `next.config.ts` contains: `"lucide-react"`, `"radix-ui"`, `"input-otp"`, `"cmdk"`, `"vaul"`, `"sonner"`, `"react-day-picker"` (date-fns SKIP per RESEARCH §6).
2. `grep -n "use client" components/sf/index.ts` returns zero matches.
3. Final `pnpm build` "Shared by all" <= 102 KB.
4. >= 80% reduction of the v1.8-start 119 KiB unused-JS budget (computed from chunk 3302 + 7525 sums delta).
5. Pixel-diff spec exits 0 with `MAX_DIFF_RATIO = 0` against `.planning/visual-baselines/v1.8-start/`.
6. Each of `61-01-RESEARCH-LOG.md`, `61-02-RESEARCH-LOG.md`, `61-03-FINAL-GATE.md` exists with populated measurement rows.
</phase_must_haves>
