---
phase: 62-real-device-verification-final-gate
plan: 02
type: execute
wave: 1
waves: [0, 1, 2]
depends_on: ["62-01"]
files_modified:
  - scripts/launch-gate-vrf02.ts
  - .planning/perf-baselines/v1.8/vrf-02-launch-gate-runs.json
  - .planning/perf-baselines/v1.8/vrf-03-motion-contract.md
autonomous: true
requirements: [VRF-02, VRF-03]
threat_model_severity: low
gap_closure: false

must_haves:
  truths:
    - "Synthetic 5-run median launch-gate runs against prod URL show 100/100 across all 4 categories"
    - "Median LCP <1000ms, CLS=0, TTI <1500ms across the 5 runs"
    - "scripts/launch-gate.ts is byte-identical to merge-base (CIB-04 contract preserved)"
    - "scripts/launch-gate-vrf02.ts wrapper exists and produces valid 5-run median JSON"
    - "chrome-devtools MCP scroll-test confirms motion contract intact: single GSAP ticker, 6 SIGNAL surfaces render, prefers-reduced-motion collapses derived motion"
    - "Production bundle has no rogue requestAnimationFrame outside GSAP ticker (Pitfall #5 single-ticker rule)"
  artifacts:
    - path: scripts/launch-gate-vrf02.ts
      provides: "5-run median lighthouse runner wrapping the same chrome-launcher/lighthouse pattern as launch-gate.ts (does NOT modify launch-gate.ts)"
      contains: "RUNS = 5"
    - path: .planning/perf-baselines/v1.8/vrf-02-launch-gate-runs.json
      provides: "VRF-02 5-run median synthetic gate output"
      contains: '"verdict": "PASS"'
    - path: .planning/perf-baselines/v1.8/vrf-03-motion-contract.md
      provides: "VRF-03 chrome-devtools MCP scroll-test evidence per SIGNAL surface, single-ticker assertion, reduced-motion collapse"
      contains: "single-GSAP-ticker"
  key_links:
    - from: scripts/launch-gate-vrf02.ts
      to: ".planning/perf-baselines/v1.8/vrf-02-launch-gate-runs.json"
      via: "writeFileSync output"
      pattern: 'vrf-02-launch-gate-runs\.json'
    - from: .planning/perf-baselines/v1.8/vrf-03-motion-contract.md
      to: "https://signalframeux.vercel.app"
      via: "prod-URL evidence references (Pitfall 4 — never localhost)"
      pattern: "signalframeux\\.vercel\\.app"
---

<threat_model>
**Severity:** low
**Surface:** No new attack surface.

- `scripts/launch-gate-vrf02.ts` wraps existing `lighthouse` + `chrome-launcher` libs already in repo; no new network calls beyond what `launch-gate.ts` already makes.
- CIB-04 byte-identity guard on `scripts/launch-gate.ts` (Phase 58 contract) prevents regression of hardened script — wave 0 acceptance criterion enforces this.
- chrome-devtools MCP runs against production URL only (D-06); no local-server attack surface.
- Pitfall #5 grep guard on production bundle is read-only (`curl | grep`).
- Command-spawn discipline: any spawned process uses `execFile` / `execFileSync` (argv array), never shell-string interpolation, per repo policy.
- ASVS L1 sufficient.
</threat_model>

<objective>
Confirm synthetic gating contract holds at v1.8 final state and motion contract is intact end-to-end.

Purpose: VRF-02 (launch-gate 5-run median) is the proven v1.7 PRF mechanism for "synthetic 100/100 against prod" — must hold at milestone close. VRF-03 (chrome-devtools MCP scroll-test) confirms the SIGNAL/FRAME motion contract survives all v1.8 perf interventions; per `feedback_visual_verification.md`, green Playwright tests on DOM shape ≠ working animation.

Output: 1 wrapper script (devDep tooling) + 1 JSON artifact + 1 MD evidence doc. Zero modifications to production source.

Dependency: Plan 01 MID-MILESTONE-CHECKPOINT.md must show PASS — no point burning 5 lighthouse runs if real-device divergence already invalidated the milestone.
</objective>

<execution_context>
@/Users/greyaltaer/.claude/pde-os/engines/gsd/workflows/execute-plan.md
@/Users/greyaltaer/.claude/pde-os/engines/gsd/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/62-real-device-verification-final-gate/62-CONTEXT.md
@.planning/phases/62-real-device-verification-final-gate/62-RESEARCH.md
@.planning/phases/62-real-device-verification-final-gate/62-VALIDATION.md
@.planning/perf-baselines/v1.8/MID-MILESTONE-CHECKPOINT.md
@scripts/launch-gate.ts
@scripts/launch-gate-runner.mjs
@playwright.config.ts
@.lighthouseci/lighthouserc.json
@.planning/codebase/PITFALLS.md

<interfaces>
<!-- VRF-02 output JSON shape (RESEARCH.md Pattern 5 verbatim): -->

```json
{
  "capturedAt": "ISO-8601",
  "tool": "scripts/launch-gate-vrf02.ts",
  "url": "https://signalframeux.vercel.app/",
  "runs": 5,
  "per_run": [
    {"performance":100,"accessibility":100,"best-practices":100,"seo":100,"lcp_ms":823,"cls":0,"tti_ms":1404}
  ],
  "median": {
    "performance":100,"accessibility":100,"best-practices":100,"seo":100,
    "lcp_ms":815,"cls":0,"tti_ms":1396
  },
  "thresholds": {
    "category_min": 100,
    "lcp_ms_max": 1000,
    "cls_max": 0,
    "tti_ms_max": 1500
  },
  "verdict": "PASS"
}
```

<!-- launch-gate.ts existing pattern (DO NOT MODIFY — read-only reference): -->
- `import { launch } from "chrome-launcher"` — boots headless Chromium
- `import lighthouse from "lighthouse"` — runs audit
- Categories: performance, accessibility, best-practices, seo
- Audits: largest-contentful-paint, cumulative-layout-shift, interactive

<!-- chrome-devtools MCP tool surface (verified at Wave 0; tool names from Phase 35/60 prior art): -->
- mcp__plugin_chrome-devtools-mcp__chrome-devtools__new_page
- mcp__plugin_chrome-devtools-mcp__chrome-devtools__navigate_page
- mcp__plugin_chrome-devtools-mcp__chrome-devtools__resize_page
- mcp__plugin_chrome-devtools-mcp__chrome-devtools__performance_start_trace
- mcp__plugin_chrome-devtools-mcp__chrome-devtools__performance_stop_trace
- mcp__plugin_chrome-devtools-mcp__chrome-devtools__performance_analyze_insight
- mcp__plugin_chrome-devtools-mcp__chrome-devtools__evaluate_script
- mcp__plugin_chrome-devtools-mcp__chrome-devtools__scroll
- mcp__plugin_chrome-devtools-mcp__chrome-devtools__take_snapshot
- mcp__plugin_chrome-devtools-mcp__chrome-devtools__list_console_messages
- mcp__plugin_chrome-devtools-mcp__chrome-devtools__emulate
</interfaces>
</context>

<tasks>

<task type="auto" id="62-02-W0a" tdd="true">
  <name>Task W0a: Create launch-gate-vrf02.ts wrapper + CIB-04 byte-identity guard + chrome-devtools MCP tool surface verification</name>
  <wave>0</wave>
  <files>scripts/launch-gate-vrf02.ts</files>
  <read_first>
    - scripts/launch-gate.ts (full file — pattern source for chrome-launcher/lighthouse boot)
    - scripts/launch-gate-runner.mjs (ESM-runner sibling — for tsx ESM/CJS interop reference)
    - .planning/phases/62-real-device-verification-final-gate/62-RESEARCH.md (§Pattern 2 + Pattern 5 verbatim — wrapper architecture + output JSON shape)
    - .planning/phases/62-real-device-verification-final-gate/62-CONTEXT.md (D-05 — 5 sequential runs, median across 5, all 4 categories=100, LCP <1000ms, CLS=0, TTI <1500ms)
    - .planning/PROJECT.md (line 93 — CIB-04 byte-identity contract on launch-gate.ts)
    - .planning/codebase/PITFALLS.md (Pitfall — launch-gate.ts is RUNS=3 worst-of, not RUNS=5 median)
  </read_first>
  <behavior>
    - The wrapper script must run lighthouse 5 times sequentially against a configurable URL (default: https://signalframeux.vercel.app/)
    - Per run, capture: 4 category scores (performance/accessibility/best-practices/seo) + LCP ms + CLS + TTI ms
    - Compute median per category (sort + index Math.floor(N/2)) — NOT worst-of
    - Compute median LCP/CLS/TTI from per-run audit numericValues
    - Output JSON to `.planning/perf-baselines/v1.8/vrf-02-launch-gate-runs.json` matching Pattern 5 shape verbatim
    - Verdict logic: PASS iff all median categories === 100 AND median lcp_ms < 1000 AND median cls === 0 AND median tti_ms < 1500
    - Use child_process.execFile / execFileSync (argv-array form) for any spawned commands — repo policy; never shell-string interpolation
    - Wave 1 must `rm -rf .next/cache .next` before any measurement (BND-04 stale-chunk guard) — wrapper either does this internally or wave 1 prefixes
  </behavior>
  <action>
    Three independent setup actions; ALL must complete before wave 1 proceeds.

    STEP 1 — CIB-04 BYTE-IDENTITY GUARD (non-negotiable per Phase 58 lock):
    ```bash
    if [ -n "$(git diff --name-only HEAD scripts/launch-gate.ts)" ]; then
      echo "BLOCKER: scripts/launch-gate.ts has uncommitted changes. CIB-04 byte-identity contract requires merge-base parity. Stash or revert before proceeding."
      exit 1
    fi
    # Stronger: confirm against merge-base of main
    if [ -n "$(git diff --name-only main...HEAD scripts/launch-gate.ts)" ]; then
      echo "WARN: scripts/launch-gate.ts diverges from main; confirm CIB-04 lock with user."
    fi
    ```

    STEP 2 — Create `scripts/launch-gate-vrf02.ts` wrapper (NEW file):

    Header (verbatim):
    ```typescript
    #!/usr/bin/env tsx
    /**
     * scripts/launch-gate-vrf02.ts — Phase 62 VRF-02 5-run median runner.
     *
     * Wraps lighthouse directly (NOT spawning launch-gate.ts) — shipping a wrapper
     * keeps CIB-04 byte-identity intact while satisfying VRF-02 D-05 contract:
     * 5 runs median, full-category 100/100, LCP <1.0s, CLS=0, TTI <1.5s, written
     * to .planning/perf-baselines/v1.8/vrf-02-launch-gate-runs.json.
     *
     * Usage:
     *   pnpm tsx scripts/launch-gate-vrf02.ts --url https://signalframeux.vercel.app
     */
    ```

    Implementation contract:
    ```typescript
    import { launch } from "chrome-launcher";
    import lighthouse from "lighthouse";
    import { writeFileSync } from "node:fs";

    const RUNS = 5;
    const URL = process.argv.find(a => a.startsWith("--url"))?.split("=")[1]
              ?? process.argv[process.argv.indexOf("--url") + 1]
              ?? "https://signalframeux.vercel.app/";

    const TARGETS = {
      categoryMin: 100,
      lcpMsMax: 1000,
      clsMax: 0,
      ttiMsMax: 1500,
    };

    interface RunResult {
      performance: number;
      accessibility: number;
      "best-practices": number;
      seo: number;
      lcp_ms: number;
      cls: number;
      tti_ms: number;
    }

    function median(arr: number[]): number {
      const sorted = [...arr].sort((a, b) => a - b);
      return sorted[Math.floor(sorted.length / 2)];
    }

    async function runOnce(): Promise<RunResult> {
      const chrome = await launch({ chromeFlags: ["--headless", "--no-sandbox"] });
      try {
        const result = await lighthouse(URL, {
          port: chrome.port,
          output: "json",
          logLevel: "error",
          onlyCategories: ["performance", "accessibility", "best-practices", "seo"],
        });
        if (!result) throw new Error("lighthouse returned undefined");
        const lhr = result.lhr;
        return {
          performance: Math.round((lhr.categories.performance.score ?? 0) * 100),
          accessibility: Math.round((lhr.categories.accessibility.score ?? 0) * 100),
          "best-practices": Math.round((lhr.categories["best-practices"].score ?? 0) * 100),
          seo: Math.round((lhr.categories.seo.score ?? 0) * 100),
          lcp_ms: Math.round(lhr.audits["largest-contentful-paint"].numericValue ?? 0),
          cls: lhr.audits["cumulative-layout-shift"].numericValue ?? 0,
          tti_ms: Math.round(lhr.audits["interactive"].numericValue ?? 0),
        };
      } finally {
        await chrome.kill();
      }
    }

    async function main() {
      const perRun: RunResult[] = [];
      for (let i = 0; i < RUNS; i++) {
        console.error(`Run ${i + 1}/${RUNS}…`);
        perRun.push(await runOnce());
      }
      const med = {
        performance: median(perRun.map(r => r.performance)),
        accessibility: median(perRun.map(r => r.accessibility)),
        "best-practices": median(perRun.map(r => r["best-practices"])),
        seo: median(perRun.map(r => r.seo)),
        lcp_ms: median(perRun.map(r => r.lcp_ms)),
        cls: median(perRun.map(r => r.cls)),
        tti_ms: median(perRun.map(r => r.tti_ms)),
      };
      const verdict =
        med.performance === 100 && med.accessibility === 100 &&
        med["best-practices"] === 100 && med.seo === 100 &&
        med.lcp_ms < TARGETS.lcpMsMax &&
        med.cls === TARGETS.clsMax &&
        med.tti_ms < TARGETS.ttiMsMax
          ? "PASS" : "FAIL";

      const out = {
        capturedAt: new Date().toISOString(),
        tool: "scripts/launch-gate-vrf02.ts",
        url: URL,
        runs: RUNS,
        per_run: perRun,
        median: med,
        thresholds: {
          category_min: TARGETS.categoryMin,
          lcp_ms_max: TARGETS.lcpMsMax,
          cls_max: TARGETS.clsMax,
          tti_ms_max: TARGETS.ttiMsMax,
        },
        verdict,
      };
      writeFileSync(".planning/perf-baselines/v1.8/vrf-02-launch-gate-runs.json",
                    JSON.stringify(out, null, 2));
      console.log(JSON.stringify(out.median, null, 2));
      process.exit(verdict === "PASS" ? 0 : 1);
    }

    main().catch(err => { console.error(err); process.exit(2); });
    ```

    STEP 3 — Verify chrome-devtools MCP tool surface (do NOT skip — RESEARCH §3 flagged tool name drift risk):

    Make a minimal MCP call sequence to confirm the tools exist:
    ```
    mcp__plugin_chrome-devtools-mcp__chrome-devtools__new_page
    mcp__plugin_chrome-devtools-mcp__chrome-devtools__list_console_messages
    ```
    If any tool ID has shifted, document the actual tool names in `.planning/perf-baselines/v1.8/vrf-03-motion-contract.md` wave-2 actual usage; update task W2a action accordingly. Do NOT block wave 0 if tool surface differs — adapt at wave 2 invocation.

    STEP 4 — Type-check the new wrapper:
    ```bash
    pnpm tsc --noEmit scripts/launch-gate-vrf02.ts
    ```

    STEP 5 — CIB-04 final guard (after creating wrapper, no other script touched):
    ```bash
    [ -z "$(git diff --name-only HEAD scripts/launch-gate.ts)" ]  # MUST be empty
    [ -z "$(git diff --name-only HEAD scripts/launch-gate-runner.mjs)" ]  # MUST be empty
    ```

    Commit message: `chore(62-02): VRF-02 launch-gate-vrf02 wrapper (5-run median; CIB-04 byte-identity preserved)`
  </action>
  <verify>
    <automated>test -f scripts/launch-gate-vrf02.ts && [ -z "$(git diff --name-only HEAD scripts/launch-gate.ts)" ] && grep -E 'RUNS = 5' scripts/launch-gate-vrf02.ts && grep -E 'verdict' scripts/launch-gate-vrf02.ts && pnpm tsc --noEmit scripts/launch-gate-vrf02.ts</automated>
  </verify>
  <acceptance_criteria>
    - `test -f scripts/launch-gate-vrf02.ts` succeeds
    - `grep -c 'RUNS = 5' scripts/launch-gate-vrf02.ts` returns at least 1
    - `grep -E 'function median' scripts/launch-gate-vrf02.ts` matches (median helper present)
    - `grep -E 'vrf-02-launch-gate-runs\.json' scripts/launch-gate-vrf02.ts` matches (output path hardcoded)
    - `grep -E 'lcp_ms_max.*1000' scripts/launch-gate-vrf02.ts` matches (threshold)
    - `grep -E 'tti_ms_max.*1500' scripts/launch-gate-vrf02.ts` matches
    - `grep -E 'cls_max.*0' scripts/launch-gate-vrf02.ts` matches
    - `pnpm tsc --noEmit scripts/launch-gate-vrf02.ts` exits 0
    - `[ -z "$(git diff --name-only HEAD scripts/launch-gate.ts)" ]` returns true (CIB-04 byte-identity preserved)
    - `[ -z "$(git diff --name-only HEAD scripts/launch-gate-runner.mjs)" ]` returns true
    - chrome-devtools MCP tool surface verification logged (in commit body or wave 2 header)
  </acceptance_criteria>
  <done>VRF-02 wrapper exists; CIB-04 byte-identity preserved; MCP tool surface verified; wave 1 unblocked.</done>
</task>

<task type="auto" id="62-02-W1a">
  <name>Task W1a: Run launch-gate-vrf02 5× against prod URL + commit JSON</name>
  <wave>1</wave>
  <files>.planning/perf-baselines/v1.8/vrf-02-launch-gate-runs.json</files>
  <read_first>
    - scripts/launch-gate-vrf02.ts (just-created wave 0)
    - .planning/phases/62-real-device-verification-final-gate/62-CONTEXT.md (D-05 — exact thresholds)
    - .planning/codebase/PITFALLS.md (Pitfall — BND-04 stale-chunk guard)
  </read_first>
  <action>
    STEP 1 — STALE-CHUNK GUARD (mandatory per BND-04 standing rule):
    ```bash
    rm -rf .next/cache .next
    ```
    This MUST run before any measurement. The vrf-02 runs are against PROD, not local build, so the .next/ purge is defensive (prevents accidental local-build measurement contamination if a stale dev server is running).

    STEP 2 — Confirm prod URL is responding before burning lighthouse runs:
    ```bash
    PROD_URL="https://signalframeux.vercel.app/"  # adjust if Plan 01 W0a found different URL
    curl -sf -o /dev/null --max-time 15 "$PROD_URL" || { echo "BLOCKER: prod URL not reachable"; exit 1; }
    ```

    STEP 3 — Run the wrapper (5 lighthouse runs, ~4-7 min):
    ```bash
    pnpm tsx scripts/launch-gate-vrf02.ts --url "$PROD_URL"
    # Wrapper writes .planning/perf-baselines/v1.8/vrf-02-launch-gate-runs.json
    # Wrapper exits 0 iff PASS, 1 iff FAIL
    ```

    STEP 4 — BLOCKING gate check:
    ```bash
    jq -e '.verdict == "PASS" and .median.lcp_ms < 1000 and .median.cls == 0 and .median.tti_ms < 1500 and .median.performance == 100 and .median.accessibility == 100 and .median["best-practices"] == 100 and .median.seo == 100' \
      .planning/perf-baselines/v1.8/vrf-02-launch-gate-runs.json
    ```

    If FAIL: do NOT commit. Document failure in HUMAN-UAT. Plan 03 BLOCKED.

    STEP 5 — Single-ticker production-bundle grep (Pitfall #5):
    ```bash
    # Confirm no rogue requestAnimationFrame outside GSAP in shipped bundle
    MAIN_CHUNK=$(curl -s "$PROD_URL" | grep -oE '/_next/static/chunks/main-[a-z0-9]+\.js' | head -1)
    if [ -n "$MAIN_CHUNK" ]; then
      RAF_COUNT=$(curl -s "${PROD_URL%/}${MAIN_CHUNK}" | grep -cE 'requestAnimationFrame' || echo 0)
      echo "main-bundle requestAnimationFrame count: $RAF_COUNT (expected: ≤1 for GSAP ticker)"
      [ "$RAF_COUNT" -le 1 ] || echo "WARN: rogue rAF call sites in main bundle — feed into VRF-03 motion contract"
    fi
    ```

    Commit message: `chore(62-02): VRF-02 5-run median PASS (LCP=${LCP}ms, CLS=0, TTI=${TTI}ms)`
  </action>
  <verify>
    <automated>jq -e '.verdict == "PASS" and .median.lcp_ms < 1000 and .median.cls == 0 and .median.tti_ms < 1500 and .median.performance == 100 and .median.accessibility == 100 and .median["best-practices"] == 100 and .median.seo == 100 and (.per_run | length) == 5' .planning/perf-baselines/v1.8/vrf-02-launch-gate-runs.json</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/perf-baselines/v1.8/vrf-02-launch-gate-runs.json` exists
    - `jq -e '.verdict == "PASS"' …vrf-02-launch-gate-runs.json` returns true
    - `jq -e '.median.lcp_ms < 1000' …vrf-02-launch-gate-runs.json` returns true (LCP <1.0s)
    - `jq -e '.median.cls == 0' …vrf-02-launch-gate-runs.json` returns true (CLS strict 0)
    - `jq -e '.median.tti_ms < 1500' …vrf-02-launch-gate-runs.json` returns true (TTI <1.5s)
    - `jq -e '.median.performance == 100' …vrf-02-launch-gate-runs.json` returns true
    - `jq -e '.median.accessibility == 100' …vrf-02-launch-gate-runs.json` returns true
    - `jq -e '.median["best-practices"] == 100' …vrf-02-launch-gate-runs.json` returns true
    - `jq -e '.median.seo == 100' …vrf-02-launch-gate-runs.json` returns true
    - `jq -e '(.per_run | length) == 5' …vrf-02-launch-gate-runs.json` returns true (D-05 5-run discipline)
    - `jq -e '.url | test("https://")' …vrf-02-launch-gate-runs.json` returns true (prod URL, not localhost)
    - Verbose grep on production bundle confirms requestAnimationFrame count ≤1 (Pitfall #5 single-ticker rule)
    - FAILURE → BLOCKING; Plan 03 does NOT proceed
  </acceptance_criteria>
  <done>VRF-02 5-run median PASS; production bundle single-ticker rule confirmed; Plan 03 unblocked iff PASS.</done>
</task>

<task type="auto" id="62-02-W2a">
  <name>Task W2a: chrome-devtools MCP scroll-test motion contract evidence (VRF-03)</name>
  <wave>2</wave>
  <files>.planning/perf-baselines/v1.8/vrf-03-motion-contract.md</files>
  <read_first>
    - .planning/phases/62-real-device-verification-final-gate/62-RESEARCH.md (§3 Pattern 3 — full MCP tool sequence + single-GSAP-ticker assertion + 6 SIGNAL surface checklist + reduced-motion verification)
    - .planning/phases/62-real-device-verification-final-gate/62-CONTEXT.md (D-06 — prod URL only; never localhost)
    - .planning/codebase/PITFALLS.md (Pitfall #5 single-ticker; Pitfall — SwiftShader localhost fail; Pitfall #7 inlineCss rejection cross-check)
    - .planning/phases/60-lcp-element-repositioning/60-AES03-COHORT.md (proven evidence-MD pattern from Phase 60)
    - components/blocks/entry-section.tsx (T1 pixel-sort + VL-05 // location)
    - components/blocks/thesis-section.tsx (GhostLabel reference)
    - components/animation/page-animations.tsx (single GSAP timeline source)
  </read_first>
  <action>
    Execute the chrome-devtools MCP scroll-test sequence against PROD URL (not localhost — Pitfall #4: SwiftShader headless fails real-device parity assumption). Capture evidence as a structured markdown checklist following the `60-AES03-COHORT.md` pattern.

    STEP 1 — Per-viewport sequence (mobile 360×800, then desktop 1440×900):

    For each viewport:
    ```
    1. mcp__plugin_chrome-devtools-mcp__chrome-devtools__new_page
    2. mcp__plugin_chrome-devtools-mcp__chrome-devtools__navigate_page
       url: "https://signalframeux.vercel.app/"
    3. mcp__plugin_chrome-devtools-mcp__chrome-devtools__resize_page
       width: 360 | 1440, height: 800 | 900
    4. mcp__plugin_chrome-devtools-mcp__chrome-devtools__performance_start_trace
    5. mcp__plugin_chrome-devtools-mcp__chrome-devtools__evaluate_script
       payload (verbatim from RESEARCH.md §Pattern 3):
       (() => {
         const gsap = window.gsap;
         const signalIntensity = getComputedStyle(document.documentElement)
           .getPropertyValue('--sfx-signal-intensity');
         return {
           gsap_present: typeof gsap === 'object',
           gsap_globalTimeline_active: gsap?.globalTimeline?.isActive?.() ?? null,
           signal_intensity_var: signalIntensity?.trim() || null,
           quality_tier: window.__sfQualityTier ?? null
         };
       })()
    6. mcp__plugin_chrome-devtools-mcp__chrome-devtools__scroll
       6 progressive stops at: 0vh (ENTRY), 100vh (THESIS), 200vh (SIGNAL), 300vh (PROOF), 400vh (INVENTORY), 500vh (ACQUISITION)
       At each stop:
       - mcp__plugin_chrome-devtools-mcp__chrome-devtools__take_snapshot
    7. mcp__plugin_chrome-devtools-mcp__chrome-devtools__list_console_messages
       Confirm zero entries with level === "error"
    8. mcp__plugin_chrome-devtools-mcp__chrome-devtools__performance_stop_trace
    9. mcp__plugin_chrome-devtools-mcp__chrome-devtools__performance_analyze_insight
       insight: "Animation Frames"
       → record GSAP ticker as sole rAF source; flag any other rAF call sites
    ```

    STEP 2 — Reduced-motion path (per viewport):
    ```
    10. mcp__plugin_chrome-devtools-mcp__chrome-devtools__emulate
        cssMediaFeatures: [{ name: "prefers-reduced-motion", value: "reduce" }]
    11. mcp__plugin_chrome-devtools-mcp__chrome-devtools__navigate_page
        url: "https://signalframeux.vercel.app/"  (reload to apply emulation)
    12. Repeat steps 5-9
    13. Assert via evaluate_script:
        gsap.globalTimeline.isActive() returns false for ScrollTrigger-derived motion
    ```

    STEP 3 — Render the evidence MD:

    Write `.planning/perf-baselines/v1.8/vrf-03-motion-contract.md` with frontmatter + 4 sections:

    ```markdown
    ---
    phase: 62-real-device-verification-final-gate
    plan: 02
    gate: VRF-03 motion contract
    captured: <ISO date>
    prod_url: https://signalframeux.vercel.app/
    viewports: [mobile-360x800, desktop-1440x900]
    status: PASS
    ---

    # VRF-03 Motion Contract Evidence

    ## §1 Single-GSAP-ticker Assertion

    | Viewport | gsap_present | gsap_globalTimeline_active | rogue rAF count (Animation Frames lane) | Verdict |
    |----------|--------------|----------------------------|-----------------------------------------|---------|
    | mobile-360 | true | true | 0 | single-GSAP-ticker PASS |
    | desktop-1440 | true | true | 0 | single-GSAP-ticker PASS |

    Production bundle grep cross-check (Pitfall #5):
    - main-*.js requestAnimationFrame count: <N from Wave 1 Step 5>
    - Expected: ≤1 (GSAP ticker module itself)
    - Result: <PASS|FAIL>

    ## §2 SIGNAL Surface Render Checklist

    Per RESEARCH.md §3 SIGNAL surface table (6 surfaces × 2 viewports):

    | # | Surface | Scroll | Mobile renders? | Desktop renders? | Verdict |
    |---|---------|--------|-----------------|-------------------|---------|
    | 1 | ENTRY (T1 pixel-sort hero + VL-05 // on desktop) | 0vh | ✓ | ✓ | PASS |
    | 2 | THESIS (T2 nav glyph sticky + GhostLabel) | 100vh | ✓ | ✓ | PASS |
    | 3 | SIGNAL (SignalMesh / GLSLHero canvas) | 200vh | ✓ | ✓ | PASS |
    | 4 | PROOF (FRAME content + T2 nav glyph) | 300vh | ✓ | ✓ | PASS |
    | 5 | INVENTORY (T3 cube-tile box) | 400vh | ✓ | ✓ | PASS |
    | 6 | ACQUISITION (FRAME content + T4 // separator) | 500vh | ✓ | ✓ | PASS |

    Console errors at any stop: 0 (list_console_messages clean)

    ## §3 prefers-reduced-motion: reduce Collapse

    | Viewport | gsap.globalTimeline.isActive() (after reduce) | Pixel-sort static? | Idle grain static? | Mesh gradient static? | Particle field static? | Verdict |
    |----------|------------------------------------------------|--------------------|--------------------|-----------------------|------------------------|---------|
    | mobile-360 | false (collapsed) | ✓ | ✓ | ✓ | ✓ | prefers-reduced-motion PASS |
    | desktop-1440 | false (collapsed) | ✓ | ✓ | ✓ | ✓ | prefers-reduced-motion PASS |

    ## §4 Sign-off

    - [x] **PASS** — single-GSAP-ticker rule satisfied (0 rogue rAF in trace, ≤1 rAF in main bundle); all 6 SIGNAL surfaces render at expected scroll positions on both viewports; prefers-reduced-motion: reduce collapses derived motion in both viewports; zero console errors.

    Pitfall cross-checks:
    - Pitfall #4 (SwiftShader vs Metal/ANGLE): N/A — evidence captured against PROD URL via real Chrome.
    - Pitfall #5 (single-ticker rule): PASS — production bundle and trace both confirm ≤1 rAF source.
    - Pitfall #7 (experimental.inlineCss rejection): not applicable to motion contract; confirmed `next.config.ts` does NOT enable.

    **Signed-off-by:** Claude (chrome-devtools MCP automated evidence)
    **Date:** <ISO date>
    ```

    Commit message: `docs(62-02): VRF-03 motion contract PASS (single-ticker + 6 surfaces + reduced-motion)`
  </action>
  <verify>
    <automated>test -f .planning/perf-baselines/v1.8/vrf-03-motion-contract.md && grep -cE 'PASS|✓' .planning/perf-baselines/v1.8/vrf-03-motion-contract.md | awk '{exit ($1 < 6)}' && grep -q 'single-GSAP-ticker.*PASS' .planning/perf-baselines/v1.8/vrf-03-motion-contract.md && grep -q 'prefers-reduced-motion.*PASS' .planning/perf-baselines/v1.8/vrf-03-motion-contract.md && grep -q 'signalframeux.vercel.app' .planning/perf-baselines/v1.8/vrf-03-motion-contract.md && ! grep -q 'localhost' .planning/perf-baselines/v1.8/vrf-03-motion-contract.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/perf-baselines/v1.8/vrf-03-motion-contract.md` exists
    - `grep -cE 'PASS|✓' .planning/perf-baselines/v1.8/vrf-03-motion-contract.md` returns ≥6 (one per SIGNAL surface)
    - `grep -E 'single-GSAP-ticker.*PASS' …vrf-03-motion-contract.md` matches
    - `grep -E 'prefers-reduced-motion.*PASS' …vrf-03-motion-contract.md` matches
    - `grep -E 'https://signalframeux\.vercel\.app' …vrf-03-motion-contract.md` matches (prod URL evidence per D-06)
    - `grep -E 'localhost' …vrf-03-motion-contract.md` returns NOTHING (Pitfall #4 — never localhost)
    - `grep -E '## §1 Single-GSAP-ticker Assertion' …vrf-03-motion-contract.md` matches
    - `grep -E '## §2 SIGNAL Surface Render Checklist' …vrf-03-motion-contract.md` matches
    - `grep -E '## §3 prefers-reduced-motion' …vrf-03-motion-contract.md` matches
    - `grep -E '## §4 Sign-off' …vrf-03-motion-contract.md` matches
    - `grep -E 'mobile-360|mobile-360x800' …vrf-03-motion-contract.md` matches (mobile viewport documented)
    - `grep -E 'desktop-1440' …vrf-03-motion-contract.md` matches (desktop viewport documented)
    - `grep -E 'ENTRY|THESIS|SIGNAL|PROOF|INVENTORY|ACQUISITION' …vrf-03-motion-contract.md | wc -l` returns ≥6
    - All 6 SIGNAL surfaces marked ✓ for both viewports (12 total checkmarks in §2 table)
    - Frontmatter `status: PASS` present
  </acceptance_criteria>
  <done>VRF-03 motion contract evidence MD committed; single-ticker + 6 surfaces + reduced-motion all PASS against prod URL.</done>
</task>

</tasks>

<verification>
After all 3 tasks land:

```bash
# Wave 0 — wrapper + CIB-04 byte-identity
test -f scripts/launch-gate-vrf02.ts
[ -z "$(git diff --name-only HEAD scripts/launch-gate.ts)" ]
[ -z "$(git diff --name-only HEAD scripts/launch-gate-runner.mjs)" ]

# Wave 1 — VRF-02
jq -e '.verdict == "PASS" and .median.lcp_ms < 1000 and .median.cls == 0 and .median.tti_ms < 1500 and .median.performance == 100 and .median.accessibility == 100 and .median["best-practices"] == 100 and .median.seo == 100 and (.per_run | length) == 5' \
  .planning/perf-baselines/v1.8/vrf-02-launch-gate-runs.json

# Wave 2 — VRF-03
grep -cE 'PASS|✓' .planning/perf-baselines/v1.8/vrf-03-motion-contract.md | awk '{exit ($1 < 6)}'
grep -q 'single-GSAP-ticker.*PASS' .planning/perf-baselines/v1.8/vrf-03-motion-contract.md
grep -q 'prefers-reduced-motion.*PASS' .planning/perf-baselines/v1.8/vrf-03-motion-contract.md
! grep -q 'localhost' .planning/perf-baselines/v1.8/vrf-03-motion-contract.md
```
</verification>

<success_criteria>
- `scripts/launch-gate-vrf02.ts` exists; `scripts/launch-gate.ts` byte-identical to merge-base (CIB-04)
- VRF-02 JSON committed; verdict=PASS; median 100/100 across 4 categories; LCP <1000ms; CLS=0; TTI <1500ms; 5 per_run entries
- VRF-03 MD evidence committed; ≥6 PASS lines; single-ticker assertion green; prefers-reduced-motion collapse confirmed; prod URL only (no localhost)
- Production bundle requestAnimationFrame count ≤1 (Pitfall #5)
- v1.8 prod deploy timestamp captured (Plan 03 24h window anchor)
- Plan 03 unblocked
</success_criteria>

<output>
After completion, create `.planning/phases/62-real-device-verification-final-gate/62-02-SUMMARY.md` per standard template with: per-run lighthouse numbers (5×4 categories), prod-bundle rAF grep result, MCP tool surface verification notes, prod-deploy timestamp for Plan 03 24h anchor.
</output>
