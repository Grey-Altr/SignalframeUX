# Phase 63.1: LCP Fast-Path Remediation - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-27
**Phase:** 63.1-lcp-fast-path-remediation
**Mode:** `--auto` (recommended-default selection per synthesis lock-in)
**Areas discussed:** Plan/PR shape · LCP fast-path strategy · Bundle reduction approach · JS deferral posture · Verification gate · Aesthetic preservation gate

---

## Plan/PR shape

| Option | Description | Selected |
|--------|-------------|----------|
| 1 plan / 1 PR | Fastest turnaround; bisect impossible if any vector regresses | |
| 2 plans / 2 PRs | Measure-then-fix split; conflates the 3 fix vectors | |
| **3 plans / 3 PRs** | **One plan per fix vector (bundle · defer · fast-path); strict-bisect** | **✓** |

**Rationale:** Phase 59 CRT-05 strict-bisect precedent. Synthesis §4 lists 3 distinct vectors → 3 plans. Independent intervention surfaces require independent PRs for clean attribution.

---

## LCP fast-path strategy

| Option | Description | Selected |
|--------|-------------|----------|
| (a) inline-critical-CSS | `experimental.inlineCss: true` — REJECTED via Pitfall #7 (breaks `@layer signalframeux` cascade; vercel/next.js#47585) | |
| (c) `font-display: optional` for JetBrains Mono on wordmark | Currently `swap`; tightening drops glyph if font misses 100ms — degrades aesthetic-of-record | |
| **(b) SVG static-asset preload + (d) decouple wordmark from React hydration** | **Static SSR carve-out OR preloaded `.svg` asset; addresses actual bottleneck (JS parser readiness gating SVG `<text>` paint per synthesis §2)** | **✓** |

**Rationale:** Synthesis §2 ¶3 identifies JS hydration as the gating factor, not font load. (a) is structurally banned by Pitfall #7. (c) trades aesthetic for perf — violates LOCKDOWN AES-02. (b)+(d) attacks the actual cost.

---

## Bundle reduction approach

| Option | Description | Selected |
|--------|-------------|----------|
| Dep swap | New runtime npm dependencies — REJECTED via REQUIREMENTS.md §Constraints (no new runtime deps in v1.8) | |
| Single-vector tree-shake | `next.config.ts` `optimizePackageImports` extension only; insufficient delta (730 KB → 200 KB needs 3.7× reduction) | |
| **Aggressive route-level code-splitting + tree-shake animation libs** | **GSAP / Lenis / ScrollTrigger lazy via dynamic imports; preserves Phase 61's 4 v1.7 named chunk IDs** | **✓** |

**Rationale:** Phase 61's chunk-ID preservation requirement (`feedback_lockin_before_execute.md`, STATE.md). Code-split is structural — moves non-LCP-critical JS off the initial chunk without touching named-chunk attribution.

---

## JS deferral posture

| Option | Description | Selected |
|--------|-------------|----------|
| `defer`/`async` everything | Naive blanket defer — would break `/sf-canvas-sync.js` (Pitfalls #2, #8) and second-rAF hydration paths (Pitfall #5) | |
| `next/script strategy="beforeInteractive"` | Also rejected for `/sf-canvas-sync.js` per Pitfall #2 | |
| **Dynamic imports + `requestIdleCallback` for animation hydration** | **Selective; preserves all 4 anti-pattern guards (Pitfalls #2/#5/#7/#8); single-ticker rule intact** | **✓** |

**Rationale:** All 4 active pitfalls (#2/#5/#7/#8) constrain the option set heavily. Dynamic imports for components + idle-callback for animation init is the only path that holds them all.

---

## Verification gate

| Option | Description | Selected |
|--------|-------------|----------|
| Synthetic-only (LHCI) | Phase 60 measured 810 ms LHCI but real LCP was 2393 ms — Pitfall #10 says synthetic-only is insufficient | |
| Single-profile WPT spot-check | Insufficient coverage; cross-network attribution lost (synthesis §1 device-vs-network finding) | |
| **Re-run vrf-01-* WPT scripts on same 3 profiles, all 3 LCP <1000 ms; LHCI median-of-5 as intermediate per-PR gate; Pitfall #10 ratio re-check on close-out** | **Original VRF-01 success criterion; same axes the synthesis measured against** | **✓** |

**Rationale:** Closes the loop on the exact failure the synthesis documented. LHCI as intermediate keeps per-PR feedback fast (Phase 58 CIB-02 already enforces).

---

## Aesthetic preservation gate

| Option | Description | Selected |
|--------|-------------|----------|
| Self-attestation | Insufficient — Phase 60 D-08 precedent requires both automated diff and human cohort review | |
| Automated diff only | Misses qualitative perception drift; trademark primitive risk | |
| **Visual baseline diff <0.5% per page (Phase 60 LCP-03 precedent) + chrome-devtools MCP scroll-test cohort review (Phase 60 D-08 precedent) + AESTHETIC-OF-RECORD §13 trademark primitive check** | **Triple-gated; matches Phase 60's shipped close-out posture** | **✓** |

**Rationale:** Wordmark glyph is one of the 3 trademark primitives (`feedback_trademark_primitives.md`); decoupling it from React hydration MUST preserve glyph rendering pixel-for-pixel. Triple gate catches font-metric drift, layout shift, and qualitative perception issues.

---

## Claude's Discretion

- Exact SSR mechanism for wordmark decoupling (Plan 03 RESEARCH.md decides — Server Component carve-out vs static `.svg` asset emission vs pre-hydration inline `<svg>` in layout `<body>`).
- Tree-shake granularity for GSAP plugins (Plan 01 RESEARCH.md).
- `requestIdleCallback` polyfill posture for Safari (Plan 02 RESEARCH.md).
- Intermediate LHCI gate environment (preview deployment vs local prod-build; default = preview).

## Deferred Ideas

- Galaxy A14 native re-measurement (Catchpoint Pro tier).
- n=5 statistical re-measurement (Pro tier).
- Field RUM 24h p75 LCP verification (Phase 65 territory).
- Desktop-only LCP repositioning (Phase 60 closed; cross-viewport LCP stable per synthesis §3).
- Petrol/steel-blue color swatch (memory `project_future_color_petrol_blue.md`; unrelated to perf scope).
- Pixel-sort transitions experiment (`exp/pixel-sort-transitions` branch; unrelated to perf scope).
