---
phase: 63.1
slug: lcp-fast-path-remediation
status: approved
nyquist_compliant: true
wave_0_complete: false
created: 2026-04-27
updated: 2026-04-28
---

# Phase 63.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Source: `63.1-RESEARCH.md` §Validation Architecture (Plan 03 hoist + Plan 01 bundle + Plan 02 idle hydration).

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Playwright 1.55 (existing); LHCI 0.15 (existing); Catchpoint WPT (Path B) |
| **Config file** | `playwright.config.ts`, `.lighthouseci/lighthouserc.json`, `~/.wpt-api-key` |
| **Quick run command** | `pnpm test:smoke` (Playwright smoke set) |
| **Full suite command** | `pnpm test && pnpm lhci:run && pnpm wpt:vrf-01` |
| **Estimated runtime** | Smoke ~45s · Full ~6m (LHCI median-of-5 = ~4m of that) |

---

## Sampling Rate

- **After every task commit:** Run `pnpm test:smoke` (smoke + tsc --noEmit + lint).
- **After every plan wave:** Run `pnpm lhci:run` (LHCI median-of-5 against preview deployment) + relevant Playwright spec subset.
- **Before `/pde:verify-work`:** Full suite must be green AND `pnpm wpt:vrf-01-post-63.1` (re-run vrf-01-* WPT scripts on all 3 profiles, all LCP <1000 ms) must pass.
- **Max feedback latency:** 45s for smoke; 4m for LHCI; 8–15m for WPT (3 profiles × ~3–5m each).

---

## Per-Task Verification Map

> Filled by planner during PLAN generation. Each PLAN.md task gets one row here.
> Plan IDs follow `63.1-{plan_num}-{task_num}` convention.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 63.1-01-00 | 01 | 1 (W0) | LCP-01 (de facto) | none — render strategy refactor | chunk-ID lock + budget gate + 4 pitfall guards (#2/#5/#7/#8) created | unit + grep | `pnpm build && node scripts/check-chunks.mjs && pnpm playwright test tests/v1.8-phase63-1-bundle-budget.spec.ts tests/v1.8-phase63-1-pitfall-guards.spec.ts` | ❌ creates 3 files | ⬜ pending |
| 63.1-01-01 | 01 | 1 | LCP-01 (de facto) | none | optimizePackageImports extended; D-05 (no new deps); chunk IDs preserved | bundle / build | `rm -rf .next/cache .next && ANALYZE=true pnpm build && node scripts/check-chunks.mjs && pnpm playwright test tests/v1.8-phase63-1-pitfall-guards.spec.ts` | ✅ Wave 0 lands first | ⬜ pending |
| 63.1-01-02 | 01 | 1 | LCP-01 (de facto) | none | below-fold sections via next/dynamic ssr:true; EntrySection static; <200KB First Load JS | bundle / SSR / build | `rm -rf .next/cache .next && ANALYZE=true pnpm build && node scripts/check-chunks.mjs && pnpm playwright test tests/v1.8-phase63-1-bundle-budget.spec.ts tests/v1.8-phase63-1-pitfall-guards.spec.ts` | ✅ Wave 0 + Task 1 land first | ⬜ pending |
| 63.1-02-00 | 02 | 2 (W0) | LCP-01 (de facto) | none | rIC pattern fidelity gate + no-polyfill (D-05) gate created | unit + grep | `pnpm playwright test tests/v1.8-phase63-1-defer-budget.spec.ts` | ❌ creates 1 file | ⬜ pending |
| 63.1-02-01 | 02 | 2 | LCP-01 (de facto) | none | useGSAP callback bodies wrapped in CRT-04 rIC fallback; useGSAP hook stays sync; single-ticker preserved | unit / integration / visual | `pnpm tsc --noEmit && rm -rf .next/cache .next && pnpm build && pnpm playwright test tests/v1.8-phase63-1-defer-budget.spec.ts tests/v1.8-phase63-1-pitfall-guards.spec.ts tests/v1.8-phase63-1-bundle-budget.spec.ts` | ✅ Wave 0 + Plan 01 land first | ⬜ pending |
| 63.1-03-00 | 03 | 3 (W0) | LCP-01 (de facto) | none | wordmark fidelity baseline (D-12) + WPT re-run runner + Pitfall #10 ratio gate created | visual + script + ratio | `pnpm build && pnpm playwright test tests/v1.8-phase63-1-wordmark-hoist.spec.ts --update-snapshots && pnpm playwright test tests/v1.8-phase63-1-pitfall-10.spec.ts` | ❌ creates 3 files (+ baseline screenshots) | ⬜ pending |
| 63.1-03-01 | 03 | 3 | LCP-01 (de facto) | none | CdCornerPanel hoisted to direct <body> child (single mount); component file unchanged; D-12 fidelity preserved | visual / build / LHCI | `pnpm tsc --noEmit && rm -rf .next/cache .next && pnpm build && pnpm playwright test tests/v1.8-phase63-1-wordmark-hoist.spec.ts tests/v1.8-phase63-1-pitfall-guards.spec.ts tests/v1.8-phase63-1-defer-budget.spec.ts tests/v1.8-phase63-1-bundle-budget.spec.ts` | ✅ all priors land first | ⬜ pending |
| 63.1-03-02 | 03 | 3 | LCP-01 (de facto) | none | 3-profile WPT re-run + D-09 ratio gate + D-11 cohort sign-off; D-07 success criterion (all 3 LCP <1000 ms) | RUM / ratio / cohort | `node scripts/wpt-rerun.mjs && pnpm playwright test tests/v1.8-phase63-1-pitfall-10.spec.ts && test -f .planning/phases/63.1-lcp-fast-path-remediation/63.1-COHORT.md` | ✅ all priors + Catchpoint Starter Path B GUI | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

*Note: phase_req_ids is null for Phase 63.1 (decimal insertion); validation maps to LCP-01 by virtue of the synthesis-defined success criterion (real-device LCP <1000 ms on all 3 profiles).*

*Threat model: every task is "no new attack surface" — pure render-strategy refactor (config flag, import-style, JSX reorder), no auth/data flows, no new deps. ASVS L1 default met. See each PLAN.md `<threat_model>` block.*

---

## Wave 0 Requirements

- [ ] `scripts/check-chunks.mjs` (Plan 01 Task 0) — verifies the 4 v1.7 named chunk IDs (3302, e9a6067a, 74c6194b, 7525) are still emitted post-Plan-01. Grep against `.next/static/chunks/` after `ANALYZE=true pnpm build`.
- [ ] `tests/v1.8-phase63-1-bundle-budget.spec.ts` (Plan 01 Task 0) — asserts `app/page` initial JS chunk weight <200 KB after `pnpm build`; reads `.next/build-manifest.json`.
- [ ] `tests/v1.8-phase63-1-pitfall-guards.spec.ts` (Plan 01 Task 0 — cross-plan; lands earliest, used by Plan 02 + Plan 03) — grep-based static gates:
  - `/sf-canvas-sync.js` is inline IIFE (not `<script src=...>`, no `defer`/`async`/`strategy=`) [Pitfalls #2, #8]
  - No second rAF call site introduced (`grep -rn "requestAnimationFrame" components/ lib/` matches expected baseline) [Pitfall #5]
  - `experimental.inlineCss: true` NOT in `next.config.ts` [Pitfall #7]
  - `@layer signalframeux` cascade preserved (`grep "@layer signalframeux" app/globals.css`) [Pitfall #7 corollary]
- [ ] `tests/v1.8-phase63-1-defer-budget.spec.ts` (Plan 02 Task 0) — rIC pattern fidelity across 4 section files + no-polyfill (D-05) + single-ticker delta + reduced-motion-precedes-rIC ordering.
- [ ] `tests/v1.8-phase63-1-wordmark-hoist.spec.ts` (Plan 03 Task 0) — Playwright visual diff of wordmark glyph against `.planning/visual-baselines/v1.8-start/` (4 viewports × homepage); fail if any pixel-fidelity drift on the trademark primitive (D-12).
- [ ] `scripts/wpt-rerun.mjs` (Plan 03 Task 0) — re-runs vrf-01-* Catchpoint scripts on the 3 canonical profiles (iPhone 14 Pro / Moto G Power 3G / Moto G Stylus 4G) and asserts all LCP <1000 ms; emits to `.planning/perf-baselines/v1.8/vrf-01-*-post-63.1.json`.
- [ ] `tests/v1.8-phase63-1-pitfall-10.spec.ts` (Plan 03 Task 0) — synthesis pass: parses post-fix vrf-01-* JSON files + phase-60-mobile-lhci.json + vrf-02-launch-gate-runs.json; computes ratios; asserts <1.3 LCP and <1.5 TTI (D-09 close-out automation).

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Wordmark glyph aesthetic preservation | D-12 (trademark primitive) | Pixel-diff catches metric drift but not perceptual drift; cohort review is the ground-truth gate | Run chrome-devtools MCP scroll-test against post-Plan-03 preview URL; compare side-by-side with `.planning/visual-baselines/v1.8-start/`; sign off in `63.1-COHORT.md` (Phase 60 D-08 precedent) |
| Pitfall #10 ratio re-check | D-09 | LHCI returns synthetic; WPT returns real; ratio must be hand-computed and asserted post-fix. Spec at `tests/v1.8-phase63-1-pitfall-10.spec.ts` automates the assertion once JSONs are present, but the JSON capture is GUI-driven (Catchpoint Starter Path B per Phase 63 _path_a_decision). | Run `node scripts/wpt-rerun.mjs` with `PROD_URL`; for each of 3 profiles, submit n=3 runs via Catchpoint GUI; download JSON to expected path; spec then asserts ratios. Document at `63.1-COHORT.md` §2. |
| Path-X decision documentation | (conditional) | If measurement forces gate loosening (e.g. one profile lands at 1050 ms but design tradeoff is justified), document via `_path_X_decision` block | Phase 60 path_a / Phase 62 path_b precedent (`feedback_path_b_pattern.md`); annotate in 63.1-COHORT.md frontmatter `status: path_X_decision`. Candidate C (pre-hydration inline-SVG injection mirroring themeScript/scaleScript/canvasSyncScript inline-literal pattern) is the documented escalation surface. |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies (per-task map filled 2026-04-28)
- [x] Sampling continuity: no 3 consecutive tasks without automated verify (every task has automated gate)
- [x] Wave 0 covers all MISSING references (chunk-check script, bundle-budget spec, pitfall-guards spec, defer-budget spec, wordmark-hoist spec, WPT re-run script, pitfall-10 ratio spec — 7 total Wave 0 artifacts across the 3 plans)
- [x] No watch-mode flags (LHCI uses median-of-5 not `--watch`; WPT is one-shot)
- [x] Feedback latency < 45s for per-task; <8m for per-wave (acceptable given Phase 58 CIB-02 thresholds)
- [x] `nyquist_compliant: true` set in frontmatter (per-task map filled by planner)

**Approval:** approved 2026-04-28 — planner completed Per-Task map (8 task rows across 3 plans) and Wave 0 stubs documented (7 artifacts). Ready for `/pde:execute-phase 63.1`.
