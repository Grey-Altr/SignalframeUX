---
phase: 64
plan: 03
task: 0
captured: 2026-04-28T20:29:49Z
branch: chore/v1.7-ratification
base: main
merge_base: 0ff2e38794ec56b244b07b4dc85705324b304d20
total_pending_commits: 203
amendment_aware: true  # honors 64-03-PLAN.md frontmatter amendment_2026_04_28 (PR #1 carries lighthouse.yml + lighthouserc + LHCI devDep to main, ungated)
---

# Phase 64 Plan 03 — Cherry-Pick Audit

> Generated by Task 0 to partition `git log main..chore/v1.7-ratification --oneline`
> into the three CRT-05 ship cohorts plus a Defer cohort.
> User MUST approve this partition before any cherry-pick runs (T-64-03 mitigation).

## Total commits pending: 203

`main` is anchored at `0ff2e38` ("Docs(phase-47): complete phase execution") — i.e. the
v1.7 Phase 47 ratification close. `chore/v1.7-ratification` carries v1.7 Phases 48–56,
v1.7 milestone close-out, AND all of v1.8 (Phases 57–64 including 63.1) on top of that.

The total pending span is therefore much larger than CRT-05 alone. This audit's job is
to identify ONLY the three CRT-05 cohorts that get cherry-picked into PRs 1/2/3, and
to put EVERYTHING ELSE in the Defer cohort (those are commits that are either (a)
already shipped through other channels, (b) routed to v1.9, or (c) require a separate
ship strategy outside of CRT-05).

---

## §1 Recommendation summary

The three CRT-05 ship cohorts are tightly defined and small:

| Cohort | Commits | Span | Purpose |
| ------ | ------- | ---- | ------- |
| **PR #1 (ship/59-01)** | 7 | `e527ace`..`ef7169f` (with 1 cohort gap — see §2) | CRT-01 canvas-sync inline + LHCI gate infrastructure (lighthouse.yml + lighthouserc + @lhci/cli devDep) |
| **PR #2 (ship/59-02)** | 11 | `5fc28b6`..`47fe585` | CRT-02 + CRT-03 — Anton subset + font-display swap with measured descriptors + AES-02 ratification |
| **PR #3 (ship/59-03)** | 8 | `654cf9e`..`52e1b96` (4 from Phase 59 + 4 from Phase 64-02) | CRT-04 Lenis rIC + Phase 64 Plan 02 pitfall-10 recalibration |
| **Defer**              | 177 | (everything else)          | v1.7 ratification close-out, Phase 57/58-Phase-58-bulk/60/61/62/63/63.1/64-research-only/etc — ships via separate channels per §6 |

**Plan 02 placement: Decision A (rides PR #3)** — since the recalibration commits sit on
the branch immediately after the Phase 63.1 close-out and are tightly coupled to the
final merge (they make the pitfall-10 spec PASS on main, mitigating Anti-Pattern #7).
Cherry-pick alongside CRT-04 in PR #3.

---

## §2 Cohort 1 — ship/59-01 (CRT-01 canvas-sync inline + LHCI gate-bringing)

**Head per amendment:** PR #1 carries `lighthouse.yml` + `.lighthouseci/lighthouserc.{json,desktop.json}`
+ `@lhci/cli@^0.15.1` devDep TO main (ungated; activates the gate for PR #2 and PR #3).

**Cherry-pick commits in order (oldest first, 7 commits):**

| # | SHA | Subject | Why in 59-01 |
| - | --- | ------- | ------------ |
| 1 | `e527ace` | Chore(58-01): install @lhci/cli@^0.15.1 as devDep | LHCI gate prereq — @lhci/cli must exist before lighthouse.yml runs |
| 2 | `36b239f` | Feat(58-01): add lighthouserc configs (mobile primary + desktop variant) | LHCI gate prereq — assertion config |
| 3 | `6167c31` | Feat(58-02): add .github/workflows/lighthouse.yml — LHCI deployment_status gate | THE gate-bringing file (registers `audit` workflow on main upon merge) |
| 4 | `235d0f0` | test(59): RED — canvas-sync inline integration spec | CRT-01 RED spec |
| 5 | `fefeda2` | test(59): GREEN — Phase 59 pixel-diff harness (Plan A target = AES-04) | CRT-01 pixel-diff harness |
| 6 | `66ac4ec` | feat(59-01): CRT-01 — inline canvas-height-sync IIFE into app/layout.tsx body tail; delete public/sf-canvas-sync.js | The CRT-01 deliverable per 59-VERIFICATION.md human_verification #1 |
| 7 | `ef7169f` | docs(59-01): complete CRT-01 canvas-sync-inline plan — SUMMARY | CRT-01 plan SUMMARY |

**Net surface:**
- Adds: `.github/workflows/lighthouse.yml`, `.lighthouseci/lighthouserc.json`, `.lighthouseci/lighthouserc.desktop.json`, `package.json` LHCI devDep entry, RED+GREEN pixel-diff specs, inline canvasSyncScript IIFE in `app/layout.tsx` body tail, ScaleCanvas no-script-mount.
- Deletes: `public/sf-canvas-sync.js`.

**LHCI gate posture:** PR #1 is the ONLY PR allowed to merge ungated (per amendment —
the gate doesn't yet exist on main; this PR brings it). PR #2 and PR #3 retain full
gate-pass requirements once the gate registers post-PR-1-merge.

**Risks for Cohort 1:** Lowest-risk PR per Phase 64 RESEARCH §Risks Risk 1 (CRT-01
removes a render-blocking external script — perf-positive by construction). Cherry-pick
should be conflict-free since these commits are early in the branch sequence and only
touch files that haven't been further-modified in the higher-numbered cohorts.

**Skipped from this cohort (intentionally — these are Phase 58 RUM scaffolding, NOT
gate infrastructure):** `46dae75`, `f034969`, `b0d7a51`, `b0d913` (sic — actually
`b0d913` is `b0d913` truncated — confirmed below)... wait, let me re-list from the
git log:

| Skipped SHA | Subject | Why deferred |
| ----------- | ------- | ------------ |
| `46dae75` | Feat(58-02): WebVitals client component + Playwright contract spec | RUM telemetry — orthogonal to CRT-05; defers (route via separate PR cluster or v1.9) |
| `f034969` | Feat(58-02): app/api/vitals/route.ts — Node-runtime RUM sink | RUM telemetry — same |
| `b0dd913` | Feat(58-02): mount <WebVitals /> in app/layout.tsx (CIB-05) | RUM telemetry — same |
| `9ea4a6d` | Test(58-02): pixel-diff spec + web-vitals contract spec hardening | Test-only on RUM features |
| `7ee6770` | Test(58-02): LCP-identity guard + CIB-04 launch-gate-untouched specs | LCP-identity test — known to FAIL post-Plan-B per 59-VERIFICATION known_regressions; defer to RUM/v1.9 |
| `9b55c77` | docs(58-01): SUMMARY — Plan 01 complete | Phase 58 plan-level docs (already represented in main via Phase 58 traceability) |
| `0f1b552`, `5758888`, `7a445d5`, `962de55`, `fb10f6f`, `52343d4`, `c9937c8` | Phase 58 docs/SUMMARY/STATE/HUMAN-UAT/CR | Phase 58 plan-level docs — defer or route through Defer cohort |
| `9715648`, `781175e` | Phase 58 plan + research seeds | Defer |

**Rationale for skipping Phase 58 RUM commits:** The amendment authorizes PR #1 to
carry the LHCI gate-bringing files (lighthouse.yml + lighthouserc + LHCI devDep). It
does NOT authorize PR #1 to also carry RUM telemetry, LCP-identity guards, or Phase 58
docs. Those are independent surfaces. Deferring them keeps PR #1's bisect surface
narrow (CRT-01 + LHCI plumbing) and reduces review burden.

If user disagrees with this scope (e.g., wants the entire Phase 58 cohort to ride PR #1
to fully reflect chore/v1.7-ratification on main in a single ship), see §6 for the
Defer cohort handling.

---

## §3 Cohort 2 — ship/59-02 (CRT-02 + CRT-03 Anton subset + swap)

**Range per 59-VERIFICATION.md human_verification #4:** `7334af0..ef9556c` (10 commits)
plus Plan B's RED specs (`5fc28b6`, `e2bd273`) plus Plan B's final SUMMARY (`47fe585`).

**Cherry-pick commits in order (oldest first, 11 commits):**

| # | SHA | Subject | Why in 59-02 |
| - | --- | ------- | ------------ |
| 1 | `5fc28b6` | test(59): RED — Anton subset coverage spec (CRT-02 / Plan B) | Plan B RED spec |
| 2 | `e2bd273` | test(59): CLS slow-3G spec + swap-event guard (CRT-03 / Plan B) | Plan B RED spec |
| 3 | `7334af0` | feat(59-02): CRT-02 — subset Anton.woff2 to full printable ASCII + TM (58.8 KB → 11.1 KB, 81% reduction) | CRT-02 deliverable |
| 4 | `8293059` | chore(59-02): measure Anton descriptors against Impact fallback (size-adjust 92.14%, ascent 127.66%, descent 35.72%) | CRT-03 measurement step |
| 5 | `4e82e77` | chore(59-02): preserve v1.8-start baselines as v1.8-pre-anton-swap (forensic backup for AES-02 exception) | AES-02 forensic backup |
| 6 | `2503f9a` | feat(59-02): CRT-03 — Anton font-display optional → swap with measured descriptors | CRT-03 deliverable |
| 7 | `adcb291` | chore(59-02): re-baseline 20 v1.8-start PNGs (AES-02 documented exception); create AES-02 exception audit log | AES-02 re-baseline |
| 8 | `b44512e` | docs(59-02): partial SUMMARY — Tasks 1-6 complete, awaiting cohort-review checkpoint for Task 7 | Plan B partial SUMMARY |
| 9 | `ce76422` | chore(59-02): cohort acceptance audit row — 8 surfaces accepted, rejected: none (2026-04-26) | AES-02 cohort acceptance row |
| 10 | `ef9556c` | docs(59-02): AESTHETIC-OF-RECORD.md Change Log — ratify Anton swap as AES-02 exception | AESTHETIC-OF-RECORD ratification |
| 11 | `47fe585` | docs(59-02): finalize SUMMARY — Plan B complete (Tasks 0-7, AES-02 exception ratified) | Plan B final SUMMARY |

**Net surface:**
- `app/fonts/Anton-Regular.woff2` resubsetted (58.8 KB → 11.1 KB)
- `app/layout.tsx` Anton `localFont` → `display: "swap"` with 4 MEASURED descriptors
- `.planning/visual-baselines/v1.8-pre-anton-swap/` (20 forensic PNGs, new dir)
- `.planning/visual-baselines/v1.8-start/` (20 re-baselined PNGs, content updated)
- `.planning/codebase/AESTHETIC-OF-RECORD.md` Change Log line 160 (AES-02 ratification entry)
- `.planning/phases/59-critical-path-restructure/59-AES02-EXCEPTION.md` cohort acceptance audit log
- 2 new test specs (subset coverage, anton-swap-cls slow-3G)
- `scripts/measure-anton-descriptors.mjs` + `package.json` opentype.js@^1.3.4 devDep (added in Plan B Task 4)

**LHCI gate posture:** PR #2 is the FIRST gated PR. The audit check from PR #1's
merged lighthouse.yml will fire on PR #2's Vercel preview and post the `audit` check
on PR #2's Checks tab. Plan 01 Task 3 (add `audit` to required checks) executes in
parallel with PR #2's review per the amended sequence.

**Risks for Cohort 2:** Highest-risk PR per Phase 64 RESEARCH §Risks Risk 2 (Anton
swap shifts FCP/LCP timing; AES-02 exception ratification surface). LCP may shift up;
predicted CLS ≤0.005 (path_a). If LHCI fails on LCP > 1000ms attributable to Anton
physics (NOT accidental regression), document via `_path_c_decision` per Phase 60 / 62
precedent.

**Skipped from this cohort:** `8293059` (descriptor measurement chore — INCLUDED above
since it's prerequisite to `2503f9a`). No skips — all 11 commits ride PR #2.

---

## §4 Cohort 3 — ship/59-03 (CRT-04 Lenis rIC + Phase 64 Plan 02 pitfall-10 recalibration)

**Range:** `654cf9e`, `8eee6f6`, `fc3827c`, `5aed97d` (Phase 59 Plan C — 4 commits)
plus `8827c43`, `2809e93`, `5063752`, `52e1b96` (Phase 64 Plan 02 — 4 commits).

**Cherry-pick commits in order (oldest first, 8 commits):**

| # | SHA | Subject | Why in 59-03 |
| - | --- | ------- | ------------ |
| 1 | `654cf9e` | test(59): RED — Lenis rIC deferral spec | Plan C RED spec |
| 2 | `8eee6f6` | feat(59-03): CRT-04 — defer Lenis init via requestIdleCallback (timeout 100ms); preserve PF-04 autoResize: true contract | CRT-04 deliverable |
| 3 | `fc3827c` | docs(59-03): complete CRT-04 plan — SUMMARY + ROADMAP tick | Plan C SUMMARY |
| 4 | `5aed97d` | docs(59): add code review report — 0 critical / 4 warning / 5 info | Phase 59 code review |
| 5 | `8827c43` | test(64-02): recalibrate Pitfall #10 LCP gate to prod-URL anchor | Plan 02 recalibration code (SYNTHETIC_LCP_MS 810→657, LCP_RATIO_MAX 1.3→3.5) |
| 6 | `2809e93` | test(64-02): make Pitfall #10 spec PASS — 3G Fast skip + TTI deferral | Plan 02 recalibration finalization |
| 7 | `5063752` | docs(64-02): complete pitfall-10-recalibration plan | Plan 02 SUMMARY |
| 8 | `52e1b96` | docs(64): merge 64-02 worktree + roadmap progress 1/3 | Plan 02 STATE/ROADMAP sync |

**Net surface:**
- `components/layout/lenis-provider.tsx` rIC-wrapped init (autoResize: true preserved)
- `tests/v1.8-phase63-1-pitfall-10.spec.ts` SYNTHETIC_LCP_MS 810→657 + LCP_RATIO_MAX 1.3→3.5 + 3G-Fast skip + TTI deferral + `_path_c_decision` block
- `tests/v1.8-phase59-lenis-ric.spec.ts` (Plan C spec)
- 2 SUMMARY docs (59-03, 64-02)
- 1 code-review doc (59-REVIEW)

**LHCI gate posture:** PR #3 is fully gated by the now-required `audit` check (Plan 01
Task 3 lands `audit` to required-status-checks during PR #2's review).

**Risks for Cohort 3:** Anti-Pattern #7 mitigated — pitfall-10.spec.ts MUST PASS on this
branch before push (verification step in Plan 03 Task 3). The 3G Fast skip filter is
expected in `2809e93` per `feedback_path_b_pattern.md` precedent.

**Plan 02 placement decision:** **Decision A — RIDE PR #3.** Rationale:
- Plan 02 commits sit immediately after the 63.1 close-out on the branch.
- Plan 02 makes pitfall-10 spec PASS — Anti-Pattern #7 requires this to land BEFORE
  any merge-to-main that would otherwise leave a FAILing spec on main breaking ci.yml.
  Riding PR #3 keeps the recalibration paired with its consumer (CRT-04 surface).
- Standalone Plan 02 PR adds one more PR cycle (LHCI run + merge button) without bisect
  benefit (pitfall-10 recalibration is doc/test only, no application code).

**Skipped from this cohort:** None for the Phase 59 + Phase 64-02 surface.

---

## §5 Defer cohort (NOT in any 59-XX PR)

The remaining 177 commits are NOT in any of the three CRT-05 ship cohorts. They split
across these subgroups:

### §5a v1.7 milestone close-out (Phases 48-56) — 64 commits

| Span | Subject pattern | Routing |
| ---- | --------------- | ------- |
| `b0d7a51`..`f57b97b` (commits across Phases 48, 49, 50, 50.1, 51, 52, 53, 54, 55, 56) | "Docs(NN-01): summary/ratify/fix stale plan ref" | These are v1.7 ratification close-out commits. They predate v1.8 entirely and do not affect CRT-05's bisect surface. **Routing: defer to a separate "v1.7-rat" PR after CRT-05 closes**, OR accept as orphan if user prefers `chore/v1.7-ratification` to remain partially-merged. |
| `508975a`, `f57b97b`, `371377d`, `5aa3a2a`, `0927d7d`, `df3ee3b` | v1.7 close handoff + monitoring + dead-derive cleanup | Same — v1.7 close-out, defer or accept. |

### §5b v1.8 milestone init + planning — 9 commits

| Span | Subject pattern | Routing |
| ---- | --------------- | ------- |
| `4f8c9a7`..`c91e3eb` (5 commits: research synthesis × 4 + roadmap) | v1.8 milestone seed + research | These are planning artifacts — they document v1.8 but don't add code. Defer; route through a separate "v1.8-plan" PR or accept as orphan. |

### §5c Phase 57 (diagnosis + AES) — 11 commits

| Span | Subject pattern | Routing |
| ---- | --------------- | ------- |
| `a2dfafd`..`326c262` (Phase 57 plans, AES-01..04 lock-in, DGN-01..03 baseline capture) | "Feat(57-NN): ..." + "docs(57-NN): SUMMARY" | These ship `AESTHETIC-OF-RECORD.md` (the AES-01..04 lock-in foundation) + 20 baseline PNGs + the v1.8-lcp-diagnosis.md. Defer; route through a separate "Phase 57 ship PR" OR accept as orphan if `AESTHETIC-OF-RECORD.md` and baseline PNGs are not required on main for any external consumer. |

### §5d Phase 58 RUM telemetry (NOT the LHCI gate — that's in Cohort 1) — 11 commits

| SHA | Subject | Routing |
| --- | ------- | ------- |
| `46dae75`, `f034969`, `b0dd913`, `9ea4a6d`, `7ee6770`, `0f1b552`, `5758888`, `7a445d5`, `962de55`, `fb10f6f`, `52343d4`, `c9937c8`, `9b55c77` | Phase 58 RUM client + API route + WebVitals mount + LCP-identity test + Phase 58 SUMMARY/STATE/HUMAN-UAT/CR docs | RUM telemetry is an orthogonal surface to CRT-05. **Routing: defer to "Phase 58 RUM ship PR" after CRT-05 closes**, OR accept as orphan. The LCP-identity guard in `7ee6770` is known to FAIL post-Plan-B per 59-VERIFICATION known_regressions — needs Phase 60 LCP-02 re-calibration before merge. |

### §5e Phase 59 plan-level docs (not Plan 01/02/03 cohorts) — 6 commits

| SHA | Subject | Routing |
| --- | ------- | ------- |
| `cea63e5`, `501e8f2`, `bb20693`, `2a58a0f`, `3528e18`, `2227d28` | Phase 59 research/plan/STATE/HUMAN-UAT/PROJECT.md sync | Phase-level docs that surround the three plan cohorts. They reference work shipped via Cohorts 1/2/3 above but are themselves separable. **Routing: ride a separate "Phase 59 docs" PR after PR #3 merges**, OR accept as orphan. |

### §5f Phase 60 (LCP element repositioning) — ~16 commits

| Span | Subject pattern | Routing |
| ---- | --------------- | ------- |
| `cf297ba`..`014083c` | Phase 60 LCP-02 plans, GhostLabel content-visibility intervention, AES-04 pixel-diff, LHCI CLS threshold loosen | Independent ship surface. Defer to "Phase 60 ship PR" after CRT-05 closes. |

### §5g Phase 61 (bundle hygiene) — ~22 commits

| Span | Subject pattern | Routing |
| ---- | --------------- | ------- |
| `cf2f793`..`0343dc2` | Phase 61 plans, optimizePackageImports cohort, AES-04 spec recalibration, Phase 59 errata | Independent ship surface. Defer to "Phase 61 ship PR" after CRT-05 closes. |

### §5h Phase 62 (real-device verification + final gate) — ~17 commits

| Span | Subject pattern | Routing |
| ---- | --------------- | ------- |
| `1b41471`..`996e33b` | Phase 62 plans, VRF-01..05 surfaces, launch-gate-vrf02 wrapper, RUM-seed runner, AES-04 verdict | Independent ship surface. Defer to "Phase 62 ship PR" after CRT-05 closes. |

### §5i Phase 63 + 63.1 (LCP fast-path remediation) — ~22 commits

| Span | Subject pattern | Routing |
| ---- | --------------- | ------- |
| `0d44733`..`c475188` | Phase 63 WPT synthesis, Phase 63.1 plans 01/02/03, CdCornerPanel hoist, Path A wordmark vectorization | **CRITICAL — these MUST NOT be cherry-picked into 59-XX cohorts (T-64-03 mitigation).** They are the Phase 63.1 carry-over surface. Per `63.1-COHORT.md §6 _path_b_decision`, these are deferred to v1.9 (3G Fast + framework chunk + iPhone variance). The Path A wordmark vectorization (`34d8d4c`) and CdCornerPanel hoist (`7458de9`) are real shipped code that affects production — they belong in a "Phase 63.1 ship PR" routed independently of CRT-05. **Defer to a Phase 63.1 ship PR after CRT-05 closes.** |

### §5j Phase 64 docs/research/plan (NOT Plan 02 — that's in Cohort 3) — 7 commits

| SHA | Subject | Routing |
| --- | ------- | ------- |
| `0d44733`, `774a1ea`, `215e5bb`, `64841c2`, `8ea11d5`, `bef432a`, `fb9b83d` | Phase 64 research, plans 01/02/03, ROADMAP expansions, amendments, v1.8-MILESTONE-AUDIT revisions | Phase-level docs for Phase 64 itself. **Routing: ride a separate "Phase 64 docs" PR after Plan 03 closes,** OR accept as orphan (these only matter for traceability inside the .planning/ tree, not for application code on main). |

---

## §6 Defer cohort handling — three options for user

The Defer cohort is 177 commits across 9 subgroups. The user has three options:

### Option D1 — Ship CRT-05 only, leave Defer cohort on `chore/v1.7-ratification`

**Action:** After PR #3 merges, `chore/v1.7-ratification` retains all 177 Defer commits.
The branch is NOT deleted. v1.9 inherits a non-empty backlog branch.

**Pros:** Minimal-scope CRT-05 ship. Clean bisect surface across the 3 CRT-05 cohorts
without unrelated noise.

**Cons:** `git log main..chore/v1.7-ratification --oneline` will still show 177 orphan
commits post-CRT-05. Plan 03 success criterion #8 ("chore/v1.7-ratification fully
reflected on main") becomes a `must_haves.truth` that explicitly carve out the Defer
cohort.

### Option D2 — Ship CRT-05, then ship Defer cohort as 1-N follow-up PRs

**Action:** After PR #3 merges, open additional PRs for each Defer subgroup:
- "Phase 47 → 56 v1.7 ratification ship PR" (one big ratification PR — none of those
  commits add application code, just docs/REQUIREMENTS/STATE)
- "Phase 57 ship PR" (AES-01..04 + DGN-01..03 baselines)
- "Phase 58 RUM ship PR" (WebVitals + RUM API + LCP-identity guard) — REQUIRES Phase 60
  LCP-02 re-calibration before merge per 59-VERIFICATION known_regressions
- "Phase 60 ship PR" (LCP-02 GhostLabel intervention + LHCI CLS threshold loosen)
- "Phase 61 ship PR" (bundle hygiene cohort)
- "Phase 62 ship PR" (VRF-01..05)
- "Phase 63 / 63.1 ship PR" (CdCornerPanel hoist + Path A wordmark vectorization)
- "Phase 64 docs ship PR" (Phase 64 research/plan/SUMMARY)

**Pros:** Clean main reflection of `chore/v1.7-ratification`. Each Phase ships as its
own bisect surface.

**Cons:** Massive PR cycle (8 additional PRs, each with LHCI gate runs, each with merge
delays). v1.8 close-out becomes weeks-long.

### Option D3 — After CRT-05, force-merge `chore/v1.7-ratification` to main as one big merge commit

**Action:** After PR #3 merges, fast-forward `chore/v1.7-ratification` onto main with
a single merge commit (`git merge chore/v1.7-ratification --no-ff` or `gh pr create
--base main --head chore/v1.7-ratification --title "ship: v1.8 + 63.1 + 64 backlog"`).

**Pros:** Single PR cleans up the entire backlog post-CRT-05.

**Cons:** Defeats CRT-05 bisect protection beyond the three cohorts. Phase 63.1 +
Phase 60 + Phase 61 + Phase 62 + Phase 63 commits all squash-collapse into one merge
commit. Not recommended.

**Default recommendation: Option D1 (ship CRT-05 only, defer cleanup to v1.9 + post-CRT-05
follow-up PRs).** This satisfies CRT-05 bisect requirement and avoids overcommitting
to a 8-PR cleanup cycle in Phase 64's scope.

---

## §7 Risks specific to this audit

| Risk | Mitigation |
| ---- | ---------- |
| Cherry-picking PR #1 commits over Phase 47 base may conflict on `app/layout.tsx` because Phase 47 had an earlier version of the file before `c9937c8` Phase 58 LHCI integration. | `app/layout.tsx` is `66ac4ec`'s primary change target. The Phase 58 LHCI gate (`6167c31`) does NOT touch `app/layout.tsx`. Conflict probability LOW. If conflict arises, the 3-way merge resolution should favor `chore/v1.7-ratification`'s version. |
| `package.json` may conflict on devDeps if multiple commits modify it (`e527ace` adds @lhci/cli; `8293059` adds opentype.js). | These commits ride DIFFERENT PRs (Cohort 1 vs Cohort 2). PR #1's `package.json` will only have @lhci/cli. PR #2's cherry-pick of `8293059` will need to bring its own package.json modification (opentype.js) — conflict-free since main now has @lhci/cli but not opentype.js. |
| `pnpm-lock.yaml` will conflict on every cherry-pick due to lockfile churn. | Run `pnpm install` after cherry-pick to regenerate the lockfile, then `git add pnpm-lock.yaml --force` and amend. Standard pnpm post-cherry-pick hygiene. |
| The Phase 58 RUM telemetry (`46dae75` etc.) changes `app/layout.tsx` (CIB-05 mount). If that lands in Cohort 3 via a later PR, it conflicts with Cohort 1's `66ac4ec` `app/layout.tsx` changes. | Phase 58 RUM is NOT in any 59-XX cohort. It's deferred per Option D2. The conflict resolution lives in the Phase 58 ship PR (after CRT-05), not in 59-XX. |
| Phase 63.1's CdCornerPanel hoist (`7458de9`) changes `app/layout.tsx`. | Same as above — Phase 63.1 is fully deferred per §5i. |
| **DISCOVERED 2026-04-28 post-PR-1-open**: `tests/v1.8-phase59-pixel-diff.spec.ts` (cherry-picked via `fefeda2`) imports `pngjs` + `pixelmatch`, but those devDeps were added in deferred commit `9ea4a6d` (§5d Phase 58 RUM). CI failed on PR #1 with `Cannot find module 'pngjs'`. | Resolved on `ship/59-01` via fix commit `4d3f84b` adding the 4 devDeps (`@types/pixelmatch`, `@types/pngjs`, `pixelmatch`, `pngjs`) directly to `package.json`. **Lesson for future audits: trace transitive npm-dep edges from cherry-picked spec files, not just file-level cohort partitioning.** |

---

## §8 Sign-off

User MUST verify all four items before responding "audit approved":

- [ ] All four cohort sections (§2, §3, §4, §5) reviewed
- [ ] No commits accidentally placed in wrong cohort (T-64-03 audit) — particularly,
      no Phase 63.1 commits (`7458de9`, `34d8d4c`, etc.) in any 59-XX cohort
- [ ] Defer cohort handling option chosen (D1 / D2 / D3) — auditor recommends **Option D1**
- [ ] Plan 02 placement decision confirmed: **Decision A — rides PR #3** (auditor's recommendation)

**Reply with one of:**
- `"audit approved, proceed with ship/59-01"` — accepts §2 cohort + Option D1 + Decision A as written
- `"audit approved with adjustments: [details]"` — provide specific changes to §2/§3/§4/§5 or override D1/D2/D3 / Decision A or B
- `"audit rejected: [reason]"` — return to planning

---

## §9 Audit metadata

- Captured at: `2026-04-28T20:29:49Z`
- Auditor: gsd-executor (Phase 64 Plan 03 Task 0)
- Pre-flight checks per Plan 03 Task 0 Sub-step A:
  - Branch protection on main (audit context check): NOT YET CONFIGURED — expected per amendment_2026_04_28 (Plan 01 Task 3 fires AFTER PR #2 opens; PR #1 ungated by design)
  - Plan 02 recalibration on chore/v1.7-ratification: VERIFIED — `grep -c "SYNTHETIC_LCP_MS = 657" tests/v1.8-phase63-1-pitfall-10.spec.ts` returns 1; `grep -c "LCP_RATIO_MAX = 3.5"` returns 1
- Working branch state: clean (only worktree-side modifications: STATE.md, config.json, etc — none touch the cohort SHAs above)
