---
phase: 67-bundle-barrel-optimization-d-04-unlock
plan: 02
type: execute
wave: 2
depends_on:
  - 67-01
files_modified:
  - tests/v1.8-phase63-1-bundle-budget.spec.ts
  - .lighthouseci/lighthouserc.json
autonomous: true
requirements:
  - BND-06
  - BND-07
gap_closure: false

threat_model:
  asvs_level: 1
  threats:
    - id: T-67-01
      severity: LOW
      description: Stale-chunk cache carryover causes false-pass on the BND-06 final-gate measurement
      surface: build/measurement
      mitigation: BND-04 standing rule (D-10) — `rm -rf .next/cache .next` before the gating build at Task 0
      encoded_in_task: T0
  out_of_scope:
    - auth, session, RBAC (no auth surface modified)
    - data persistence, SQL injection (no DB surface)
    - XSS, CSRF (no input handling modified)
    - external IO / SSRF (no fetch surface modified)

must_haves:
  truths:
    - "Fresh post-reshape gating build measured: homepage `/` First-Load JS gzip total recorded against 200 KB CLAUDE.md target"
    - "D-06 outcome ladder applied: BUDGET_BYTES mutated per measured-result branch (≤200 retire, 201-220 path_q replace, >220 escalate)"
    - "_path_k_decision header retired or replaced per D-06 branch (NOT extended)"
    - "Final phase-end AES-04 PASS: 5 routes × 4 viewports vs `.planning/visual-baselines/v1.8-start/` ≤0.5% per page"
    - "LHCI threshold review documented (Plan 02 may or may not tighten — discretionary per RESEARCH §LHCI)"
    - "BND-06 + BND-07 close-out: spec gate + (if 201-220 KB) new path_q_decision block recorded"
  artifacts:
    - path: "tests/v1.8-phase63-1-bundle-budget.spec.ts"
      provides: "Mutated bundle-budget gate: BUDGET_BYTES updated per D-06 branch + header decision block updated"
      contains: "BUDGET_BYTES = "
    - path: ".lighthouseci/lighthouserc.json"
      provides: "LHCI thresholds (reviewed; may be tightened if TBT improved post-reshape — discretionary)"
      contains: "categories:performance"
    - path: ".planning/phases/67-bundle-barrel-optimization-d-04-unlock/67-02-FINAL-GATE.md"
      provides: "Phase 67 4-way scorecard mirroring 61-03-FINAL-GATE.md format (BND-05/06/07 + AES-04 verdicts)"
  key_links:
    - from: "tests/v1.8-phase63-1-bundle-budget.spec.ts"
      to: ".next/app-build-manifest.json `pages['/page']`"
      via: "manifest read + per-chunk gzip in-memory + sum < BUDGET_BYTES"
      pattern: "BUDGET_BYTES"
    - from: "_path_k_decision OR _path_q_decision"
      to: ".planning/codebase/v1.9-bundle-reshape.md"
      via: "evidence + ratified_to_main_via citation"
      pattern: "v1.9-bundle-reshape"
    - from: ".lighthouseci/lighthouserc.json"
      to: "LHCI gate"
      via: "categories:performance / total-blocking-time / largest-contentful-paint thresholds"
      pattern: "minScore|maxNumericValue"
---

<objective>
Apply the D-06 outcome ladder to retire (or replace) the `_path_k_decision` annotation block based on the measured post-Phase-67-Plan-01 homepage First-Load JS, run the final phase-end AES-04 gate against `.planning/visual-baselines/v1.8-start/`, review LHCI thresholds for discretionary tightening, and emit a Phase 67 final-gate scorecard.

Purpose: Close BND-06 (homepage `/` First-Load JS ≤200 KB OR documented outcome-ladder ratification) and BND-07 (spec/LHCI half — re-tighten the bundle-budget gate from path_k 260 KB toward CLAUDE.md 200 KB target).

Output: Mutated `tests/v1.8-phase63-1-bundle-budget.spec.ts` with new BUDGET_BYTES + retired/replaced decision block; LHCI review record (in 67-02-FINAL-GATE.md); final phase-end AES-04 verdict.
</objective>

<execution_context>
@/Users/greyaltaer/.claude/pde-os/engines/gsd/workflows/execute-plan.md
@/Users/greyaltaer/.claude/pde-os/engines/gsd/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

@.planning/phases/67-bundle-barrel-optimization-d-04-unlock/67-CONTEXT.md
@.planning/phases/67-bundle-barrel-optimization-d-04-unlock/67-RESEARCH.md
@.planning/phases/67-bundle-barrel-optimization-d-04-unlock/67-VALIDATION.md

@.planning/codebase/v1.9-bundle-reshape.md
@.planning/codebase/AESTHETIC-OF-RECORD.md

@tests/v1.8-phase63-1-bundle-budget.spec.ts
@tests/v1.9-phase67-aes04.spec.ts
@.lighthouseci/lighthouserc.json

@.planning/phases/61-bundle-hygiene/61-03-FINAL-GATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 0: Fresh gating build (D-10 stale-chunk guard) + measure final homepage gzip total</name>
  <files>/tmp/67-build-final-plan02.log, /tmp/67-budget-final-plan02.log</files>
  <read_first>
    - .planning/phases/67-bundle-barrel-optimization-d-04-unlock/67-VALIDATION.md (§"Build Measurement Protocol (BND-04 standing rule)")
    - .planning/codebase/v1.9-bundle-reshape.md (Plan 01 output — §1 Reshape Vector Outcome Summary tells you the anticipated D-06 branch)
    - tests/v1.8-phase63-1-bundle-budget.spec.ts (lines 71-95 measurement methodology — DO NOT mutate yet)
  </read_first>
  <action>
    Run the BND-04 stale-chunk guard, do a fresh production build, and capture the canonical gating measurement that drives the D-06 branch in Task 1.

    1. `cd /Users/greyaltaer/code/projects/SignalframeUX && rm -rf .next/cache .next`
    2. `pnpm build 2>&1 | tee /tmp/67-build-final-plan02.log`
    3. `pnpm exec playwright test ./tests/v1.8-phase63-1-bundle-budget.spec.ts --reporter=list 2>&1 | tee /tmp/67-budget-final-plan02.log`
       - The current spec asserts <260 KB (path_k threshold). It MUST PASS at this stage — Plan 01 reshape only reduces, never increases. If it FAILs, STOP and reopen Plan 01.
    4. Extract the homepage gzip total from console output. The spec logs `Total: NNN.N KB (budget: 260 KB, post-path_k)`. Record this number to `/tmp/67-final-gzip-total.txt`:
       ```bash
       grep -Eo "Total: [0-9]+\.[0-9]+ KB" /tmp/67-budget-final-plan02.log | head -1 | grep -Eo "[0-9]+\.[0-9]+" > /tmp/67-final-gzip-total.txt
       cat /tmp/67-final-gzip-total.txt
       ```
    5. Determine D-06 branch and record to `/tmp/67-d06-branch.txt`:
       ```bash
       node -e "
       const fs = require('fs');
       const total = parseFloat(fs.readFileSync('/tmp/67-final-gzip-total.txt', 'utf-8').trim());
       let branch;
       if (total <= 200.0) branch = 'A_RETIRE';
       else if (total <= 220.0) branch = 'B_REPLACE_PATH_Q';
       else branch = 'C_ESCALATE';
       fs.writeFileSync('/tmp/67-d06-branch.txt', branch + '\n');
       console.log('measured=' + total + 'KB; branch=' + branch);
       "
       ```

    Do NOT mutate any spec or config in this task. Measurement-only.
  </action>
  <verify>
    <automated>test -f /tmp/67-build-final-plan02.log && grep -c "Compiled successfully" /tmp/67-build-final-plan02.log | grep -E "^[1-9]" && test -f /tmp/67-final-gzip-total.txt && test -f /tmp/67-d06-branch.txt && pnpm exec playwright test ./tests/v1.8-phase63-1-bundle-budget.spec.ts --reporter=list</automated>
  </verify>
  <acceptance_criteria>
    - `/tmp/67-build-final-plan02.log` exists and contains `Compiled successfully`
    - `/tmp/67-final-gzip-total.txt` contains a single float value (homepage gzip KB)
    - `/tmp/67-d06-branch.txt` contains exactly one of: `A_RETIRE`, `B_REPLACE_PATH_Q`, `C_ESCALATE`
    - `pnpm exec playwright test ./tests/v1.8-phase63-1-bundle-budget.spec.ts --reporter=list` exits 0 (against the unchanged 260 KB threshold — Plan 01 reductions held)
    - The Plan 01 v1.9-bundle-reshape.md §1 anticipated branch matches `/tmp/67-d06-branch.txt` (sanity check: anticipated and measured agree; if not, document divergence in Task 3 final-gate)
  </acceptance_criteria>
  <done>
    Fresh production build is the gating surface. Homepage gzip total measured; D-06 branch determined. Task 1 reads `/tmp/67-d06-branch.txt` to pick the mutation strategy.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 1: Apply D-06 outcome ladder — mutate tests/v1.8-phase63-1-bundle-budget.spec.ts</name>
  <files>tests/v1.8-phase63-1-bundle-budget.spec.ts</files>
  <read_first>
    - tests/v1.8-phase63-1-bundle-budget.spec.ts (full file — lines 11-52 _path_k_decision header, line 59 BUDGET_BYTES, line 61 test name, line 120 console log, line 123 error message)
    - /tmp/67-d06-branch.txt (Task 0 output — drives branch selection)
    - /tmp/67-final-gzip-total.txt (Task 0 output — drives BUDGET_BYTES value in Branch B)
    - .planning/phases/67-bundle-barrel-optimization-d-04-unlock/67-CONTEXT.md (D-06 outcome ladder spec)
    - .planning/codebase/v1.9-bundle-reshape.md (Plan 01 output — evidence citation for any new path_decision block)
    - feedback_path_b_pattern.md memory (the `_path_X_decision` block format — decided/audit/scope/original/new/rationale/evidence/review_gate/ratified_to_main_via)
  </read_first>
  <action>
    Read `/tmp/67-d06-branch.txt` and apply EXACTLY ONE branch below. Do NOT mix branches.

    ---

    **Branch A — `A_RETIRE` (homepage ≤ 200.0 KB gzip):**

    1. **Delete the entire `_path_k_decision` header block** at lines 11-52 (everything from `// _path_k_decision (2026-04-29):` through the end of `// ratified_to_main_via:` line). Keep the leading file-purpose comment block at lines 1-9 (methodology + file role).
    2. **Replace lines 1-9** with this updated header (preserve methodology, drop path_k references, cite Phase 67 closure):
       ```
       // Phase 67 Plan 02 BND-06 close — bundle budget gate restored to CLAUDE.md hard
       // constraint after the Phase 67 reshape. Asserts homepage / First Load JS chunk
       // sum < BUDGET_BYTES (gzip) after pnpm build.
       // Uses .next/app-build-manifest.json pages["/page"] for App Router builds (Next 15).
       // Falls back to build-manifest.json pages["/"] for Pages Router format.
       // Skips gracefully if build artifacts are not present.
       //
       // Measurement methodology: reads each chunk file, gzip-compresses in memory, sums bytes.
       // This matches the "First Load JS" column in Next.js Route (app) build output — which
       // reports the compressed transfer size that users actually download.
       //
       // History: Phase 63.1 ratified 260 KB (path_k); Phase 67 reshape (BND-05/06/07) restored
       // the 200 KB target via @/components/sf optimizePackageImports + DCE of zero-consumer
       // barrel exports + TooltipProviderLazy hydration-gate. _path_k_decision retired
       // 2026-04-30; rationale + chunk-ID lock at .planning/codebase/v1.9-bundle-reshape.md.
       ```
    3. **Mutate line 59**: `const BUDGET_BYTES = 260 * 1024;` → `const BUDGET_BYTES = 200 * 1024;`
    4. **Mutate the inline trailing comment on line 59**: `// 260 KB gzip — see _path_k_decision above` → `// 200 KB gzip — CLAUDE.md hard constraint, restored Phase 67 (BND-06)`
    5. **Mutate line 61** test name: `"Phase 63.1 Plan 01 — homepage First Load JS < 260 KB (post-path_k)"` → `"Phase 67 Plan 02 — homepage First Load JS < 200 KB (BND-06 / CLAUDE.md hard constraint)"`
    6. **Mutate line 120** console output: `Total: ${totalKB} KB (budget: 260 KB, post-path_k)` → `Total: ${totalKB} KB (budget: 200 KB, post-Phase-67 BND-06)`
    7. **Mutate lines 122-127** error message: replace the path_k error preamble with: `` `Homepage First Load JS is ${totalKB} KB — budget is 200 KB ` + `(CLAUDE.md hard constraint, restored at Phase 67 BND-06 close per ` + `.planning/codebase/v1.9-bundle-reshape.md). If this fails after a recent commit, ` + `the commit bloated the bundle past the post-Phase-67 baseline — investigate ` + `the new commit's imports rather than bumping the threshold. Run ANALYZE=true ` + `pnpm build and inspect .next/analyze/client.html to identify which chunks grew.` ``

    Commit message: `Chore(67-02): retire _path_k_decision; BUDGET_BYTES = 200 * 1024 per D-06 Branch A (BND-06 close)`

    ---

    **Branch B — `B_REPLACE_PATH_Q` (homepage 201.0 - 220.0 KB gzip):**

    1. **Delete the entire `_path_k_decision` header block** at lines 11-52.
    2. **Insert a new `_path_q_decision` header block** in its place. Compute the new threshold:
       ```
       new_threshold_kb = ceil(measured_kb + 1.0)   # 1 KB margin
       BUDGET_BYTES_value = new_threshold_kb * 1024
       ```
       Block format (mirror `_path_k_decision` schema; the canonical `_path_X_decision` template per `feedback_path_b_pattern.md`):
       ```
       // _path_q_decision (2026-04-30):
       //   decided: 2026-04-30 (Phase 67 Plan 02 BND-06 close)
       //   audit: homepage First Load JS gzip
       //   scope: tests/v1.8-phase63-1-bundle-budget.spec.ts (CI test surface only)
       //   original_threshold: 200 KB (CLAUDE.md hard constraint)
       //   new_threshold:      {new_threshold_kb} KB
       //   rationale: Phase 67 reshape (BND-05/06/07) reduced homepage First Load JS from
       //     258.9 KB (Phase 63.1 path_k) to {measured_kb} KB via @/components/sf
       //     optimizePackageImports + DCE of zero-consumer barrel exports + TooltipProviderLazy
       //     hydration-gate. Falls short of CLAUDE.md 200 KB hard target by {measured_kb - 200} KB.
       //     The framework chunk floor (~120 KB: React 53.1 + Next runtime 44.9 + webpack/main 21.9
       //     per .planning/codebase/v1.8-lcp-diagnosis.md §2b) is unreachable without App Router
       //     config / Next.js version reshape, deferred OUT OF SCOPE per Phase 67 CONTEXT.md
       //     §deferred. Tight ratification at measured + 1 KB margin.
       //   evidence: .planning/codebase/v1.9-bundle-reshape.md §1 Reshape Vector Outcome Summary
       //     + §3 Pre/post homepage First-Load JS comparison (Phase 67 Plan 01 close).
       //     /tmp/67-budget-final-plan02.log (Phase 67 Plan 02 Task 0 measurement).
       //   review_gate: Tighten to 200 KB when (a) framework-chunk reshape (App Router config /
       //     Next.js version / React-server-component boundary work) ships in a future phase,
       //     OR (b) further DCE on lib/api-docs/* moves chunks 4901-class out of /page graph,
       //     OR (c) Three.js code-split (e9a6067a + 74c6194b chunks) moves below the line.
       //     Until then, {new_threshold_kb} KB is the standing rule.
       //   ratified_to_main_via: Phase 67 Plan 02 commit (this file) — closes BND-06 with
       //     documented outcome-ladder branch B.
       ```
    3. **Mutate line 59**: `const BUDGET_BYTES = 260 * 1024;` → `const BUDGET_BYTES = {new_threshold_kb} * 1024;`
    4. **Mutate inline trailing comment on line 59**: → `// {new_threshold_kb} KB gzip — see _path_q_decision above`
    5. **Mutate line 61** test name: → `"Phase 67 Plan 02 — homepage First Load JS < {new_threshold_kb} KB (post-path_q, BND-06)"`
    6. **Mutate line 120** console output: → `` `Total: ${totalKB} KB (budget: {new_threshold_kb} KB, post-path_q)` ``
    7. **Mutate lines 122-127** error message: replace path_k references with path_q + cite v1.9-bundle-reshape.md.

    Replace `{new_threshold_kb}` and `{measured_kb}` literals with computed numeric values. Resulting file MUST contain ZERO `{...}` placeholder tokens.

    Commit message: `Chore(67-02): replace _path_k_decision with _path_q_decision; BUDGET_BYTES = {new_threshold_kb} * 1024 per D-06 Branch B (BND-06 close)`

    ---

    **Branch C — `C_ESCALATE` (homepage > 220.0 KB gzip):**

    DO NOT mutate the spec. STOP and:
    1. Document the escalation in `.planning/phases/67-bundle-barrel-optimization-d-04-unlock/67-02-ESCALATION.md`:
       - Measured KB
       - Per-vector deltas (read from /tmp/67-vector-deltas.txt)
       - Anticipated next-phase scope (framework-chunk reshape per CONTEXT.md §deferred)
       - Citation: ROADMAP.md §1051 D-06 outcome ladder Branch C
    2. Skip Tasks 2 and 3 (final AES-04 + final gate). Plan 02 closes with status `escalate-required`.
    3. Commit message: `Chore(67-02): D-06 Branch C — escalation required (measured {measured_kb} KB > 220 KB cap)`
  </action>
  <verify>
    <automated>BRANCH=$(cat /tmp/67-d06-branch.txt 2>/dev/null); if [ "$BRANCH" = "A_RETIRE" ]; then grep -c "BUDGET_BYTES = 200 \* 1024" tests/v1.8-phase63-1-bundle-budget.spec.ts | grep -E "^1$" && grep -c "_path_k_decision" tests/v1.8-phase63-1-bundle-budget.spec.ts | grep -E "^0$" && grep -c "_path_q_decision" tests/v1.8-phase63-1-bundle-budget.spec.ts | grep -E "^0$" && pnpm exec playwright test ./tests/v1.8-phase63-1-bundle-budget.spec.ts --reporter=list; elif [ "$BRANCH" = "B_REPLACE_PATH_Q" ]; then grep -c "_path_k_decision" tests/v1.8-phase63-1-bundle-budget.spec.ts | grep -E "^0$" && grep -c "_path_q_decision" tests/v1.8-phase63-1-bundle-budget.spec.ts | grep -E "^[1-9]" && grep -E "BUDGET_BYTES = (20[1-9]|21[0-9]|220) \* 1024" tests/v1.8-phase63-1-bundle-budget.spec.ts && pnpm exec playwright test ./tests/v1.8-phase63-1-bundle-budget.spec.ts --reporter=list; elif [ "$BRANCH" = "C_ESCALATE" ]; then test -f .planning/phases/67-bundle-barrel-optimization-d-04-unlock/67-02-ESCALATION.md; else echo "UNKNOWN BRANCH"; exit 1; fi</automated>
  </verify>
  <acceptance_criteria>
    Branch A (RETIRE — homepage ≤200 KB):
      - `grep -c "BUDGET_BYTES = 200 \* 1024" tests/v1.8-phase63-1-bundle-budget.spec.ts` equals 1
      - `grep -c "_path_k_decision" tests/v1.8-phase63-1-bundle-budget.spec.ts` equals 0
      - `grep -c "_path_q_decision" tests/v1.8-phase63-1-bundle-budget.spec.ts` equals 0
      - `grep -c "Phase 67 Plan 02" tests/v1.8-phase63-1-bundle-budget.spec.ts` ≥ 1 (test name + comments updated)
      - `pnpm exec playwright test ./tests/v1.8-phase63-1-bundle-budget.spec.ts --reporter=list` exits 0 (homepage <200 KB)
      - Git log shows commit `Chore(67-02): retire _path_k_decision; BUDGET_BYTES = 200 * 1024`

    Branch B (REPLACE — homepage 201-220 KB):
      - `grep -c "_path_k_decision" tests/v1.8-phase63-1-bundle-budget.spec.ts` equals 0 (path_k retired)
      - `grep -c "_path_q_decision" tests/v1.8-phase63-1-bundle-budget.spec.ts` ≥ 1 (path_q present)
      - `grep -E "BUDGET_BYTES = (20[1-9]|21[0-9]|220) \* 1024" tests/v1.8-phase63-1-bundle-budget.spec.ts` matches exactly one line
      - File contains zero `{...}` placeholder tokens (all literals replaced with computed values)
      - `pnpm exec playwright test ./tests/v1.8-phase63-1-bundle-budget.spec.ts --reporter=list` exits 0
      - Git log shows commit `Chore(67-02): replace _path_k_decision with _path_q_decision`

    Branch C (ESCALATE — homepage >220 KB):
      - `test -f .planning/phases/67-bundle-barrel-optimization-d-04-unlock/67-02-ESCALATION.md` returns 0
      - `tests/v1.8-phase63-1-bundle-budget.spec.ts` UNCHANGED from pre-Task-1 state (`grep -c "BUDGET_BYTES = 260 \* 1024" tests/v1.8-phase63-1-bundle-budget.spec.ts` equals 1)
      - Plan 02 closes with `status: escalate-required` in 67-02-SUMMARY.md
      - Git log shows commit `Chore(67-02): D-06 Branch C — escalation required`
  </acceptance_criteria>
  <done>
    BUDGET_BYTES + decision-block header reflect the measured branch outcome. BND-06 closed with documented branch verdict (or escalation path opened with documented rationale).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Final phase-end AES-04 gate (5 routes × 4 viewports vs v1.8-start ≤0.5%)</name>
  <files>/tmp/67-aes04-final.log</files>
  <read_first>
    - tests/v1.9-phase67-aes04.spec.ts (Plan 01 Task 1 Wave 0 spec)
    - /tmp/67-d06-branch.txt (skip this task entirely if branch is C_ESCALATE)
    - .planning/codebase/AESTHETIC-OF-RECORD.md (§AES-04 ≤0.5%)
    - .planning/visual-baselines/v1.8-start/ (20 baseline PNGs at 5 routes × 4 viewports)
  </read_first>
  <action>
    **Skip this task entirely if `/tmp/67-d06-branch.txt` contains `C_ESCALATE`** — escalation freezes the phase before final-gate captures.

    Otherwise, run the final phase-end AES-04 against the post-Plan-01 build with the post-Plan-02 test threshold mutation in place:

    1. Ensure prod build is fresh:
       ```bash
       rm -rf .next/cache .next && pnpm build 2>&1 | tee /tmp/67-build-aes04-final.log
       ```
    2. Start prod server in background:
       ```bash
       pnpm start &
       PROD_PID=$!
       sleep 6
       ```
    3. Run AES-04 spec with strict ≤0.5% gate:
       ```bash
       pnpm exec playwright test tests/v1.9-phase67-aes04.spec.ts --reporter=list 2>&1 | tee /tmp/67-aes04-final.log
       AES_EXIT=$?
       kill $PROD_PID 2>/dev/null || true
       wait $PROD_PID 2>/dev/null || true
       ```
    4. All 20 tests (5 routes × 4 viewports) must PASS at MAX_DIFF_RATIO = 0.005. If any FAILs, the failure artifacts at `.playwright-artifacts/phase67-bundle-reshape/{slug}-{viewport}-DIFF.png` document which surfaces drifted; the commit/branch must be reverted before Phase 67 closes.

    Document the verdict (passed/failed counts + per-route diff percentages) in `/tmp/67-aes04-final-summary.txt`:
    ```bash
    grep -E "✓|✗|matches v1.8-start|pixel-diff" /tmp/67-aes04-final.log > /tmp/67-aes04-final-summary.txt
    ```

    No commit in this task — measurement-only. The result feeds Task 3 final-gate doc.
  </action>
  <verify>
    <automated>BRANCH=$(cat /tmp/67-d06-branch.txt 2>/dev/null); if [ "$BRANCH" = "C_ESCALATE" ]; then echo "SKIPPED — branch C"; exit 0; else test -f /tmp/67-aes04-final.log && grep -c "passed" /tmp/67-aes04-final.log | grep -E "^[1-9]" && grep -cE "failed|expected" /tmp/67-aes04-final.log | grep -E "^0$"; fi</automated>
  </verify>
  <acceptance_criteria>
    Branch A or B:
      - `/tmp/67-aes04-final.log` exists
      - Log contains `20 passed` (or equivalent — all 20 tests pass)
      - Log contains zero `failed` lines
      - `/tmp/67-aes04-final-summary.txt` exists for inclusion in Task 3 final-gate doc
      - No `.playwright-artifacts/phase67-bundle-reshape/*-DIFF.png` files created (zero diff > 0.5% surfaces)

    Branch C:
      - Task SKIPPED — verify acceptance check returns the SKIPPED stub
  </acceptance_criteria>
  <done>
    Final phase-end AES-04 verdict captured. Per-route diff percentages recorded for inclusion in 67-02-FINAL-GATE.md.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Author 67-02-FINAL-GATE.md scorecard + LHCI threshold review</name>
  <files>.planning/phases/67-bundle-barrel-optimization-d-04-unlock/67-02-FINAL-GATE.md, .lighthouseci/lighthouserc.json</files>
  <read_first>
    - .planning/phases/61-bundle-hygiene/61-03-FINAL-GATE.md (template — mirror 4-way scorecard format + FALSE-PASS GUARD section)
    - .planning/codebase/v1.9-bundle-reshape.md (Plan 01 output)
    - /tmp/67-final-gzip-total.txt, /tmp/67-d06-branch.txt (Task 0)
    - /tmp/67-aes04-final.log, /tmp/67-aes04-final-summary.txt (Task 2)
    - /tmp/67-vector-deltas.txt (Plan 01 vector ledger)
    - .lighthouseci/lighthouserc.json (current LHCI thresholds)
    - .planning/phases/67-bundle-barrel-optimization-d-04-unlock/67-RESEARCH.md (§"LHCI Threshold Analysis")
  </read_first>
  <action>
    **Step 1 — Author `.planning/phases/67-bundle-barrel-optimization-d-04-unlock/67-02-FINAL-GATE.md`** mirroring the Phase 61 4-way scorecard format. Include sections:

    1. **Header** — Phase 67 BND-05/06/07 + AES-04 verdict; D-06 branch outcome (A/B/C); measured homepage gzip total.

    2. **§1 — 4-Way Scorecard**:
       | Requirement | Verdict | Evidence |
       | BND-05 (D-04 lock break + re-lock) | PASS / FAIL | .planning/codebase/v1.9-bundle-reshape.md §2a + next.config.ts Phase 67 comment |
       | BND-06 (homepage ≤200 KB OR documented outcome ladder) | PASS / RATIFIED-VIA-PATH-Q / ESCALATED | /tmp/67-final-gzip-total.txt + tests/v1.8-phase63-1-bundle-budget.spec.ts BUDGET_BYTES |
       | BND-07 (new chunk-ID baseline + LHCI re-tighten) | PASS | v1.9-bundle-reshape.md §2a-2b + spec mutation + LHCI review (this file §3) |
       | AES-04 (≤0.5% per page across 5 routes × 4 viewports) | PASS / FAIL | /tmp/67-aes04-final-summary.txt |

    3. **§2 — Per-vector outcome ledger** (from /tmp/67-vector-deltas.txt + Plan 01 SUMMARY):
       | Vector | Action | Delta KB gz | D-02 floor | AES-04 | Status |
       (Mirror Plan 01 v1.9-bundle-reshape.md §1 table.)

    4. **§3 — LHCI threshold review**:
       Read `.lighthouseci/lighthouserc.json`. Per RESEARCH.md §"LHCI Threshold Analysis" + RESEARCH.md §"LHCI Threshold Analysis" decision logic:
       - LHCI does NOT have a bundle-size-specific gate. The bundle-budget spec is the gate for BND-06.
       - Discretionary tightening is allowed if `total-blocking-time` improved post-reshape.
       - Action policy:
         - If post-reshape pnpm build production TBT (from build output) is observably lower vs Phase 63.1 baseline AND consistently <500ms across LHCI runs → tighten `total-blocking-time` `maxNumericValue` from 700ms to 500ms (annotate via `_path_decision`-style block in lighthouserc commit message).
         - Otherwise → leave LHCI untouched. Document the no-op decision in this section.

       Run a single `pnpm exec lhci collect --numberOfRuns=3 --collect.startServerCommand="pnpm start" --collect.url="http://localhost:3000/"` if helpful for confidence; otherwise reference Phase 58 LHCI baseline.

       Record verdict (`tightened` or `unchanged`) + rationale in this section.

    5. **§4 — FALSE-PASS GUARD** (per Phase 61 precedent):
       - Confirm BND-04 D-10 stale-chunk discipline applied (every gating build prefixed with `rm -rf .next/cache .next`); cite the build log paths.
       - Confirm production webpack (NOT Turbopack dev) used for all gating measurements.
       - Confirm AES-04 ran against `pnpm build && pnpm start`, NOT `pnpm dev`.

    6. **§5 — Carry-forwards / next-phase hand-offs**:
       - If Branch A: BND target restored; future phases can reference 200 KB hard-line again.
       - If Branch B: `_path_q_decision` review_gate names framework-chunk reshape (Three.js code-split / lib/api-docs DCE) as the path to 200 KB. Connects to v1.8 carry-over `feedback_path_b_pattern.md`.
       - If Branch C: framework-chunk reshape phase becomes next milestone scope per CONTEXT.md §deferred.
       - Phase 66 sequencing: Phase 66 was already shipped (per STATE.md). No further blocking.
       - VRF-08 path_b_decision review_gate fires after Phase 67 close (per project_phase70_closed.md memory) — flag for v1.9 milestone close-out.

    **Step 2 — LHCI mutation (only if §3 verdict = `tightened`)**:

    Edit `.lighthouseci/lighthouserc.json` per the decision in §3. Add a comment-style annotation in the commit message (since lighthouserc.json is JSON and cannot host inline comments):
    ```
    Chore(67-02): LHCI total-blocking-time 700→500ms (Phase 67 post-reshape TBT improvement; closes BND-07 LHCI half)
    ```

    If §3 verdict = `unchanged`, do NOT touch lighthouserc.json. Commit only the FINAL-GATE.md doc.

    **Step 3 — Final commit** combining FINAL-GATE doc + (optional) LHCI mutation:
    ```
    Chore(67-02): 67-02-FINAL-GATE.md + LHCI {tightened|unchanged} (BND-06 + BND-07 close, D-06 branch {A|B|C})
    ```
  </action>
  <verify>
    <automated>test -f .planning/phases/67-bundle-barrel-optimization-d-04-unlock/67-02-FINAL-GATE.md && grep -c "BND-05" .planning/phases/67-bundle-barrel-optimization-d-04-unlock/67-02-FINAL-GATE.md | grep -E "^[1-9]" && grep -c "BND-06" .planning/phases/67-bundle-barrel-optimization-d-04-unlock/67-02-FINAL-GATE.md | grep -E "^[1-9]" && grep -c "BND-07" .planning/phases/67-bundle-barrel-optimization-d-04-unlock/67-02-FINAL-GATE.md | grep -E "^[1-9]" && grep -c "AES-04" .planning/phases/67-bundle-barrel-optimization-d-04-unlock/67-02-FINAL-GATE.md | grep -E "^[1-9]" && grep -c "FALSE-PASS GUARD" .planning/phases/67-bundle-barrel-optimization-d-04-unlock/67-02-FINAL-GATE.md | grep -E "^1$" && python3 -c "import json; json.load(open('.lighthouseci/lighthouserc.json'))"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/67-bundle-barrel-optimization-d-04-unlock/67-02-FINAL-GATE.md` returns 0
    - FINAL-GATE.md contains all 5 sections: 4-Way Scorecard, Per-vector ledger, LHCI review, FALSE-PASS GUARD, Carry-forwards
    - `grep -E "BND-0[567]|AES-04" .planning/phases/67-bundle-barrel-optimization-d-04-unlock/67-02-FINAL-GATE.md` returns ≥4 matches (each requirement cited)
    - `grep -c "FALSE-PASS GUARD" .planning/phases/67-bundle-barrel-optimization-d-04-unlock/67-02-FINAL-GATE.md` equals 1
    - `grep -E "Branch [ABC]|D-06" .planning/phases/67-bundle-barrel-optimization-d-04-unlock/67-02-FINAL-GATE.md` returns ≥1 match (branch outcome cited)
    - `.lighthouseci/lighthouserc.json` parses as valid JSON: `python3 -c "import json; json.load(open('.lighthouseci/lighthouserc.json'))"` exits 0
    - If §3 verdict tightened: `grep -c "maxNumericValue.*500" .lighthouseci/lighthouserc.json` ≥ 1 (TBT tightened)
    - If §3 verdict unchanged: `git diff .lighthouseci/lighthouserc.json` shows no changes since pre-Task-3 state
    - Git log shows final commit `Chore(67-02): 67-02-FINAL-GATE.md + LHCI`
  </acceptance_criteria>
  <done>
    Phase 67 final scorecard authored, LHCI threshold reviewed (and optionally tightened), all carry-forwards documented. Plan 02 ready for `/pde:verify-work`.
  </done>
</task>

</tasks>

<verification>
**Plan 02 close-out gates:**

1. **D-06 outcome ladder applied** — `tests/v1.8-phase63-1-bundle-budget.spec.ts` BUDGET_BYTES reflects measured branch:
   - Branch A: 200 KB; `_path_k_decision` retired entirely.
   - Branch B: 201-220 KB; `_path_k_decision` replaced by `_path_q_decision` (NOT extended).
   - Branch C: spec unchanged at 260 KB; escalation doc exists; phase closes with status `escalate-required`.

2. **Bundle-budget spec PASS** — `pnpm exec playwright test ./tests/v1.8-phase63-1-bundle-budget.spec.ts --reporter=list` exits 0 against the new BUDGET_BYTES (Branches A and B). Branch C: spec PASS-against-260-KB persists since unchanged.

3. **Final phase-end AES-04 PASS** (Branches A and B) — `tests/v1.9-phase67-aes04.spec.ts` produces 20/20 ≤0.5% PASS. No DIFF artifacts.

4. **D-10 stale-chunk discipline** — Every gating build in this plan prefixed with `rm -rf .next/cache .next` (verifiable via /tmp/67-build-final-plan02.log + /tmp/67-build-aes04-final.log).

5. **LHCI threshold review documented** — 67-02-FINAL-GATE.md §3 records `tightened` or `unchanged` verdict + rationale. `.lighthouseci/lighthouserc.json` either edited (if tightened) or untouched (if unchanged).

6. **FALSE-PASS GUARD section present** — 67-02-FINAL-GATE.md §4 cites build invocation, prod webpack vs Turbopack, AES-04 against `pnpm start` not `pnpm dev`.

7. **No regressions in adjacent invariants**:
   - `grep -c "autoResize: true" components/layout/lenis-provider.tsx` equals 1 (PF-04)
   - `grep -rn "ScrollSmoother" --include="*.ts" --include="*.tsx" lib/ components/ app/` returns 0 matches
   - `grep -c "@/components/sf" next.config.ts` equals 1 (Plan 01 commit not reverted)
</verification>

<success_criteria>
- BND-06 closed: homepage `/` First-Load JS gzip ≤200 KB OR documented outcome-ladder ratification (path_q with measured threshold) OR escalation path opened (Branch C).
- BND-07 closed: spec gate mutated (BUDGET_BYTES updated) + LHCI review recorded + new chunk-ID baseline already locked in `.planning/codebase/v1.9-bundle-reshape.md` (Plan 01).
- AES-04 final phase-end gate PASS (≤0.5% per page across 5 routes × 4 viewports) for Branches A and B.
- `_path_k_decision` retired (Branch A) or replaced with `_path_q_decision` (Branch B) — NEVER extended.
- D-10 BND-04 stale-chunk discipline honored on every gating build in this plan.
- ROADMAP §v1.9 rules 5/7/8 (no runtime deps / single-ticker / PF-04) preserved (no Plan 02 mutations touch lenis-provider.tsx or rAF call sites; spec + JSON config + doc only).
- 67-02-FINAL-GATE.md scorecard authored mirroring Phase 61 4-way + FALSE-PASS GUARD precedent.
</success_criteria>

<output>
After completion, create `.planning/phases/67-bundle-barrel-optimization-d-04-unlock/67-02-SUMMARY.md` recording:
- D-06 branch verdict (A_RETIRE / B_REPLACE_PATH_Q / C_ESCALATE) + measured homepage gzip
- BUDGET_BYTES new value (or unchanged for Branch C)
- AES-04 final-gate verdict (passed/failed counts)
- LHCI review verdict (tightened/unchanged) + rationale citation to FINAL-GATE.md §3
- Per-requirement close-out: BND-05 (Plan 01) / BND-06 (Plan 02 Task 1) / BND-07 (Plan 01 doc + Plan 02 spec/LHCI)
- v1.9 carry-forward hand-off: VRF-08 path_b_decision review_gate now fires; framework-chunk reshape becomes next-milestone scope (Branches B + C only)
</output>
