---
phase: 70-v1-8-verification-closure-vrf-01-04-05
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - .planning/perf-baselines/v1.9/rum-p75-lcp.json
  - .planning/phases/70-v1-8-verification-closure-vrf-01-04-05/70-CAPTURE-LOG.md
autonomous: false
requirements: [VRF-06, VRF-07]
nyquist_compliant: true

must_haves:
  truths:
    - "≥100 LCP samples captured in .planning/perf-baselines/v1.9/rum-p75-lcp.json within Hobby 1h log-retention window"
    - "p75 LCP <1000ms across all cohorts (VRF-06 PASS verdict)"
    - "iOS sub-cohort verdict is PASS (≥10 samples + median <2000ms) OR INSUFFICIENT_SAMPLES (deferred to natural-traffic accumulation)"
    - "sample_source labeled accurately (synthetic-seeded / mixed / natural) per actual capture conditions"
    - "Vercel tier decision recorded in CAPTURE-LOG.md (Hobby 1h cycle vs Pro 24h upgrade)"
    - "Prod deployment URL captured and used with --deployment flag (no branch-scope coupling)"
  artifacts:
    - path: ".planning/perf-baselines/v1.9/rum-p75-lcp.json"
      provides: "VRF-06 + VRF-07 evidence baseline; aggregator output from real prod /api/vitals beacons"
      contains: "vrf_07_ios_cohort"
    - path: ".planning/phases/70-v1-8-verification-closure-vrf-01-04-05/70-CAPTURE-LOG.md"
      provides: "Audit trail for capture cycle: tier decision, prod URL, window timing, sample-source verdict labeling, retry count if applicable"
      min_lines: 30
  key_links:
    - from: "scripts/v1.9-rum-aggregate.ts (Plan 01)"
      to: ".planning/perf-baselines/v1.9/rum-p75-lcp.json"
      via: "WINDOW_START + DEPLOYMENT_URL + SAMPLE_SOURCE env-driven script invocation"
      pattern: "WINDOW_START.*DEPLOYMENT_URL.*pnpm tsx scripts/v1.9-rum-aggregate.ts"
    - from: "scripts/v1.8-rum-seed-runner.mjs"
      to: "https://signalframeux.vercel.app/api/vitals"
      via: "PROD_URL env + Playwright chromium driving 105 sessions emitting LCP/CLS/INP/TTFB/FCP beacons"
      pattern: "PROD_URL=.*pnpm tsx scripts/v1.8-rum-seed-runner.mjs"

---

<threat_model>
**T-RUM-01 (LOW, mitigated by Phase 58) — DoS via log flooding**
Phase 58 `/api/vitals` route handler enforces 2KB payload cap + content-type gate; aggregator-side schema discipline already in place. No additional Plan 02 mitigation needed — this is documentation only.

**T-SEED-01 (LOW) — Synthetic seed traffic mislabeled as natural**
Seeder spoofs iPhone/Android/desktop UAs via Playwright; if `sample_source` is left as default "natural" in the output JSON, downstream consumers may interpret synthetic Playwright Chromium runs as real iOS Safari WebKit JIT cost (Pitfall #4 from RESEARCH §340-348).
**Mitigation:** Plan 02 Task 8 user-discretion checkpoint explicitly labels `sample_source` as "synthetic-seeded" (Hobby cycle) or "mixed" (1h window includes natural traffic). User confirms verdict labeling acceptable for v1.8 VRF-05 closure.
</threat_model>

<objective>
Execute the seed-and-aggregate-within-1h capture cycle on Vercel Hobby tier (or Pro tier if user upgrades). Capture VRF-06 verdict (≥100 LCP samples, p75 less than 1000ms) and VRF-07 iOS sub-cohort verdict (≥10 samples + median less than 2000ms OR INSUFFICIENT_SAMPLES). Maintain audit trail in CAPTURE-LOG.md including tier decision, prod URL, window timing, and sample-source labeling.

Purpose: Closes v1.8 VRF-05 deferral by exercising the existing RUM pipeline against fresh prod deploy that includes `app/api/vitals/route.ts`. Wall-clock-bound only on the seeder run (~3min for 105 sessions on Hobby).
Output: 1 baseline JSON + 1 audit log. Zero source-file edits, zero new runtime npm deps.
</objective>

<execution_context>
@/Users/greyaltaer/.claude/pde-os/engines/gsd/workflows/execute-plan.md
@/Users/greyaltaer/.claude/pde-os/engines/gsd/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/70-v1-8-verification-closure-vrf-01-04-05/70-RESEARCH.md
@.planning/phases/70-v1-8-verification-closure-vrf-01-04-05/70-VALIDATION.md
@.planning/perf-baselines/v1.8/vrf-05-rum-p75-lcp.json
@scripts/v1.8-rum-seed-runner.mjs
@scripts/v1.9-rum-aggregate.ts
@app/api/vitals/route.ts

<interfaces>
v1.9 aggregator script-mode env contract (Plan 01 output):
- DEPLOYMENT_URL env defaults to https://signalframeux.vercel.app
- SINCE env (regex /^[0-9]+(s|m|h|d)$/) defaults to 24h
- WINDOW_START env (ISO 8601) optional anchor filter
- SAMPLE_SOURCE env enum (natural|synthetic-seeded|mixed|deferred) defaults to natural
- Output: .planning/perf-baselines/v1.9/rum-p75-lcp.json

Seeder invocation contract:
- PROD_URL=https://signalframeux.vercel.app VISITS_PER_ROUTE=7 pnpm tsx scripts/v1.8-rum-seed-runner.mjs
- Output: .planning/perf-baselines/v1.8/vrf-05-rum-seed-log.json (existing v1.8 path; reuse OK)
- 5 routes × 3 viewports × 7 visits = 105 sessions; ~3min wall-clock

Vercel CLI requirements:
- vercel whoami MUST return account email (login state check)
- vercel ls --prod MUST list at least one production deployment
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: User-discretion — Vercel tier choice (Hobby 1h cycle vs Pro 24h upgrade)</name>
  <read_first>
    - 70-RESEARCH.md lines 14-16 (Hobby 1h retention vs Pro 1d retention tradeoff)
    - 70-VALIDATION.md Manual-Only Verifications first row
    - .planning/perf-baselines/v1.8/vrf-05-rum-p75-lcp.json v1_9_unblock_recipe field (existing precedent path)
  </read_first>
  <files>.planning/phases/70-v1-8-verification-closure-vrf-01-04-05/70-CAPTURE-LOG.md</files>
  <decision>Vercel tier strategy for VRF-06 sampling-window satisfaction</decision>
  <context>
    Vercel Hobby tier caps runtime log retention at 1 hour. The "≥24h sampling window" requirement in REQUIREMENTS.md VRF-06 cannot be satisfied via natural vercel logs queries on Hobby. Two viable paths.

    Path A (default, autonomous): Stay on Hobby + seed-and-aggregate within 1h cycle. Cost $0. Synthetic-seeded via scripts/v1.8-rum-seed-runner.mjs (105 sessions, ~525 beacons, ~3min wall-clock). Sample source labeled "synthetic-seeded" or "mixed". VRF-07 iOS verdict caveat: Playwright Chromium iPhone UA != real Safari WebKit JIT.

    Path B: Upgrade to Vercel Pro tier (~$20/mo). Natural 24h+ window. Real visitor traffic. Semantically correct VRF-07 iOS verdict. Out of autonomous scope (user account action).
  </context>
  <options>
    <option id="path-a-hobby">
      <name>Path A — Hobby + seed-and-aggregate within 1h (DEFAULT under --auto)</name>
      <pros>Zero cost; precedent path from v1.8 unblock recipe; ~3min wall-clock</pros>
      <cons>Synthetic seeding caveat on VRF-07 iOS cohort; sample_source labeled synthetic-seeded or mixed</cons>
    </option>
    <option id="path-b-pro">
      <name>Path B — Upgrade to Pro tier for natural 24h sampling</name>
      <pros>Real visitor traffic; semantically correct iOS verdict; long-term observability dividend</pros>
      <cons>$20/mo cost; out of autonomous scope; 24h+ wall-clock window</cons>
    </option>
  </options>
  <action>
    Create file `.planning/phases/70-v1-8-verification-closure-vrf-01-04-05/70-CAPTURE-LOG.md` with header section. Wait for user response, then record decision verbatim.

    Initial header content:

    ```
    # Phase 70 — Capture Log

    > Audit trail for VRF-06/07 capture cycle. Pairs with .planning/perf-baselines/v1.9/rum-p75-lcp.json.

    ## Tier Decision (Plan 02 Task 1)

    Decided: {ISO 8601 timestamp}
    Path: {path-a-hobby | path-b-pro}
    Rationale: {user response OR "default under --auto"}
    ```

    If user responds path-b-pro: pause, emit resume-signal requiring user to complete Vercel Pro upgrade in dashboard, confirm via vercel whoami showing Pro tier, type "upgraded" to resume. Plan execution then skips Task 4 (seeder) and runs Task 5 with SAMPLE_SOURCE=natural and SINCE=24h.

    If user responds path-a-hobby OR no response under --auto: proceed to Task 2 with default Hobby cycle.
  </action>
  <verify>
    <automated>test -f .planning/phases/70-v1-8-verification-closure-vrf-01-04-05/70-CAPTURE-LOG.md &amp;&amp; grep -qE "Path: (path-a-hobby|path-b-pro)" .planning/phases/70-v1-8-verification-closure-vrf-01-04-05/70-CAPTURE-LOG.md</automated>
  </verify>
  <resume-signal>Type `path-a-hobby`, `path-b-pro`, or `auto` (accepts default Path A)</resume-signal>
  <done>
    CAPTURE-LOG.md exists with tier decision recorded. Subsequent tasks branch on the recorded path.
  </done>
  <acceptance_criteria>
    - File `.planning/phases/70-v1-8-verification-closure-vrf-01-04-05/70-CAPTURE-LOG.md` exists
    - File contains `Path: path-a-hobby` OR `Path: path-b-pro`
    - File contains `Decided:` line with ISO 8601 timestamp
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Verify Vercel CLI auth + identify prod deployment URL</name>
  <read_first>
    - 70-CAPTURE-LOG.md (just created in Task 1)
    - 70-RESEARCH.md Vercel CLI 50.43.0 lines 101-121 (flag schema)
    - feedback_vercel_logs_branch_scope memory: aggregator MUST use --no-branch --deployment flags
  </read_first>
  <files>.planning/phases/70-v1-8-verification-closure-vrf-01-04-05/70-CAPTURE-LOG.md</files>
  <behavior>
    - vercel whoami returns the account email (proves auth state)
    - vercel ls --prod lists ≥1 production deployment; latest is captured as prod-url
    - Smoke-test confirms /api/vitals route is live (HTTP 200)
    - prod-url recorded in CAPTURE-LOG.md
  </behavior>
  <action>
    Run sequentially:

    ```
    # 1. Auth check; if "Not authenticated", instruct user to run vercel login then resume
    vercel whoami

    # 2. Identify latest prod deployment
    vercel ls --prod | head -10
    # Capture production alias URL (typically https://signalframeux.vercel.app)
    # If vercel ls --prod returns no deployments, ABORT — Phase 70 cannot proceed without fresh prod deploy

    # 3. Smoke test the /api/vitals route
    PROD_URL=https://signalframeux.vercel.app
    curl -sS -o /tmp/vitals-smoke.txt -w '%{http_code}\n' -X POST "$PROD_URL/api/vitals" \
      -H 'content-type: application/json' \
      -d '{"name":"LCP","value":500,"id":"phase70-smoke","rating":"good","navigationType":"navigate","url":"'"$PROD_URL"'/","timestamp":1700000000000}'
    # Expected: 200. If 404, prod deploy predates Phase 58 — ABORT and instruct user to redeploy.
    ```

    Append to CAPTURE-LOG.md:

    ```
    ## Auth + Prod URL (Plan 02 Task 2)

    vercel_whoami: {email returned}
    prod_url: {captured URL, e.g. https://signalframeux.vercel.app}
    smoke_test_status: {200 | 404 | other}
    smoke_test_timestamp: {ISO 8601}
    ```

    If smoke test returns 404: ABORT plan with message "Prod deploy at $PROD_URL does not include /api/vitals route. Redeploy main and resume."
  </action>
  <verify>
    <automated>grep -qE "smoke_test_status: 200" .planning/phases/70-v1-8-verification-closure-vrf-01-04-05/70-CAPTURE-LOG.md &amp;&amp; grep -qE "prod_url: https" .planning/phases/70-v1-8-verification-closure-vrf-01-04-05/70-CAPTURE-LOG.md</automated>
  </verify>
  <done>
    Vercel auth confirmed; prod URL identified; /api/vitals smoke test returned HTTP 200; all three values recorded in CAPTURE-LOG.md.
  </done>
  <acceptance_criteria>
    - CAPTURE-LOG.md contains `vercel_whoami:` line followed by an email
    - CAPTURE-LOG.md contains `prod_url:` line followed by an https:// URL
    - CAPTURE-LOG.md contains `smoke_test_status: 200`
    - CAPTURE-LOG.md contains `smoke_test_timestamp:` with ISO 8601 value
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Anchor capture window (record WINDOW_START)</name>
  <read_first>
    - 70-CAPTURE-LOG.md (Task 2 just appended prod URL)
    - 70-RESEARCH.md Pattern: Window-Start Anchor + 1h Retention Discipline lines 264-283
  </read_first>
  <files>.planning/phases/70-v1-8-verification-closure-vrf-01-04-05/70-CAPTURE-LOG.md</files>
  <behavior>
    Capture an ISO 8601 UTC timestamp marking the start of the seed-and-aggregate window. Aggregator (Task 5) uses this as WINDOW_START to filter only beacons emitted after seeder kickoff.
  </behavior>
  <action>
    ```
    WINDOW_START=$(date -u +%Y-%m-%dT%H:%M:%SZ)
    echo "Captured WINDOW_START=$WINDOW_START"
    ```

    Append to CAPTURE-LOG.md:

    ```
    ## Window Anchor (Plan 02 Task 3)

    window_start: {ISO 8601 UTC, e.g. 2026-04-30T18:30:00Z}
    retention_window_minutes: 60 (Hobby) | 1440 (Pro)
    expiry_estimate: {window_start + retention_window} — aggregator MUST run before this expiry
    ```
  </action>
  <verify>
    <automated>grep -qE "window_start: 20[0-9]{2}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}Z" .planning/phases/70-v1-8-verification-closure-vrf-01-04-05/70-CAPTURE-LOG.md</automated>
  </verify>
  <done>
    window_start recorded as ISO 8601 UTC string in CAPTURE-LOG.md. Aggregator (Task 5) will pass this as WINDOW_START env var.
  </done>
  <acceptance_criteria>
    - CAPTURE-LOG.md contains `window_start:` line with ISO 8601 UTC value matching pattern `YYYY-MM-DDTHH:MM:SSZ`
    - CAPTURE-LOG.md contains `retention_window_minutes:` line
    - CAPTURE-LOG.md contains `expiry_estimate:` line
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Seed prod /api/vitals with 105 synthetic sessions (Hobby cycle only)</name>
  <read_first>
    - scripts/v1.8-rum-seed-runner.mjs (READ ENTIRELY — invocation contract + UA spoofing logic + session driver)
    - 70-CAPTURE-LOG.md (Task 3 just appended window_start)
    - 70-RESEARCH.md Plan 02 sequence lines 188-215
    - 70-RESEARCH.md Pitfall 4 (Synthetic-Seed UA Spoofing != Real iPhone) lines 340-348
  </read_first>
  <files>.planning/perf-baselines/v1.8/vrf-05-rum-seed-log.json</files>
  <behavior>
    - If tier decision was path-b-pro: SKIP this task entirely; advance to Task 5 with SAMPLE_SOURCE=natural and SINCE=24h
    - If tier decision was path-a-hobby (default): Invoke seeder against prod URL; expect 105 sessions, ~3min wall-clock, ~525 beacons emitted to /api/vitals; seed log written to .planning/perf-baselines/v1.8/vrf-05-rum-seed-log.json
    - Wait 30s after seeder completes for sendBeacon flush + Vercel log ingestion
  </behavior>
  <action>
    Read 70-CAPTURE-LOG.md and check the Path field:

    ```
    if grep -q "Path: path-b-pro" .planning/phases/70-v1-8-verification-closure-vrf-01-04-05/70-CAPTURE-LOG.md; then
      echo "Path B (Pro tier) — skipping synthetic seed; advancing to Task 5 with SAMPLE_SOURCE=natural"
      exit 0
    fi

    # Path A (Hobby cycle) — run seeder
    PROD_URL=https://signalframeux.vercel.app \
      VISITS_PER_ROUTE=7 \
      pnpm tsx scripts/v1.8-rum-seed-runner.mjs
    # Expected output: "v1.8-rum-seed: wrote .planning/perf-baselines/v1.8/vrf-05-rum-seed-log.json"
    # Expected: 100+ ok / 0-5 fail (Vercel cold-start may fail first 1-2 sessions)

    # Settle for sendBeacon flush
    sleep 30
    ```

    Append to CAPTURE-LOG.md:

    ```
    ## Synthetic Seed (Plan 02 Task 4)

    executed: {true if Path A; false if Path B with reason "Pro tier — natural sampling"}
    seeder_started_at: {from vrf-05-rum-seed-log.json startedAt field}
    seeder_finished_at: {from vrf-05-rum-seed-log.json finishedAt field}
    expected_sessions: 105
    successful_sessions: {from vrf-05-rum-seed-log.json successful_sessions}
    failed_sessions: {from vrf-05-rum-seed-log.json failed_sessions}
    estimated_beacons: {successful_sessions * 5}
    ```
  </action>
  <verify>
    <automated>grep -q "Path: path-b-pro" .planning/phases/70-v1-8-verification-closure-vrf-01-04-05/70-CAPTURE-LOG.md || (jq -e ".successful_sessions &gt;= 100" .planning/perf-baselines/v1.8/vrf-05-rum-seed-log.json &amp;&amp; grep -qE "successful_sessions: [0-9]+" .planning/phases/70-v1-8-verification-closure-vrf-01-04-05/70-CAPTURE-LOG.md)</automated>
  </verify>
  <done>
    Path A: seeder produced ≥100 successful sessions; ~525 beacons emitted to /api/vitals; 30s settle elapsed; counts recorded in CAPTURE-LOG.md.
    Path B: task skipped; seeder not invoked; advance to Task 5.
  </done>
  <acceptance_criteria>
    - Path A: `.planning/perf-baselines/v1.8/vrf-05-rum-seed-log.json` updated with `successful_sessions >= 100`
    - Path A: CAPTURE-LOG.md contains `successful_sessions:` line with numeric value ≥100
    - Path B: CAPTURE-LOG.md contains `executed: false` with reason text mentioning "Pro tier"
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 5: Run v1.9 aggregator against prod deployment</name>
  <read_first>
    - scripts/v1.9-rum-aggregate.ts (Plan 01 output — READ ENTIRELY for env contract)
    - 70-CAPTURE-LOG.md (Tasks 2-4 populated prod_url, window_start, executed status)
    - 70-RESEARCH.md Pitfall 1 (Branch-Coupled Log Query) lines 311-317
    - 70-RESEARCH.md Pitfall 2 (Hobby 1h Retention Window Expires Mid-Aggregation) lines 319-323
  </read_first>
  <files>.planning/perf-baselines/v1.9/rum-p75-lcp.json</files>
  <behavior>
    - Aggregator pulls vercel logs with --no-branch --deployment flags (T-RUM-03 mitigation)
    - Filters to /api/vitals POST entries within WINDOW_START + SINCE bounds
    - Writes .planning/perf-baselines/v1.9/rum-p75-lcp.json with full schema (capturedAt, window_start, window_end, sample_count, sample_count_lcp, by_metric, by_viewport, vrf_07_ios_cohort, thresholds, sample_source, verdict)
    - Path A (Hobby): SINCE=70m, SAMPLE_SOURCE=synthetic-seeded
    - Path B (Pro): SINCE=24h, SAMPLE_SOURCE=natural
  </behavior>
  <action>
    Read CAPTURE-LOG.md to extract prod_url and window_start. Branch on Path:

    ```
    PROD_URL=$(grep "prod_url:" .planning/phases/70-v1-8-verification-closure-vrf-01-04-05/70-CAPTURE-LOG.md | awk '{print $2}')
    WINDOW_START=$(grep "window_start:" .planning/phases/70-v1-8-verification-closure-vrf-01-04-05/70-CAPTURE-LOG.md | awk '{print $2}')

    if grep -q "Path: path-b-pro" .planning/phases/70-v1-8-verification-closure-vrf-01-04-05/70-CAPTURE-LOG.md; then
      SINCE_VAL=24h
      SOURCE_VAL=natural
    else
      SINCE_VAL=70m
      SOURCE_VAL=synthetic-seeded
    fi

    DEPLOYMENT_URL=$PROD_URL \
      WINDOW_START=$WINDOW_START \
      SINCE=$SINCE_VAL \
      SAMPLE_SOURCE=$SOURCE_VAL \
      pnpm tsx scripts/v1.9-rum-aggregate.ts
    # Expected output: "Sample count: 100+ (LCP: 100+); verdict: PASS"
    # Expected file written: .planning/perf-baselines/v1.9/rum-p75-lcp.json
    # Expected runtime: ~5-30 seconds (vercel logs query + parse + write)
    ```

    If aggregator runtime exceeds 5min: ABORT (something is hung — check vercel logs CLI version, network, deployment URL spelling).

    If aggregator exits non-zero with verdict FAIL_OR_INSUFFICIENT and sample_count_lcp < 100: proceed to Task 6 (retry seed) up to once.
  </action>
  <verify>
    <automated>test -f .planning/perf-baselines/v1.9/rum-p75-lcp.json &amp;&amp; jq -e ".sample_count_lcp" .planning/perf-baselines/v1.9/rum-p75-lcp.json</automated>
  </verify>
  <done>
    .planning/perf-baselines/v1.9/rum-p75-lcp.json exists with valid JSON schema. sample_count_lcp field is present (verdict may be PASS or FAIL_OR_INSUFFICIENT — verdict gating happens in Task 7).
  </done>
  <acceptance_criteria>
    - File `.planning/perf-baselines/v1.9/rum-p75-lcp.json` exists
    - `jq -e ".sample_count_lcp" .planning/perf-baselines/v1.9/rum-p75-lcp.json` exits 0
    - `jq -e ".by_metric.LCP.p75" .planning/perf-baselines/v1.9/rum-p75-lcp.json` exits 0
    - `jq -e ".vrf_07_ios_cohort.verdict" .planning/perf-baselines/v1.9/rum-p75-lcp.json` exits 0
    - `jq -e ".sample_source" .planning/perf-baselines/v1.9/rum-p75-lcp.json` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 6: Verify VRF-06 gate (≥100 LCP samples + p75 LCP <1000ms)</name>
  <read_first>
    - .planning/perf-baselines/v1.9/rum-p75-lcp.json (Task 5 just wrote)
    - 70-RESEARCH.md Plan 02 verdict flowchart lines 219-223
    - 70-VALIDATION.md rows 70-02-sessions and 70-02-p75
  </read_first>
  <files>.planning/phases/70-v1-8-verification-closure-vrf-01-04-05/70-CAPTURE-LOG.md</files>
  <behavior>
    - VRF-06 PASS requires: sample_count_lcp ≥ 100 AND by_metric.LCP.p75 < 1000ms
    - If sample_count_lcp < 100: retry seed cycle once (Path A only — re-run Task 4 with VISITS_PER_ROUTE=10 then Task 5), then re-evaluate
    - If sample_count_lcp ≥ 100 but p75 ≥ 1000ms: VRF-06 FAIL — escalate to user (do NOT propose path_decision; v1.9 perf budget should genuinely be ≤1000ms post-Phase-67 chunk-graph reshape)
  </behavior>
  <action>
    ```
    SAMPLE_COUNT=$(jq -r ".sample_count_lcp" .planning/perf-baselines/v1.9/rum-p75-lcp.json)
    P75=$(jq -r ".by_metric.LCP.p75" .planning/perf-baselines/v1.9/rum-p75-lcp.json)

    if [ "$SAMPLE_COUNT" -lt 100 ]; then
      # Retry up to once (Path A only)
      if grep -q "Path: path-a-hobby" .planning/phases/70-v1-8-verification-closure-vrf-01-04-05/70-CAPTURE-LOG.md && ! grep -q "retry_count:" .planning/phases/70-v1-8-verification-closure-vrf-01-04-05/70-CAPTURE-LOG.md; then
        echo "VRF-06 INSUFFICIENT_SAMPLES (n=$SAMPLE_COUNT) — retrying seed cycle once with VISITS_PER_ROUTE=10"
        # Re-anchor window_start, re-seed, re-aggregate
        # Append "retry_count: 1" to CAPTURE-LOG.md before retry to prevent infinite loop
        # ... (redo Task 3 + Task 4 + Task 5)
      else
        echo "VRF-06 FAIL_OR_INSUFFICIENT after retry — escalate to user"
        exit 1
      fi
    fi

    jq -e ".sample_count_lcp >= 100 and .by_metric.LCP.p75 < 1000" .planning/perf-baselines/v1.9/rum-p75-lcp.json
    ```

    Append to CAPTURE-LOG.md:

    ```
    ## VRF-06 Verdict (Plan 02 Task 6)

    sample_count_lcp: {N}
    p75_lcp_ms: {value}
    threshold_p75_ms: 1000
    threshold_sample_min: 100
    verdict: {PASS | FAIL_OR_INSUFFICIENT}
    retry_count: {0 | 1}
    ```
  </action>
  <verify>
    <automated>jq -e ".sample_count_lcp &gt;= 100 and .by_metric.LCP.p75 &lt; 1000" .planning/perf-baselines/v1.9/rum-p75-lcp.json</automated>
  </verify>
  <done>
    VRF-06 PASS verdict in JSON output (sample_count_lcp ≥ 100 AND p75 LCP < 1000ms). Verdict + thresholds + retry_count recorded in CAPTURE-LOG.md.
  </done>
  <acceptance_criteria>
    - `jq -e ".sample_count_lcp >= 100" .planning/perf-baselines/v1.9/rum-p75-lcp.json` exits 0
    - `jq -e ".by_metric.LCP.p75 < 1000" .planning/perf-baselines/v1.9/rum-p75-lcp.json` exits 0
    - `jq -e ".verdict == \"PASS\"" .planning/perf-baselines/v1.9/rum-p75-lcp.json` exits 0
    - CAPTURE-LOG.md contains `verdict: PASS`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 7: Verify VRF-07 iOS cohort gate</name>
  <read_first>
    - .planning/perf-baselines/v1.9/rum-p75-lcp.json (Task 5 + 6 wrote/validated)
    - 70-RESEARCH.md Plan 02 iOS verdict flowchart lines 225-229
    - 70-RESEARCH.md Pitfall 4 lines 340-348 (synthetic UA spoofing caveat)
    - 70-VALIDATION.md row 70-02-ios-cohort
  </read_first>
  <files>.planning/phases/70-v1-8-verification-closure-vrf-01-04-05/70-CAPTURE-LOG.md</files>
  <behavior>
    - Acceptable VRF-07 verdicts: PASS (≥10 iOS samples + median <2000ms) OR INSUFFICIENT_SAMPLES (deferred to natural-traffic accumulation; not a phase failure)
    - FAIL verdict (≥10 samples + median ≥2000ms) requires path_decision escalation — out of Plan 02 autonomous scope
  </behavior>
  <action>
    ```
    IOS_VERDICT=$(jq -r ".vrf_07_ios_cohort.verdict" .planning/perf-baselines/v1.9/rum-p75-lcp.json)
    IOS_COUNT=$(jq -r ".vrf_07_ios_cohort.count_lcp" .planning/perf-baselines/v1.9/rum-p75-lcp.json)
    IOS_MEDIAN=$(jq -r ".vrf_07_ios_cohort.median_lcp_ms" .planning/perf-baselines/v1.9/rum-p75-lcp.json)

    if [ "$IOS_VERDICT" = "PASS" ]; then
      echo "VRF-07 PASS: iOS cohort n=$IOS_COUNT, median=${IOS_MEDIAN}ms"
    elif [ "$IOS_VERDICT" = "INSUFFICIENT_SAMPLES" ]; then
      echo "VRF-07 INSUFFICIENT_SAMPLES: iOS cohort n=$IOS_COUNT (deferred to natural traffic)"
    elif [ "$IOS_VERDICT" = "FAIL" ]; then
      echo "VRF-07 FAIL: iOS cohort n=$IOS_COUNT, median=${IOS_MEDIAN}ms exceeds 2000ms — ESCALATE to user (path_decision needed)"
      exit 1
    fi
    ```

    Append to CAPTURE-LOG.md:

    ```
    ## VRF-07 iOS Cohort Verdict (Plan 02 Task 7)

    ios_count_lcp: {N}
    ios_median_lcp_ms: {value}
    threshold_median_ms: 2000
    threshold_count_min: 10
    verdict: {PASS | INSUFFICIENT_SAMPLES | FAIL}
    note: {if INSUFFICIENT_SAMPLES — "Deferred to natural-traffic accumulation. Not a phase failure per RESEARCH §490."}
    ```
  </action>
  <verify>
    <automated>jq -e ".vrf_07_ios_cohort.verdict | test(\"PASS|INSUFFICIENT_SAMPLES\")" .planning/perf-baselines/v1.9/rum-p75-lcp.json</automated>
  </verify>
  <done>
    VRF-07 verdict is PASS or INSUFFICIENT_SAMPLES (FAIL path escalates to user). Verdict + cohort details recorded in CAPTURE-LOG.md.
  </done>
  <acceptance_criteria>
    - `jq -e ".vrf_07_ios_cohort.verdict == \"PASS\" or .vrf_07_ios_cohort.verdict == \"INSUFFICIENT_SAMPLES\"" .planning/perf-baselines/v1.9/rum-p75-lcp.json` exits 0
    - CAPTURE-LOG.md contains `ios_count_lcp:` line with numeric value
    - CAPTURE-LOG.md contains `verdict: PASS` OR `verdict: INSUFFICIENT_SAMPLES` in the VRF-07 section
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 8: User-discretion — confirm sample_source verdict labeling acceptable</name>
  <read_first>
    - .planning/perf-baselines/v1.9/rum-p75-lcp.json (Tasks 5-7 produced)
    - 70-CAPTURE-LOG.md (Tasks 1-7 populated)
    - 70-VALIDATION.md Manual-Only Verifications second + third rows
    - 70-RESEARCH.md Open Question 1 lines 472-475 (sample_source labeling guidance)
  </read_first>
  <files>.planning/phases/70-v1-8-verification-closure-vrf-01-04-05/70-CAPTURE-LOG.md</files>
  <what-built>
    Plan 02 Tasks 1-7 complete: Vercel tier decided + prod URL captured + capture window anchored + (Path A only) 105 synthetic sessions seeded + aggregator run with --no-branch + VRF-06 PASS + VRF-07 PASS or INSUFFICIENT_SAMPLES. Two artifacts produced: `.planning/perf-baselines/v1.9/rum-p75-lcp.json` (the baseline) and `.planning/phases/70-v1-8-verification-closure-vrf-01-04-05/70-CAPTURE-LOG.md` (the audit trail).
  </what-built>
  <how-to-verify>
    1. Read the captured baseline:

    ```
    jq '.' .planning/perf-baselines/v1.9/rum-p75-lcp.json
    ```

    2. Confirm `sample_source` field accurately reflects the actual capture conditions:
       - "synthetic-seeded" — Path A Hobby cycle, only seeder traffic in window
       - "mixed" — Path A Hobby cycle, seeder traffic + organic visitors landed in 1h window
       - "natural" — Path B Pro tier, only organic visitors

    3. If sample_source needs correction (e.g., aggregator defaulted to "natural" but Path A was used): manually edit the JSON, set the correct value, document the correction in CAPTURE-LOG.md.

    4. Confirm VRF-07 iOS verdict caveat is acceptable:
       - If PASS via synthetic seeding: caveat is "platform reachability proxy" not "real iOS performance" — acceptable for v1.8 VRF-05 closure since Phase 70 is measurement-bound, not implementation-bound
       - If INSUFFICIENT_SAMPLES: deferred to natural-traffic accumulation, not a phase failure
       - If you want stronger evidence: schedule a recurring synthetic-seed cron OR upgrade to Pro tier for natural sampling (out of Phase 70 scope)

    5. Type "approved" to record acceptance and unblock Wave 3 (Plan 04).
  </how-to-verify>
  <action>
    Append to CAPTURE-LOG.md:

    ```
    ## Sample-Source Verdict Labeling (Plan 02 Task 8)

    sample_source: {synthetic-seeded | mixed | natural}
    user_acknowledged_caveat: {true | false}
    user_approval_timestamp: {ISO 8601}
    user_notes: {free-form user notes if any}
    ```
  </action>
  <verify>
    <automated>grep -qE "user_acknowledged_caveat: true" .planning/phases/70-v1-8-verification-closure-vrf-01-04-05/70-CAPTURE-LOG.md &amp;&amp; jq -e ".sample_source | test(\"synthetic-seeded|mixed|natural\")" .planning/perf-baselines/v1.9/rum-p75-lcp.json</automated>
  </verify>
  <resume-signal>Type "approved" to accept verdict labeling, or describe any corrections needed</resume-signal>
  <done>
    User confirmed sample_source labeling is accurate; VRF-07 caveat (synthetic vs natural) explicitly acknowledged. Plan 04 unblocked.
  </done>
  <acceptance_criteria>
    - CAPTURE-LOG.md contains `user_acknowledged_caveat: true`
    - CAPTURE-LOG.md contains `user_approval_timestamp:` with ISO 8601 value
    - JSON `sample_source` field matches one of: synthetic-seeded, mixed, natural
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `.planning/perf-baselines/v1.9/rum-p75-lcp.json` exists with valid schema
- VRF-06 verdict PASS (sample_count_lcp ≥ 100 AND p75 LCP < 1000ms)
- VRF-07 iOS cohort verdict PASS or INSUFFICIENT_SAMPLES (FAIL escalates to user)
- sample_source labeled accurately and user-acknowledged
- CAPTURE-LOG.md records full audit trail (tier decision, prod URL, smoke test, window anchor, seed counts if applicable, both verdicts, user approval)
- Zero source-file edits; zero new runtime npm deps
- Phase 67 chunk-graph contract intact (no next.config.ts modifications)
</verification>

<success_criteria>
- 2 new files committed: `.planning/perf-baselines/v1.9/rum-p75-lcp.json` + `.planning/phases/70-v1-8-verification-closure-vrf-01-04-05/70-CAPTURE-LOG.md`
- VRF-06 PASS verdict captured in baseline JSON
- VRF-07 PASS or INSUFFICIENT_SAMPLES verdict captured in baseline JSON
- User-discretion checkpoints recorded for tier decision (Task 1) and sample-source labeling (Task 8)
- Plan 04 unblocked (Wave 3 closure)
</success_criteria>

<output>
After completion, create `.planning/phases/70-v1-8-verification-closure-vrf-01-04-05/70-02-SUMMARY.md` recording:
- Tier path executed (Path A Hobby vs Path B Pro)
- Prod URL + smoke test result
- Window anchor timestamp + retention bound
- Seeder counts (if Path A) — successful_sessions, failed_sessions, estimated_beacons
- Aggregator output: sample_count_lcp, p75 LCP, sample_source, verdict
- iOS cohort: count, median, verdict
- User approvals (timestamps for Task 1 + Task 8)
- Retry count (0 or 1)
</output>
