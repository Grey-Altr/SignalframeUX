---
phase: 70-v1-8-verification-closure-vrf-01-04-05
plan: 03
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - .planning/perf-baselines/v1.9/vrf-08-path-b-decision.json
autonomous: true
requirements: [VRF-08]
nyquist_compliant: true

must_haves:
  truths:
    - "VRF-08 ratification recorded as `_path_b_decision` block per the project's standard schema"
    - "All 7 schema fields present: decided / audit / original / new / rationale / evidence / review_gate"
    - "Moto G Power 3G Fast formally moved to 'supported but not gated' tier with full rationale"
    - "review_gate field cross-references Phase 67 BND-05/06/07 (chunk-graph reshape gate that, if successful, retires this decision)"
    - "evidence field references concrete artifacts (v1.8-lcp-diagnosis.md, project_phase63_1_checkpoint, ROADMAP §v1.9 rule 2, vrf-05-rum-p75-lcp.json deferral chain)"
    - "Zero source-file edits; framework-chunk reshape NOT attempted (Phase 67 conflict per RESEARCH §VRF-08)"
  artifacts:
    - path: ".planning/perf-baselines/v1.9/vrf-08-path-b-decision.json"
      provides: "VRF-08 path_b_decision ratification block (schema-locked, 7 required fields, JSON-parseable)"
      contains: "review_gate"
      min_lines: 30
  key_links:
    - from: ".planning/perf-baselines/v1.9/vrf-08-path-b-decision.json"
      to: "Phase 67 (BND-05/06/07 chunk-graph reshape)"
      via: "review_gate field text containing 'Phase 67' literal — re-measurement trigger documented"
      pattern: "review_gate.*Phase 67"
    - from: ".planning/perf-baselines/v1.9/vrf-08-path-b-decision.json"
      to: ".planning/perf-baselines/v1.8/vrf-05-rum-p75-lcp.json + project_phase63_1_checkpoint"
      via: "evidence array referencing concrete v1.8 deferral artifacts and Phase 63.1 framework-chunk diagnosis"
      pattern: "evidence.*v1.8-lcp-diagnosis"

---

<threat_model>
**T-PATH-01 (LOW) — Decision-block tampering**
Adversary edits `vrf-08-path-b-decision.json` post-merge to silently weaken the ratification (e.g., remove review_gate, alter rationale, blur tier-move language). The schema represents a binding policy decision per `feedback_path_b_pattern.md`; tampering would invalidate the audit trail.
**Mitigation:** (a) Plan 04 grep gate asserts ≥3 Validated rows in REQUIREMENTS.md trace table cross-referencing this file; (b) commit signing via existing branch-protection ruleset (Phase 64 CRT-05 cohort); (c) schema validation one-line `node -e` check (acceptance criteria below) catches any field deletion.
</threat_model>

<objective>
Author `.planning/perf-baselines/v1.9/vrf-08-path-b-decision.json` ratifying the formal move of Moto G Power 3G Fast LCP gate to "supported but not gated" tier. The block is a JSON document conforming to the standard `_path_X_decision` schema (Phase 60 path_a + Phase 62 path_b precedents per `feedback_path_b_pattern.md`). All 7 required fields populated with concrete cross-references. Path B (this decision block) is mandated by RESEARCH §VRF-08 — Path A (framework chunk reshape) is structurally invalid because Phase 67 owns chunk-graph mutation and v1.9 build-order rule 2 forbids parallel mutations.

Purpose: Closes v1.8 VRF-01 last device profile (Moto G Power 3G Fast) without invalidating Phase 67's BND-05/06/07 chunk-graph baseline. Pure-document plan — no source-file edits, no scripts, no test changes.
Output: 1 new JSON file. Zero source-file edits. Zero new npm deps. Parallel-safe with Plan 02.
</objective>

<execution_context>
@/Users/greyaltaer/.claude/pde-os/engines/gsd/workflows/execute-plan.md
@/Users/greyaltaer/.claude/pde-os/engines/gsd/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/70-v1-8-verification-closure-vrf-01-04-05/70-RESEARCH.md
@.planning/phases/70-v1-8-verification-closure-vrf-01-04-05/70-VALIDATION.md
@.planning/perf-baselines/v1.8/vrf-05-rum-p75-lcp.json
@.planning/perf-baselines/v1.8/MID-MILESTONE-CHECKPOINT.md

<interfaces>
The `_path_X_decision` schema (from `feedback_path_b_pattern.md` and Phase 60/62 precedent):

```typescript
interface PathDecisionBlock {
  decided: string;       // ISO 8601 date — when the decision was ratified
  audit: string;         // What original spec said + which audit step is invoking the decision
  original: string;      // The original gating threshold or behavior being changed
  new: string;           // The new policy — concrete posture replacing original
  rationale: string;     // Why the change is justified — multi-paragraph permitted; concrete tradeoffs
  evidence: string[];    // Array of file paths / memory references / measurement artifacts
  review_gate: string;   // Future event/measurement that would re-open the decision
}
```

Phase 60 + Phase 62 precedents (location: `.planning/perf-baselines/v1.8/MID-MILESTONE-CHECKPOINT.md`):
- Phase 60 path_a: ratified LHCI threshold loosening from 0 to 0.005 (Wave 3 CLS regression context)
- Phase 62 path_b: ratified VRF-02 Catchpoint n=3 verdict under variance band

This plan extends that precedent with Phase 70 path_b (VRF-08).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author vrf-08-path-b-decision.json with all 7 schema fields</name>
  <read_first>
    - .planning/perf-baselines/v1.8/MID-MILESTONE-CHECKPOINT.md (READ ENTIRELY — Phase 60 path_a + Phase 62 path_b are the precedent schema)
    - .planning/perf-baselines/v1.8/vrf-05-rum-p75-lcp.json (existing v1.8 deferral chain — referenced in evidence)
    - 70-RESEARCH.md §Plan 03 lines 231-262 (verbatim YAML for the decision block)
    - 70-RESEARCH.md §Pitfall 6 (Phase 67 collision) lines 358-362 (mandates Path B)
    - 70-VALIDATION.md rows 70-03-pathb-schema and 70-03-phase67-xref
  </read_first>
  <files>.planning/perf-baselines/v1.9/vrf-08-path-b-decision.json</files>
  <behavior>
    File is a JSON document with 7 top-level fields matching the `_path_X_decision` schema. All fields are non-empty strings (or array for `evidence`). The `review_gate` field contains the literal string "Phase 67" so the cross-reference grep gate (Plan 04 + 70-VALIDATION row 70-03-phase67-xref) succeeds.
  </behavior>
  <action>
    Create `.planning/perf-baselines/v1.9/vrf-08-path-b-decision.json` with the following exact JSON content. The directory `.planning/perf-baselines/v1.9/` already exists (Plan 01 Task 4 created the .gitkeep).

    ```json
    {
      "decided": "2026-04-30",
      "audit": "Phase 70 Plan 03 Task 1 — VRF-08 ratification audit. Original VRF-08 spec (REQUIREMENTS.md line 45) offered two paths: (a) framework chunk 2979 reduced via App Router config tweak with median <2000ms retest, OR (b) 3G Fast formally moved to 'supported but not gated' tier via _path_decision block. Path A is structurally invalid because Phase 67 (BND-05/06/07) owns chunk-graph mutation per ROADMAP §v1.9 build-order rule 2 (Phase 67 NOT parallel-safe with Phase 70 if Phase 70 reshapes chunks). Path B is the only correct posture.",
      "original": "Moto G Power 3G Fast LCP gated at <2000ms median strict (n>=3) as part of v1.8 VRF-01 device-matrix verification. v1.8 measurement: 3605ms LCP at 1867ms TBT — chunk 2979 attribution dominates the budget.",
      "new": "Moto G Power 3G Fast profile formally moved to 'supported but not gated' tier. LCP measurement is preserved as informational signal; failure does NOT block phase or milestone closure. The existing 4G LTE iPhone gate (VRF-07 / VRF-01-iOS) remains the cellular performance gate; LAN/WiFi desktop+tablet gates remain unchanged. 3G Fast becomes the floor of supported network conditions, with best-effort performance posture.",
      "rationale": "Phase 63.1 COHORT diagnosed Moto G Power 3G Fast LCP=3605ms as dominated by chunk 2979 (Next.js App Router framework runtime: ClientSegmentRoot + AppRouterAnnouncer + HeadManagerContext + server-action) consuming 1867ms TBT — 53% of total budget. This cost is intrinsic to Next.js 15.x App Router runtime architecture, not application code. Phase 67 (BND-05/06/07) explicitly owns chunk-graph reshape for v1.9; Phase 70 modifying splitChunks in parallel creates an unsafe collision per ROADMAP.md §v1.9 build-order rule 2 ('Phase 67 NOT parallel-safe with Phase 66; both touch the rendered output and chunk graph'). The audience for a portfolio-tier design system site is fiber/4G LTE/5G; 3G Fast represents <2% of real production traffic on signalframeux.vercel.app. Tier-move is the correct posture pending Phase 67's BND-05 unlock — and even then, framework-chunk reduction may not bring 3G Fast under 2000ms because the cost is largely intrinsic to App Router. Reverting tier on optimistic Phase 67 outcome is the documented review_gate path.",
      "evidence": [
        ".planning/perf-baselines/v1.8/vrf-05-rum-p75-lcp.json (v1.8 VRF-05 deferral chain documenting Hobby retention + branch-scope + 3G Fast carry-over)",
        ".planning/codebase/v1.8-lcp-diagnosis.md §2 (chunk 2979 attribution: 168 KB / 56 KB gzipped, 1867ms TBT on Moto G Power 3G Fast)",
        ".planning/phases/63.1-lcp-fast-path-remediation/63.1-COHORT.md (Phase 63.1 COHORT framework-chunk diagnosis + deferral to Phase 64+)",
        "memory: project_phase63_1_checkpoint.md (1867ms TBT measurement against chunk 2979)",
        "memory: feedback_path_b_pattern.md (_path_X_decision block schema used by Phase 60 path_a + Phase 62 path_b)",
        ".planning/ROADMAP.md §v1.9 Build-Order Constraints rule 2 (Phase 67 NOT parallel-safe with chunk-graph mutations)",
        ".planning/REQUIREMENTS.md §v1.9 BND-05/06/07 (Phase 67 chunk-graph ownership)",
        ".planning/perf-baselines/v1.8/MID-MILESTONE-CHECKPOINT.md (Phase 60 path_a + Phase 62 path_b precedent for _path_X_decision schema)"
      ],
      "review_gate": "After Phase 67 BND-05/BND-06/BND-07 ships and chunk-graph is re-locked at new chunk IDs, re-run Moto G Power 3G Fast LCP measurement (n>=3 via Catchpoint or WPT). If chunk reshape reduces TBT by >50% on the framework chunk and median LCP drops below 2000ms, this _path_b_decision is invalidated and 3G Fast returns to gated tier. If LCP remains >=2000ms after Phase 67's full chunk-graph reshape, the tier decision stands permanently and 3G Fast is documented in PROJECT.md as supported-but-not-gated for the lifetime of the App Router architecture choice."
    }
    ```

    Notes:
    - JSON (not YAML) for direct programmatic consumption by `node -e` schema check + `jq` cross-reference assertion.
    - `decided` is `2026-04-30` matching today's date.
    - `evidence` is an array of strings (each a file path or memory reference), not a single concatenated string.
    - `rationale` is a single multi-paragraph string (escape newlines OR keep as one paragraph); a single paragraph is acceptable since JSON does not need YAML's `|` literal block syntax.
    - `review_gate` MUST contain the literal string "Phase 67" so the grep cross-reference test passes.
  </action>
  <verify>
    <automated>node -e "const d=require('./.planning/perf-baselines/v1.9/vrf-08-path-b-decision.json'); for (const k of ['decided','audit','original','new','rationale','evidence','review_gate']) { if (!d[k]) throw new Error('missing field: '+k); } if (!Array.isArray(d.evidence) || d.evidence.length &lt; 3) throw new Error('evidence must be array with &gt;=3 entries'); if (!/Phase 67/.test(d.review_gate)) throw new Error('review_gate must reference Phase 67'); console.log('OK');"</automated>
  </verify>
  <done>
    File `.planning/perf-baselines/v1.9/vrf-08-path-b-decision.json` exists, parses as JSON, contains all 7 required schema fields, evidence array has ≥3 entries, review_gate references "Phase 67". Schema-validation one-liner exits 0.
  </done>
  <acceptance_criteria>
    - File `.planning/perf-baselines/v1.9/vrf-08-path-b-decision.json` exists
    - `node -e "JSON.parse(require('fs').readFileSync('.planning/perf-baselines/v1.9/vrf-08-path-b-decision.json','utf8'))"` exits 0 (valid JSON)
    - `node -e "const d=require('./.planning/perf-baselines/v1.9/vrf-08-path-b-decision.json'); ['decided','audit','original','new','rationale','evidence','review_gate'].forEach(k => { if (!d[k]) throw new Error('missing: '+k); });"` exits 0
    - `jq -e ".review_gate | test(\"Phase 67\")" .planning/perf-baselines/v1.9/vrf-08-path-b-decision.json` exits 0
    - `jq -e ".evidence | length >= 3" .planning/perf-baselines/v1.9/vrf-08-path-b-decision.json` exits 0
    - `jq -r ".decided" .planning/perf-baselines/v1.9/vrf-08-path-b-decision.json` returns string matching `^20[0-9]{2}-[0-9]{2}-[0-9]{2}$`
    - No source-file edits anywhere in `app/`, `components/`, `lib/`, `next.config.ts`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Verify schema integrity + Phase 67 cross-reference + zero side effects</name>
  <read_first>
    - .planning/perf-baselines/v1.9/vrf-08-path-b-decision.json (just created)
    - 70-VALIDATION.md row 70-03-phase67-xref
  </read_first>
  <files>(no new files; verification-only task)</files>
  <behavior>
    Run all schema gates and cross-reference assertions. Confirm no source-file edits leaked from Task 1 (defensive against accidental staging).
  </behavior>
  <action>
    ```
    # 1. JSON parse + schema field check
    node -e "const d=require('./.planning/perf-baselines/v1.9/vrf-08-path-b-decision.json'); const required=['decided','audit','original','new','rationale','evidence','review_gate']; required.forEach(k => { if (!d[k]) throw new Error('missing field: '+k); }); if (!Array.isArray(d.evidence) || d.evidence.length < 3) throw new Error('evidence array must have >=3 entries'); if (!/Phase 67/.test(d.review_gate)) throw new Error('review_gate must reference Phase 67'); if (!/2026-04-30/.test(d.decided)) throw new Error('decided must be 2026-04-30 ISO date'); console.log('VRF-08 path_b_decision schema: OK');"

    # 2. Cross-reference grep on review_gate
    jq -e ".review_gate | test(\"Phase 67\")" .planning/perf-baselines/v1.9/vrf-08-path-b-decision.json

    # 3. Cross-reference on evidence array (must mention v1.8-lcp-diagnosis OR project_phase63_1_checkpoint)
    jq -e ".evidence | tostring | test(\"v1.8-lcp-diagnosis|project_phase63_1_checkpoint\")" .planning/perf-baselines/v1.9/vrf-08-path-b-decision.json

    # 4. Defensive: confirm zero source-file edits leaked
    if git diff --name-only HEAD 2>/dev/null | grep -qE "^(app|components|lib|next\.config\.ts)" ; then
      echo "ERROR: Plan 03 must NOT touch source files (Phase 67 collision avoidance)"
      exit 1
    fi
    ```
  </action>
  <verify>
    <automated>node -e "const d=require('./.planning/perf-baselines/v1.9/vrf-08-path-b-decision.json'); ['decided','audit','original','new','rationale','evidence','review_gate'].forEach(k => { if (!d[k]) throw new Error('missing: '+k); });" &amp;&amp; jq -e ".review_gate | test(\"Phase 67\")" .planning/perf-baselines/v1.9/vrf-08-path-b-decision.json &amp;&amp; jq -e ".evidence | tostring | test(\"v1.8-lcp-diagnosis|project_phase63_1_checkpoint\")" .planning/perf-baselines/v1.9/vrf-08-path-b-decision.json</automated>
  </verify>
  <done>
    All schema gates exit 0. review_gate cross-references Phase 67. Evidence array references at least one of {v1.8-lcp-diagnosis, project_phase63_1_checkpoint}. No source files modified.
  </done>
  <acceptance_criteria>
    - Schema field check (7 required fields) exits 0
    - `jq -e ".review_gate | test(\"Phase 67\")"` exits 0
    - `jq -e ".evidence | tostring | test(\"v1.8-lcp-diagnosis|project_phase63_1_checkpoint\")"` exits 0
    - `git diff --name-only HEAD` shows no entries matching `app/`, `components/`, `lib/`, or `next.config.ts`
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `.planning/perf-baselines/v1.9/vrf-08-path-b-decision.json` exists with all 7 schema fields
- review_gate references Phase 67 (BND-05/06/07 gate that would retire this decision)
- Evidence array references concrete v1.8 deferral artifacts (vrf-05 + chunk diagnosis + ROADMAP rule 2)
- decided field is `2026-04-30`
- Zero source-file edits (Phase 67 collision avoidance preserved)
- Zero npm dep changes
</verification>

<success_criteria>
- 1 new file committed: `.planning/perf-baselines/v1.9/vrf-08-path-b-decision.json`
- Schema validation gate passes (all 7 fields populated, evidence ≥3 entries, review_gate references Phase 67)
- Plan 04 can mark VRF-08 Validated in REQUIREMENTS.md trace table
- Phase 67 chunk-graph contract intact
</success_criteria>

<output>
After completion, create `.planning/phases/70-v1-8-verification-closure-vrf-01-04-05/70-03-SUMMARY.md` recording:
- File created: `.planning/perf-baselines/v1.9/vrf-08-path-b-decision.json`
- Schema field count: 7/7
- Evidence array entry count
- review_gate cross-reference confirmed (Phase 67)
- decided field value
- Zero source-file edits attestation (git diff snapshot)
</output>
