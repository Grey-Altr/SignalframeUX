---
phase: 70-v1-8-verification-closure-vrf-01-04-05
plan: 04
type: execute
wave: 3
depends_on: ["02", "03"]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/PROJECT.md
autonomous: true
requirements: [VRF-06, VRF-07, VRF-08]
nyquist_compliant: true

must_haves:
  truths:
    - "REQUIREMENTS.md trace table marks VRF-06 Validated (2026-04-30) with Plan 02 evidence reference"
    - "REQUIREMENTS.md trace table marks VRF-07 Validated (2026-04-30) with Plan 02 iOS sub-cohort evidence reference"
    - "REQUIREMENTS.md trace table marks VRF-08 Validated (2026-04-30) with Plan 03 _path_b_decision evidence reference"
    - "v1.8 VRF-01/04/05 carry-forward closure recorded (either in PROJECT.md if it tracks v1.8 explicitly, or noted in REQUIREMENTS.md trace under VRF-06/07/08 as 'closes v1.8 VRF-01/04/05')"
    - "grep gate confirms ≥3 'VRF-0[678] | ... Validated' rows in REQUIREMENTS.md"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated traceability — VRF-06/07/08 Validated rows linking to Phase 70 evidence files"
      contains: "Validated"
    - path: ".planning/PROJECT.md"
      provides: "v1.8 VRF-01/04/05 carry-forward closure (only if PROJECT.md tracks v1.8 explicitly; skip if no such tracking)"
  key_links:
    - from: ".planning/REQUIREMENTS.md (VRF-06 row)"
      to: ".planning/perf-baselines/v1.9/rum-p75-lcp.json"
      via: "Validated row body referencing the baseline JSON path"
      pattern: "VRF-06.*Validated.*rum-p75-lcp.json"
    - from: ".planning/REQUIREMENTS.md (VRF-08 row)"
      to: ".planning/perf-baselines/v1.9/vrf-08-path-b-decision.json"
      via: "Validated row body referencing the path_b_decision JSON path"
      pattern: "VRF-08.*Validated.*vrf-08-path-b-decision.json"

---

<threat_model>
**T-DOC-01 (LOW) — REQUIREMENTS.md status drift**
Trace table edits are manual (markdown table cells). Risk of typo, wrong status, missing requirement ID, or markdown table column misalignment that breaks downstream tooling parse.
**Mitigation:** (a) grep gate asserts exactly 3 rows match `^| VRF-0[678] |.*Validated`; (b) post-edit `head/tail` visual inspection; (c) Plan 03's path_b_decision file is referenced as evidence in the VRF-08 row, so a broken path triggers the cross-reference test failure rather than silent drift.
</threat_model>

<objective>
Mark VRF-06, VRF-07, and VRF-08 as Validated in `.planning/REQUIREMENTS.md` with `(2026-04-30)` timestamp and evidence-file references back to Plans 02 and 03 outputs. Record v1.8 carry-forward closure (VRF-01/04/05) — either in PROJECT.md if it tracks v1.8 requirements explicitly, or as inline notes in the VRF-06/07/08 row bodies. Final grep gate confirms ≥3 Validated rows present.

Purpose: Closes Phase 70 traceability loop. Without this plan, Plans 02 and 03 produce evidence artifacts but the requirements table is silent; downstream tooling (`pde-tools phase-plan-index`, milestone-complete audits) would mark VRF-06/07/08 as still pending.
Output: 1-2 markdown files updated in place. Zero source-file edits. Zero new npm deps.
</objective>

<execution_context>
@/Users/greyaltaer/.claude/pde-os/engines/gsd/workflows/execute-plan.md
@/Users/greyaltaer/.claude/pde-os/engines/gsd/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/PROJECT.md
@.planning/perf-baselines/v1.9/rum-p75-lcp.json
@.planning/perf-baselines/v1.9/vrf-08-path-b-decision.json
@.planning/phases/70-v1-8-verification-closure-vrf-01-04-05/70-CAPTURE-LOG.md
@.planning/phases/70-v1-8-verification-closure-vrf-01-04-05/70-RESEARCH.md
@.planning/phases/70-v1-8-verification-closure-vrf-01-04-05/70-VALIDATION.md

<interfaces>
Existing REQUIREMENTS.md table format (line 43-45 is the pre-edit state):

```
- [ ] **VRF-06**: Field RUM 75th-percentile LCP <1.0s sampled over ≥24h on prod ...
- [ ] **VRF-07**: iPhone 14 Pro 4G LTE LCP variance reduction ...
- [ ] **VRF-08**: Moto G Power 3G Fast LCP retest ...
```

Post-edit shape (Plan 04 produces) — preserves bullet format but flips checkbox + appends Validated annotation:

```
- [x] **VRF-06**: ... Validated (2026-04-30) — see .planning/perf-baselines/v1.9/rum-p75-lcp.json (Phase 70 Plan 02). Closes v1.8 VRF-05.
- [x] **VRF-07**: ... Validated (2026-04-30) — see .planning/perf-baselines/v1.9/rum-p75-lcp.json (vrf_07_ios_cohort block, Phase 70 Plan 02). Closes v1.8 VRF-01 iOS partial + VRF-04 cascade.
- [x] **VRF-08**: ... Validated (2026-04-30) — see .planning/perf-baselines/v1.9/vrf-08-path-b-decision.json (Phase 70 Plan 03). Closes v1.8 VRF-01 last profile via tier-move.
```

70-VALIDATION.md row 70-04-validated specifies the grep gate:
```
grep -E '\| VRF-0[678] \|.*Validated' .planning/REQUIREMENTS.md | wc -l ≥ 3
```

NOTE: That regex pattern assumes a markdown table format with `| VRF-XX |`. The current REQUIREMENTS.md uses bullet format (`- [ ] **VRF-XX**:`). Plan 04 preserves bullet format AND ALSO ensures the grep passes — by appending a literal substring `Validated` to each bullet so grep -E finds it under either pattern. The verification command is updated to match the actual file format.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mark VRF-06/07/08 Validated in REQUIREMENTS.md</name>
  <read_first>
    - .planning/REQUIREMENTS.md (READ ENTIRELY — confirm bullet format vs table format; lines 41-45 are the VRF section)
    - .planning/perf-baselines/v1.9/rum-p75-lcp.json (Plan 02 output — confirm exists before referencing)
    - .planning/perf-baselines/v1.9/vrf-08-path-b-decision.json (Plan 03 output — confirm exists before referencing)
    - .planning/phases/70-v1-8-verification-closure-vrf-01-04-05/70-CAPTURE-LOG.md (Plan 02 audit — extract VRF-06 verdict + VRF-07 verdict for reference)
  </read_first>
  <files>.planning/REQUIREMENTS.md</files>
  <behavior>
    - Three bullet rows matching pattern `- [ ] **VRF-06**:`, `- [ ] **VRF-07**:`, `- [ ] **VRF-08**:` are flipped to `- [x]`
    - Each row body has `Validated (2026-04-30) — see <evidence path>` appended (preserve existing description text)
    - Each row references the correct evidence file
    - Each row notes the v1.8 carry-forward closure (VRF-05 / VRF-01 iOS / VRF-01 3G Fast respectively)
  </behavior>
  <action>
    Read REQUIREMENTS.md first. Locate lines 43-45 (the VRF-06/07/08 bullets in the "v1.8 Verification Closure (VRF — continuing from v1.8 VRF-01..05)" section). Use Edit tool (or equivalent in-place edit) for each of the three rows.

    Concrete edit pattern for VRF-06:
    - Find: `- [ ] **VRF-06**: Field RUM 75th-percentile LCP <1.0s sampled over ≥24h on prod — via Phase 58 \`/api/vitals\` route post-PR-#4 deploy; ≥100 sessions captured; aggregation script outputs p75 LCP from \`.planning/perf-baselines/v1.9/rum-p75-lcp.json\`. Closes v1.8 VRF-05 deferred.`
    - Replace: `- [x] **VRF-06**: Field RUM 75th-percentile LCP <1.0s sampled over ≥24h on prod — via Phase 58 \`/api/vitals\` route post-PR-#4 deploy; ≥100 sessions captured; aggregation script outputs p75 LCP from \`.planning/perf-baselines/v1.9/rum-p75-lcp.json\`. Closes v1.8 VRF-05 deferred. **Validated (2026-04-30)** — see .planning/perf-baselines/v1.9/rum-p75-lcp.json (Phase 70 Plan 02).`

    Concrete edit pattern for VRF-07:
    - Find: `- [ ] **VRF-07**: iPhone 14 Pro 4G LTE LCP variance reduction — n>10 sampling via WPT (or RUM-based via VRF-06 sub-cohort filter); median <2000ms strict 4G LTE Throttled. Closes v1.8 VRF-01 partial + VRF-04 cascade for iOS profile.`
    - Replace: `- [x] **VRF-07**: iPhone 14 Pro 4G LTE LCP variance reduction — n>10 sampling via WPT (or RUM-based via VRF-06 sub-cohort filter); median <2000ms strict 4G LTE Throttled. Closes v1.8 VRF-01 partial + VRF-04 cascade for iOS profile. **Validated (2026-04-30)** — see .planning/perf-baselines/v1.9/rum-p75-lcp.json (vrf_07_ios_cohort block, Phase 70 Plan 02).`

    Concrete edit pattern for VRF-08:
    - Find: `- [ ] **VRF-08**: Moto G Power 3G Fast LCP retest — after Next.js App Router framework chunk investigation (chunk 2979 ~56 KB gzipped consumes 1867ms TBT per \`project_phase63_1_checkpoint.md\`); either (a) framework chunk reduced via App Router config, OR (b) 3G Fast formally moved to "supported but not gated" tier with \`_path_decision\` block. Closes v1.8 VRF-01 last profile.`
    - Replace: `- [x] **VRF-08**: Moto G Power 3G Fast LCP retest — after Next.js App Router framework chunk investigation (chunk 2979 ~56 KB gzipped consumes 1867ms TBT per \`project_phase63_1_checkpoint.md\`); either (a) framework chunk reduced via App Router config, OR (b) 3G Fast formally moved to "supported but not gated" tier with \`_path_decision\` block. Closes v1.8 VRF-01 last profile. **Validated (2026-04-30)** — Path B taken; see .planning/perf-baselines/v1.9/vrf-08-path-b-decision.json (Phase 70 Plan 03). Phase 67 BND-05/06/07 closure is the review_gate.`

    If any row's exact text does not match (e.g., trailing whitespace, slight phrasing drift), Read the file at the exact line range (offset 41 limit 6) and use the actual line text verbatim as the Find pattern.

    Do NOT add new bullet rows. Do NOT delete existing rows. Only flip checkbox + append Validated annotation.
  </action>
  <verify>
    <automated>grep -cE "^- \[x\] \*\*VRF-0[678]\*\*.*Validated \(2026-04-30\)" .planning/REQUIREMENTS.md | grep -qE "^3$"</automated>
  </verify>
  <done>
    All three VRF-06/07/08 bullets in REQUIREMENTS.md have checkbox flipped to `[x]` and contain literal substring `Validated (2026-04-30)` with evidence-file reference. No other rows modified.
  </done>
  <acceptance_criteria>
    - `grep -cE "^- \[x\] \*\*VRF-06\*\*.*Validated" .planning/REQUIREMENTS.md` returns 1
    - `grep -cE "^- \[x\] \*\*VRF-07\*\*.*Validated" .planning/REQUIREMENTS.md` returns 1
    - `grep -cE "^- \[x\] \*\*VRF-08\*\*.*Validated" .planning/REQUIREMENTS.md` returns 1
    - `grep -cE "^- \[x\] \*\*VRF-0[678]\*\*.*Validated \(2026-04-30\)" .planning/REQUIREMENTS.md` returns 3
    - `grep -E "VRF-06.*rum-p75-lcp.json" .planning/REQUIREMENTS.md` returns ≥1 line
    - `grep -E "VRF-08.*vrf-08-path-b-decision.json" .planning/REQUIREMENTS.md` returns ≥1 line
    - No other VRF requirement bullets are checkbox-flipped (VRF-01..05 carry-forward language preserved if present elsewhere)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Update PROJECT.md with v1.8 carry-forward closure (conditional)</name>
  <read_first>
    - .planning/PROJECT.md (READ ENTIRELY first — determine if it tracks v1.8 VRF-01/04/05 explicitly)
    - .planning/STATE.md lines 50-67 (v1.8 Phase Map showing VRF-01/04/05 deferred state)
  </read_first>
  <files>.planning/PROJECT.md</files>
  <behavior>
    - If PROJECT.md contains explicit per-requirement tracking for v1.8 VRF-01, VRF-04, or VRF-05 (e.g., a "Validated requirements" table or "v1.8 deferrals" section): update those entries with `Validated via Phase 70 (Plan 02 + Plan 03), 2026-04-30`
    - If PROJECT.md tracks v1.8 only at milestone-summary level (no per-requirement rows): SKIP this task — REQUIREMENTS.md row body annotations already record the carry-forward closure
    - Decision criterion: `grep -E "VRF-0[145]" .planning/PROJECT.md` returns ≥1 line → update; returns 0 → skip
  </behavior>
  <action>
    Read PROJECT.md first. Run grep to detect explicit v1.8 VRF tracking:

    ```
    grep -E "VRF-0[145]" .planning/PROJECT.md
    ```

    If grep returns ≥1 match: identify the relevant section (likely a "v1.8 deferrals", "Carry-forward backlog", or "Open requirements" subsection) and append a closure annotation:

    ```
    > **Update 2026-04-30:** v1.8 VRF-01 (iOS partial + 3G Fast last-profile) + VRF-04 (mid-milestone synthesis) + VRF-05 (field RUM telemetry) closed via Phase 70 Plan 02 (RUM capture + iOS sub-cohort) + Plan 03 (3G Fast _path_b_decision). See .planning/perf-baselines/v1.9/rum-p75-lcp.json + .planning/perf-baselines/v1.9/vrf-08-path-b-decision.json for evidence; .planning/REQUIREMENTS.md VRF-06/07/08 rows for traceability links.
    ```

    If grep returns 0 matches: write a single comment marker to confirm task ran (NOT a content edit):

    ```
    echo "PROJECT.md does not track v1.8 VRF-01/04/05 explicitly — closure recorded via REQUIREMENTS.md VRF-06/07/08 row annotations only. No PROJECT.md edit needed."
    ```

    Either path is acceptable. Acceptance criteria branches accordingly.
  </action>
  <verify>
    <automated>(grep -qE "VRF-0[145]" .planning/PROJECT.md &amp;&amp; grep -qE "Phase 70.*Plan 0[23]|Validated.*2026-04-30" .planning/PROJECT.md) || ! grep -qE "VRF-0[145]" .planning/PROJECT.md</automated>
  </verify>
  <done>
    Either: (a) PROJECT.md contains v1.8 carry-forward closure annotation referencing Phase 70 Plan 02 + Plan 03; OR (b) PROJECT.md was confirmed to not track v1.8 VRF-01/04/05 explicitly and was left unchanged.
  </done>
  <acceptance_criteria>
    - One of the following holds:
      - PROJECT.md contains the literal string "Phase 70" AND "2026-04-30" in the v1.8 carry-forward context, OR
      - `grep -cE "VRF-0[145]" .planning/PROJECT.md` returns 0 (PROJECT.md does not track v1.8 VRF reqs explicitly; no edit was needed)
    - PROJECT.md is still valid markdown (no broken table cells, no orphan section headers introduced)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Final phase-closure grep gate</name>
  <read_first>
    - .planning/REQUIREMENTS.md (Task 1 just edited)
    - .planning/PROJECT.md (Task 2 conditionally edited)
    - .planning/perf-baselines/v1.9/rum-p75-lcp.json (Plan 02 evidence)
    - .planning/perf-baselines/v1.9/vrf-08-path-b-decision.json (Plan 03 evidence)
    - 70-VALIDATION.md row 70-04-validated
  </read_first>
  <files>(no new files; verification-only task)</files>
  <behavior>
    Run the phase-gate assertion from 70-VALIDATION.md row 70-04-validated. Confirm all 3 VRF requirements show Validated in REQUIREMENTS.md AND both evidence files exist on disk AND no source-file edits leaked.
  </behavior>
  <action>
    ```
    # 1. Three Validated rows in REQUIREMENTS.md
    COUNT=$(grep -cE "^- \[x\] \*\*VRF-0[678]\*\*.*Validated" .planning/REQUIREMENTS.md)
    if [ "$COUNT" -lt 3 ]; then
      echo "FAIL: REQUIREMENTS.md has only $COUNT Validated VRF-06/07/08 rows (need 3)"
      exit 1
    fi
    echo "OK: REQUIREMENTS.md has $COUNT Validated VRF-06/07/08 rows"

    # 2. Evidence files exist
    test -f .planning/perf-baselines/v1.9/rum-p75-lcp.json || { echo "FAIL: rum-p75-lcp.json missing (Plan 02 not complete)"; exit 1; }
    test -f .planning/perf-baselines/v1.9/vrf-08-path-b-decision.json || { echo "FAIL: vrf-08-path-b-decision.json missing (Plan 03 not complete)"; exit 1; }

    # 3. Plan 02 verdict gates still PASS (defensive — confirm Plan 02's output wasn't subsequently corrupted)
    jq -e ".verdict == \"PASS\"" .planning/perf-baselines/v1.9/rum-p75-lcp.json
    jq -e ".vrf_07_ios_cohort.verdict | test(\"PASS|INSUFFICIENT_SAMPLES\")" .planning/perf-baselines/v1.9/rum-p75-lcp.json

    # 4. Plan 03 schema gate still PASS
    node -e "const d=require('./.planning/perf-baselines/v1.9/vrf-08-path-b-decision.json'); ['decided','audit','original','new','rationale','evidence','review_gate'].forEach(k => { if (!d[k]) throw new Error('missing: '+k); });"

    # 5. Defensive: confirm zero source-file edits leaked from Plan 04
    if git diff --name-only HEAD 2>/dev/null | grep -qE "^(app|components|lib|next\.config\.ts)" ; then
      echo "ERROR: Plan 04 must NOT touch source files"
      exit 1
    fi
    echo "Phase 70 closure gate: ALL PASS"
    ```
  </action>
  <verify>
    <automated>test "$(grep -cE '^- \[x\] \*\*VRF-0[678]\*\*.*Validated' .planning/REQUIREMENTS.md)" -ge 3 &amp;&amp; test -f .planning/perf-baselines/v1.9/rum-p75-lcp.json &amp;&amp; test -f .planning/perf-baselines/v1.9/vrf-08-path-b-decision.json</automated>
  </verify>
  <done>
    All gates pass: 3 Validated rows in REQUIREMENTS.md, both evidence JSONs exist with valid schemas, no source-file edits leaked. Phase 70 ready for `/pde:complete-phase` invocation.
  </done>
  <acceptance_criteria>
    - `grep -cE "^- \[x\] \*\*VRF-0[678]\*\*.*Validated" .planning/REQUIREMENTS.md` returns ≥3
    - `test -f .planning/perf-baselines/v1.9/rum-p75-lcp.json` exits 0
    - `test -f .planning/perf-baselines/v1.9/vrf-08-path-b-decision.json` exits 0
    - `jq -e ".verdict == \"PASS\"" .planning/perf-baselines/v1.9/rum-p75-lcp.json` exits 0
    - `git diff --name-only HEAD | grep -E "^(app|components|lib|next\.config\.ts)"` returns no matches (zero source-file edits)
  </acceptance_criteria>
</task>

</tasks>

<verification>
- VRF-06/07/08 marked Validated in REQUIREMENTS.md with 2026-04-30 timestamp + evidence file references
- v1.8 carry-forward closure recorded (PROJECT.md if applicable, otherwise via REQUIREMENTS.md row body)
- Plan 02 + Plan 03 evidence JSONs intact (verdicts still PASS, schemas still valid)
- Zero source-file edits anywhere in Phase 70
- Phase 70 traceability loop closed
</verification>

<success_criteria>
- 1-2 markdown files updated: REQUIREMENTS.md (always), PROJECT.md (conditional)
- 3 Validated rows confirmed via grep gate
- All evidence file references resolvable (Plan 02 + Plan 03 outputs intact)
- Phase 70 ready for `/pde:complete-phase` workflow
- v1.8 VRF-01/04/05 carry-forward closure documented
</success_criteria>

<output>
After completion, create `.planning/phases/70-v1-8-verification-closure-vrf-01-04-05/70-04-SUMMARY.md` recording:
- REQUIREMENTS.md edit count (3 rows flipped)
- PROJECT.md edit (yes with annotation, OR no — explanation)
- Final grep gate counts (3 Validated rows confirmed)
- Phase 70 closure status: COMPLETE
- v1.8 VRF-01/04/05 closure mapping (which Phase 70 plan closes which v1.8 deferred req)
- Diff summary (files modified, line additions/deletions)
</output>
