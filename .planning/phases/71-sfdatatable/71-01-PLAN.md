---
phase: 71-sfdatatable
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/sf/sf-data-table.tsx
  - package.json
  - pnpm-lock.yaml
autonomous: true
requirements:
  - DEP-01
gap_closure: false
must_haves:
  truths:
    - "_dep_dt_01_decision 7-field comment block exists at the top of components/sf/sf-data-table.tsx and is committed BEFORE pnpm add"
    - "@tanstack/react-table@^8.21.3 is installed and recorded in pnpm-lock.yaml"
    - "next.config.ts optimizePackageImports does NOT contain @tanstack/react-table (D-04 lock holds)"
    - "_dep_dt_01_decision.bundle_evidence field is populated with MEASURED gzip + First Load JS values from ANALYZE=true pnpm build (not estimates)"
  artifacts:
    - path: "components/sf/sf-data-table.tsx"
      provides: "Placeholder Pattern B file hosting _dep_dt_01_decision block; no impl yet"
      contains: "_dep_dt_01_decision"
    - path: "package.json"
      provides: "@tanstack/react-table dep declaration at ^8.21.3"
      contains: "@tanstack/react-table"
    - path: "pnpm-lock.yaml"
      provides: "Resolved dependency tree for @tanstack/react-table"
      contains: "@tanstack/react-table"
  key_links:
    - from: "components/sf/sf-data-table.tsx"
      to: "components/sf/index.ts"
      via: "ABSENCE — file MUST NOT appear in barrel"
      pattern: "components/sf/index.ts MUST NOT match /sf-data-table/"
    - from: "components/sf/sf-data-table.tsx"
      to: "next.config.ts"
      via: "ABSENCE — package MUST NOT appear in optimizePackageImports"
      pattern: "next.config.ts MUST NOT match /@tanstack\\/react-table/"
---

<objective>
Author the `_dep_dt_01_decision` 7-field ratification block (REQ-namespaced precedent for v1.10 runtime-dep additions), then install `@tanstack/react-table@^8.21.3`, then measure the bundle impact and back-fill the `bundle_evidence` field with measured (NOT estimated) values. This establishes the dep-decision-at-plan-time precedent before any code touches TanStack Table.

Purpose: This plan is the audit trail for v1.10's first runtime-npm-dep introduction. The block lands BEFORE the install so reviewers see a "why we're adding this" rationale ratified at plan-time, not retro-fitted. The bundle_evidence field is back-filled post-install with measured gzip/First-Load-JS values from `ANALYZE=true pnpm build` — NEVER an estimate. This is the schema sibling of `_wmk_01_decision` (Phase 69) and the precedent for `_dep_re_01_decision` (Phase 73) and any future v1.10 dep ratifications.

Output: A placeholder `components/sf/sf-data-table.tsx` whose only contents are the `_dep_dt_01_decision` block + a `'use client'` directive + a stub export comment ("impl in Plan 02"). The dep is installed. The bundle_evidence field carries real numbers.
</objective>

<execution_context>
@/Users/greyaltaer/.claude/pde-os/engines/gsd/workflows/execute-plan.md
@/Users/greyaltaer/.claude/pde-os/engines/gsd/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/71-sfdatatable/71-RESEARCH.md
@.planning/phases/71-sfdatatable/71-VALIDATION.md
@CLAUDE.md
@components/sf/index.ts
@next.config.ts
@tests/v1.8-phase63-1-bundle-budget.spec.ts

<interfaces>
Schema precedent for `_dep_dt_01_decision` is `_wmk_01_decision` at tests/v1.8-phase63-1-wordmark-hoist.spec.ts:1-37.

The 7 fields adapt as follows:

    decided          → ISO date (Phase 71 plan close date)
    audit            → "sf-data-table:runtime-dep"
    dep_added        → array of npm package names (replaces original_threshold)
    version          → exact semver pin (replaces new_threshold)
    rationale        → multi-line freeform
    bundle_evidence  → array of MEASURED bundle facts (replaces evidence)
    review_gate      → when to re-evaluate (e.g. "TanStack Table v9 stable release")

Current barrel state (verified — must NOT change in this plan):

    components/sf/index.ts MUST NOT export sf-data-table or sf-data-table-lazy
    Pattern B = NOT in barrel

Current next.config.ts optimizePackageImports state (verified — must NOT change in this plan):

    8 entries (Phase 67 baseline): @/components/sf, lucide-react, radix-ui,
    input-otp, cmdk, vaul, sonner, react-day-picker
    D-04 chunk-id stability lock holds — NO additions in v1.10

Bundle baseline (Phase 67 close, verified at tests/v1.8-phase63-1-bundle-budget.spec.ts):

    BUDGET_BYTES = 200 * 1024  // 200 KB gzip — CLAUDE.md hard constraint
    Current homepage `/` First Load JS: 187.6 KB gzip (12.4 KB headroom)
</interfaces>

<threat_model>
ASVS L1 — Phase 71 Plan 01 threat surface:

| Threat | Severity | Mitigation |
|--------|----------|------------|
| Bundle escape: developer adds `@tanstack/react-table` to `optimizePackageImports` thinking it's a "performance win" — reshuffles webpack splitChunks — dissolves D-04 chunk-id lock | HIGH | Acceptance criterion `next.config.ts grep "@tanstack/react-table" returns 0 lines`; explicit comment in placeholder file: "// D-04 LOCK: @tanstack/react-table NEVER in optimizePackageImports" |
| Estimated (not measured) bundle_evidence — reviewer accepts a guessed gzip number — `_dep_X_decision` precedent corrupted for 4 more components | HIGH | Acceptance criterion grep verifies bundle_evidence carries real KB numbers AND explicit "MEASURED via ANALYZE=true pnpm build" string AND a non-empty resolved version (e.g. "8.21.3", not "TBD") |
| Worktree leakage: file authored in `.claude/worktrees/agent-N/` leaks to main tree as untracked, then committed by mistake — component file appears with no audit trail | MEDIUM | Pre-commit `git status` check; explicit acceptance criterion verifies file was authored in this plan and not pre-existing |
| Pre-Plan-02 placeholder file accidentally exported from barrel | MEDIUM | Acceptance criterion `grep -c "sf-data-table" components/sf/index.ts` MUST equal 0 |

No user-input attack surface in this plan (no rendering, no DOM). All threats are supply-chain / process-integrity.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Author _dep_dt_01_decision placeholder file (BEFORE pnpm add)</name>
  <files>components/sf/sf-data-table.tsx</files>
  <read_first>
    - components/sf/sf-data-table.tsx (verify file does NOT yet exist via `ls`; if it does, STOP — worktree leakage suspect)
    - components/sf/index.ts (verify barrel does NOT mention sf-data-table)
    - .planning/phases/71-sfdatatable/71-RESEARCH.md §Code Examples Example 7 (canonical block)
    - .planning/STATE.md "v1.10 Critical Constraints" section
    - tests/v1.8-phase63-1-wordmark-hoist.spec.ts:1-37 (the `_wmk_01_decision` schema precedent)
  </read_first>
  <action>
    Run `git status --porcelain components/sf/sf-data-table.tsx` first. If it returns ANY output, STOP and surface a worktree-leakage warning to the user — file MUST be authored fresh in-phase.

    Create `components/sf/sf-data-table.tsx` with EXACTLY this content (replace `<TODAY_ISO>` with the output of `date -u +%Y-%m-%d`):

        "use client";

        /**
         * SFDataTable — FRAME layer headless data table (Pattern B, P3 lazy).
         *
         * IMPLEMENTATION SHIPS IN PLAN 02. This file currently hosts only the
         * _dep_dt_01_decision ratification block (DEP-01) per the v1.10
         * dep-decision-at-plan-time invariant.
         *
         * Pattern B contract (DO NOT VIOLATE):
         *   - NEVER export from components/sf/index.ts barrel
         *   - NEVER add @tanstack/react-table to next.config.ts optimizePackageImports (D-04 lock)
         *   - Consumers import via @/components/sf/sf-data-table-lazy (lands in Plan 03)
         *   - Direct import of this file is SUPPORTED but discouraged outside the lazy wrapper
         */

        // ---------------------------------------------------------------------------
        // _dep_dt_01_decision — runtime-dep ratification block (REQ-namespaced)
        //
        // Schema precedent: _wmk_01_decision at tests/v1.8-phase63-1-wordmark-hoist.spec.ts:1-37
        //
        // _dep_dt_01_decision:
        //   decided: "<TODAY_ISO>"          # populated by Task 1 author
        //   audit: "sf-data-table:runtime-dep"
        //   dep_added:
        //     - "@tanstack/react-table"
        //   version: "<TBD — populated by Task 2 post-install>"
        //   rationale: |
        //     SFDataTable composes TanStack Table v8 headless logic (sort, filter,
        //     pagination, row selection state machine; ~12 KB gzip; zero DOM) over
        //     the existing SFTable family. v8 is the maintained-track de-facto React
        //     headless-table standard; v9 is open RFC #5834, not yet released.
        //     AG Grid (200+ KB) and react-table v7 (deprecated; React 18/19
        //     concurrent-mode issues) rejected per .planning/research/STACK.md.
        //
        //     P3 lazy posture (next/dynamic ssr:false in sf-data-table-lazy.tsx,
        //     NOT in sf/index.ts barrel, NOT in next.config.ts optimizePackageImports
        //     — D-04 chunk-id stability lock holds) keeps homepage First Load JS
        //     impact at 0 KB. Sort headers use <button type="button"> per WCAG 2.1.1;
        //     axe-core enforces in Phase 71 Plan 03 TST-03.
        //   bundle_evidence:
        //     - "TBD — populated by Task 3 post-install via ANALYZE=true pnpm build"
        //     - "Pre-add homepage First Load JS baseline: 187.6 KB gzip (Phase 67 close, MILESTONES.md)"
        //   review_gate: |
        //     Re-evaluate when @tanstack/react-table v9 reaches stable release
        //     (currently RFC #5834, no release date). Re-pin and re-measure if
        //     v9 changes API surface or bundle profile materially. Also fires
        //     if BND-08 budget changes (currently 200 KB hard target) or if D-04
        //     chunk-id stability lock is intentionally broken in a future BND phase.
        //   scope: "@tanstack/react-table runtime dep — single P3 lazy chunk"
        //   ratified_to_main_via: "Phase 71 (Plan 01 commit)"
        // ---------------------------------------------------------------------------

        // Implementation lands in Plan 02. This export prevents accidental
        // barrel inclusion bugs (TypeScript will error if anyone tries to
        // import { SFDataTable } before Plan 02 ships the real symbol).
        export {};

    Replace `<TODAY_ISO>` with today's ISO date (YYYY-MM-DD format).

    DO NOT modify components/sf/index.ts. DO NOT modify next.config.ts.

    Commit with message: `Chore(71-01): author _dep_dt_01_decision block (pre-install ratification)`
  </action>
  <verify>
    <automated>test -f components/sf/sf-data-table.tsx && grep -q "_dep_dt_01_decision:" components/sf/sf-data-table.tsx && grep -q "audit: \"sf-data-table:runtime-dep\"" components/sf/sf-data-table.tsx && grep -q "@tanstack/react-table" components/sf/sf-data-table.tsx && grep -q "review_gate:" components/sf/sf-data-table.tsx && [ "$(grep -c 'sf-data-table' components/sf/index.ts)" = "0" ] && [ "$(grep -c '@tanstack/react-table' next.config.ts)" = "0" ]</automated>
  </verify>
  <acceptance_criteria>
    - `components/sf/sf-data-table.tsx` exists and contains the literal string `_dep_dt_01_decision:` exactly once at the top of the file
    - File contains all 7 schema fields verifiable by grep: `decided:`, `audit:`, `dep_added:`, `version:`, `rationale:`, `bundle_evidence:`, `review_gate:`
    - `audit:` field value is exactly `"sf-data-table:runtime-dep"` (matches threat-model entry)
    - File starts with `"use client";` directive (Pattern B requirement)
    - File ends with `export {};` (placeholder export — no real symbol exported until Plan 02)
    - `grep -c "sf-data-table" components/sf/index.ts` returns `0` (Pattern B: NOT in barrel)
    - `grep -c "@tanstack/react-table" next.config.ts` returns `0` (D-04 lock holds)
    - `git log -1 --format=%s -- components/sf/sf-data-table.tsx` matches pattern `Chore(71-01):`
  </acceptance_criteria>
  <done>
    Placeholder file committed with the `_dep_dt_01_decision` block in place. Bundle_evidence field shows "TBD" placeholder values that Task 3 will populate. Reviewers can read the rationale, dep_added, and review_gate fields right now without running anything.
  </done>
</task>

<task type="auto">
  <name>Task 2: Install @tanstack/react-table@^8.21.3 (automation-first; pre-install version probe + leakage check)</name>
  <files>package.json, pnpm-lock.yaml</files>
  <read_first>
    - package.json (verify @tanstack/react-table is NOT already a dep)
    - .planning/phases/71-sfdatatable/71-RESEARCH.md §Open Questions item 1 (npm view check)
    - components/sf/sf-data-table.tsx (verify Task 1 placeholder file with `_dep_dt_01_decision` block is committed and on disk)
  </read_first>
  <action>
    Per the planning rules' automation-first principle: `pnpm add` is a CLI operation that Claude can run directly. No human-in-loop checkpoint needed; this task runs autonomously and any failure (peer-dep block, v9 surprise, leakage) surfaces via the acceptance criteria below.

    Step 1 — pre-install npm version probe (informational, populates `version` field in Task 3):

        npm view @tanstack/react-table version

    Capture the printed version string. If the version starts with `9.` (i.e., v9 has shipped), STOP — Plan 01 needs review per the `review_gate` clause in the `_dep_dt_01_decision` block. Surface this to the user and do not proceed with `pnpm add`.

    Step 2 — install:

        pnpm add @tanstack/react-table@^8.21.3

    Expected: package added to `package.json` `dependencies`; `pnpm-lock.yaml` updated; no peer-dep ERRORS (warnings under React 19 noise band are acceptable).

    Step 3 — verify installation:

        pnpm list @tanstack/react-table --depth=0

    Expected: a single entry showing the resolved version (`8.21.x`).

    Step 4 — worktree leakage check:

        git status --porcelain | grep -v "package.json\|pnpm-lock.yaml\|sf-data-table.tsx"

    Expected: NO output (only the 3 files this plan modifies are modified). If output appears, STOP — investigate `.claude/worktrees/`. Do NOT commit until leakage is resolved.

    Step 5 — commit:

        git add package.json pnpm-lock.yaml
        git commit -m "Chore(71-01): pnpm add @tanstack/react-table@^8.21.3 (DEP-01 install)"

    DO NOT modify `components/sf/sf-data-table.tsx` in this commit; the back-fill of bundle_evidence is Task 3.
    DO NOT modify `next.config.ts` (D-04 lock holds).
    DO NOT modify `components/sf/index.ts` (Pattern B contract — file remains out of barrel).
  </action>
  <verify>
    <automated>pnpm list @tanstack/react-table --depth=0 2>&1 | grep -q "@tanstack/react-table 8\." && grep -q '"@tanstack/react-table"' package.json && grep -q "@tanstack/react-table" pnpm-lock.yaml && [ "$(grep -c '@tanstack/react-table' next.config.ts)" = "0" ] && [ "$(grep -c 'sf-data-table' components/sf/index.ts)" = "0" ] && git log -1 --format=%s | grep -qE "Chore\(71-01\):.*pnpm add"</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm list @tanstack/react-table --depth=0` exits 0 and prints a version starting with `8.21.`
    - `package.json` `dependencies` field contains the literal key `"@tanstack/react-table"` (verifiable: `node -e "console.log(JSON.stringify(require('./package.json').dependencies['@tanstack/react-table']))"` returns a non-null version range starting with `^8.21.`)
    - `pnpm-lock.yaml` contains the literal substring `'@tanstack/react-table'`
    - `next.config.ts` does NOT contain the substring `@tanstack/react-table` (D-04 lock holds)
    - `components/sf/index.ts` does NOT contain the substring `sf-data-table` (Pattern B contract)
    - `git status --porcelain` shows ONLY package.json + pnpm-lock.yaml as modified by this task's commit (verifiable via `git show --stat HEAD` listing only those two files)
    - `git log -1 --format=%s` matches the pattern `Chore(71-01):.*pnpm add`
  </acceptance_criteria>
  <done>
    `@tanstack/react-table@^8.21.3` installed and committed; pre-install version probe confirmed v8 (not v9 surprise); worktree clean (no leakage from `.claude/worktrees/`); the resolved version string is captured for Task 3 to populate the `version` field of `_dep_dt_01_decision`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Measure bundle impact via ANALYZE=true pnpm build, back-fill bundle_evidence</name>
  <files>components/sf/sf-data-table.tsx</files>
  <read_first>
    - components/sf/sf-data-table.tsx (read current state — bundle_evidence field still TBD)
    - tests/v1.8-phase63-1-bundle-budget.spec.ts (existing 200 KB hard-constraint spec)
    - .planning/phases/71-sfdatatable/71-RESEARCH.md §Code Examples Example 9 (measurement protocol)
  </read_first>
  <action>
    Run measurement protocol (BND-04 stale-chunk guard):

        rm -rf .next/cache .next
        ANALYZE=true pnpm build 2>&1 | tee /tmp/71-01-bundle.log

    Wait for build to complete. Then extract:

    Step 1 — Homepage First Load JS:

        # Try the build-output line in pnpm logs:
        grep -E "^[┌├└│]?\s*[┬├└]?\s*/\s+[0-9]" /tmp/71-01-bundle.log | head -3

    Or directly inspect `.next/build-manifest.json`:

        node -e "const m = require('./.next/build-manifest.json'); const homepageChunks = m.pages['/']; console.log('Homepage chunks:', homepageChunks.length); console.log(JSON.stringify(homepageChunks, null, 2))"

    Step 2 — Verify @tanstack/react-table is NOT in the homepage chunk manifest (placeholder file is `export {}` so this MUST be true at this stage — confirms baseline before any real impl):

        grep -lE "@tanstack/react-table|tanstack" .next/static/chunks/pages/*.js 2>/dev/null || echo "OK: tanstack not in homepage chunks"

    Expected: `OK: tanstack not in homepage chunks` — placeholder file has no import.

    Step 3 — Capture the resolved version:

        node -e "console.log(require('./node_modules/@tanstack/react-table/package.json').version)"

    Save this exact string (e.g., `8.21.3`).

    Step 4 — back-fill the placeholder fields in `components/sf/sf-data-table.tsx`:

      - Replace `version: "<TBD — populated by Task 2 post-install>"` with `version: "<RESOLVED_VERSION>"` (use the exact string from Step 3, e.g. `version: "8.21.3"`).

      - Replace the bundle_evidence array placeholder lines with MEASURED values. Format:

            //   bundle_evidence:
            //     - "Homepage / First Load JS pre-add baseline: 187.6 KB gzip (Phase 67 close)"
            //     - "Homepage / First Load JS post-add (placeholder file, no impl): <MEASURED> KB gzip"
            //     - "Headroom remaining: <MEASURED> KB under 200 KB hard target"
            //     - "TanStack Table chunk: ABSENT from homepage manifest (placeholder file has no import — real chunk lands Plan 02)"
            //     - "Devtools subpath audit: zero references in .next/analyze/client.html (verified via grep)"
            //     - "Resolved version: <RESOLVED_VERSION>"
            //     - "Measurement command: rm -rf .next/cache .next && ANALYZE=true pnpm build"
            //     - "Measurement date: <TODAY_ISO>"

      Replace ALL `<MEASURED>`, `<RESOLVED_VERSION>`, and `<TODAY_ISO>` placeholders with the real values from Steps 1–3. Do NOT estimate. If the build does not produce a clean number, STOP and surface the issue.

    Step 5 — run the bundle budget spec to confirm we are still under 200 KB:

        pnpm exec playwright test tests/v1.8-phase63-1-bundle-budget.spec.ts --project=chromium

    Expected: PASS.

    Step 6 — commit:

        git add components/sf/sf-data-table.tsx
        git commit -m "Chore(71-01): populate _dep_dt_01_decision.bundle_evidence with measured post-install values"
  </action>
  <verify>
    <automated>grep -q "version: \"8\.21\." components/sf/sf-data-table.tsx && grep -q "bundle_evidence:" components/sf/sf-data-table.tsx && ! grep -q "TBD" components/sf/sf-data-table.tsx && ! grep -q "MEASURED>" components/sf/sf-data-table.tsx && ! grep -q "RESOLVED_VERSION>" components/sf/sf-data-table.tsx && pnpm exec playwright test tests/v1.8-phase63-1-bundle-budget.spec.ts --project=chromium --reporter=line</automated>
  </verify>
  <acceptance_criteria>
    - `components/sf/sf-data-table.tsx` `version:` field is populated with a literal version string starting with `8.21.` (matches `pnpm list` output from Task 2)
    - `components/sf/sf-data-table.tsx` `bundle_evidence:` array contains AT LEAST 6 literal entries (verifiable: `grep -c "^//\s*-" components/sf/sf-data-table.tsx` returns 6 or more)
    - File contains the literal string `Measurement command: rm -rf .next/cache .next && ANALYZE=true pnpm build` (proves measurement was run, not estimated)
    - File does NOT contain the substring `TBD` (no placeholder text remaining)
    - File does NOT contain the substring `MEASURED>` or `RESOLVED_VERSION>` (no template placeholders remaining)
    - `pnpm exec playwright test tests/v1.8-phase63-1-bundle-budget.spec.ts --project=chromium` exits 0 (homepage First Load JS still 200 KB or less; placeholder file added zero impact)
    - `git log -1 --format=%s -- components/sf/sf-data-table.tsx` for the second commit on this file matches pattern `Chore(71-01):.*populate.*bundle_evidence`
  </acceptance_criteria>
  <done>
    `_dep_dt_01_decision` block carries 7 fully-populated fields with measured (not estimated) bundle_evidence and a real resolved version. The 200 KB bundle budget spec is green. The audit trail for the dep introduction is complete and committed BEFORE Plan 02 starts writing real impl.
  </done>
</task>

</tasks>

<verification>
End-to-end verification of Plan 01:

(1) `_dep_dt_01_decision` block well-formed:

    grep -E "decided:|audit:|dep_added:|version:|rationale:|bundle_evidence:|review_gate:" components/sf/sf-data-table.tsx | wc -l

Expected: 7 or more (one for each schema field).

(2) No "TBD" anywhere — all fields populated with real values:

    ! grep -E "TBD|MEASURED>|RESOLVED_VERSION>|TODAY_ISO>" components/sf/sf-data-table.tsx

Expected: empty output.

(3) D-04 lock holds:

    grep -c "@tanstack/react-table" next.config.ts

Expected: `0`.

(4) Pattern B contract (NOT in barrel):

    grep -c "sf-data-table" components/sf/index.ts

Expected: `0`.

(5) Bundle budget green:

    pnpm exec playwright test tests/v1.8-phase63-1-bundle-budget.spec.ts --project=chromium

Expected: PASS, homepage First Load JS 200 KB or less.

(6) Worktree clean:

    git status --porcelain

Expected: clean (or only the expected Plan 01 files staged for next commit if mid-plan).
</verification>

<success_criteria>
- [ ] `components/sf/sf-data-table.tsx` exists with 7-field `_dep_dt_01_decision` block populated with measured values
- [ ] `@tanstack/react-table@^8.21.3` is installed (`package.json` + `pnpm-lock.yaml`)
- [ ] `next.config.ts` is unchanged (D-04 lock holds — package NOT in `optimizePackageImports`)
- [ ] `components/sf/index.ts` is unchanged (Pattern B — file NOT in barrel)
- [ ] `tests/v1.8-phase63-1-bundle-budget.spec.ts` PASSES (200 KB or less homepage First Load JS)
- [ ] No worktree leakage; only the 3 expected files (sf-data-table.tsx, package.json, pnpm-lock.yaml) modified
- [ ] Three commits land: (1) block authored pre-install, (2) pnpm add, (3) bundle_evidence back-filled post-install
</success_criteria>

<output>
After completion, create `.planning/phases/71-sfdatatable/71-01-SUMMARY.md` documenting:
- Final resolved `@tanstack/react-table` version
- Measured bundle deltas (pre-add baseline → post-add placeholder)
- The three commit SHAs for the audit trail
- Forward link to Plan 02 (impl)
</output>
