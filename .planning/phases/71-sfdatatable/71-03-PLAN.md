---
phase: 71-sfdatatable
plan: 03
type: execute
wave: 3
depends_on:
  - 71-02
files_modified:
  - components/sf/sf-data-table.tsx
  - components/sf/sf-data-table-lazy.tsx
  - app/_dev/playground/sf-data-table/page.tsx
  - tests/v1.10-phase71-sf-data-table.spec.ts
  - tests/v1.10-phase71-sf-data-table-axe.spec.ts
autonomous: true
requirements:
  - DT-03
  - DT-06
  - TST-03
  - DEP-01
gap_closure: false
must_haves:
  truths:
    - "A consumer can navigate pages via SFPagination composed inside SFDataTable; clicking a page link calls table.setPageIndex(n) and the pageIndex state reflects the change"
    - "A consumer imports SFDataTableLazy from @/components/sf/sf-data-table-lazy and the component renders with an SFSkeleton fallback during dynamic load (next/dynamic ssr:false)"
    - "axe-core scan against the playground fixture reports zero violations on button-name, aria-sort, color-contrast, and label rules"
    - "Production build (ANALYZE=true pnpm build, fresh cache) verifies @tanstack/react-table appears in a SEPARATE lazy chunk and is ABSENT from the homepage / First Load chunk manifest"
    - "Production build verifies zero references to 'devtools' string anywhere under .next/static/"
    - "Homepage / First Load JS remains under or equal to 200 KB after the full Phase 71 component lands (BND budget green)"
  artifacts:
    - path: "components/sf/sf-data-table.tsx"
      provides: "DT-03 pagination composition added to Plan 02 impl"
      contains: "SFPagination"
    - path: "components/sf/sf-data-table-lazy.tsx"
      provides: "P3 lazy wrapper for SFDataTable via next/dynamic({ ssr: false })"
      exports: ["SFDataTableLazy"]
      min_lines: 20
    - path: "app/_dev/playground/sf-data-table/page.tsx"
      provides: "Playground fixture mount surface for Playwright tests; kept out of public route map"
      exports: ["default"]
    - path: "tests/v1.10-phase71-sf-data-table.spec.ts"
      provides: "Playwright spec — DT-01 sort cycle + keyboard, DT-02 filter debounce, DT-03 pagination, DT-04 selection + indeterminate"
    - path: "tests/v1.10-phase71-sf-data-table-axe.spec.ts"
      provides: "axe-core spec — button-name, aria-sort, color-contrast, label rules"
  key_links:
    - from: "components/sf/sf-data-table-lazy.tsx"
      to: "components/sf/sf-data-table"
      via: "next/dynamic import — ssr: false"
      pattern: "next/dynamic.*ssr:\\s*false"
    - from: "app/_dev/playground/sf-data-table/page.tsx"
      to: "components/sf/sf-data-table-lazy.tsx"
      via: "direct import — playground consumer of canonical lazy API"
      pattern: "from ['\"]@/components/sf/sf-data-table-lazy['\"]"
    - from: "tests/v1.10-phase71-sf-data-table.spec.ts"
      to: "app/_dev/playground/sf-data-table/page.tsx"
      via: "Playwright page.goto fixture URL"
      pattern: "/_dev/playground/sf-data-table"
    - from: "tests/v1.10-phase71-sf-data-table-axe.spec.ts"
      to: "@axe-core/playwright"
      via: "AxeBuilder direct rule scan (precedent: tests/v1.9-phase66-arc-axe.spec.ts)"
      pattern: "AxeBuilder"
---

<objective>
Close Phase 71 with the consumer-facing API (SFDataTableLazy), pagination composition (DT-03), the playground fixture for Playwright, and the test specs (TST-03) — then run the production-grade bundle audit that closes DEP-01: assert TanStack Table chunk absence from homepage manifest, devtools string absence from `.next/static/`, and BND budget green.

Purpose: Plan 02 shipped the impl. Plan 03 ships the SHAPE consumers actually touch (the lazy wrapper) AND the evidence trail (playground fixture + Playwright + axe + production chunk audit). This plan also folds DT-03 (pagination via SFPagination composition) into the Plan 02 impl since pagination is the smallest of the four DT-feature gaps and lives naturally inside the same render tree.

Output: 5 files modified — 1 edit (sf-data-table.tsx) + 4 new files (lazy wrapper, playground page, two test specs). Phase 71 closes with all 8 requirement IDs satisfied (DT-01..06 + DEP-01 + TST-03), bundle 200 KB or less, axe green.
</objective>

<execution_context>
@/Users/greyaltaer/.claude/pde-os/engines/gsd/workflows/execute-plan.md
@/Users/greyaltaer/.claude/pde-os/engines/gsd/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/71-sfdatatable/71-RESEARCH.md
@.planning/phases/71-sfdatatable/71-VALIDATION.md
@.planning/phases/71-sfdatatable/71-01-SUMMARY.md
@.planning/phases/71-sfdatatable/71-02-SUMMARY.md
@CLAUDE.md
@components/sf/index.ts
@components/sf/sf-pagination.tsx
@components/sf/sf-calendar-lazy.tsx
@components/sf/sf-data-table.tsx
@tests/v1.9-phase66-arc-axe.spec.ts
@tests/v1.8-phase63-1-bundle-budget.spec.ts
@playwright.config.ts
@next.config.ts

<interfaces>
P3 lazy wrapper precedent (sf-calendar-lazy.tsx:1-35 verbatim):

    "use client";
    import dynamic from "next/dynamic";
    import { SFSkeleton } from "@/components/sf";

    const SFCalendarDynamic = dynamic(
      () => import("@/components/sf/sf-calendar").then((m) => ({ default: m.SFCalendar })),
      { ssr: false, loading: () => <SFSkeleton className="h-[350px] w-[280px]" /> }
    );

    export function SFCalendarLazy(props: React.ComponentProps<typeof SFCalendarDynamic>) {
      return <SFCalendarDynamic {...props} />;
    }

TanStack Table v8 pagination API (verified in 71-RESEARCH.md §Code Examples 4):

    import { getPaginationRowModel, type PaginationState } from "@tanstack/react-table";
    table.getPageCount()        // number
    table.getCanPreviousPage()  // boolean
    table.getCanNextPage()      // boolean
    table.previousPage()        // void
    table.nextPage()            // void
    table.setPageIndex(n)       // void
    table.getState().pagination // PaginationState

SFPagination existing API (sf-pagination.tsx:127-134 — already in barrel):

    import {
      SFPagination, SFPaginationContent, SFPaginationItem,
      SFPaginationLink, SFPaginationPrevious, SFPaginationNext,
    } from "@/components/sf";

@axe-core/playwright precedent (tests/v1.9-phase66-arc-axe.spec.ts:1-141):

    import { test, expect } from "@playwright/test";
    import AxeBuilder from "@axe-core/playwright";
    const ABS_BASE = process.env.CAPTURE_BASE_URL ?? "http://localhost:3000";

    test("rule-name", async ({ page }) => {
      await page.goto(`${ABS_BASE}/path`, { waitUntil: "networkidle" });
      const results = await new AxeBuilder({ page }).withRules(["button-name", "aria-sort"]).analyze();
      expect(results.violations.filter((v) => v.id === "button-name")).toHaveLength(0);
    });

Existing 200 KB budget spec (tests/v1.8-phase63-1-bundle-budget.spec.ts:22):

    const BUDGET_BYTES = 200 * 1024;  // CLAUDE.md hard constraint, restored Phase 67
</interfaces>

<threat_model>
ASVS L1 — Phase 71 Plan 03 threat surface:

| Threat | Severity | Mitigation |
|--------|----------|------------|
| Bundle escape: SFDataTableLazy or sf-data-table accidentally added to `components/sf/index.ts` barrel — spikes homepage First Load JS by ~12 KB — BND-08 fail | HIGH | Acceptance criterion `grep -c "sf-data-table" components/sf/index.ts` returns 0; Task 5 production-chunk audit runs `ANALYZE=true pnpm build` and asserts manifest absence |
| TanStack devtools subpath leaks into a production chunk (any future code path) | HIGH | Task 5 acceptance criterion: `grep -rL "devtools" .next/static/` returns no matches; Pitfall B in 71-RESEARCH.md is the canonical reference |
| Playground route enters public sitemap / robots.txt — exposed dev fixture in production | MEDIUM | Place under `app/_dev/playground/` (leading underscore convention); Next.js does not enforce this — verify by inspecting `app/sitemap.ts` (if exists) and `next-sitemap.config` (if exists); Task 3 acceptance criterion confirms no public-route-table addition; deploy-time should ideally `noindex` but for v1.10 the convention-only suppression is acceptable (this is a fixture for tests, not a production feature) |
| axe-core test passes vacuously (no violations because no component rendered) | MEDIUM | Acceptance criterion: axe spec MUST first assert page mounted and table is visible (`await expect(page.locator('table')).toBeVisible()`) before running rules — prevents false-green on a 404 / blank page |
| Playwright spec passes vacuously due to missing fixture data | MEDIUM | Playground fixture (Task 3) MUST render at minimum 25 rows of test data so pagination (page size 10) produces 3 pages; spec asserts `page.locator('tr').count()` returns at least 11 (10 data rows + 1 header) before any DT-XX assertion |
| `next/dynamic({ ssr: false })` loading state never shown in test — flaky `waitForSelector('skeleton')` | LOW | Use `page.waitForFunction` against a stable post-mount selector; do NOT race against the loading skeleton |

No XSS surface: column headers and cell content come from playground fixture (controlled test data), not user input.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add DT-03 pagination composition to SFDataTable</name>
  <files>components/sf/sf-data-table.tsx</files>
  <read_first>
    - components/sf/sf-data-table.tsx (Plan 02 output — has sort/filter/selection/density; needs pagination wired)
    - components/sf/sf-pagination.tsx (existing primitive — observe sub-component API)
    - .planning/phases/71-sfdatatable/71-RESEARCH.md §Code Examples 4 (verbatim composition)
  </read_first>
  <action>
    Edit `components/sf/sf-data-table.tsx` (the Plan 02 impl) to add DT-03 pagination. Three additions:

    (1) Import additions — extend the existing `@tanstack/react-table` import block to include `getPaginationRowModel` and `type PaginationState`. Extend the existing `@/components/sf` barrel import to include the 6 pagination sub-components: `SFPagination`, `SFPaginationContent`, `SFPaginationItem`, `SFPaginationLink`, `SFPaginationPrevious`, `SFPaginationNext`.

    (2) Props extension — add to `SFDataTableProps<TData>` (note: `pageSize` was already in the Plan 02 type for skeleton sizing — keep it; this just clarifies its dual role for pagination):

        /** Initial page size (default 10). Component owns pagination state internally. */
        pageSize?: number;
        /** Optional controlled pagination state. */
        pagination?: PaginationState;
        onPaginationChange?: (state: PaginationState) => void;

    (3) State + render wiring:
      - State: `const [pagination, setPagination] = useState<PaginationState>({ pageIndex: 0, pageSize: props.pageSize ?? 10 })`. If `props.pagination` is provided, use it for the controlled-state branch and forward `onPaginationChange` updates to the consumer.
      - `useReactTable` config additions: `state: { ..., pagination }`, `onPaginationChange` wires the local setter and the consumer callback, `getPaginationRowModel: getPaginationRowModel()`.
      - Render — AFTER the closing `</SFTable>` and AFTER `</SFScrollArea>`, add a conditional pagination block (only when `table.getPageCount() > 1`):

            {table.getPageCount() > 1 && (
              <SFPagination className="mt-[var(--sfx-space-3)]">
                <SFPaginationContent>
                  <SFPaginationItem>
                    <SFPaginationPrevious
                      onClick={() => table.previousPage()}
                      aria-disabled={!table.getCanPreviousPage()}
                    />
                  </SFPaginationItem>
                  {Array.from({ length: table.getPageCount() }, (_, i) => (
                    <SFPaginationItem key={i}>
                      <SFPaginationLink
                        isActive={table.getState().pagination.pageIndex === i}
                        onClick={() => table.setPageIndex(i)}
                      >
                        {i + 1}
                      </SFPaginationLink>
                    </SFPaginationItem>
                  ))}
                  <SFPaginationItem>
                    <SFPaginationNext
                      onClick={() => table.nextPage()}
                      aria-disabled={!table.getCanNextPage()}
                    />
                  </SFPaginationItem>
                </SFPaginationContent>
              </SFPagination>
            )}

    PRESERVE the `_dep_dt_01_decision` block at top of file VERBATIM. PRESERVE the Plan 02 sort/filter/selection/density logic — pagination is purely additive.

    Commit: `Feat(71-03): SFDataTable pagination composition (DT-03 via SFPagination)`
  </action>
  <verify>
    <automated>grep -q "_dep_dt_01_decision:" components/sf/sf-data-table.tsx && grep -q "getPaginationRowModel" components/sf/sf-data-table.tsx && grep -q "SFPaginationLink" components/sf/sf-data-table.tsx && grep -q "table.previousPage" components/sf/sf-data-table.tsx && grep -q "table.nextPage" components/sf/sf-data-table.tsx && grep -q "table.setPageIndex" components/sf/sf-data-table.tsx && grep -q "table.getPageCount" components/sf/sf-data-table.tsx && pnpm exec tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `_dep_dt_01_decision:` block PRESERVED at top of file (verifiable via `grep -q "_dep_dt_01_decision:"`)
    - File contains the literal substring `getPaginationRowModel` (TanStack import + state config)
    - File contains the literal substring `SFPaginationLink` (composing existing primitive)
    - File contains all four pagination action calls: `table.previousPage`, `table.nextPage`, `table.setPageIndex`, `table.getPageCount`
    - File contains the literal substring `getCanPreviousPage` AND `getCanNextPage` (used in `aria-disabled` props)
    - The pagination block is conditionally rendered: file contains `table.getPageCount() > 1` (no empty pagination on single-page tables)
    - `pnpm exec tsc --noEmit` returns 0 errors
    - `git diff HEAD~1 -- components/sf/sf-data-table.tsx` shows ADDITIONS only (no Plan 02 logic deleted): the line-removed count from diff is 5 or fewer (only existing lines that were re-typed for context belong)
  </acceptance_criteria>
  <done>
    SFDataTable now ships DT-03 pagination via composition with the existing SFPagination primitive. State is owned internally with optional controlled override. Sort/filter/selection from Plan 02 are unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create SFDataTableLazy wrapper (DT-06 P3 lazy)</name>
  <files>components/sf/sf-data-table-lazy.tsx</files>
  <read_first>
    - components/sf/sf-calendar-lazy.tsx (canonical pattern, lines 1-35)
    - components/sf/sf-data-table.tsx (Task 1 output — confirms SFDataTable<TData> generic signature is exported)
    - components/sf/index.ts (verify barrel does NOT yet contain sf-data-table or sf-data-table-lazy)
  </read_first>
  <action>
    Create `components/sf/sf-data-table-lazy.tsx` mirroring `sf-calendar-lazy.tsx` verbatim, adapted for the generic `SFDataTable<TData>`:

        "use client";

        /**
         * Lazy loader for SFDataTable — loads @tanstack/react-table only when rendered.
         *
         * P3 component (Pattern B): NEVER exported from sf/index.ts barrel.
         * Import this file directly when you need the data table.
         *
         * Adding `export { SFDataTableLazy } from "./sf-data-table-lazy"` to
         * components/sf/index.ts will spike homepage First Load JS by ~12 KB
         * (TanStack Table) and break BND-08. Phase 76 audit asserts absence of
         * "sf-data-table" substring from the barrel.
         *
         * @example
         * import { SFDataTableLazy } from "@/components/sf/sf-data-table-lazy";
         *
         * <SFDataTableLazy
         *   data={users}
         *   columns={columns}
         *   density="default"
         *   pageSize={10}
         *   enableRowSelection
         * />
         */

        import dynamic from "next/dynamic";
        import { SFSkeleton } from "@/components/sf";
        import type { SFDataTableProps } from "@/components/sf/sf-data-table";

        const SFDataTableDynamic = dynamic(
          () =>
            import("@/components/sf/sf-data-table").then((m) => ({
              default: m.SFDataTable as <TData>(
                props: SFDataTableProps<TData>
              ) => React.ReactElement,
            })),
          {
            ssr: false,
            loading: () => <SFSkeleton className="h-[400px] w-full" />,
          }
        );

        export function SFDataTableLazy<TData>(props: SFDataTableProps<TData>) {
          return <SFDataTableDynamic {...(props as SFDataTableProps<unknown>)} />;
        }

    NOTE on the generic narrowing: `next/dynamic` does NOT preserve generic type parameters. The cast in the inner `.then` and the prop spread is a localized any-narrowing that we accept here for Pattern B parity with `sf-calendar-lazy.tsx`. Consumers retain full type-safety on `data`/`columns` because `SFDataTableLazy<TData>(props: SFDataTableProps<TData>)` re-narrows at the boundary.

    Do NOT modify `components/sf/index.ts`. Do NOT modify `next.config.ts`.

    Commit: `Feat(71-03): SFDataTableLazy P3 wrapper (DT-06 next/dynamic ssr:false)`
  </action>
  <verify>
    <automated>test -f components/sf/sf-data-table-lazy.tsx && grep -q "export function SFDataTableLazy" components/sf/sf-data-table-lazy.tsx && grep -q "next/dynamic" components/sf/sf-data-table-lazy.tsx && grep -q "ssr: false" components/sf/sf-data-table-lazy.tsx && grep -q "SFSkeleton" components/sf/sf-data-table-lazy.tsx && [ "$(grep -c "sf-data-table" components/sf/index.ts)" = "0" ] && pnpm exec tsc --noEmit 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `components/sf/sf-data-table-lazy.tsx` exists
    - File starts with `"use client";` directive
    - File contains the literal substring `export function SFDataTableLazy<TData>` (generic signature preserved at boundary)
    - File contains the literal substring `next/dynamic` (the lazy mechanism)
    - File contains the literal substring `ssr: false` (P3 contract)
    - File contains the literal substring `SFSkeleton` (loading fallback)
    - File contains the literal substring `import("@/components/sf/sf-data-table")` (correctly references the impl path)
    - JSDoc contains the literal substring `NEVER exported from sf/index.ts barrel` (forward warning to maintainers)
    - `grep -c "sf-data-table" components/sf/index.ts` returns `0` (Pattern B contract holds — neither file in barrel)
    - `pnpm exec tsc --noEmit` returns 0 errors
    - File line count is 20 or more (sufficient for the JSDoc + dynamic block)
  </acceptance_criteria>
  <done>
    Consumer-facing SFDataTableLazy ships. `next/dynamic({ ssr: false })` ensures @tanstack/react-table never enters the SSR bundle and is split into its own lazy chunk. JSDoc warns against barrel inclusion.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create playground fixture for Playwright tests</name>
  <files>app/_dev/playground/sf-data-table/page.tsx</files>
  <read_first>
    - app/ (verify _dev/ does not yet exist; if it does, this is fine — just add the new sf-data-table subdir)
    - components/sf/sf-data-table-lazy.tsx (Task 2 output — the component this fixture mounts)
    - .planning/phases/71-sfdatatable/71-VALIDATION.md §Wave 0 Requirements (playground fixture spec)
    - app/sitemap.ts if it exists (verify _dev/* is not a sitemap entry; do NOT add)
  </read_first>
  <action>
    Create the directory chain `app/_dev/playground/sf-data-table/` and place `page.tsx` inside.

    File `app/_dev/playground/sf-data-table/page.tsx`:

        /**
         * Playground fixture — Phase 71 Playwright + axe-core tests target this URL.
         *
         * Path: /_dev/playground/sf-data-table
         *
         * Convention-only suppression: leading underscore in `_dev` keeps this out
         * of typical sitemap/discovery paths. This is a TEST FIXTURE, not a public
         * surface. Do not link to it from production navigation. If a sitemap or
         * robots.txt is added in a future phase, ensure /_dev/* is excluded.
         */

        "use client";

        import { useState } from "react";
        import type { ColumnDef } from "@tanstack/react-table";
        import { SFDataTableLazy } from "@/components/sf/sf-data-table-lazy";

        type Row = {
          id: string;
          name: string;
          status: "active" | "pending" | "archived";
          created: string;
        };

        // 25 rows of test data — exercises pagination (page size 10 => 3 pages),
        // filter (typing "user-1" matches user-1, user-10..19), and sort cycles.
        const data: Row[] = Array.from({ length: 25 }, (_, i) => ({
          id: `user-${i}`,
          name: `User ${String.fromCharCode(65 + (i % 26))}${i}`,
          status: (["active", "pending", "archived"] as const)[i % 3],
          created: `2026-${String((i % 12) + 1).padStart(2, "0")}-15`,
        }));

        const columns: ColumnDef<Row, unknown>[] = [
          { accessorKey: "id", header: "ID", enableSorting: true, enableColumnFilter: true },
          { accessorKey: "name", header: "Name", enableSorting: true, enableColumnFilter: true },
          { accessorKey: "status", header: "Status", enableSorting: true },
          { accessorKey: "created", header: "Created", enableSorting: true },
        ];

        export default function SFDataTablePlayground() {
          const [selected, setSelected] = useState({});

          return (
            <main className="p-[var(--sfx-space-6)] min-h-screen bg-background text-foreground">
              <h1 className="font-mono text-xs uppercase tracking-wider mb-[var(--sfx-space-4)]">
                Phase 71 — SFDataTable Playground
              </h1>
              <p data-testid="row-count" className="font-mono text-xs mb-[var(--sfx-space-3)]">
                Selected: {Object.keys(selected).length} of {data.length}
              </p>
              <SFDataTableLazy<Row>
                data={data}
                columns={columns}
                density="default"
                pageSize={10}
                enableRowSelection
                rowSelection={selected}
                onRowSelectionChange={setSelected}
              />
            </main>
          );
        }

    Do NOT add this route to any sitemap, navigation, or public route table. The leading-underscore convention plus the absence of links from production routes is the suppression mechanism.

    Commit: `Chore(71-03): playground fixture for Phase 71 Playwright tests (kept out of public route map)`
  </action>
  <verify>
    <automated>test -f app/_dev/playground/sf-data-table/page.tsx && grep -q "SFDataTableLazy" app/_dev/playground/sf-data-table/page.tsx && grep -q "enableRowSelection" app/_dev/playground/sf-data-table/page.tsx && grep -q "length: 25" app/_dev/playground/sf-data-table/page.tsx && grep -q "data-testid=\"row-count\"" app/_dev/playground/sf-data-table/page.tsx && pnpm exec tsc --noEmit 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `app/_dev/playground/sf-data-table/page.tsx` exists
    - File starts with `"use client"` directive (required for `useState`)
    - File imports `SFDataTableLazy` from `"@/components/sf/sf-data-table-lazy"` (exact path)
    - File generates exactly 25 rows of test data: `grep -q "length: 25" app/_dev/playground/sf-data-table/page.tsx` returns true
    - File renders the table with `enableRowSelection`, `pageSize={10}`, `density="default"`
    - File has a `data-testid="row-count"` element exposing selected count for Playwright assertions
    - File default-exports a function (`grep -q "export default function" app/_dev/playground/sf-data-table/page.tsx` returns true)
    - File does NOT modify `app/sitemap.ts` or any other public-route table (verifiable: `git status --porcelain` shows ONLY the new playground file path)
    - `pnpm exec tsc --noEmit` returns 0 errors
  </acceptance_criteria>
  <done>
    Playwright + axe specs (Tasks 4, 5) have a deterministic mount point with 25 rows of fixture data. The route is suppressed from public discovery via the leading-underscore convention.
  </done>
</task>

<task type="auto">
  <name>Task 4: Playwright spec — DT-01/02/03/04 acceptance tests</name>
  <files>tests/v1.10-phase71-sf-data-table.spec.ts</files>
  <read_first>
    - tests/v1.9-phase66-arc-axe.spec.ts (precedent for ABS_BASE pattern + viewport setup)
    - playwright.config.ts (verify webServer config + chromium project)
    - app/_dev/playground/sf-data-table/page.tsx (Task 3 fixture URL + 25-row dataset shape)
    - components/sf/sf-data-table.tsx (Task 1 output — confirm pagination renders only when getPageCount() > 1)
  </read_first>
  <action>
    Create `tests/v1.10-phase71-sf-data-table.spec.ts`. Skeleton:

        import { test, expect } from "@playwright/test";

        const ABS_BASE = process.env.CAPTURE_BASE_URL ?? "http://localhost:3000";
        const PLAYGROUND_URL = `${ABS_BASE}/_dev/playground/sf-data-table`;

        test.describe("@v1.10-phase71 SFDataTable", () => {
          test.beforeEach(async ({ page }) => {
            await page.goto(PLAYGROUND_URL, { waitUntil: "networkidle" });
            // Sanity gate: ensure fixture mounted (prevents vacuous green on 404)
            await expect(page.locator("table")).toBeVisible();
            await expect(page.locator("tbody tr")).toHaveCount(10); // page size 10
          });

          test("DT-01: sort cycle asc -> desc -> none on column header click", async ({ page }) => {
            const header = page.locator("th button", { hasText: "ID" });
            const th = page.locator("th", { hasText: "ID" });
            await expect(th).toHaveAttribute("aria-sort", "none");
            await header.click();
            await expect(th).toHaveAttribute("aria-sort", "ascending");
            await header.click();
            await expect(th).toHaveAttribute("aria-sort", "descending");
            await header.click();
            await expect(th).toHaveAttribute("aria-sort", "none");
          });

          test("DT-01: keyboard Enter and Space trigger sort identically", async ({ page }) => {
            const header = page.locator("th button", { hasText: "ID" });
            await header.focus();
            await page.keyboard.press("Enter");
            await expect(page.locator("th", { hasText: "ID" })).toHaveAttribute("aria-sort", "ascending");
            await page.keyboard.press("Space");
            await expect(page.locator("th", { hasText: "ID" })).toHaveAttribute("aria-sort", "descending");
          });

          test("DT-02: filter input debounces (300ms) and reduces row count", async ({ page }) => {
            const input = page.getByLabel("Global filter");
            await input.fill("user-1");
            await page.waitForTimeout(350); // > 300ms debounce window
            const rowCount = await page.locator("tbody tr").count();
            // user-1, user-10..19 = 11 matches; first page caps at 10
            expect(rowCount).toBeLessThanOrEqual(10);
            expect(rowCount).toBeGreaterThan(0);
          });

          test("DT-03: pagination — page 2 click advances pageIndex", async ({ page }) => {
            // 25 rows / 10 per page = 3 pages
            await expect(page.locator("[role='navigation'], nav").first()).toBeVisible();
            const page2 = page.locator("a, button", { hasText: /^2$/ }).first();
            await page2.click();
            // After click, the active page link should be 2 (data-active or isActive class)
            // Generic check: clicking advances visibly different rows
            const firstRowAfter = await page.locator("tbody tr").first().textContent();
            expect(firstRowAfter).toContain("user-1"); // user-10 starts page 2 (sorted lex by default? — adapt as needed)
          });

          test("DT-04: row selection — single + multi + indeterminate header state", async ({ page }) => {
            const headerCheckbox = page.locator("thead input[type='checkbox'], thead [role='checkbox']").first();
            const firstRowCheckbox = page.locator("tbody tr input[type='checkbox'], tbody tr [role='checkbox']").first();

            // Single row click
            await firstRowCheckbox.click();
            await expect(headerCheckbox).toHaveAttribute("data-state", "indeterminate");

            // Click header to toggle all
            await headerCheckbox.click();
            await expect(headerCheckbox).toHaveAttribute("data-state", "checked");

            // The data-testid="row-count" mirrors selected count
            await expect(page.locator("[data-testid='row-count']")).toContainText(/Selected: \d+/);
          });
        });

    Adjust the DT-03 assertion if your default sort order differs — the goal is "page 2 click changes the displayed rows". The DT-04 checkbox locator may need adjustment for Radix's actual DOM (Radix uses `[role='checkbox']` + a hidden `<button>`); inspect the rendered DOM if the strict-mode locator matches multiple elements.

    Run: `pnpm exec playwright test tests/v1.10-phase71-sf-data-table.spec.ts --project=chromium --reporter=line`
    Expected: 5 tests pass (or pass with --headed inspection of any locator-resolution issues).

    Commit: `Test(71-03): Playwright spec for DT-01/02/03/04 acceptance (TST-03)`
  </action>
  <verify>
    <automated>test -f tests/v1.10-phase71-sf-data-table.spec.ts && grep -q "DT-01" tests/v1.10-phase71-sf-data-table.spec.ts && grep -q "DT-02" tests/v1.10-phase71-sf-data-table.spec.ts && grep -q "DT-03" tests/v1.10-phase71-sf-data-table.spec.ts && grep -q "DT-04" tests/v1.10-phase71-sf-data-table.spec.ts && grep -q "ABS_BASE" tests/v1.10-phase71-sf-data-table.spec.ts && grep -q "_dev/playground/sf-data-table" tests/v1.10-phase71-sf-data-table.spec.ts && grep -q "aria-sort" tests/v1.10-phase71-sf-data-table.spec.ts && grep -q "indeterminate" tests/v1.10-phase71-sf-data-table.spec.ts && pnpm exec playwright test tests/v1.10-phase71-sf-data-table.spec.ts --project=chromium --reporter=line</automated>
  </verify>
  <acceptance_criteria>
    - `tests/v1.10-phase71-sf-data-table.spec.ts` exists
    - File contains exactly 5 named test cases via `test(...)` calls (DT-01 sort cycle, DT-01 keyboard, DT-02 filter, DT-03 pagination, DT-04 selection): `grep -c '^  test(' tests/v1.10-phase71-sf-data-table.spec.ts` returns 5
    - File contains the literal substring `_dev/playground/sf-data-table` (correct fixture URL)
    - File contains the literal substring `ABS_BASE` (matches Phase 66 axe-spec pattern for CI portability)
    - File contains the literal substring `aria-sort` (DT-01 verification)
    - File contains the literal substring `data-state` and `indeterminate` (DT-04 verification)
    - `beforeEach` asserts `await expect(page.locator("table")).toBeVisible()` and `await expect(page.locator("tbody tr")).toHaveCount(10)` — prevents vacuous green
    - `pnpm exec playwright test tests/v1.10-phase71-sf-data-table.spec.ts --project=chromium` exits 0 (5/5 tests pass)
  </acceptance_criteria>
  <done>
    DT-01, DT-02, DT-03, DT-04 are all behaviorally verified by Playwright against the production-shape playground fixture. Tests run in < 30s per the Phase 71 validation latency budget.
  </done>
</task>

<task type="auto">
  <name>Task 5: axe-core spec + production chunk audit + bundle gate (TST-03 + DEP-01 closeout)</name>
  <files>tests/v1.10-phase71-sf-data-table-axe.spec.ts</files>
  <read_first>
    - tests/v1.9-phase66-arc-axe.spec.ts (precedent for AxeBuilder per-rule scan pattern)
    - tests/v1.8-phase63-1-bundle-budget.spec.ts (existing 200 KB hard-constraint spec; will be re-run as part of this task)
    - .planning/phases/71-sfdatatable/71-RESEARCH.md §Pitfall B (devtools subpath)
    - app/_dev/playground/sf-data-table/page.tsx (fixture URL)
  </read_first>
  <action>
    PART A — axe-core spec.

    Create `tests/v1.10-phase71-sf-data-table-axe.spec.ts`. Skeleton (mirrors `tests/v1.9-phase66-arc-axe.spec.ts:42-99` direct-rule pattern):

        import { test, expect } from "@playwright/test";
        import AxeBuilder from "@axe-core/playwright";

        const ABS_BASE = process.env.CAPTURE_BASE_URL ?? "http://localhost:3000";
        const PLAYGROUND_URL = `${ABS_BASE}/_dev/playground/sf-data-table`;

        test.describe("@v1.10-phase71 SFDataTable axe-core", () => {
          test.beforeEach(async ({ page }) => {
            await page.goto(PLAYGROUND_URL, { waitUntil: "networkidle" });
            // Vacuous-green guard
            await expect(page.locator("table")).toBeVisible();
          });

          test("DT-01 button-name + aria-sort: sort headers are accessible buttons with valid aria-sort", async ({ page }) => {
            const results = await new AxeBuilder({ page })
              .withRules(["button-name", "aria-allowed-attr", "aria-valid-attr-value"])
              .analyze();
            const violations = results.violations.filter((v) =>
              ["button-name", "aria-allowed-attr", "aria-valid-attr-value"].includes(v.id)
            );
            expect(
              violations,
              `axe violations on sort headers (DT-01):\n` +
                violations.map((v) => `  ${v.id}: ${v.description}\n${v.nodes.slice(0, 3).map((n) => `    ${n.html}`).join("\n")}`).join("\n")
            ).toHaveLength(0);
          });

          test("DT-04 label: row-selection checkboxes have accessible names", async ({ page }) => {
            const results = await new AxeBuilder({ page }).withRules(["label", "aria-checked"]).analyze();
            const violations = results.violations.filter((v) => ["label", "aria-checked"].includes(v.id));
            expect(violations).toHaveLength(0);
          });

          test("DT-05 color-contrast: all rendered text meets WCAG AA (light scheme)", async ({ page }) => {
            const results = await new AxeBuilder({ page }).withRules(["color-contrast"]).analyze();
            const violations = results.violations.filter((v) => v.id === "color-contrast");
            expect(violations).toHaveLength(0);
          });
        });

    PART B — production chunk audit (DEP-01 closeout).

    Run the BND-04 stale-chunk guard + production build + chunk audit, in that order:

        rm -rf .next/cache .next
        ANALYZE=true pnpm build 2>&1 | tee /tmp/71-03-bundle.log

    Then audit:

    (B1) Verify @tanstack/react-table is NOT in homepage chunk manifest:

        node -e "
          const m = require('./.next/build-manifest.json');
          const homepageChunks = m.pages['/'] || [];
          console.log('Homepage / chunks:', homepageChunks.length);
          // Read each chunk file and check for tanstack symbol presence
          const fs = require('fs');
          const path = require('path');
          let leaks = 0;
          for (const chunk of homepageChunks) {
            const fp = path.join('.next', chunk);
            if (!fs.existsSync(fp)) continue;
            const src = fs.readFileSync(fp, 'utf8');
            if (/@tanstack\\/react-table|tanstack-react-table/.test(src)) {
              console.error('LEAK:', chunk);
              leaks++;
            }
          }
          if (leaks > 0) { process.exit(1); }
          console.log('OK: @tanstack/react-table absent from homepage / chunks');
        "

    Expected: "OK: @tanstack/react-table absent from homepage / chunks". If LEAK appears, STOP — Pattern B is broken.

    (B2) Verify devtools string absent from .next/static:

        if grep -rEl "react-table-devtools|tanstack.*devtools" .next/static/ 2>/dev/null; then
          echo "FAIL: devtools string leaked into production static chunks"
          exit 1
        else
          echo "OK: zero devtools references in .next/static/"
        fi

    Expected: "OK: zero devtools references in .next/static/".

    (B3) Re-run the existing 200 KB budget spec:

        pnpm exec playwright test tests/v1.8-phase63-1-bundle-budget.spec.ts --project=chromium --reporter=line

    Expected: PASS. Homepage First Load JS still 200 KB or less (the entire SFDataTable surface is in a lazy chunk; homepage-shape unchanged).

    PART C — back-fill `_dep_dt_01_decision.bundle_evidence` with the lazy-chunk ID.

    From the bundle build, extract the lazy chunk that contains @tanstack/react-table:

        node -e "
          const fs = require('fs');
          const path = require('path');
          const dir = '.next/static/chunks';
          for (const f of fs.readdirSync(dir)) {
            if (!f.endsWith('.js')) continue;
            const src = fs.readFileSync(path.join(dir, f), 'utf8');
            if (/@tanstack\\/react-table/.test(src)) {
              const stat = fs.statSync(path.join(dir, f));
              console.log(f, stat.size, 'bytes');
            }
          }
        "

    Append the result to the `bundle_evidence` array in `components/sf/sf-data-table.tsx`. Add a final line:

        //     - "Lazy chunk hosting @tanstack/react-table: <chunk-name>.js, <bytes> raw bytes (post-Phase-71 Plan 03 close measurement)"
        //     - "Final ratification: Phase 71 Plan 03 close (TST-03 green; production chunk audit OK; BND-08 200 KB green)"

    Replace `<chunk-name>` and `<bytes>` with measured values.

    Run: `pnpm exec playwright test tests/v1.10-phase71-sf-data-table-axe.spec.ts --project=chromium --reporter=line`
    Expected: 3/3 axe tests pass.

    Commit: `Test(71-03): axe spec + production chunk audit + DEP-01 close (TST-03)` — bundles axe spec, the back-filled decision block update, and the audit script run.
  </action>
  <verify>
    <automated>test -f tests/v1.10-phase71-sf-data-table-axe.spec.ts && grep -q "AxeBuilder" tests/v1.10-phase71-sf-data-table-axe.spec.ts && grep -q "button-name" tests/v1.10-phase71-sf-data-table-axe.spec.ts && grep -q "color-contrast" tests/v1.10-phase71-sf-data-table-axe.spec.ts && grep -q "Lazy chunk hosting @tanstack/react-table" components/sf/sf-data-table.tsx && rm -rf .next/cache .next && ANALYZE=true pnpm build > /tmp/71-03-build.log 2>&1 && ! grep -rEl "react-table-devtools|tanstack.*devtools" .next/static/ 2>/dev/null && pnpm exec playwright test tests/v1.10-phase71-sf-data-table-axe.spec.ts --project=chromium --reporter=line && pnpm exec playwright test tests/v1.8-phase63-1-bundle-budget.spec.ts --project=chromium --reporter=line</automated>
  </verify>
  <acceptance_criteria>
    - `tests/v1.10-phase71-sf-data-table-axe.spec.ts` exists
    - File contains exactly 3 named axe tests (button-name+aria-sort, label+aria-checked, color-contrast): `grep -c '^  test(' tests/v1.10-phase71-sf-data-table-axe.spec.ts` returns 3
    - File contains the literal substring `AxeBuilder` (mirrors Phase 66 precedent)
    - `pnpm exec playwright test tests/v1.10-phase71-sf-data-table-axe.spec.ts --project=chromium` exits 0 (3/3 axe tests pass; zero violations)
    - `rm -rf .next/cache .next && ANALYZE=true pnpm build` exits 0
    - `grep -rEl "react-table-devtools|tanstack.*devtools" .next/static/` returns NO matches (PART B2 — devtools absent from production)
    - `node -e ...` chunk audit (PART B1) prints `OK: @tanstack/react-table absent from homepage / chunks` (Pattern B contract verified at production-build level)
    - `tests/v1.8-phase63-1-bundle-budget.spec.ts` PASSES (homepage `/` First Load JS 200 KB or less)
    - `components/sf/sf-data-table.tsx` `_dep_dt_01_decision.bundle_evidence` contains a final entry of the form `Lazy chunk hosting @tanstack/react-table: <name>.js, <N> raw bytes` with measured values (verifiable: `grep -E "Lazy chunk hosting @tanstack/react-table:.*\\.js, [0-9]+ raw bytes" components/sf/sf-data-table.tsx` returns 1 match)
    - `git status --porcelain` shows ONLY the 5 expected Plan 03 file paths modified (no `node_modules`, no `.next`, no worktree leakage)
  </acceptance_criteria>
  <done>
    All 8 Phase 71 requirement IDs satisfied. axe-core green on 3 rule clusters covering DT-01/04/05. Production-grade chunk audit confirms (a) Pattern B holds at the bundle level, (b) devtools absent from production, (c) 200 KB BND budget green. `_dep_dt_01_decision.bundle_evidence` is updated with the lazy-chunk size measurement closing DEP-01. Phase 71 ships clean.
  </done>
</task>

</tasks>

<verification>
End-to-end verification of Plan 03 (also closes Phase 71):

(1) Full Playwright + axe pass against the playground fixture:

    pnpm exec playwright test tests/v1.10-phase71-sf-data-table*.spec.ts --project=chromium

Expected: 5 + 3 = 8/8 tests green.

(2) Production-grade bundle audit:

    rm -rf .next/cache .next
    ANALYZE=true pnpm build
    pnpm exec playwright test tests/v1.8-phase63-1-bundle-budget.spec.ts --project=chromium

Expected: build OK, budget spec PASS, homepage First Load JS 200 KB or less.

(3) Pattern B + D-04 locks intact:

    grep -c "sf-data-table" components/sf/index.ts        # must be 0
    grep -c "@tanstack/react-table" next.config.ts        # must be 0

Expected: both 0.

(4) Production chunk audit:

    grep -rEl "react-table-devtools|tanstack.*devtools" .next/static/  # must be empty

Expected: empty (no matches).

(5) Decision block fully populated with measured values, no TBD strings remaining:

    ! grep -E "TBD" components/sf/sf-data-table.tsx
    grep -E "Lazy chunk hosting @tanstack/react-table:.*\\.js, [0-9]+ raw bytes" components/sf/sf-data-table.tsx | wc -l

Expected: first command empty, second command returns 1.

(6) TypeScript clean:

    pnpm exec tsc --noEmit

Expected: 0 errors.

(7) Worktree clean:

    git status --porcelain

Expected: only expected files (Plan 03 file set) staged or modified; no leakage from `.claude/worktrees/`.
</verification>

<success_criteria>
- [ ] DT-03 pagination composition added to `components/sf/sf-data-table.tsx` via SFPagination primitives
- [ ] `components/sf/sf-data-table-lazy.tsx` ships SFDataTableLazy<TData> via next/dynamic({ ssr: false }) with SFSkeleton fallback
- [ ] `app/_dev/playground/sf-data-table/page.tsx` mounts the lazy wrapper with 25 rows of fixture data
- [ ] `tests/v1.10-phase71-sf-data-table.spec.ts` ships 5 Playwright tests covering DT-01..04; all green
- [ ] `tests/v1.10-phase71-sf-data-table-axe.spec.ts` ships 3 axe-core scans (button-name+aria-sort, label+aria-checked, color-contrast); all green
- [ ] Production build: @tanstack/react-table absent from homepage chunk manifest; present only in a lazy chunk
- [ ] Production build: zero `devtools` substring matches under `.next/static/`
- [ ] `tests/v1.8-phase63-1-bundle-budget.spec.ts` PASSES (200 KB or less homepage First Load JS)
- [ ] `_dep_dt_01_decision.bundle_evidence` contains the measured lazy-chunk name + size
- [ ] `components/sf/index.ts` and `next.config.ts` UNCHANGED across all of Phase 71
- [ ] Phase 71 closes with all 8 requirement IDs (DT-01, DT-02, DT-03, DT-04, DT-05, DT-06, DEP-01, TST-03) satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/71-sfdatatable/71-03-SUMMARY.md` documenting:
- The 5 commits across Tasks 1-5 (or however many landed)
- Final lazy-chunk name + raw size in bytes
- Final homepage / First Load JS measurement
- Playwright spec result (8/8 green)
- axe-core spec result (3/3 green)
- Production chunk-audit result (devtools absent, tanstack absent from homepage)
- Confirmation that all 8 Phase 71 requirement IDs are satisfied

Then create `.planning/phases/71-sfdatatable/71-SUMMARY.md` (phase-level summary) referencing the three plan summaries and recording the v1.10 milestone _dep_X_decision precedent that's now established for Phases 73 (`_dep_re_01_decision`) and any future runtime-dep additions.
</output>
