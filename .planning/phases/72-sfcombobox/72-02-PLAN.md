---
phase: 72-sfcombobox
plan: 02
type: execute
wave: 2
depends_on:
  - 72-01
files_modified:
  - components/sf/sf-combobox.tsx
  - app/dev-playground/sf-combobox/page.tsx
  - tests/v1.10-phase72-sf-combobox.spec.ts
  - tests/v1.10-phase72-sf-combobox-axe.spec.ts
autonomous: true
requirements:
  - CB-03
  - CB-04
  - TST-03
gap_closure: false
must_haves:
  truths:
    - "A consumer renders SFCombobox with multiple={true} and a value: string[] controlled API — selecting an option toggles its presence in the value array; popover STAYS OPEN after selection (does not close)"
    - "Selected items render as SFBadge intent='outline' chips inside the trigger button area; each chip has a remove × affordance with aria-label='Remove {label}' that stops propagation and removes only that value"
    - "Multi-select listbox carries aria-multiselectable='true' on the SFCommandList — verifiable via DOM inspection AND axe-core aria-allowed-attr rule"
    - "Playwright spec at tests/v1.10-phase72-sf-combobox.spec.ts mounts the playground fixture and verifies CB-01 (open/filter/keyboard/Enter/Escape), CB-02 (clear + grouping), and CB-03 (multi-select chips + stay-open + chip removal); all tests green"
    - "axe-core spec at tests/v1.10-phase72-sf-combobox-axe.spec.ts opens the popover BEFORE analyze() runs (vacuous-green guard via assert listbox visible) and verifies zero violations on aria-allowed-attr, aria-required-children, listbox accessible name, button-name (trigger + clear + chip remove), color-contrast, aria-multiselectable on multi-select"
    - "Production build (ANALYZE=true pnpm build, fresh cache) verifies cmdk is ABSENT from the homepage / First Load chunk manifest (homepage does not render SFCombobox); cmdk appears only in chunks for routes that mount SFCombobox"
    - "Homepage / First Load JS remains under or equal to 200 KB after the full Phase 72 component lands (BND budget green; cmdk already in optimizePackageImports D-04 list — no new entry added)"
  artifacts:
    - path: "components/sf/sf-combobox.tsx"
      provides: "Multi-select branch replacing the Plan 01 console.warn fallback — SFBadge chips inside trigger, aria-multiselectable, popover stay-open on select, chip remove × with stopPropagation"
      contains: "SFBadge"
    - path: "app/dev-playground/sf-combobox/page.tsx"
      provides: "Playground fixture mounting all 6 test sections (single uncontrolled, single controlled+grouped, multi uncontrolled, loading, empty, grouped-only); kept out of public route map"
      exports: ["default"]
      min_lines: 120
    - path: "tests/v1.10-phase72-sf-combobox.spec.ts"
      provides: "Playwright spec — CB-01 keyboard/filter/Enter/Escape, CB-02 clear+grouping, CB-03 multi-select chips+stay-open+remove (≥10 named tests)"
    - path: "tests/v1.10-phase72-sf-combobox-axe.spec.ts"
      provides: "axe-core spec — open-state listbox, aria-allowed-attr, aria-required-children, button-name, color-contrast, aria-multiselectable; vacuous-green guard before each analyze()"
  key_links:
    - from: "components/sf/sf-combobox.tsx"
      to: "components/sf/sf-badge.tsx"
      via: "barrel import — SFBadge intent='outline' for multi-select chips"
      pattern: "SFBadge"
    - from: "app/dev-playground/sf-combobox/page.tsx"
      to: "components/sf/sf-combobox.tsx"
      via: "barrel import — SFCombobox is Pattern C, consumed via @/components/sf"
      pattern: "from ['\"]@/components/sf['\"]"
    - from: "tests/v1.10-phase72-sf-combobox.spec.ts"
      to: "app/dev-playground/sf-combobox/page.tsx"
      via: "Playwright page.goto fixture URL"
      pattern: "/dev-playground/sf-combobox"
    - from: "tests/v1.10-phase72-sf-combobox-axe.spec.ts"
      to: "@axe-core/playwright"
      via: "AxeBuilder direct rule scan (precedent: tests/v1.10-phase71-sf-data-table-axe.spec.ts)"
      pattern: "AxeBuilder"
---

<objective>
Close Phase 72 with the multi-select branch (CB-03), the playground fixture for tests, and the test specs (TST-03) — then run the production-grade bundle audit that confirms cmdk is absent from the homepage First Load chunk despite SFCombobox being barrel-exported. The Plan 01 single-select impl already shipped; Plan 02 finishes the discriminated-union implementation by replacing the v0.1 multi-select fallback with the real SFBadge-chip impl, then ships the Playwright + axe-core specs against a 6-section playground fixture.

Purpose: Plan 01 shipped CB-01/CB-02/CB-04 component-side. Plan 02 ships CB-03 (multi-select via `multiple` prop with chip rendering and `value: string[]` API), the TST-03 acceptance gate (≥10 Playwright tests + ≥6 axe-core scans), and the bundle gate that confirms barrel-exporting SFCombobox does NOT spike homepage First Load JS — cmdk only appears in route-bound chunks for pages that actually mount SFCombobox. This closes Phase 72.

Output: 4 files modified — `components/sf/sf-combobox.tsx` (replace Plan 01 multi fallback with real impl), new `app/dev-playground/sf-combobox/page.tsx` fixture, two new test files. Phase 72 closes with all 5 requirement IDs satisfied (CB-01, CB-02, CB-03, CB-04, TST-03).
</objective>

<execution_context>
@/Users/greyaltaer/.claude/pde-os/engines/gsd/workflows/execute-plan.md
@/Users/greyaltaer/.claude/pde-os/engines/gsd/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/72-sfcombobox/72-RESEARCH.md
@.planning/phases/72-sfcombobox/72-VALIDATION.md
@.planning/phases/72-sfcombobox/72-01-SUMMARY.md
@CLAUDE.md
@.planning/LOCKDOWN.md
@components/sf/sf-combobox.tsx
@components/sf/sf-badge.tsx
@components/sf/index.ts
@app/dev-playground/sf-data-table/page.tsx
@tests/v1.10-phase71-sf-data-table.spec.ts
@tests/v1.10-phase71-sf-data-table-axe.spec.ts
@tests/v1.8-phase63-1-bundle-budget.spec.ts
@playwright.config.ts
@next.config.ts

<interfaces>
<!-- SFBadge API (verified at components/sf/sf-badge.tsx:5-20) -->

import { SFBadge } from "@/components/sf";

<SFBadge intent="outline">  // 2px border-foreground, transparent bg, font-mono uppercase
  {label}
  <button type="button" aria-label={`Remove ${label}`} onClick={...}>×</button>
</SFBadge>

<!-- aria-multiselectable on listbox (cmdk does NOT auto-set; consumer must thread) -->

<SFCommandList aria-multiselectable={multiple ? true : undefined}>
  ...
</SFCommandList>

<!-- Playground fixture path (verified Phase 71 actually shipped at app/dev-playground/, NOT app/_dev/playground/) -->

app/dev-playground/sf-combobox/page.tsx  → URL /dev-playground/sf-combobox

<!-- @axe-core/playwright precedent (Phase 71 Plan 03 pattern) -->

import { test, expect } from "@playwright/test";
import AxeBuilder from "@axe-core/playwright";

const ABS_BASE = process.env.CAPTURE_BASE_URL ?? "http://localhost:3000";
const PLAYGROUND_URL = `${ABS_BASE}/dev-playground/sf-combobox`;

test.beforeEach(async ({ page }) => {
  await page.goto(PLAYGROUND_URL, { waitUntil: "networkidle" });
  // VACUOUS-GREEN GUARD: open the popover and assert listbox visible BEFORE analyze()
  await page.getByTestId("section-1-trigger").click();
  await expect(page.locator('[role="listbox"]').first()).toBeVisible();
});

const results = await new AxeBuilder({ page })
  .withRules(["aria-allowed-attr", "aria-required-children", "button-name"])
  .analyze();

<!-- Existing 200 KB budget spec -->

tests/v1.8-phase63-1-bundle-budget.spec.ts:22 — const BUDGET_BYTES = 200 * 1024;
</interfaces>

<threat_model>
ASVS L1 — Phase 72 Plan 02 threat surface:

| Threat | Severity | Mitigation |
|--------|----------|------------|
| Bundle escape: cmdk drags into homepage First Load chunk because SFCombobox is now barrel-exported AND something on the homepage transitively touches the barrel — homepage page renders SFCombobox indirectly | HIGH | Acceptance criterion: post-build chunk audit greps homepage `/` build-manifest chunks for `cmdk` substring; expected absent. cmdk is in optimizePackageImports — Next.js per-route tree-shakes barrel imports, so homepage chunks should not contain cmdk unless homepage code actually mounts SFCombobox. The homepage `/` page does NOT render SFCombobox; this verifies that empirically. |
| Vacuous-green axe pass: axe scan runs on closed popover (listbox not in DOM) → zero violations because nothing was scanned | HIGH | beforeEach hook clicks trigger AND asserts `[role="listbox"]` visible BEFORE analyze(); acceptance criterion verifies the spec contains both `await page.getByTestId(...).click()` AND `await expect(page.locator('[role="listbox"]')).toBeVisible()` BEFORE the AxeBuilder call |
| Multi-select aria-multiselectable not threaded — axe `aria-allowed-attr` flags missing required attribute on listbox in multi-mode | HIGH | Acceptance criterion: source contains `aria-multiselectable={multiple ?` literal; axe spec for multi-select section asserts `[aria-multiselectable="true"]` selector present BEFORE running aria-allowed-attr scan |
| Chip remove button propagates click to trigger — opens popover unintentionally on chip removal | MEDIUM | Acceptance criterion: chip remove button onClick contains `e.stopPropagation()` literal; Playwright test verifies clicking × removes chip without opening popover |
| Nested interactive elements: chip remove `<button>` rendered as descendant of trigger `<button>` — invalid HTML, focus trap misfires | HIGH | Architecture from research §Anti-Pattern 5: chips and remove buttons render INSIDE the trigger button content area but the trigger itself is the OUTER button; the actual chip-remove `<button>` is a SIBLING to the trigger button, NOT nested. Playwright test verifies tab-focus path: Tab → trigger button → chip remove button (not trigger → trigger again). axe `nested-interactive` rule covers this. |
| Playground fixture leaks into public sitemap | MEDIUM | Mirror Phase 71 convention — leading-`dev-playground/` prefix; do NOT add to sitemap.ts; acceptance criterion verifies app/sitemap.ts unchanged |
| Playwright spec passes vacuously due to missing fixture data | MEDIUM | Each spec asserts the corresponding section's option count BEFORE running CB-XX assertions — e.g., section-3 (multi) must show 5 visible options after open before chip-test runs |

No XSS surface in tests/fixtures (controlled test data, not user input). Production component XSS surface is option labels rendered as text — see Plan 01 threat-model.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Replace Plan 01 multi-select fallback with real impl (CB-03 — SFBadge chips, stay-open, aria-multiselectable)</name>
  <files>components/sf/sf-combobox.tsx</files>
  <read_first>
    - components/sf/sf-combobox.tsx (Plan 01 output — has single-select impl + multi-select console.warn fallback at the top of the function body)
    - components/sf/sf-badge.tsx (verify SFBadge intent="outline" API; chips will use this)
    - components/sf/index.ts (verify SFBadge is exported via barrel for Pattern C consumption)
    - .planning/phases/72-sfcombobox/72-RESEARCH.md §Multi-Select State (verbatim handleSelect)
    - .planning/phases/72-sfcombobox/72-RESEARCH.md §Chip Rendering (Multi-Select) — verbatim chip JSX
    - .planning/phases/72-sfcombobox/72-RESEARCH.md §A11y Mechanics (aria-multiselectable on SFCommandList)
    - .planning/phases/72-sfcombobox/72-RESEARCH.md §Anti-Pattern 4 (do NOT setOpen(false) in multi-select onSelect)
  </read_first>
  <action>
    Edit `components/sf/sf-combobox.tsx` to replace the Plan 01 multi-select fallback with the real impl. The Plan 01 file contains an `if (isMulti && process.env.NODE_ENV !== "production")` console.warn block — that block is REPLACED with branching state + handlers + chip rendering.

    Five additions:

    (1) Extend the imports block at the top — add SFBadge to the `from "@/components/sf"` import:

        import {
          SFPopover,
          SFPopoverTrigger,
          SFPopoverContent,
          SFButton,
          SFBadge,         // NEW for CB-03 chips
        } from "@/components/sf";

    (2) Replace the entire single-state-only branch (`const [internalValue, setInternalValue] = useState<string | undefined>(...)`, `selectedValue`, `handleSelect`, `handleClear`) with discriminated-union state. Replace lines roughly between `const [open, setOpen] = useState(false);` and `const grouped = useMemo(...)` with:

        const [open, setOpen] = useState(false);

        // Single-select state branch
        const isControlledSingle = !isMulti && (props as SFComboboxSingleProps).value !== undefined;
        const [internalSingleValue, setInternalSingleValue] = useState<string | undefined>(
          !isMulti ? (props as SFComboboxSingleProps).defaultValue : undefined
        );
        const selectedSingleValue: string | undefined = isControlledSingle
          ? (props as SFComboboxSingleProps).value
          : internalSingleValue;

        // Multi-select state branch
        const isControlledMulti = isMulti && (props as SFComboboxMultiProps).value !== undefined;
        const [internalMultiValue, setInternalMultiValue] = useState<string[]>(
          isMulti ? ((props as SFComboboxMultiProps).defaultValue ?? []) : []
        );
        const selectedMultiValues: string[] = isControlledMulti
          ? ((props as SFComboboxMultiProps).value ?? [])
          : internalMultiValue;

        // Unified "isSelected" check
        const isOptionSelected = (value: string): boolean =>
          isMulti ? selectedMultiValues.includes(value) : selectedSingleValue === value;

        // hasSelection unified
        const hasSelection = isMulti
          ? selectedMultiValues.length > 0
          : selectedSingleValue !== undefined;

        const handleSelect = (itemValue: string) => {
          if (isMulti) {
            const next = selectedMultiValues.includes(itemValue)
              ? selectedMultiValues.filter((v) => v !== itemValue)
              : [...selectedMultiValues, itemValue];
            if (!isControlledMulti) setInternalMultiValue(next);
            (props as SFComboboxMultiProps).onChange?.(next);
            // CRITICAL: do NOT setOpen(false) in multi-select — popover stays open
            // for next selection. Closing happens via Escape / click-outside.
            return;
          }
          // Single-select branch
          const next = itemValue === selectedSingleValue ? undefined : itemValue;
          if (!isControlledSingle) setInternalSingleValue(next);
          (props as SFComboboxSingleProps).onChange?.(next);
          setOpen(false);
        };

        const handleClear = () => {
          if (isMulti) {
            if (!isControlledMulti) setInternalMultiValue([]);
            (props as SFComboboxMultiProps).onChange?.([]);
            return;
          }
          if (!isControlledSingle) setInternalSingleValue(undefined);
          (props as SFComboboxSingleProps).onChange?.(undefined);
        };

        const handleRemoveChip = (value: string) => {
          if (!isMulti) return;
          const next = selectedMultiValues.filter((v) => v !== value);
          if (!isControlledMulti) setInternalMultiValue(next);
          (props as SFComboboxMultiProps).onChange?.(next);
        };

    DELETE the Plan 01 `if (isMulti && process.env.NODE_ENV !== "production") { console.warn(...) }` block — Plan 02 replaces the fallback behavior.

    (3) Update the `selectedLabel` computation (single-select branch only):

        const selectedSingleLabel = !isMulti && selectedSingleValue
          ? options.find((o) => o.value === selectedSingleValue)?.label ?? selectedSingleValue
          : undefined;

    (4) Replace the trigger button's content area (the `<span>` with `selectedLabel ?? placeholder`) with a conditional that renders chips for multi-select:

        <SFPopoverTrigger asChild>
          <button
            type="button"
            disabled={disabled}
            aria-label={ariaLabel ?? placeholder ?? "Select option"}
            className={cn(
              "sf-focusable sf-pressable",
              "flex-1 flex items-center justify-between",
              "px-[var(--sfx-space-3)] py-[var(--sfx-space-2)]",
              "font-mono uppercase tracking-wider text-xs",
              "bg-transparent text-foreground",
              "cursor-pointer",
              "min-h-[40px]",
              "data-[state=open]:bg-foreground data-[state=open]:text-background"
            )}
          >
            {isMulti ? (
              <div className="flex flex-wrap gap-[var(--sfx-space-1)] flex-1 items-center">
                {selectedMultiValues.length === 0 ? (
                  <span className="text-muted-foreground">{placeholder ?? "Select..."}</span>
                ) : (
                  selectedMultiValues.map((v) => {
                    const opt = options.find((o) => o.value === v);
                    const label = opt?.label ?? v;
                    return (
                      <SFBadge
                        key={v}
                        intent="outline"
                        className="gap-[var(--sfx-space-1)] pr-[var(--sfx-space-1)]"
                      >
                        <span>{label}</span>
                        <span
                          role="button"
                          tabIndex={0}
                          aria-label={`Remove ${label}`}
                          onClick={(e) => {
                            e.stopPropagation();
                            handleRemoveChip(v);
                          }}
                          onKeyDown={(e) => {
                            if (e.key === "Enter" || e.key === " ") {
                              e.preventDefault();
                              e.stopPropagation();
                              handleRemoveChip(v);
                            }
                          }}
                          className="cursor-pointer hover:bg-foreground hover:text-background px-[var(--sfx-space-1)]"
                        >
                          ×
                        </span>
                      </SFBadge>
                    );
                  })
                )}
              </div>
            ) : (
              <span
                className={cn(
                  "truncate",
                  !hasSelection && "text-muted-foreground"
                )}
              >
                {selectedSingleLabel ?? placeholder ?? "Select..."}
              </span>
            )}
            <span aria-hidden="true" className="ml-[var(--sfx-space-2)] font-mono text-xs">
              {open ? "▴" : "▾"}
            </span>
          </button>
        </SFPopoverTrigger>

    NOTE on the chip remove element: it uses `<span role="button" tabIndex={0}>` instead of a nested `<button>` to avoid the nested-interactive-elements anti-pattern. `role="button"` + `tabIndex={0}` + Enter/Space onKeyDown gives equivalent keyboard-affordance to a real button while keeping the HTML valid (button cannot contain button — but a span with role=button can be inside a button per ARIA-in-HTML practice; axe `nested-interactive` checks for nested `<button>`/`<a>` elements specifically).

    (5) Update `<SFCommandList>` to thread `aria-multiselectable` when `multiple` is true:

        <SFCommandList aria-multiselectable={isMulti ? true : undefined}>

    (6) Update each `<SFCommandItem aria-selected={...}>` to use the unified `isOptionSelected` check:

        <SFCommandItem
          key={opt.value}
          value={opt.value}
          disabled={opt.disabled}
          onSelect={handleSelect}
          aria-selected={isOptionSelected(opt.value)}
        >
          {opt.label}
        </SFCommandItem>

    Apply this in BOTH the grouped and ungrouped item maps.

    PRESERVE the JSDoc block at the top, the import statements (just adding SFBadge), the type definitions, and the Pattern C contract reminder comment block at the end of file. The `_dep_` block doesn't apply here (no dep added).

    Worktree leakage check: `git status --porcelain | grep -v "components/sf/sf-combobox.tsx"` — expected: empty output (only this file modified by Task 1).

    Commit: `Feat(72-02): SFCombobox multi-select impl (CB-03 SFBadge chips + stay-open + aria-multiselectable)`
  </action>
  <verify>
    <automated>grep -q "SFBadge" components/sf/sf-combobox.tsx && grep -q "isControlledMulti" components/sf/sf-combobox.tsx && grep -q "selectedMultiValues" components/sf/sf-combobox.tsx && grep -q "handleRemoveChip" components/sf/sf-combobox.tsx && grep -q "aria-multiselectable=" components/sf/sf-combobox.tsx && grep -q "e.stopPropagation()" components/sf/sf-combobox.tsx && ! grep -q "console.warn" components/sf/sf-combobox.tsx && grep -q "isOptionSelected" components/sf/sf-combobox.tsx && grep -q 'role="button"' components/sf/sf-combobox.tsx && grep -q "Remove \\${" components/sf/sf-combobox.tsx && pnpm exec tsc --noEmit 2>&1 | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `components/sf/sf-combobox.tsx` imports `SFBadge` from `@/components/sf` (`grep -q "SFBadge" components/sf/sf-combobox.tsx` returns true; the import line includes it)
    - File contains the literal substring `selectedMultiValues` (multi-select state)
    - File contains the literal substring `handleRemoveChip` (chip remove handler)
    - File contains the literal substring `isControlledMulti` (controlled-vs-uncontrolled multi state)
    - File contains the literal substring `aria-multiselectable={isMulti` — listbox carries the aria attr conditionally
    - File contains the literal substring `e.stopPropagation()` at least once (chip remove guard)
    - File does NOT contain the substring `console.warn` (Plan 01 fallback removed)
    - File contains the literal substring `isOptionSelected` (unified check used in `aria-selected`)
    - File contains the literal substring `role="button"` (chip remove uses span+role=button to avoid nested-interactive)
    - File contains the literal substring `aria-label={\`Remove ${` (chip remove accessible name; both Playwright and axe rely on this)
    - Multi-select branch does NOT call `setOpen(false)` after `(props as SFComboboxMultiProps).onChange?.(next)` — verifiable by reading the file: the only `setOpen(false)` call is in the single-select branch
    - File still contains the Plan 01 single-select impl unchanged in spirit (`selectedSingleValue`, `setInternalSingleValue`, `selectedSingleLabel` exist)
    - File still contains the Pattern C contract reminder comment block at the end
    - File line count ≥ 280 — multi-select branch adds ~60 lines (`wc -l components/sf/sf-combobox.tsx` ≥ 280)
    - `pnpm exec tsc --noEmit` returns 0 errors
    - `git log -1 --format=%s -- components/sf/sf-combobox.tsx` matches pattern `Feat\\(72-02\\):.*multi-select`
  </acceptance_criteria>
  <done>
    SFCombobox now ships full multi-select via `multiple={true}`: SFBadge chips inside trigger, chip remove × with stopPropagation + keyboard handler, popover stays open on multi-select, aria-multiselectable threaded onto listbox. Single-select impl from Plan 01 unchanged in behavior.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create playground fixture for Playwright + axe-core tests</name>
  <files>app/dev-playground/sf-combobox/page.tsx</files>
  <read_first>
    - app/dev-playground/sf-data-table/page.tsx (Phase 71 fixture precedent — observe section pattern, useState for controlled, data-testid hooks)
    - components/sf/sf-combobox.tsx (Task 1 output — verify SFCombobox + SFComboboxOption + SFComboboxProps export)
    - components/sf/index.ts (verify SFCombobox is exported via barrel)
    - .planning/phases/72-sfcombobox/72-RESEARCH.md §Fixture Design + §Sample Fixtures
    - .planning/phases/72-sfcombobox/72-VALIDATION.md §Wave 0 Requirements (fixture mounting all 6 sections)
  </read_first>
  <action>
    Create the directory chain `app/dev-playground/sf-combobox/` and place `page.tsx` inside.

    File `app/dev-playground/sf-combobox/page.tsx`:

        /**
         * Playground fixture — Phase 72 Playwright + axe-core tests target this URL.
         *
         * Path: /dev-playground/sf-combobox
         *
         * Convention-only suppression: the `dev-playground/` prefix mirrors Phase 71
         * precedent. This is a TEST FIXTURE, not a production surface. Do not link
         * from production navigation. If a sitemap is added in a future phase,
         * exclude /dev-playground/*.
         */

        "use client";

        import { useState } from "react";
        import { SFCombobox, type SFComboboxOption } from "@/components/sf";

        const flatOptions: SFComboboxOption[] = [
          { value: "alpha", label: "Alpha" },
          { value: "bravo", label: "Bravo" },
          { value: "charlie", label: "Charlie" },
          { value: "delta", label: "Delta" },
          { value: "echo", label: "Echo" },
        ];

        const groupedOptions: SFComboboxOption[] = [
          { value: "north-1", label: "Atlas", group: "North" },
          { value: "north-2", label: "Boreal", group: "North" },
          { value: "north-3", label: "Cinder", group: "North" },
          { value: "south-1", label: "Dune", group: "South" },
          { value: "south-2", label: "Ember", group: "South" },
          { value: "south-3", label: "Foundry", group: "South" },
          { value: "east-1", label: "Granite", group: "East" },
          { value: "east-2", label: "Helix", group: "East" },
          { value: "east-3", label: "Ion", group: "East" },
        ];

        export default function SFComboboxPlayground() {
          const [section2Value, setSection2Value] = useState<string | undefined>("north-1");
          const [section3Values, setSection3Values] = useState<string[]>([]);

          return (
            <main className="p-[var(--sfx-space-6)] min-h-screen bg-background text-foreground space-y-[var(--sfx-space-8)]">
              <h1 className="font-mono text-xs uppercase tracking-wider">
                Phase 72 — SFCombobox Playground
              </h1>

              {/* Section 1: Uncontrolled single-select, flat options */}
              <section data-testid="section-1" className="space-y-[var(--sfx-space-2)]">
                <h2 className="font-mono text-xs uppercase tracking-wider">
                  1. Uncontrolled single-select
                </h2>
                <div className="w-[280px]" data-testid="section-1-trigger-wrapper">
                  <SFCombobox
                    options={flatOptions}
                    placeholder="Pick one"
                    ariaLabel="Section 1 single-select"
                  />
                </div>
              </section>

              {/* Section 2: Controlled single-select, grouped */}
              <section data-testid="section-2" className="space-y-[var(--sfx-space-2)]">
                <h2 className="font-mono text-xs uppercase tracking-wider">
                  2. Controlled single-select with groups
                </h2>
                <p
                  data-testid="section-2-value"
                  className="font-mono text-xs"
                >
                  Value: {section2Value ?? "(none)"}
                </p>
                <div className="w-[280px]">
                  <SFCombobox
                    options={groupedOptions}
                    value={section2Value}
                    onChange={setSection2Value}
                    placeholder="Pick a region"
                    ariaLabel="Section 2 grouped single-select"
                  />
                </div>
              </section>

              {/* Section 3: Multi-select, controlled */}
              <section data-testid="section-3" className="space-y-[var(--sfx-space-2)]">
                <h2 className="font-mono text-xs uppercase tracking-wider">
                  3. Multi-select (controlled, value: string[])
                </h2>
                <p
                  data-testid="section-3-count"
                  className="font-mono text-xs"
                >
                  Selected: {section3Values.length} of {flatOptions.length}
                </p>
                <div className="w-[480px]">
                  <SFCombobox
                    multiple
                    options={flatOptions}
                    value={section3Values}
                    onChange={setSection3Values}
                    placeholder="Pick many"
                    ariaLabel="Section 3 multi-select"
                  />
                </div>
              </section>

              {/* Section 4: Loading state */}
              <section data-testid="section-4" className="space-y-[var(--sfx-space-2)]">
                <h2 className="font-mono text-xs uppercase tracking-wider">
                  4. Loading state
                </h2>
                <div className="w-[280px]">
                  <SFCombobox
                    options={[]}
                    loading
                    placeholder="Loading data..."
                    ariaLabel="Section 4 loading"
                  />
                </div>
              </section>

              {/* Section 5: Empty options array */}
              <section data-testid="section-5" className="space-y-[var(--sfx-space-2)]">
                <h2 className="font-mono text-xs uppercase tracking-wider">
                  5. Empty options
                </h2>
                <div className="w-[280px]">
                  <SFCombobox
                    options={[]}
                    placeholder="No options to pick"
                    emptyText="No items available."
                    ariaLabel="Section 5 empty"
                  />
                </div>
              </section>

              {/* Section 6: Grouped-only options for axe + grouping verification */}
              <section data-testid="section-6" className="space-y-[var(--sfx-space-2)]">
                <h2 className="font-mono text-xs uppercase tracking-wider">
                  6. Grouped options (uncontrolled)
                </h2>
                <div className="w-[280px]">
                  <SFCombobox
                    options={groupedOptions}
                    placeholder="Pick from groups"
                    ariaLabel="Section 6 grouped"
                  />
                </div>
              </section>
            </main>
          );
        }

    Spacing tokens used: `--sfx-space-2`, `--sfx-space-6`, `--sfx-space-8` — all blessed stops (8 / 24 / 32).

    Do NOT add this route to any sitemap, navigation, or public route table. The convention from Phase 71 + the absence of links from production routes is the suppression mechanism.

    Worktree leakage check: `git status --porcelain | grep -v "app/dev-playground/sf-combobox/page.tsx"` — expected: empty output (only this new file modified by Task 2).

    Commit: `Chore(72-02): playground fixture for Phase 72 Playwright + axe tests (6 sections)`
  </action>
  <verify>
    <automated>test -f app/dev-playground/sf-combobox/page.tsx && grep -q "SFCombobox" app/dev-playground/sf-combobox/page.tsx && grep -q "from \"@/components/sf\"" app/dev-playground/sf-combobox/page.tsx && grep -q "data-testid=\"section-1\"" app/dev-playground/sf-combobox/page.tsx && grep -q "data-testid=\"section-2\"" app/dev-playground/sf-combobox/page.tsx && grep -q "data-testid=\"section-3\"" app/dev-playground/sf-combobox/page.tsx && grep -q "data-testid=\"section-4\"" app/dev-playground/sf-combobox/page.tsx && grep -q "data-testid=\"section-5\"" app/dev-playground/sf-combobox/page.tsx && grep -q "data-testid=\"section-6\"" app/dev-playground/sf-combobox/page.tsx && grep -q "multiple" app/dev-playground/sf-combobox/page.tsx && grep -q "loading" app/dev-playground/sf-combobox/page.tsx && grep -q "section-2-value" app/dev-playground/sf-combobox/page.tsx && grep -q "section-3-count" app/dev-playground/sf-combobox/page.tsx && grep -q "export default function" app/dev-playground/sf-combobox/page.tsx && pnpm exec tsc --noEmit 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `app/dev-playground/sf-combobox/page.tsx` exists
    - File starts with `"use client"` directive (required for `useState`)
    - File imports `SFCombobox` and `SFComboboxOption` from `@/components/sf` (barrel — verifies CB-04 barrel export works)
    - File contains 6 sections with `data-testid="section-1"` through `data-testid="section-6"` — verifiable: `grep -c "data-testid=\"section-[1-6]\"" app/dev-playground/sf-combobox/page.tsx` returns 6
    - File contains `data-testid="section-2-value"` AND `data-testid="section-3-count"` (controlled-state observers)
    - File contains literal substring `multiple` (multi-select prop in section 3)
    - File contains literal substring `loading` (loading prop in section 4)
    - File default-exports a function (`grep -q "export default function" app/dev-playground/sf-combobox/page.tsx` returns true)
    - File renders Section 3 multi-select with controlled `value={section3Values}` AND `onChange={setSection3Values}` — verifiable: both substrings present in file
    - File uses ONLY blessed-stop spacing tokens (`--sfx-space-2`, `--sfx-space-6`, `--sfx-space-8`) — `grep -oE "var\\(--sfx-space-[0-9]+\\)" app/dev-playground/sf-combobox/page.tsx | sort -u` outputs only entries from blessed stops
    - File does NOT modify `app/sitemap.ts` or any other public-route table — `git status --porcelain` shows ONLY the new playground file
    - `pnpm exec tsc --noEmit` returns 0 errors
  </acceptance_criteria>
  <done>
    Playwright + axe specs (Tasks 3, 4) have a deterministic mount point with 6 distinct sections: uncontrolled single, controlled single+grouped, controlled multi, loading, empty, grouped-only. The route is suppressed from public discovery via the `dev-playground/` convention.
  </done>
</task>

<task type="auto">
  <name>Task 3: Playwright spec — CB-01/CB-02/CB-03 acceptance tests</name>
  <files>tests/v1.10-phase72-sf-combobox.spec.ts</files>
  <read_first>
    - tests/v1.10-phase71-sf-data-table.spec.ts (precedent for ABS_BASE pattern + beforeEach + section-scoped locators)
    - playwright.config.ts (verify webServer config + chromium project)
    - app/dev-playground/sf-combobox/page.tsx (Task 2 fixture — section testids + dataset shape)
    - components/sf/sf-combobox.tsx (Task 1 output — confirm aria-multiselectable threading + chip remove behavior)
  </read_first>
  <action>
    Create `tests/v1.10-phase72-sf-combobox.spec.ts`. Skeleton (≥10 named tests covering CB-01, CB-02, CB-03):

        import { test, expect } from "@playwright/test";

        const ABS_BASE = process.env.CAPTURE_BASE_URL ?? "http://localhost:3000";
        const PLAYGROUND_URL = `${ABS_BASE}/dev-playground/sf-combobox`;

        test.describe("@v1.10-phase72 SFCombobox", () => {
          test.beforeEach(async ({ page }) => {
            await page.goto(PLAYGROUND_URL, { waitUntil: "networkidle" });
            // Sanity gate: ensure fixture mounted (prevents vacuous green on 404)
            await expect(page.getByTestId("section-1")).toBeVisible();
            await expect(page.getByTestId("section-3")).toBeVisible();
          });

          // ----- CB-01: single-select keyboard + filter + Enter + Escape -----

          test("CB-01 open: clicking trigger opens popover with listbox visible", async ({ page }) => {
            const trigger = page.getByTestId("section-1").locator("button[type='button']").first();
            await trigger.click();
            await expect(page.locator('[role="listbox"]').first()).toBeVisible();
            await expect(page.locator('[role="option"]').first()).toBeVisible();
          });

          test("CB-01 filter: typing in CommandInput filters visible options", async ({ page }) => {
            const trigger = page.getByTestId("section-1").locator("button[type='button']").first();
            await trigger.click();
            await page.locator('[role="combobox"]').first().fill("Alp");
            // Only "Alpha" matches the filter
            const optionCount = await page.locator('[role="option"]:visible').count();
            expect(optionCount).toBeLessThanOrEqual(2); // Alpha + maybe one fuzzy
            await expect(page.locator('[role="option"]', { hasText: "Alpha" })).toBeVisible();
          });

          test("CB-01 keyboard: ArrowDown highlights next option, ArrowUp highlights previous", async ({ page }) => {
            const trigger = page.getByTestId("section-1").locator("button[type='button']").first();
            await trigger.click();
            await page.keyboard.press("ArrowDown");
            // After ArrowDown, the second option should be aria-selected (cmdk auto-selects first on open)
            const selected = page.locator('[role="option"][aria-selected="true"]').first();
            await expect(selected).toBeVisible();
            await page.keyboard.press("ArrowUp");
            // ArrowUp moves selection back; loop=true means it wraps. Just assert one is still selected.
            await expect(page.locator('[role="option"][aria-selected="true"]').first()).toBeVisible();
          });

          test("CB-01 Enter: pressing Enter on highlighted option selects and closes popover", async ({ page }) => {
            const trigger = page.getByTestId("section-1").locator("button[type='button']").first();
            await trigger.click();
            await page.keyboard.press("ArrowDown"); // ensure something highlighted
            await page.keyboard.press("Enter");
            await expect(page.locator('[role="listbox"]').first()).not.toBeVisible();
          });

          test("CB-01 Escape: pressing Escape closes popover without selecting", async ({ page }) => {
            const triggerWrapper = page.getByTestId("section-2"); // controlled section — observe value didn't change
            const initialValue = await triggerWrapper.getByTestId("section-2-value").textContent();
            const trigger = triggerWrapper.locator("button[type='button']").first();
            await trigger.click();
            await page.keyboard.press("Escape");
            await expect(page.locator('[role="listbox"]').first()).not.toBeVisible();
            const afterValue = await triggerWrapper.getByTestId("section-2-value").textContent();
            expect(afterValue).toEqual(initialValue);
          });

          test("CB-01 controlled: external value prop reflects in trigger display", async ({ page }) => {
            // Section 2 starts with value="north-1" (label "Atlas")
            const trigger = page.getByTestId("section-2").locator("button[type='button']").first();
            await expect(trigger).toContainText(/atlas/i);
          });

          // ----- CB-02: clear + grouping -----

          test("CB-02 clear: clicking × resets selection (controlled section 2)", async ({ page }) => {
            // Section 2 has an initial value, so a clear button is visible
            const section = page.getByTestId("section-2");
            const clearBtn = section.locator('button[aria-label="Clear selection"]').first();
            await expect(clearBtn).toBeVisible();
            await clearBtn.click();
            await expect(section.getByTestId("section-2-value")).toContainText(/\(none\)/);
          });

          test("CB-02 grouping: opened section 2 popover shows group headings", async ({ page }) => {
            const trigger = page.getByTestId("section-2").locator("button[type='button']").first();
            await trigger.click();
            // Group headings render as cmdk-group-heading; SFCommandGroup auto-styles them.
            // The fixture has groups "North", "South", "East" — assert all three present.
            await expect(page.locator('[role="listbox"]').first()).toBeVisible();
            await expect(page.locator('[cmdk-group-heading]', { hasText: /north/i }).first()).toBeVisible();
            await expect(page.locator('[cmdk-group-heading]', { hasText: /south/i }).first()).toBeVisible();
            await expect(page.locator('[cmdk-group-heading]', { hasText: /east/i }).first()).toBeVisible();
          });

          // ----- CB-03: multi-select chips + stay-open + chip removal -----

          test("CB-03 multi: selecting an option keeps popover OPEN (does not close)", async ({ page }) => {
            const trigger = page.getByTestId("section-3").locator("button[type='button']").first();
            await trigger.click();
            await expect(page.locator('[role="listbox"]').first()).toBeVisible();
            // Click first option
            await page.locator('[role="option"]').first().click();
            // Popover MUST stay open
            await expect(page.locator('[role="listbox"]').first()).toBeVisible();
          });

          test("CB-03 multi chips: each selected value renders an SFBadge chip in trigger", async ({ page }) => {
            const section = page.getByTestId("section-3");
            const trigger = section.locator("button[type='button']").first();
            await trigger.click();
            await page.locator('[role="option"]').first().click(); // select Alpha
            await page.locator('[role="option"]').nth(1).click();   // select Bravo
            await expect(section.getByTestId("section-3-count")).toContainText("Selected: 2");
            // Close popover by Escape
            await page.keyboard.press("Escape");
            // Chips: Alpha + Bravo each rendered with a remove affordance
            await expect(section.locator('[aria-label^="Remove"]')).toHaveCount(2);
          });

          test("CB-03 chip remove: clicking × on a chip deselects only that value", async ({ page }) => {
            const section = page.getByTestId("section-3");
            const trigger = section.locator("button[type='button']").first();
            await trigger.click();
            await page.locator('[role="option"]').first().click();
            await page.locator('[role="option"]').nth(1).click();
            await page.keyboard.press("Escape");
            await expect(section.getByTestId("section-3-count")).toContainText("Selected: 2");
            // Click first chip remove
            await section.locator('[aria-label^="Remove"]').first().click();
            await expect(section.getByTestId("section-3-count")).toContainText("Selected: 1");
            // Popover should NOT have opened from the chip click
            await expect(page.locator('[role="listbox"]').first()).not.toBeVisible();
          });

          test("CB-03 controlled: external value: string[] prop reflects current selection in chip count", async ({ page }) => {
            // The section-3-count testid mirrors section3Values.length in the fixture useState
            const section = page.getByTestId("section-3");
            await expect(section.getByTestId("section-3-count")).toContainText(/Selected: \d+ of 5/);
          });
        });

    Run: `pnpm exec playwright test tests/v1.10-phase72-sf-combobox.spec.ts --project=chromium --reporter=line`
    Expected: 12 tests pass.

    NOTE on the sort-of-fragile selectors: cmdk renders `[cmdk-group-heading]` attribute on group headings; if that exact attribute name has changed in the installed cmdk version, adjust the selector to `[role="presentation"]` or to a section heading text match. Inspect the rendered DOM with `page.pause()` if needed.

    Commit: `Test(72-02): Playwright spec for CB-01/02/03 acceptance (TST-03)`
  </action>
  <verify>
    <automated>test -f tests/v1.10-phase72-sf-combobox.spec.ts && grep -q "CB-01" tests/v1.10-phase72-sf-combobox.spec.ts && grep -q "CB-02" tests/v1.10-phase72-sf-combobox.spec.ts && grep -q "CB-03" tests/v1.10-phase72-sf-combobox.spec.ts && grep -q "ABS_BASE" tests/v1.10-phase72-sf-combobox.spec.ts && grep -q "/dev-playground/sf-combobox" tests/v1.10-phase72-sf-combobox.spec.ts && grep -q "role=\"listbox\"" tests/v1.10-phase72-sf-combobox.spec.ts && grep -q "role=\"option\"" tests/v1.10-phase72-sf-combobox.spec.ts && grep -q "aria-label=\"Clear selection\"" tests/v1.10-phase72-sf-combobox.spec.ts && grep -q "Remove" tests/v1.10-phase72-sf-combobox.spec.ts && grep -q "stays.*open\|stay.*open\|OPEN\|setOpen\|listbox.*toBeVisible" tests/v1.10-phase72-sf-combobox.spec.ts && [ "$(grep -cE "^  test\\(" tests/v1.10-phase72-sf-combobox.spec.ts)" -ge 10 ] && pnpm exec playwright test tests/v1.10-phase72-sf-combobox.spec.ts --project=chromium --reporter=line</automated>
  </verify>
  <acceptance_criteria>
    - `tests/v1.10-phase72-sf-combobox.spec.ts` exists
    - File contains ≥ 10 named test cases via `test(...)` calls — verifiable: `grep -cE "^  test\\(" tests/v1.10-phase72-sf-combobox.spec.ts` returns ≥ 10
    - File contains the literal substring `/dev-playground/sf-combobox` (correct fixture URL — matches Phase 71 actual ship pattern)
    - File contains the literal substring `ABS_BASE` (CI portability via env var)
    - `beforeEach` asserts `await expect(page.getByTestId("section-1")).toBeVisible()` AND `await expect(page.getByTestId("section-3")).toBeVisible()` — prevents vacuous green
    - File covers CB-01 keyboard nav: contains `ArrowDown`, `Enter`, `Escape` literal key strings
    - File covers CB-02 clear: contains `aria-label="Clear selection"` literal
    - File covers CB-02 grouping: contains `cmdk-group-heading` OR equivalent group-heading selector (adapter pattern documented in code-comments)
    - File covers CB-03 stay-open: at least one test asserts popover IS visible after clicking an option in section-3 — verifiable via grep for "stays open" semantics or by reading the multi-select test
    - File covers CB-03 chip count: at least one test asserts `getByTestId("section-3-count")` contains "Selected: N"
    - File covers CB-03 chip remove: at least one test clicks `[aria-label^="Remove"]` and asserts count decreases
    - `pnpm exec playwright test tests/v1.10-phase72-sf-combobox.spec.ts --project=chromium` exits 0 (≥ 10/10 tests pass)
  </acceptance_criteria>
  <done>
    CB-01 (single-select keyboard + filter + Enter/Escape + controlled), CB-02 (clear + grouping), and CB-03 (multi-select stay-open + chips + remove + controlled string[] API) are all behaviorally verified by Playwright against the 6-section playground fixture. Tests run in < 30s per Phase 72 validation latency budget.
  </done>
</task>

<task type="auto">
  <name>Task 4: axe-core spec + production chunk audit + bundle gate (TST-03 + CB-04 closeout)</name>
  <files>tests/v1.10-phase72-sf-combobox-axe.spec.ts</files>
  <read_first>
    - tests/v1.10-phase71-sf-data-table-axe.spec.ts (precedent for AxeBuilder per-rule scan + vacuous-green guard)
    - tests/v1.8-phase63-1-bundle-budget.spec.ts (existing 200 KB hard-constraint spec; will be re-run as part of this task)
    - .planning/phases/72-sfcombobox/72-RESEARCH.md §A11y Mechanics for axe Gate (rules to target)
    - .planning/phases/72-sfcombobox/72-RESEARCH.md §Bundle Impact Check (cmdk barrel risk)
    - app/dev-playground/sf-combobox/page.tsx (fixture URL + section testids)
    - components/sf/sf-combobox.tsx (Task 1 output — confirms aria-multiselectable threading)
  </read_first>
  <action>
    PART A — axe-core spec.

    Create `tests/v1.10-phase72-sf-combobox-axe.spec.ts`:

        import { test, expect } from "@playwright/test";
        import AxeBuilder from "@axe-core/playwright";

        const ABS_BASE = process.env.CAPTURE_BASE_URL ?? "http://localhost:3000";
        const PLAYGROUND_URL = `${ABS_BASE}/dev-playground/sf-combobox`;

        // axe-core rules targeted by Phase 72 (research §A11y Mechanics):
        const SINGLE_SELECT_RULES = [
          "aria-allowed-attr",       // role=combobox MUST NOT carry aria-haspopup
          "aria-valid-attr-value",   // aria-activedescendant references valid id
          "aria-required-children",  // listbox > option children
          "button-name",             // trigger + clear + chip remove all named
          "color-contrast",          // foreground/background WCAG AA
          "nested-interactive",      // chip remove span+role=button MUST NOT trigger nested-interactive flag inside outer trigger button (axe 4.x heuristic)
        ];

        const MULTI_SELECT_RULES = [
          ...SINGLE_SELECT_RULES,
          "aria-allowed-attr",       // aria-multiselectable allowed on listbox
        ];

        test.describe("@v1.10-phase72 SFCombobox axe-core", () => {
          test.beforeEach(async ({ page }) => {
            await page.goto(PLAYGROUND_URL, { waitUntil: "networkidle" });
            // Vacuous-green guard: confirm fixture mounted before opening any popover
            await expect(page.getByTestId("section-1")).toBeVisible();
          });

          test("section-1 single-select OPEN: aria-allowed-attr + aria-required-children + button-name + color-contrast", async ({ page }) => {
            // Open the popover BEFORE analyze() — axe cannot scan inside a hidden Radix portal
            const trigger = page.getByTestId("section-1").locator("button[type='button']").first();
            await trigger.click();
            await expect(page.locator('[role="listbox"]').first()).toBeVisible();

            const results = await new AxeBuilder({ page }).withRules(SINGLE_SELECT_RULES).analyze();
            const violations = results.violations.filter((v) => SINGLE_SELECT_RULES.includes(v.id));
            expect(
              violations,
              `axe single-select violations:\n` +
                violations
                  .map((v) => `  ${v.id}: ${v.description}\n${v.nodes.slice(0, 3).map((n) => `    ${n.html}`).join("\n")}`)
                  .join("\n")
            ).toHaveLength(0);
          });

          test("section-2 controlled+grouped OPEN: listbox accessible name (aria-label or aria-labelledby)", async ({ page }) => {
            const trigger = page.getByTestId("section-2").locator("button[type='button']").first();
            await trigger.click();
            await expect(page.locator('[role="listbox"]').first()).toBeVisible();

            // Manual assertion: SFCommand `label` prop generates accessible name on listbox
            const listbox = page.locator('[role="listbox"]').first();
            const ariaLabel = await listbox.getAttribute("aria-label");
            const labelledBy = await listbox.getAttribute("aria-labelledby");
            expect(ariaLabel || labelledBy, "listbox must have accessible name via aria-label or aria-labelledby").toBeTruthy();

            // axe scan
            const results = await new AxeBuilder({ page }).withRules(["aria-allowed-attr", "aria-required-children", "button-name"]).analyze();
            expect(results.violations).toHaveLength(0);
          });

          test("section-2 clear button: button-name (accessible name on clear ×)", async ({ page }) => {
            // Section 2 starts with controlled value="north-1", so clear button is visible
            await expect(page.getByTestId("section-2").locator('button[aria-label="Clear selection"]')).toBeVisible();
            const results = await new AxeBuilder({ page }).withRules(["button-name"]).analyze();
            const buttonNameViolations = results.violations.filter((v) => v.id === "button-name");
            expect(buttonNameViolations).toHaveLength(0);
          });

          test("section-3 multi-select OPEN: aria-multiselectable on listbox + zero aria-allowed-attr violations", async ({ page }) => {
            const trigger = page.getByTestId("section-3").locator("button[type='button']").first();
            await trigger.click();
            await expect(page.locator('[role="listbox"]').first()).toBeVisible();

            // Verify aria-multiselectable threading
            const listbox = page.locator('[role="listbox"][aria-multiselectable="true"]').first();
            await expect(listbox).toBeVisible();

            const results = await new AxeBuilder({ page }).withRules(MULTI_SELECT_RULES).analyze();
            const violations = results.violations.filter((v) => MULTI_SELECT_RULES.includes(v.id));
            expect(violations).toHaveLength(0);
          });

          test("section-3 multi-select chips: chip remove buttons have accessible names", async ({ page }) => {
            const section = page.getByTestId("section-3");
            const trigger = section.locator("button[type='button']").first();
            await trigger.click();
            await page.locator('[role="option"]').first().click();
            await page.locator('[role="option"]').nth(1).click();
            await page.keyboard.press("Escape");
            // Chips rendered with aria-label="Remove {label}" on each remove element
            const chipRemovers = section.locator('[aria-label^="Remove"]');
            await expect(chipRemovers).toHaveCount(2);
            const results = await new AxeBuilder({ page }).withRules(["button-name", "color-contrast"]).analyze();
            const violations = results.violations.filter((v) => ["button-name", "color-contrast"].includes(v.id));
            expect(violations).toHaveLength(0);
          });

          test("section-4 loading state OPEN: zero color-contrast violations on loading indicator", async ({ page }) => {
            const trigger = page.getByTestId("section-4").locator("button[type='button']").first();
            await trigger.click();
            await expect(page.locator('[role="progressbar"]').first()).toBeVisible();
            const results = await new AxeBuilder({ page }).withRules(["color-contrast"]).analyze();
            const violations = results.violations.filter((v) => v.id === "color-contrast");
            expect(violations).toHaveLength(0);
          });
        });

    PART B — production chunk audit (CB-04 closeout).

    Run the BND-04 stale-chunk guard + production build + chunk audit, in that order:

        rm -rf .next/cache .next
        ANALYZE=true pnpm build 2>&1 | tee /tmp/72-02-bundle.log

    Then audit:

    (B1) Verify cmdk is NOT in the homepage `/` chunk manifest (homepage does not render SFCombobox; barrel-export of SFCombobox should not drag cmdk to homepage):

        node -e "
          const fs = require('fs');
          const path = require('path');
          // Next.js 15 App Router emits app-build-manifest.json (key is '/page', NOT '/')
          // Pages Router emits build-manifest.json (key is '/'). Read both, prefer App Router.
          let homepageChunks = [];
          try {
            const appManifest = require('./.next/app-build-manifest.json');
            homepageChunks = appManifest.pages['/page'] || appManifest.pages['/'] || [];
          } catch (e) { /* not an App Router project */ }
          if (homepageChunks.length === 0) {
            try {
              const pagesManifest = require('./.next/build-manifest.json');
              homepageChunks = pagesManifest.pages['/'] || [];
            } catch (e) {}
          }
          if (homepageChunks.length === 0) {
            console.error('FAIL: could not resolve homepage chunks from any manifest — audit cannot run');
            process.exit(2);
          }
          console.log('Homepage / chunks:', homepageChunks.length, '(via', fs.existsSync('./.next/app-build-manifest.json') ? 'app-build-manifest' : 'build-manifest', ')');
          let leaks = 0;
          for (const chunk of homepageChunks) {
            const fp = path.join('.next', chunk);
            if (!fs.existsSync(fp)) continue;
            const src = fs.readFileSync(fp, 'utf8');
            // cmdk module name appears as 'cmdk' or 'cmdk/dist'
            if (/(?:^|[^a-zA-Z])cmdk(?:[^a-zA-Z]|$)|cmdk\\/dist/.test(src)) {
              console.error('LEAK:', chunk);
              leaks++;
            }
          }
          if (leaks > 0) { process.exit(1); }
          console.log('OK: cmdk absent from homepage / chunks');
        "

    Expected: "OK: cmdk absent from homepage / chunks". If LEAK appears, this is the bundle-watch failure documented in research §Bundle Impact Check — Pattern C contract holds in spirit only if cmdk does not land on the homepage. STOP and surface to user; a `_path_X_decision` would be needed (or SFCombobox must be P3 lazy contra CB-04).

    (B2) Verify cmdk DOES appear in the route chunk for `/dev-playground/sf-combobox` (sanity check that the fixture page actually loads cmdk):

        node -e "
          const fs = require('fs');
          const path = require('path');
          let fixtureChunks = [];
          // Try app-build-manifest.json (App Router) — key uses '/page' suffix
          try {
            const appManifest = require('./.next/app-build-manifest.json');
            fixtureChunks = appManifest.pages['/dev-playground/sf-combobox/page'] || appManifest.pages['/dev-playground/sf-combobox'] || [];
          } catch (e) {}
          if (fixtureChunks.length === 0) {
            try {
              const pagesManifest = require('./.next/build-manifest.json');
              fixtureChunks = pagesManifest.pages['/dev-playground/sf-combobox'] || [];
            } catch (e) {}
          }
          console.log('Fixture chunks:', fixtureChunks.length);
          let found = false;
          for (const chunk of fixtureChunks) {
            const fp = path.join('.next', chunk);
            if (!fs.existsSync(fp)) continue;
            const src = fs.readFileSync(fp, 'utf8');
            if (/cmdk\\/dist|require.*cmdk/.test(src)) { found = true; break; }
          }
          if (!found) {
            // Fallback: cmdk may be in a shared chunk reached by the route — scan static/chunks.
            // NOTE: this fallback proves cmdk is in the production bundle SOMEWHERE; it does NOT
            // re-prove homepage absence (B1 is the authoritative homepage check).
            const dir = '.next/static/chunks';
            for (const f of fs.readdirSync(dir)) {
              if (!f.endsWith('.js')) continue;
              const src = fs.readFileSync(path.join(dir, f), 'utf8');
              if (/cmdk\\/dist|require.*cmdk/.test(src)) { found = true; break; }
            }
          }
          if (!found) { console.error('FAIL: cmdk not in any production chunk'); process.exit(1); }
          console.log('OK: cmdk present in production chunks');
        "

    Expected: "OK: cmdk present in production chunks".

    (B3) Re-run the existing 200 KB budget spec:

        pnpm exec playwright test tests/v1.8-phase63-1-bundle-budget.spec.ts --project=chromium --reporter=line

    Expected: PASS. Homepage First Load JS still 200 KB or less.

    PART C — run the axe spec.

        pnpm exec playwright test tests/v1.10-phase72-sf-combobox-axe.spec.ts --project=chromium --reporter=line

    Expected: 6/6 axe tests pass.

    Commit: `Test(72-02): axe spec + production chunk audit + CB-04 close (TST-03)` — bundles axe spec and the chunk-audit run.
  </action>
  <verify>
    <automated>test -f tests/v1.10-phase72-sf-combobox-axe.spec.ts && grep -q "AxeBuilder" tests/v1.10-phase72-sf-combobox-axe.spec.ts && grep -q "aria-allowed-attr" tests/v1.10-phase72-sf-combobox-axe.spec.ts && grep -q "aria-multiselectable" tests/v1.10-phase72-sf-combobox-axe.spec.ts && grep -q "button-name" tests/v1.10-phase72-sf-combobox-axe.spec.ts && grep -q "color-contrast" tests/v1.10-phase72-sf-combobox-axe.spec.ts && grep -q "nested-interactive" tests/v1.10-phase72-sf-combobox-axe.spec.ts && grep -q "role=\"listbox\".*toBeVisible\|listbox.*toBeVisible" tests/v1.10-phase72-sf-combobox-axe.spec.ts && [ "$(grep -cE "^  test\\(" tests/v1.10-phase72-sf-combobox-axe.spec.ts)" -ge 6 ] && rm -rf .next/cache .next && ANALYZE=true pnpm build > /tmp/72-02-build.log 2>&1 && pnpm exec playwright test tests/v1.10-phase72-sf-combobox-axe.spec.ts --project=chromium --reporter=line && pnpm exec playwright test tests/v1.8-phase63-1-bundle-budget.spec.ts --project=chromium --reporter=line</automated>
  </verify>
  <acceptance_criteria>
    - `tests/v1.10-phase72-sf-combobox-axe.spec.ts` exists
    - File contains ≥ 6 named axe tests via `test(...)` calls — verifiable: `grep -cE "^  test\\(" tests/v1.10-phase72-sf-combobox-axe.spec.ts` returns ≥ 6
    - File contains the literal substring `AxeBuilder` (mirrors Phase 71 precedent)
    - File contains all 6 target rule literals: `aria-allowed-attr`, `aria-valid-attr-value`, `aria-required-children`, `button-name`, `color-contrast`, `nested-interactive` (verifiable via 6 separate `grep -q` calls; `nested-interactive` covers the chip remove span+role=button anti-pattern guard)
    - File contains the literal substring `aria-multiselectable` (multi-select listbox attr verification)
    - File contains a vacuous-green guard: every test that calls `analyze()` is preceded by either `await expect(page.locator('[role="listbox"]')).toBeVisible()` OR `await expect(page.locator('[role="progressbar"]')).toBeVisible()` — verifiable by reading the file
    - `pnpm exec playwright test tests/v1.10-phase72-sf-combobox-axe.spec.ts --project=chromium` exits 0 (≥ 6/6 axe tests pass; zero violations)
    - `rm -rf .next/cache .next && ANALYZE=true pnpm build` exits 0
    - Production chunk audit (PART B1) prints `OK: cmdk absent from homepage / chunks` — Pattern C bundle health verified at production-build level
    - Production chunk audit (PART B2) prints `OK: cmdk present in production chunks` — sanity check that cmdk reaches the route that consumes it
    - `tests/v1.8-phase63-1-bundle-budget.spec.ts` PASSES (homepage `/` First Load JS 200 KB or less)
    - `git status --porcelain` shows ONLY the 4 expected Plan 02 file paths modified across the plan (no `node_modules`, no `.next`, no worktree leakage)
  </acceptance_criteria>
  <done>
    All 5 Phase 72 requirement IDs satisfied (CB-01, CB-02, CB-03, CB-04, TST-03). axe-core green on 5 rule clusters. Production-grade chunk audit confirms (a) cmdk does NOT leak to homepage despite SFCombobox being barrel-exported, (b) cmdk present in route-bound production chunks, (c) 200 KB BND budget green. Phase 72 closes clean.
  </done>
</task>

</tasks>

<verification>
End-to-end verification of Plan 02 (also closes Phase 72):

(1) Full Playwright + axe pass against the playground fixture:

    pnpm exec playwright test tests/v1.10-phase72-sf-combobox*.spec.ts --project=chromium

Expected: ≥ 10 + ≥ 6 = ≥ 16 tests green.

(2) Production-grade bundle audit:

    rm -rf .next/cache .next
    ANALYZE=true pnpm build
    pnpm exec playwright test tests/v1.8-phase63-1-bundle-budget.spec.ts --project=chromium

Expected: build OK, budget spec PASS, homepage First Load JS 200 KB or less.

(3) cmdk barrel-exclusion bundle health:

    node -e "
      const m = require('./.next/build-manifest.json');
      const fs = require('fs');
      const path = require('path');
      let homepageLeak = false;
      for (const chunk of (m.pages['/'] || [])) {
        const fp = path.join('.next', chunk);
        if (!fs.existsSync(fp)) continue;
        const src = fs.readFileSync(fp, 'utf8');
        if (/cmdk\\/dist/.test(src)) { homepageLeak = true; break; }
      }
      console.log('Homepage cmdk leak:', homepageLeak);
    "

Expected: "Homepage cmdk leak: false".

(4) D-04 lock holds:

    grep -c "cmdk" next.config.ts                          # ≥ 1 (cmdk REMAINS in optimizePackageImports)
    git diff HEAD~5 -- next.config.ts                      # empty (no changes through Phase 72)

Expected: ≥ 1, empty.

(5) SFCombobox barrel export holds (CB-04):

    grep -c "export { SFCombobox" components/sf/index.ts   # 1
    grep -cE "SFCommand[A-Z]" components/sf/index.ts       # 1 (only the comment line)

Expected: 1, 1.

(6) TypeScript clean:

    pnpm exec tsc --noEmit

Expected: 0 errors.

(7) Worktree clean:

    git status --porcelain

Expected: only expected files (Plan 02 file set) staged or modified.
</verification>

<success_criteria>
- [ ] `components/sf/sf-combobox.tsx` Plan 01 multi-select fallback REPLACED with real impl (SFBadge chips, stay-open, aria-multiselectable, chip remove with stopPropagation)
- [ ] `app/dev-playground/sf-combobox/page.tsx` mounts 6 sections covering all CB-01..04 + TST-03 scenarios
- [ ] `tests/v1.10-phase72-sf-combobox.spec.ts` ships ≥ 10 Playwright tests; all green
- [ ] `tests/v1.10-phase72-sf-combobox-axe.spec.ts` ships ≥ 6 axe-core scans (5 rule clusters); all green; vacuous-green guards in place
- [ ] Production build: cmdk absent from homepage `/` chunk manifest (CB-04 + Pattern C bundle health)
- [ ] Production build: cmdk present in `/dev-playground/sf-combobox` route or shared chunk (sanity)
- [ ] `tests/v1.8-phase63-1-bundle-budget.spec.ts` PASSES (200 KB or less homepage First Load JS)
- [ ] `next.config.ts` UNCHANGED across all of Phase 72 (D-04 lock holds; cmdk already in list, no additions)
- [ ] `components/sf/index.ts` SFCommand* exclusion comment preserved
- [ ] Phase 72 closes with all 5 requirement IDs (CB-01, CB-02, CB-03, CB-04, TST-03) satisfied
- [ ] Zero new runtime npm deps across both plans (CB-04 zero-dep contract)
</success_criteria>

<output>
After completion, create `.planning/phases/72-sfcombobox/72-02-SUMMARY.md` documenting:
- The 4 commits across Tasks 1-4 (or however many landed)
- Final homepage / First Load JS measurement
- Playwright spec result (≥ 10/10 green)
- axe-core spec result (≥ 6/6 green)
- Production chunk-audit result (cmdk absent from homepage; present in route chunks)
- Confirmation that all 5 Phase 72 requirement IDs are satisfied
- Confirmation that Pattern C contract held: SFCombobox barrel-exported but cmdk did NOT leak to homepage

Then create `.planning/phases/72-sfcombobox/72-SUMMARY.md` (phase-level summary) referencing the two plan summaries and recording:
- Phase 72 close confirmation
- 5 REQ-IDs satisfied (CB-01, CB-02, CB-03, CB-04, TST-03)
- Pattern C bundle health verified empirically (zero new deps, zero homepage leak, full feature parity)
- Forward link to Phase 73 (SFRichEditor — Tiptap dep ratification)
</output>
</content>
