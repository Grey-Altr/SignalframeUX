---
phase: 73-sfricheditor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/sf/sf-rich-editor.tsx
  - package.json
  - pnpm-lock.yaml
  - app/globals.css
autonomous: true
requirements_addressed:
  - DEP-02
  - RE-04
gap_closure: false
must_haves:
  truths:
    - "_dep_re_01_decision 7-field comment block exists at the top of components/sf/sf-rich-editor.tsx and is committed BEFORE pnpm add @tiptap/* runs"
    - "@tiptap/react, @tiptap/pm, @tiptap/starter-kit, and @tiptap/extension-link are installed and recorded in pnpm-lock.yaml (exact versions verified via pnpm list --depth=0 immediately post-install)"
    - "next.config.ts optimizePackageImports does NOT contain any @tiptap/* entry (D-04 lock holds)"
    - "_dep_re_01_decision.bundle_evidence field is populated with MEASURED KB values from ANALYZE=true pnpm build (not estimates); field contains the literal string 'Measurement command: rm -rf .next/cache .next && ANALYZE=true pnpm build'"
    - "_dep_re_01_decision.bundle_evidence contains the literal string 'PASS' confirming @tiptap/* absent from homepage First Load chunk manifest"
    - "app/globals.css contains @layer signalframeux { .ProseMirror ... } block with at least 4 element rules (p, h1-h4, ul/ol/li, blockquote); block is positioned AFTER the @layer utilities block"
    - "No Tiptap CSS import exists anywhere in app/ components/ lib/"
    - "components/sf/index.ts does NOT export SFRichEditor or SFRichEditorLazy"
  artifacts:
    - path: "components/sf/sf-rich-editor.tsx"
      provides: "Placeholder Pattern B file hosting _dep_re_01_decision block with 7 fully-populated fields including measured bundle_evidence; 'use client' directive; export {}; NOT in barrel"
      contains: "_dep_re_01_decision"
    - path: "package.json"
      provides: "@tiptap/react, @tiptap/pm, @tiptap/starter-kit, @tiptap/extension-link dep declarations"
      contains: "@tiptap/react"
    - path: "pnpm-lock.yaml"
      provides: "Resolved dependency tree for all @tiptap/* packages"
      contains: "@tiptap/react"
    - path: "app/globals.css"
      provides: "First authored @layer signalframeux block with 4 ProseMirror element rules — scoped CSS isolation for contenteditable area; injectCSS: false pairing documented in comment"
      contains: ".ProseMirror"
  key_links:
    - from: "components/sf/sf-rich-editor.tsx"
      to: "components/sf/index.ts"
      via: "ABSENCE — file MUST NOT appear in barrel"
      pattern: "components/sf/index.ts MUST NOT match /sf-rich-editor/"
    - from: "components/sf/sf-rich-editor.tsx"
      to: "next.config.ts"
      via: "ABSENCE — @tiptap/* packages MUST NOT appear in optimizePackageImports"
      pattern: "next.config.ts MUST NOT match /@tiptap/"
    - from: "app/globals.css"
      to: "@layer signalframeux"
      via: "FIRST authored block — all .ProseMirror rules inside this layer; not inside @layer base or @layer utilities"
      pattern: "@layer signalframeux"
---

<objective>
Author the `_dep_re_01_decision` 7-field ratification block (mirroring `_dep_dt_01_decision` schema from Phase 71) in a placeholder `components/sf/sf-rich-editor.tsx`, then install Tiptap v3.22.5 (`@tiptap/react`, `@tiptap/pm`, `@tiptap/starter-kit`, `@tiptap/extension-link`), measure bundle impact via `ANALYZE=true pnpm build`, back-fill `bundle_evidence` with measured values, then author the `@layer signalframeux { .ProseMirror ... }` block in `app/globals.css` (RE-04 CSS isolation — first authored `@layer signalframeux` block in this file).

Purpose: Establishes the dep-decision audit trail BEFORE any implementation code. The `_dep_re_01_decision` block must be committed before `pnpm add` so reviewers see rationale at plan-time. The bundle_evidence field carries measured gzip numbers from a clean build — NEVER estimates. The globals.css `@layer signalframeux` block is authored in this plan (not Plan 02) because it pairs with `injectCSS: false` which must be committed together with the CSS isolation architecture decision.

Output: A placeholder `components/sf/sf-rich-editor.tsx` with 7-field decision block + measured bundle_evidence; Tiptap deps in package.json + pnpm-lock.yaml; `app/globals.css` with the first `@layer signalframeux` block containing 4 `.ProseMirror` element rules.
</objective>

<execution_context>
@/Users/greyaltaer/.claude/pde-os/engines/gsd/workflows/execute-plan.md
@/Users/greyaltaer/.claude/pde-os/engines/gsd/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/73-sfricheditor/73-RESEARCH.md
@.planning/phases/73-sfricheditor/73-VALIDATION.md
@CLAUDE.md
@.planning/LOCKDOWN.md
@components/sf/index.ts
@next.config.ts
@app/globals.css
@.planning/phases/71-sfdatatable/71-01-PLAN.md

<interfaces>
<!-- _dep_re_01_decision schema (mirrors _dep_dt_01_decision from Phase 71 Plan 01 field-for-field) -->

Fields (all 7 required, in this order):
  decided          → ISO date authored (pre-install)
  audit            → "sf-rich-editor:runtime-dep"
  dep_added        → list of all @tiptap/* packages added (verify exact set via pnpm list --depth=0 post-install)
  version          → exact resolved version string (from pnpm list post-install; placeholder TBD pre-install, filled post)
  rationale        → multi-line: why Tiptap, why no zero-dep substitute, why P3 lazy
  bundle_evidence  → array of MEASURED facts from ANALYZE=true pnpm build (filled post-build; NOT pre-install)
  review_gate      → "Tiptap v4 stable release — re-evaluate dep set and version pin"

Block placement: comment block at top of sf-rich-editor.tsx, after "use client" directive and JSDoc header.

<!-- @layer signalframeux placement in globals.css -->

Current globals.css structure (verified in research):
  @layer base { ... }      — existing
  @layer utilities { ... } — existing (ends ~line 869)
  detail panel z-index overrides (unlayered CSS — existing)

Insertion point: immediately AFTER the closing `}` of the last `@layer utilities` block, BEFORE the detail-panel z-index overrides. Add a section comment before the layer block.

<!-- Tiptap v3 install command -->

pnpm add @tiptap/react@3.22.5 @tiptap/pm@3.22.5 @tiptap/starter-kit@3.22.5 @tiptap/extension-link@3.22.5

Note: If 3.22.5 is not published at install time (version probe returns a lower v3 patch), use whatever v3.x is latest. Record actual resolved version in bundle_evidence. If pnpm reports a v3 DOES NOT EXIST error, fall back to latest v3.x (pnpm add @tiptap/react@^3 @tiptap/pm@^3 @tiptap/starter-kit@^3 @tiptap/extension-link@^3) and document in bundle_evidence.

<!-- Bundle leak verification node script -->

node -e "
const m = require('./.next/build-manifest.json');
const pages = m.pages || {};
const home = pages['/'] || [];
const homeStr = JSON.stringify(home);
const tiptapLeak = /tiptap|prosemirror|starter.kit/.test(homeStr);
console.log('Tiptap in homepage First Load:', tiptapLeak ? 'FAIL' : 'PASS');
"

<!-- character-count approach (zero-dep) -->
Character count will be delivered via editor.getText().length in Plan 02 — no @tiptap/extension-character-count dep needed. Document explicitly in rationale.
</interfaces>

<threat_model>
ASVS L1 — Phase 73 Plan 01 threat surface:

| Threat | Severity | Mitigation |
|--------|----------|------------|
| Estimated (not measured) bundle_evidence — reviewer accepts a guessed gzip number — _dep_X_decision precedent corrupted for remaining v1.10 components | HIGH | Acceptance criterion: bundle_evidence field contains literal "Measurement command: rm -rf .next/cache .next && ANALYZE=true pnpm build" AND non-placeholder numeric KB values AND "PASS" for tiptap-absent check |
| @tiptap/* added to optimizePackageImports — D-04 chunk-id stability lock broken, reshuffles splitChunks IDs for all existing components | HIGH | Acceptance criterion: `grep -c "@tiptap" next.config.ts` returns 0; explicit lock comment in placeholder file |
| _dep_re_01_decision committed AFTER pnpm add (wrong commit order) — audit trail breaks the "decision before dep" invariant | HIGH | Three-commit structure: (1) decision block authored + committed, (2) pnpm add committed, (3) bundle_evidence populated + committed. Task 1 commits before Task 2 runs pnpm add |
| sf-rich-editor.tsx accidentally exported from barrel (Pattern B violation) — Tiptap dragged into homepage First Load JS | HIGH | Acceptance criterion: `grep -c "sf-rich-editor" components/sf/index.ts` returns 0 throughout Plan 01 |
| @layer signalframeux CSS rules conflict with existing @layer base or @layer utilities — cascade order undefined | MEDIUM | Insertion point: explicitly after @layer utilities close, before detail-panel overrides; Tailwind v4 handles named @layer declarations correctly — verified in research |
| No Tiptap CSS import guard — developer adds import later thinking it is harmless | MEDIUM | Acceptance criterion: `grep -r "tiptap.*\.css\|prosemirror.*\.css" app/ components/ lib/` returns 0 matches; explicit JSDoc comment in globals.css block: "DO NOT import any Tiptap/ProseMirror CSS file — this @layer block IS the replacement" |
| Worktree leakage: file authored in .claude/worktrees/agent-N/ leaks to main tree as untracked | MEDIUM | Pre-task git status --porcelain check on each file; acceptance criteria verify git show --stat for each commit |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Author _dep_re_01_decision placeholder file (BEFORE pnpm add)</name>
  <files>components/sf/sf-rich-editor.tsx</files>
  <read_first>
    - components/sf/sf-rich-editor.tsx (verify file does NOT yet exist via `ls components/sf/ | grep sf-rich-editor`; if it exists, STOP — worktree leakage suspect)
    - components/sf/index.ts (verify barrel does NOT mention sf-rich-editor — `grep -c "sf-rich-editor" components/sf/index.ts` must return 0)
    - next.config.ts (verify @tiptap not in optimizePackageImports — `grep -c "@tiptap" next.config.ts` must return 0)
    - .planning/phases/73-sfricheditor/73-RESEARCH.md §_dep_re_01_decision Block Schema (lines 544–568) — canonical 7-field comment template
    - .planning/phases/71-sfdatatable/71-01-PLAN.md Task 1 action block — _dep_dt_01_decision precedent, field-for-field
    - .planning/STATE.md "v1.10 Critical Constraints" section — bundle headroom baseline (187.6 KB, 12.4 KB headroom)
  </read_first>
  <action>
    Run `git status --porcelain components/sf/sf-rich-editor.tsx` first. If it returns ANY output, STOP and surface a worktree-leakage warning — file MUST be authored fresh in-phase.

    Also run `ls components/sf/ | grep sf-rich-editor` — if any result, STOP for same reason.

    Create `components/sf/sf-rich-editor.tsx` with EXACTLY this content (replace `<TODAY_ISO>` with `date -u +%Y-%m-%d` output):

        "use client";

        /**
         * SFRichEditor — FRAME layer ProseMirror rich text editor (Pattern B, P3 lazy).
         *
         * IMPLEMENTATION SHIPS IN PLAN 02. This file currently hosts only the
         * _dep_re_01_decision ratification block (DEP-02) per the v1.10
         * dep-decision-at-plan-time invariant.
         *
         * Pattern B contract (DO NOT VIOLATE):
         *   - NEVER export from components/sf/index.ts barrel
         *   - NEVER add @tiptap/* to next.config.ts optimizePackageImports (D-04 lock)
         *   - Consumers import via @/components/sf/sf-rich-editor-lazy (lands in Plan 02)
         *   - Direct import of this file is SUPPORTED but only behind next/dynamic ssr:false
         *
         * CSS isolation contract (DO NOT VIOLATE):
         *   - ALWAYS pass injectCSS: false on every useEditor() call in this file
         *   - ALWAYS pass immediatelyRender: false on every useEditor() call
         *   - The @layer signalframeux { .ProseMirror ... } block in app/globals.css
         *     IS the stylesheet replacement — DO NOT import any Tiptap/ProseMirror CSS file
         *
         * Character count: delivered via editor.getText().length (zero-dep) in Plan 02.
         * @tiptap/extension-character-count is NOT a dep — documented in _dep_re_01_decision.
         */

        // ---------------------------------------------------------------------------
        // _dep_re_01_decision — runtime-dep ratification block (DEP-02)
        //
        // Schema precedent: _dep_dt_01_decision at components/sf/sf-data-table.tsx
        // (Phase 71 Plan 01 — field-for-field identical schema)
        //
        // _dep_re_01_decision:
        //   decided: "<TODAY_ISO>"
        //   audit: "sf-rich-editor:runtime-dep"
        //   dep_added:
        //     - "@tiptap/react"
        //     - "@tiptap/pm"
        //     - "@tiptap/starter-kit"
        //     - "@tiptap/extension-link"
        //   version: "<TBD — populated by Task 2 post-install via pnpm list --depth=0>"
        //   rationale: |
        //     SFRichEditor wraps ProseMirror via Tiptap v3 (useEditor + EditorContent
        //     + StarterKit extension model). No viable zero-dep substitute:
        //     Quill auto-injects unlayered CSS (overrides @layer signalframeux tokens;
        //     no injectCSS:false equivalent). Slate requires a full custom schema and
        //     has no first-class toolbar model. Raw ProseMirror requires ~2000 LOC
        //     plugin authorship for comparable feature surface.
        //     @tiptap/pm replaces individual prosemirror-* packages in v3 (bundle
        //     deduplication). @tiptap/extension-link is separate from StarterKit in v3
        //     (verified post-install via pnpm list). Character count delivered via
        //     editor.getText().length (zero-dep) — @tiptap/extension-character-count
        //     NOT added. P3 lazy posture (next/dynamic ssr:false in
        //     sf-rich-editor-lazy.tsx, NOT in sf/index.ts barrel, NOT in
        //     next.config.ts optimizePackageImports — D-04 chunk-id stability lock
        //     holds) keeps homepage First Load JS impact at 0 KB.
        //     injectCSS:false + immediatelyRender:false are mandatory SSR/cascade guards
        //     — documented in JSDoc and enforced by grep acceptance criteria in
        //     73-01-PLAN.md and 73-02-PLAN.md.
        //   bundle_evidence:
        //     - "TBD — populated by Task 3 post-install via ANALYZE=true pnpm build"
        //     - "Pre-add homepage First Load JS baseline: 187.6 KB gzip (Phase 72 close, MILESTONES.md)"
        //   review_gate: |
        //     Re-evaluate when Tiptap v4 reaches stable release. Re-pin and
        //     re-measure if v4 changes API surface (useEditor options, extension
        //     model) or bundle profile materially. Also fires if BND budget changes
        //     (currently 200 KB hard target from CLAUDE.md) or if D-04 chunk-id
        //     stability lock is intentionally broken in a future BND phase.
        //   scope: "@tiptap/* runtime deps — single P3 lazy chunk (sf-rich-editor)"
        //   ratified_to_main_via: "Phase 73 (Plan 01 commit)"
        // ---------------------------------------------------------------------------

        // Implementation lands in Plan 02. This export prevents accidental
        // barrel inclusion bugs (TypeScript will error if anyone tries to
        // import { SFRichEditor } before Plan 02 ships the real symbol).
        export {};

    Replace `<TODAY_ISO>` with the output of `date -u +%Y-%m-%d`.

    DO NOT modify components/sf/index.ts. DO NOT modify next.config.ts. DO NOT run pnpm add yet.

    Commit: `Chore(73-01): author _dep_re_01_decision block (pre-install ratification)`
  </action>
  <verify>
    <automated>test -f components/sf/sf-rich-editor.tsx && grep -q "_dep_re_01_decision:" components/sf/sf-rich-editor.tsx && grep -q "audit: \"sf-rich-editor:runtime-dep\"" components/sf/sf-rich-editor.tsx && grep -q "@tiptap/react" components/sf/sf-rich-editor.tsx && grep -q "review_gate:" components/sf/sf-rich-editor.tsx && grep -q "injectCSS: false" components/sf/sf-rich-editor.tsx && [ "$(grep -c 'sf-rich-editor' components/sf/index.ts)" = "0" ] && [ "$(grep -c '@tiptap' next.config.ts)" = "0" ] && git log -1 --format=%s | grep -qE "Chore\(73-01\):.*_dep_re_01_decision"</automated>
  </verify>
  <acceptance_criteria>
    - `components/sf/sf-rich-editor.tsx` exists and starts with `"use client";` directive on line 1
    - File contains the literal string `_dep_re_01_decision:` exactly once
    - File contains all 7 schema fields verifiable by grep: `grep -c "decided:\|audit:\|dep_added:\|version:\|rationale:\|bundle_evidence:\|review_gate:" components/sf/sf-rich-editor.tsx` returns ≥ 7
    - `audit:` field value is exactly `"sf-rich-editor:runtime-dep"`
    - File contains the literal string `injectCSS: false` and `immediatelyRender: false` in the JSDoc/comment section (pre-impl documentation)
    - File ends with `export {};` (placeholder export — no real symbol until Plan 02)
    - `grep -c "sf-rich-editor" components/sf/index.ts` returns `0` (Pattern B: NOT in barrel)
    - `grep -c "@tiptap" next.config.ts` returns `0` (D-04 lock holds)
    - `git log -1 --format=%s -- components/sf/sf-rich-editor.tsx` matches pattern `Chore\(73-01\):.*_dep_re_01_decision`
  </acceptance_criteria>
  <done>
    Placeholder file committed with `_dep_re_01_decision` block pre-install. bundle_evidence shows "TBD" placeholder values. Task 2 will install the deps; Task 3 will populate bundle_evidence with measured values.
  </done>
</task>

<task type="auto">
  <name>Task 2: Install @tiptap/* v3 packages (automation-first; pre-install version probe + link verification)</name>
  <files>package.json, pnpm-lock.yaml</files>
  <read_first>
    - package.json (verify @tiptap/* NOT already present — `node -e "const p=require('./package.json'); console.log(Object.keys({...p.dependencies,...p.devDependencies}).filter(k=>k.includes('tiptap')))"` must return empty array)
    - components/sf/sf-rich-editor.tsx (verify Task 1 placeholder file with `_dep_re_01_decision` block is committed and on disk)
    - .planning/phases/73-sfricheditor/73-RESEARCH.md §Tiptap Version Decision §Dep Set to Record (lines 76–89) — exact package set and v3/v2 fallback logic
  </read_first>
  <action>
    Per the automation-first principle: `pnpm add` is a CLI operation that runs autonomously.

    Step 1 — pre-install version probe:

        npm view @tiptap/react version

    Capture the printed version string. If the version starts with `4.` (i.e., Tiptap v4 has shipped), STOP — Plan 01 needs review per the `review_gate` clause. Surface this to the user and do not proceed.

    If version is `2.x`, note it — research recommends v3 but v2 is the documented fallback. If v3 is unavailable, proceed with `@tiptap/react@^2 @tiptap/pm@^2 @tiptap/starter-kit@^2 @tiptap/extension-link@^2` and document in bundle_evidence.

    Step 2 — install (attempt v3.22.5 first; fall back to ^3 if 3.22.5 not published):

        pnpm add @tiptap/react@3.22.5 @tiptap/pm@3.22.5 @tiptap/starter-kit@3.22.5 @tiptap/extension-link@3.22.5

    If `pnpm add @tiptap/react@3.22.5` produces a "No matching version found" error, use:

        pnpm add @tiptap/react@^3 @tiptap/pm@^3 @tiptap/starter-kit@^3 @tiptap/extension-link@^3

    Expected: packages added to `package.json` dependencies; `pnpm-lock.yaml` updated; no peer-dep ERRORS (React 19 peer-dep warnings are acceptable noise).

    Step 3 — verify installation and capture resolved versions:

        pnpm list @tiptap/react @tiptap/pm @tiptap/starter-kit @tiptap/extension-link --depth=0

    Record the exact resolved versions from this output. The `version` field in `_dep_re_01_decision` will be populated in Task 3 using these values.

    Step 4 — verify @tiptap/extension-link is separate from StarterKit (research uncertainty item):

        pnpm list @tiptap/extension-link --depth=0

    If this returns no output (meaning extension-link IS bundled in StarterKit v3 and not a separate top-level dep), update the dep_added list in Task 3 accordingly. If it IS a separate dep (expected for v3), it confirms the research prediction.

    Step 5 — worktree leakage check:

        git status --porcelain | grep -vE "package\.json|pnpm-lock\.yaml|sf-rich-editor\.tsx"

    Expected: NO output (only the listed files modified). If output appears, STOP — investigate.

    Step 6 — commit:

        git add package.json pnpm-lock.yaml
        git commit -m "Chore(73-01): pnpm add @tiptap/* v3 (DEP-02 install)"

    DO NOT modify `components/sf/sf-rich-editor.tsx` in this commit; bundle_evidence back-fill is Task 3.
    DO NOT modify `components/sf/index.ts` (Pattern B contract).
    DO NOT modify `next.config.ts` (D-04 lock holds).
  </action>
  <verify>
    <automated>pnpm list @tiptap/react --depth=0 2>&1 | grep -q "@tiptap/react" && grep -q '"@tiptap/react"' package.json && grep -q "@tiptap/react" pnpm-lock.yaml && [ "$(grep -c '@tiptap' next.config.ts)" = "0" ] && [ "$(grep -c 'sf-rich-editor' components/sf/index.ts)" = "0" ] && git log -1 --format=%s | grep -qE "Chore\(73-01\):.*pnpm add"</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm list @tiptap/react --depth=0` exits 0 and prints a @tiptap/react entry at v2.x or v3.x (not v1, not v4)
    - `package.json` `dependencies` field contains the literal key `"@tiptap/react"` — `node -e "console.log(require('./package.json').dependencies['@tiptap/react'])"` returns a non-null version range
    - `package.json` also contains `"@tiptap/pm"`, `"@tiptap/starter-kit"`, `"@tiptap/extension-link"` as deps
    - `pnpm-lock.yaml` contains the literal substring `'@tiptap/react'`
    - `next.config.ts` does NOT contain the substring `@tiptap` — `grep -c "@tiptap" next.config.ts` returns `0` (D-04 lock holds)
    - `components/sf/index.ts` does NOT contain the substring `sf-rich-editor` (Pattern B contract)
    - `git show --stat HEAD` lists ONLY package.json + pnpm-lock.yaml (two files; sf-rich-editor.tsx NOT in this commit)
    - `git log -1 --format=%s` matches the pattern `Chore\(73-01\):.*pnpm add`
  </acceptance_criteria>
  <done>
    @tiptap/* installed and committed. Pre-install version probe confirmed version track (v2 or v3). Resolved version captured for Task 3. Worktree clean. Pattern B and D-04 locks hold.
  </done>
</task>

<task type="auto">
  <name>Task 3: Measure bundle impact, back-fill bundle_evidence, author @layer signalframeux CSS block</name>
  <files>components/sf/sf-rich-editor.tsx, app/globals.css</files>
  <read_first>
    - components/sf/sf-rich-editor.tsx (current state — bundle_evidence field still shows TBD; version field shows TBD)
    - app/globals.css (full file — locate exact line where @layer utilities closes and where to insert @layer signalframeux; verify no existing @layer signalframeux block)
    - .planning/phases/73-sfricheditor/73-RESEARCH.md §ProseMirror CSS Isolation Architecture (lines 93–193) — exact CSS rules and their spacing token values
    - .planning/phases/73-sfricheditor/73-RESEARCH.md §Validation Architecture RE-04 predicates (lines 794–802) — grep commands used in acceptance criteria
    - tests/v1.8-phase63-1-bundle-budget.spec.ts (existing 200 KB hard-constraint spec — will run at end of task)
  </read_first>
  <action>
    Two sub-tasks: bundle measurement then globals.css edit. Both committed together (they form the "decision complete" cohort).

    --- SUB-TASK A: Bundle measurement and _dep_re_01_decision back-fill ---

    Step 1 — clean build + measure:

        rm -rf .next/cache .next
        ANALYZE=true pnpm build 2>&1 | tee /tmp/73-01-bundle.log

    Wait for build to complete.

    Step 2 — capture homepage First Load JS (App Router manifest):

        node -e "
        const fs = require('fs');
        try {
          const m = JSON.parse(fs.readFileSync('.next/build-manifest.json', 'utf8'));
          const pages = m.pages || {};
          const home = pages['/'] || [];
          console.log('Homepage chunks:', home.length);
        } catch(e) {
          // App Router may use app-build-manifest.json
          const m2 = JSON.parse(fs.readFileSync('.next/app-build-manifest.json', 'utf8'));
          const pages = m2.pages || {};
          const home = pages['/page'] || [];
          console.log('Homepage chunks (app router):', home.length);
        }
        "

    Read the Next.js build CLI output for the `/ Route (App)` line showing First Load JS size in KB.

    Step 3 — verify @tiptap/* absent from homepage First Load chunk manifest:

        node -e "
        const fs = require('fs');
        let homeStr = '';
        try {
          homeStr = JSON.stringify(JSON.parse(fs.readFileSync('.next/build-manifest.json', 'utf8')).pages['/'] || []);
        } catch(e) {
          homeStr = JSON.stringify(JSON.parse(fs.readFileSync('.next/app-build-manifest.json', 'utf8')).pages['/page'] || []);
        }
        const tiptapLeak = /tiptap|prosemirror|starter.kit/.test(homeStr);
        console.log('Tiptap in homepage First Load:', tiptapLeak ? 'FAIL' : 'PASS');
        "

    Expected: `Tiptap in homepage First Load: PASS` (placeholder file has export {} — no import).

    Step 4 — capture resolved version:

        node -e "console.log(require('./node_modules/@tiptap/react/package.json').version)"

    Save this exact string (e.g., `3.22.5`).

    Step 5 — back-fill placeholder fields in `components/sf/sf-rich-editor.tsx`:

    Replace the `version:` field: change `"<TBD — populated by Task 2 post-install via pnpm list --depth=0>"` to the exact resolved version string from Step 4.

    Replace the `bundle_evidence:` array placeholder with MEASURED values:

        //   bundle_evidence:
        //     - "Homepage / First Load JS pre-add baseline: 187.6 KB gzip (Phase 72 close, MILESTONES.md)"
        //     - "Homepage / First Load JS post-add (placeholder file, no impl): <MEASURED> KB gzip"
        //     - "Headroom remaining: <MEASURED> KB under 200 KB hard target"
        //     - "Tiptap in homepage First Load chunk: PASS (placeholder export {} has no @tiptap import)"
        //     - "Tiptap lazy chunk: ABSENT from homepage manifest (real chunk lands Plan 02 behind next/dynamic)"
        //     - "Resolved version: @tiptap/react@<RESOLVED_VERSION>"
        //     - "Measurement command: rm -rf .next/cache .next && ANALYZE=true pnpm build"
        //     - "Measurement date: <TODAY_ISO>"

    Replace ALL `<MEASURED>`, `<RESOLVED_VERSION>`, `<TODAY_ISO>` with real values. Do NOT estimate.

    --- SUB-TASK B: @layer signalframeux block in globals.css ---

    Step 1 — locate insertion point: Find the closing `}` of the last `@layer utilities` block in `app/globals.css`. Insert the new block AFTER this closing `}` and BEFORE any unlayered CSS that follows (detail-panel z-index overrides, etc.).

    Step 2 — insert the following block verbatim at the identified insertion point:

        /* ══════════════════════════════════════════════════════════════
           PROSEMIRROR / TIPTAP CONTENT ISOLATION
           Paired with: injectCSS: false on every useEditor() call in
           components/sf/sf-rich-editor.tsx. These rules ARE the stylesheet
           replacement. DO NOT import any Tiptap/ProseMirror CSS file
           anywhere in app/, components/, or lib/. Adding such an import
           would inject unlayered styles that override these @layer rules.
           Only 4 element rules — structural minimum. Do not expand.
           ══════════════════════════════════════════════════════════════ */
        @layer signalframeux {
          .ProseMirror {
            font-family: inherit;
            outline: none;
            min-height: var(--sfx-space-16);
          }

          .ProseMirror p {
            margin: 0 0 var(--sfx-space-4) 0;
            line-height: 1.5;
          }

          .ProseMirror h1,
          .ProseMirror h2,
          .ProseMirror h3,
          .ProseMirror h4 {
            margin: 0 0 var(--sfx-space-3) 0;
            font-family: var(--sfx-font-sans);
            font-weight: 700;
            line-height: 1.15;
          }

          .ProseMirror ul,
          .ProseMirror ol {
            padding-left: var(--sfx-space-6);
            margin: 0 0 var(--sfx-space-4) 0;
          }

          .ProseMirror li {
            margin-bottom: var(--sfx-space-2);
          }

          .ProseMirror blockquote {
            border-left: 2px solid var(--sfx-foreground);
            padding-left: var(--sfx-space-4);
            margin: 0 0 var(--sfx-space-4) var(--sfx-space-2);
            color: var(--sfx-muted-foreground);
            font-style: italic;
          }
        }

    Spacing tokens used: --sfx-space-2 (8px), --sfx-space-3 (12px), --sfx-space-4 (16px), --sfx-space-6 (24px), --sfx-space-16 (for min-height floor). All blessed stops per CLAUDE.md.

    Step 3 — verify no Tiptap CSS import anywhere:

        grep -r "tiptap.*\.css\|prosemirror.*\.css" app/ components/ lib/

    Expected: 0 matches.

    Step 4 — run bundle budget spec:

        pnpm exec playwright test tests/v1.8-phase63-1-bundle-budget.spec.ts --project=chromium --reporter=line

    Expected: PASS.

    Step 5 — commit both files together:

        git add components/sf/sf-rich-editor.tsx app/globals.css
        git commit -m "Chore(73-01): populate bundle_evidence with measured values + @layer signalframeux ProseMirror CSS isolation (DEP-02 + RE-04)"
  </action>
  <verify>
    <automated>grep -q "version: \"" components/sf/sf-rich-editor.tsx && ! grep -q "TBD" components/sf/sf-rich-editor.tsx && ! grep -q "MEASURED>" components/sf/sf-rich-editor.tsx && grep -q "Measurement command: rm -rf .next/cache .next && ANALYZE=true pnpm build" components/sf/sf-rich-editor.tsx && grep -q "PASS" components/sf/sf-rich-editor.tsx && grep -c "\.ProseMirror" app/globals.css | grep -qE "^[4-9]|^[1-9][0-9]" && grep -q "@layer signalframeux" app/globals.css && ! grep -r "tiptap.*\.css\|prosemirror.*\.css" app/ components/ lib/ && pnpm exec playwright test tests/v1.8-phase63-1-bundle-budget.spec.ts --project=chromium --reporter=line</automated>
  </verify>
  <acceptance_criteria>
    - `components/sf/sf-rich-editor.tsx` `version:` field is populated with a literal version string (v2.x or v3.x — not "TBD")
    - File `bundle_evidence:` array contains at least 6 literal entries — `grep -c "^//\s*-" components/sf/sf-rich-editor.tsx` returns ≥ 6
    - File contains the literal string `Measurement command: rm -rf .next/cache .next && ANALYZE=true pnpm build` (proves measurement was run, not estimated)
    - File contains the literal string `PASS` in the bundle_evidence array (tiptap-absent check passed)
    - File does NOT contain the substring `TBD` — `! grep -q "TBD" components/sf/sf-rich-editor.tsx`
    - File does NOT contain `MEASURED>` or `RESOLVED_VERSION>` or `TODAY_ISO>` (no template placeholders remaining)
    - `app/globals.css` contains the literal substring `@layer signalframeux` — `grep -c "@layer signalframeux" app/globals.css` returns ≥ 1
    - `app/globals.css` contains at least 4 `.ProseMirror` selectors — `grep -c "\.ProseMirror" app/globals.css` returns ≥ 4 (covering p, h1-h4, ul/ol/li, blockquote)
    - `.ProseMirror` rules are inside the `@layer signalframeux` block — verify by reading the block: `awk '/@layer signalframeux/,/^}/' app/globals.css | grep -c "ProseMirror"` returns ≥ 4
    - `app/globals.css` does NOT contain `border-radius` inside the `.ProseMirror` block (zero border-radius rule)
    - `grep -r "tiptap.*\.css\|prosemirror.*\.css" app/ components/ lib/` returns 0 matches (no Tiptap CSS import)
    - `pnpm exec playwright test tests/v1.8-phase63-1-bundle-budget.spec.ts --project=chromium` exits 0 (homepage First Load JS still ≤ 200 KB)
    - `git log -1 --format=%s -- app/globals.css` matches pattern `Chore\(73-01\):.*bundle_evidence.*@layer signalframeux`
  </acceptance_criteria>
  <done>
    `_dep_re_01_decision` block carries 7 fully-populated fields with measured bundle_evidence and real resolved version. `app/globals.css` now has the first authored `@layer signalframeux` block with 4 `.ProseMirror` element rules. The 200 KB bundle budget spec is green. The audit trail for the dep introduction is complete and committed before Plan 02 starts writing real impl.
  </done>
</task>

</tasks>

<verification>
End-to-end verification of Plan 01:

(1) `_dep_re_01_decision` block well-formed (7 fields):

    grep -E "decided:|audit:|dep_added:|version:|rationale:|bundle_evidence:|review_gate:" components/sf/sf-rich-editor.tsx | wc -l

Expected: 7 or more.

(2) No placeholder text remaining:

    ! grep -E "TBD|MEASURED>|RESOLVED_VERSION>|TODAY_ISO>" components/sf/sf-rich-editor.tsx

Expected: empty output.

(3) D-04 lock holds (no @tiptap in optimizePackageImports):

    grep -c "@tiptap" next.config.ts

Expected: 0.

(4) Pattern B contract (NOT in barrel):

    grep -c "sf-rich-editor" components/sf/index.ts

Expected: 0.

(5) CSS isolation in globals.css:

    grep -c "@layer signalframeux" app/globals.css
    grep -c "\.ProseMirror" app/globals.css
    grep -r "tiptap.*\.css\|prosemirror.*\.css" app/ components/ lib/

Expected: ≥ 1, ≥ 4, 0 matches.

(6) Bundle budget green:

    pnpm exec playwright test tests/v1.8-phase63-1-bundle-budget.spec.ts --project=chromium

Expected: PASS.

(7) Three-commit audit trail:

    git log --oneline -5 -- components/sf/sf-rich-editor.tsx

Expected: 2 commits on this file (decision block authored; bundle_evidence + globals.css landed together). Plus 1 commit for package.json + pnpm-lock.yaml.

(8) Worktree clean:

    git status --porcelain

Expected: clean.
</verification>

<success_criteria>
- [ ] `components/sf/sf-rich-editor.tsx` exists with 7-field `_dep_re_01_decision` block populated with measured values (no TBD, no template placeholders)
- [ ] `@tiptap/react`, `@tiptap/pm`, `@tiptap/starter-kit`, `@tiptap/extension-link` are installed (`package.json` + `pnpm-lock.yaml`)
- [ ] `next.config.ts` UNCHANGED — D-04 lock holds, `@tiptap/*` NOT in `optimizePackageImports`
- [ ] `components/sf/index.ts` UNCHANGED — `sf-rich-editor` absent from barrel (Pattern B)
- [ ] `app/globals.css` has first `@layer signalframeux` block with ≥ 4 `.ProseMirror` element rules
- [ ] No Tiptap/ProseMirror CSS file imported anywhere in app/, components/, lib/
- [ ] `tests/v1.8-phase63-1-bundle-budget.spec.ts` PASSES (≤ 200 KB homepage First Load JS)
- [ ] Three commits land: (1) decision block authored pre-install, (2) pnpm add, (3) bundle_evidence + globals.css cohort
</success_criteria>

<output>
After completion, create `.planning/phases/73-sfricheditor/73-01-SUMMARY.md` documenting:
- Final resolved @tiptap/* versions
- Measured bundle deltas (pre-add baseline → post-add placeholder)
- The three commit SHAs for the audit trail
- Confirmation that @layer signalframeux block is authored and the CSS layer structure is valid
- Forward link to Plan 02 (impl + P3 lazy + playground)
</output>
