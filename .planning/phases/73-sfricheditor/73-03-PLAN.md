---
phase: 73-sfricheditor
plan: 03
type: execute
wave: 3
depends_on:
  - "73-02"
files_modified:
  - tests/v1.10-phase73-sf-rich-editor.spec.ts
  - tests/v1.10-phase73-sf-rich-editor-axe.spec.ts
  - .planning/phases/73-sfricheditor/73-VALIDATION.md
autonomous: true
requirements_addressed:
  - TST-03
  - RE-05
gap_closure: false
must_haves:
  truths:
    - "tests/v1.10-phase73-sf-rich-editor.spec.ts exists and passes: covers toolbar button active-state (data-active), controlled value API, onChange fires, readOnly toolbar absent from DOM, defaultValue initializes content, Escape key returns focus to toolbar"
    - "tests/v1.10-phase73-sf-rich-editor-axe.spec.ts exists and passes: vacuous-green guard asserts [contenteditable='true'] is visible before axe.analyze(); zero axe violations; covers toolbar role, button-name, region, color-contrast"
    - "@tiptap/* and prosemirror-* are absent from the homepage / First Load chunk manifest — verified via node script on .next/build-manifest.json or app-build-manifest.json after clean ANALYZE=true pnpm build"
    - "Homepage First Load JS remains <= 200 KB — tests/v1.8-phase63-1-bundle-budget.spec.ts PASSES"
    - "SFRichEditor and SFRichEditorLazy are absent from components/sf/index.ts barrel — grep -c 'sf-rich-editor' components/sf/index.ts returns 0"
    - "73-VALIDATION.md per-task rows populated with actual automated commands and status updated to green"
    - "nyquist_compliant: true set in 73-VALIDATION.md frontmatter after all tasks pass"
  artifacts:
    - path: "tests/v1.10-phase73-sf-rich-editor.spec.ts"
      provides: "Playwright acceptance spec — toolbar buttons, controlled API, readOnly, defaultValue, keyboard nav (Escape)"
      contains: "contenteditable"
    - path: "tests/v1.10-phase73-sf-rich-editor-axe.spec.ts"
      provides: "axe-core WCAG AA spec — vacuous-green guard, toolbar role, button-name, region landmark, zero violations"
      contains: "axe.analyze"
    - path: ".planning/phases/73-sfricheditor/73-VALIDATION.md"
      provides: "Per-task rows populated; nyquist_compliant: true; all Wave 0 items checked"
      contains: "nyquist_compliant: true"
  key_links:
    - from: "tests/v1.10-phase73-sf-rich-editor.spec.ts"
      to: "app/dev-playground/sf-rich-editor/page.tsx"
      via: "Playwright navigates to /dev-playground/sf-rich-editor; fixture provides testid anchors"
      pattern: "dev-playground/sf-rich-editor"
    - from: "tests/v1.10-phase73-sf-rich-editor-axe.spec.ts"
      to: "app/dev-playground/sf-rich-editor/page.tsx"
      via: "axe scans the same fixture page after editor hydration; vacuous-green guard asserts [contenteditable='true'] before analyze()"
      pattern: "contenteditable"
---

<objective>
Author Playwright acceptance tests (`tests/v1.10-phase73-sf-rich-editor.spec.ts`) and axe-core WCAG AA tests (`tests/v1.10-phase73-sf-rich-editor-axe.spec.ts`), run all tests green, perform bundle audit closeout (verify Tiptap absent from homepage First Load chunk), verify barrel non-export, and mark the VALIDATION.md Nyquist-compliant.

Purpose: This is the evidence wave. Plan 02 shipped the implementation; Plan 03 proves it satisfies every spec predicate from 73-RESEARCH.md §Validation Architecture. The bundle audit in Task 3 is the final DEP-02 closeout — it uses the same node script from the research doc to confirm `@tiptap/*` is absent from the homepage manifest after a clean production build including the full component implementation (not just the Plan 01 placeholder).

Output: 2 test files (green); bundle audit evidence captured; VALIDATION.md updated to `nyquist_compliant: true`.
</objective>

<execution_context>
@/Users/greyaltaer/.claude/pde-os/engines/gsd/workflows/execute-plan.md
@/Users/greyaltaer/.claude/pde-os/engines/gsd/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/73-sfricheditor/73-RESEARCH.md
@.planning/phases/73-sfricheditor/73-VALIDATION.md
@CLAUDE.md
@components/sf/index.ts
@app/dev-playground/sf-rich-editor/page.tsx
@components/sf/sf-rich-editor.tsx
@components/sf/sf-rich-editor-lazy.tsx
@tests/v1.10-phase72-sf-combobox-axe.spec.ts
@tests/v1.10-phase71-sf-data-table-axe.spec.ts

<interfaces>
<!-- Playwright fixture URL -->

const FIXTURE_URL = "/dev-playground/sf-rich-editor";

<!-- axe-core import pattern (mirrors Phase 71/72 precedent) -->

import AxeBuilder from "@axe-core/playwright";
// Before axe.analyze() — vacuous-green guard:
await expect(page.locator('[contenteditable="true"]')).toBeVisible();
const results = await new AxeBuilder({ page }).analyze();
expect(results.violations).toHaveLength(0);

<!-- Playwright test for toolbar button active-state (RE-01 predicate) -->

await page.goto(FIXTURE_URL);
await page.waitForSelector('[contenteditable="true"]', { state: "visible" });
// Click in editor to focus it
await page.locator('[data-testid="fixture-uncontrolled-empty"] [contenteditable="true"]').click();
// Type some text first (editor must have content for bold to apply)
await page.keyboard.type("Test content");
// Select all
await page.keyboard.press("Control+a");
// Click Bold button
await page.getByRole("button", { name: "Bold" }).first().click();
// Assert data-active is present on Bold button
await expect(page.getByRole("button", { name: "Bold" }).first()).toHaveAttribute("data-active", "");
// Assert editor HTML contains <strong>
const html = await page.locator('[data-testid="fixture-controlled-output"]').innerText();
// Note: use controlled fixture for HTML output inspection

<!-- Playwright test for readOnly (RE-03 predicate) -->

await page.goto(FIXTURE_URL);
await page.waitForSelector('[contenteditable="true"]', { state: "visible" });
// Read-only section: toolbar should be absent from DOM
const toolbarInReadonly = await page.locator('[data-testid="fixture-readonly"] [role="toolbar"]').count();
expect(toolbarInReadonly).toBe(0);
// contenteditable="false" in read-only mode
await expect(
  page.locator('[data-testid="fixture-readonly"] [contenteditable]')
).toHaveAttribute("contenteditable", "false");

<!-- Playwright test for onChange + controlled value (RE-03 predicate) -->

await page.goto(FIXTURE_URL);
await page.waitForSelector('[data-testid="fixture-controlled"] [contenteditable="true"]', { state: "visible" });
// Type in controlled editor
await page.locator('[data-testid="fixture-controlled"] [contenteditable="true"]').click();
await page.keyboard.press("Control+a");
await page.keyboard.type("New controlled content");
// fixture-controlled-output pre element should update
await expect(page.locator('[data-testid="fixture-controlled-output"]')).toContainText("New controlled content");

<!-- axe vacuous-green guard (TST-03 predicate) -->

// Guard: editor must be mounted + hydrated before axe scan
await page.waitForSelector('[contenteditable="true"]', { state: "visible", timeout: 10000 });
await expect(page.locator('[contenteditable="true"]').first()).toBeVisible();
// Now safe to run axe
const results = await new AxeBuilder({ page }).analyze();

<!-- Escape key focus-return test -->

await page.locator('[data-testid="fixture-uncontrolled-empty"] [contenteditable="true"]').click();
await page.keyboard.press("Escape");
// First toolbar button should be focused
const focusedElement = await page.evaluate(() => document.activeElement?.getAttribute("aria-label"));
expect(focusedElement).toBe("Bold");

<!-- Bundle leak verification (RE-05 / DEP-02 closeout) -->

node -e "
const fs = require('fs');
let homeStr = '';
try {
  homeStr = JSON.stringify(JSON.parse(fs.readFileSync('.next/build-manifest.json', 'utf8')).pages['/'] || []);
} catch(e) {
  homeStr = JSON.stringify(JSON.parse(fs.readFileSync('.next/app-build-manifest.json', 'utf8')).pages['/page'] || []);
}
const tiptapLeak = /tiptap|prosemirror|starter.kit/.test(homeStr);
console.log('Tiptap in homepage First Load:', tiptapLeak ? 'FAIL' : 'PASS');
process.exit(tiptapLeak ? 1 : 0);
"
</interfaces>

<threat_model>
ASVS L1 — Phase 73 Plan 03 threat surface:

| Threat | Severity | Mitigation |
|--------|----------|------------|
| Vacuous-green axe test: axe.analyze() runs before ProseMirror hydrates — toolbar is absent (skeleton) so zero violations trivially; test passes but proves nothing | HIGH | Acceptance criterion: test file contains `[contenteditable="true"]` as a waitForSelector / toBeVisible assertion BEFORE axe.analyze(); test FAILS if editor not mounted |
| Bundle leak (Plan 02 full impl): sf-rich-editor.tsx now has real @tiptap/* imports — if barrel IS touched (despite guard), Tiptap appears in homepage chunk | HIGH | Acceptance criterion: bundle audit node script exits 0 ("PASS"); Playwright bundle-budget spec passes; bundle audit runs on clean ANALYZE=true pnpm build (not cached) |
| Playwright tests pass on skeleton state (editor not hydrated): locator finds contenteditable but it is the SFSkeleton placeholder, not the ProseMirror editor | MEDIUM | Acceptance criterion: RE-01 test asserts `data-active` attribute on Bold button AFTER clicking Bold; data-active is only set by the real ProseMirror editor — skeleton has no toolbar |
| axe `region` violation: SFRichEditor renders inside a `<main>` but editor surface not in a landmark region | MEDIUM | SFRichEditor wraps in `<section aria-label="Rich text editor">` — this IS a landmark region (sectioning element with accessible name); axe `region` rule will pass |
| Test brittle on editor HTML output format: ProseMirror may produce slightly different HTML (e.g., `<p class="">` instead of `<p>`) — string equality assertions fail | LOW | Use toContainText() on the output pre-element rather than exact HTML match; or use `expect(html).toMatch(/<strong>/)` pattern matching |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Author Playwright acceptance spec — toolbar, controlled API, readOnly, keyboard nav (TST-03 + RE-01/02/03)</name>
  <files>tests/v1.10-phase73-sf-rich-editor.spec.ts</files>
  <read_first>
    - tests/v1.10-phase73-sf-rich-editor.spec.ts (verify file does NOT yet exist — worktree leakage check)
    - tests/v1.10-phase71-sf-data-table-axe.spec.ts (axe spec file pattern — import, test structure, waitFor pattern)
    - app/dev-playground/sf-rich-editor/page.tsx (confirm testid anchors: fixture-uncontrolled-empty, fixture-controlled, fixture-controlled-output, fixture-readonly, fixture-default-value)
    - components/sf/sf-rich-editor.tsx (confirm aria-label strings for toolbar buttons — exactly "Bold", "Italic", "Underline", "Strikethrough", "Inline code", "Heading 1", "Heading 2", "Heading 3", "Bullet list", "Ordered list", "Blockquote", "Code block", "Link")
    - .planning/phases/73-sfricheditor/73-RESEARCH.md §Validation Architecture RE-01/RE-02/RE-03 predicates (lines 814–833) — exact predicate strings used in acceptance criteria
  </read_first>
  <action>
    Run `git status --porcelain tests/v1.10-phase73-sf-rich-editor.spec.ts` first. If output appears, STOP — worktree leakage.

    Create `tests/v1.10-phase73-sf-rich-editor.spec.ts`:

        import { test, expect } from "@playwright/test";

        const FIXTURE = "/dev-playground/sf-rich-editor";

        test.describe("SFRichEditor — acceptance (Phase 73 RE-01/02/03)", () => {
          test.beforeEach(async ({ page }) => {
            await page.goto(FIXTURE);
            // Wait for ProseMirror hydration — editor must be mounted before any test runs
            await page.waitForSelector('[contenteditable="true"]', {
              state: "visible",
              timeout: 10000,
            });
          });

          // RE-03: controlled value initializes editor content
          test("controlled fixture renders initial HTML content", async ({ page }) => {
            const content = await page
              .locator('[data-testid="fixture-controlled"] [contenteditable="true"]')
              .innerText();
            expect(content).toContain("Hello");
            expect(content).toContain("world");
          });

          // RE-03: onChange fires and updates controlled output
          test("controlled onChange updates output pre on edit", async ({ page }) => {
            const editor = page.locator(
              '[data-testid="fixture-controlled"] [contenteditable="true"]'
            );
            await editor.click();
            await page.keyboard.press("Control+a");
            await page.keyboard.type("Changed content via keyboard");
            await expect(
              page.locator('[data-testid="fixture-controlled-output"]')
            ).toContainText("Changed content via keyboard");
          });

          // RE-03: readOnly — toolbar absent from DOM (not just hidden)
          test("readOnly removes toolbar from DOM", async ({ page }) => {
            const toolbarCount = await page
              .locator('[data-testid="fixture-readonly"] [role="toolbar"]')
              .count();
            expect(toolbarCount).toBe(0);
          });

          // RE-03: readOnly — contenteditable is false
          test("readOnly sets contenteditable=false", async ({ page }) => {
            await expect(
              page.locator('[data-testid="fixture-readonly"] [contenteditable]')
            ).toHaveAttribute("contenteditable", "false");
          });

          // RE-03: defaultValue initializes content in uncontrolled mode
          test("defaultValue fixture shows initial content", async ({ page }) => {
            const content = await page
              .locator(
                '[data-testid="fixture-default-value"] [contenteditable="true"]'
              )
              .innerText();
            expect(content).toContain("Initial content");
          });

          // RE-01: toolbar role present in non-readOnly editor
          test("toolbar has role=toolbar and aria-label", async ({ page }) => {
            await expect(
              page.locator('[data-testid="fixture-uncontrolled-empty"] [role="toolbar"]')
            ).toBeVisible();
            await expect(
              page.locator('[data-testid="fixture-uncontrolled-empty"] [role="toolbar"]')
            ).toHaveAttribute("aria-label", "Text formatting");
          });

          // RE-01: all 13 toolbar buttons present by aria-label
          test("all RE-01 and RE-02 toolbar buttons are present and accessible", async ({
            page,
          }) => {
            const expectedLabels = [
              "Bold",
              "Italic",
              "Underline",
              "Strikethrough",
              "Inline code",
              "Heading 1",
              "Heading 2",
              "Heading 3",
              "Bullet list",
              "Ordered list",
              "Blockquote",
              "Code block",
              "Link",
            ];
            for (const label of expectedLabels) {
              await expect(
                page.getByRole("button", { name: label }).first()
              ).toBeVisible();
            }
          });

          // RE-01: Bold button sets data-active attribute after applying bold
          test("Bold button sets data-active when bold is active", async ({ page }) => {
            const editor = page.locator(
              '[data-testid="fixture-uncontrolled-empty"] [contenteditable="true"]'
            );
            await editor.click();
            await page.keyboard.type("bold test");
            await page.keyboard.press("Control+a");
            const boldButton = page
              .locator('[data-testid="fixture-uncontrolled-empty"] [role="toolbar"]')
              .getByRole("button", { name: "Bold" });
            await boldButton.click();
            // data-active attribute must be present (not "false" — it should be "" or truthy)
            await expect(boldButton).toHaveAttribute("data-active");
          });

          // RE-02: Link button opens native prompt dialog
          test("Link button opens window.prompt dialog", async ({ page }) => {
            const dialogPromise = page.waitForEvent("dialog");
            const editor = page.locator(
              '[data-testid="fixture-uncontrolled-empty"] [contenteditable="true"]'
            );
            await editor.click();
            await page.keyboard.type("link text");
            await page.keyboard.press("Control+a");
            await page
              .locator('[data-testid="fixture-uncontrolled-empty"] [role="toolbar"]')
              .getByRole("button", { name: "Link" })
              .click();
            const dialog = await dialogPromise;
            expect(dialog.type()).toBe("prompt");
            await dialog.dismiss();
          });

          // RE-03: Escape key returns focus to first toolbar button
          test("Escape from contenteditable returns focus to first toolbar button", async ({
            page,
          }) => {
            const editor = page.locator(
              '[data-testid="fixture-uncontrolled-empty"] [contenteditable="true"]'
            );
            await editor.click();
            await page.keyboard.press("Escape");
            // After Escape, the focused element should have aria-label "Bold" (first toolbar button)
            const focusedLabel = await page.evaluate(
              () => document.activeElement?.getAttribute("aria-label")
            );
            expect(focusedLabel).toBe("Bold");
          });
        });

    Run the spec to verify all tests pass:

        pnpm exec playwright test tests/v1.10-phase73-sf-rich-editor.spec.ts --project=chromium --reporter=line

    If any test fails, debug the fixture or component — do NOT weaken the test predicate.

    Commit:

        git add tests/v1.10-phase73-sf-rich-editor.spec.ts
        git commit -m "Feat(73-03): Playwright acceptance spec — toolbar, controlled API, readOnly, keyboard nav (TST-03)"
  </action>
  <verify>
    <automated>test -f tests/v1.10-phase73-sf-rich-editor.spec.ts && grep -q "contenteditable" tests/v1.10-phase73-sf-rich-editor.spec.ts && grep -q "waitForSelector" tests/v1.10-phase73-sf-rich-editor.spec.ts && grep -q "data-active" tests/v1.10-phase73-sf-rich-editor.spec.ts && grep -q "role.*toolbar" tests/v1.10-phase73-sf-rich-editor.spec.ts && grep -q "readOnly.*toolbar\|toolbar.*readOnly\|role..toolbar...*count\|count.*0" tests/v1.10-phase73-sf-rich-editor.spec.ts && grep -q "dialog\|prompt" tests/v1.10-phase73-sf-rich-editor.spec.ts && grep -q "Escape" tests/v1.10-phase73-sf-rich-editor.spec.ts && pnpm exec playwright test tests/v1.10-phase73-sf-rich-editor.spec.ts --project=chromium --reporter=line</automated>
  </verify>
  <acceptance_criteria>
    - `tests/v1.10-phase73-sf-rich-editor.spec.ts` exists
    - File contains `waitForSelector('[contenteditable="true"]'` (vacuous-green guard pattern — editor must be mounted before assertions)
    - File covers controlled API: contains `fixture-controlled` testid reference with assertion on `fixture-controlled-output`
    - File covers readOnly toolbar-absent: `role="toolbar"` count assertion returning `toBe(0)` inside the `fixture-readonly` section
    - File covers readOnly contenteditable=false: `toHaveAttribute("contenteditable", "false")` assertion
    - File covers defaultValue: `fixture-default-value` testid reference with content assertion
    - File covers Bold data-active: `toHaveAttribute("data-active")` on Bold button after clicking
    - File covers Link prompt dialog: `waitForEvent("dialog")` + `dialog.type()` toBe `"prompt"` (RE-02)
    - File covers Escape focus-return: `page.keyboard.press("Escape")` + `document.activeElement?.getAttribute("aria-label")` === "Bold"
    - File covers all 13 toolbar button labels in a loop assertion (verifiable: `grep -c "Bold\|Italic\|Underline\|Strikethrough\|Inline code\|Heading 1\|Heading 2\|Heading 3\|Bullet list\|Ordered list\|Blockquote\|Code block\|Link" tests/v1.10-phase73-sf-rich-editor.spec.ts` returns ≥ 13 total occurrences)
    - `pnpm exec playwright test tests/v1.10-phase73-sf-rich-editor.spec.ts --project=chromium --reporter=line` exits 0 (all tests pass)
    - `git log -1 --format=%s -- tests/v1.10-phase73-sf-rich-editor.spec.ts` matches `Feat\(73-03\):.*Playwright`
  </acceptance_criteria>
  <done>
    Playwright acceptance spec ships and passes. RE-01/02/03 predicates from RESEARCH.md Validation Architecture verified. Toolbar functionality, controlled API, readOnly mode, and keyboard nav all green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author axe-core WCAG AA spec — vacuous-green guard, toolbar role, button-name, region (TST-03)</name>
  <files>tests/v1.10-phase73-sf-rich-editor-axe.spec.ts</files>
  <read_first>
    - tests/v1.10-phase73-sf-rich-editor-axe.spec.ts (verify file does NOT yet exist — worktree leakage check)
    - tests/v1.10-phase71-sf-data-table-axe.spec.ts (canonical axe spec pattern — AxeBuilder import, vacuous-green guard, violations assertion shape)
    - tests/v1.10-phase72-sf-combobox-axe.spec.ts (Phase 72 precedent — combobox axe spec structure)
    - app/dev-playground/sf-rich-editor/page.tsx (fixture page sections with testids)
    - .planning/phases/73-sfricheditor/73-RESEARCH.md §axe-core Focus Management (lines 678–730) — rules that fire on contenteditable, vacuous-green guard requirement, fixture states required (empty, with-content, read-only)
    - .planning/phases/73-sfricheditor/73-RESEARCH.md §Validation Architecture TST-03 predicates (lines 845–853)
  </read_first>
  <action>
    Run `git status --porcelain tests/v1.10-phase73-sf-rich-editor-axe.spec.ts` first. If output appears, STOP — worktree leakage.

    Create `tests/v1.10-phase73-sf-rich-editor-axe.spec.ts` (mirrors Phase 71/72 axe spec structure):

        import { test, expect } from "@playwright/test";
        import AxeBuilder from "@axe-core/playwright";

        const FIXTURE = "/dev-playground/sf-rich-editor";

        test.describe("SFRichEditor — axe WCAG AA (Phase 73 TST-03)", () => {
          // VACUOUS-GREEN GUARD:
          // All three test cases must assert [contenteditable="true"] is visible
          // BEFORE calling axe.analyze(). This prevents the axe scan from running
          // on the skeleton loading state (which has no toolbar — zero violations
          // trivially — but proves nothing about the real component).

          test("empty editor: zero axe violations (toolbar + contenteditable mounted)", async ({
            page,
          }) => {
            await page.goto(FIXTURE);

            // Vacuous-green guard — MANDATORY before axe.analyze():
            await expect(
              page.locator('[data-testid="fixture-uncontrolled-empty"] [contenteditable="true"]')
            ).toBeVisible({ timeout: 10000 });

            // Scan only the empty-editor fixture section to avoid noise from other sections
            const results = await new AxeBuilder({ page })
              .include('[data-testid="fixture-uncontrolled-empty"]')
              .analyze();

            expect(results.violations).toHaveLength(0);
          });

          test("editor with rich content: zero axe violations (isActive button states)", async ({
            page,
          }) => {
            await page.goto(FIXTURE);

            // Vacuous-green guard:
            await expect(
              page.locator('[data-testid="fixture-controlled"] [contenteditable="true"]')
            ).toBeVisible({ timeout: 10000 });

            // Click Bold in the controlled fixture to trigger isActive button state
            await page
              .locator('[data-testid="fixture-controlled"] [contenteditable="true"]')
              .click();
            await page.keyboard.press("Control+a");
            await page
              .locator('[data-testid="fixture-controlled"] [role="toolbar"]')
              .getByRole("button", { name: "Bold" })
              .click();

            // Now scan the controlled section with bold active (tests data-active button state)
            const results = await new AxeBuilder({ page })
              .include('[data-testid="fixture-controlled"]')
              .analyze();

            expect(results.violations).toHaveLength(0);
          });

          test("read-only editor: zero axe violations (toolbar absent — no missing button-name)", async ({
            page,
          }) => {
            await page.goto(FIXTURE);

            // Vacuous-green guard — read-only section uses contenteditable="false"
            // but element still exists in DOM
            await expect(
              page.locator('[data-testid="fixture-readonly"] [contenteditable]')
            ).toBeVisible({ timeout: 10000 });

            const results = await new AxeBuilder({ page })
              .include('[data-testid="fixture-readonly"]')
              .analyze();

            expect(results.violations).toHaveLength(0);
          });

          // Structural assertions verifying editor ARIA is present (belt-and-suspenders)
          test("toolbar role and aria-label present in editable editor", async ({ page }) => {
            await page.goto(FIXTURE);
            await expect(
              page.locator('[data-testid="fixture-uncontrolled-empty"] [contenteditable="true"]')
            ).toBeVisible({ timeout: 10000 });

            // role="toolbar" must be present
            await expect(
              page.locator('[data-testid="fixture-uncontrolled-empty"] [role="toolbar"]')
            ).toBeVisible();

            // At minimum 1 toolbar button with accessible name must be present
            const buttonCount = await page
              .locator('[data-testid="fixture-uncontrolled-empty"] [role="toolbar"] button[aria-label]')
              .count();
            expect(buttonCount).toBeGreaterThanOrEqual(13);
          });

          test("editor content area is inside a region landmark (section with aria-label)", async ({
            page,
          }) => {
            await page.goto(FIXTURE);
            await expect(
              page.locator('[data-testid="fixture-uncontrolled-empty"] [contenteditable="true"]')
            ).toBeVisible({ timeout: 10000 });

            // SFRichEditor wraps in <section aria-label="Rich text editor">
            // which is a landmark region
            await expect(
              page.locator('[data-testid="fixture-uncontrolled-empty"] section[aria-label="Rich text editor"]')
            ).toBeVisible();
          });
        });

    Run the spec to verify all tests pass:

        pnpm exec playwright test tests/v1.10-phase73-sf-rich-editor-axe.spec.ts --project=chromium --reporter=line

    If axe violations appear, investigate and fix the component (do NOT weaken the test). Common causes:
    - Missing aria-label on a toolbar button — add to the button in Task 1 Plan 02 output
    - Missing landmark region — SFRichEditor must wrap in `<section aria-label="Rich text editor">`
    - Color contrast failure — verify `--sfx-foreground` on `--sfx-background` (should be 21:1 per globals.css)

    Commit:

        git add tests/v1.10-phase73-sf-rich-editor-axe.spec.ts
        git commit -m "Feat(73-03): axe WCAG AA spec — vacuous-green guard, toolbar role, region landmark (TST-03)"
  </action>
  <verify>
    <automated>test -f tests/v1.10-phase73-sf-rich-editor-axe.spec.ts && grep -q "AxeBuilder" tests/v1.10-phase73-sf-rich-editor-axe.spec.ts && grep -q "axe.analyze\|\.analyze()" tests/v1.10-phase73-sf-rich-editor-axe.spec.ts && grep -q "contenteditable.*true\|contenteditable" tests/v1.10-phase73-sf-rich-editor-axe.spec.ts && grep -q "toBeVisible" tests/v1.10-phase73-sf-rich-editor-axe.spec.ts && grep -q "violations.*toHaveLength.*0\|toHaveLength(0)" tests/v1.10-phase73-sf-rich-editor-axe.spec.ts && grep -q "VACUOUS\|vacuous\|Vacuous" tests/v1.10-phase73-sf-rich-editor-axe.spec.ts && pnpm exec playwright test tests/v1.10-phase73-sf-rich-editor-axe.spec.ts --project=chromium --reporter=line</automated>
  </verify>
  <acceptance_criteria>
    - `tests/v1.10-phase73-sf-rich-editor-axe.spec.ts` exists
    - File imports `AxeBuilder from "@axe-core/playwright"` (matching Phase 71/72 import path)
    - File contains a vacuous-green guard comment AND a `toBeVisible()` assertion on `[contenteditable="true"]` BEFORE every `.analyze()` call — `grep -c "toBeVisible" tests/v1.10-phase73-sf-rich-editor-axe.spec.ts` returns ≥ 3 (one per describe block / test that calls analyze())
    - File contains `expect(results.violations).toHaveLength(0)` at least 3 times (three fixture states scanned: empty, with-content, read-only)
    - File contains a structural assertion that `role="toolbar"` is present with ≥ 13 buttons with aria-label
    - File contains a structural assertion for `section[aria-label="Rich text editor"]` (region landmark)
    - `pnpm exec playwright test tests/v1.10-phase73-sf-rich-editor-axe.spec.ts --project=chromium --reporter=line` exits 0 (all tests pass, zero axe violations)
    - `git log -1 --format=%s -- tests/v1.10-phase73-sf-rich-editor-axe.spec.ts` matches `Feat\(73-03\):.*axe`
  </acceptance_criteria>
  <done>
    axe-core WCAG AA spec ships and passes. Vacuous-green guard prevents false-green on skeleton state. Three fixture states scanned. Zero violations. Toolbar role + button-name + region landmark + color-contrast all verified.
  </done>
</task>

<task type="auto">
  <name>Task 3: Bundle audit closeout + barrel non-export verification + Validation Sign-Off (RE-05 + DEP-02 closeout)</name>
  <files>.planning/phases/73-sfricheditor/73-VALIDATION.md</files>
  <read_first>
    - .planning/phases/73-sfricheditor/73-VALIDATION.md (current state — per-task rows as scaffold; Wave 0 checklist; nyquist_compliant: false)
    - components/sf/index.ts (confirm sf-rich-editor and sf-rich-editor-lazy absent from barrel — FINAL check)
    - next.config.ts (confirm @tiptap NOT in optimizePackageImports — FINAL check)
    - .planning/phases/73-sfricheditor/73-RESEARCH.md §P3 Lazy Pattern bundle verification script (lines 489–498)
    - .planning/phases/73-sfricheditor/73-RESEARCH.md §Validation Architecture RE-05 predicates (lines 804–812)
    - tests/v1.8-phase63-1-bundle-budget.spec.ts (200 KB hard constraint spec)
  </read_first>
  <action>
    This task produces no new source files — it runs the bundle audit, verifies barrel contracts, populates the VALIDATION.md sign-off, and commits the updated validation file.

    Step 1 — Barrel non-export verification (Pattern B final check):

        grep -c "sf-rich-editor\|SFRichEditor" components/sf/index.ts

    Expected: 0. If non-zero, STOP — Pattern B violation; trace which task accidentally added it.

        grep -c "SFRichEditorLazy\|sf-rich-editor-lazy" components/sf/index.ts

    Expected: 0.

    Step 2 — D-04 lock final check:

        grep -c "@tiptap" next.config.ts

    Expected: 0.

    Step 3 — Clean production build + bundle audit (FULL implementation, not placeholder):

        rm -rf .next/cache .next
        ANALYZE=true pnpm build 2>&1 | tee /tmp/73-03-bundle.log

    Wait for build to complete.

    Step 4 — Tiptap absent from homepage First Load chunk manifest:

        node -e "
        const fs = require('fs');
        let homeStr = '';
        try {
          homeStr = JSON.stringify(JSON.parse(fs.readFileSync('.next/build-manifest.json', 'utf8')).pages['/'] || []);
        } catch(e) {
          homeStr = JSON.stringify(JSON.parse(fs.readFileSync('.next/app-build-manifest.json', 'utf8')).pages['/page'] || []);
        }
        const tiptapLeak = /tiptap|prosemirror|starter.kit/.test(homeStr);
        console.log('Tiptap in homepage First Load:', tiptapLeak ? 'FAIL' : 'PASS');
        process.exit(tiptapLeak ? 1 : 0);
        "

    Expected: exit 0 + "Tiptap in homepage First Load: PASS".

    If FAIL: investigate — check if any barrel-reachable component accidentally imported from sf-rich-editor.tsx. The `grep -c "sf-rich-editor" components/sf/index.ts` = 0 from Step 1 should prevent this; if it still FAILs, run `ANALYZE=true pnpm build` and inspect the bundle analyzer HTML for the homepage chunk.

    Step 5 — Homepage First Load JS within 200 KB:

        pnpm exec playwright test tests/v1.8-phase63-1-bundle-budget.spec.ts --project=chromium --reporter=line

    Expected: PASS.

    Step 6 — Full Phase 73 test suite:

        pnpm exec playwright test tests/v1.10-phase73-sf-rich-editor.spec.ts tests/v1.10-phase73-sf-rich-editor-axe.spec.ts --project=chromium --reporter=line

    Expected: all tests pass.

    Step 7 — Populate VALIDATION.md per-task rows and set nyquist_compliant: true.

    Edit `.planning/phases/73-sfricheditor/73-VALIDATION.md`:

    (a) Update frontmatter: change `nyquist_compliant: false` to `nyquist_compliant: true`.

    (b) Replace the scaffold per-task row table with populated rows. Minimum rows to document (one per major predicate cluster, not one per micro-predicate):

        | Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
        |---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
        | 73-01-01 | 01 | 1 | DEP-02 | tiptap-pre-decision | _dep_re_01_decision 7 fields committed before pnpm add | grep | grep -c "decided:\|audit:\|dep_added:\|version:\|rationale:\|bundle_evidence:\|review_gate:" components/sf/sf-rich-editor.tsx | ✅ | ✅ green |
        | 73-01-02 | 01 | 1 | DEP-02 | bundle-evidence-not-estimated | bundle_evidence contains "Measurement command" literal + "PASS" for tiptap-absent check | grep | grep -q "Measurement command: rm -rf .next/cache .next" components/sf/sf-rich-editor.tsx | ✅ | ✅ green |
        | 73-01-03 | 01 | 1 | RE-04 | injectCSS-leak | @layer signalframeux block with ≥4 .ProseMirror rules in globals.css | grep | awk '/@layer signalframeux/,/^}/' app/globals.css \| grep -c "ProseMirror" | ✅ | ✅ green |
        | 73-01-04 | 01 | 1 | RE-04 | tiptap-css-import | No Tiptap/ProseMirror CSS file imported | grep | grep -r "tiptap.*\.css\|prosemirror.*\.css" app/ components/ lib/ (0 matches) | ✅ | ✅ green |
        | 73-02-01 | 02 | 2 | RE-04 | ssr-crash | immediatelyRender: false on every useEditor() | grep | grep -c "immediatelyRender: false" components/sf/sf-rich-editor.tsx | ✅ | ✅ green |
        | 73-02-02 | 02 | 2 | RE-04 | injectCSS-leak | injectCSS: false on every useEditor() | grep | grep -c "injectCSS: false" components/sf/sf-rich-editor.tsx | ✅ | ✅ green |
        | 73-02-03 | 02 | 2 | RE-03 | controlled-loop-bug | editor.getHTML() === value loop guard present | grep | grep -q "editor.getHTML() === value" components/sf/sf-rich-editor.tsx | ✅ | ✅ green |
        | 73-02-04 | 02 | 2 | RE-06 | anti-feature-undoc | Anti-features NOT shipped JSDoc block present | grep | grep -q "Anti-features NOT shipped" components/sf/sf-rich-editor.tsx | ✅ | ✅ green |
        | 73-02-05 | 02 | 2 | RE-05 | tiptap-leak-via-barrel | SFRichEditor absent from barrel | grep | grep -c "sf-rich-editor" components/sf/index.ts (0) | ✅ | ✅ green |
        | 73-02-06 | 02 | 2 | RE-05 | ssr-crash | ssr: false on lazy wrapper | grep | grep -c "ssr: false" components/sf/sf-rich-editor-lazy.tsx | ✅ | ✅ green |
        | 73-03-01 | 03 | 3 | TST-03 | vacuous-green | [contenteditable="true"] toBeVisible before axe.analyze() | playwright+axe | pnpm exec playwright test tests/v1.10-phase73-sf-rich-editor-axe.spec.ts | ✅ | ✅ green |
        | 73-03-02 | 03 | 3 | TST-03 | toolbar-a11y | zero axe violations on toolbar + contenteditable | playwright+axe | pnpm exec playwright test tests/v1.10-phase73-sf-rich-editor-axe.spec.ts | ✅ | ✅ green |
        | 73-03-03 | 03 | 3 | RE-05 | tiptap-leak-via-barrel | @tiptap/* absent from homepage First Load chunk manifest | shell+node | node bundle-leak-check script (see task) | ✅ | ✅ green |
        | 73-03-04 | 03 | 3 | RE-05 | budget-exceeded | homepage First Load JS ≤ 200 KB | playwright | pnpm exec playwright test tests/v1.8-phase63-1-bundle-budget.spec.ts | ✅ | ✅ green |

    (c) Check all Wave 0 items in the Wave 0 Requirements checklist:

        - [x] tests/v1.10-phase73-sf-rich-editor.spec.ts
        - [x] tests/v1.10-phase73-sf-rich-editor-axe.spec.ts
        - [x] app/dev-playground/sf-rich-editor/page.tsx
        - [x] _dep_re_01_decision block authored BEFORE pnpm add
        - [x] No new framework installs

    (d) Set Validation Sign-Off checkboxes to checked:

        - [x] All tasks have `<automated>` verify or Wave 0 dependencies
        - [x] Sampling continuity: no 3 consecutive tasks without automated verify
        - [x] Wave 0 covers all MISSING references
        - [x] No watch-mode flags
        - [x] Feedback latency < 30s
        - [x] `nyquist_compliant: true` set in frontmatter

    Commit:

        git add .planning/phases/73-sfricheditor/73-VALIDATION.md
        git commit -m "Chore(73-03): bundle audit PASS + VALIDATION.md sign-off — nyquist_compliant=true (Phase 73 closeout)"
  </action>
  <verify>
    <automated>grep -c "sf-rich-editor\|SFRichEditor" components/sf/index.ts | grep -q "^0$" && grep -c "@tiptap" next.config.ts | grep -q "^0$" && pnpm exec playwright test tests/v1.10-phase73-sf-rich-editor.spec.ts tests/v1.10-phase73-sf-rich-editor-axe.spec.ts --project=chromium --reporter=line && pnpm exec playwright test tests/v1.8-phase63-1-bundle-budget.spec.ts --project=chromium --reporter=line && grep -q "nyquist_compliant: true" .planning/phases/73-sfricheditor/73-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "sf-rich-editor\|SFRichEditor" components/sf/index.ts` returns `0` (barrel non-export final check — Pattern B preserved)
    - `grep -c "@tiptap" next.config.ts` returns `0` (D-04 lock final check — no optimizePackageImports addition)
    - Bundle audit node script exits 0: `Tiptap in homepage First Load: PASS` (full impl with real @tiptap/* imports — Tiptap stayed in lazy chunk, not homepage)
    - `pnpm exec playwright test tests/v1.10-phase73-sf-rich-editor.spec.ts --project=chromium` exits 0 (all acceptance tests pass)
    - `pnpm exec playwright test tests/v1.10-phase73-sf-rich-editor-axe.spec.ts --project=chromium` exits 0 (all axe tests pass, zero violations)
    - `pnpm exec playwright test tests/v1.8-phase63-1-bundle-budget.spec.ts --project=chromium` exits 0 (homepage ≤ 200 KB)
    - `.planning/phases/73-sfricheditor/73-VALIDATION.md` frontmatter contains `nyquist_compliant: true`
    - VALIDATION.md per-task rows table is populated (not the scaffold placeholder row) — `grep -c "73-0[123]-0[1-9]" .planning/phases/73-sfricheditor/73-VALIDATION.md` returns ≥ 10
    - VALIDATION.md Wave 0 items all checked — `grep -c "\[x\]" .planning/phases/73-sfricheditor/73-VALIDATION.md` returns ≥ 10
    - `git log -1 --format=%s -- .planning/phases/73-sfricheditor/73-VALIDATION.md` matches `Chore\(73-03\):.*bundle audit.*VALIDATION`
  </acceptance_criteria>
  <done>
    Phase 73 evidence wave complete. Bundle audit PASS with full Tiptap implementation (not placeholder). Barrel non-export, D-04 lock, Pattern B all hold. Full test suite green. VALIDATION.md sign-off complete. Phase 73 SFRichEditor is ship-ready.
  </done>
</task>

</tasks>

<verification>
End-to-end verification of Plan 03 (and Phase 73 closeout):

(1) Full Phase 73 test suite:

    pnpm exec playwright test tests/v1.10-phase73-sf-rich-editor.spec.ts tests/v1.10-phase73-sf-rich-editor-axe.spec.ts --project=chromium --reporter=line

Expected: all tests pass (≥ 12 tests green).

(2) Bundle budget:

    pnpm exec playwright test tests/v1.8-phase63-1-bundle-budget.spec.ts --project=chromium

Expected: PASS (≤ 200 KB homepage First Load JS).

(3) Tiptap absent from homepage chunk (full impl — not placeholder):

    node -e "
    const fs = require('fs');
    let homeStr = '';
    try {
      homeStr = JSON.stringify(JSON.parse(fs.readFileSync('.next/build-manifest.json', 'utf8')).pages['/'] || []);
    } catch(e) {
      homeStr = JSON.stringify(JSON.parse(fs.readFileSync('.next/app-build-manifest.json', 'utf8')).pages['/page'] || []);
    }
    const tiptapLeak = /tiptap|prosemirror|starter.kit/.test(homeStr);
    console.log('Tiptap in homepage First Load:', tiptapLeak ? 'FAIL' : 'PASS');
    process.exit(tiptapLeak ? 1 : 0);
    "

Expected: exit 0, "PASS".

(4) Pattern B final:

    grep -c "sf-rich-editor\|SFRichEditor\|SFRichEditorLazy" components/sf/index.ts

Expected: 0.

(5) D-04 lock final:

    grep -c "@tiptap" next.config.ts

Expected: 0.

(6) RE-06 anti-features absent from impl:

    grep -c "level: [456]" components/sf/sf-rich-editor.tsx
    grep -c "BubbleMenu\|FloatingMenu" components/sf/sf-rich-editor.tsx
    grep -c "FontFamily\|TextStyle" components/sf/sf-rich-editor.tsx

Expected: 0, 0, 0.

(7) Nyquist compliant:

    grep -q "nyquist_compliant: true" .planning/phases/73-sfricheditor/73-VALIDATION.md

Expected: exit 0.

(8) Worktree clean:

    git status --porcelain

Expected: clean.
</verification>

<success_criteria>
- [ ] `tests/v1.10-phase73-sf-rich-editor.spec.ts` PASSES: toolbar, controlled API, readOnly, keyboard nav (Escape focus-return, Link prompt dialog)
- [ ] `tests/v1.10-phase73-sf-rich-editor-axe.spec.ts` PASSES: zero axe violations across 3 fixture states; vacuous-green guard prevents false-green
- [ ] Bundle audit node script exits 0: `Tiptap in homepage First Load: PASS` (full impl)
- [ ] `tests/v1.8-phase63-1-bundle-budget.spec.ts` PASSES: homepage ≤ 200 KB
- [ ] `grep -c "sf-rich-editor" components/sf/index.ts` returns `0` (Pattern B preserved)
- [ ] `grep -c "@tiptap" next.config.ts` returns `0` (D-04 lock preserved)
- [ ] RE-06 anti-features absent from implementation code (H4-H6, FontFamily, BubbleMenu/FloatingMenu — all 0 matches)
- [ ] `73-VALIDATION.md` has `nyquist_compliant: true` and populated per-task rows (≥ 10 rows)
- [ ] Worktree clean; all commits follow `Feat/Chore(73-03):` prefix convention
</success_criteria>

<output>
After completion, create `.planning/phases/73-sfricheditor/73-03-SUMMARY.md` documenting:
- All test counts and results
- Bundle audit evidence (homepage KB after full impl; "PASS" for tiptap-absent check)
- Confirmation that all 8 requirements (RE-01..06, DEP-02, TST-03) are satisfied
- The commit SHAs for Plan 03
- Forward link: Phase 73 complete → Phase 74 (SFFileUpload) per ROADMAP
</output>
