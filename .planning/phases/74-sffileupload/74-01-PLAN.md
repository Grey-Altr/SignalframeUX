---
phase: 74-sffileupload
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/sf/sf-file-upload.tsx
  - components/sf/index.ts
  - app/dev-playground/sf-file-upload/page.tsx
  - public/r/sf-file-upload.json
  - public/r/registry.json
autonomous: true
requirements:
  - FU-01
  - FU-02
  - FU-03
  - FU-04
gap_closure: false
must_haves:
  truths:
    - "A consumer renders SFFileUpload and can select files via click-to-browse — clicking the drop zone (or pressing Enter/Space when focused) opens the OS file dialog via the hidden <input type=\"file\"> click() programmatic trigger; selected files appear in a list with per-file remove × affordance"
    - "A consumer renders SFFileUpload with multiple={true}, and selecting multiple files in the OS dialog populates the file list with one row per file; each file independently passes/fails MIME (accept) + size (maxSize) validation and surfaces a per-file error message inline"
    - "A consumer renders SFFileUpload with progress={ 'photo.jpg': 47 } and the per-file row for photo.jpg renders an SFProgress with value=47 and aria-label='Upload progress for photo.jpg'; rows for files with no progress entry render no SFProgress element"
    - "A consumer renders SFFileUpload with an image file in the accepted state and a preview <img> appears with src set to a blob: URL produced via URL.createObjectURL; on file removal OR component unmount the URL is revoked via URL.revokeObjectURL inside a useEffect cleanup (StrictMode-safe)"
    - "A consumer renders SFFileUpload as a controlled component via files={...}+onChange={...} or uncontrolled (no files prop); the discriminated state machine maintains a single source of truth for SFFileEntry[]; onChange fires on add, remove, and validation completion"
    - "Drop zone wrapper has role=\"button\", tabIndex=0 (or -1 when disabled), aria-label describing both keyboard + drag flows, and visible focus-visible ring via sf-focusable; hidden <input type=\"file\"> is the keyboard fallback per WCAG 2.5.1 — Enter/Space on the wrapper calls inputRef.current.click()"
    - "onDragOver and onDrop handlers call e.preventDefault() to suppress browser default file-navigation; onDragLeave clears isDragging visual state; onPaste reads e.clipboardData.files and routes through the same handleFiles() function as drop+click"
    - "SFFileUpload is exported from components/sf/index.ts barrel (Pattern C — zero runtime deps; safe for barrel inclusion); SFFileEntry type also exported"
    - "Component contains zero fetch(), XMLHttpRequest, or axios calls — consumer owns the HTTP upload entirely (FU-04); JSDoc anti-features block enumerates the rejected behaviors verbatim"
    - "Component contains zero FileReader.readAsDataURL calls — only URL.createObjectURL is used for image preview (memory-safe for large files per FU-03)"
    - "When disabled={true}, the hidden <input type=\"file\"> ALSO receives the disabled attribute to prevent OS-level AT triggering; drop zone wrapper sets tabIndex=-1 and pointer-events:none"
    - "A registry entry for sf-file-upload lands in public/r/registry.json AND public/r/sf-file-upload.json same-commit as the component file (REG-01 same-commit rule for Pattern C); meta.layer=\"frame\", meta.pattern=\"C\""
    - "Playground fixture at app/dev-playground/sf-file-upload/page.tsx mounts ≥4 sections (uncontrolled default + accept image, controlled files+onChange echo, multi-file with simulated progress slider, error fixture with maxSize=1KB) and is consumable by Plan 02's Playwright + axe specs"
  artifacts:
    - path: "components/sf/sf-file-upload.tsx"
      provides: "Pattern C SFFileUpload component — drop zone (role=button) + hidden <input type=file> keyboard fallback + file list + per-file remove + MIME/size validation + URL.createObjectURL preview with revoke cleanup + SFProgress per-file rendering driven by progress prop + clipboard paste handler + controlled/uncontrolled API; zero runtime deps; zero HTTP; zero FileReader; comprehensive JSDoc anti-features block"
      exports: ["SFFileUpload", "type SFFileEntry", "type SFFileUploadProps"]
      min_lines: 280
    - path: "components/sf/index.ts"
      provides: "Barrel export for SFFileUpload (Pattern C)"
      contains: "export { SFFileUpload, type SFFileEntry"
    - path: "app/dev-playground/sf-file-upload/page.tsx"
      provides: "Playwright + axe + Storybook fixture page — ≥4 sections covering uncontrolled default+accept-image, controlled files+onChange echo, multi-file with progress slider, maxSize=1KB error fixture; testid anchors prefixed fixture-{section}"
      exports: ["default"]
      min_lines: 120
    - path: "public/r/sf-file-upload.json"
      provides: "Standalone registry-item file (mirrors sf-combobox.json shape) — meta.layer=frame, meta.pattern=C, registryDependencies=[\"progress\"]"
      contains: "\"name\": \"sf-file-upload\""
    - path: "public/r/registry.json"
      provides: "New items[] entry for sf-file-upload — same-commit cohort with component file"
      contains: "\"name\": \"sf-file-upload\""
  key_links:
    - from: "components/sf/sf-file-upload.tsx"
      to: "components/sf/sf-progress.tsx"
      via: "barrel import — SFProgress drives per-file progress rendering"
      pattern: "from ['\"]@/components/sf['\"]"
    - from: "components/sf/sf-file-upload.tsx"
      to: "browser native File API"
      via: "URL.createObjectURL + URL.revokeObjectURL + useEffect cleanup"
      pattern: "URL\\.(createObjectURL|revokeObjectURL)"
    - from: "components/sf/index.ts"
      to: "components/sf/sf-file-upload.tsx"
      via: "barrel export — Pattern C; FU-04 acceptance"
      pattern: "export \\{ SFFileUpload"
    - from: "app/dev-playground/sf-file-upload/page.tsx"
      to: "components/sf/sf-file-upload.tsx"
      via: "barrel import — fixture mounts SFFileUpload from @/components/sf"
      pattern: "from ['\"]@/components/sf['\"]"
    - from: "package.json dependencies"
      to: "ABSENCE of new deps"
      via: "FU-04 zero-dep contract; no react-dropzone, react-dnd, or similar"
      pattern: "git diff HEAD -- package.json | grep -cE '^\\+.*\"' (= 0 new prod deps)"
---

<objective>
Implement SFFileUpload as a Pattern C composition — drop zone with role="button" + hidden `<input type="file">` keyboard fallback + file list + per-file remove + MIME/size validation + multi-file support + clipboard paste + controlled/uncontrolled API + image preview via `URL.createObjectURL` with `useEffect` revoke cleanup + per-file `SFProgress` rendering driven by a consumer-supplied `progress: Record<fileName, number>` prop. Zero new runtime npm deps. Zero HTTP inside the component (consumer owns the upload). Zero `FileReader.readAsDataURL` (memory-safe for large files). Export from `sf/index.ts` barrel and add same-commit registry entries (`public/r/sf-file-upload.json` standalone + new `items[]` entry in `public/r/registry.json`). Author the playground fixture at `app/dev-playground/sf-file-upload/page.tsx` so Plan 02's Playwright + axe specs and the Storybook drag-state story (Plan 02) can mount it.

Purpose: Deliver FU-01 (drop zone + click-to-browse + file list + per-file remove + clipboard paste), FU-02 (MIME validation via accept prop, size validation via maxSize prop, multi-file via multiple prop, per-file error state), FU-03 (per-file SFProgress rendering driven by progress prop, controlled+uncontrolled API, disabled state, image preview via URL.createObjectURL — NEVER FileReader), and FU-04 (Pattern C barrel export + zero runtime deps + zero HTTP). FU-05 (split test strategy + dataTransfer.files gap documentation in 74-VERIFICATION.md), TST-03 (Playwright + axe specs), and TST-04 (split-test strategy doc in 74-VERIFICATION.md) ship in Plan 02.

Output: 5 files modified — new `components/sf/sf-file-upload.tsx` ships the SFFileUpload component, `components/sf/index.ts` adds barrel export, new `app/dev-playground/sf-file-upload/page.tsx` ships the fixture page, new `public/r/sf-file-upload.json` and the items array in `public/r/registry.json` gain registry entries.
</objective>

<execution_context>
@/Users/greyaltaer/.claude/pde-os/engines/gsd/workflows/execute-plan.md
@/Users/greyaltaer/.claude/pde-os/engines/gsd/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/74-sffileupload/74-RESEARCH.md
@.planning/phases/74-sffileupload/74-VALIDATION.md
@CLAUDE.md
@.planning/LOCKDOWN.md
@components/sf/index.ts
@components/sf/sf-progress.tsx
@components/sf/sf-input.tsx
@components/sf/sf-combobox.tsx
@components/sf/sf-button.tsx
@public/r/registry.json
@public/r/sf-combobox.json

<interfaces>
<!-- SFProgress API (verified at components/sf/sf-progress.tsx:26-29) -->

interface SFProgressProps extends React.ComponentProps<typeof ProgressPrimitive.Root> {
  value?: number;  // 0-100
}

// Renders <div role="progressbar" aria-valuenow={value} aria-valuemin=0 aria-valuemax=100>.
// aria-label MUST be passed (axe `progressbar` rule requires accessible name).
// Default height h-1 (4px); override via className.
// rounded-none enforced internally on Root + Indicator.

import { SFProgress } from "@/components/sf";

<SFProgress
  value={progress[entry.file.name] ?? 0}
  aria-label={`Upload progress for ${entry.file.name}`}
  className="h-1 mt-[var(--sfx-space-1)]"
/>

<!-- Native File API surface (research §Native File API Surface) -->

interface File extends Blob {
  readonly name: string;        // filename including extension
  readonly lastModified: number;
  readonly size: number;        // bytes
  readonly type: string;        // MIME (may be empty string for unknown types — validate with extension fallback)
}

// FileList → File[]:  Array.from(input.files)  OR  [...input.files]

<!-- URL.createObjectURL lifecycle (research §URL.createObjectURL Lifecycle) -->

useEffect(() => {
  // Build map of preview URLs for accepted image files
  const urls: Record<string, string> = {};
  for (const f of files) {
    if (f.accepted && /^image\//.test(f.file.type)) {
      urls[f.key] = URL.createObjectURL(f.file);
    }
  }
  setPreviewUrls(urls);

  return () => {
    // Cleanup: revoke ALL created URLs on next-effect-run OR unmount.
    // StrictMode double-mount: first effect runs → cleanup runs → effect runs
    // again with fresh URLs. Both URLs are valid; second one supersedes first.
    for (const url of Object.values(urls)) {
      URL.revokeObjectURL(url);
    }
  };
}, [files]);

<!-- Drop zone wrapper structure (research §Drag-and-Drop Accessibility Pattern) -->

<div
  role="button"
  tabIndex={disabled ? -1 : 0}
  aria-label="Upload files. Press Enter or Space to browse, or drag and drop files here."
  aria-disabled={disabled || undefined}
  onClick={() => !disabled && hiddenInputRef.current?.click()}
  onKeyDown={(e) => {
    if (disabled) return;
    if (e.key === "Enter" || e.key === " ") {
      e.preventDefault();      // CRITICAL: prevents Lenis Space-advance per LOCKDOWN R-64-d
      hiddenInputRef.current?.click();
    }
  }}
  onDragOver={(e) => { e.preventDefault(); if (!disabled) setIsDragging(true); }}
  onDragEnter={(e) => { e.preventDefault(); if (!disabled) setIsDragging(true); }}
  onDragLeave={(e) => { setIsDragging(false); }}
  onDrop={(e) => {
    e.preventDefault();
    setIsDragging(false);
    if (disabled) return;
    handleFiles(Array.from(e.dataTransfer.files));
  }}
  onPaste={(e) => {
    if (disabled) return;
    const pasted = Array.from(e.clipboardData.files);
    if (pasted.length > 0) handleFiles(pasted);
  }}
  className={cn(
    "sf-focusable",                         // double-ring focus per LOCKDOWN R-64-b
    "border-2 border-foreground bg-muted",  // R-60-e: bg-muted survives borderless mode (border zeros, surface remains)
    "p-[var(--sfx-space-6)] text-center",   // 24 px (blessed)
    "cursor-pointer",
    isDragging && "bg-foreground/10",       // tonal step up from bg-muted for drag-active state
    disabled && "opacity-50 pointer-events-none"
  )}
>
  {/* visible label / icon / hint copy */}
  <input
    ref={hiddenInputRef}
    type="file"
    accept={accept}
    multiple={multiple}
    disabled={disabled}                     // FU-04 cascade: disabled propagates to hidden input
    onChange={(e) => {
      handleFiles(Array.from(e.target.files ?? []));
      e.target.value = "";                  // allow same-file re-select
    }}
    style={{ display: "none" }}
  />
</div>

<!-- File list (role=list with role=listitem rows) -->

<ul role="list" aria-label="Selected files" className="mt-[var(--sfx-space-3)] divide-y-2 divide-foreground">
  {files.map((entry) => (
    <li
      key={entry.key}
      role="listitem"
      className="flex items-start gap-[var(--sfx-space-3)] p-[var(--sfx-space-3)]"
    >
      {/* image preview if entry.accepted && image MIME && previewUrls[entry.key] */}
      {/* filename + size + error (if any) — RENDERED AS REACT TEXT NODES; never raw-HTML */}
      {/* SFProgress when progress[entry.file.name] !== undefined */}
      <button
        type="button"
        aria-label={`Remove ${entry.file.name}`}
        onClick={() => handleRemove(entry.key)}
        className="sf-focusable px-[var(--sfx-space-2)] py-[var(--sfx-space-1)] font-mono uppercase tracking-wider text-xs"
      >
        ×
      </button>
    </li>
  ))}
</ul>

<!-- aria-live region for screen reader announcements -->

<div aria-live="polite" aria-atomic="true" className="sr-only">
  {liveAnnouncement}
</div>

// liveAnnouncement is set after each handleFiles / handleRemove operation,
// e.g. "3 files selected", "photo.jpg removed", "image.gif rejected: file too large"

<!-- Validation helpers (research §Validation Strategy) -->

function makeFileKey(file: File): string {
  return `${file.name}__${file.size}__${file.lastModified}`;
}

function validateMime(file: File, accept?: string): boolean {
  if (!accept) return true;
  const parts = accept.split(",").map((s) => s.trim());
  return parts.some((p) => {
    if (p.startsWith(".")) return file.name.toLowerCase().endsWith(p.toLowerCase());
    if (p.endsWith("/*")) return file.type.startsWith(p.slice(0, -1));
    return file.type === p;
  });
}

function validateSize(file: File, maxSize?: number): boolean {
  if (maxSize === undefined) return true;
  return file.size <= maxSize;
}

<!-- SFFileEntry + props -->

export type SFFileEntry = {
  key: string;            // makeFileKey output — stable React key + identity
  file: File;             // native File reference (consumer reads .name, .size, .type for upload)
  accepted: boolean;      // passed both MIME and size validation
  error?: string;         // human-readable rejection reason (e.g. "File type not allowed", "File too large (5 MB > 1 MB)")
};

export interface SFFileUploadProps {
  files?: SFFileEntry[];                                  // controlled
  onChange?: (files: SFFileEntry[]) => void;              // fires on add / remove / validate

  accept?: string;                                        // MIME or extension list, e.g. "image/*,.pdf"
  maxSize?: number;                                       // per-file size limit in BYTES
  multiple?: boolean;                                     // multi-file mode

  progress?: Record<string, number>;                      // KEY: file.name (NOT entry.key) — collision documented in JSDoc

  disabled?: boolean;
  className?: string;

  /** Optional accessible label for the drop zone wrapper. Defaults to a copy describing both keyboard + drag flows. */
  ariaLabel?: string;
}
</interfaces>

<threat_model>
ASVS L1 — Phase 74 Plan 01 threat surface:

| Threat ID | Threat | Severity | Mitigation |
|-----------|--------|----------|------------|
| T-74-01 | MIME spoofing — `evil.exe` renamed to `evil.png` passes `accept="image/*"` because browser sets `file.type="image/png"` from extension | LOW | `accept` and `File.type` are CLIENT HINTS for UX only — server-side validation is consumer's responsibility. JSDoc anti-features block states verbatim: "Server-side file-type validation is OUT OF SCOPE — `accept`+`File.type` are browser hints; consumers MUST validate uploaded bytes server-side (magic-number sniffing, AV scan)." |
| T-74-02 | Memory leak — `URL.createObjectURL` not revoked → blob references accumulate in browser memory | MEDIUM | `useEffect` cleanup revokes ALL preview URLs on next-effect-run OR component unmount; StrictMode double-mount safe (cleanup fires between mounts). Acceptance criterion: `grep -c 'URL.revokeObjectURL' components/sf/sf-file-upload.tsx` ≥ 1 AND `grep -c 'return () =>' components/sf/sf-file-upload.tsx` ≥ 1 (cleanup function returned from useEffect) |
| T-74-03 | DoS via 1000-file selection — multi-mode + no count limit lets the user freeze the renderer with a huge FileList | LOW | Per-file `maxSize` is the only built-in defense; consumer imposes total-count limit at form level (FU-06 deferred). JSDoc anti-features block: "Aggregate file count limit (`maxFiles`) is FU-06 deferred — consumer enforces at form level." |
| T-74-04 | XSS via File.name — `<script>alert(1)</script>.png` rendered into the file list | LOW | React auto-escapes string children; verify `entry.file.name` is rendered as a text node only. Acceptance criterion: source contains zero raw-HTML inject prop usages — `grep -c 'dangerously' components/sf/sf-file-upload.tsx` = 0 |
| T-74-05 | Browser default drop behavior — dropping outside the drop zone (or without preventDefault) navigates browser to the file as a download | HIGH | `e.preventDefault()` MANDATORY on `onDragOver` AND `onDrop`. Acceptance criterion: `grep -cE 'preventDefault' components/sf/sf-file-upload.tsx` ≥ 2 (one for dragover, one for drop, plus optional dragenter) |
| T-74-06 | A11y bypass — keyboard trap or SR users blocked from drop zone; borderless-mode surface contrast collapse (LOCKDOWN R-60-d zeros border-width, leaving an invisible drop zone if surface is `bg-background` on `bg-background` page) | MEDIUM | Hidden `<input type="file">` IS the keyboard fallback (WCAG 2.5.1); `aria-live` region announces selection/removal/error; double-ring focus via `sf-focusable` (R-64-b); **drop zone surface is `bg-muted` (per R-60-e — structural separation via surface color, not via `border-width` which zeros under borderless mode)**; axe spec in Plan 02 covers `button-name`, `progressbar`, `aria-valid-attr-value`, `color-contrast` |
| T-74-07 | Worktree leakage (memory: feedback_agent_worktree_leakage): file authored in `.claude/worktrees/agent-N/` leaks to main tree as untracked, then committed by mistake | MEDIUM | Pre-task `git status --porcelain` check; explicit acceptance criterion verifies file was authored in this plan and not pre-existing |
| T-74-08 | Bundle bloat: a third-party drag-drop library (`react-dropzone`, `react-dnd`) sneaks into deps | HIGH | FU-04 zero-dep contract. Acceptance criterion: `git diff HEAD -- package.json` shows zero additions to `dependencies` block. JSDoc anti-features block enumerates rejected libraries verbatim. |
| T-74-09 | `disabled` not propagated to hidden input — OS-level AT can still trigger file dialog | MEDIUM | When `disabled={true}`, hidden `<input type="file">` ALSO receives `disabled` attribute. Acceptance criterion: source contains literal `disabled={disabled}` on the hidden input. |
| T-74-10 | Registry drift: registry.json items[] entry not added same-commit as sf-file-upload.tsx — REG-01 same-commit rule violated | MEDIUM | Single-commit-cohort acceptance criterion: `git show <commit> --stat` includes BOTH `components/sf/sf-file-upload.tsx` AND `public/r/registry.json` AND `public/r/sf-file-upload.json` |

No SSR-time secret/token surface. Component is `'use client'`. No fetch/XHR — consumer owns network. No raw-HTML user content threading; filenames + error messages render via React's default text-node escaping only.
</threat_model>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Implement SFFileUpload component (FU-01 + FU-02 + FU-03 + FU-04)</name>
  <files>components/sf/sf-file-upload.tsx</files>
  <read_first>
    - components/sf/sf-file-upload.tsx (verify file does NOT yet exist via `ls` — if it does, STOP, worktree leakage suspect)
    - components/sf/sf-progress.tsx (SFProgress API: `value?: number` 0-100; Radix Progress wrapper; renders role=progressbar; `aria-label` MUST be passed for axe `progressbar` rule)
    - components/sf/sf-input.tsx (`sf-focusable sf-border-draw-focus` pattern reference; font-mono uppercase tracking-wider register)
    - components/sf/sf-combobox.tsx (Pattern C reference — controlled/uncontrolled discriminated state, JSDoc anti-pattern block, barrel export contract)
    - components/sf/sf-button.tsx (intent="ghost" size="sm" pattern for any internal buttons)
    - components/sf/index.ts (verify SFProgress exported at line 133; SFFileUpload slot pending)
    - .planning/phases/74-sffileupload/74-RESEARCH.md (entire file — Native File API surface, URL.createObjectURL lifecycle, drag-drop accessibility pattern, validation strategy, controlled API, SFProgress integration)
    - .planning/phases/74-sffileupload/74-VALIDATION.md (per-task verification map rows 74-01-01..14 — exact predicate commands)
    - CLAUDE.md (blessed spacing 4/8/12/16/24/32/48/64/96; zero border-radius; OKLCH only; FRAME/SIGNAL register; structural copy)
    - .planning/LOCKDOWN.md §2.5 (R-60 borderless-first); §6.5 (R-61 rAF no-layout-reads — n/a here, no rAF); §9 (R-64-b double-ring focus, R-64-d Space-key focus guard for role=button)
  </read_first>
  <action>
    Run `git status --porcelain components/sf/sf-file-upload.tsx` first. If it returns ANY output, STOP and surface a worktree-leakage warning — file MUST be authored fresh in-phase.

    Create `components/sf/sf-file-upload.tsx`. The file MUST satisfy ALL acceptance criteria below.

    File structure (top to bottom):

    1. `"use client";` directive on line 1.

    2. JSDoc block immediately above the type definitions:

        /**
         * File upload composition — FRAME layer Pattern C primitive.
         *
         * Drop zone with role="button" + hidden <input type="file"> keyboard
         * fallback (WCAG 2.5.1) + file list with per-file remove + per-file
         * progress via SFProgress + image preview via URL.createObjectURL +
         * controlled/uncontrolled API. Zero new runtime deps. Consumer owns
         * the HTTP upload entirely — component owns UI state only.
         *
         * ============================================================
         * ANTI-FEATURES (NOT shipped — by design, not by oversight)
         * ============================================================
         *
         * 1. HTTP upload (fetch / XMLHttpRequest / axios) — Consumer owns
         *    the network call. Component emits files via onChange and
         *    renders consumer-supplied `progress: Record<fileName, number>`
         *    via SFProgress. Embedding fetch violates FU-04 zero-dep +
         *    single-responsibility (UI state only, not network state).
         *
         * 2. FileReader.readAsDataURL — Converts entire file to a base64
         *    string in JS heap (~13 MB string for a 10 MB image). Use
         *    URL.createObjectURL — O(1), references browser's in-memory
         *    blob without copying.
         *
         * 3. Aggregate maxTotalSize — FU-06 deferred. Consumer enforces
         *    at form level. Per-file `maxSize` only.
         *
         * 4. Retry on failure — FU-06 deferred. Consumer owns upload
         *    state machine including retry policy.
         *
         * 5. Drop-anywhere mode (window-level drop handler) — FU-06
         *    deferred. Drop is scoped to the drop zone wrapper only.
         *
         * 6. react-dropzone, react-dnd, or any third-party drag-drop
         *    library — FU-04 zero-dep contract. Native HTML5 drag-drop
         *    + <input type="file"> is ~60 LOC; no abstraction earns a
         *    new dependency.
         *
         * 7. Server-side file-type validation — `accept` and `File.type`
         *    are BROWSER HINTS for UX only. A renamed `evil.exe` →
         *    `evil.png` will pass MIME validation. Consumers MUST
         *    validate uploaded bytes server-side (magic-number sniffing,
         *    AV scan, content inspection).
         *
         * ============================================================
         * KNOWN CAVEATS
         * ============================================================
         *
         * - `progress` keying collision: two files named `photo.jpg`
         *   from different directories share a progress entry. The
         *   `progress` prop is keyed by `file.name` per FU-03 contract;
         *   consumers needing per-instance progress should de-duplicate
         *   filenames before passing files to the component.
         *
         * - `dataTransfer.files` cannot be set from JS in Chromium for
         *   security reasons (crbug.com/531834, microsoft/playwright#13362).
         *   This means automated tests cannot E2E-verify the drag→drop
         *   acceptance path — see 74-VERIFICATION.md split-test strategy.
         *   The visual drag-active state IS testable via Storybook play()
         *   + fireEvent.dragOver().
         *
         * @example  // uncontrolled
         * <SFFileUpload
         *   accept="image/*,.pdf"
         *   maxSize={5 * 1024 * 1024}
         *   multiple
         *   onChange={(files) => console.log(files)}
         * />
         *
         * @example  // controlled with progress
         * <SFFileUpload
         *   files={files}
         *   onChange={setFiles}
         *   progress={{ "photo.jpg": 47 }}
         * />
         */

    3. Imports (NO new runtime npm deps; SFProgress via barrel; cn from lib/utils):

        import { useEffect, useRef, useState, useCallback, useMemo } from "react";
        import { SFProgress } from "@/components/sf";
        import { cn } from "@/lib/utils";

    4. Public types — exported (verbatim shape from <interfaces> block):

        export type SFFileEntry = {
          key: string;
          file: File;
          accepted: boolean;
          error?: string;
        };

        export interface SFFileUploadProps {
          files?: SFFileEntry[];
          onChange?: (files: SFFileEntry[]) => void;
          accept?: string;
          maxSize?: number;
          multiple?: boolean;
          progress?: Record<string, number>;
          disabled?: boolean;
          className?: string;
          ariaLabel?: string;
        }

    5. Module-level helper functions (BEFORE the component function):

        function makeFileKey(file: File): string {
          return `${file.name}__${file.size}__${file.lastModified}`;
        }

        function validateMime(file: File, accept?: string): boolean {
          if (!accept) return true;
          const parts = accept.split(",").map((s) => s.trim()).filter(Boolean);
          if (parts.length === 0) return true;
          return parts.some((p) => {
            if (p.startsWith(".")) return file.name.toLowerCase().endsWith(p.toLowerCase());
            if (p.endsWith("/*")) return file.type.startsWith(p.slice(0, -1));
            return file.type === p;
          });
        }

        function validateSize(file: File, maxSize?: number): boolean {
          if (maxSize === undefined) return true;
          return file.size <= maxSize;
        }

        function formatBytes(bytes: number): string {
          if (bytes < 1024) return `${bytes} B`;
          if (bytes < 1024 * 1024) return `${(bytes / 1024).toFixed(1)} KB`;
          return `${(bytes / 1024 / 1024).toFixed(1)} MB`;
        }

        function isImageMime(type: string): boolean {
          return type.startsWith("image/");
        }

    6. Component function — controlled/uncontrolled state, drop zone, file list, preview URL lifecycle:

        export function SFFileUpload(props: SFFileUploadProps) {
          const {
            files: filesProp,
            onChange,
            accept,
            maxSize,
            multiple,
            progress,
            disabled,
            className,
            ariaLabel,
          } = props;

          const isControlled = filesProp !== undefined;
          const [internalFiles, setInternalFiles] = useState<SFFileEntry[]>([]);
          const files: SFFileEntry[] = isControlled ? (filesProp ?? []) : internalFiles;

          const [isDragging, setIsDragging] = useState(false);
          const [liveAnnouncement, setLiveAnnouncement] = useState("");
          const [previewUrls, setPreviewUrls] = useState<Record<string, string>>({});

          const hiddenInputRef = useRef<HTMLInputElement>(null);

          // Build/revoke preview URLs for accepted image files.
          // StrictMode-safe: cleanup runs on next-effect-run AND unmount.
          useEffect(() => {
            const urls: Record<string, string> = {};
            for (const f of files) {
              if (f.accepted && isImageMime(f.file.type)) {
                urls[f.key] = URL.createObjectURL(f.file);
              }
            }
            setPreviewUrls(urls);
            return () => {
              for (const url of Object.values(urls)) {
                URL.revokeObjectURL(url);
              }
            };
          }, [files]);

          const commitFiles = useCallback(
            (next: SFFileEntry[]) => {
              if (!isControlled) setInternalFiles(next);
              onChange?.(next);
            },
            [isControlled, onChange]
          );

          const handleFiles = useCallback(
            (incoming: File[]) => {
              if (incoming.length === 0) return;

              // De-duplicate against existing entries by makeFileKey.
              const existingKeys = new Set(files.map((f) => f.key));
              const fresh = incoming.filter((f) => !existingKeys.has(makeFileKey(f)));
              if (fresh.length === 0) return;

              // In single-file mode, take only the first incoming file and replace state.
              const slice = multiple ? fresh : fresh.slice(0, 1);

              const validated: SFFileEntry[] = slice.map((f) => {
                const okMime = validateMime(f, accept);
                const okSize = validateSize(f, maxSize);
                let error: string | undefined;
                if (!okMime) error = "File type not allowed";
                else if (!okSize)
                  error = `File too large (${formatBytes(f.size)} > ${formatBytes(maxSize ?? 0)})`;
                return {
                  key: makeFileKey(f),
                  file: f,
                  accepted: okMime && okSize,
                  error,
                };
              });

              const next = multiple ? [...files, ...validated] : validated;
              commitFiles(next);

              const accepted = validated.filter((e) => e.accepted).length;
              const rejected = validated.length - accepted;
              setLiveAnnouncement(
                rejected > 0
                  ? `${accepted} files added, ${rejected} rejected`
                  : `${accepted} file${accepted === 1 ? "" : "s"} selected`
              );
            },
            [files, multiple, accept, maxSize, commitFiles]
          );

          const handleRemove = useCallback(
            (key: string) => {
              const target = files.find((f) => f.key === key);
              const next = files.filter((f) => f.key !== key);
              commitFiles(next);
              if (target) setLiveAnnouncement(`${target.file.name} removed`);
            },
            [files, commitFiles]
          );

          const dropZoneLabel = useMemo(
            () =>
              ariaLabel ??
              "Upload files. Press Enter or Space to browse, or drag and drop files here.",
            [ariaLabel]
          );

          return (
            <div className={cn("flex flex-col", className)}>
              {/* Drop zone wrapper */}
              <div
                role="button"
                tabIndex={disabled ? -1 : 0}
                aria-label={dropZoneLabel}
                aria-disabled={disabled || undefined}
                data-drag-active={isDragging || undefined}
                data-disabled={disabled || undefined}
                onClick={() => {
                  if (disabled) return;
                  hiddenInputRef.current?.click();
                }}
                onKeyDown={(e) => {
                  if (disabled) return;
                  if (e.key === "Enter" || e.key === " ") {
                    e.preventDefault();
                    hiddenInputRef.current?.click();
                  }
                }}
                onDragOver={(e) => {
                  e.preventDefault();
                  if (!disabled) setIsDragging(true);
                }}
                onDragEnter={(e) => {
                  e.preventDefault();
                  if (!disabled) setIsDragging(true);
                }}
                onDragLeave={() => setIsDragging(false)}
                onDrop={(e) => {
                  e.preventDefault();
                  setIsDragging(false);
                  if (disabled) return;
                  handleFiles(Array.from(e.dataTransfer.files));
                }}
                onPaste={(e) => {
                  if (disabled) return;
                  const pasted = Array.from(e.clipboardData.files);
                  if (pasted.length > 0) handleFiles(pasted);
                }}
                className={cn(
                  "sf-focusable",
                  "border-2 border-foreground bg-muted",
                  "p-[var(--sfx-space-6)] text-center",
                  "cursor-pointer select-none",
                  "font-mono uppercase tracking-wider text-xs text-foreground",
                  isDragging && "bg-foreground/10",
                  disabled && "opacity-50 pointer-events-none"
                )}
              >
                <span aria-hidden="true" className="block text-2xl mb-[var(--sfx-space-2)]">+</span>
                <span className="block">
                  {isDragging ? "Drop // here" : "Drop files // or // click to browse"}
                </span>
                {accept && (
                  <span className="block mt-[var(--sfx-space-2)] text-muted-foreground normal-case tracking-normal">
                    Accept // {accept}
                  </span>
                )}
                {maxSize !== undefined && (
                  <span className="block mt-[var(--sfx-space-1)] text-muted-foreground normal-case tracking-normal">
                    Max // {formatBytes(maxSize)}
                  </span>
                )}
                <input
                  ref={hiddenInputRef}
                  type="file"
                  accept={accept}
                  multiple={multiple}
                  disabled={disabled}
                  onChange={(e) => {
                    handleFiles(Array.from(e.target.files ?? []));
                    e.target.value = "";
                  }}
                  style={{ display: "none" }}
                  data-testid="sf-file-upload-input"
                />
              </div>

              {/* aria-live announcement region */}
              <div aria-live="polite" aria-atomic="true" className="sr-only">
                {liveAnnouncement}
              </div>

              {/* File list */}
              {files.length > 0 && (
                <ul
                  role="list"
                  aria-label="Selected files"
                  className="mt-[var(--sfx-space-3)] divide-y-2 divide-foreground border-2 border-foreground"
                >
                  {files.map((entry) => {
                    const previewUrl = previewUrls[entry.key];
                    const fileProgress = progress?.[entry.file.name];
                    return (
                      <li
                        key={entry.key}
                        role="listitem"
                        data-accepted={entry.accepted || undefined}
                        data-rejected={!entry.accepted || undefined}
                        className={cn(
                          "flex items-start gap-[var(--sfx-space-3)] p-[var(--sfx-space-3)]",
                          !entry.accepted && "bg-destructive/10"
                        )}
                      >
                        {previewUrl && (
                          // eslint-disable-next-line @next/next/no-img-element
                          <img
                            src={previewUrl}
                            alt=""
                            className="w-[48px] h-[48px] object-cover border-2 border-foreground"
                          />
                        )}
                        <div className="flex-1 min-w-0">
                          <div className="font-mono uppercase tracking-wider text-xs truncate text-foreground">
                            {entry.file.name}
                          </div>
                          <div className="font-mono text-xs text-muted-foreground normal-case tracking-normal mt-[var(--sfx-space-1)]">
                            {formatBytes(entry.file.size)}
                            {entry.file.type && (
                              <>
                                {" // "}
                                {entry.file.type}
                              </>
                            )}
                          </div>
                          {entry.error && (
                            <div
                              className="font-mono uppercase tracking-wider text-xs text-destructive mt-[var(--sfx-space-1)]"
                              data-error
                            >
                              {entry.error}
                            </div>
                          )}
                          {entry.accepted && fileProgress !== undefined && (
                            <SFProgress
                              value={fileProgress}
                              aria-label={`Upload progress for ${entry.file.name}`}
                              className="h-1 mt-[var(--sfx-space-2)]"
                            />
                          )}
                        </div>
                        <button
                          type="button"
                          aria-label={`Remove ${entry.file.name}`}
                          onClick={() => handleRemove(entry.key)}
                          className={cn(
                            "sf-focusable",
                            "px-[var(--sfx-space-3)] py-[var(--sfx-space-2)]",
                            "border-l-2 border-foreground",
                            "font-mono uppercase tracking-wider text-xs",
                            "text-foreground hover:bg-foreground hover:text-background",
                            "cursor-pointer self-stretch"
                          )}
                        >
                          ×
                        </button>
                      </li>
                    );
                  })}
                </ul>
              )}
            </div>
          );
        }

    7. Append a Pattern C contract reminder comment block at end of file:

        // -----------------------------------------------------------------------
        // Pattern C contract reminder (FU-04):
        //   - SFFileUpload IS exported from components/sf/index.ts barrel
        //   - Zero new runtime npm deps — native HTML5 drag-drop + <input type="file">
        //     + URL.createObjectURL only. NO react-dropzone / react-dnd / similar.
        //   - Consumer owns HTTP — component contains zero fetch / XHR / axios calls.
        //     The `progress: Record<fileName, number>` prop is the consumer's
        //     contract for surface-level progress reporting via SFProgress.
        //   - URL.createObjectURL paired with URL.revokeObjectURL in useEffect
        //     cleanup is the ONLY image-preview path. NEVER FileReader.readAsDataURL
        //     (heap blow-up on large files).
        //   - dataTransfer.files cannot be set from JS in Chromium (crbug.com/531834);
        //     drag-drop file ingestion is NOT E2E-testable in CI Playwright. Split-
        //     test strategy lives at .planning/phases/74-sffileupload/74-VERIFICATION.md.
        // -----------------------------------------------------------------------

    Spacing values used: `--sfx-space-1` (4 px), `--sfx-space-2` (8 px), `--sfx-space-3` (12 px), `--sfx-space-6` (24 px). All blessed stops per CLAUDE.md.

    Border-radius: ZERO — no `rounded-*` classes anywhere in the file (drop zone, file list, file rows, remove button, error chip, progress bar).

    Color tokens: `text-foreground`, `text-muted-foreground`, `text-destructive`, `bg-muted` (drop zone default surface — R-60-e), `bg-foreground/10` (drag-active state — tonal step up from `bg-muted`), `bg-destructive/10`, `border-foreground`, `divide-foreground`, `hover:bg-foreground`, `hover:text-background`. All slot tokens — no hardcoded hex/oklch/named colors. Drop zone surface MUST be `bg-muted` (NOT `bg-background`) so the zone reads against the page in borderless default mode (R-60-a SSR sets `<html data-borderless="true">`; R-60-d zeros `border-width` on `*`).

    Focus ring: `sf-focusable` class on drop zone wrapper AND remove button — applies the LOCKDOWN R-64-b double-ring `:focus-visible` style; do NOT hand-roll a custom focus style.

    Filenames + error messages render via React text-node escaping ONLY. Do not use any raw-HTML inject prop on file-list rows.

    Commit: `Feat(74-01): SFFileUpload component (FU-01 drop+browse+paste+list, FU-02 mime+size+multi, FU-03 progress+preview+controlled, FU-04 zero-dep)`
  </action>
  <verify>
    <automated>test -f components/sf/sf-file-upload.tsx && grep -q "^\"use client\";" components/sf/sf-file-upload.tsx && grep -q "export function SFFileUpload" components/sf/sf-file-upload.tsx && grep -q "export type SFFileEntry" components/sf/sf-file-upload.tsx && grep -q "export interface SFFileUploadProps" components/sf/sf-file-upload.tsx && grep -q "role=\"button\"" components/sf/sf-file-upload.tsx && grep -q "type=\"file\"" components/sf/sf-file-upload.tsx && grep -q "onDrop=" components/sf/sf-file-upload.tsx && grep -q "onDragOver=" components/sf/sf-file-upload.tsx && grep -q "onPaste=" components/sf/sf-file-upload.tsx && [ "$(grep -c 'preventDefault' components/sf/sf-file-upload.tsx)" -ge 2 ] && grep -q "URL.createObjectURL" components/sf/sf-file-upload.tsx && grep -q "URL.revokeObjectURL" components/sf/sf-file-upload.tsx && [ "$(grep -c 'FileReader' components/sf/sf-file-upload.tsx)" = "0" ] && [ "$(grep -cE 'fetch\(|XMLHttpRequest|axios' components/sf/sf-file-upload.tsx)" = "0" ] && grep -q "SFProgress" components/sf/sf-file-upload.tsx && grep -q 'aria-label.*[Rr]emove' components/sf/sf-file-upload.tsx && grep -q "aria-live" components/sf/sf-file-upload.tsx && grep -q "disabled={disabled}" components/sf/sf-file-upload.tsx && [ "$(awk '/ANTI-FEATURES/,/KNOWN CAVEATS/' components/sf/sf-file-upload.tsx | grep -cE '^[[:space:]]*\*[[:space:]]+[0-9]+\.')" -ge 7 ] && [ "$(grep -c 'dangerously' components/sf/sf-file-upload.tsx)" = "0" ] && [ "$(grep -c 'bg-muted' components/sf/sf-file-upload.tsx)" -ge "1" ] && ! grep -E "rounded-(sm|md|lg|xl|2xl|full)" components/sf/sf-file-upload.tsx && ! grep -qE "#[0-9a-fA-F]{6}" components/sf/sf-file-upload.tsx && pnpm exec tsc --noEmit 2>&1 | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `components/sf/sf-file-upload.tsx` exists and starts with `"use client";` directive on line 1
    - File contains the literal export `export function SFFileUpload` exactly once
    - File exports the literal types `export type SFFileEntry` and `export interface SFFileUploadProps`
    - File contains `role="button"` on the drop zone wrapper — `grep -c 'role="button"' components/sf/sf-file-upload.tsx` ≥ 1 (74-VALIDATION row 74-01-01)
    - File contains hidden `<input type="file">` — `grep -c 'type="file"' components/sf/sf-file-upload.tsx` ≥ 1 (74-VALIDATION row 74-01-01)
    - File contains `onDrop=` AND `onDragOver=` handlers AND ≥ 2 `preventDefault` calls — covers 74-VALIDATION row 74-01-02
    - File contains `aria-label.*[Rr]emove` for per-file remove button — `grep -c` ≥ 1 (74-VALIDATION row 74-01-03)
    - File contains MIME validation against `accept` — `grep -cE 'accept.*split\|\.type\.startsWith\|endsWith' components/sf/sf-file-upload.tsx` ≥ 1 (74-VALIDATION row 74-01-04)
    - File contains `file.size` and `maxSize` — `grep -cE 'file\.size\|maxSize' components/sf/sf-file-upload.tsx` ≥ 2 (74-VALIDATION row 74-01-05)
    - File contains `error?: string` in the SFFileEntry type — `grep -cE 'error\?:\s*string' components/sf/sf-file-upload.tsx` ≥ 1 (74-VALIDATION row 74-01-06)
    - File contains `multiple` prop forwarded to hidden input — `grep -c 'multiple={multiple}\|multiple\b' components/sf/sf-file-upload.tsx` ≥ 1 (74-VALIDATION row 74-01-07)
    - File contains ZERO `FileReader` references — `grep -c 'FileReader' components/sf/sf-file-upload.tsx` = 0 (74-VALIDATION row 74-01-08)
    - File contains `URL.createObjectURL` AND `URL.revokeObjectURL` in cleanup — both `grep -c` ≥ 1 (74-VALIDATION row 74-01-09)
    - File imports and uses `SFProgress` AND consumes `progress[` indexing — both `grep -c` ≥ 1 (74-VALIDATION row 74-01-10)
    - File contains `files?:` prop AND `onChange?:` callback declarations — both `grep -cE` ≥ 1 (74-VALIDATION row 74-01-11)
    - File contains ZERO `fetch(`, `XMLHttpRequest`, or `axios` references — `grep -cE 'fetch\(|XMLHttpRequest|axios' components/sf/sf-file-upload.tsx` = 0 (74-VALIDATION row 74-01-12)
    - File contains `aria-live` region — `grep -c 'aria-live' components/sf/sf-file-upload.tsx` ≥ 1
    - File contains `onPaste=` handler reading `clipboardData.files`
    - File contains `disabled={disabled}` propagated to the hidden input element (T-74-09 mitigation)
    - File contains the literal substring `ANTI-FEATURES` in the JSDoc — anti-features block enumerated verbatim with ALL 7 entries: HTTP upload, FileReader, maxTotalSize, retry, drop-anywhere, react-dropzone/react-dnd, server-side validation
    - Anti-features block contains AT LEAST 7 numbered entries (count-enforced; addresses plan-checker INFO 4 — drift to <7 entries fails verify): `awk '/ANTI-FEATURES/,/KNOWN CAVEATS/' components/sf/sf-file-upload.tsx | grep -cE '^[[:space:]]*\*[[:space:]]+[0-9]+\.'` ≥ 7
    - File uses ONLY blessed-stop spacing tokens — `grep -oE "var\\(--sfx-space-[0-9]+\\)" components/sf/sf-file-upload.tsx | sort -u` outputs only entries from {space-1, space-2, space-3, space-6} (4 / 8 / 12 / 24 px)
    - File contains ZERO non-`rounded-none` rounded-* classes — `grep -E "rounded-(sm|md|lg|xl|2xl|full)" components/sf/sf-file-upload.tsx` returns 0 matches
    - File contains ZERO hex literals — `grep -qE "#[0-9a-fA-F]{6}" components/sf/sf-file-upload.tsx` returns false
    - File contains ZERO raw-HTML inject prop usages — `grep -c 'dangerously' components/sf/sf-file-upload.tsx` returns 0 (T-74-04 mitigation)
    - File line count ≥ 280 — `wc -l components/sf/sf-file-upload.tsx` ≥ 280
    - File ends with the Pattern C contract reminder comment block — `grep -q "Pattern C contract reminder" components/sf/sf-file-upload.tsx`
    - File uses `bg-muted` on the drop zone wrapper (NOT `bg-background`) — `grep -c 'bg-muted' components/sf/sf-file-upload.tsx` ≥ 1 (LOCKDOWN R-60-e borderless surface contrast; addresses plan-checker WARNING 1)
    - File uses `sf-focusable` class on drop zone wrapper AND remove button (LOCKDOWN R-64-b double-ring)
    - `pnpm exec tsc --noEmit` returns 0 errors
    - `git log -1 --format=%s -- components/sf/sf-file-upload.tsx` matches pattern `Feat\\(74-01\\):.*SFFileUpload`
  </acceptance_criteria>
  <done>
    SFFileUpload component lands. Drop zone with role=button + hidden input keyboard fallback work. MIME + size validation reports per-file errors. Multi-file mode appends to existing list; single-file mode replaces. URL.createObjectURL preview lifecycle is StrictMode-safe via useEffect cleanup. SFProgress renders only for accepted files with a `progress` entry. Controlled and uncontrolled modes work via discriminated state. Zero runtime deps, zero HTTP, zero FileReader, zero hardcoded colors, zero rounded corners. Anti-features documented in JSDoc.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add SFFileUpload barrel export + same-commit registry entries (FU-04 / REG-01)</name>
  <files>components/sf/index.ts, public/r/sf-file-upload.json, public/r/registry.json</files>
  <read_first>
    - components/sf/index.ts (current barrel state — verify SFFileUpload NOT yet exported; SFProgress at line 133 is the natural neighbor)
    - components/sf/sf-file-upload.tsx (Task 1 output — confirms SFFileUpload + SFFileEntry + SFFileUploadProps exports exist)
    - public/r/registry.json (verify items[] count is currently 54 from Phase 72; sf-file-upload NOT yet present)
    - public/r/sf-combobox.json (Pattern C standalone registry-item shape — meta.layer=frame, meta.pattern=C, registryDependencies array)
    - public/r/sf-button.json (canonical standalone registry-item shape)
    - .planning/phases/72-sfcombobox/72-01-PLAN.md Task 3 (barrel + registry insertion pattern; same-commit cohort precedent)
  </read_first>
  <action>
    Three files modified in this task. Land them in a SINGLE commit (REG-01 same-commit rule — Pattern C ships barrel export + registry entry + standalone JSON together).

    (1) Edit `components/sf/index.ts`. Insert the SFFileUpload export immediately AFTER the existing `export { SFProgress } from "./sf-progress";` line (currently line 133). The natural location keeps progress + file-upload adjacent (file-upload consumes SFProgress). Insert:

        export { SFFileUpload, type SFFileEntry, type SFFileUploadProps } from "./sf-file-upload";

    Do NOT modify any other line of `components/sf/index.ts`. Do NOT add/remove any directive (barrel must remain directive-free per v1.3 carry-forward rule).

    Do NOT modify `next.config.ts`. SFFileUpload has zero new deps — D-04 lock holds.

    (2) Create `public/r/sf-file-upload.json` (standalone registry-item file, mirrors `sf-combobox.json` shape):

        {
          "$schema": "https://ui.shadcn.com/schema/registry-item.json",
          "name": "sf-file-upload",
          "title": "SF File Upload",
          "description": "Industrial file upload — drop zone + click-to-browse + per-file remove + MIME/size validation + per-file progress via SFProgress + image preview via URL.createObjectURL. Zero new runtime deps. Consumer owns HTTP.",
          "registryDependencies": [
            "progress"
          ],
          "files": [
            {
              "path": "components/sf/sf-file-upload.tsx",
              "type": "registry:ui"
            }
          ],
          "meta": {
            "layer": "frame",
            "pattern": "C"
          }
        }

    (3) Edit `public/r/registry.json` items[] array. Insert a new entry. The natural insertion point is immediately after the existing `sf-progress` items[] entry (registry-dependency relationship — sf-file-upload depends on sf-progress). The new entry:

        {
          "name": "sf-file-upload",
          "type": "registry:ui",
          "title": "SF File Upload",
          "description": "Industrial file upload — drop zone + click-to-browse + per-file remove + MIME/size validation + per-file progress via SFProgress + image preview via URL.createObjectURL. Zero new runtime deps. Consumer owns HTTP.",
          "registryDependencies": ["progress"],
          "files": [
            {
              "path": "components/sf/sf-file-upload.tsx",
              "type": "registry:ui"
            }
          ],
          "meta": {
            "layer": "frame",
            "pattern": "C"
          }
        },

    Verify the JSON is valid: `node -e "JSON.parse(require('fs').readFileSync('public/r/registry.json', 'utf8')); console.log('OK')"` must print OK and exit 0. Verify items count is now 55: `node -e "console.log(require('./public/r/registry.json').items.length)"` must print 55 (was 54 after Phase 72 close).

    Worktree leakage check: `git status --porcelain | grep -v "components/sf/sf-file-upload.tsx\\|components/sf/index.ts\\|public/r/registry.json\\|public/r/sf-file-upload.json\\|app/dev-playground/sf-file-upload/page.tsx"` — expected: empty output (only the in-scope files modified across Plan 01 thus far).

    Commit: `Chore(74-01): SFFileUpload barrel export + registry entries (FU-04 same-commit cohort)`
  </action>
  <verify>
    <automated>grep -q "export \\{ SFFileUpload" components/sf/index.ts && grep -q "from \"./sf-file-upload\"" components/sf/index.ts && [ "$(grep -c "'use client'" components/sf/index.ts)" = "0" ] && test -f public/r/sf-file-upload.json && grep -q "\"name\": \"sf-file-upload\"" public/r/sf-file-upload.json && grep -q "\"pattern\": \"C\"" public/r/sf-file-upload.json && grep -q "\"layer\": \"frame\"" public/r/sf-file-upload.json && grep -q "\"name\": \"sf-file-upload\"" public/r/registry.json && node -e "const r=require('./public/r/registry.json'); if (r.items.length !== 55) { console.error('FAIL items='+r.items.length); process.exit(1) }; const fu = r.items.find(i=>i.name==='sf-file-upload'); if (!fu || fu.meta.pattern!=='C' || fu.meta.layer!=='frame') { console.error('FAIL meta'); process.exit(1) }; console.log('OK')" && [ "$(git diff HEAD -- next.config.ts | wc -l)" = "0" ] && node -e "const p=require('./package.json'); const blocked=['react-dropzone','react-dnd','filepond','react-files']; const violations=blocked.filter(d => p.dependencies && d in p.dependencies); if (violations.length) { console.error('FORBIDDEN_DEP:', violations); process.exit(1) }; console.log('PASS')" && pnpm exec tsc --noEmit 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `components/sf/index.ts` contains the literal substring `export { SFFileUpload` and `from "./sf-file-upload"` (Pattern C barrel export — FU-04; 74-VALIDATION row 74-01-14)
    - `components/sf/index.ts` contains zero `'use client'` directives — `grep -c "'use client'" components/sf/index.ts` returns 0 (barrel directive-free rule per v1.3 carry-forward)
    - `components/sf/index.ts` has no other lines modified — `git diff HEAD -- components/sf/index.ts` shows ONLY one inserted export line
    - `public/r/sf-file-upload.json` exists and is valid JSON — `node -e "JSON.parse(require('fs').readFileSync('public/r/sf-file-upload.json', 'utf8'))"` exits 0
    - `public/r/sf-file-upload.json` contains literal `"name": "sf-file-upload"`, `"pattern": "C"`, `"layer": "frame"`, `registryDependencies: ["progress"]`
    - `public/r/registry.json` items[] now has length `55` (was 54) — verifiable via `node -e "console.log(require('./public/r/registry.json').items.length)"` returns `55`
    - `public/r/registry.json` items[] contains an entry with `name === "sf-file-upload"`, `meta.pattern === "C"`, `meta.layer === "frame"`
    - `public/r/registry.json` is valid JSON — `node -e "JSON.parse(require('fs').readFileSync('public/r/registry.json', 'utf8'))"` exits 0
    - `next.config.ts` UNCHANGED — `git diff HEAD -- next.config.ts | wc -l` returns 0 (D-04 lock; SFFileUpload adds nothing to optimizePackageImports)
    - `package.json` `dependencies` block contains ZERO forbidden drag-drop libraries — `node -e "const p=require('./package.json'); const blocked=['react-dropzone','react-dnd','filepond','react-files']; const v=blocked.filter(d => p.dependencies && d in p.dependencies); process.exit(v.length?1:0)"` exits 0 (JSON-aware; addresses plan-checker INFO 3 — replaces broken `git diff` grep predicate; 74-VALIDATION row 74-01-13; FU-04 zero-dep contract)
    - `package.json` `dependencies` block UNCHANGED at the line level — `git diff HEAD -- package.json | grep -cE '^\+\s*"[^@]'` returns 0 NEW dependency lines (secondary signal; not load-bearing because git diff base shifts during phase work)
    - `pnpm exec tsc --noEmit` returns 0 errors
    - `git log -1 --format=%s` matches pattern `Chore\\(74-01\\):.*barrel.*registry`
  </acceptance_criteria>
  <done>
    SFFileUpload is exported from the barrel — consumers can `import { SFFileUpload, type SFFileEntry } from "@/components/sf"`. Same-commit cohort lands the standalone registry-item file + items[] entry per REG-01. D-04 lock holds (next.config.ts unchanged). Zero new runtime npm deps in package.json (FU-04 zero-dep contract verified).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Author dev-playground fixture page (consumed by Plan 02 Playwright + axe + Storybook)</name>
  <files>app/dev-playground/sf-file-upload/page.tsx</files>
  <read_first>
    - app/dev-playground/sf-file-upload/page.tsx (verify file does NOT yet exist via `ls`; STOP if it does — worktree leakage)
    - components/sf/sf-file-upload.tsx (Task 1 output — confirms public API: SFFileUpload, SFFileEntry, SFFileUploadProps)
    - components/sf/index.ts (Task 2 output — confirms barrel export)
    - app/dev-playground/sf-data-table/page.tsx (Phase 71 fixture pattern — testid prefix `fixture-{section}`, controlled-state echo via JSON.stringify, multi-section layout)
    - app/dev-playground/sf-combobox/page.tsx (Phase 72 fixture — Pattern C precedent for fixtures consuming barrel-exported components; ≥4 sections; testid anchor convention)
    - .planning/phases/74-sffileupload/74-VALIDATION.md Wave 0 Requirements (4 mandated sections: uncontrolled+accept-image, controlled+onChange echo, multi-file with progress slider, error fixture maxSize=1KB)
  </read_first>
  <action>
    Run `git status --porcelain app/dev-playground/sf-file-upload/page.tsx` first. If output, STOP — worktree leakage.

    Create `app/dev-playground/sf-file-upload/page.tsx`. The fixture mounts FIVE sections to satisfy 74-VALIDATION Wave 0 requirements (4 mandated + a disabled-state section) and provide DOM anchors for Plan 02's Playwright + axe specs. The page MUST be a client component (`'use client'`) because it manages controlled state and a progress slider.

    File structure:

    1. `"use client";` directive on line 1.

    2. JSDoc block:

        /**
         * SFFileUpload dev playground — Plan 02 Playwright + axe + Storybook fixture.
         *
         * Sections (testid prefix `fixture-{section}`):
         *   1. fixture-uncontrolled-image     — uncontrolled, accept="image/*", maxSize 5 MB, multiple
         *   2. fixture-controlled-echo        — controlled files+onChange; pre element echoes JSON state
         *   3. fixture-multi-progress         — multi-file with progress slider (0..100); per-file SFProgress
         *   4. fixture-error-1kb              — maxSize=1024 bytes; any non-trivial file rejects
         *   5. fixture-disabled               — disabled={true} state
         *
         * Not in sitemap. Not linked from public nav.
         */

    3. Imports:

        import { useState } from "react";
        import { SFFileUpload, type SFFileEntry } from "@/components/sf";

    4. Default export — useState requires client. Render a `<main>` landmark with a section per fixture:

        export default function SFFileUploadPlaygroundPage() {
          const [controlledFiles, setControlledFiles] = useState<SFFileEntry[]>([]);
          const [multiFiles, setMultiFiles] = useState<SFFileEntry[]>([]);
          const [progressPct, setProgressPct] = useState(0);

          // Build the progress map keyed by file.name (FU-03 contract):
          const progressMap: Record<string, number> = Object.fromEntries(
            multiFiles.filter((e) => e.accepted).map((e) => [e.file.name, progressPct])
          );

          return (
            <main
              data-testid="sf-file-upload-playground"
              className="p-[var(--sfx-space-6)] space-y-[var(--sfx-space-12)] bg-background text-foreground"
            >
              <h1 className="font-mono uppercase tracking-wider text-2xl">
                SFFileUpload // Playground
              </h1>

              {/* Section 1: uncontrolled + accept image */}
              <section
                data-testid="fixture-uncontrolled-image"
                aria-label="Uncontrolled image-only file upload"
              >
                <h2 className="font-mono uppercase tracking-wider text-sm mb-[var(--sfx-space-3)]">
                  // 01 // Uncontrolled // Image-only // 5 MB max
                </h2>
                <SFFileUpload
                  accept="image/*"
                  maxSize={5 * 1024 * 1024}
                  multiple
                />
              </section>

              {/* Section 2: controlled with onChange echo */}
              <section
                data-testid="fixture-controlled-echo"
                aria-label="Controlled file upload with state echo"
              >
                <h2 className="font-mono uppercase tracking-wider text-sm mb-[var(--sfx-space-3)]">
                  // 02 // Controlled // onChange echo
                </h2>
                <SFFileUpload
                  files={controlledFiles}
                  onChange={setControlledFiles}
                  multiple
                />
                <pre
                  data-testid="fixture-controlled-output"
                  className="mt-[var(--sfx-space-3)] p-[var(--sfx-space-3)] border-2 border-foreground bg-muted font-mono text-xs whitespace-pre-wrap break-all"
                >
                  {JSON.stringify(
                    controlledFiles.map((e) => ({
                      name: e.file.name,
                      size: e.file.size,
                      type: e.file.type,
                      accepted: e.accepted,
                      error: e.error,
                    })),
                    null,
                    2
                  )}
                </pre>
              </section>

              {/* Section 3: multi-file with progress slider */}
              <section
                data-testid="fixture-multi-progress"
                aria-label="Multi-file upload with simulated progress"
              >
                <h2 className="font-mono uppercase tracking-wider text-sm mb-[var(--sfx-space-3)]">
                  // 03 // Multi // Progress slider
                </h2>
                <SFFileUpload
                  files={multiFiles}
                  onChange={setMultiFiles}
                  multiple
                  progress={progressMap}
                />
                <label className="block mt-[var(--sfx-space-3)] font-mono uppercase tracking-wider text-xs">
                  Progress // {progressPct}%
                  <input
                    type="range"
                    min={0}
                    max={100}
                    value={progressPct}
                    onChange={(e) => setProgressPct(Number(e.target.value))}
                    data-testid="fixture-progress-slider"
                    aria-label="Simulated upload progress"
                    className="block mt-[var(--sfx-space-2)] w-full"
                  />
                </label>
              </section>

              {/* Section 4: error fixture maxSize=1KB */}
              <section
                data-testid="fixture-error-1kb"
                aria-label="File upload with 1 KB max size for error testing"
              >
                <h2 className="font-mono uppercase tracking-wider text-sm mb-[var(--sfx-space-3)]">
                  // 04 // 1 KB max // Error state
                </h2>
                <SFFileUpload
                  maxSize={1024}
                  multiple
                />
              </section>

              {/* Section 5: disabled */}
              <section
                data-testid="fixture-disabled"
                aria-label="Disabled file upload"
              >
                <h2 className="font-mono uppercase tracking-wider text-sm mb-[var(--sfx-space-3)]">
                  // 05 // Disabled
                </h2>
                <SFFileUpload disabled multiple />
              </section>
            </main>
          );
        }

    Spacing values used: `--sfx-space-2` (8 px), `--sfx-space-3` (12 px), `--sfx-space-6` (24 px), `--sfx-space-12` (48 px). All blessed stops.

    Border-radius: ZERO. No `rounded-*` classes anywhere.

    Color tokens: `bg-background`, `text-foreground`, `bg-muted`, `border-foreground`. All slot tokens.

    Do NOT add `app/dev-playground/sf-file-upload` to `app/sitemap.ts`. Do NOT link from any public nav route. The fixture is a private testbed.

    Worktree leakage check: `git status --porcelain | grep -v "components/sf/sf-file-upload.tsx\\|components/sf/index.ts\\|public/r/registry.json\\|public/r/sf-file-upload.json\\|app/dev-playground/sf-file-upload/page.tsx"` — expected: empty output.

    Commit: `Feat(74-01): SFFileUpload dev-playground fixture (5 sections; consumed by Plan 02 tests)`
  </action>
  <verify>
    <automated>test -f app/dev-playground/sf-file-upload/page.tsx && grep -q "^\"use client\";" app/dev-playground/sf-file-upload/page.tsx && grep -q "export default function SFFileUploadPlaygroundPage" app/dev-playground/sf-file-upload/page.tsx && grep -q "from \"@/components/sf\"" app/dev-playground/sf-file-upload/page.tsx && grep -q "SFFileUpload" app/dev-playground/sf-file-upload/page.tsx && grep -q "fixture-uncontrolled-image" app/dev-playground/sf-file-upload/page.tsx && grep -q "fixture-controlled-echo" app/dev-playground/sf-file-upload/page.tsx && grep -q "fixture-multi-progress" app/dev-playground/sf-file-upload/page.tsx && grep -q "fixture-error-1kb" app/dev-playground/sf-file-upload/page.tsx && grep -q "fixture-disabled" app/dev-playground/sf-file-upload/page.tsx && grep -q "fixture-controlled-output" app/dev-playground/sf-file-upload/page.tsx && grep -q "fixture-progress-slider" app/dev-playground/sf-file-upload/page.tsx && ! grep -E "rounded-(sm|md|lg|xl|2xl|full)" app/dev-playground/sf-file-upload/page.tsx && [ "$(grep -c 'app/dev-playground/sf-file-upload' app/sitemap.ts 2>/dev/null || echo 0)" = "0" ] && pnpm exec tsc --noEmit 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `app/dev-playground/sf-file-upload/page.tsx` exists and starts with `"use client";` directive
    - File contains `export default function SFFileUploadPlaygroundPage`
    - File imports `SFFileUpload` AND `type SFFileEntry` from `@/components/sf` (barrel — Pattern C)
    - File contains all 5 testid anchors: `fixture-uncontrolled-image`, `fixture-controlled-echo`, `fixture-multi-progress`, `fixture-error-1kb`, `fixture-disabled`
    - File contains `fixture-controlled-output` testid (Plan 02's controlled-API spec uses this pre element to assert state)
    - File contains `fixture-progress-slider` testid (Plan 02's progress spec drives the slider)
    - File contains a section with `accept="image/*"` AND `maxSize={5 * 1024 * 1024}` (Section 1)
    - File contains a section with controlled `files={controlledFiles}` + `onChange={setControlledFiles}` AND a `<pre>` echo of JSON state (Section 2)
    - File contains a section with `progress={progressMap}` driven by a `<input type="range">` slider (Section 3)
    - File contains a section with `maxSize={1024}` (Section 4 — error fixture)
    - File contains a section with `disabled` prop (Section 5)
    - File contains ZERO `rounded-(sm|md|lg|xl|2xl|full)` classes — `grep -E` returns 0 matches
    - File line count ≥ 120 — `wc -l app/dev-playground/sf-file-upload/page.tsx` ≥ 120
    - `app/sitemap.ts` UNCHANGED (does NOT reference the playground route) — verifiable via `grep -c 'sf-file-upload' app/sitemap.ts` = 0 if file exists; ignore if file absent
    - `pnpm exec tsc --noEmit` returns 0 errors
    - `git log -1 --format=%s -- app/dev-playground/sf-file-upload/page.tsx` matches pattern `Feat\\(74-01\\):.*playground`
  </acceptance_criteria>
  <done>
    Playground fixture lands at `/dev-playground/sf-file-upload`. Five sections cover the 4 mandated test surfaces from 74-VALIDATION Wave 0 plus a disabled-state section. Plan 02's Playwright spec navigates to this URL; axe spec scans the same fixture; Storybook stories MAY mirror these compositions or use lighter-weight stories. Fixture is not in sitemap and not linked from public routes.
  </done>
</task>

</tasks>

<verification>
End-to-end verification of Plan 01:

(1) TypeScript compiles cleanly:

    pnpm exec tsc --noEmit

Expected: 0 errors.

(2) SFFileUpload component lands:

    test -f components/sf/sf-file-upload.tsx
    grep -q "export function SFFileUpload" components/sf/sf-file-upload.tsx
    grep -q "export type SFFileEntry" components/sf/sf-file-upload.tsx
    grep -q "export interface SFFileUploadProps" components/sf/sf-file-upload.tsx

Expected: all 4 succeed.

(3) Anti-features enforced:

    [ "$(grep -c 'FileReader' components/sf/sf-file-upload.tsx)" = "0" ]                           # FU-03 no FileReader
    [ "$(grep -cE 'fetch\(|XMLHttpRequest|axios' components/sf/sf-file-upload.tsx)" = "0" ]        # FU-04 no HTTP
    grep -q "ANTI-FEATURES" components/sf/sf-file-upload.tsx                                        # JSDoc anti-features block

Expected: all 3 succeed.

(4) URL.createObjectURL paired with revocation:

    grep -q "URL.createObjectURL" components/sf/sf-file-upload.tsx
    grep -q "URL.revokeObjectURL" components/sf/sf-file-upload.tsx
    grep -q "return () =>" components/sf/sf-file-upload.tsx                                         # useEffect cleanup pattern

Expected: all 3 succeed.

(5) Drop zone + keyboard fallback:

    grep -q 'role="button"' components/sf/sf-file-upload.tsx
    grep -q 'type="file"' components/sf/sf-file-upload.tsx
    grep -q 'onDrop=' components/sf/sf-file-upload.tsx
    grep -q 'onDragOver=' components/sf/sf-file-upload.tsx
    grep -q 'onPaste=' components/sf/sf-file-upload.tsx
    [ "$(grep -c 'preventDefault' components/sf/sf-file-upload.tsx)" -ge 2 ]
    grep -q 'aria-live' components/sf/sf-file-upload.tsx

Expected: all 7 succeed.

(6) Disabled cascade:

    grep -q 'disabled={disabled}' components/sf/sf-file-upload.tsx                                  # propagates to hidden input

Expected: succeeds.

(7) Barrel export + registry same-commit:

    grep -q 'export { SFFileUpload' components/sf/index.ts
    grep -q 'from "./sf-file-upload"' components/sf/index.ts
    [ "$(grep -c "'use client'" components/sf/index.ts)" = "0" ]                                    # barrel directive-free
    test -f public/r/sf-file-upload.json
    node -e "console.log(require('./public/r/registry.json').items.length)"                          # 55

Expected: file checks succeed, items count is 55.

(8) D-04 + zero-dep locks hold:

    [ "$(git diff HEAD -- next.config.ts | wc -l)" = "0" ]                                          # next.config.ts unchanged
    [ "$(git diff HEAD -- package.json | grep -cE '^\+\s*\"[^@]' || true)" = "0" ]                  # no new deps

Expected: both 0.

(9) Zero rounded-* and zero hex literals across modified files:

    ! grep -rE "rounded-(sm|md|lg|xl|2xl|full)" components/sf/sf-file-upload.tsx app/dev-playground/sf-file-upload/page.tsx
    ! grep -rqE "#[0-9a-fA-F]{6}" components/sf/sf-file-upload.tsx

Expected: empty / true.

(10) Playground fixture present:

    test -f app/dev-playground/sf-file-upload/page.tsx
    grep -q "fixture-uncontrolled-image" app/dev-playground/sf-file-upload/page.tsx
    grep -q "fixture-controlled-echo" app/dev-playground/sf-file-upload/page.tsx
    grep -q "fixture-multi-progress" app/dev-playground/sf-file-upload/page.tsx
    grep -q "fixture-error-1kb" app/dev-playground/sf-file-upload/page.tsx

Expected: all 5 succeed.

(11) Bundle budget green (clean build NOT required at this stage — Plan 02 runs the full ANALYZE; here we just spot-check the existing bundle test still passes):

    pnpm exec playwright test tests/v1.8-phase63-1-bundle-budget.spec.ts --project=chromium --reporter=line

Expected: PASS (homepage First Load JS ≤ 200 KB; SFFileUpload is barrel-exported but homepage doesn't render it, so cmdk-style barrel-tree-shaking applies).

(12) Worktree clean:

    git status --porcelain

Expected: clean (all 5 in-scope files committed across the 3 task commits).
</verification>

<success_criteria>
- [ ] `components/sf/sf-file-upload.tsx` ships SFFileUpload + SFFileEntry + SFFileUploadProps with role=button drop zone, hidden <input type=file> keyboard fallback, MIME + size validation, multi-file mode, controlled+uncontrolled API, URL.createObjectURL preview with useEffect revoke cleanup, per-file SFProgress driven by `progress` prop, clipboard paste handler, disabled cascade to hidden input, aria-live region, double-ring focus via sf-focusable
- [ ] JSDoc anti-features block enumerates verbatim: HTTP upload (consumer owns), FileReader.readAsDataURL (memory), aggregate maxTotalSize (FU-06 deferred), retry (FU-06 deferred), drop-anywhere (FU-06 deferred), react-dropzone/react-dnd (FU-04), server-side validation (consumer owns)
- [ ] Zero `FileReader` references; zero `fetch(`/`XMLHttpRequest`/`axios` references; zero raw-HTML inject prop usages
- [ ] Zero hex color literals; zero non-`rounded-none` `rounded-*` classes; only blessed-stop spacing tokens (`--sfx-space-1`/`-2`/`-3`/`-6`/`-12` used)
- [ ] `components/sf/index.ts` exports `SFFileUpload`, `type SFFileEntry`, `type SFFileUploadProps` via barrel; barrel remains directive-free
- [ ] `public/r/sf-file-upload.json` standalone registry-item file lands with `meta.layer="frame"`, `meta.pattern="C"`, `registryDependencies=["progress"]`
- [ ] `public/r/registry.json` items[] count is 55 (was 54 from Phase 72 close); sf-file-upload entry has correct meta
- [ ] `next.config.ts` UNCHANGED (D-04 lock holds); `package.json` `dependencies` block UNCHANGED (FU-04 zero-dep contract)
- [ ] `app/dev-playground/sf-file-upload/page.tsx` mounts 5 sections (fixture-uncontrolled-image, fixture-controlled-echo, fixture-multi-progress, fixture-error-1kb, fixture-disabled); not in sitemap
- [ ] `pnpm exec tsc --noEmit` returns 0 errors
- [ ] Bundle budget spec (`tests/v1.8-phase63-1-bundle-budget.spec.ts`) PASSES
- [ ] Worktree clean after all 3 task commits
</success_criteria>

<output>
After completion, create `.planning/phases/74-sffileupload/74-01-SUMMARY.md` documenting:
- The 3 commits across Tasks 1-3 (Feat impl, Chore barrel+registry, Feat fixture)
- Total LOC added (~430-500 across 5 files: ~290 component + ~5 barrel + ~150 fixture + ~17 standalone JSON + ~13 registry items[] entry)
- Verification commands all-green
- Forward link to Plan 02 (Playwright + axe + Storybook + 74-VERIFICATION.md split-test strategy + bundle audit)
- Confirmation that FU-04 zero-dep contract held throughout (zero new deps in package.json)
- Confirmation that registry items[] count is 55 (REG-01 same-commit rule honored)
- Confirmation that anti-features JSDoc block is verbatim and complete
</output>
