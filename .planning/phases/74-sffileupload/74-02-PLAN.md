---
phase: 74-sffileupload
plan: 02
type: execute
wave: 2
depends_on:
  - "74-01"
files_modified:
  - tests/v1.10-phase74-sf-file-upload.spec.ts
  - tests/v1.10-phase74-sf-file-upload-axe.spec.ts
  - stories/sf-file-upload.stories.tsx
  - .planning/phases/74-sffileupload/74-VERIFICATION.md
  - .planning/phases/74-sffileupload/74-VALIDATION.md
autonomous: true
requirements:
  - FU-05
  - TST-03
  - TST-04
gap_closure: false
must_haves:
  truths:
    - "tests/v1.10-phase74-sf-file-upload.spec.ts covers FU-01 (click-to-browse via setInputFiles, file list rows, per-file remove, paste handler exists), FU-02 (MIME reject shows error chip, oversized file shows error chip, multi-file mode appends, single-file mode replaces), FU-03 (controlled files+onChange echo, progress prop drives SFProgress value attribute, image preview <img> with blob: src for image files); ≥10 named test cases; vacuous-green guard asserts drop zone role=button visible BEFORE any state assertion"
    - "tests/v1.10-phase74-sf-file-upload-axe.spec.ts runs axe-core across 4 fixture states (empty / with-files / with-progress / with-error) with the rule set [button-name, progressbar, aria-valid-attr-value, color-contrast, region]; vacuous-green guard asserts [role=button][aria-label] is visible BEFORE each analyze() call; zero violations across all sections"
    - "stories/sf-file-upload.stories.tsx exports ≥8 stories: Default, MultiFile, WithProgress, ImagePreview, MimeReject, SizeReject, Disabled, DragActive; the DragActive story uses play() with fireEvent.dragOver(canvas.getByRole('button')) from @storybook/test to capture the drag-active visual state for Chromatic snapshot; every story sets parameters.chromatic.delay = 200"
    - ".planning/phases/74-sffileupload/74-VERIFICATION.md exists and explicitly documents the dataTransfer.files Chromium permanent gap with primary source citations: microsoft/playwright#13362 (Won't Fix), crbug.com/531834 (WontFix), WHATWG HTML §6.10.6; documents the same gap for clipboardData.files; states that handleFiles() is the single shared code path covered by setInputFiles; explicitly states that no Playwright test calls page.dispatchEvent('drop', ...) and asserts green without dataTransfer.files round-trip (TST-04 contract)"
    - "Production build (rm -rf .next/cache .next && ANALYZE=true pnpm build) verifies SFFileUpload is tree-shake-clean — sf-file-upload chunk is ABSENT from the homepage / First Load chunk manifest; SFFileUpload only appears in chunks for routes that mount it (e.g. dev-playground)"
    - "Homepage / First Load JS remains <= 200 KB after Plan 01 component lands and is barrel-exported; tests/v1.8-phase63-1-bundle-budget.spec.ts PASSES against the new build"
    - "74-VALIDATION.md per-task verification map updated: rows 74-01-* and 74-02-* statuses set to ✅ green with the exact automated command and outcome captured; nyquist_compliant: true set in frontmatter; status: passed in frontmatter"
  artifacts:
    - path: "tests/v1.10-phase74-sf-file-upload.spec.ts"
      provides: "Playwright acceptance spec — ≥10 test cases covering FU-01/02/03 via locator.setInputFiles(); vacuous-green guard pattern; controlled+onChange echo; progress aria-valuenow assertion; image preview img[src^=blob:] assertion; multi-file append vs single-file replace; per-file remove; MIME + size error chips"
    - path: "tests/v1.10-phase74-sf-file-upload-axe.spec.ts"
      provides: "axe-core spec — ≥6 scans across 4 fixture states; rule set [button-name, progressbar, aria-valid-attr-value, color-contrast, region]; vacuous-green guard before each analyze(); zero violations; aria-live region presence checked"
    - path: "stories/sf-file-upload.stories.tsx"
      provides: "Storybook stories — ≥8 stories including DragActive with play() + fireEvent.dragOver from @storybook/test; parameters.chromatic.delay = 200 on every story"
    - path: ".planning/phases/74-sffileupload/74-VERIFICATION.md"
      provides: "Split test strategy doc — explicit dataTransfer.files Chromium permanent gap with primary source citations; clipboardData.files same-class gap noted; manual-verification list for visual drag state on real Chromium browser; TST-04 contract: no vacuously-passing drop test"
      contains: "dataTransfer.files"
    - path: ".planning/phases/74-sffileupload/74-VALIDATION.md"
      provides: "Per-task rows updated with green status; nyquist_compliant: true; status: passed in frontmatter"
      contains: "nyquist_compliant: true"
  key_links:
    - from: "tests/v1.10-phase74-sf-file-upload.spec.ts"
      to: "app/dev-playground/sf-file-upload/page.tsx"
      via: "Playwright page.goto fixture URL /dev-playground/sf-file-upload; testid anchors fixture-{section}"
      pattern: "/dev-playground/sf-file-upload"
    - from: "tests/v1.10-phase74-sf-file-upload-axe.spec.ts"
      to: "@axe-core/playwright"
      via: "AxeBuilder rule scan; mirrors Phase 71/72/73 precedent"
      pattern: "AxeBuilder"
    - from: "stories/sf-file-upload.stories.tsx"
      to: "@storybook/test"
      via: "fireEvent.dragOver in play() function for DragActive story"
      pattern: "@storybook/test|fireEvent"
    - from: ".planning/phases/74-sffileupload/74-VERIFICATION.md"
      to: "primary-source citations"
      via: "FU-05 + TST-04 contract — Chromium bug tracker + Playwright issue tracker URLs"
      pattern: "crbug|microsoft/playwright|whatwg"
---

<objective>
Close Phase 74 with the test specs (TST-03), the Storybook stories with a DragActive story for the visual diff path (FU-05 / TST-04 visual side), and the `74-VERIFICATION.md` doc that explicitly documents the permanent `dataTransfer.files` Chromium gap as a first-class deliverable (FU-05 / TST-04 doc side). Plan 01 shipped the SFFileUpload component, barrel export, registry entries, and the playground fixture. Plan 02 ships the evidence: Playwright acceptance via `locator.setInputFiles()`, axe-core WCAG AA across 4 fixture states, Storybook stories including the `DragActive` story that uses `play()` + `fireEvent.dragOver()` from `@storybook/test` for Chromatic snapshot capture, the explicit gap-documentation in `74-VERIFICATION.md`, and a bundle audit confirming SFFileUpload is tree-shake-clean (absent from the homepage First Load chunk despite being barrel-exported).

Purpose: Plan 01 satisfied FU-01..04 component-side. Plan 02 ships:
- TST-03 — Per-component Playwright + axe specs land same-phase as the component; all green before phase close
- FU-05 — Split test strategy (Playwright `setInputFiles` for acceptance + Chromatic for drag visual) explicitly documented in `74-VERIFICATION.md`; permanent `dataTransfer.files` gap acknowledged with primary sources
- TST-04 — `74-VERIFICATION.md` is the canonical artifact; not papered over with a vacuous Playwright drop test

The phase closes with all 7 requirement IDs satisfied (FU-01..05, TST-03, TST-04), the bundle headroom intact (≤ 200 KB First Load JS), and the registry at 55 entries.

Output: 5 files modified — 2 new test specs, 1 new Storybook stories file, 1 new `74-VERIFICATION.md`, and `74-VALIDATION.md` updated to `nyquist_compliant: true`.
</objective>

<execution_context>
@/Users/greyaltaer/.claude/pde-os/engines/gsd/workflows/execute-plan.md
@/Users/greyaltaer/.claude/pde-os/engines/gsd/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/74-sffileupload/74-RESEARCH.md
@.planning/phases/74-sffileupload/74-VALIDATION.md
@.planning/phases/74-sffileupload/74-01-SUMMARY.md
@CLAUDE.md
@.planning/LOCKDOWN.md
@components/sf/sf-file-upload.tsx
@components/sf/index.ts
@app/dev-playground/sf-file-upload/page.tsx
@tests/v1.10-phase72-sf-combobox.spec.ts
@tests/v1.10-phase72-sf-combobox-axe.spec.ts
@tests/v1.10-phase73-sf-rich-editor.spec.ts
@tests/v1.10-phase73-sf-rich-editor-axe.spec.ts
@tests/v1.8-phase63-1-bundle-budget.spec.ts
@stories/sf-rich-editor.stories.tsx
@.storybook/preview.ts
@playwright.config.ts
@next.config.ts

<interfaces>
<!-- Playwright fixture URL + setInputFiles primitive -->

const FIXTURE = "/dev-playground/sf-file-upload";

await page.goto(FIXTURE, { waitUntil: "networkidle" });
// Vacuous-green guard:
await expect(page.locator('[role="button"]').first()).toBeVisible();

// Programmatic file selection via hidden input — bypasses OS file dialog:
await page.locator('[data-testid="sf-file-upload-input"]').first().setInputFiles({
  name: "test.png",
  mimeType: "image/png",
  buffer: Buffer.from(
    // 1x1 transparent PNG (smallest valid PNG)
    "89504e470d0a1a0a0000000d49484452000000010000000108060000001f15c4890000000a49444154789c6300010000000500010d0a2db40000000049454e44ae426082",
    "hex"
  ),
});

<!-- Multi-file via setInputFiles array -->

await page.locator('[data-testid="sf-file-upload-input"]').first().setInputFiles([
  { name: "a.png", mimeType: "image/png", buffer: pngBuf },
  { name: "b.txt", mimeType: "text/plain", buffer: Buffer.from("hello") },
]);

<!-- Controlled API + onChange echo assertion (Section 2 fixture) -->

const echoLocator = page.locator('[data-testid="fixture-controlled-output"]');
await page.locator('[data-testid="fixture-controlled-echo"] [data-testid="sf-file-upload-input"]').setInputFiles({
  name: "selected.txt",
  mimeType: "text/plain",
  buffer: Buffer.from("a"),
});
await expect(echoLocator).toContainText("selected.txt");
await expect(echoLocator).toContainText('"accepted": true');

<!-- Per-file SFProgress assertion (Section 3 fixture) -->

await page.locator('[data-testid="fixture-multi-progress"] [data-testid="sf-file-upload-input"]').setInputFiles({
  name: "doc.pdf",
  mimeType: "application/pdf",
  buffer: Buffer.from("%PDF-1.4\n"),
});
// Drive the slider to 47%
await page.locator('[data-testid="fixture-progress-slider"]').fill("47");
const progressbar = page.locator('[data-testid="fixture-multi-progress"] [role="progressbar"]').first();
await expect(progressbar).toBeVisible();
await expect(progressbar).toHaveAttribute("aria-valuenow", "47");
await expect(progressbar).toHaveAttribute("aria-label", /Upload progress for doc\.pdf/);

<!-- Image preview blob: URL assertion -->

await page.locator('[data-testid="fixture-uncontrolled-image"] [data-testid="sf-file-upload-input"]').setInputFiles({
  name: "photo.png",
  mimeType: "image/png",
  buffer: pngBuf,
});
const previewImg = page.locator('[data-testid="fixture-uncontrolled-image"] img').first();
await expect(previewImg).toBeVisible();
const src = await previewImg.getAttribute("src");
expect(src).toMatch(/^blob:/);

<!-- Per-file remove + aria-live announcement -->

await page.getByRole("button", { name: /^Remove photo\.png$/ }).click();
await expect(previewImg).toHaveCount(0);
// aria-live region (sr-only) updates to "photo.png removed"
await expect(page.locator('[aria-live="polite"]')).toContainText("photo.png removed");

<!-- MIME reject (Section 1 fixture: accept="image/*") -->

await page.locator('[data-testid="fixture-uncontrolled-image"] [data-testid="sf-file-upload-input"]').setInputFiles({
  name: "doc.pdf",
  mimeType: "application/pdf",
  buffer: Buffer.from("%PDF-1.4\n"),
});
const errorRow = page.locator('[data-testid="fixture-uncontrolled-image"] [data-rejected]').first();
await expect(errorRow).toContainText("File type not allowed");

<!-- Size reject (Section 4 fixture: maxSize=1024) -->

const big = Buffer.alloc(2048, 0); // 2 KB > 1 KB max
await page.locator('[data-testid="fixture-error-1kb"] [data-testid="sf-file-upload-input"]').setInputFiles({
  name: "big.txt",
  mimeType: "text/plain",
  buffer: big,
});
await expect(
  page.locator('[data-testid="fixture-error-1kb"] [data-rejected]').first()
).toContainText("File too large");

<!-- axe-core scan with vacuous-green guard (mirrors Phase 72/73 precedent) -->

import AxeBuilder from "@axe-core/playwright";

test.beforeEach(async ({ page }) => {
  await page.goto(FIXTURE, { waitUntil: "networkidle" });
  // Vacuous-green guard: drop zone must be visible BEFORE analyze()
  await expect(page.locator('[role="button"]').first()).toBeVisible();
});

const results = await new AxeBuilder({ page })
  .withRules(["button-name", "progressbar", "aria-valid-attr-value", "color-contrast", "region"])
  .analyze();
expect(results.violations).toEqual([]);

<!-- Storybook DragActive story (research §Chromatic / Visual-Test Stack Reconciliation §Recommended story pattern) -->

import type { Meta, StoryObj } from "@storybook/react";
import { fireEvent, within } from "@storybook/test";
import { SFFileUpload } from "@/components/sf";

const meta: Meta<typeof SFFileUpload> = {
  title: "SF/SFFileUpload",
  component: SFFileUpload,
  parameters: { layout: "centered", chromatic: { delay: 200 } },
};

export default meta;
type Story = StoryObj<typeof SFFileUpload>;

export const DragActive: Story = {
  args: { multiple: true, accept: "image/*" },
  play: async ({ canvasElement }) => {
    const dropZone = within(canvasElement).getByRole("button");
    // fireEvent.dragOver fires the React onDragOver handler in JSDOM/Storybook DOM,
    // setting isDragging=true. Unlike Playwright, Storybook's interaction layer runs
    // inside the same-origin browser DOM where dragover events DO fire handlers.
    await fireEvent.dragOver(dropZone);
  },
  parameters: { chromatic: { delay: 400 } },
};

<!-- 74-VERIFICATION.md primary-source citations (research §The Playwright dataTransfer.files Gap) -->

| # | Source | URL / Reference | Status |
|---|--------|-----------------|--------|
| 1 | Playwright maintainer issue | https://github.com/microsoft/playwright/issues/13362 | Open, Won't Fix (upstream Chromium constraint) |
| 2 | Chromium bug tracker | https://crbug.com/531834 | WontFix (since 2015) |
| 3 | WHATWG HTML spec §6.10.6 | https://html.spec.whatwg.org/multipage/dnd.html#the-datatransfer-interface | DataTransfer.files is platform-data-store-backed; scripts can only READ |

<!-- Bundle audit script (research §Pattern C Audit §Tree-Shake-Cleanliness) -->

node -e "
const fs = require('fs');
let homeStr = '';
try {
  homeStr = JSON.stringify(JSON.parse(fs.readFileSync('.next/build-manifest.json', 'utf8')).pages['/'] || []);
} catch(e) {
  homeStr = JSON.stringify(JSON.parse(fs.readFileSync('.next/app-build-manifest.json', 'utf8')).pages['/page'] || []);
}
const fileUploadLeak = /sf-file-upload/.test(homeStr);
console.log('SFFileUpload in homepage First Load:', fileUploadLeak ? 'FAIL' : 'PASS');
process.exit(fileUploadLeak ? 1 : 0);
"

<!-- Existing 200 KB budget spec -->

tests/v1.8-phase63-1-bundle-budget.spec.ts:22 — const BUDGET_BYTES = 200 * 1024;
</interfaces>

<threat_model>
ASVS L1 — Phase 74 Plan 02 threat surface:

| Threat ID | Threat | Severity | Mitigation |
|-----------|--------|----------|------------|
| T-74-V01 | Vacuous-green Playwright spec: tests pass against an un-mounted SFFileUpload (CSS hides drop zone or fixture page 404s) | HIGH | beforeEach asserts `[role="button"]` visible BEFORE any state assertion; per-section selectors scope to `[data-testid="fixture-{section}"]` so a missing fixture fails fast (74-VALIDATION row 74-02-04) |
| T-74-V02 | Vacuous-green axe-core spec: axe.analyze() runs on empty fixture state (drop zone not yet hydrated) → zero violations trivially | HIGH | beforeEach hook asserts `[role="button"][aria-label]` visible BEFORE each analyze() call; acceptance criterion verifies the spec contains BOTH `await expect(...).toBeVisible()` AND `new AxeBuilder({ page })...analyze()` in that order (74-VALIDATION row 74-02-04) |
| T-74-V03 | Drag-drop test papered over with a `page.dispatchEvent('drop', { dataTransfer: { files: [...] }})` and naive green assertion (Chromium silently empty-FileLists synthetic events) | HIGH | TST-04 contract enforced: `74-VERIFICATION.md` explicitly states no Playwright test simulates drop; acceptance criterion: `grep -c "dispatchEvent.*['\"]drop['\"]" tests/v1.10-phase74-sf-file-upload.spec.ts` returns 0 (74-VALIDATION row 74-02-08) |
| T-74-V04 | Chromatic story DragActive uses a synthetic prop (`_forceDragActive`) that pollutes the public API surface | HIGH | Per phase_specific_constraints decision 1: use `play()` + `fireEvent.dragOver(canvas.getByRole('button'))` from `@storybook/test`; acceptance criterion: stories file does NOT contain the literal `_forceDragActive`; DragActive story IS a `play()` story |
| T-74-V05 | Bundle escape: SFFileUpload barrel-exported AND homepage transitively imports the barrel, dragging file-upload code into homepage chunk | HIGH | Bundle audit node script (Task 4) on clean ANALYZE build asserts `sf-file-upload` substring is ABSENT from homepage `/page` chunk manifest; acceptance criterion: script exits 0; the existing `tests/v1.8-phase63-1-bundle-budget.spec.ts` ALSO passes (200 KB hard target) (74-VALIDATION row 74-02-09) |
| T-74-V06 | Test runtime explosion: spec runs Playwright across mobile/tablet/desktop projects unnecessarily — slow per-task feedback latency violates 25 s ceiling from 74-VALIDATION | MEDIUM | All commands explicitly pass `--project=chromium`; runtime ≤ 25 s for `--grep` quick runs (mirrors Phase 72/73 precedent) |
| T-74-V07 | Image preview test brittle on JSDOM/Playwright — `URL.createObjectURL` may not produce a `blob:` scheme in all environments | LOW | Playwright runs Chromium where URL.createObjectURL returns blob:; assertion uses regex `/^blob:/` not exact URL match; if assertion flakes, snapshot a screenshot via `page.screenshot()` for diagnostic and document fallback |
| T-74-V08 | aria-live region not picked up by axe (sr-only class collapses element to 1px) — axe `region` rule may flag missing landmark | LOW | Component renders inside the playground `<main>` element which IS a region landmark; aria-live is announced via the element's content, not as a landmark; axe `region` rule scans for landmarks only — sr-only aria-live is not a landmark requirement |
| T-74-V09 | 74-VERIFICATION.md ratifies a future-loosening (e.g., changes the 200 KB budget) instead of documenting the gap | MEDIUM | TST-04 contract: VERIFICATION.md ONLY documents the permanent gap + manual verification surface; no path_X_decision blocks; if a path_X_decision becomes necessary, requires user approval and ROADMAP standing-rules sign-off |
| T-74-V10 | Storybook story file ships without `parameters.chromatic.delay` causing flaky pixel-diff captures (ProseMirror precedent — Phase 73 set delay=500 because Tiptap async hydrates; SFFileUpload is sync but progress slider + image preview decode benefit from delay=200) | LOW | Acceptance criterion: every story export sets `parameters.chromatic.delay` ≥ 100; default meta sets 200; DragActive bumps to 400 for fireEvent.dragOver state propagation |

No SSR-time secret/token surface. No fetch in test code (MSW would be a new dep — rejected). No raw-HTML user content threading; test buffers are programmatic constants.
</threat_model>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author Playwright acceptance spec — FU-01 + FU-02 + FU-03 (TST-03)</name>
  <files>tests/v1.10-phase74-sf-file-upload.spec.ts</files>
  <read_first>
    - tests/v1.10-phase74-sf-file-upload.spec.ts (verify file does NOT yet exist via `ls` — STOP if it does, worktree leakage)
    - tests/v1.10-phase72-sf-combobox.spec.ts (Phase 72 acceptance spec — vacuous-green guard pattern; describe-block structure; per-section testid scoping)
    - tests/v1.10-phase73-sf-rich-editor.spec.ts (Phase 73 acceptance spec — controlled-output assertion via JSON pre element; precedent for state echo)
    - app/dev-playground/sf-file-upload/page.tsx (Plan 01 fixture — confirms 5 sections + testid anchors fixture-uncontrolled-image, fixture-controlled-echo, fixture-multi-progress, fixture-error-1kb, fixture-disabled, fixture-controlled-output, fixture-progress-slider, sf-file-upload-input)
    - components/sf/sf-file-upload.tsx (Plan 01 component — confirms aria-label strings: "Remove {file.name}", "Upload progress for {file.name}", "Upload files. Press Enter or Space to browse, or drag and drop files here.")
    - .planning/phases/74-sffileupload/74-VALIDATION.md row 74-02-07 (acceptance-broken predicate — covers click-to-browse, multi-file, MIME reject, size reject, controlled onChange, progress render, image preview)
    - .planning/phases/74-sffileupload/74-RESEARCH.md §Validation Architecture FU-01/FU-02/FU-03 predicates (lines 689–728)
    - playwright.config.ts (verify --project=chromium is the canonical CI project; CAPTURE_BASE_URL pattern from Phase 71/72 specs)
  </read_first>
  <action>
    Run `git status --porcelain tests/v1.10-phase74-sf-file-upload.spec.ts` first. If output, STOP — worktree leakage.

    Create `tests/v1.10-phase74-sf-file-upload.spec.ts`. Spec MUST include ≥10 test cases organized into 3 describe blocks (FU-01, FU-02, FU-03). Use `locator.setInputFiles()` against the hidden input `[data-testid="sf-file-upload-input"]` — NEVER simulate drag-drop via `page.dispatchEvent('drop', ...)` (TST-04 contract enforced by acceptance criterion).

    File structure:

        import { test, expect } from "@playwright/test";

        const ABS_BASE = process.env.CAPTURE_BASE_URL ?? "http://localhost:3000";
        const FIXTURE = `${ABS_BASE}/dev-playground/sf-file-upload`;

        // 1x1 transparent PNG — smallest valid PNG, used as image-preview test buffer.
        const PNG_HEX =
          "89504e470d0a1a0a0000000d49484452000000010000000108060000001f15c4890000000a49444154789c6300010000000500010d0a2db40000000049454e44ae426082";
        const PNG_BUF = Buffer.from(PNG_HEX, "hex");

        const PDF_BUF = Buffer.from("%PDF-1.4\n");

        test.describe("SFFileUpload — FU-01 click-to-browse + file list + paste", () => {
          test.beforeEach(async ({ page }) => {
            await page.goto(FIXTURE, { waitUntil: "networkidle" });
            // Vacuous-green guard: drop zone must be visible BEFORE assertions
            await expect(
              page.locator('[data-testid="fixture-uncontrolled-image"] [role="button"]').first()
            ).toBeVisible();
          });

          test("FU-01 click-to-browse via setInputFiles populates file list", async ({ page }) => {
            const input = page
              .locator('[data-testid="fixture-uncontrolled-image"] [data-testid="sf-file-upload-input"]')
              .first();
            await input.setInputFiles({
              name: "photo.png",
              mimeType: "image/png",
              buffer: PNG_BUF,
            });
            // file list row appears
            const list = page.locator('[data-testid="fixture-uncontrolled-image"] [role="list"]');
            await expect(list).toBeVisible();
            await expect(list.locator('[role="listitem"]')).toHaveCount(1);
            await expect(list).toContainText("photo.png");
          });

          test("FU-01 per-file remove button clears the row + announces removal via aria-live", async ({ page }) => {
            const input = page
              .locator('[data-testid="fixture-uncontrolled-image"] [data-testid="sf-file-upload-input"]')
              .first();
            await input.setInputFiles({ name: "a.png", mimeType: "image/png", buffer: PNG_BUF });
            const list = page.locator('[data-testid="fixture-uncontrolled-image"] [role="list"]');
            await expect(list.locator('[role="listitem"]')).toHaveCount(1);
            await page.getByRole("button", { name: /^Remove a\.png$/ }).click();
            await expect(list).toHaveCount(0);
            await expect(
              page.locator('[data-testid="fixture-uncontrolled-image"] [aria-live="polite"]').first()
            ).toContainText("a.png removed");
          });

          test("FU-01 paste handler exists on drop zone", async ({ page }) => {
            // We cannot programmatically populate clipboardData.files in Chromium for the
            // same reason dataTransfer.files is restricted (74-VERIFICATION.md). What we
            // CAN verify: the onPaste handler is wired (drop zone visible + accepts focus).
            const dropZone = page
              .locator('[data-testid="fixture-uncontrolled-image"] [role="button"]')
              .first();
            await dropZone.focus();
            await expect(dropZone).toBeFocused();
          });
        });

        test.describe("SFFileUpload — FU-02 validation (MIME, size, multi-file)", () => {
          test.beforeEach(async ({ page }) => {
            await page.goto(FIXTURE, { waitUntil: "networkidle" });
          });

          test("FU-02 MIME reject — PDF in image-only fixture surfaces error chip", async ({ page }) => {
            const input = page
              .locator('[data-testid="fixture-uncontrolled-image"] [data-testid="sf-file-upload-input"]')
              .first();
            await input.setInputFiles({
              name: "doc.pdf",
              mimeType: "application/pdf",
              buffer: PDF_BUF,
            });
            const rejectedRow = page
              .locator('[data-testid="fixture-uncontrolled-image"] [data-rejected]')
              .first();
            await expect(rejectedRow).toBeVisible();
            await expect(rejectedRow).toContainText("File type not allowed");
          });

          test("FU-02 size reject — 2 KB file in 1 KB fixture surfaces error chip", async ({ page }) => {
            const big = Buffer.alloc(2048, 0);
            const input = page
              .locator('[data-testid="fixture-error-1kb"] [data-testid="sf-file-upload-input"]')
              .first();
            await input.setInputFiles({
              name: "big.txt",
              mimeType: "text/plain",
              buffer: big,
            });
            const rejectedRow = page
              .locator('[data-testid="fixture-error-1kb"] [data-rejected]')
              .first();
            await expect(rejectedRow).toBeVisible();
            await expect(rejectedRow).toContainText("File too large");
          });

          test("FU-02 multi-file mode appends multiple files to the list", async ({ page }) => {
            const input = page
              .locator('[data-testid="fixture-uncontrolled-image"] [data-testid="sf-file-upload-input"]')
              .first();
            await input.setInputFiles([
              { name: "one.png", mimeType: "image/png", buffer: PNG_BUF },
              { name: "two.png", mimeType: "image/png", buffer: PNG_BUF },
            ]);
            const list = page.locator('[data-testid="fixture-uncontrolled-image"] [role="list"]');
            await expect(list.locator('[role="listitem"]')).toHaveCount(2);
            await expect(list).toContainText("one.png");
            await expect(list).toContainText("two.png");
          });

          test("FU-02 mixed accept/reject in one selection — both rows surface, accepted shows preview, rejected shows error", async ({ page }) => {
            const input = page
              .locator('[data-testid="fixture-uncontrolled-image"] [data-testid="sf-file-upload-input"]')
              .first();
            await input.setInputFiles([
              { name: "ok.png", mimeType: "image/png", buffer: PNG_BUF },
              { name: "bad.pdf", mimeType: "application/pdf", buffer: PDF_BUF },
            ]);
            const list = page.locator('[data-testid="fixture-uncontrolled-image"] [role="list"]');
            await expect(list.locator('[role="listitem"]')).toHaveCount(2);
            await expect(list.locator("[data-accepted]")).toHaveCount(1);
            await expect(list.locator("[data-rejected]")).toHaveCount(1);
          });
        });

        test.describe("SFFileUpload — FU-03 progress + controlled API + image preview", () => {
          test.beforeEach(async ({ page }) => {
            await page.goto(FIXTURE, { waitUntil: "networkidle" });
          });

          test("FU-03 controlled files+onChange — selection updates JSON echo", async ({ page }) => {
            const input = page
              .locator('[data-testid="fixture-controlled-echo"] [data-testid="sf-file-upload-input"]')
              .first();
            await input.setInputFiles({
              name: "selected.txt",
              mimeType: "text/plain",
              buffer: Buffer.from("a"),
            });
            const echo = page.locator('[data-testid="fixture-controlled-output"]');
            await expect(echo).toContainText("selected.txt");
            await expect(echo).toContainText('"accepted": true');
          });

          test("FU-03 progress prop drives SFProgress aria-valuenow + aria-label", async ({ page }) => {
            const input = page
              .locator('[data-testid="fixture-multi-progress"] [data-testid="sf-file-upload-input"]')
              .first();
            await input.setInputFiles({
              name: "doc.pdf",
              mimeType: "application/pdf",
              buffer: PDF_BUF,
            });
            await page.locator('[data-testid="fixture-progress-slider"]').fill("47");
            const progressbar = page
              .locator('[data-testid="fixture-multi-progress"] [role="progressbar"]')
              .first();
            await expect(progressbar).toBeVisible();
            await expect(progressbar).toHaveAttribute("aria-valuenow", "47");
            await expect(progressbar).toHaveAttribute(
              "aria-label",
              /Upload progress for doc\.pdf/
            );
          });

          test("FU-03 image preview — accepted image renders <img> with blob: src", async ({ page }) => {
            const input = page
              .locator('[data-testid="fixture-uncontrolled-image"] [data-testid="sf-file-upload-input"]')
              .first();
            await input.setInputFiles({
              name: "photo.png",
              mimeType: "image/png",
              buffer: PNG_BUF,
            });
            const previewImg = page
              .locator('[data-testid="fixture-uncontrolled-image"] img')
              .first();
            await expect(previewImg).toBeVisible();
            const src = await previewImg.getAttribute("src");
            expect(src).toMatch(/^blob:/);
          });

          test("FU-03 disabled fixture has tabIndex=-1 on drop zone + disabled hidden input", async ({ page }) => {
            const dropZone = page
              .locator('[data-testid="fixture-disabled"] [role="button"]')
              .first();
            await expect(dropZone).toHaveAttribute("tabindex", "-1");
            const input = page
              .locator('[data-testid="fixture-disabled"] [data-testid="sf-file-upload-input"]')
              .first();
            await expect(input).toBeDisabled();
          });
        });

    Worktree leakage check: `git status --porcelain | grep -v "tests/v1.10-phase74-sf-file-upload.spec.ts\\|tests/v1.10-phase74-sf-file-upload-axe.spec.ts\\|stories/sf-file-upload.stories.tsx\\|.planning/phases/74-sffileupload/74-VERIFICATION.md\\|.planning/phases/74-sffileupload/74-VALIDATION.md"` — expected: empty.

    Run the spec to verify all tests pass:

        pnpm exec playwright test tests/v1.10-phase74-sf-file-upload.spec.ts --project=chromium --reporter=line

    Expected: all 10 tests pass. If any test fails, fix the spec OR escalate to user — DO NOT modify the Plan 01 component to make the spec pass without explicit user approval (the component is the reference; tests verify it).

    Commit: `Test(74-02): SFFileUpload Playwright acceptance spec (FU-01/02/03; 10 tests)`
  </action>
  <verify>
    <automated>test -f tests/v1.10-phase74-sf-file-upload.spec.ts && grep -q "import { test, expect } from \"@playwright/test\"" tests/v1.10-phase74-sf-file-upload.spec.ts && [ "$(grep -c 'test(' tests/v1.10-phase74-sf-file-upload.spec.ts)" -ge 10 ] && [ "$(grep -c 'test\\.describe' tests/v1.10-phase74-sf-file-upload.spec.ts)" = "3" ] && grep -q "setInputFiles" tests/v1.10-phase74-sf-file-upload.spec.ts && grep -q "fixture-uncontrolled-image" tests/v1.10-phase74-sf-file-upload.spec.ts && grep -q "fixture-controlled-echo" tests/v1.10-phase74-sf-file-upload.spec.ts && grep -q "fixture-multi-progress" tests/v1.10-phase74-sf-file-upload.spec.ts && grep -q "fixture-error-1kb" tests/v1.10-phase74-sf-file-upload.spec.ts && grep -q "fixture-disabled" tests/v1.10-phase74-sf-file-upload.spec.ts && grep -q "blob:" tests/v1.10-phase74-sf-file-upload.spec.ts && grep -q "aria-valuenow" tests/v1.10-phase74-sf-file-upload.spec.ts && grep -q "aria-live" tests/v1.10-phase74-sf-file-upload.spec.ts && [ "$(grep -cE \"dispatchEvent.*['\\\"']drop['\\\"']\" tests/v1.10-phase74-sf-file-upload.spec.ts)" = "0" ] && pnpm exec playwright test tests/v1.10-phase74-sf-file-upload.spec.ts --project=chromium --reporter=line 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `tests/v1.10-phase74-sf-file-upload.spec.ts` exists and imports `{ test, expect } from "@playwright/test"`
    - Spec contains exactly 3 `test.describe` blocks (FU-01, FU-02, FU-03)
    - Spec contains ≥ 10 `test(` cases — `grep -c 'test(' tests/v1.10-phase74-sf-file-upload.spec.ts` ≥ 10 (74-VALIDATION row 74-02-07)
    - Each `describe` has a `beforeEach` with vacuous-green guard asserting `[role="button"]` visible before any state-mutating test
    - Spec uses `locator.setInputFiles()` for ALL file-input simulation — `grep -c 'setInputFiles' tests/v1.10-phase74-sf-file-upload.spec.ts` ≥ 5 (5+ test cases use it)
    - Spec scopes selectors to fixture testids: `fixture-uncontrolled-image`, `fixture-controlled-echo`, `fixture-multi-progress`, `fixture-error-1kb`, `fixture-disabled` — all 5 referenced (74-VALIDATION row 74-02-07)
    - Spec contains an image-preview blob: assertion — `grep -c 'blob:' tests/v1.10-phase74-sf-file-upload.spec.ts` ≥ 1
    - Spec contains a progress-bar `aria-valuenow` assertion — `grep -c 'aria-valuenow' tests/v1.10-phase74-sf-file-upload.spec.ts` ≥ 1
    - Spec contains an aria-live announcement assertion — `grep -c 'aria-live' tests/v1.10-phase74-sf-file-upload.spec.ts` ≥ 1
    - Spec contains ZERO `page.dispatchEvent('drop', ...)` calls — `grep -cE "dispatchEvent.*['\\\"]drop['\\\"]" tests/v1.10-phase74-sf-file-upload.spec.ts` = 0 (TST-04 contract; 74-VALIDATION row 74-02-08)
    - All tests pass — `pnpm exec playwright test tests/v1.10-phase74-sf-file-upload.spec.ts --project=chromium` exits 0
    - `git log -1 --format=%s -- tests/v1.10-phase74-sf-file-upload.spec.ts` matches pattern `Test\\(74-02\\):.*Playwright`
  </acceptance_criteria>
  <done>
    Playwright acceptance spec lands. ≥10 test cases across FU-01/02/03 cover click-to-browse, file list, per-file remove, paste-handler-exists, MIME reject, size reject, multi-file append, mixed accept/reject, controlled+onChange echo, progress prop driving SFProgress, image preview blob: URL, disabled state. All tests green. No drag-drop simulation (TST-04 contract).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Author axe-core WCAG AA spec — 4 fixture states (TST-03)</name>
  <files>tests/v1.10-phase74-sf-file-upload-axe.spec.ts</files>
  <read_first>
    - tests/v1.10-phase74-sf-file-upload-axe.spec.ts (verify file does NOT yet exist)
    - tests/v1.10-phase72-sf-combobox-axe.spec.ts (Phase 72 axe spec — vacuous-green guard pattern; @axe-core/playwright import; rule scoping; assertion shape)
    - tests/v1.10-phase73-sf-rich-editor-axe.spec.ts (Phase 73 axe spec — multi-state scan; chromatic delay alternative)
    - app/dev-playground/sf-file-upload/page.tsx (5 fixture sections — axe MUST scan each in turn after seeding files)
    - components/sf/sf-file-upload.tsx (Plan 01 — confirms aria-label / aria-live / role attributes that axe tests against)
    - .planning/phases/74-sffileupload/74-VALIDATION.md rows 74-02-04, 74-02-05, 74-02-06 (vacuous-green, a11y-violation, aria-live-missing predicates)
    - .planning/phases/74-sffileupload/74-RESEARCH.md §Drag-and-Drop Accessibility Pattern §WCAG 2.2 AA Criteria Applicable (lines 264-274)
  </read_first>
  <action>
    Run `git status --porcelain tests/v1.10-phase74-sf-file-upload-axe.spec.ts` first. STOP if output.

    Create `tests/v1.10-phase74-sf-file-upload-axe.spec.ts`. Spec scans the playground page in 4 distinct fixture states and runs axe with the rule set [`button-name`, `progressbar`, `aria-valid-attr-value`, `color-contrast`, `region`]. Each scan asserts zero violations. Vacuous-green guard runs in `beforeEach`: drop zone `[role="button"][aria-label]` must be visible BEFORE every analyze().

    File structure:

        import { test, expect } from "@playwright/test";
        import AxeBuilder from "@axe-core/playwright";

        const ABS_BASE = process.env.CAPTURE_BASE_URL ?? "http://localhost:3000";
        const FIXTURE = `${ABS_BASE}/dev-playground/sf-file-upload`;

        const PNG_BUF = Buffer.from(
          "89504e470d0a1a0a0000000d49484452000000010000000108060000001f15c4890000000a49444154789c6300010000000500010d0a2db40000000049454e44ae426082",
          "hex"
        );

        const AXE_RULES = [
          "button-name",
          "progressbar",
          "aria-valid-attr-value",
          "color-contrast",
          "region",
        ];

        test.describe("SFFileUpload — TST-03 axe-core WCAG AA", () => {
          test.beforeEach(async ({ page }) => {
            await page.goto(FIXTURE, { waitUntil: "networkidle" });
            // VACUOUS-GREEN GUARD: drop zone must be present + labeled BEFORE analyze()
            const dropZone = page.locator('[role="button"][aria-label]').first();
            await expect(dropZone).toBeVisible();
          });

          test("axe — empty state (drop zone, no files selected)", async ({ page }) => {
            const results = await new AxeBuilder({ page })
              .withRules(AXE_RULES)
              .analyze();
            expect(results.violations).toEqual([]);
          });

          test("axe — with files (multi-file fixture, accepted state)", async ({ page }) => {
            const input = page
              .locator('[data-testid="fixture-uncontrolled-image"] [data-testid="sf-file-upload-input"]')
              .first();
            await input.setInputFiles([
              { name: "one.png", mimeType: "image/png", buffer: PNG_BUF },
              { name: "two.png", mimeType: "image/png", buffer: PNG_BUF },
            ]);
            await expect(
              page.locator('[data-testid="fixture-uncontrolled-image"] [role="list"]')
            ).toBeVisible();
            const results = await new AxeBuilder({ page })
              .withRules(AXE_RULES)
              .analyze();
            expect(results.violations).toEqual([]);
          });

          test("axe — with progress (slider at 50%, SFProgress visible)", async ({ page }) => {
            const input = page
              .locator('[data-testid="fixture-multi-progress"] [data-testid="sf-file-upload-input"]')
              .first();
            await input.setInputFiles({
              name: "doc.pdf",
              mimeType: "application/pdf",
              buffer: Buffer.from("%PDF-1.4\n"),
            });
            await page.locator('[data-testid="fixture-progress-slider"]').fill("50");
            const progressbar = page
              .locator('[data-testid="fixture-multi-progress"] [role="progressbar"]')
              .first();
            await expect(progressbar).toBeVisible();
            const results = await new AxeBuilder({ page })
              .withRules(AXE_RULES)
              .analyze();
            expect(results.violations).toEqual([]);
          });

          test("axe — with error (oversized file in 1 KB fixture)", async ({ page }) => {
            const big = Buffer.alloc(2048, 0);
            const input = page
              .locator('[data-testid="fixture-error-1kb"] [data-testid="sf-file-upload-input"]')
              .first();
            await input.setInputFiles({
              name: "big.txt",
              mimeType: "text/plain",
              buffer: big,
            });
            await expect(
              page.locator('[data-testid="fixture-error-1kb"] [data-rejected]').first()
            ).toBeVisible();
            const results = await new AxeBuilder({ page })
              .withRules(AXE_RULES)
              .analyze();
            expect(results.violations).toEqual([]);
          });

          test("axe — disabled state (drop zone tabindex=-1, hidden input disabled)", async ({ page }) => {
            const results = await new AxeBuilder({ page })
              .include('[data-testid="fixture-disabled"]')
              .withRules(AXE_RULES)
              .analyze();
            expect(results.violations).toEqual([]);
          });

          test("aria-live region present on drop zone", async ({ page }) => {
            // Structural assertion (not axe): aria-live region IS in DOM, visible to AT
            const liveRegion = page.locator('[aria-live="polite"]').first();
            await expect(liveRegion).toHaveCount(5); // one per fixture section
          });
        });

    Run the spec:

        pnpm exec playwright test tests/v1.10-phase74-sf-file-upload-axe.spec.ts --project=chromium --reporter=line

    Expected: all 6 tests pass with zero axe violations. If `region` rule fails because the playground sections aren't landmarks, scope axe `.include()` to drop zone wrappers AND adjust the spec to not run `region` rule on inner SFFileUpload (the playground `<main>` is the region landmark; SFFileUpload is content within it). Document any ratifications.

    Commit: `Test(74-02): SFFileUpload axe-core WCAG AA spec (4 fixture states + aria-live presence)`
  </action>
  <verify>
    <automated>test -f tests/v1.10-phase74-sf-file-upload-axe.spec.ts && grep -q "import AxeBuilder from \"@axe-core/playwright\"" tests/v1.10-phase74-sf-file-upload-axe.spec.ts && [ "$(grep -c 'test(' tests/v1.10-phase74-sf-file-upload-axe.spec.ts)" -ge 6 ] && grep -q "AxeBuilder({ page })" tests/v1.10-phase74-sf-file-upload-axe.spec.ts && [ "$(grep -c 'analyze()' tests/v1.10-phase74-sf-file-upload-axe.spec.ts)" -ge 5 ] && grep -q "VACUOUS-GREEN" tests/v1.10-phase74-sf-file-upload-axe.spec.ts && grep -q '\[role=\"button\"\]\[aria-label\]' tests/v1.10-phase74-sf-file-upload-axe.spec.ts && grep -q "button-name" tests/v1.10-phase74-sf-file-upload-axe.spec.ts && grep -q "progressbar" tests/v1.10-phase74-sf-file-upload-axe.spec.ts && grep -q "aria-valid-attr-value" tests/v1.10-phase74-sf-file-upload-axe.spec.ts && grep -q "color-contrast" tests/v1.10-phase74-sf-file-upload-axe.spec.ts && grep -q "aria-live" tests/v1.10-phase74-sf-file-upload-axe.spec.ts && pnpm exec playwright test tests/v1.10-phase74-sf-file-upload-axe.spec.ts --project=chromium --reporter=line 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `tests/v1.10-phase74-sf-file-upload-axe.spec.ts` exists and imports `AxeBuilder from "@axe-core/playwright"`
    - Spec contains ≥ 6 `test(` cases — `grep -c 'test(' tests/v1.10-phase74-sf-file-upload-axe.spec.ts` ≥ 6
    - Spec contains the literal substring `VACUOUS-GREEN` in a comment AND a vacuous-green guard `await expect(page.locator('[role="button"][aria-label]').first()).toBeVisible()` in `beforeEach` (74-VALIDATION row 74-02-04)
    - Spec runs axe `analyze()` ≥ 5 times across 4 fixture states (empty / with-files / with-progress / with-error / disabled) — `grep -c 'analyze()' tests/v1.10-phase74-sf-file-upload-axe.spec.ts` ≥ 5
    - Spec withRules covers: `button-name`, `progressbar`, `aria-valid-attr-value`, `color-contrast` — all 4 strings present
    - Spec includes a structural test for `aria-live` region presence (74-VALIDATION row 74-02-06)
    - All tests pass — `pnpm exec playwright test tests/v1.10-phase74-sf-file-upload-axe.spec.ts --project=chromium` exits 0 (74-VALIDATION row 74-02-05)
    - `git log -1 --format=%s -- tests/v1.10-phase74-sf-file-upload-axe.spec.ts` matches pattern `Test\\(74-02\\):.*axe`
  </acceptance_criteria>
  <done>
    axe-core WCAG AA spec lands. 5 axe scans across empty / with-files / with-progress / with-error / disabled fixture states all return zero violations. aria-live region presence verified structurally. Vacuous-green guard prevents axe-passes-on-empty-DOM regressions.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Author Storybook stories with DragActive play() story (FU-05 visual / TST-04 visual)</name>
  <files>stories/sf-file-upload.stories.tsx</files>
  <read_first>
    - stories/sf-file-upload.stories.tsx (verify file does NOT yet exist)
    - stories/sf-rich-editor.stories.tsx (Phase 73 stories — meta + chromatic.delay pattern; story export shape)
    - .storybook/preview.ts (verify withThemeByClassName decorator + chromatic devDep present)
    - components/sf/sf-file-upload.tsx (Plan 01 — confirm SFFileUpload public API; props for stories)
    - .planning/phases/74-sffileupload/74-RESEARCH.md §Chromatic / Visual-Test Stack Reconciliation §Recommended story pattern (lines 605-642)
    - .planning/phases/74-sffileupload/74-VALIDATION.md rows 74-02-03, 74-02-08 (DragActive story present + drag-test-vacuous predicates)
    - package.json devDependencies (verify @storybook/test is present — wraps @testing-library/dom; this is the canonical fireEvent source per research)
  </read_first>
  <action>
    Run `git status --porcelain stories/sf-file-upload.stories.tsx` first. STOP if output.

    Create `stories/sf-file-upload.stories.tsx`. The file MUST export ≥8 stories: `Default`, `MultiFile`, `WithProgress`, `ImagePreview`, `MimeReject`, `SizeReject`, `Disabled`, `DragActive`. The `DragActive` story uses `play()` + `fireEvent.dragOver(canvas.getByRole('button'))` from `@storybook/test` to set the component's internal `isDragging=true` state for Chromatic snapshot — NEVER expose a synthetic test prop on the public API.

    File structure:

        import type { Meta, StoryObj } from "@storybook/react";
        import { fireEvent, within } from "@storybook/test";
        import { useState } from "react";
        import { SFFileUpload, type SFFileEntry } from "@/components/sf";

        const meta: Meta<typeof SFFileUpload> = {
          title: "SF/SFFileUpload",
          component: SFFileUpload,
          parameters: {
            layout: "centered",
            chromatic: { delay: 200 },
          },
          tags: ["autodocs"],
        };

        export default meta;
        type Story = StoryObj<typeof SFFileUpload>;

        // Synthetic preset SFFileEntry — used for non-controlled stories that need to render
        // file rows without OS file dialog interaction. Note: this entry uses a fake File via
        // new File([], name); URL.createObjectURL on an empty File returns blob:null/<uuid> in
        // most browsers — image preview will still mount the <img> element.
        function makePresetEntry(name: string, type: string, size: number, accepted: boolean, error?: string): SFFileEntry {
          const file = new File([new Blob(["x".repeat(Math.min(size, 1024))], { type })], name, { type });
          // Spoof size for visual testing — actual file body is small, but display uses entry.file.size
          Object.defineProperty(file, "size", { value: size });
          return {
            key: `${name}__${size}__0`,
            file,
            accepted,
            error,
          };
        }

        export const Default: Story = {
          args: { accept: "image/*", maxSize: 5 * 1024 * 1024 },
          parameters: { chromatic: { delay: 200 } },
        };

        export const MultiFile: Story = {
          args: { accept: "image/*,.pdf", multiple: true, maxSize: 10 * 1024 * 1024 },
          parameters: { chromatic: { delay: 200 } },
        };

        export const WithProgress: Story = {
          render: (args) => {
            const files: SFFileEntry[] = [
              makePresetEntry("uploading.pdf", "application/pdf", 256_000, true),
              makePresetEntry("done.png", "image/png", 128_000, true),
            ];
            return (
              <SFFileUpload
                {...args}
                files={files}
                progress={{ "uploading.pdf": 47, "done.png": 100 }}
              />
            );
          },
          args: { multiple: true },
          parameters: { chromatic: { delay: 200 } },
        };

        export const ImagePreview: Story = {
          render: (args) => {
            const files: SFFileEntry[] = [
              makePresetEntry("hero.png", "image/png", 64_000, true),
            ];
            return <SFFileUpload {...args} files={files} />;
          },
          parameters: { chromatic: { delay: 200 } },
        };

        export const MimeReject: Story = {
          render: (args) => {
            const files: SFFileEntry[] = [
              makePresetEntry("doc.pdf", "application/pdf", 100_000, false, "File type not allowed"),
            ];
            return <SFFileUpload {...args} files={files} />;
          },
          args: { accept: "image/*" },
          parameters: { chromatic: { delay: 200 } },
        };

        export const SizeReject: Story = {
          render: (args) => {
            const files: SFFileEntry[] = [
              makePresetEntry("huge.png", "image/png", 10_000_000, false, "File too large (10.0 MB > 1.0 MB)"),
            ];
            return <SFFileUpload {...args} files={files} />;
          },
          args: { accept: "image/*", maxSize: 1024 * 1024 },
          parameters: { chromatic: { delay: 200 } },
        };

        export const Disabled: Story = {
          args: { disabled: true, multiple: true },
          parameters: { chromatic: { delay: 200 } },
        };

        export const DragActive: Story = {
          args: { multiple: true, accept: "image/*" },
          play: async ({ canvasElement }) => {
            // Storybook's interaction layer runs inside the same-origin browser DOM.
            // fireEvent.dragOver fires the React onDragOver handler, setting isDragging=true.
            // This is the visual-state capture path documented in 74-VERIFICATION.md
            // as Track 2 — Chromatic story for drag-active visual diff. Note: this does
            // NOT populate dataTransfer.files (Chromium security model — see crbug.com/531834);
            // the file ingestion path is verified separately via Playwright setInputFiles().
            const dropZone = within(canvasElement).getByRole("button");
            await fireEvent.dragOver(dropZone);
          },
          parameters: { chromatic: { delay: 400 } },
        };

    Run TypeScript check:

        pnpm exec tsc --noEmit

    Expected: 0 errors. The `Object.defineProperty(file, "size", ...)` may need a `// @ts-expect-error` if File.size is readonly in your TS config; if so, add the comment with reason "Spoof size for visual story; not used at runtime".

    OPTIONAL: run Storybook locally to spot-check the stories render — not required for plan acceptance (stories are visual-diff captured via Chromatic, not Playwright).

    Commit: `Test(74-02): SFFileUpload Storybook stories (8 stories incl. DragActive play())`
  </action>
  <verify>
    <automated>test -f stories/sf-file-upload.stories.tsx && grep -q "import type { Meta, StoryObj } from \"@storybook/react\"" stories/sf-file-upload.stories.tsx && grep -q "from \"@storybook/test\"" stories/sf-file-upload.stories.tsx && grep -q "fireEvent" stories/sf-file-upload.stories.tsx && grep -q "from \"@/components/sf\"" stories/sf-file-upload.stories.tsx && grep -q "export const Default" stories/sf-file-upload.stories.tsx && grep -q "export const MultiFile" stories/sf-file-upload.stories.tsx && grep -q "export const WithProgress" stories/sf-file-upload.stories.tsx && grep -q "export const ImagePreview" stories/sf-file-upload.stories.tsx && grep -q "export const MimeReject" stories/sf-file-upload.stories.tsx && grep -q "export const SizeReject" stories/sf-file-upload.stories.tsx && grep -q "export const Disabled" stories/sf-file-upload.stories.tsx && grep -q "export const DragActive" stories/sf-file-upload.stories.tsx && grep -q "play:" stories/sf-file-upload.stories.tsx && grep -q "fireEvent.dragOver" stories/sf-file-upload.stories.tsx && [ "$(grep -c 'chromatic: { delay:' stories/sf-file-upload.stories.tsx)" -ge 8 ] && [ "$(grep -c '_forceDragActive' stories/sf-file-upload.stories.tsx)" = "0" ] && pnpm exec tsc --noEmit 2>&1 | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `stories/sf-file-upload.stories.tsx` exists and imports `Meta, StoryObj from "@storybook/react"` AND `fireEvent, within from "@storybook/test"`
    - File imports `SFFileUpload, type SFFileEntry from "@/components/sf"` (Pattern C barrel)
    - File exports exactly 8 stories: `Default`, `MultiFile`, `WithProgress`, `ImagePreview`, `MimeReject`, `SizeReject`, `Disabled`, `DragActive` — all 8 `export const Xxx` literals present
    - `DragActive` story uses `play:` AND `fireEvent.dragOver` AND `within(canvasElement).getByRole("button")` (74-VALIDATION row 74-02-03)
    - File contains ZERO `_forceDragActive` references — `grep -c '_forceDragActive' stories/sf-file-upload.stories.tsx` = 0 (T-74-V04 mitigation)
    - Every story sets `parameters.chromatic.delay` ≥ 100 — `grep -c 'chromatic: { delay:' stories/sf-file-upload.stories.tsx` ≥ 8 (one default + per-story overrides count)
    - `pnpm exec tsc --noEmit` returns 0 errors (or only `@ts-expect-error`-shielded errors with reason comments)
    - `git log -1 --format=%s -- stories/sf-file-upload.stories.tsx` matches pattern `Test\\(74-02\\):.*Storybook`
  </acceptance_criteria>
  <done>
    Storybook stories file lands. 8 stories cover empty default, multi-file, progress display, image preview, MIME reject, size reject, disabled, and drag-active states. DragActive uses `play()` + `fireEvent.dragOver()` from `@storybook/test` — does NOT pollute the public SFFileUpload API with a test-only prop. Every story has a chromatic delay set.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Author 74-VERIFICATION.md split-test strategy doc + bundle audit (FU-05 / TST-04 / FU-04 closeout)</name>
  <files>.planning/phases/74-sffileupload/74-VERIFICATION.md, .planning/phases/74-sffileupload/74-VALIDATION.md</files>
  <read_first>
    - .planning/phases/74-sffileupload/74-VERIFICATION.md (verify file does NOT yet exist)
    - .planning/phases/72-sfcombobox/72-VERIFICATION.md (Phase 72 VERIFICATION format precedent)
    - .planning/phases/73-sfricheditor/73-VERIFICATION.md (Phase 73 VERIFICATION format precedent)
    - .planning/phases/74-sffileupload/74-RESEARCH.md §The Playwright dataTransfer.files Gap (lines 278-380) — primary source citations
    - .planning/phases/74-sffileupload/74-VALIDATION.md (current state; will be updated to nyquist_compliant: true at end of this task)
    - tests/v1.10-phase74-sf-file-upload.spec.ts (Task 1 output — verify ≥10 tests pass)
    - tests/v1.10-phase74-sf-file-upload-axe.spec.ts (Task 2 output — verify ≥6 axe scans pass)
    - stories/sf-file-upload.stories.tsx (Task 3 output — verify DragActive story exists)
    - components/sf/sf-file-upload.tsx (Plan 01 — confirm tree-shake-clean property holds)
    - tests/v1.8-phase63-1-bundle-budget.spec.ts (200 KB hard target)
  </read_first>
  <action>
    Two outputs in this task: (a) author `74-VERIFICATION.md`, (b) run the bundle audit (clean ANALYZE build) and capture evidence in the doc, (c) update `74-VALIDATION.md` per-task rows + frontmatter.

    Run `git status --porcelain .planning/phases/74-sffileupload/74-VERIFICATION.md` first. STOP if output.

    (1) Run the clean bundle audit:

        rm -rf .next/cache .next
        ANALYZE=true pnpm build 2>&1 | tail -100

    Wait for build to complete (~90 s). Capture homepage `/` First Load JS in KB from the route table output. Confirm ≤ 200 KB (current baseline 187.7 KB; SFFileUpload barrel-export must NOT push it over).

    Run the tree-shake audit (research §Pattern C Audit §Tree-Shake-Cleanliness):

        node -e "
        const fs = require('fs');
        let homeStr = '';
        try {
          const m = JSON.parse(fs.readFileSync('.next/build-manifest.json', 'utf8'));
          homeStr = JSON.stringify(m.pages['/'] || []);
        } catch(e) {
          const m = JSON.parse(fs.readFileSync('.next/app-build-manifest.json', 'utf8'));
          homeStr = JSON.stringify(m.pages['/page'] || []);
        }
        const leak = /sf-file-upload/.test(homeStr);
        console.log('SFFileUpload in homepage First Load:', leak ? 'FAIL' : 'PASS');
        process.exit(leak ? 1 : 0);
        "

    Expected: PASS (exit 0). If FAIL, the homepage transitively imports `@/components/sf` AND something in the tree pulls SFFileUpload — investigate barrel-tree-shaking; may need to move sf-file-upload.tsx to non-barrel and ratify with `_path_X_decision`. Document any deviation.

    Run the bundle budget spec:

        pnpm exec playwright test tests/v1.8-phase63-1-bundle-budget.spec.ts --project=chromium --reporter=line

    Expected: PASS.

    (2) Create `.planning/phases/74-sffileupload/74-VERIFICATION.md`:

        ---
        phase: 74
        slug: sffileupload
        status: passed
        validated: 2026-05-02
        ---

        # Phase 74 — Verification (Split Test Strategy + Permanent Gap Documentation)

        > FU-05 + TST-04 first-class deliverable. The dataTransfer.files Chromium gap is
        > acknowledged here as a permanent CI limitation, NOT papered over with a vacuously-
        > passing Playwright drop test.

        ---

        ## Split Test Strategy (FU-05)

        SFFileUpload's drag-drop file ingestion path cannot be E2E-tested in Chromium-based
        Playwright CI. Test coverage is split across two tracks:

        ### Track 1 — Playwright `locator.setInputFiles()` (Acceptance Logic)

        File: `tests/v1.10-phase74-sf-file-upload.spec.ts`

        Coverage:
        - FU-01 click-to-browse path (10 tests)
        - FU-01 file list rendering + per-file remove
        - FU-01 paste-handler-exists structural check (clipboardData.files has same Chromium gap)
        - FU-02 MIME validation (accept prop)
        - FU-02 size validation (maxSize prop)
        - FU-02 multi-file mode (multiple prop)
        - FU-02 mixed accept/reject in one selection
        - FU-03 controlled files+onChange echo
        - FU-03 progress prop drives SFProgress aria-valuenow + aria-label
        - FU-03 image preview <img> with blob: src
        - FU-03 disabled state (tabindex=-1, hidden input disabled)

        Result: 10/10 passing on chromium project at phase close.

        ### Track 2 — Storybook `play()` + Chromatic (Drag Visual State)

        File: `stories/sf-file-upload.stories.tsx`

        Coverage:
        - 8 stories: Default, MultiFile, WithProgress, ImagePreview, MimeReject, SizeReject,
          Disabled, DragActive
        - DragActive uses `play({ canvasElement })` with `fireEvent.dragOver(canvas.getByRole('button'))`
          from `@storybook/test`. Storybook's interaction layer runs inside the same-origin
          browser DOM where dragover events DO fire React handlers — `isDragging=true` state
          is set, drop zone visually shifts to drag-active variant, and Chromatic captures
          the pixel-diff baseline.

        Result: 8/8 stories deployed; DragActive captures the visual drag-state.

        ### Track 3 — axe-core WCAG AA (TST-03)

        File: `tests/v1.10-phase74-sf-file-upload-axe.spec.ts`

        Coverage:
        - 6 axe scans across 4 fixture states (empty, with-files, with-progress, with-error,
          disabled) + structural aria-live region check
        - Rule set: button-name, progressbar, aria-valid-attr-value, color-contrast, region
        - Vacuous-green guard asserts `[role="button"][aria-label]` visible BEFORE every analyze()

        Result: 6/6 passing; zero violations across all 4 fixture states.

        ---

        ## Permanent Gap — `dataTransfer.files` (Chromium CI / Playwright)

        **What cannot be tested via automation:** The drag→drop file ingestion path. Specifically:
        the `onDrop` event handler reading `e.dataTransfer.files` to populate the file list when
        a real user drags a file from Finder/Explorer onto the drop zone.

        **Why it's permanent:** `DataTransfer.files` is a `FileList` backed by the platform drag
        data store. The Chromium security model treats this property as readable only — scripts
        cannot programmatically populate it via synthetic DragEvent / dispatchEvent. Playwright's
        `page.dispatchEvent('drop', ...)` succeeds in firing the event but the `dataTransfer.files`
        FileList is empty. This is a feature, not a bug: it prevents malicious scripts from
        silently triggering file system access.

        ### Primary Sources

        | # | Source | URL / Reference | Status |
        |---|--------|-----------------|--------|
        | 1 | Playwright maintainer issue | https://github.com/microsoft/playwright/issues/13362 | Open since 2022; Won't Fix (upstream Chromium constraint) |
        | 2 | Chromium bug tracker | https://crbug.com/531834 | WontFix since 2015 ("Setting dataTransfer.files via JS is intentionally prohibited.") |
        | 3 | WHATWG HTML spec §6.10.6 | https://html.spec.whatwg.org/multipage/dnd.html#the-datatransfer-interface | DataTransfer.files is platform-data-store-backed; scripts can only READ |

        ### What's Covered Anyway

        The acceptance logic — validation, file-list update, controlled API, progress rendering,
        image preview, error state — is identical between click-to-browse, drag-drop, and clipboard
        paste paths. All three call the same `handleFiles(File[])` function in
        `components/sf/sf-file-upload.tsx`. Track 1's `setInputFiles()` tests `handleFiles()`
        end-to-end via the click-to-browse code path; the drag-drop and paste paths are reduced
        to two-line handler bodies that delegate to `handleFiles()` after `e.preventDefault()`.
        The ONLY untested surface is `e.dataTransfer.files` and `e.clipboardData.files` reads
        themselves — and those reads are guarded by `e.preventDefault()` (test-coverable structurally
        via grep predicates per 74-VALIDATION row 74-01-02).

        ### Same Gap, Same Severity — `clipboardData.files`

        Note that `e.clipboardData.files` (the paste-from-clipboard handler) has the same
        Chromium security restriction: synthetic ClipboardEvent dispatched via JS has empty
        `files`. The paste handler in SFFileUpload is structurally tested (handler exists, drop
        zone is focusable for paste targeting) but the actual file ingestion via paste is the
        same untested surface. Track 1 covers `handleFiles()` via `setInputFiles()` for both.

        ### What is NOT acceptable (TST-04 contract)

        A Playwright test that calls `page.dispatchEvent('drop', { dataTransfer: { files: [...] } })`
        and asserts the drop zone "handled" the event by checking ONLY that the visual state
        changed (rather than that files were processed) is vacuously green. This contract is
        verified by 74-VALIDATION row 74-02-08: `grep -c "dispatchEvent.*['\"]drop['\"]"
        tests/v1.10-phase74-sf-file-upload.spec.ts` returns 0.

        ---

        ## Manual Verification (Real Chromium on DEV Hardware)

        These are the human-only acceptance checks. Run before phase closeout sign-off:

        | # | Behavior | Steps | Expected |
        |---|----------|-------|----------|
        | M-01 | Drag-active visual state on real browser | Open `/dev-playground/sf-file-upload` in Chromium → drag a real file from Finder/Explorer over Section 1 drop zone → release outside drop zone (cancel drop) | Drop zone background shifts to `bg-muted` on dragover; reverts on dragleave |
        | M-02 | Real drag→drop ingestion (the gap) | Drag a real PNG from Finder onto Section 1 drop zone → release ON drop zone | File appears in list with image preview thumbnail (blob: URL) |
        | M-03 | Image preview render quality | Drop a 5 MB photo into Section 1 → wait ≤ 100 ms | Preview <img> renders without flicker; no broken-image flash |
        | M-04 | URL.revokeObjectURL on removal | Drop a photo → click × remove → DevTools Memory tab → inspect URL registry | No orphaned blob: URL after removal |
        | M-05 | Screen reader announcement | Open with macOS VoiceOver (Cmd+F5) → Tab to drop zone → Enter to browse → select 2 files → click × on one | "2 files selected" announced; "{name} removed" announced |
        | M-06 | Light + dark mode parity | Toggle theme on `/dev-playground/sf-file-upload` | Drop zone, file list, error state, progress bar all read correctly in both themes; no Radix or browser default colors |
        | M-07 | Keyboard parity (LOCKDOWN R-64-d) | Tab to drop zone → press Space | OS file dialog opens (does NOT advance to next panel); per LOCKDOWN R-64-d focus guard for role=button |

        ---

        ## Bundle Audit (FU-04 / BND-08 closeout precedent)

        ### Clean ANALYZE Build

        Command: `rm -rf .next/cache .next && ANALYZE=true pnpm build`

        Result: TBD per executor — measure homepage `/` First Load JS from route table output.
        Expected: ≤ 200 KB (baseline 187.7 KB pre-Phase 74; SFFileUpload barrel-export adds
        ~3-5 KB to the SF chunk that homepage may reference, but barrel tree-shaking keeps
        sf-file-upload code out of homepage chunk).

        Recorded measurement: __________ KB (executor fills in).

        ### Tree-Shake Audit

        Command:

        ```bash
        node -e "
        const fs = require('fs');
        let homeStr = '';
        try {
          homeStr = JSON.stringify(JSON.parse(fs.readFileSync('.next/build-manifest.json', 'utf8')).pages['/'] || []);
        } catch(e) {
          homeStr = JSON.stringify(JSON.parse(fs.readFileSync('.next/app-build-manifest.json', 'utf8')).pages['/page'] || []);
        }
        const leak = /sf-file-upload/.test(homeStr);
        console.log('SFFileUpload in homepage First Load:', leak ? 'FAIL' : 'PASS');
        process.exit(leak ? 1 : 0);
        "
        ```

        Result: PASS (executor confirms exit 0).

        ### Bundle Budget Spec

        Command: `pnpm exec playwright test tests/v1.8-phase63-1-bundle-budget.spec.ts --project=chromium`

        Result: PASS (executor confirms 1/1 green).

        ---

        ## Per-Requirement Closeout

        | REQ-ID | Status | Evidence |
        |--------|--------|----------|
        | FU-01 | PASSED | tests/v1.10-phase74-sf-file-upload.spec.ts FU-01 describe (3 tests green); component grep predicates 74-01-01..03 |
        | FU-02 | PASSED | tests/v1.10-phase74-sf-file-upload.spec.ts FU-02 describe (4 tests green); grep predicates 74-01-04..07 |
        | FU-03 | PASSED | tests/v1.10-phase74-sf-file-upload.spec.ts FU-03 describe (4 tests green); grep predicates 74-01-08..11 |
        | FU-04 | PASSED | components/sf/index.ts barrel export; package.json zero new deps; grep predicates 74-01-12..14; bundle audit confirms tree-shake-clean |
        | FU-05 | PASSED | This document; primary sources cited; split test strategy enumerated; stories/sf-file-upload.stories.tsx DragActive story |
        | TST-03 | PASSED | tests/v1.10-phase74-sf-file-upload.spec.ts (10 tests) + tests/v1.10-phase74-sf-file-upload-axe.spec.ts (6 scans) all green |
        | TST-04 | PASSED | This document; permanent gap documented; no vacuous Playwright drop test (predicate 74-02-08 = 0) |

        ---

        ## Sign-Off

        Verified: 2026-05-02
        Phase 74 status: passed (pending user sign-off on M-01..M-07 manual checks)

    (3) Update `.planning/phases/74-sffileupload/74-VALIDATION.md` frontmatter:

        - Change `status: draft` → `status: passed`
        - Change `nyquist_compliant: false` → `nyquist_compliant: true`
        - Change `wave_0_complete: false` → `wave_0_complete: true`

    Update each per-task verification map row in the table — set Status column from `⬜ pending` to `✅ green` for rows where the predicate is now satisfied; leave entries needing executor measurement (e.g., 74-02-09 bundle audit) with the captured measurement value. Update File Exists column from `⬜ W0` to `✅ W0` for files that now exist.

    Worktree leakage check: `git status --porcelain` — should show only the in-scope files for Plan 02 (74-VERIFICATION.md new + 74-VALIDATION.md modified + 3 test/story files added in Tasks 1-3).

    Commit: `Docs(74-02): 74-VERIFICATION.md split test strategy + bundle audit + 74-VALIDATION nyquist_compliant`
  </action>
  <verify>
    <automated>test -f .planning/phases/74-sffileupload/74-VERIFICATION.md && grep -q "dataTransfer.files" .planning/phases/74-sffileupload/74-VERIFICATION.md && grep -q "crbug.com/531834" .planning/phases/74-sffileupload/74-VERIFICATION.md && grep -q "microsoft/playwright" .planning/phases/74-sffileupload/74-VERIFICATION.md && grep -q "WHATWG" .planning/phases/74-sffileupload/74-VERIFICATION.md && grep -q "Permanent" .planning/phases/74-sffileupload/74-VERIFICATION.md && grep -q "Won.*Fix\\|WontFix" .planning/phases/74-sffileupload/74-VERIFICATION.md && grep -q "clipboardData.files" .planning/phases/74-sffileupload/74-VERIFICATION.md && grep -q "Track 1" .planning/phases/74-sffileupload/74-VERIFICATION.md && grep -q "Track 2" .planning/phases/74-sffileupload/74-VERIFICATION.md && grep -q "DragActive" .planning/phases/74-sffileupload/74-VERIFICATION.md && grep -q "nyquist_compliant: true" .planning/phases/74-sffileupload/74-VALIDATION.md && grep -q "status: passed" .planning/phases/74-sffileupload/74-VALIDATION.md && [ "$(grep -cE \"dispatchEvent.*['\\\"']drop['\\\"']\" tests/v1.10-phase74-sf-file-upload.spec.ts)" = "0" ] && pnpm exec playwright test tests/v1.10-phase74-sf-file-upload.spec.ts tests/v1.10-phase74-sf-file-upload-axe.spec.ts tests/v1.8-phase63-1-bundle-budget.spec.ts --project=chromium --reporter=line 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/74-sffileupload/74-VERIFICATION.md` exists (74-VALIDATION row 74-02-01)
    - File contains the literal substring `dataTransfer.files` (74-VALIDATION row 74-02-01; FU-05 contract)
    - File contains primary source citations: `crbug.com/531834` AND `microsoft/playwright/issues/13362` (or `microsoft/playwright#13362`) AND `WHATWG` (74-VALIDATION row 74-02-02)
    - File contains the literal substring `Permanent` (or `permanent`) AND `WontFix` or `Won't Fix` (74-VALIDATION row 74-02-01 / TST-04 contract)
    - File contains the literal substring `clipboardData.files` — same-class gap noted
    - File contains the literal substring `Track 1` AND `Track 2` (split test strategy headings)
    - File contains the literal substring `DragActive` referencing the Storybook story
    - File contains a `Per-Requirement Closeout` table with all 7 REQ-IDs (FU-01..05, TST-03, TST-04) marked PASSED
    - File contains a `Bundle Audit` section with the tree-shake audit script and PASS evidence
    - File contains a `Manual Verification` table with ≥ 5 manual-only checks (M-01..M-07 listed)
    - `.planning/phases/74-sffileupload/74-VALIDATION.md` frontmatter contains `nyquist_compliant: true` (74-VALIDATION row 74-02-* group)
    - `.planning/phases/74-sffileupload/74-VALIDATION.md` frontmatter contains `status: passed`
    - Per-task verification map rows updated to green status (verifiable by `grep -c '✅' .planning/phases/74-sffileupload/74-VALIDATION.md` ≥ 14 — at least one green checkmark per row)
    - `tests/v1.10-phase74-sf-file-upload.spec.ts` contains ZERO `page.dispatchEvent('drop', ...)` calls (74-VALIDATION row 74-02-08; TST-04 contract enforced)
    - All test specs pass — `pnpm exec playwright test tests/v1.10-phase74-sf-file-upload.spec.ts tests/v1.10-phase74-sf-file-upload-axe.spec.ts tests/v1.8-phase63-1-bundle-budget.spec.ts --project=chromium` exits 0 (74-VALIDATION row 74-02-09)
    - Tree-shake audit script returns PASS (homepage manifest does not contain `sf-file-upload` substring)
    - Homepage First Load JS ≤ 200 KB after clean ANALYZE build (recorded in 74-VERIFICATION.md Bundle Audit section)
    - `git log -1 --format=%s` matches pattern `Docs\\(74-02\\):.*VERIFICATION`
  </acceptance_criteria>
  <done>
    74-VERIFICATION.md ships as the canonical FU-05 / TST-04 deliverable. Permanent dataTransfer.files Chromium gap is documented with three primary source citations (Playwright issue, Chromium bug, WHATWG spec). Same-class clipboardData.files gap noted. Split test strategy enumerated across 3 tracks (Playwright setInputFiles + Storybook play() Chromatic + axe-core WCAG AA). 7 manual-verification checks documented. Bundle audit captured: clean ANALYZE build under 200 KB; SFFileUpload tree-shake-clean (absent from homepage manifest). 74-VALIDATION.md frontmatter set to `nyquist_compliant: true` and `status: passed`. All per-task rows green.
  </done>
</task>

</tasks>

<verification>
End-to-end verification of Plan 02:

(1) Test files exist and pass:

    test -f tests/v1.10-phase74-sf-file-upload.spec.ts
    test -f tests/v1.10-phase74-sf-file-upload-axe.spec.ts
    pnpm exec playwright test tests/v1.10-phase74-sf-file-upload.spec.ts tests/v1.10-phase74-sf-file-upload-axe.spec.ts --project=chromium --reporter=line

Expected: file checks succeed; ≥16 tests green (≥10 acceptance + ≥6 axe).

(2) Storybook stories exist and TypeScript compiles:

    test -f stories/sf-file-upload.stories.tsx
    grep -q "export const DragActive" stories/sf-file-upload.stories.tsx
    grep -q "fireEvent.dragOver" stories/sf-file-upload.stories.tsx
    pnpm exec tsc --noEmit

Expected: file exists, DragActive story present, 0 TS errors.

(3) 74-VERIFICATION.md ships with all required content:

    test -f .planning/phases/74-sffileupload/74-VERIFICATION.md
    grep -q "dataTransfer.files" .planning/phases/74-sffileupload/74-VERIFICATION.md
    grep -q "crbug.com/531834" .planning/phases/74-sffileupload/74-VERIFICATION.md
    grep -q "microsoft/playwright" .planning/phases/74-sffileupload/74-VERIFICATION.md
    grep -q "Permanent" .planning/phases/74-sffileupload/74-VERIFICATION.md
    grep -q "clipboardData.files" .planning/phases/74-sffileupload/74-VERIFICATION.md

Expected: all 6 succeed.

(4) TST-04 contract — no vacuous drop test:

    grep -cE "dispatchEvent.*['\"]drop['\"]" tests/v1.10-phase74-sf-file-upload.spec.ts

Expected: 0.

(5) Tree-shake audit clean:

    rm -rf .next/cache .next
    ANALYZE=true pnpm build > /tmp/74-build.log 2>&1
    node -e "
    const fs = require('fs');
    let homeStr = '';
    try { homeStr = JSON.stringify(JSON.parse(fs.readFileSync('.next/build-manifest.json','utf8')).pages['/']||[]); } catch(e) {
      homeStr = JSON.stringify(JSON.parse(fs.readFileSync('.next/app-build-manifest.json','utf8')).pages['/page']||[]);
    }
    process.exit(/sf-file-upload/.test(homeStr) ? 1 : 0);
    "

Expected: exit 0.

(6) Bundle budget spec:

    pnpm exec playwright test tests/v1.8-phase63-1-bundle-budget.spec.ts --project=chromium --reporter=line

Expected: PASS.

(7) 74-VALIDATION.md frontmatter:

    grep -q "nyquist_compliant: true" .planning/phases/74-sffileupload/74-VALIDATION.md
    grep -q "status: passed" .planning/phases/74-sffileupload/74-VALIDATION.md

Expected: both succeed.

(8) Worktree clean:

    git status --porcelain

Expected: empty (all 5 in-scope files committed).
</verification>

<success_criteria>
- [ ] `tests/v1.10-phase74-sf-file-upload.spec.ts` ships ≥10 Playwright tests across 3 describe blocks (FU-01/02/03); all green on chromium project
- [ ] `tests/v1.10-phase74-sf-file-upload-axe.spec.ts` ships ≥6 axe-core scans across 4 fixture states + aria-live presence; zero violations across rule set [button-name, progressbar, aria-valid-attr-value, color-contrast, region]
- [ ] `stories/sf-file-upload.stories.tsx` ships 8 stories (Default, MultiFile, WithProgress, ImagePreview, MimeReject, SizeReject, Disabled, DragActive); DragActive uses `play()` + `fireEvent.dragOver()` from `@storybook/test`; every story has `parameters.chromatic.delay`; ZERO `_forceDragActive` references
- [ ] `74-VERIFICATION.md` documents the permanent `dataTransfer.files` Chromium gap with 3 primary source citations (Playwright issue, Chromium bug, WHATWG spec); same-class `clipboardData.files` gap noted; split test strategy across 3 tracks; 7 manual-verification checks; per-requirement closeout for all 7 REQ-IDs
- [ ] TST-04 contract enforced: zero `page.dispatchEvent('drop', ...)` calls in the Playwright spec
- [ ] Bundle audit: clean ANALYZE build shows homepage `/` First Load JS ≤ 200 KB; tree-shake audit script confirms `sf-file-upload` substring absent from homepage manifest; bundle budget spec PASSES
- [ ] `74-VALIDATION.md` frontmatter set to `nyquist_compliant: true` and `status: passed`; all per-task rows updated to green status
- [ ] All 7 phase requirement IDs (FU-01..05, TST-03, TST-04) verified PASSED
- [ ] D-04 lock holds (next.config.ts unchanged); FU-04 zero-dep contract holds (package.json dependencies block unchanged)
- [ ] Worktree clean after all 4 task commits
- [ ] `pnpm exec tsc --noEmit` returns 0 errors across all modified files
</success_criteria>

<output>
After completion, create `.planning/phases/74-sffileupload/74-02-SUMMARY.md` documenting:
- The 4 commits across Tasks 1-4 (Test Playwright spec, Test axe spec, Test Storybook stories, Docs VERIFICATION + bundle audit + VALIDATION nyquist_compliant)
- Total test count: 10 acceptance + 6 axe = 16 tests; 8 Storybook stories
- Bundle headroom recorded: homepage `/` First Load JS = X KB (≤ 200 KB target)
- Confirmation that SFFileUpload is tree-shake-clean (absent from homepage manifest despite barrel-export)
- Confirmation that `dataTransfer.files` permanent gap is documented with 3 primary sources
- Confirmation that TST-04 contract enforced: no vacuous drop test in Playwright spec
- Phase 74 closeout: all 7 REQ-IDs (FU-01..05, TST-03, TST-04) PASSED; status: passed
- Forward link to Phase 75 (SFDateRangePicker — react-day-picker classNames isolation, SSR hydration guard, Pattern C barrel export); next command: `/pde:plan-phase 75`
</output>
