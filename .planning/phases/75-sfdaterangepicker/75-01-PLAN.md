---
phase: 75-sfdaterangepicker
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/sf/sf-date-range-picker.tsx
  - components/sf/index.ts
  - app/showcase/date-range-picker/page.tsx
  - public/r/sf-date-range-picker.json
  - public/r/registry.json
autonomous: true
requirements:
  - DR-01
  - DR-02
  - DR-03
  - DR-04
  - DR-05
  - DR-06
requirements_addressed:
  - DR-01
  - DR-02
  - DR-03
  - DR-04
  - DR-05
  - DR-06
gap_closure: false
must_haves:
  truths:
    - "A consumer renders SFDateRangePicker with mode=\"range\" — the popover trigger is a read-only SFInput displaying a formatted range string ('SHORT_MONTH DAY, YYYY — SHORT_MONTH DAY, YYYY') or empty placeholder; clicking the trigger or pressing Enter/Space when focused opens an SFPopoverContent containing SFCalendarLazy in mode=\"range\""
    - "A consumer renders SFDateRangePicker and selecting a range start day + range end day applies classNames keys range_start (start boundary), range_middle (interior days), and range_end (end boundary) to the corresponding day-cell DOM nodes; selected boundaries also receive bg-primary text-primary-foreground rounded-none classes"
    - "A consumer renders SFDateRangePicker with mode=\"single\" — only one date highlights with the selected classNames key (bg-primary text-primary-foreground rounded-none); the trigger displays a single formatted date string (not a range)"
    - "A consumer renders SFDateRangePicker with fromDate, toDate, and a disabled prop (boolean OR (date) => boolean predicate) — these are forwarded verbatim to SFCalendarLazy and react-day-picker enforces the bounds + disabled-day rendering"
    - "A consumer renders SFDateRangePicker with presets={[{label:'LAST 7 DAYS', getValue: () => ({from: subDays(new Date(), 7), to: new Date()})}, ...]} — presets render as a left-rail <ul role=\"list\"> of SFButton variant=\"ghost\" buttons inside SFPopoverContent; clicking a preset calls onValueChange(preset.getValue()) AND closes the popover (setOpen(false))"
    - "A consumer renders SFDateRangePicker with withTime={true} — two inline <SFInput type=\"time\"> elements render below the calendar inside the popover with aria-label=\"Start time\" and aria-label=\"End time\"; their values flow through startTime / endTime props with onStartTimeChange / onEndTimeChange callbacks; NO SFTimePicker primitive is created (D-06)"
    - "A consumer renders SFDateRangePicker with locale={fr} (or any date-fns Locale object) — the Locale type is imported via `import type { Locale } from 'date-fns/locale'` (type-only; verbatimModuleSyntax-safe); the type-only import line is the ONLY date-fns reference in the file; zero runtime date-fns code lands in the bundle"
    - "Component is 'use client'; new Date() appears ONLY inside useMemo (for defaultMonth) AND inside preset getValue() thunks supplied by consumers (executed only on click) — NEVER at module scope, NEVER directly in the function body outside a hook/thunk; SSR hydration emits zero warnings on the showcase route (verified by Plan 02)"
    - "Component composes SFCalendarLazy (P3 lazy boundary inherited — react-day-picker stays out of homepage First Load JS) + SFPopover/SFPopoverTrigger/SFPopoverContent + SFInput; trigger has aria-haspopup=\"dialog\" and aria-expanded={open}; component contains ZERO `import 'react-day-picker/dist/style.css'` lines; styling is exclusively via the classNames prop with rounded-none on every key (range_start, range_middle, range_end, selected, today, day, day_button, month_grid)"
    - "SFDateRangePicker is exported from components/sf/index.ts barrel (Pattern C — D-01); barrel remains directive-free (no 'use client' line introduced); SFCalendarLazy is NOT promoted to the barrel (it stays as direct-import P3 lazy per its existing contract); a registry entry for sf-date-range-picker lands in public/r/registry.json AND public/r/sf-date-range-picker.json same-commit (REG-01 same-commit rule; meta.layer=\"frame\", meta.pattern=\"C\")"
    - "Showcase fixture at app/showcase/date-range-picker/page.tsx mounts ≥3 sections (uncontrolled range-mode default, controlled range-mode with presets toggle, withTime mode) and is consumable by Plan 02's Playwright hydration spec + DCE chunk-grep proof; the route MUST exist as a separate consumer-route directory so the chunk-grep proof can verify react-day-picker IS in app/showcase/date-range-picker chunks but ABSENT from homepage chunks"
  artifacts:
    - path: "components/sf/sf-date-range-picker.tsx"
      provides: "Pattern C SFDateRangePicker component — SFCalendarLazy + SFPopover + SFInput composition; controlled value+onValueChange API; mode='range'|'single'; fromDate/toDate/disabled bounds; presets with thunk getValue; withTime variant via two inline <SFInput type='time'>; locale type-only pass-through; classNames-only styling with rounded-none on every key + bg-primary/text-primary-foreground for selected; SSR-safe new Date() inside useMemo only; no react-day-picker stylesheet import; no runtime date-fns import"
      exports: ["SFDateRangePicker", "type DateRange", "type SFDateRangePreset", "type SFDateRangePickerProps"]
      min_lines: 220
    - path: "components/sf/index.ts"
      provides: "Barrel export for SFDateRangePicker (Pattern C) — single export line added near the existing SFFileUpload export; barrel remains directive-free"
      contains: "export { SFDateRangePicker"
    - path: "app/showcase/date-range-picker/page.tsx"
      provides: "Showcase route fixture — 'use client' page with ≥3 sections (uncontrolled range, controlled range with presets, withTime); testid anchors fixture-uncontrolled-range, fixture-controlled-presets, fixture-withtime; consumer route exists separately so Plan 02 chunk-grep can prove react-day-picker IS bundled into this route's chunks but ABSENT from homepage chunks (DCE proof requires consumer-route witness)"
      exports: ["default"]
      min_lines: 110
    - path: "public/r/sf-date-range-picker.json"
      provides: "Standalone registry-item file (mirrors sf-file-upload.json shape) — meta.layer=frame, meta.pattern=C, registryDependencies includes calendar/popover/input/button"
      contains: "\"name\": \"sf-date-range-picker\""
    - path: "public/r/registry.json"
      provides: "New items[] entry for sf-date-range-picker — same-commit cohort with component file (REG-01); items count rises from 56 to 57"
      contains: "\"name\": \"sf-date-range-picker\""
  key_links:
    - from: "components/sf/sf-date-range-picker.tsx"
      to: "components/sf/sf-calendar-lazy.tsx"
      via: "direct import — SFCalendarLazy is the P3 lazy boundary; composing it (NOT SFCalendar directly) is what keeps react-day-picker out of homepage First Load JS"
      pattern: "from ['\"]@/components/sf/sf-calendar-lazy['\"]"
    - from: "components/sf/sf-date-range-picker.tsx"
      to: "components/sf/sf-popover.tsx + components/sf/sf-input.tsx + components/sf/sf-button.tsx"
      via: "barrel import from @/components/sf — SFPopover/SFPopoverTrigger/SFPopoverContent + SFInput + SFButton"
      pattern: "from ['\"]@/components/sf['\"]"
    - from: "components/sf/sf-date-range-picker.tsx"
      to: "date-fns/locale (TYPE ONLY)"
      via: "import type { Locale } from 'date-fns/locale' — TS erases at emit; zero runtime cost"
      pattern: "^import type \\{ Locale \\} from 'date-fns/locale'"
    - from: "components/sf/index.ts"
      to: "components/sf/sf-date-range-picker.tsx"
      via: "barrel export — Pattern C; DR-05 acceptance"
      pattern: "export \\{ SFDateRangePicker"
    - from: "app/showcase/date-range-picker/page.tsx"
      to: "components/sf/sf-date-range-picker.tsx"
      via: "barrel import — fixture mounts SFDateRangePicker via @/components/sf"
      pattern: "from ['\"]@/components/sf['\"]"
    - from: "package.json dependencies"
      to: "ABSENCE of new deps"
      via: "DR-05 zero-new-deps; react-day-picker + date-fns ALREADY present (verified package.json: ^9.14.0 + ^4.1.0)"
      pattern: "git diff HEAD -- package.json | grep -cE '^\\+.*\"' (= 0 new prod deps)"
---

<objective>
Implement SFDateRangePicker as a Pattern C composition — SFCalendarLazy (inherited P3 lazy boundary) + SFPopover/SFPopoverTrigger/SFPopoverContent + SFInput (read-only trigger) + presets left-rail using SFButton + optional withTime variant using two inline `<SFInput type="time">`. Controlled-only API via `value: DateRange | undefined` + `onValueChange`. Locale is a type-only pass-through (`import type { Locale } from 'date-fns/locale'`) — zero runtime date-fns. SSR-safe: `new Date()` ONLY inside `useMemo` (defaultMonth) and consumer-supplied preset thunks. No `import 'react-day-picker/dist/style.css'` anywhere. classNames-only styling with `rounded-none` on every key and `bg-primary text-primary-foreground` on selected/range_start/range_end. Export from `sf/index.ts` barrel and add same-commit registry entries (`public/r/sf-date-range-picker.json` standalone + new `items[]` entry in `public/r/registry.json`). Author the showcase fixture at `app/showcase/date-range-picker/page.tsx` so Plan 02's Playwright hydration spec and DCE chunk-grep proof can mount/measure it.

Purpose: Deliver DR-01 (range mode + popover trigger + range_start/middle/end visual states), DR-02 (read-only SFInput trigger + popover composition + bounds + disabled + SSR-safe new Date()), DR-03 (presets API with lazy thunks + Locale type-only pass-through + controlled API), DR-04 (`withTime` with two inline `<SFInput type="time">`; no SFTimePicker), DR-05 (Pattern C barrel export + zero new runtime deps + SFCalendarLazy composition), DR-06 (no react-day-picker stylesheet import + classNames-only with rounded-none + bg-primary/text-primary-foreground). TST-03 (Playwright hydration spec + axe spec) ships in Plan 02.

Output: 5 files modified — new `components/sf/sf-date-range-picker.tsx` ships the SFDateRangePicker component, `components/sf/index.ts` adds the barrel export, new `app/showcase/date-range-picker/page.tsx` ships the showcase fixture, new `public/r/sf-date-range-picker.json` ships the standalone registry-item file, `public/r/registry.json` gains a new items[] entry.
</objective>

<execution_context>
@/Users/greyaltaer/.claude/pde-os/engines/gsd/workflows/execute-plan.md
@/Users/greyaltaer/.claude/pde-os/engines/gsd/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/75-sfdaterangepicker/75-RESEARCH.md
@.planning/phases/75-sfdaterangepicker/75-VALIDATION.md
@CLAUDE.md
@.planning/LOCKDOWN.md
@components/sf/index.ts
@components/sf/sf-calendar.tsx
@components/sf/sf-calendar-lazy.tsx
@components/sf/sf-popover.tsx
@components/sf/sf-input.tsx
@components/sf/sf-button.tsx
@components/sf/sf-file-upload.tsx
@public/r/registry.json
@public/r/sf-file-upload.json
@next.config.ts

<interfaces>
<!-- SFCalendarLazy API (verified at components/sf/sf-calendar-lazy.tsx + sf-calendar.tsx) -->

import { SFCalendarLazy } from "@/components/sf/sf-calendar-lazy";

// SFCalendarLazy is dynamic({ ssr: false }) wrapper of SFCalendar.
// SFCalendar is React.ComponentProps<typeof Calendar> from @/components/ui/calendar
// — i.e. it accepts ALL react-day-picker v9.14.0 props, including:
//   mode: "single" | "range" | "multiple"
//   selected: Date | DateRange | Date[]
//   onSelect: (selected, triggerDate, modifiers, e) => void
//   defaultMonth: Date
//   fromDate?: Date
//   toDate?: Date
//   disabled?: boolean | Date | Date[] | DateBefore | DateAfter | DateInterval | DateRange | ((date: Date) => boolean)
//   locale?: Locale (from date-fns/locale)
//   classNames?: Partial<Record<ClassNames, string>>  // RDP v9.14.0 keys
//
// SFCalendar already merges defaults: rounded-none border-2 border-border [--cell-radius:0px]
// + classNames: { dropdown_root, caption_label, weekday, day, range_start,
//                 range_middle, range_end, today } all set to rounded-none.
// The classNames prop on SFCalendarLazy MERGES into these defaults — overriding
// is additive (e.g. you can add bg-primary text-primary-foreground to range_start).

<!-- SFPopover API (verified at components/sf/sf-popover.tsx) -->

import { SFPopover, SFPopoverTrigger, SFPopoverContent } from "@/components/sf";

<SFPopover open={open} onOpenChange={setOpen}>
  <SFPopoverTrigger asChild>
    {/* trigger element — focusable; Radix manages aria-expanded + aria-controls */}
  </SFPopoverTrigger>
  <SFPopoverContent align="start" className="...">
    {/* SFPopoverContent applies: rounded-none border-2 border-foreground bg-background shadow-none ring-0 */}
    {/* + cn() merges any consumer className */}
  </SFPopoverContent>
</SFPopover>

// Radix Popover built-ins:
//   - Esc closes the popover, returns focus to trigger
//   - Outside-click closes the popover
//   - aria-haspopup, aria-expanded, aria-controls auto-wired

<!-- SFInput API (verified at components/sf/sf-input.tsx) -->

import { SFInput } from "@/components/sf";

<SFInput
  readOnly
  aria-haspopup="dialog"
  aria-expanded={open}
  value={displayValue}
  placeholder="SELECT DATE RANGE"
  className="cursor-pointer"  // signals click-to-open; readOnly suppresses caret
/>

// SFInput applies: sf-focusable sf-border-draw-focus font-mono uppercase tracking-wider text-xs
//                  border-2 border-foreground bg-background focus-visible:ring-0
//                  placeholder:text-muted-foreground placeholder:uppercase placeholder:tracking-wider

<!-- SFButton API (verified at components/sf/sf-button.tsx via barrel export at index.ts:9) -->

import { SFButton } from "@/components/sf";

<SFButton variant="ghost" size="sm" onClick={...} className="...">LAST 7 DAYS</SFButton>
// (SFButton accepts variant + size; ghost variant has no fill — composes well in left-rail panel)

<!-- DateRange type (LOCAL, not re-exported from react-day-picker per D-07) -->

export type DateRange = { from: Date | undefined; to: Date | undefined };

<!-- SFDateRangePreset type (D-08 lazy thunk pattern) -->

export interface SFDateRangePreset {
  label: string;
  getValue: () => DateRange;  // thunk — called ONLY on user click; never at module scope
}

<!-- Locale type-only import (D-05) -->

import type { Locale } from "date-fns/locale";
// TS erases this at emit. The component file MUST contain ZERO non-type-only
// imports from any 'date-fns' subpath (verified by Plan 02 chunk-grep).

<!-- Reference impl shape (75-RESEARCH.md §Reference Implementation) -->

"use client";

export function SFDateRangePicker(props: SFDateRangePickerProps) {
  const { value, onValueChange, mode = "range", fromDate, toDate, disabled,
          presets, withTime, startTime, endTime,
          onStartTimeChange, onEndTimeChange, locale,
          placeholder = "SELECT DATE RANGE", align = "start", className } = props;

  const [open, setOpen] = useState(false);

  // SSR guard: new Date() ONLY inside useMemo (D-03)
  const defaultMonth = useMemo(() => new Date(), []);

  const displayValue = formatRange(value, mode);

  return (
    <SFPopover open={open} onOpenChange={setOpen}>
      <SFPopoverTrigger asChild>
        <SFInput
          readOnly
          aria-haspopup="dialog"
          aria-expanded={open}
          value={displayValue}
          placeholder={placeholder}
          className={cn("cursor-pointer", className)}
          data-testid="sf-date-range-picker-trigger"
        />
      </SFPopoverTrigger>
      <SFPopoverContent align={align} className="p-0 w-auto flex flex-row rounded-none">
        {presets && presets.length > 0 && (
          <ul role="list" className="border-r-2 border-border p-[var(--sfx-space-3)] flex flex-col gap-[var(--sfx-space-1)] min-w-[140px]">
            {presets.map((preset) => (
              <li key={preset.label}>
                <SFButton
                  variant="ghost"
                  size="sm"
                  className="w-full justify-start text-left font-mono uppercase tracking-wider text-xs rounded-none"
                  onClick={() => {
                    onValueChange?.(preset.getValue());  // thunk runs ONLY here — client click handler
                    setOpen(false);                       // D-08 close-on-select for presets
                  }}
                >
                  {preset.label}
                </SFButton>
              </li>
            ))}
          </ul>
        )}
        <div className="p-[var(--sfx-space-3)]">
          <SFCalendarLazy
            mode={mode}
            selected={value as never}  // RDP discriminates on `mode`; cast at composition boundary
            onSelect={(v) => onValueChange?.(v as DateRange | undefined)}
            defaultMonth={value?.from ?? defaultMonth}
            fromDate={fromDate}
            toDate={toDate}
            disabled={disabled}
            locale={locale}
            classNames={{
              range_start: "rounded-none bg-primary text-primary-foreground",
              range_middle: "rounded-none bg-primary/20",
              range_end: "rounded-none bg-primary text-primary-foreground",
              selected: "rounded-none bg-primary text-primary-foreground",
              today: "rounded-none underline",
              day: "rounded-none",
              day_button: "rounded-none",
              month_grid: "rounded-none",
            }}
          />
          {withTime && (
            <div className="border-t-2 border-border pt-[var(--sfx-space-3)] mt-[var(--sfx-space-3)] flex gap-[var(--sfx-space-2)]">
              <SFInput
                type="time"
                value={startTime ?? ""}
                onChange={(e) => onStartTimeChange?.(e.target.value)}
                aria-label="Start time"
              />
              <SFInput
                type="time"
                value={endTime ?? ""}
                onChange={(e) => onEndTimeChange?.(e.target.value)}
                aria-label="End time"
              />
            </div>
          )}
        </div>
      </SFPopoverContent>
    </SFPopover>
  );
}

function formatRange(value: DateRange | undefined, mode: "range" | "single"): string {
  if (!value?.from) return "";
  const fmt = (d: Date) => d.toLocaleDateString("en-US", { month: "short", day: "numeric", year: "numeric" });
  if (mode === "single" || !value.to) return fmt(value.from);
  return `${fmt(value.from)} — ${fmt(value.to)}`;
}
</interfaces>

<threat_model>
ASVS L1 — Phase 75 Plan 01 threat surface. SFDateRangePicker is a UI primitive with no network/auth/PII surface; trust boundary is consumer-supplied props per Pattern C policy.

| Threat ID | Threat | Severity | Mitigation |
|-----------|--------|----------|------------|
| T-75-01 | XSS via preset labels — consumer passes `<script>alert(1)</script>` as `preset.label` | LOW | React auto-escapes string children; `{preset.label}` renders as a text node only. Acceptance criterion: file contains ZERO raw-HTML inject prop usages — `grep -c 'dangerously' components/sf/sf-date-range-picker.tsx` = 0. |
| T-75-02 | Prototype pollution via uncontrolled `value` shape — consumer passes `{ __proto__: { polluted: true } }` instead of `{ from, to }` | LOW | Component reads only `value.from` and `value.to` properties via destructuring; never spreads or merges via `Object.assign`/`{ ...value }` into a default-prototype object; passes `value` straight through to SFCalendarLazy which is also property-read only. Trust boundary documented in JSDoc: "value is consumer-supplied; component performs no deep merge or property spread." |
| T-75-03 | DoS via malformed preset `getValue()` thunk — consumer's thunk throws OR returns malformed shape | LOW | The preset thunk is called only inside the onClick handler. If the thunk throws, React error boundary at the showcase route catches it; component does NOT wrap in try/catch (trust boundary — consumer owns thunk correctness). JSDoc states verbatim: "preset.getValue() is consumer-supplied; thunk MUST return DateRange-shaped object; component does not validate the return shape." |
| T-75-04 | SSR hydration mismatch — `new Date()` at module scope or in component body would produce different UTC vs local-time values between server render and client hydration, triggering React hydration warnings | HIGH | D-03 enforced: `new Date()` ONLY inside `useMemo(() => new Date(), [])` for defaultMonth; preset thunks (`getValue: () => ({ from: subDays(new Date(), 7), to: new Date() })`) are consumer-owned and execute only inside the onClick handler (client-only). Acceptance criterion: every `new Date()` occurrence in `components/sf/sf-date-range-picker.tsx` is structurally inside a `useMemo` callback or a function-body that only runs in event handlers (NOT module-scope, NOT component-body-top-level). Plan 02 verifies via Playwright hydration test. |
| T-75-05 | react-day-picker stylesheet leakage — accidental `import 'react-day-picker/dist/style.css'` line would inject unlayered CSS bypassing `@layer signalframeux` | HIGH | Acceptance criterion: `grep -c "react-day-picker/dist" components/sf/sf-date-range-picker.tsx` = 0 (D-04). Plan 02 expands to all of `components/ app/`. |
| T-75-06 | date-fns runtime escape — accidental `import { fr } from 'date-fns/locale'` (non-type-only) would drag date-fns runtime into the lazy chunk and potentially the homepage chunk via cross-chunk hoisting | HIGH | Acceptance criterion: `grep -cE "^import type \\{ Locale \\} from 'date-fns/locale'" components/sf/sf-date-range-picker.tsx` ≥ 1 AND `grep -cE "^import \\{[^}]*\\} from 'date-fns" components/sf/sf-date-range-picker.tsx` = 0. Plan 02 chunk-grep DCE proof double-confirms `date-fns` substring absent from homepage chunks. |
| T-75-07 | classNames key drift — react-day-picker silently ignores unknown classNames keys; renaming `range_middle` upstream would silently break range visualization | MEDIUM | Acceptance criterion: `grep -E "range_start\|range_middle\|range_end" components/sf/sf-date-range-picker.tsx \| wc -l` ≥ 3. Plan 02 Playwright spec asserts the actual DOM classes appear after a range selection (functional verification, not just structural). RESEARCH.md §Q12 R1 documents the upgrade-checklist note. |
| T-75-08 | Worktree leakage (memory: feedback_agent_worktree_leakage) — file authored in `.claude/worktrees/agent-N/` leaks to main tree as untracked, then committed by mistake | MEDIUM | Pre-task `git status --porcelain components/sf/sf-date-range-picker.tsx` check; STOP if any output (file MUST be authored fresh in-phase). |
| T-75-09 | Bundle bloat: a third-party date-range library (`react-date-range`, `react-datepicker`) sneaks into deps | HIGH | DR-05 zero-new-deps contract. Acceptance criterion: `git diff HEAD -- package.json` shows zero additions to `dependencies` block. JSDoc anti-features block enumerates rejected libraries verbatim. |
| T-75-10 | Registry drift: registry.json items[] entry not added same-commit as sf-date-range-picker.tsx — REG-01 same-commit rule violated | MEDIUM | Single-commit-cohort acceptance criterion: `git show <commit> --stat` includes BOTH `components/sf/sf-date-range-picker.tsx` AND `public/r/registry.json` AND `public/r/sf-date-range-picker.json`. |
| T-75-11 | SFCalendar (NOT SFCalendarLazy) imported by accident — pulls react-day-picker into the barrel chunk, dragging it into homepage First Load JS | HIGH | Acceptance criterion: `grep -c "from ['\"]@/components/sf/sf-calendar-lazy['\"]" components/sf/sf-date-range-picker.tsx` ≥ 1 AND `grep -cE "from ['\"]@/components/sf/sf-calendar['\"]" components/sf/sf-date-range-picker.tsx` = 0 (no direct SFCalendar import). Plan 02 chunk-grep is the empirical confirmation. |

No SSR-time secret/token surface. Component is `'use client'`. No fetch/XHR. No raw-HTML threading; preset labels and time values render via React text-node escaping only.
</threat_model>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Implement SFDateRangePicker component (DR-01 + DR-02 + DR-03 + DR-04 + DR-05 + DR-06)</name>
  <files>components/sf/sf-date-range-picker.tsx</files>
  <read_first>
    - components/sf/sf-date-range-picker.tsx (verify file does NOT yet exist via `git status --porcelain components/sf/sf-date-range-picker.tsx` — if it returns any output, STOP, worktree leakage suspect)
    - components/sf/sf-calendar.tsx (current classNames defaults: rounded-none on dropdown_root, caption_label, weekday, day, range_start, range_middle, range_end, today; the SFCalendar default merges into the consumer's classNames prop via spread — additive override pattern)
    - components/sf/sf-calendar-lazy.tsx (P3 lazy boundary — `dynamic(() => import('@/components/sf/sf-calendar').then(m => ({default: m.SFCalendar})), { ssr: false, loading: () => <SFSkeleton /> })`; consumer accepts ALL React.ComponentProps<typeof SFCalendarDynamic>)
    - components/sf/sf-popover.tsx (SFPopover/SFPopoverTrigger/SFPopoverContent API; SFPopoverContent merges `rounded-none border-2 border-foreground bg-background shadow-none ring-0` with consumer className via cn())
    - components/sf/sf-input.tsx (sf-focusable sf-border-draw-focus font-mono uppercase tracking-wider text-xs border-2 border-foreground bg-background; readOnly + aria-haspopup pattern reference)
    - components/sf/sf-file-upload.tsx (Pattern C reference — JSDoc anti-features block format, controlled-API discipline, barrel-export contract, file-bottom Pattern C contract reminder comment block)
    - components/sf/index.ts (verify SFDateRangePicker NOT yet exported — grep should return 0 hits before this task)
    - .planning/phases/75-sfdaterangepicker/75-RESEARCH.md (entire file — Q1 classNames keys, Q2 SSR hydration pattern, Q3 CSS isolation proof, Q4 trigger pattern, Q5 presets API, Q6 withTime variant, Q7 controlled-only API, Q8 Locale type-only, Q9 bundle accounting, Q11 CRT/aesthetic; Locked Decisions D-01..D-13)
    - .planning/phases/75-sfdaterangepicker/75-VALIDATION.md §Per-Requirement Predicates DR-01..DR-06 (the EXACT grep predicates that this task must satisfy — copy them into acceptance criteria verbatim)
    - CLAUDE.md (blessed spacing 4/8/12/16/24/32/48/64/96; zero border-radius; OKLCH only; FRAME/SIGNAL register; SF-prefix barrel)
    - .planning/LOCKDOWN.md §2.5 (R-60 borderless-first); §9 (R-64-b double-ring focus, R-64-d Space-key focus guard for role=button — n/a here, no role=button drop zone; trigger is SFInput which is a native input)
    - package.json (verify react-day-picker ^9.14.0 + date-fns ^4.1.0 ALREADY present — DR-05 zero-new-deps contract; this task introduces ZERO new entries to dependencies)
  </read_first>
  <action>
    Run `git status --porcelain components/sf/sf-date-range-picker.tsx` first. If output, STOP — worktree leakage. Run `grep -c "SFDateRangePicker" components/sf/index.ts` — if ≥ 1, STOP — barrel was modified out-of-band.

    Create `components/sf/sf-date-range-picker.tsx`. The file MUST satisfy ALL acceptance criteria below.

    File structure (top to bottom):

    1. `"use client";` directive on line 1.

    2. JSDoc block immediately above the type definitions:

        /**
         * Date range picker — FRAME layer Pattern C composition.
         *
         * Read-only SFInput trigger + SFPopover panel containing SFCalendarLazy
         * (inherited P3 lazy boundary — react-day-picker stays out of homepage
         * First Load JS) + optional left-rail presets (SFButton) + optional
         * inline time inputs (two `<SFInput type="time">` when withTime=true).
         * Controlled-only API. classNames-only styling — zero stylesheet imports.
         *
         * ============================================================
         * ANTI-FEATURES (NOT shipped — by design, not by oversight)
         * ============================================================
         *
         * 1. Built-in date library (date-fns runtime / dayjs / luxon) — Locale
         *    is a TYPE-ONLY pass-through; consumers bring their own date library
         *    for preset thunks (`subDays`, `startOfMonth`, etc.). DR-05 contract.
         *
         * 2. SFTimePicker primitive extraction — Single v1.10 consumer; we use
         *    two inline `<SFInput type="time">` when withTime=true. D-06 lock.
         *
         * 3. Datetime mode / comparison range / time-only mode — DR-07 deferred
         *    to v1.11+. Triple-feature scope explosion.
         *
         * 4. Uncontrolled-mode `defaultValue` — Pattern C precedent (Phase 71-74)
         *    is controlled-only API for new components. Consumers manage state
         *    via useState + value/onValueChange.
         *
         * 5. react-day-picker stylesheet (`react-day-picker/dist/style.css`) —
         *    NEVER imported. All styling via classNames prop using --sfx-* token
         *    classes. D-04 lock; DR-06 contract.
         *
         * 6. Custom popover positioning — Radix Popover handles avoidCollisions,
         *    flip, and viewport-edge detection. Consumers pass `align` only.
         *
         * 7. Internal date validation / parsing — fromDate/toDate/disabled flow
         *    straight to react-day-picker which owns the date math. The
         *    component contains zero comparison-arithmetic on Date objects.
         *
         * ============================================================
         * KNOWN CAVEATS
         * ============================================================
         *
         * - `value` is consumer-supplied; the component reads only `value.from`
         *   and `value.to` (no spread, no Object.assign). Trust boundary: malformed
         *   `value` shapes are NOT validated by the component (Pattern C policy).
         *
         * - `preset.getValue()` is a consumer-supplied thunk — called ONLY inside
         *   the preset onClick handler (never at module scope, never at render).
         *   Consumer owns thunk correctness; component does not catch thrown
         *   exceptions (parent error boundary catches).
         *
         * - `Locale` is `import type` only — TS erases at emit. Zero date-fns
         *   runtime code lands in the bundle from this component. Consumers who
         *   want localization install date-fns themselves (peer dep of
         *   react-day-picker, so already in node_modules) and pass a Locale
         *   object: `import { fr } from 'date-fns/locale'; <SFDateRangePicker locale={fr} />`.
         *
         * - `range_middle` classNames key is the most upgrade-fragile predicate
         *   — a future react-day-picker minor that renames this key would
         *   silently fail (RDP ignores unknown classNames keys). Re-verify on
         *   any version bump.
         *
         * - `<SFInput type="time">` keyboard parity varies across browsers
         *   (Chrome step-arrows vs Safari spinner vs Firefox). Manual UAT item
         *   in 75-VALIDATION.md.
         *
         * @example  // controlled range mode
         * const [range, setRange] = useState<DateRange>({ from: undefined, to: undefined });
         * <SFDateRangePicker mode="range" value={range} onValueChange={setRange} />
         *
         * @example  // with presets (consumer owns date-fns)
         * import { subDays, startOfMonth } from "date-fns";
         * <SFDateRangePicker
         *   value={range}
         *   onValueChange={setRange}
         *   presets={[
         *     { label: "LAST 7 DAYS",  getValue: () => ({ from: subDays(new Date(), 7), to: new Date() }) },
         *     { label: "LAST 30 DAYS", getValue: () => ({ from: subDays(new Date(), 30), to: new Date() }) },
         *     { label: "THIS MONTH",   getValue: () => ({ from: startOfMonth(new Date()), to: new Date() }) },
         *   ]}
         * />
         *
         * @example  // with time variant
         * <SFDateRangePicker
         *   value={range}
         *   onValueChange={setRange}
         *   withTime
         *   startTime={startTime}
         *   endTime={endTime}
         *   onStartTimeChange={setStartTime}
         *   onEndTimeChange={setEndTime}
         * />
         */

    3. Imports — VERBATIM, in this order. The Locale import MUST be `import type` on its own line for verbatimModuleSyntax safety AND so the structural grep `^import type { Locale } from 'date-fns/locale'` matches:

        import { useMemo, useState } from "react";
        import type { Locale } from "date-fns/locale";
        import { SFCalendarLazy } from "@/components/sf/sf-calendar-lazy";
        import {
          SFPopover,
          SFPopoverTrigger,
          SFPopoverContent,
          SFInput,
          SFButton,
        } from "@/components/sf";
        import { cn } from "@/lib/utils";

       Note: SFCalendarLazy is imported VIA DIRECT PATH (not via `@/components/sf` barrel) because SFCalendarLazy is NOT exported from the barrel — it is a P3 lazy that consumers must direct-import. The other primitives (SFPopover/SFInput/SFButton) ARE in the barrel.

    4. Public types — exported (verbatim shape):

        export type DateRange = {
          from: Date | undefined;
          to: Date | undefined;
        };

        export interface SFDateRangePreset {
          label: string;
          /**
           * Lazy thunk — called ONLY inside the preset onClick handler
           * (client-only execution; SSR-safe). Consumers compose date-fns
           * helpers (subDays, startOfMonth, etc.) inside the thunk body.
           */
          getValue: () => DateRange;
        }

        export interface SFDateRangePickerProps {
          /** Controlled value. */
          value?: DateRange;
          /** Controlled change callback. */
          onValueChange?: (value: DateRange | undefined) => void;
          /** Selection mode — "range" (default) or "single". */
          mode?: "range" | "single";
          /** Lower date bound (inclusive). Forwarded to react-day-picker. */
          fromDate?: Date;
          /** Upper date bound (inclusive). Forwarded to react-day-picker. */
          toDate?: Date;
          /** Disabled state — boolean OR a (date) => boolean predicate. */
          disabled?: boolean | ((date: Date) => boolean);
          /** Optional left-rail presets. Each preset's getValue() is a thunk. */
          presets?: SFDateRangePreset[];
          /** When true, render two inline <SFInput type="time"> below the calendar. */
          withTime?: boolean;
          startTime?: string;
          endTime?: string;
          onStartTimeChange?: (v: string) => void;
          onEndTimeChange?: (v: string) => void;
          /** date-fns Locale object (TYPE-ONLY pass-through; zero runtime cost). */
          locale?: Locale;
          /** Trigger placeholder copy. Defaults to "SELECT DATE RANGE". */
          placeholder?: string;
          /** Popover alignment. Defaults to "start". */
          align?: "start" | "center" | "end";
          /** Trigger className override; merged via cn() after defaults. */
          className?: string;
        }

    5. Module-level helper function (BEFORE the component function):

        function formatRange(value: DateRange | undefined, mode: "range" | "single"): string {
          if (!value?.from) return "";
          const fmt = (d: Date) =>
            d.toLocaleDateString("en-US", { month: "short", day: "numeric", year: "numeric" });
          if (mode === "single" || !value.to) return fmt(value.from);
          return `${fmt(value.from)} — ${fmt(value.to)}`;
        }

       Note: `d.toLocaleDateString(...)` is called ONLY when `formatRange` is invoked from inside the component body (which is itself client-only via 'use client'). This is SSR-safe because the component re-renders on the client with the same locale — and the input `value` is consumer-controlled, not derived from `new Date()` at render time.

    6. Component function — controlled state, popover trigger, presets rail, calendar, optional time row:

        export function SFDateRangePicker(props: SFDateRangePickerProps) {
          const {
            value,
            onValueChange,
            mode = "range",
            fromDate,
            toDate,
            disabled,
            presets,
            withTime,
            startTime,
            endTime,
            onStartTimeChange,
            onEndTimeChange,
            locale,
            placeholder = "SELECT DATE RANGE",
            align = "start",
            className,
          } = props;

          const [open, setOpen] = useState(false);

          // SSR guard (D-03): new Date() ONLY inside useMemo — client-only via 'use client'.
          // defaultMonth is the calendar's initial visible month when no value is set.
          const defaultMonth = useMemo(() => new Date(), []);

          const displayValue = formatRange(value, mode);

          return (
            <SFPopover open={open} onOpenChange={setOpen}>
              <SFPopoverTrigger asChild>
                <SFInput
                  readOnly
                  aria-haspopup="dialog"
                  aria-expanded={open}
                  value={displayValue}
                  placeholder={placeholder}
                  className={cn("cursor-pointer", className)}
                  data-testid="sf-date-range-picker-trigger"
                />
              </SFPopoverTrigger>
              <SFPopoverContent
                align={align}
                className="p-0 w-auto flex flex-row rounded-none"
                data-testid="sf-date-range-picker-content"
              >
                {presets && presets.length > 0 && (
                  <ul
                    role="list"
                    className="border-r-2 border-border p-[var(--sfx-space-3)] flex flex-col gap-[var(--sfx-space-1)] min-w-[140px]"
                    data-testid="sf-date-range-picker-presets"
                  >
                    {presets.map((preset) => (
                      <li key={preset.label}>
                        <SFButton
                          variant="ghost"
                          size="sm"
                          className="w-full justify-start text-left font-mono uppercase tracking-wider text-xs rounded-none"
                          onClick={() => {
                            // D-08 lazy thunk: getValue() runs ONLY inside this onClick handler.
                            onValueChange?.(preset.getValue());
                            setOpen(false);
                          }}
                        >
                          {preset.label}
                        </SFButton>
                      </li>
                    ))}
                  </ul>
                )}
                <div className="p-[var(--sfx-space-3)]">
                  <SFCalendarLazy
                    mode={mode}
                    selected={value as never}
                    onSelect={(v: unknown) => onValueChange?.(v as DateRange | undefined)}
                    defaultMonth={value?.from ?? defaultMonth}
                    fromDate={fromDate}
                    toDate={toDate}
                    disabled={disabled}
                    locale={locale}
                    classNames={{
                      range_start: "rounded-none bg-primary text-primary-foreground",
                      range_middle: "rounded-none bg-primary/20",
                      range_end: "rounded-none bg-primary text-primary-foreground",
                      selected: "rounded-none bg-primary text-primary-foreground",
                      today: "rounded-none underline",
                      day: "rounded-none",
                      day_button: "rounded-none",
                      month_grid: "rounded-none",
                    }}
                  />
                  {withTime && (
                    <div
                      className="border-t-2 border-border pt-[var(--sfx-space-3)] mt-[var(--sfx-space-3)] flex gap-[var(--sfx-space-2)]"
                      data-testid="sf-date-range-picker-time-row"
                    >
                      <SFInput
                        type="time"
                        value={startTime ?? ""}
                        onChange={(e) => onStartTimeChange?.(e.target.value)}
                        aria-label="Start time"
                      />
                      <SFInput
                        type="time"
                        value={endTime ?? ""}
                        onChange={(e) => onEndTimeChange?.(e.target.value)}
                        aria-label="End time"
                      />
                    </div>
                  )}
                </div>
              </SFPopoverContent>
            </SFPopover>
          );
        }

    7. Append the Pattern C contract reminder comment block at end of file:

        // -----------------------------------------------------------------------
        // Pattern C contract reminder (DR-05 + DR-06):
        //   - SFDateRangePicker IS exported from components/sf/index.ts barrel
        //   - Zero new runtime npm deps — react-day-picker (^9.14.0) and date-fns
        //     (^4.1.0) ALREADY in package.json before this phase. date-fns is
        //     TYPE-ONLY (`import type { Locale }`) — zero runtime code lands
        //     in the bundle from this file.
        //   - Composes SFCalendarLazy (NOT SFCalendar directly) — react-day-picker
        //     stays in the deferred lazy chunk inherited from SFCalendarLazy's
        //     dynamic({ ssr: false }) boundary; absent from homepage First Load JS.
        //   - NEVER `import 'react-day-picker/dist/style.css'`. All styling via
        //     classNames prop with rounded-none on every key + bg-primary
        //     text-primary-foreground for selected/range_start/range_end +
        //     bg-primary/20 for range_middle.
        //   - new Date() ONLY inside useMemo (defaultMonth) and inside consumer
        //     preset thunks (executed only on click). NEVER at module scope.
        //   - Plan 02 chunk-grep DCE proof verifies react-day-picker IS in
        //     app/showcase/date-range-picker chunks but ABSENT from homepage
        //     chunks (pages-*.js, app-*.js, main-*.js).
        // -----------------------------------------------------------------------

    Spacing values used: `--sfx-space-1` (4 px), `--sfx-space-2` (8 px), `--sfx-space-3` (12 px). All blessed stops per CLAUDE.md.

    Border-radius: ZERO `rounded-*` classes other than `rounded-none` anywhere in the file (drop trigger, popover content, presets rail, calendar classNames, time row).

    Color tokens: `bg-primary`, `text-primary-foreground`, `bg-primary/20`, `border-border`. All slot tokens — no hardcoded hex/oklch/named colors.

    Filenames + preset labels render via React text-node escaping ONLY. Do not use any raw-HTML inject prop.

    Run `pnpm exec tsc --noEmit` and confirm 0 errors before commit.

    Commit: `Feat(75-01): SFDateRangePicker component (DR-01 range+popover, DR-02 bounds+disabled+SSR-safe, DR-03 presets+locale-type-only, DR-04 withTime, DR-05 SFCalendarLazy composition, DR-06 classNames-only)`
  </action>
  <verify>
    <automated>test -f components/sf/sf-date-range-picker.tsx && grep -q "^\"use client\";" components/sf/sf-date-range-picker.tsx && grep -q "export function SFDateRangePicker" components/sf/sf-date-range-picker.tsx && grep -q "export type DateRange" components/sf/sf-date-range-picker.tsx && grep -q "export interface SFDateRangePreset" components/sf/sf-date-range-picker.tsx && grep -q "export interface SFDateRangePickerProps" components/sf/sf-date-range-picker.tsx && [ "$(grep -cE '\brange_start\b' components/sf/sf-date-range-picker.tsx)" -ge 1 ] && [ "$(grep -cE '\brange_middle\b' components/sf/sf-date-range-picker.tsx)" -ge 1 ] && [ "$(grep -cE '\brange_end\b' components/sf/sf-date-range-picker.tsx)" -ge 1 ] && [ "$(grep -c 'react-day-picker/dist' components/sf/sf-date-range-picker.tsx)" = "0" ] && [ "$(grep -cE '\bbg-primary text-primary-foreground\b' components/sf/sf-date-range-picker.tsx)" -ge 1 ] && grep -q "SFCalendarLazy" components/sf/sf-date-range-picker.tsx && [ "$(grep -cE "from ['\\\"]@/components/sf/sf-calendar-lazy['\\\"]" components/sf/sf-date-range-picker.tsx)" -ge 1 ] && [ "$(grep -cE "from ['\\\"]@/components/sf/sf-calendar['\\\"]" components/sf/sf-date-range-picker.tsx)" = "0" ] && grep -q "SFPopover" components/sf/sf-date-range-picker.tsx && grep -q "SFInput" components/sf/sf-date-range-picker.tsx && grep -qE "readOnly" components/sf/sf-date-range-picker.tsx && grep -qE "aria-haspopup=\"dialog\"" components/sf/sf-date-range-picker.tsx && [ "$(grep -cE 'presets\??:\s*SFDateRangePreset' components/sf/sf-date-range-picker.tsx)" -ge 1 ] && [ "$(grep -cE 'getValue:\s*\(\)\s*=>' components/sf/sf-date-range-picker.tsx)" -ge 1 ] && grep -q 'role="list"' components/sf/sf-date-range-picker.tsx && [ "$(grep -cE "^import type \{ Locale \} from 'date-fns/locale'" components/sf/sf-date-range-picker.tsx)" -ge 1 ] && [ "$(grep -cE "^import \{[^}]*\} from 'date-fns" components/sf/sf-date-range-picker.tsx)" = "0" ] && [ "$(grep -cE 'withTime\??:\s*boolean' components/sf/sf-date-range-picker.tsx)" -ge 1 ] && [ "$(grep -c 'type="time"' components/sf/sf-date-range-picker.tsx)" -ge 2 ] && [ "$(grep -c 'SFTimePicker' components/sf/sf-date-range-picker.tsx)" = "0" ] && [ "$(grep -c 'rounded-none' components/sf/sf-date-range-picker.tsx)" -ge 4 ] && [ "$(grep -c 'fromDate' components/sf/sf-date-range-picker.tsx)" -ge 1 ] && [ "$(grep -c 'toDate' components/sf/sf-date-range-picker.tsx)" -ge 1 ] && [ "$(grep -c 'disabled' components/sf/sf-date-range-picker.tsx)" -ge 2 ] && [ "$(grep -c 'useMemo' components/sf/sf-date-range-picker.tsx)" -ge 1 ] && grep -q "Pattern C contract reminder" components/sf/sf-date-range-picker.tsx && [ "$(grep -c 'dangerously' components/sf/sf-date-range-picker.tsx)" = "0" ] && ! grep -E "rounded-(sm|md|lg|xl|2xl|full)" components/sf/sf-date-range-picker.tsx && ! grep -qE "#[0-9a-fA-F]{6}" components/sf/sf-date-range-picker.tsx && pnpm exec tsc --noEmit 2>&1 | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `components/sf/sf-date-range-picker.tsx` exists and starts with `"use client";` directive on line 1
    - File contains exactly one `export function SFDateRangePicker` declaration
    - File exports the literal types `export type DateRange`, `export interface SFDateRangePreset`, `export interface SFDateRangePickerProps`
    - **DR-01 range mode:** File contains all three classNames keys — `grep -E "range_start|range_middle|range_end" components/sf/sf-date-range-picker.tsx | wc -l` ≥ 3 (75-VALIDATION DR-01)
    - **DR-01 token grep:** File applies SF token classes to selected/range states — `grep -cE "\bbg-primary text-primary-foreground\b" components/sf/sf-date-range-picker.tsx` ≥ 1 (75-VALIDATION DR-01)
    - **DR-02 read-only trigger:** File contains `readOnly` AND `aria-haspopup="dialog"` on the SFInput trigger — `grep -cE 'readOnly|aria-haspopup="dialog"' components/sf/sf-date-range-picker.tsx` ≥ 1 (75-VALIDATION DR-02)
    - **DR-02 composition partners:** File composes all three partners — `grep -E "SFCalendarLazy|SFPopover|SFInput" components/sf/sf-date-range-picker.tsx | wc -l` ≥ 3 (75-VALIDATION DR-02)
    - **DR-02 SSR-safe new Date():** Every `new Date()` occurrence in the file is structurally inside a `useMemo` callback OR a function-body that only runs in event handlers — verified by `grep -n 'new Date()' components/sf/sf-date-range-picker.tsx` returning hits ONLY inside useMemo (no module-scope, no top-of-component-body); for this implementation: exactly one `new Date()` inside `useMemo(() => new Date(), [])` (75-VALIDATION DR-02)
    - **DR-02 bounds wired:** `grep -c 'fromDate' components/sf/sf-date-range-picker.tsx` ≥ 1 AND `grep -c 'toDate' components/sf/sf-date-range-picker.tsx` ≥ 1 AND `grep -c 'disabled' components/sf/sf-date-range-picker.tsx` ≥ 2
    - **DR-03 presets API:** File contains presets prop typed as SFDateRangePreset[] — `grep -cE 'presets\??:\s*SFDateRangePreset' components/sf/sf-date-range-picker.tsx` ≥ 1 (75-VALIDATION DR-03 type grep)
    - **DR-03 lazy thunk:** File contains the `getValue: () =>` thunk pattern — `grep -cE 'getValue:\s*\(\)\s*=>' components/sf/sf-date-range-picker.tsx` ≥ 1 (75-VALIDATION DR-03)
    - **DR-03 presets render as list:** `grep -c 'role="list"' components/sf/sf-date-range-picker.tsx` ≥ 1 (75-VALIDATION DR-03)
    - **DR-03 Locale type-only import:** `grep -cE "^import type \{ Locale \} from 'date-fns/locale'" components/sf/sf-date-range-picker.tsx` ≥ 1 AND no runtime date-fns import — `grep -cE "^import \{[^}]*\} from 'date-fns" components/sf/sf-date-range-picker.tsx` = 0 (75-VALIDATION DR-05 type-only-import predicates)
    - **DR-04 withTime prop:** `grep -cE 'withTime\??:\s*boolean' components/sf/sf-date-range-picker.tsx` ≥ 1 (75-VALIDATION DR-04)
    - **DR-04 inline time inputs:** `grep -c 'type="time"' components/sf/sf-date-range-picker.tsx` ≥ 2 (75-VALIDATION DR-04 — two SFInput type="time" in withTime branch)
    - **DR-04 no SFTimePicker:** `grep -c 'SFTimePicker' components/sf/sf-date-range-picker.tsx` = 0 (75-VALIDATION DR-04 + D-06 lock)
    - **DR-05 SFCalendarLazy composed (not SFCalendar):** `grep -cE "from ['\"]@/components/sf/sf-calendar-lazy['\"]" components/sf/sf-date-range-picker.tsx` ≥ 1 AND `grep -cE "from ['\"]@/components/sf/sf-calendar['\"]" components/sf/sf-date-range-picker.tsx` = 0 (T-75-11 mitigation; D-02 lock)
    - **DR-05 no direct react-day-picker import:** `grep -cE "from 'react-day-picker'" components/sf/sf-date-range-picker.tsx` = 0
    - **DR-06 no stylesheet import:** `grep -c 'react-day-picker/dist' components/sf/sf-date-range-picker.tsx` = 0 (75-VALIDATION DR-06)
    - **DR-06 rounded-none on every classNames entry:** `grep -c 'rounded-none' components/sf/sf-date-range-picker.tsx` ≥ 4 (75-VALIDATION DR-06)
    - File uses ONLY blessed-stop spacing tokens — `grep -oE "var\(--sfx-space-[0-9]+\)" components/sf/sf-date-range-picker.tsx | sort -u` outputs only entries from {space-1, space-2, space-3} (4 / 8 / 12 px)
    - File contains ZERO non-`rounded-none` rounded-* classes — `grep -E "rounded-(sm|md|lg|xl|2xl|full)" components/sf/sf-date-range-picker.tsx` returns 0 matches
    - File contains ZERO hex literals — `grep -qE "#[0-9a-fA-F]{6}" components/sf/sf-date-range-picker.tsx` returns false
    - File contains ZERO raw-HTML inject prop usages — `grep -c 'dangerously' components/sf/sf-date-range-picker.tsx` returns 0 (T-75-01 mitigation)
    - File line count ≥ 220 — `wc -l components/sf/sf-date-range-picker.tsx` ≥ 220
    - File ends with the Pattern C contract reminder comment block — `grep -q "Pattern C contract reminder" components/sf/sf-date-range-picker.tsx`
    - File contains the literal substring `ANTI-FEATURES` in the JSDoc — anti-features block enumerated verbatim with ≥ 7 entries
    - File contains `data-testid="sf-date-range-picker-trigger"` on the SFInput trigger (Plan 02 selectors depend on this)
    - File contains `data-testid="sf-date-range-picker-content"` on SFPopoverContent
    - File contains `data-testid="sf-date-range-picker-time-row"` on the withTime container
    - File contains `data-testid="sf-date-range-picker-presets"` on the presets `<ul>`
    - `pnpm exec tsc --noEmit` returns 0 errors
    - `git status --porcelain package.json` shows ZERO additions to dependencies (DR-05 zero-new-deps; T-75-09 mitigation)
    - `git log -1 --format=%s -- components/sf/sf-date-range-picker.tsx` matches pattern `Feat\(75-01\):.*SFDateRangePicker`
  </acceptance_criteria>
  <done>
    SFDateRangePicker component lands. Read-only SFInput trigger opens SFPopoverContent with SFCalendarLazy + optional left-rail presets + optional withTime row. Range/single mode work. fromDate/toDate/disabled flow to the calendar. Presets call getValue() thunks only on click and close the popover. withTime renders two inline `<SFInput type="time">`. Locale is type-only — zero runtime date-fns. new Date() only inside useMemo. classNames carry rounded-none on every key + bg-primary text-primary-foreground for selected/range_start/range_end + bg-primary/20 for range_middle. Zero stylesheet import. Zero new runtime deps. Pattern C contract reminder block at file bottom.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add SFDateRangePicker barrel export + same-commit registry entries (DR-05 / REG-01)</name>
  <files>components/sf/index.ts, public/r/sf-date-range-picker.json, public/r/registry.json</files>
  <read_first>
    - components/sf/index.ts (current barrel state — verify SFDateRangePicker NOT yet exported; SFFileUpload at line 134 is the natural neighbor for adjacency)
    - components/sf/sf-date-range-picker.tsx (Task 1 output — confirms SFDateRangePicker + DateRange + SFDateRangePreset + SFDateRangePickerProps exports exist)
    - public/r/registry.json (verify items[] count is currently 56 from Phase 74 close — `grep -c '"name":' public/r/registry.json` → 56; sf-date-range-picker NOT yet present)
    - public/r/sf-file-upload.json (Pattern C standalone registry-item shape — meta.layer=frame, meta.pattern=C, registryDependencies array)
    - public/r/sf-combobox.json (additional Pattern C standalone reference)
    - .planning/phases/74-sffileupload/74-01-PLAN.md Task 2 (barrel + registry insertion pattern; same-commit cohort precedent)
    - .planning/phases/75-sfdaterangepicker/75-VALIDATION.md DR-06 barrel grep predicate (verbatim regex this task must satisfy)
  </read_first>
  <action>
    Three files modified in this task. Land them in a SINGLE commit (REG-01 same-commit rule — Pattern C ships barrel export + registry entry + standalone JSON together).

    (1) Edit `components/sf/index.ts`. Insert the SFDateRangePicker export immediately AFTER the existing `export { SFFileUpload, type SFFileEntry, type SFFileUploadProps } from "./sf-file-upload";` line (currently line 134). The natural location keeps file-upload + date-range-picker adjacent (both Pattern C v1.10 components). Insert exactly:

        export {
          SFDateRangePicker,
          type DateRange,
          type SFDateRangePreset,
          type SFDateRangePickerProps,
        } from "./sf-date-range-picker";

    Do NOT modify any other line of `components/sf/index.ts`. Do NOT add/remove any directive (barrel must remain directive-free per v1.3 carry-forward rule). The barrel grep `grep -c "'use client'" components/sf/index.ts` MUST remain 0.

    Do NOT modify `next.config.ts`. SFDateRangePicker has zero new deps — D-04 chunk-id stability lock holds. `react-day-picker` is ALREADY in optimizePackageImports (verified next.config.ts:40); no addition is needed because SFDateRangePicker composes SFCalendarLazy which already inherits the lazy boundary.

    (2) Create `public/r/sf-date-range-picker.json` (standalone registry-item file, mirrors `sf-file-upload.json` shape):

        {
          "$schema": "https://ui.shadcn.com/schema/registry-item.json",
          "name": "sf-date-range-picker",
          "title": "SF Date Range Picker",
          "description": "Industrial date range picker — read-only SFInput trigger + SFPopover panel + SFCalendarLazy (P3 lazy boundary inherited; react-day-picker stays out of homepage First Load JS) + optional left-rail presets + optional inline time inputs. classNames-only styling with rounded-none + bg-primary/text-primary-foreground for selected. Zero new runtime deps. Locale is type-only.",
          "type": "registry:component",
          "registryDependencies": ["calendar", "popover", "input", "button"],
          "meta": {
            "layer": "frame",
            "pattern": "C"
          },
          "files": [
            {
              "path": "components/sf/sf-date-range-picker.tsx",
              "type": "registry:component"
            }
          ]
        }

    Mirror `public/r/sf-file-upload.json` for the exact JSON-key ordering. If `sf-file-upload.json` uses different ordering (alphabetic, etc.), match it — same-shape rule for registry items.

    (3) Edit `public/r/registry.json`. Append a new entry to the `items[]` array directly AFTER the existing `sf-file-upload` entry. The new entry shape (mirror the sf-file-upload entry exactly for shape; only diff the name/title/description/registryDependencies/files path):

        {
          "name": "sf-date-range-picker",
          "title": "SF Date Range Picker",
          "description": "Industrial date range picker — read-only SFInput trigger + SFPopover panel + SFCalendarLazy + optional presets + optional inline time inputs. Pattern C zero-deps composition.",
          "type": "registry:component",
          "registryDependencies": ["calendar", "popover", "input", "button"],
          "meta": {
            "layer": "frame",
            "pattern": "C"
          },
          "files": [
            {
              "path": "components/sf/sf-date-range-picker.tsx",
              "type": "registry:component"
            }
          ]
        }

    The exact JSON shape MUST match the existing items[] entries for `sf-file-upload` and `sf-combobox`. Run `jq '.items | length' public/r/registry.json` BEFORE the edit (expect 56) and AFTER the edit (expect 57); if AFTER ≠ BEFORE + 1, STOP and inspect.

    Run `pnpm exec tsc --noEmit` to confirm the barrel re-export compiles (Task 1's component file MUST already exist on disk).

    Stage all three files together and commit ONCE:

        git add components/sf/index.ts public/r/sf-date-range-picker.json public/r/registry.json
        git commit -m "Feat(75-01): SFDateRangePicker barrel export + registry entries (DR-05 + REG-01 same-commit)"

    Verify the commit cohort:

        git show HEAD --stat | grep -E "components/sf/index.ts|public/r/sf-date-range-picker.json|public/r/registry.json" | wc -l
        # MUST output 3 (all three files in the same commit)
  </action>
  <verify>
    <automated>[ "$(grep -cE "(export \{[^}]*SFDateRangePicker[^}]*\}|export \* from) ['\\\"]\\./sf-date-range-picker['\\\"]" components/sf/index.ts)" -ge 1 ] && [ "$(grep -c "'use client'" components/sf/index.ts)" = "0" ] && test -f public/r/sf-date-range-picker.json && grep -q '"name": "sf-date-range-picker"' public/r/sf-date-range-picker.json && grep -q '"meta"' public/r/sf-date-range-picker.json && grep -q '"layer": "frame"' public/r/sf-date-range-picker.json && grep -q '"pattern": "C"' public/r/sf-date-range-picker.json && grep -q '"name": "sf-date-range-picker"' public/r/registry.json && [ "$(grep -c '"name":' public/r/registry.json)" = "57" ] && pnpm exec tsc --noEmit 2>&1 | head -10 && [ "$(git show HEAD --stat 2>/dev/null | grep -cE "components/sf/index.ts|public/r/sf-date-range-picker.json|public/r/registry.json")" = "3" ]</automated>
  </verify>
  <acceptance_criteria>
    - `components/sf/index.ts` contains the SFDateRangePicker export — `grep -cE "(export \{[^}]*SFDateRangePicker[^}]*\}|export \* from) ['\"]./sf-date-range-picker['\"]" components/sf/index.ts` ≥ 1 (75-VALIDATION DR-06 barrel grep)
    - Barrel remains directive-free — `grep -c "'use client'" components/sf/index.ts` = 0 (v1.3 carry-forward rule; 75-VALIDATION DR-06 indirect)
    - Barrel re-exports the type aliases — `grep -c 'type DateRange' components/sf/index.ts` ≥ 1 AND `grep -c 'type SFDateRangePreset' components/sf/index.ts` ≥ 1 AND `grep -c 'type SFDateRangePickerProps' components/sf/index.ts` ≥ 1
    - `public/r/sf-date-range-picker.json` exists with `"name": "sf-date-range-picker"` AND `"meta": { "layer": "frame", "pattern": "C" }`
    - `public/r/registry.json` contains an items[] entry with `"name": "sf-date-range-picker"` — `grep -c '"name": "sf-date-range-picker"' public/r/registry.json` ≥ 1
    - `public/r/registry.json` items[] count is exactly 57 — `grep -c '"name":' public/r/registry.json` = 57 (was 56 from Phase 74 close; +1 for sf-date-range-picker)
    - `next.config.ts` UNCHANGED — `git diff HEAD -- next.config.ts` is empty (D-04 chunk-id stability lock; no optimizePackageImports addition)
    - `package.json` UNCHANGED — `git diff HEAD -- package.json` is empty (DR-05 zero-new-deps)
    - `pnpm exec tsc --noEmit` returns 0 errors
    - All three files land in a SINGLE commit — `git show HEAD --stat | grep -cE "components/sf/index.ts|public/r/sf-date-range-picker.json|public/r/registry.json"` = 3 (REG-01 same-commit rule; T-75-10 mitigation)
    - Commit message matches `Feat\(75-01\):.*barrel.*REG-01` (or equivalent same-commit cohort prefix)
  </acceptance_criteria>
  <done>
    SFDateRangePicker is barrel-exported from `components/sf/index.ts` (Pattern C). Standalone registry-item file `public/r/sf-date-range-picker.json` lands. New entry appended to `public/r/registry.json` items[] (count 56 → 57). All three files committed together (REG-01 same-commit rule). next.config.ts and package.json unchanged. Barrel remains directive-free.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Author showcase fixture at app/showcase/date-range-picker/page.tsx (Wave 0; enables Plan 02 hydration spec + DCE chunk-grep)</name>
  <files>app/showcase/date-range-picker/page.tsx</files>
  <read_first>
    - app/showcase/date-range-picker/page.tsx (verify file does NOT yet exist via `git status --porcelain app/showcase/date-range-picker/page.tsx` — STOP if any output)
    - app/dev-playground/sf-file-upload/page.tsx (Phase 74 fixture precedent — `'use client'` page with multiple labelled sections, testid anchors, simple state management with useState; mirror the section structure for SFDateRangePicker)
    - app/dev-playground/sf-combobox/page.tsx (Phase 72 fixture precedent — Pattern C consumer route, verifying barrel-import resolves)
    - components/sf/sf-date-range-picker.tsx (Task 1 output — confirms exported component name + DateRange/SFDateRangePreset types + the testid anchors that Plan 02 selectors depend on: sf-date-range-picker-trigger, sf-date-range-picker-content, sf-date-range-picker-presets, sf-date-range-picker-time-row)
    - .planning/phases/75-sfdaterangepicker/75-VALIDATION.md §Wave 0 Requirements (showcase route MUST exist) AND DR-06 chunk-grep predicate (the route path `app/showcase/date-range-picker` MUST contain the consumer chunks for the DCE proof)
    - CLAUDE.md (FRAME/SIGNAL register; SF-prefix barrel; OKLCH-only; zero border-radius)
    - package.json (verify date-fns available — Task 1's preset thunks in this fixture import `subDays` + `startOfMonth` from date-fns to demonstrate the consumer-owns-date-fns pattern)
  </read_first>
  <action>
    Run `git status --porcelain app/showcase/date-range-picker/page.tsx` first. If output, STOP — worktree leakage. Verify the parent directory does NOT exist: `ls app/showcase 2>&1` (expected: "No such file or directory" — this task creates the entire `app/showcase/` directory tree as part of the fixture authoring).

    The route exists at a DELIBERATELY new path `app/showcase/` (not `app/dev-playground/`) because:
    - 75-VALIDATION.md Wave 0 + DR-06 chunk-grep predicate authoritatively reference `app/showcase/date-range-picker/`
    - Phase 75 establishes the showcase pattern as the v1.10 successor to dev-playground for components that need DCE-proof verification (Plan 02 chunk-grep proves react-day-picker IS in `app/showcase/date-range-picker` chunks but ABSENT from homepage chunks)
    - The new directory creation is deliberate — Next.js compiles each app-route subtree to its own chunks (verifiable via `.next/static/chunks/app/showcase/date-range-picker/`)

    Create `app/showcase/date-range-picker/page.tsx`. Structure (top to bottom):

    1. `"use client";` directive on line 1 (the page consumes useState and the SFDateRangePicker which is a Client Component).

    2. Imports — barrel + SFDateRangePicker + date-fns helpers for preset thunks:

        import { useState } from "react";
        import { subDays, startOfMonth } from "date-fns";
        import {
          SFDateRangePicker,
          type DateRange,
          type SFDateRangePreset,
        } from "@/components/sf";

       Note: `date-fns` is imported HERE (in the consumer fixture) — NOT in the SFDateRangePicker component file (where it MUST remain type-only per DR-03). Consumer routes are allowed to use date-fns at runtime; this is the documented contract.

    3. Module-level constants (presets — use the lazy thunk pattern; new Date() runs ONLY when the user clicks a preset):

        const PRESETS: SFDateRangePreset[] = [
          {
            label: "LAST 7 DAYS",
            getValue: () => ({ from: subDays(new Date(), 7), to: new Date() }),
          },
          {
            label: "LAST 30 DAYS",
            getValue: () => ({ from: subDays(new Date(), 30), to: new Date() }),
          },
          {
            label: "THIS MONTH",
            getValue: () => ({ from: startOfMonth(new Date()), to: new Date() }),
          },
        ];

    4. Page default export — three labelled sections, each wrapped in a `<section data-testid="fixture-{name}">` for Plan 02 selectors:

        export default function ShowcaseDateRangePickerPage() {
          // Section 1: uncontrolled-feeling (controlled, but consumer logs only)
          const [section1Range, setSection1Range] = useState<DateRange | undefined>({
            from: undefined,
            to: undefined,
          });

          // Section 2: controlled with presets — JSON echo of selected range
          const [section2Range, setSection2Range] = useState<DateRange | undefined>({
            from: undefined,
            to: undefined,
          });

          // Section 3: withTime mode
          const [section3Range, setSection3Range] = useState<DateRange | undefined>({
            from: undefined,
            to: undefined,
          });
          const [startTime, setStartTime] = useState<string>("09:00");
          const [endTime, setEndTime] = useState<string>("17:00");

          return (
            <main className="min-h-screen p-[var(--sfx-space-12)] flex flex-col gap-[var(--sfx-space-12)] bg-background text-foreground">
              <header>
                <h1 className="font-mono uppercase tracking-wider text-2xl">
                  SFDateRangePicker // Showcase
                </h1>
                <p className="font-mono text-xs text-muted-foreground mt-[var(--sfx-space-2)]">
                  Pattern C // Phase 75 // DR-01..DR-06 + TST-03
                </p>
              </header>

              <section
                data-testid="fixture-uncontrolled-range"
                className="flex flex-col gap-[var(--sfx-space-3)]"
              >
                <h2 className="font-mono uppercase tracking-wider text-sm">
                  Section 1 // Range Mode (Default)
                </h2>
                <SFDateRangePicker
                  mode="range"
                  value={section1Range}
                  onValueChange={setSection1Range}
                />
                <pre
                  data-testid="fixture-section1-output"
                  className="font-mono text-xs border-2 border-border p-[var(--sfx-space-2)] bg-muted"
                >
                  {JSON.stringify(
                    {
                      from: section1Range?.from?.toISOString() ?? null,
                      to: section1Range?.to?.toISOString() ?? null,
                    },
                    null,
                    2
                  )}
                </pre>
              </section>

              <section
                data-testid="fixture-controlled-presets"
                className="flex flex-col gap-[var(--sfx-space-3)]"
              >
                <h2 className="font-mono uppercase tracking-wider text-sm">
                  Section 2 // Controlled + Presets
                </h2>
                <SFDateRangePicker
                  mode="range"
                  value={section2Range}
                  onValueChange={setSection2Range}
                  presets={PRESETS}
                />
                <pre
                  data-testid="fixture-section2-output"
                  className="font-mono text-xs border-2 border-border p-[var(--sfx-space-2)] bg-muted"
                >
                  {JSON.stringify(
                    {
                      from: section2Range?.from?.toISOString() ?? null,
                      to: section2Range?.to?.toISOString() ?? null,
                    },
                    null,
                    2
                  )}
                </pre>
              </section>

              <section
                data-testid="fixture-withtime"
                className="flex flex-col gap-[var(--sfx-space-3)]"
              >
                <h2 className="font-mono uppercase tracking-wider text-sm">
                  Section 3 // With Time
                </h2>
                <SFDateRangePicker
                  mode="range"
                  value={section3Range}
                  onValueChange={setSection3Range}
                  withTime
                  startTime={startTime}
                  endTime={endTime}
                  onStartTimeChange={setStartTime}
                  onEndTimeChange={setEndTime}
                />
                <pre
                  data-testid="fixture-section3-output"
                  className="font-mono text-xs border-2 border-border p-[var(--sfx-space-2)] bg-muted"
                >
                  {JSON.stringify(
                    {
                      from: section3Range?.from?.toISOString() ?? null,
                      to: section3Range?.to?.toISOString() ?? null,
                      startTime,
                      endTime,
                    },
                    null,
                    2
                  )}
                </pre>
              </section>
            </main>
          );
        }

    Spacing values used: `--sfx-space-2` (8 px), `--sfx-space-3` (12 px), `--sfx-space-12` (96 px). All blessed stops.

    Color tokens: `bg-background`, `text-foreground`, `text-muted-foreground`, `bg-muted`, `border-border`. All slot tokens.

    Border-radius: ZERO `rounded-*` classes other than `rounded-none` (n/a here — no rounded utilities used at all).

    Run `pnpm exec tsc --noEmit` and confirm 0 errors. Run `pnpm dev` briefly OR `pnpm build` to confirm the route resolves (catches any barrel-export typo).

    Commit: `Feat(75-01): SFDateRangePicker showcase fixture (3 sections — uncontrolled, controlled+presets, withTime)`
  </action>
  <verify>
    <automated>test -f app/showcase/date-range-picker/page.tsx && grep -q "^\"use client\";" app/showcase/date-range-picker/page.tsx && grep -q "export default function" app/showcase/date-range-picker/page.tsx && grep -q "SFDateRangePicker" app/showcase/date-range-picker/page.tsx && [ "$(grep -cE "from ['\\\"]@/components/sf['\\\"]" app/showcase/date-range-picker/page.tsx)" -ge 1 ] && grep -q "fixture-uncontrolled-range" app/showcase/date-range-picker/page.tsx && grep -q "fixture-controlled-presets" app/showcase/date-range-picker/page.tsx && grep -q "fixture-withtime" app/showcase/date-range-picker/page.tsx && grep -q "presets={PRESETS}" app/showcase/date-range-picker/page.tsx && grep -q "withTime" app/showcase/date-range-picker/page.tsx && grep -q "subDays\|startOfMonth" app/showcase/date-range-picker/page.tsx && [ "$(grep -c 'data-testid' app/showcase/date-range-picker/page.tsx)" -ge 6 ] && pnpm exec tsc --noEmit 2>&1 | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `app/showcase/date-range-picker/page.tsx` exists and starts with `"use client";` directive on line 1
    - File contains `export default function` (Next.js page convention)
    - File imports SFDateRangePicker + DateRange + SFDateRangePreset from `@/components/sf` barrel — `grep -cE "from ['\"]@/components/sf['\"]" app/showcase/date-range-picker/page.tsx` ≥ 1
    - File imports `subDays` and `startOfMonth` from `date-fns` (consumer is allowed to import date-fns at runtime; component file is the only one bound by the type-only contract)
    - File contains all three section testids: `fixture-uncontrolled-range`, `fixture-controlled-presets`, `fixture-withtime` — each `grep -q` returns true
    - File contains all three section-output testids: `fixture-section1-output`, `fixture-section2-output`, `fixture-section3-output` (JSON echo for Plan 02 controlled-API verification)
    - File mounts a presets-enabled SFDateRangePicker — `grep -q "presets={PRESETS}" app/showcase/date-range-picker/page.tsx`
    - File mounts a withTime-enabled SFDateRangePicker — `grep -q "withTime" app/showcase/date-range-picker/page.tsx`
    - File contains ≥ 3 `<SFDateRangePicker` mount sites — `grep -c '<SFDateRangePicker' app/showcase/date-range-picker/page.tsx` ≥ 3
    - File line count ≥ 110 — `wc -l app/showcase/date-range-picker/page.tsx` ≥ 110
    - File uses ONLY blessed-stop spacing tokens — `grep -oE "var\(--sfx-space-[0-9]+\)" app/showcase/date-range-picker/page.tsx | sort -u` outputs only entries from {space-2, space-3, space-12} (8 / 12 / 96 px)
    - File contains ZERO non-`rounded-none` rounded-* classes — `grep -E "rounded-(sm|md|lg|xl|2xl|full)" app/showcase/date-range-picker/page.tsx` returns 0 matches
    - File contains ZERO hex literals — `grep -qE "#[0-9a-fA-F]{6}" app/showcase/date-range-picker/page.tsx` returns false
    - `pnpm exec tsc --noEmit` returns 0 errors
    - `git log -1 --format=%s -- app/showcase/date-range-picker/page.tsx` matches pattern `Feat\(75-01\):.*showcase`
  </acceptance_criteria>
  <done>
    Showcase fixture lands at `app/showcase/date-range-picker/page.tsx`. Three sections demonstrate uncontrolled-feeling range mode, controlled mode with presets (date-fns thunks), and withTime mode with start/end time inputs. JSON echo `<pre>` blocks expose state for Plan 02 controlled-API assertions. Plan 02 hydration spec + chunk-grep DCE proof can mount this route.
  </done>
</task>

</tasks>

<verification>
After all 3 tasks complete:

1. **Component + barrel + registry land as Pattern C cohort:**
   - `test -f components/sf/sf-date-range-picker.tsx`
   - `grep -E "(export \{[^}]*SFDateRangePicker[^}]*\}|export \* from) ['\"]./sf-date-range-picker['\"]" components/sf/index.ts` ≥ 1
   - `test -f public/r/sf-date-range-picker.json`
   - `grep -c '"name":' public/r/registry.json` = 57

2. **Showcase fixture mounts the component:**
   - `test -f app/showcase/date-range-picker/page.tsx`
   - `grep -c "<SFDateRangePicker" app/showcase/date-range-picker/page.tsx` ≥ 3

3. **All DR-01..DR-06 structural predicates pass (75-VALIDATION verbatim):**
   - DR-01: `grep -E "range_start|range_middle|range_end" components/sf/sf-date-range-picker.tsx | wc -l` ≥ 3 AND `grep -r "react-day-picker/dist/style.css" components/ app/` returns NOTHING
   - DR-02: `grep -E "SFCalendarLazy|SFPopover|SFInput" components/sf/sf-date-range-picker.tsx | wc -l` ≥ 3 AND `grep -E "readOnly|aria-haspopup" components/sf/sf-date-range-picker.tsx` ≥ 1
   - DR-03: `grep -cE 'getValue:\s*\(\)\s*=>' components/sf/sf-date-range-picker.tsx` ≥ 1 AND `grep -cE "^import type \{ Locale \} from 'date-fns/locale'" components/sf/sf-date-range-picker.tsx` ≥ 1
   - DR-04: `grep -c 'type="time"' components/sf/sf-date-range-picker.tsx` ≥ 2 AND `grep -c 'SFTimePicker' components/sf/sf-date-range-picker.tsx` = 0
   - DR-05: `grep -E "(export \{[^}]*SFDateRangePicker[^}]*\}|export \* from) ['\"]./sf-date-range-picker['\"]" components/sf/index.ts` ≥ 1 AND `grep -c "'use client'" components/sf/index.ts` = 0
   - DR-06: `grep -c 'react-day-picker/dist' components/sf/sf-date-range-picker.tsx` = 0 AND `grep -c 'rounded-none' components/sf/sf-date-range-picker.tsx` ≥ 4 AND `grep -c 'bg-primary text-primary-foreground' components/sf/sf-date-range-picker.tsx` ≥ 1

4. **No new deps, no chunk-id reshuffle:**
   - `git diff HEAD~3 -- package.json` is empty (DR-05)
   - `git diff HEAD~3 -- next.config.ts` is empty (D-04)

5. **Type safety:**
   - `pnpm exec tsc --noEmit` returns 0 errors

6. **Commit cohort discipline:**
   - Task 2 single commit contains all three barrel + standalone + registry files
   - All three task commits prefixed `Feat(75-01):`
</verification>

<success_criteria>
- SFDateRangePicker is a Pattern C primitive composing SFCalendarLazy + SFPopover + SFInput; controlled-only API; SSR-safe; zero stylesheet import; zero runtime date-fns; no SFTimePicker
- All six DR-XX requirement IDs are addressed with structural grep predicates copied verbatim from 75-VALIDATION.md into acceptance criteria
- Component is barrel-exported (Pattern C); registry entries land same-commit as the component file (REG-01); registry items count rises from 56 to 57
- Showcase fixture exists at `app/showcase/date-range-picker/page.tsx` with three sections, ready for Plan 02's hydration spec + DCE chunk-grep
- Zero new runtime npm deps; `next.config.ts` unchanged (D-04 chunk-id stability lock); `package.json` unchanged
- Bundle headroom intact (verified empirically by Plan 02 chunk-grep + budget spec)
- No worktree leakage; all files authored fresh in this plan
</success_criteria>

<output>
After completion, create `.planning/phases/75-sfdaterangepicker/75-01-SUMMARY.md` per the standard summary template, including:
- Files modified (5 listed in frontmatter)
- DR-XX requirement coverage table (DR-01..DR-06 all marked Component-Side Complete; TST-03 Plan 02)
- Pattern C verification status (barrel directive-free; SFCalendarLazy composed not SFCalendar; classNames-only styling)
- next.config.ts and package.json status (UNCHANGED — D-04 + DR-05 honored)
- Registry items count (56 → 57)
- Carry-forward notes for Plan 02 (testid anchors authored; showcase route ready; chunk-grep DCE proof + hydration spec are Plan 02's deliverables)
</output>
